# v77 — 2026-05-25 — Subagent removal: code and procedures

**What shipped.** Scope 0079. The entire subagent layer removed and replaced with procedure docs. Five agent bodies (`plugin/agents/planning.md`, `before-build.md`, `batch-executor.md`, `after-build.md`, `setup.md`) converted to five procedure docs at `plugin/docs/procedures/` — stripped frontmatter and subagent framing, kept all procedural substance. The `plugin/agents/` directory deleted. Stop hook (`stop.py`) deleted and removed from `hooks.json`. PreToolUse matcher narrowed from `Edit|Write|MultiEdit|Task` to `Edit|Write|MultiEdit` — the Task branch and V29 Task-gate removed entirely. Test-confirmation gate reframed: from blocking Task→batch-executor to blocking build-phase file edits when an active batch exists and unconfirmed rows remain. Build completion flow changed: instead of Stop hook auto-chaining to after-build, the build procedure itself directs Claude to read `after-build.md`. Three skill files rewritten (`before-build/SKILL.md`, `build/SKILL.md`, `setup/SKILL.md`) — subagent spawning replaced with procedure-doc reading. `universal-behaviour.md` routing table rewritten — routes point to procedure docs, "Subagent invocation prompts" section replaced with "Procedure docs — how to invoke." All downstream docs updated: VOCABULARY.md, DOC-STRUCTURE.md, INVENTORY.md, Reference manual, crash-course HTML, templates, scripts, hook docstrings. `test_stop.py` deleted; Task-related tests in `test_pre_tool_use.py` removed. Method version V65→V66, plugin version 0.64.0→0.66.0.

**Decisions.** Planning procedure doc accepted at ~220 lines (scope file default: "Acceptable for now"). Procedure docs kept the full procedural detail from agent bodies — no simplification pass; that's a future option. The build procedure's completion path changed from an implicit inter-turn handoff (Stop hook) to an explicit in-procedure directive ("read after-build.md"), which is simpler and doesn't require hook infrastructure. Test-confirmation gate's new active-batch precondition (via `run_parser`) ensures the gate only fires during builds, not during planning or idle sessions.

**Pivots.** None.

**Carried forward.** Nothing.
