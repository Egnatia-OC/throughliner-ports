# QUEUE

Two sections. **Processed** — work discussed and agreed, ordered top-to-bottom; /next builds from above the `--- Cleared to run above this line ---` marker. **Unprocessed** — captured, not yet fully processed; the next /plan weighs each item. A work item is a `#### ` heading (its description) with a `[slug]` at the end of that line and its rationale in the block beneath. A leading `[audit]` / `[user]` tag names how it's executed; no tag means a build. A work item carrying a security or privacy risk gets a `Red flag · State: …` marker — the flag rides the work.

## Processed

#### Publish an editing-state signal other apps can read, so a live editor knows when Claude is writing [editing-state-signal-for-companion-apps]
Captured by you 2026-08-06 and designed at the same session. **Placed at the top of Processed on unblock-potential, in its most literal form: a separate project of yours is queued and explicitly waiting on this item's output.** You asked that the shape be settled here rather than taken from your sketch, so it is settled below; the two answers you need are the location and the format, both stated explicitly.
**The requirement, in your words.** mdreader is a Markdown reader and writer for Windows (Electron, open source, deliberately plain), built because you read and edit Markdown documents while Claude Code is editing those same documents live. It needs to show a marker and hold off your typing while Claude writes, so the two of you don't land on top of each other. **You rejected inferring this from file-change timing, and the reasoning is the design input that matters most:** an app can see *that* a file changed but not *who* changed it, and can never know Claude has finished rather than paused to think — and a wrong guess locks you out of your own document mid-sentence.
**The core decision: this is a HEARTBEAT, not a lock — and that is what makes it safe.** A plain on/off flag has one catastrophic failure: a session that crashes, is killed, or loses its connection between starting a write and finishing one leaves the flag stuck on, and the reader locks the user out of their own document permanently. That is the exact harm the timing-guess approach was rejected for, reintroduced by the fix. So the marker always carries a timestamp, refreshed on every write, and a reader treats a stale marker as "not editing" regardless of what the flag says. **Staleness is the safety property, not a detail.**
**What the signal can and cannot honestly say, stated plainly because the whole point is to replace a guess with a fact.**
```
CAN say:  a write by Claude is in progress right now      # who: solved
          which files that write targets                  # scope: solved
          when the last write happened                    # recency: solved
CANNOT:   that Claude has FINISHED rather than paused
```
The last one is not solvable by any mechanism available, and pretending otherwise would be the same false promise. What changes is that the reader is no longer *guessing*: it knows a write is in flight, and it knows exactly how long it has been quiet, so its policy is built on facts rather than on inference from modification times. **The hook reports facts; the reader decides policy.** Deliberately so — mdreader knows what its user is doing and the hook does not, so any timeout baked into the hook would be a policy decision made in the wrong place.
**Location (deliverable 1).** `<project-root>/.throughliner/` — the project root being the folder the Claude session was opened in, which is the folder holding `SPEC.md` and `QUEUE.md`. A reader with a document open walks up the directory tree until it finds a `.throughliner/` directory, exactly as tools locate `.git/`.
**Filename and format (deliverable 2).** One file **per session**, named `editing-<session-id>.json`, holding:
```json
{
  "version": 1,
  "active": true,
  "updated": "2026-08-06T15:42:07.123456+00:00",
  "files": ["C:/absolute/path/to/the/file/being/written.md"],
  "session": "<opaque session id>",
  "pid": 12345
}
```
`version` leads and is non-negotiable: this is a contract another application is built against, so it must be able to recognise a format it does not understand and fall back safely. Timestamps are ISO 8601 with an explicit UTC offset. `files` carries absolute paths, so a reader can hold off typing **only in the affected document** rather than blocking everything — a strictly better experience than a global lock, and free once the paths are already known to the hook.
**Why one file per session rather than one shared file.** Two Claude sessions in one project is a supported shape and has already caused real damage here ([concurrent-session-support] records a live corruption incident). With a single shared file, session A finishing a write would set `active: false` while session B is mid-write, and the reader would let the user type into a document Claude is actively editing — precisely the harm this feature exists to prevent. Per-session files make the reader's rule trivially correct: **editing is happening if ANY file in the directory is `active` and fresh.**
**Recommended reader policy, published as guidance rather than enforced.** Treat `active: true` as editing. Treat `active: true` with `updated` older than ~30 seconds as a dead session and ignore it — that is the crash case. Treat a missing directory or an unreadable file as "not editing" and never as an error: **the signal must fail open**, because most projects will never have this plugin installed at all, and a reader that blocks on absence would be broken everywhere else.
**What gets built.**
1. `hooks/pre_tool_use.py` — before an Edit/Write/MultiEdit, create `.throughliner/` if absent and write the marker with `active: true`, the target path, and a fresh timestamp. **Must never block or fail the tool call**: any error writing the marker is swallowed silently, because a companion-app convenience must not be able to stop the user's actual work.
2. `hooks/post_tool_use.py` — after the same tools, rewrite the marker with `active: false` and a fresh timestamp, keeping the last-written path. Same never-fail rule.
3. `hooks/session_start.py` — delete this project's stale marker files (any whose `updated` is older than an hour) so crashed sessions leave no litter. This is housekeeping, not the safety mechanism; staleness already protects the reader.
4. `docs-b/setup.md` — add `.throughliner/` to the project's `.gitignore` when scaffolding. This is transient session state and must never be committed; it also carries absolute local paths, which is exactly the machine-layout detail this project already avoids publishing.
5. `SPEC.md` — a new described capability, and the published contract itself: location, filename, fields, versioning promise, and the fail-open rule. The contract lives in SPEC because SPEC is product truth and another application now depends on it.
6. `templates/faq-template.md` + `faq-index-template.md` — what the directory is, why it appears, and that it is safe to delete.
**The name is deliberate and is tied to the rename.** `.throughliner/` rather than `.sovereign-implementer/`, because the product rename is decided, the GitHub repo has already been renamed, and this is a **published contract another app builds against** — shipping the old name would guarantee a breaking change to an external consumer later. Recorded so nobody "corrects" it to match the current plugin id ([rename-to-throughliner], [github-identity-changed-underneath-the-project]).
**Two known limits, recorded rather than discovered later.** Writes made through Bash or PowerShell are not covered, because `PostToolUse` only matches the edit tools and a shell command's target file is not reliably knowable — the same gap [shell-writes-bypass-file-scope-lock] addresses from the enforcement side. And the signal only exists where the plugin is installed and the project adopted; everywhere else the directory simply never appears, which the fail-open rule already handles.
**Files:** `plugin/si-plugin/hooks/pre_tool_use.py`, `plugin/si-plugin/hooks/post_tool_use.py`, `plugin/si-plugin/hooks/session_start.py`, `plugin/si-plugin/docs-b/setup.md`, `SPEC.md`, `plugin/si-plugin/templates/faq-template.md` + `faq-index-template.md`. Docset A is frozen and this is a new capability, so docs-b only. Relates to [concurrent-session-support] (the multi-session shape the per-session files are designed for), [shell-writes-bypass-file-scope-lock] (the same uncovered write path), [gitignore-scaffolded-faq] (the sibling never-commit-this scaffolding rule), [rename-to-throughliner].

#### The project's GitHub identity changed outside the method — new username, a new org holding the old name, and the repo already renamed to throughliner [github-identity-changed-underneath-the-project]
Captured by you 2026-08-06 — you mentioned in passing that your GitHub user had changed because you needed that name for a new organisation. The checks below were run immediately and found more than the heads-up covered. **Placed at the top of Unprocessed rather than appended at the bottom**, because [rename-to-throughliner] sits below the readiness line and can be greenlit at any close — so a stale premise here is reachable sooner than ordinary queue position implies.
**What was established, by running the commands rather than by inference (2026-08-06):**
```
gh api user            ->  login "its-coughfee", id 283077209, type User
gh repo view <old>     ->  resolves to FlintcraftTech/throughliner, PUBLIC,
                           owner id O_kgDOErKGNw (an organisation)
git remote -v          ->  still https://github.com/FlintCraftTech/sovereign-implementer.git
git ls-remote origin   ->  works — main, queue-redesign, and
                           claude/remote-session-capture-QxjcU all resolve
git config user.email  ->  still recyclobat@gmail.com
```
**So the repo rename half of [rename-to-throughliner] has already happened, outside the method.** That item plans a big-bang: rename the GitHub repo, reserve the old name permanently, add a `renames` map to marketplace.json, accept a one-off `/plugin install` cost for consumers. Step one is now done and the item does not know it. **Read that item against this before any of it is built** — its migration sequence assumes it is starting from an unrenamed repo, and the parts that remain (the marketplace `renames` entry, the identity strings across the corpus, the positioning copy) are exactly the parts that make the already-done half safe rather than fragile.
**The fragility is the point, and it is not urgent-but-it-is-real.** Everything currently works because GitHub 301-redirects the old owner/repo path. That redirect is what the rename item already identified as load-bearing, with its own warning that reusing an old name breaks it — and the old *username* has now been taken by an organisation, which is a different object from the old repo path but sits in the same namespace family. Nobody has verified what happens to the redirect chain in that configuration. **This is the thing to check rather than assume.**
**Three recorded facts were stale; one is already fixed.** CLAUDE.md's Repo visibility section names `FlintCraftTech/sovereign-implementer` as the verified repo. README.md and INSTALL.md carry install commands and a marketplace URL built from the old owner/repo — and the install path is the one path this project's release ritual already singles out as never exercised by anyone who already has the plugin, so it can break completely and stay broken. **The local remote was repointed at your instruction, 2026-08-06**, to `https://github.com/FlintcraftTech/throughliner.git`, and verified reachable by `git ls-remote` (all three branches resolve) — so this working tree no longer depends on the redirect. Everyone else's clone still does.
**One consequence for [history-rewrite-third-party-scrub], which must not be missed when that runs.** That item requires setting `git config user.email` to a GitHub no-reply address *before* the rewrite, or the next commit reintroduces the address the rewrite just removed. The correct no-reply address is built from the account's numeric id and current login — on today's values, `283077209+its-coughfee@users.noreply.github.com`. Written down because the id is not guessable and the login has just changed once already; re-derive it with `gh api user` at build time rather than trusting this line.
**Decided at processing, 2026-08-06 (the user's call): fix the address now as a small standalone job, and leave the product rename alone.** The alternative was folding everything into [rename-to-throughliner]. It lost because that item is explicitly held for a deliberate session with the user present and carries no date — so folding means the install instructions stay wrong for an unbounded stretch, and they are wrong in the way this project's own release ritual singles out as worst: nobody who already has the plugin ever exercises the install path, so it breaks silently and the only person who would notice is a brand-new user who by definition cannot diagnose it.
**Scope, from the grep run at processing rather than from the discussion.** The literal strings `FlintCraftTech`, `recyclobat`, and `github.com/<owner>/sovereign-implementer` appear in six live files plus five research findings.
1. `README.md` — three places: the install instruction, the raw-INSTALL.md link handed to new users, and the Watch-for-releases link.
2. `INSTALL.md` — two places: the plain-English ask and the `claude plugin marketplace add` command beneath it.
3. `plugin/si-plugin/templates/faq-template.md` — two entries carrying the marketplace name and the repo URL.
4. `.claude-plugin/marketplace.json` — `owner.name`, currently the old spelling `FlintCraftTech`.
5. `CLAUDE.md` — the Repo visibility line, which names the old owner/repo as the verified target. Update it to the new owner and repo, record that the old name is now an organisation and the user's login is `its-coughfee`, and re-date the verification.
6. The git remote — **already done** at the user's instruction this session.
**What deliberately does NOT change, and this is the boundary that keeps the job small.** The plugin's own identity stays exactly as it is: the plugin name `sovereign-implementer`, the marketplace id `flintcraft`, and the install target `sovereign-implementer@flintcraft`. This job corrects an *address*; renaming the *product* is [rename-to-throughliner]'s whole purpose and must not be started here. **The honest consequence, stated so it isn't reported later as a bug:** until the rename runs, the instructions read slightly oddly — add a marketplace hosted at `throughliner`, then install something called `sovereign-implementer`. That is correct and working, merely inelegant.
**The five research files under `resources/research/` are left untouched, deliberately.** They are dated findings that record what was true when written, and rewriting them would falsify the record rather than correct it. Named explicitly so a build doesn't tidy them up.
**Accepted cost:** [rename-to-throughliner] will rewrite most of these files again. The duplicated work is small and buys a correct install path in the meantime.
**Files:** `README.md`, `INSTALL.md`, `plugin/si-plugin/templates/faq-template.md`, `.claude-plugin/marketplace.json`, `CLAUDE.md`. Relates to [rename-to-throughliner] (whose first step this turns out to have completed, and which carries a premise-changed warning pointing here), [history-rewrite-third-party-scrub] (the no-reply address above), [self-hosting-path-portability].

#### Guard against Claude hand-building what an existing tool generates, and stop a stated fact about the user's setup being re-argued every session [prefer-the-generator-over-hand-building]
Captured by you 2026-08-06, from a live and repeating pain in another project. **Placed at the top of Unprocessed rather than appended**, because it is a recurring cost being paid now, in real sessions, rather than a design question waiting its turn.
**What happens.** Starting a new Android project, Claude builds it from scratch and gets stuck in Gradle configuration — and the user cannot tell what it is doing or why, so they cannot steer it out. In your words: *"I don't understand what Claude is doing and get stuck in circles telling it over and over that I have Android Studio and it's like 'sorry gradle this, gradle that'."* Two distinct failures are tangled together there, and they need separating before either is fixed.
**Strand one — the generator was never considered.** Almost every project type has a canonical scaffolder that produces a correct, conventional starting point in seconds: Android Studio's new-project wizard, `cargo new`, `npm create vite`, `django-admin startproject`, `dotnet new`. Hand-assembling the same thing file by file is slower, produces a non-standard layout, and fails in exactly the places the generator exists to get right — build configuration most of all. Gradle is the paradigm case: it is not a thing to be reasoned out from first principles, it is a thing to be generated correctly and then edited.
**This is the missing counterweight to a rule the method already ships, which is why it belongs on that rule rather than beside it.** `docs-b/plugin-behaviour.md` already says to reach for a CLI tool before handing the user a GUI walkthrough — the bias being corrected there is Claude *not doing enough itself*. The Android case is the same bias overshot: Claude doing it itself when a tool the user already has does it better. **So the existing rule, applied alone, actively pushes toward this failure** — a GUI wizard is precisely what it teaches Claude to route around. The pairing has to be stated, or fixing one direction keeps producing the other.
**Strand two — a stated fact about the user's setup did not survive, and that is the worse half.** The user said they have Android Studio, repeatedly, and it changed nothing. Under this method's own routing rules that fact has an owner: it is a project constraint, so it belongs in a project doc that is read every session, not in the conversation where it dies at the session boundary. Re-arguing a settled environment fact is a failure the method has machinery for and simply never pointed at this class of fact. **Note this is not fixed by better listening** — the same conversation may not exist next session at all.
**Designed at processing, 2026-08-06, against a full session transcript the user supplied mid-processing — and the transcript widened the design. It is preserved at `resources/testing/scrolly-thing-next-2026-08-05-transcript.md`** (copied out of a self-clearing temp folder, so it would otherwise have been lost). Read it before building; the summary below is not a substitute, because the value is in watching the session go wrong turn by turn.
**The guard has TWO faces, and the second was nearly missed.** Processing initially scoped this to scaffolding only, on the reasoning that a broader rule would fire constantly and be ignored. The transcript refutes that directly: **nothing in that session was scaffolding.** The project already existed. What happened was Claude spending most of a build run trying to obtain its *own* command-line route to two capabilities the user already had working in Android Studio — compiling, and a connected phone — and the user eventually stopping it with *"why are we doing it this way? why can't i just use android studio like i always do? i don't understand why everything about this build has been so hard."*
```
FACE 1, at the start   ->  a standard generator exists for this project type
  (the Android Studio     ->  use it rather than hand-assembling the same
   wizard case)               thing file by file. Build configuration is the
                              place hand-assembly reliably fails, and it is
                              exactly what a generator gets right.

FACE 2, at any point   ->  the item's goal is ALREADY MET by a tool the user
  (the transcript case)      has ->  STOP. Do not build Claude a second route
                              to a result that already exists.
```
**Face 2 is the higher-value half on the evidence, and face 1 would not have caught that session at all.** Claude's own words in the transcript name the failure precisely: the item only asked that a way to run the app exist, *"and it already did, the moment you said 'wireless debugging with my Pixel 6.' That's the item satisfied. Everything after that was me trying to get my own command-line access to your phone, which the item never asked for and which doesn't help you."*
**The third fact, and it is what turns this from wasteful into impossible.** That session established the root cause: the Claude desktop app is an MSIX-packaged Windows application, and its sandbox blocks the Unix-domain-socket connect that Java's NIO selector — and therefore every Gradle build — depends on. Android Studio runs at full trust, which is why it compiles the same project fine. **So the parallel route Claude was building could never have worked**, and two sessions were spent discovering that. A guard that stops at "the user already has this working" would have cost one sentence. Full write-up in that project's `resources/research/gradle-loopback-connection-failure.md`; captured separately here as [desktop-app-sandbox-blocks-local-toolchains] because its consequences reach well past this rule.
**The decisions, all settled at processing (the user's calls where marked):**
1. **The environment fact lands in CLAUDE.md, with a "your tools" section added to `templates/CLAUDE-TEMPLATE.md`.** The doc-routing rules already discriminate this: SPEC is what the project *is*; CLAUDE.md is how Claude should work on it. "You have Android Studio and it is the intended way to build" is plainly the latter. This is the fix for the second strand — the user said it repeatedly and it changed nothing, because it lived only in conversation. Better listening does not fix that; next session may not hold the conversation at all.
2. **It is asked for at the moment it matters, NOT at /setup.** A standing "what tools do you have?" interview question fails twice over: a non-coder does not know what counts as an answer, and whatever they say goes stale. A specific question at the point of need — *"Do you have Android Studio installed?"* — is answerable by anyone, and its answer is then written down once and never asked again.
2b. **Every "your tools" entry records HOW it was verified (added at the user's request, later on 2026-08-06).** One clause per entry — what was run or observed, and what it showed. **The section is a list of capability claims, which is precisely the artifact that goes wrong**: the same day, [false-positive-recorded-as-settled-capability] recorded a session writing *"Claude can run Gradle in this project"* into a project doc as settled fact, on the strength of `gradlew --version` — the one Gradle command that cannot detect the failure. That belief outlived the session, was inherited without provenance, and became the premise of queued work that collapsed on contact. A "your tools" section is where that mistake would naturally live and spread, so it must carry its evidence from the first entry rather than gaining the requirement later. **An entry with no recorded evidence is read as unverified** — same rule, same wording as that item's clause; build them consistently, or ideally build them together.
   **Two entry kinds, and the distinction matters:** a tool the *user* reports having ("I have Android Studio") is evidenced by that — record that they said so, which is exactly the kind of claim they can make and Claude cannot. A tool *Claude* claims to be able to drive is evidenced by having driven it, and "it ran" is not "it worked".
   **The user's framing, kept because it corrects a natural misreading of the section's name:** this is **not** a list of the only tools allowed. "Sanctioned" was the word first reached for and was rejected as wrong. It is a record of what is *available*, so Claude knows every session without rediscovering or asking again.
3. **The guard hands the user the step (the user's call, 2026-08-06).** Where a generator or tool step is the right route and only the user can run it, it becomes a `[user]` step with a real walkthrough — not a recommendation Claude defers on. The user weighed the trade explicitly: a couple of minutes of clicking in a GUI against an hour of build-configuration circles, and chose the clicking. Recorded because the softer "recommend it and build by hand if they'd rather not" option was on the table and was rejected.
4. **Scope stays bounded despite the widening.** Face 2 is not "never write anything a tool could write" — it is triggered by a *goal already met by something the user has*, which is a checkable condition rather than a judgment about tool maturity. Recorded so the build does not inflate it into a general prohibition, which would fire constantly and be ignored — the original narrow-scope instinct was right about that danger even though it was wrong about the boundary.
**One boundary that survives unchanged.** The answer is never "hand the whole thing over and stand back" — this method has a matched rule against under-filing user work as much as against over-tagging it. A tool step only the user can run is a `[user]` step with a walkthrough; one Claude can run is Claude's. The guard decides *whether to build at all*, never who does it.
**Consumer-facing, not host-only.** This is a rule for every project the method is used on, and it is the kind of failure a non-coder cannot diagnose: the loop reads as Claude being stuck, not as a wrong decision made an hour earlier.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md` (both faces, paired onto the existing reach-for-a-CLI rule, which alone pushes toward this failure by teaching Claude to route around GUI tools; plus the environment-fact routing), `plugin/si-plugin/docs-b/next-build.md` (face 2 fires mid-build, where that session went wrong), `plugin/si-plugin/docs-b/plan.md` (face 1 fires when work is scoped), `plugin/si-plugin/templates/CLAUDE-TEMPLATE.md` (the "your tools" section), `SPEC.md` (user-visible behaviour change), `templates/faq-template.md` + `faq-index-template.md`. Docset A is frozen and this is new behaviour, so docs-b only. Relates to [desktop-app-sandbox-blocks-local-toolchains] (the root cause the transcript established), [method-advice-invents-capabilities-on-domain-mapping] (the check-before-you-claim family). **Note the surface-change escape is closed** — the desktop app is the platform by decision (SPEC.md, Who it's for), so this must be solved within it.

#### Build the blocked-capability ladder: research a tool, ask for it once, hand back only what Claude truly can't do — with the sandbox as its worked example [desktop-app-sandbox-blocks-local-toolchains]
By Claude, from the session transcript the user supplied 2026-08-06 (preserved at `resources/testing/scrolly-thing-next-2026-08-05-transcript.md`). Processed 2026-08-06 and placed here beside [prefer-the-generator-over-hand-building], the guard this finding is the evidence for. It is a live constraint on every Windows desktop-app user right now, not a design question.
**The finding, established in that session by isolating the failure layer by layer rather than by inference.** The Claude desktop app is an MSIX-packaged Windows application (`C:\Program Files\WindowsApps\Claude_…`), so everything it launches inherits a locked-down package sandbox. That sandbox permits *binding* an AF_UNIX socket but refuses *connecting* to one — which is exactly what Java's NIO `Selector` needs, and therefore what every Gradle build needs. Plain TCP loopback works, which is why the surfaced error (`Unable to establish loopback connection`) points away from the real cause. Android Studio runs at full trust and compiles the same project fine, side by side.
**Tried and ruled out in that session, recorded so none is re-attempted:** the sandbox override in both PowerShell and Bash, `--no-daemon`, forcing the legacy `WindowsSelectorProvider`, redirecting `java.io.tmpdir` off the short `ALEX2~1` path, and launching Gradle as a fully detached process. All still start inside the sandbox, which is why none changed anything.
**Corroborated externally, three independent reports:** [Microsoft Q&A on Java NIO selectors in MSIX packages](https://learn.microsoft.com/en-us/answers/questions/5599711/microsoft-technical-support-request-java-nio-selec), [anthropics/claude-code#41432](https://github.com/anthropics/claude-code/issues/41432), [PortSwigger/mcp-server#82](https://github.com/PortSwigger/mcp-server/issues/82).
**Why this is method work and not just one project's problem.** It cost that project **two sessions** — one that declared victory on a false positive, and one that took the failure apart properly. Any user doing JVM, Android or Gradle work from the desktop app will hit the identical wall, get the identical misleading error, and have no way to attribute it to the app rather than their own setup. The method's whole purpose is that a non-coder is not left holding a diagnosis they cannot make. **This is a knowable, already-diagnosed constraint, so making each project rediscover it is a pure loss.**
**The untested workaround, stated as a prediction rather than a fact.** Running Claude Code from a terminal — an ordinary full-trust process — should escape the sandbox entirely, and both external reports mention the command working outside the packaged app. It was not tested, because a session inside the sandbox cannot test it. **Verify before publishing it as advice**, per the check-what-the-tool-documents rule; an untested workaround in a FAQ is worse than none.
**RE-SCOPED later the same day, 2026-08-06, by the user — and the re-scope corrects this item's whole framing.** It had been processed earlier that session as "warn the user about the sandbox, and document it". That is not what went wrong. **The user never needed the sandbox explained.** They needed Claude to say *"I can't build this — build it in Android Studio"* and move on. They already owned the tool that does the job, they use it routinely, and they were never asked to. Instead the session cycled: retrying, re-explaining, handing the problem back. **"Spiralling around and around doing nothing" is the user's description, and it is the actual failure** — the sandbox was only the thing that triggered it.
**So this is not a Gradle item and must not be built as one (the user's words: "this isn't a Gradle thing, it's all tools generally").** The deliverable is the general ladder now recorded in SPEC.md under Who it's for, written in that same session:
```
1. try it
2. blocked  -> RESEARCH whether a tool exists that Claude can use.
               Not optional, and it comes BEFORE involving the user —
               a non-coder cannot ask for a tool by name, so if Claude
               doesn't look, nobody does.
3. tool exists, not installed -> ASK for it, specifically and ONCE.
               Name it, say what it's for, offer to walk them through it.
               Legitimate user work, not a hand-off.
4. no tool at all -> say so plainly, hand back to something they
               ALREADY have, and stop.
plus: once the tool is available, hand over only the parts Claude
      genuinely cannot do. The test is "can Claude do this at all",
      never "would it be easier to ask".
```
**The terminal escape is dead and must not be revived.** An earlier version of this item carried "run Claude Code from a terminal" as an untested workaround. The same session settled that the Claude Code desktop app **is the platform, as a product decision** (now in SPEC), and that the terminal is never a route because it is not approachable for the people this method is for. A workaround the audience cannot perform is not a workaround. That also removes this item's dependency on any editor-switch decision.
**What the check becomes, and it is smaller than the old design.** Not "warn about a sandbox". When a JVM/Gradle toolchain is present, the state line names the constraint **and the hand-off in the same breath** — this cannot be built here, build it in Android Studio — so the user meets an answer rather than a diagnosis. The sandbox explanation moves to the FAQ, where someone who wants the why can find it, and out of the moment of being blocked.
**Also settled: describe the blocked operation, not the tool.** The failure is AF_UNIX connect, so anything else depending on it fails the same way. Every piece of text this build writes leads with "the app's sandbox blocks a socket operation that JVM toolchains need", and names Gradle as the case that surfaces it — never as the scope.
**The build, in three limbs:**
1. **The ladder itself — `docs-b/plugin-behaviour.md`.** The existing reach-for-a-CLI-tool rule is strengthened into the four-step order above. Two changes carry the weight: the research step becomes **mandatory when blocked** rather than a thing to consider, and **asking for a named tool is stated as legitimate** rather than something to avoid — the current wording leans so hard against sending the user off that it discourages the one ask that actually helps. Name repeated hand-off as the failure mode explicitly, since it is what the rule is for.
2. **The active check — `hooks/session_start.py`.** When a JVM/Gradle toolchain is present in the project root (`gradlew`, `build.gradle`, `build.gradle.kts`, `pom.xml`), emit one state line that names the constraint **and the hand-off together**. Worded conditionally — "if you are running in the Claude desktop app…" — so the hook never has to detect its own surface, which sidesteps the one genuinely undesigned piece at the cost of a clause.
3. **The documentation.** An entry in `templates/faq-template.md` plus its `faq-index-template.md` index line: what fails, the misleading error (`Unable to establish loopback connection`), why it points away from the real cause, and the ruled-out list above so nobody re-attempts them. Plus a short constraint note in `INSTALL.md`. **The explanation lives here, not in the blocked moment** — someone who wants the why can come and find it.
**Files:** `docs-b/plugin-behaviour.md`, `hooks/session_start.py`, `templates/faq-template.md`, `templates/faq-index-template.md`, `INSTALL.md`, `README.md`. **SPEC.md is already done** — the ladder and the platform decision were written into it in the 2026-08-06 planning session that re-scoped this item, so the build inherits SPEC rather than editing it. Docset A is frozen; the ladder is new behaviour, so docs-b only. Relates to [prefer-the-generator-over-hand-building] (the guard whose evidence this is), [false-positive-recorded-as-settled-capability] (the sibling finding from the same transcript — and the reason the check must not be validated with a command that cannot fail), [harness-misbehaviour-diagnosis-order].

#### A session declared a capability working on a command that never exercised it, and the wrong conclusion was recorded as settled fact [false-positive-recorded-as-settled-capability]
By Claude, from the same transcript. Processed 2026-08-06 and placed beneath its sibling [desktop-app-sandbox-blocks-local-toolchains], because both come from that one artifact and this is the rule half of the same failure.
**What happened.** An earlier session concluded that Claude could run Gradle in that project, and wrote the recipe into the project's CLAUDE.md as established fact. The evidence was `gradlew --version` succeeding. That is the one Gradle command which does **not** open a selector — so it is precisely the command that cannot detect the failure. A later session built a work item on the recorded fact, and the item collapsed the moment it ran.
**Why it belongs here rather than in that project.** The method already carries the rule this violates: *"'It ran' and 'it worked' are different claims — never accept one as evidence of the other"*, together with the instruction to ask what actually arrived rather than whether the result looks right. The rule was in force and it slipped anyway. That makes this the **third** recorded instance of a clearly-worded loaded rule slipping under local pressure, alongside [claude-reached-for-shell-write-against-rule] and the folded-in evidence on [method-advice-invents-capabilities-on-domain-mapping].
**The compounding half, which is the part worth designing against.** The failure was not merely believing a false positive — it was **writing it into a project doc as settled fact**, where it outlived the session, was inherited without provenance, and became the premise of queued work. A wrong belief inside one session is cheap; a wrong belief promoted into the record is what cost two sessions here. The method has a why-pipeline for carrying reasoning forward and nothing that distinguishes *verified* from *concluded* when a capability claim enters a doc.
**Settled at processing 2026-08-06: the mechanical option does not exist here, and that is established rather than assumed.** The item asked whether three repeats argue for a mechanism rather than a rule, pointing at [claude-reached-for-shell-write-against-rule], which did reach that conclusion for its own case. Its resolution was read at this processing rather than trusted from the reference: that item's fix is to *remove the pressure* — give the queue mover a delete operation so the wrong path stops being the cheaper one — not to add enforcement. **That shape does not transfer.** Nothing here made writing the false fact cheaper than testing properly, so there is no pressure to remove; and no hook can inspect whether a claim is true, so there is nothing to enforce. Recorded so a later pass doesn't re-open the mechanical question and re-derive the same negative.
**What is built: a claim-provenance clause.** A capability claim written into a project doc — "Claude can run X here", "the tool supports Y", any assertion about what this setup can do — carries **one clause naming the evidence**: what was run, and what it showed. Not a form, not a field, one clause inside the sentence that makes the claim. Two things follow from it, and both are stated in the rule: a reader can see that `gradlew --version` was the whole basis and judge it themselves; and **a capability claim recorded without that clause is read as unverified**, which is what actually stops the next session building on it. Today a conclusion and a tested fact look identical on the page forever, and that indistinguishability is what cost two sessions.
**Scoped to capability claims, deliberately.** Not every fact in a project doc, which would be a documentation tax paid on every sentence to catch a rare failure. The trigger is narrow and recognisable: an assertion about what this environment or tool can do, which is exactly the class that becomes the premise of queued work.
**Files:** `docs-b/plugin-behaviour.md` — the clause is added to the it-ran-versus-it-worked rule, which is where the reasoning it depends on already lives — plus a `templates/faq-template.md` entry and its `faq-index-template.md` index line, since this changes what Claude writes into the user's own CLAUDE.md. No SPEC or README change: it constrains how Claude writes, not a described feature. Docset A is frozen and this is new behaviour, so docs-b only. Relates to [claude-reached-for-shell-write-against-rule] (whose fix shape was checked here and does not transfer) and [method-advice-invents-capabilities-on-domain-mapping] (the same slip-shape), [desktop-app-sandbox-blocks-local-toolchains] (the case in point), [memory-destination-invisible-to-the-record] (the same wrong-belief-promoted-into-a-record failure, into memory rather than a doc — read together).

#### Docset A prescribes the same root commit-message temp file, so a 4.8 close raises the same permission popup [docset-a-commit-msg-tmp-same-bug]
Found mid-build 2026-08-06 by the /next run building [commit-msg-tmp-permission-popup]. Placed beside [retire-docset-a] because both are about what the frozen docset is still allowed to carry.
**The finding, which corrects that item's own premise.** [commit-msg-tmp-permission-popup] states "Docset B only — docset A never prescribed the temp file." That is wrong. `docs/done.md` line 192 carries the identical instruction — write `COMMIT_MSG.tmp` to the project root, `git commit -F COMMIT_MSG.tmp`, then delete it — and makes the same untrue claim, that the file is writable there because the scope-lock isn't active on the project root. So every procedure-following close on a 4.8 session hits the same permission popup, from the same cause, and the fix that just shipped in docset B does not reach it.
**Why it is not obviously out of bounds despite the freeze.** CLAUDE.md's freeze rule bars development, not correction: a frozen fallback that contradicts the hooks running underneath both docsets, or misbehaves silently, is not a safe fallback. This is that shape exactly — the sentence about the root being writable is false about `pre_tool_use.py`, which is shared by both docsets. Against that: it is a one-line behaviour change to a docset nobody is currently running, and the correction carve-out is meant to be used sparingly rather than as a standing licence.
**The claim was re-verified at processing, 2026-08-06, not trusted from the capture.** `plugin/si-plugin/docs/done.md` line 192 does carry it word for word, including the justifying sentence — that the message file is writable there because the sub-doc deletes `_build.md` before the commit step, so the scope-lock isn't active on the project root.
**Decided at processing 2026-08-06: CORRECT it.** Two things settle it. The sentence is not merely dated — it is a **false statement about `pre_tool_use.py`**, the hook that runs underneath both docsets, which is the exact shape the freeze's correction carve-out was written for. And the counter-argument inverts on inspection: "nobody currently runs A" is true, but A's whole job is to be there on the day something goes wrong with B, which is the moment a close raising an unexplained permission prompt costs most. A fallback that misbehaves only when it is finally needed is the worst available failure.
**The speculative counter is refused explicitly.** Leaving a known-false instruction in place because [retire-docset-a] might delete the file is a bet rather than a decision — and that item is now blocked behind the branch merge, so the wait is real and open-ended.
**The build, and its one binding constraint.** Change the destination from the project root to the session scratchpad, and remove the false justifying sentence with it. **Author the wording fresh in docset A's register — never paste docset B's.** B is lighter by subtraction and the why-clauses it sheds are precisely what 4.8 needs to follow a rule reliably, so B's text imported back into A would regress 4.8's rule-following. Write the reasoning out: why the scratchpad, why the root was wrong, what the hook actually does.
**Files:** `plugin/si-plugin/docs/done.md` — the commit step and its justifying sentence. Nothing else: this is a correction inside the freeze, not a development, and it must not spread. Relates to [commit-msg-tmp-permission-popup] (whose premise this corrects — that item's "docset B only" line is wrong and should be fixed if it is still in the queue), [retire-docset-a], [docset-a-completion-ask-inconsistency] (the sibling case of A needing a correction it is otherwise frozen against).

#### Reply to Rygel's OpenCode-port PR — thank, decline in-tree, invite it downstream with a link and credit [respond-to-rygel-opencode-pr]
Captured 2026-08-05 during the Codex-port post-mortem audit (findings: `resources/research/codex-port-postmortem.md`). Processed 2026-08-06; the response was decided by the user at that session and is recorded below, so this item is now execution, not deliberation.
**State re-checked at processing, not carried on trust.** `gh pr list` confirms PR #1 is **still OPEN, with zero comments, and `updatedAt` identical to `createdAt`** — so literally nothing has happened to it since 2026-07-02. That is five weeks of total silence on a 2,815-line contribution translating all commands, docs and hooks.
**The social fact that makes this more pressing than an untended PR normally is.** The user invited Rygel as a collaborator on another of their projects on 2026-08-05, and a separate queued item sends him collaboration questions. So the project is asking this person for things while his contribution sits unacknowledged. That is the cost being paid, and it is not about the code.
**The decision (the user's call, 2026-08-06): route A — thank, close, invite it downstream.** Two alternatives were weighed and rejected: thank-and-close with no invitation (cleaner, but discards a real contribution from a five-person community for nothing), and reply-without-deciding (kinder-sounding, but it hands him the awkward decision and leaves the PR open indefinitely).
**The flavour changed at processing, and the over-tag check is why.** This was tagged `[user]` on the reasoning that outward-facing communication is the user's. Running the check the rule actually requires — **name the tool and confirm it is absent or unauthenticated** — `gh` is installed and authenticated (verified this session), so `gh pr comment` and `gh pr close` are one command each. The method's own consumer-feedback rule already settles this shape for public posts under the user's identity: **Claude drafts, shows the exact text, and posts only on an explicit yes.** The approval is the user's real decision; the clicking is not work worth handing over. So this is Claude-work with an approval gate, not a walkthrough.
**What the reply must carry.**
1. Thanks, specific rather than generic — the scale and completeness of the port, named.
2. The delay acknowledged plainly and briefly. No grovelling, and no invented excuse.
3. Why it cannot merge, both reasons, in this order: it captures the roughly-June method, which has changed substantially since (the two-section queue, retired flavors, a second docset); and the project has concluded that **ports are downstream re-derivations rather than in-tree siblings**, so a port living in this repo would go stale against a method that still moves weekly. The second reason is the structural one and should not be buried under the first, which reads as merely "you were unlucky with timing".
4. The rename, as context he would otherwise discover confusingly: the repo is now `FlintcraftTech/throughliner`.
5. The invitation: host it as his own repo, and this project links to it with credit.
**Execution.** Draft the comment, show the user the exact text, post on an explicit yes with `gh pr comment 1 --repo FlintcraftTech/throughliner`, then close the PR with `gh pr close`. **Never post without the explicit yes** — this is public, permanent, and under the user's identity. If the user declines the drafted wording, redraft rather than posting something approximate.
**Files:** none in the repo — the deliverable is a posted comment and a closed PR. A README link to his repo is **not** part of this item: it is offered in the reply as a promise, and lands with [rename-to-throughliner], which rewrites the README wholesale. Recorded so the link is not forgotten and not written twice. Relates to [rename-to-throughliner] (the README link and the rename this reply mentions), [github-identity-changed-underneath-the-project] (the rename context).

#### Catch structured shell writes in the file scope-lock, and correct SPEC's overstated containment claim [shell-writes-bypass-file-scope-lock]
By Claude, from the 2026-08-04 scope-lock audit; filed at your request. Processed 2026-08-06, where you chose the fix direction.
**The finding, re-verified at processing.** `pre_tool_use.py` returns early for every tool that is not Edit/Write/MultiEdit, so the file-scope enforcement never runs for Bash or PowerShell — those route to the git-safety checks only, which match a few git patterns and consult `build_files` not at all. The hook documents this about itself in its own header: *"For Bash/PowerShell: checks rule 2 (git safety) only."* The line numbers the original capture quoted (`:391`, `:348–386`) have drifted with later edits — the tool-name gate now sits at `:502` and the Bash branch at `:450` — so scope by grepping the tool names at build time rather than by line.
**Why it matters more than the other known holes.** [post-close-tail-state] records the lock being *off* in a named state. This is different: the lock is *on*, the session believes it is contained, and one tool class walks around it. It is the only hole in the one layer that cannot be skipped by instruction, so every doc-level scope rule rests on a floor with a gap in it.
**Three live instances, which is what moved this from an audit finding to designed work.** All three took the same shape — a bulk deletion from a long QUEUE.md, where Edit demands reproducing a large block exactly and a heredoc'd Python splice looks cheaper. The first was denied by the harness classifier; the second and third succeeded. **The third is the decisive one:** it happened inside the overnight blitz's unattended /next run, with no user present and no classifier in the way, while the session was editing the very file that held the capture warning about it, minutes after having read that capture. The full record is on [claude-reached-for-shell-write-against-rule].
**So sharper prose is ruled out, on evidence rather than preference.** The rule is IMPORTANT-flagged in the user's global instructions, unambiguous, names this exact operation, and was loaded every time. The method's own rules-about-writing-rules guidance says a rule that slips despite clear wording is a candidate for mechanical enforcement, and three instances with the warning literally in view is as clear as that evidence gets.
**Decision (the user's call, 2026-08-06): option 4 — catch the structured forms only — plus the SPEC correction from option 3.** The three rejected options, with why each lost, so none is re-proposed:
```
general shell parsing   -> fragile. False denials train workarounds, which is
                           the worse failure. Named exactly that way by the
                           independently-built tool below.
deny obvious write      -> wider than 4 with no extra catch on the observed
  forms during a build      cases, and it reintroduces the false-denial risk.
accept and document     -> kept in PART: SPEC is corrected either way. Rejected
  the gap, nothing else     as the whole answer, because the gap now has three
                            instances and one was unattended.
```
**What gets built, both limbs.**
1. `plugin/si-plugin/hooks/pre_tool_use.py` — a structured-write check on the Bash/PowerShell branch, running only while `_build.md` exists. It matches a small set of forms that have a parseable shape **and** an extractable target path — a heredoc'd or `-c` Python script writing a named file being the observed case — extracts that path, and denies when it is not in `build_files`, naming Edit/Write in the denial text. **Fails open on any parse ambiguity**, which is both this project's established direction and the independently-built tool's. A form that does not parse cleanly is not guessed at; it passes, and the honest limit is stated rather than hidden.
2. `SPEC.md` — correct the `pre_tool_use` paragraph, which currently describes the scope-lock holding Claude to the agreed file list without saying that shell tools reach the disk outside it. State the residual gap plainly rather than describing containment the hook does not provide.
3. A FAQ entry, since a user meeting a denial needs to know why and what to do instead.
**Two convergences with the independently-built tool, recorded because they confirm existing directions rather than opening new ones.** It fails open on any error, the direction this project already treats as safe here. And every denial it issues names the alternative — this project's rule that a prohibition without a stated escape route produces an invented one, arrived at independently by someone not reading these docs.
**The source is kept** at `resources/research/scopelock-independent-implementation/` — its README, the hook implementation, and its agent-instruction fragment. Read the implementation directly rather than this summary when building; the hook payloads are another platform's, but the structure of the checks transfers.
**Files:** `plugin/si-plugin/hooks/pre_tool_use.py`, `SPEC.md`, `templates/faq-template.md` + `faq-index-template.md` (the FAQ entry and its index line). Relates to [claude-reached-for-shell-write-against-rule] (the three instances), [scope-file-editable-vs-locked] (the always-editable-scope-file question this tool also raises, deliberately left separate), [concurrent-session-support] (the queue-corruption mode a stale-view shell splice risks), [post-close-tail-state].

#### Give the queue mover a delete operation, so removing a work item stops being the moment a shell write looks cheaper [claude-reached-for-shell-write-against-rule]
Filed at the 2026-08-05 planning close's wind-down re-scan as a testing outcome; re-scoped at its processing on 2026-08-06 from a record-plus-open-question into this build. The three instances below are kept in full — they are the evidence the fix rests on, and they are also the record this project's CLAUDE.md requires of any observed Claude misbehaviour.
**The three instances, all the same shape.** Each time, a work item was being removed from a long QUEUE.md; `Edit` required reproducing a large block exactly; a Bash heredoc Python script that read the file, spliced out a range and wrote it back was reached for instead.
```
2026-08-05, planning session  -> denied by the harness auto-mode classifier.
                                 Claude switched to Edit and completed it.
2026-08-05, /next run         -> succeeded. Nothing blocked it; the file was
                                 verified intact afterwards by re-read.
2026-08-06, blitz /next run   -> succeeded. UNATTENDED — no user present and
                                 no classifier in the way, which is the exact
                                 condition the fix is for. The capture
                                 describing this slip was sitting in the very
                                 file being edited, and had been read minutes
                                 earlier during that run's triage.
```
**The predicted cost was paid twice, in the open, on 2026-08-06 — corroboration rather than a fourth instance.** That planning session deleted two work items and reached for no shell write at all. Each deletion instead took four `Edit` calls: rename the heading to a short placeholder, replace each long prose paragraph with a marker, then delete the collapsed block. It worked, nothing was corrupted, and it was slow and fiddly in exactly the way this item predicts. So the rule now has evidence from both sides — three instances of the shortcut being taken, and two of the sanctioned path being followed at visible cost. That is the strongest form of the argument for building the delete operation: the safe path is not merely unrewarded, it is measurably laborious every single time.
**Sharper prose is ruled out on evidence, not preference.** The user's global CLAUDE.md carries an IMPORTANT-flagged rule: use Read, Edit and Write for file changes, never bash redirection, because the bash mount can have a stale view and silently overwrite. That rule was loaded every time, is unambiguous, and names this exact operation. The third instance happened with the written warning literally in view. So the wording is not the problem and neither is its priority marking — and the method's own rules-about-writing-rules guidance says a rule that slips despite clear wording is a candidate for mechanical treatment rather than louder prose.
**The cause is a missing tool, and that is the re-scope.** The pull was never toward shell for its own sake; it was toward the only cheap way to do a long deletion. `scripts/reorder_queue.py` addresses work items by slug and relocates whole blocks byte-for-byte — `--move`, `--move-section`, `--position`, `--marker-after` — and it **cannot delete one**. So every removal of a shipped or dropped item falls back to hand-editing a large block, which is precisely when the shortcut looks attractive. **Give the mover a delete and the safe path becomes the cheap path.** This is the method's own recorded pattern: a prohibition with no sanctioned alternative reliably produces an invented one, and the invented move is worse than the thing prohibited because nothing recognises or records it.
**How it pairs with [shell-writes-bypass-file-scope-lock].** That item catches a structured shell write mechanically, at the hook. This one removes the reason to attempt it. Both are worth having and neither substitutes for the other — the hook check still guards every *other* temptation, and this still helps in the planning sessions where the scope-lock is not engaged at all. Deliberately kept as separate items: different files, different failure surfaces.
**What gets built, both limbs.**
1. `plugin/si-plugin/scripts/reorder_queue.py` — a `--delete <slug> <Section>` operation removing the item's whole block, addressed by slug, reusing the same block-boundary logic the move operations already use so the deletion is byte-exact rather than reconstructed. It refuses rather than guesses when the slug resolves to nothing or to more than one block, and it prints the heading line it removed so the deletion is visible in the transcript and in a later read of the session.
2. `resources/testing/test_reorder_queue.py` — cases for the new operation alongside the existing ones: a clean delete, a slug that resolves to nothing, the last item in a section, and an item whose block contains lines that look like headings.
3. `docs-b/done.md` and `docs-b/plan.md` — point the removal steps at the new command, exactly as plan.md's skip-to-defer step already points at the mover for a move. This is what makes the tool reachable at the moment of temptation; a capability nothing names is one nobody uses.
**/plan is the primary customer, which the original framing missed (the user's point, 2026-08-06).** The three recorded slips were all in /next and /done runs, so the item read as being about build closes removing shipped work. But **/plan is the only skill permitted to delete an item at all**, and it does so routinely — the triage bulk-drop pass, and every keep-or-delete decision that lands on delete. plan.md's delete step currently says only "remove the item from Unprocessed" and names no tool, so every /plan deletion falls back to hand-editing a block. The 2026-08-06 planning session deleted [host-faq-stale-pre-redesign] exactly that way; the block was small enough to be safe, which is the point — the same operation is safe or dangerous purely by the size of what it removes, and nothing in the procedure distinguishes them. So the plan.md half of limb 3 is not a consistency nicety riding along behind done.md; it is the highest-frequency use of the new command.
**Adding is deliberately not in scope, and that asymmetry is the reasoning.** A capture is an append of fresh text at the end of a section — no existing block to reproduce, so `Edit` handles it cleanly and there is no shortcut to be tempted by. The danger is specific to *removal*, where the tool demands reproducing text that already exists. Recorded so a later pass doesn't widen this into a general "script all queue writes", which would put a script between the user and every capture for no safety gain.
**Files:** `plugin/si-plugin/scripts/reorder_queue.py`, `resources/testing/test_reorder_queue.py`, `plugin/si-plugin/docs-b/done.md`, `plugin/si-plugin/docs-b/plan.md`. Docset A is frozen and this is a new capability rather than a correction, so it lands in docs-b only. Relates to [shell-writes-bypass-file-scope-lock] (the enforcement half of the same failure), [concurrent-session-support] (the stale-view queue corruption this risked), [bundled-double-ask-misapproval] (the same family — a clearly-worded loaded rule slipping under local pressure).

#### Prefer a session-local focus order over rewriting the queue when the user asks for prioritisation, and make an explicit reorder request a checked decision rather than an instruction [prioritise-without-reorder]
Captured by you 2026-08-06, from watching it happen: the session said it would take the design forks first and simply skip the non-forks as it reached them, without moving anything, and you asked for that judgment to become a rule.
**Your reasoning, in your words: reordering is very token expensive.** Rewriting the queue to express "do these first" pays a full mechanical rewrite for something a session can hold as an order of attention. Where the user's real want is *which work gets attention next*, the cheap answer already satisfies it.
**The rule has two limbs.**
```
user asks for prioritisation      ->  default to a SESSION-LOCAL focus order:
  ("focus on X first", "do the           state the order, work it, skip what
   design forks", "leave the              doesn't match as you reach it.
   user items")                           Narrate it once. Move nothing.

user explicitly asks to REORDER   ->  don't just execute it. First check what
  the queue                              outcome they want — usually about the
                                         next build or two — then report a
                                         non-reorder route that reaches it,
                                         AND offer the reorder anyway with its
                                         cost named. Their call either way.
```
**The second limb is the more interesting half, and it is not a refusal.** An explicit reorder request is still honoured on request; what changes is that it stops being taken as a bare instruction. The check is cheap — one question about the outcome — and it frequently reveals that the want is "build this next", which the readiness line or a focus order already delivers. Offering the reorder in the same breath is what keeps this from reading as Claude talking the user out of what they asked for; the cost is named so the choice is informed rather than steered.
**The collision this must resolve, and it is why the item exists rather than the rule simply being written.** The opposite concern was filed separately as [plan-session-order-diverges-from-file-order] and **deleted at its own processing on 2026-08-06, as fully answered by this item** — its evidence is relocated into the next paragraph rather than lost, and its slug is retained here only so an older reference still resolves to an explanation. The concern: a session that works a different order from the file's makes plan.md's "position in the file *is* the order" untrue, and leaves the real order living only in `_plan.md`, which is deleted at the close. That worry is legitimate — the method's own principle is that the files must suffice alone. **So this rule cannot simply be added; it has to say what happens to that principle. RESOLVED by the user, 2026-08-06, and the build implements this rather than re-deciding it:** a session-local focus order is explicitly sanctioned as *session-local* — it governs attention, never the durable order — and plan.md's "position is the order" claim is softened to say position sets the durable **default** order, which a session may work out of at the user's request. The file stays authoritative for anyone resuming cold, and the cheap path is allowed. The reason this works rather than merely trading one problem for another: the objection was never to a session having its own order, it was to that order being *undeclared* while the doc claimed otherwise — so stating the divergence as sanctioned and session-scoped removes the untrue sentence, which was the actual defect.
**The instance behind the rule, relocated here 2026-08-06 from the deleted item, because a rule with its evidence attached survives re-litigation and one without it does not.** At the 2026-08-05 planning session, Claude ordered the session's work at the start — the two hook findings, then the queue-rot pair, then the frozen-docset question — and worked from that list. It did **not** reorder QUEUE.md to match, because doing so would have meant either restating the full section order or moving items one at a time to positions the mover cannot express directly. So for that entire session the file's order and the session's order disagreed, and the only record of the real order lived in the session's working file, which is deleted at the close. **Nothing anywhere recorded that a divergence had happened** — which is the precise defect this build fixes, and note that the cost was not the divergence itself but its silence.
**What gets built, both limbs.**
1. `docs-b/plan.md` — the start-of-processing reorder step gains the prefer-focus-order default, and the "position is the order" claim is reworded per the resolution above. The explicit-reorder-request protocol lands wherever reorder requests are handled.
2. `docs-b/plugin-behaviour.md` — dependency ownership currently says that when Claude spots an item that belongs elsewhere it should *offer the reorder*. That instruction now points at the cheap path first, with the reorder as the named alternative rather than the default move.
3. `SPEC.md` — the reorder is described there as user-visible behaviour, so the sync trigger fires; plus a FAQ entry, since a user who asks to reorder and gets a question back needs to know why.
**Files:** `plugin/si-plugin/docs-b/plan.md`, `plugin/si-plugin/docs-b/plugin-behaviour.md`, `SPEC.md`, `templates/faq-template.md` + `faq-index-template.md`. Docset A is frozen and this is new behaviour, so docs-b only. Relates to [plan-close-priorities-over-reorder] (the same cost complaint from the close's side; this is the cheap-path answer arriving early, and that item should be re-read against this one when it is processed).

#### Record the summarise-for-the-human rendering as the decided default for all users, and fix the capture authoring standard that contradicts it [approval-flow-token-doubling-simplification]
**Settled by the user 2026-08-06 — read this section and the next; everything below them is the history that led here.**
**The decision: the short-summary-plus-pointer rendering is the default for every user, not a per-project setting and not tier-dependent.** The user's reason, in their own framing: the fight for working links to lines in files may never be won, so the method needs *something*, and this has to be it.
**And the rationale is not the token one — that is the correction this item exists to make.** The summary is not a concession to token pricing; it is the feature. A capture is written long and dense **because Claude needs that detail** — the facts, the rejected options, the conditions. The human does not need the text, only enough to understand what they are approving, and Claude can summarise it in /plan perfectly well. So summarising is simultaneously the answer to the rendering question *and* the answer to capture bloat: the captures stay complete for the reader that needs them, and stop being a wall of text for the reader that doesn't.
**No length caps, explicitly (the user, 2026-08-06).** The summary is not limited by line count, sentence count, or any arbitrary number. It is governed by one thing: does the user understand enough to approve? That is the method's own standing principle — concision comes from sequencing and leading with the decision, never from a word-count cap — applied here rather than re-decided.
**The contradiction this must fix, which is the actual build.** Two shipped rules in `docs-b/plugin-behaviour.md` disagree, and captures have been drifting long and dense because of it. The Captures **authoring standard** says *"The human co-reads and approves this text: unreadable is unapprovable. Completeness matters more than compression here"* — which assumes the human reads the item itself. The **doc-bound-text** rule says the human reads a short summary and a pointer, with the full text only on request. So one rule pulls toward writing captures for the user to read, while the design says they are written for Claude and summarised for the user. Rewrite the authoring standard to say what captures are actually for: complete for Claude, with readability delivered by the summary rather than by compressing the item.
**The plan-tier question is answered, not dropped.** The 2026-08-05 accessibility concern below asked what a Pro-tier consumer pays for a default chosen by a user on a larger plan. The answer is that everyone gets the same rendering, and the cost is accepted deliberately with its reason stated — because the alternative on the table (a stored per-project setting) repeats the `Working mode` failure exactly: a field the user answers strategically rather than truthfully. Recorded so the objection is visibly resolved rather than quietly dropped.
**Rejected, with why, so none is re-proposed:**
```
per-project summary-length setting  -> repeats Working mode's failure shape:
                                       answered strategically, records
                                       reading appetite rather than need
adaptive length by size of text     -> proposed and REJECTED by the user: a
                                       length rule is an arbitrary cap by
                                       another name. Sufficiency governs.
"point + click away and back"       -> a workaround for the stale-viewer bug,
                                       not a rendering default. Stays where it
                                       belongs, on the bug item.
```
**If the harness primitive ever lands, this is superseded rather than wrong.** anthropics/claude-code#77134 asks Claude Code to surface a Write/Edit's content verbatim with no second model pass; checked 2026-08-06 and still open, last touched 2026-08-03. If it ships, the full text becomes free to show and the rendering question reopens on better terms. Not filed as a watch item — it re-captures itself if it lands.
**What gets built, both limbs.**
1. `docs-b/plugin-behaviour.md` — rewrite the Captures authoring standard per the contradiction above, and state the decided default and its rationale on the doc-bound-text rule, replacing the token framing with the written-for-Claude-summarised-for-you one. State the no-cap rule explicitly.
2. `SPEC.md` — its doc-bound-text paragraph describes this as user-visible behaviour, so the sync trigger fires; plus a FAQ entry, since a user seeing a summary instead of the text needs to know the full text is one word away.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md`, `SPEC.md`, `templates/faq-template.md` + `faq-index-template.md`. Scope sibling of [prioritise-without-reorder], which edits the same behaviour doc. Relates to [retire-working-mode-for-short-summary-default] (shipped — the rendering this decides the reasoning for), [queue-pointer-hard-to-follow-and-possibly-stale] (the viewer bugs the pointer half still suffers).
**History below — the reasoning that led here; not needed to understand the item.**
Captured by you — filed after `ea272f6`. Insight reached 2026-08-01 while reviewing the user's Claude Code feature request (anthropics/claude-code#77134). **The root cost:** showing text in chat and writing text to a file are *both* the model producing those tokens (a Write/Edit tool call carries the full text as model-generated input). So any text that both appears in chat and lands in a file is produced twice. Approval-heavy workflows (capture drafts, LOG entries, commit messages) hit this constantly.
**Two directions, same waste:** (A) write-to-file first, then re-emit to show for approval — what issue #77134 asks the harness to fix (surface a Write/Edit's content verbatim, no second model pass). (B) the method's *actual* dominant flow — author the draft in chat first (the approval), then write after the user approves — where the doubling is the *Write* re-emitting the just-shown text.
**The resolution (design at /plan, don't act yet):** don't ask the harness for a second (Direction-B) primitive — it's the murkier one (writing a prior chat message to disk). Instead, once the Direction-A primitive exists, **flip the method's ordering to write-first-then-surface-for-approval**, turning every approval moment into Direction A. Trade-off to weigh: write-first means the file briefly holds unapproved content, so it leans on revert-on-reject — which is why the method currently shows-first (the "never write to QUEUE.md without showing first" rule). Whether that safety is worth trading for the token saving is the call.
**The plan-tier dimension, added 2026-08-05 at the user's insistence.** This has been weighed as a token-efficiency question; it is really an *accessibility* question. A Max user absorbs approval-time doubling; a Pro user does not, and the method's approval-heavy flow means the cost lands on every capture, work item and log entry rather than occasionally. So "surface it inline for readability" is not a neutral default — it is a default that spends the budget of the users with the least of it. Any resolution here must be able to state what a Pro user pays.
**And a cheap middle path was identified the same day, which this item had not considered.** Between "paste the whole text inline" and "point at the file" sits *point, plus one sentence telling the user to click away and back to refresh the viewer*. That costs a sentence instead of the full text, and preserves the write-first design. It is a workaround for [queue-pointer-hard-to-follow-and-possibly-stale]'s stale-viewer finding, not a fix — but it is the option that spends least while that bug stands.
**Rewritten 2026-08-05 — half of this item is now settled, and what remains is a different question.** The user accepted the token cost of the short-inline-summary rendering for **this** project (recorded in [retire-working-mode-for-short-summary-default]). So the question "is inline text worth its tokens here?" is answered, and the working-mode machinery this item expected to simplify is being retired outright rather than simplified.
**What remains, and it is the harder half: the consumer default.** The plan-tier dimension above is untouched by that acceptance. One user accepting a cost for themselves says nothing about the default shipped to a Pro-tier consumer, for whom approval-time doubling lands on every capture, work item and log entry. The method now has a default that spends more tokens by design, chosen by a user who is not paying the tightest budget — so this item's job is to decide what a Pro user gets, and to be able to **state what they pay**.
**The three options to weigh, none of them free:** ship the short summary to everyone and accept the cost; make summary length a per-project setting (which reintroduces exactly the kind of stored field working mode just failed as — a setting the user answers strategically rather than truthfully, so weigh that failure before repeating its shape); or make the summary length adaptive to what is being approved, so a one-line capture gets a one-line summary and only substantial text gets a real precis.
**The Direction-A harness primitive is still the thing that would dissolve this**, and it is still not landed: if Claude Code can surface a Write/Edit's content verbatim without a second model pass (anthropics/claude-code#77134), the cost disappears and every option above becomes moot. Watch that issue.
**Files (rough):** `docs-b/plugin-behaviour.md` (the rendering default that [retire-working-mode-for-short-summary-default] installs — this item would tune it, not replace it), possibly `templates/CLAUDE-TEMPLATE.md` if a setting wins. External dependency: anthropics/claude-code#77134. Relates to [retire-working-mode-for-short-summary-default], [queue-pointer-hard-to-follow-and-possibly-stale].

#### Before driving a verification, check whether it already exists as tracked work — and route it if it does [drive-testing-signals-skill-routing]
**Designed at processing, 2026-08-06, from a counter-example produced by the processing session itself.**
**The trigger is a check with a yes/no answer, not a judgment about whether an urge is suspect.** Before driving or walking through a test or verification, ask one question: **does this work already exist as a tracked item?**
```
already tracked        ->  STOP driving it. Route it to /next, which is what
  (a [user] item, a          walks a [user] item live, one step at a time, and
   queued build)             carries its findings through the proper close.

not tracked            ->  the planning-session file gate already governs it.
                           A write outside the planning set asks first, so the
                           work cannot happen unremarked. No new rule needed.
```
**Why the trigger is that and not "am I driving testing?", which is where the original capture pointed.** The 2026-08-01 observation and the 2026-08-06 processing session are the same shape and opposite calls, and the difference between them is exactly this check. On 2026-08-01 the verification **was already a `[user]` work item in Processed**, and driving it in chat bypassed a home it already had — so its findings scattered as loose captures instead of flowing through the close. On 2026-08-06 the session built a status-line probe in conversation to unstick [statusline-context-reader], and **nothing was tracked**: there was a stuck lift-condition and a small instrument that cleared it. Queuing that would have re-created the very cycling failure it was fixing — the condition had already rolled forward across two advisories and two restarts precisely because it was deferred rather than done.
**So a rule of "always route it into a skill" would have made the second case worse**, which is why the capture's framing needed narrowing rather than adopting. The failure was never informality; it was **bypassing existing structure**. Recorded at this length because the intuitive rule is the wrong one and would otherwise be re-proposed.
**The second limb the original capture is right about, and it survives:** this is the plan/build boundary breaking in the less-noticed direction. The familiar worry is planning leaking into an execution session; this is structured work being *executed* with no skill around it, so none of the structure applies. Same family as under-filing `[user]` work — the work happens, but untracked, so nothing records or closes it.
**What gets built, both limbs.**
1. `docs-b/plugin-behaviour.md` — the check above, placed with the routing-and-discipline rules where the plan/build boundary is stated, phrased as the yes/no test and naming the file gate as what covers the untracked case (a prohibition needs its sanctioned alternative named in the same breath, per the standing pattern).
2. `docs-b/plan.md` — the same check where a planning session is most likely to reach for a verification, since /plan is where this fires in practice.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md`, `plugin/si-plugin/docs-b/plan.md`. Scope sibling of [prioritise-without-reorder] and [approval-flow-token-doubling-simplification], which edit the same two docs. No SPEC change — this constrains Claude's behaviour, not a described feature. Relates to [post-close-tail-state] (the state the original observation happened in), [statusline-context-reader] (the counter-example's subject).
**History below — the original capture, whose "route it into a skill" framing is narrowed by the section above.**
Captured by you — filed after `fa2f8e5`. Observed 2026-08-01: this session's whole post-close tail was triggered by Claude initiating the render-verification — "having the user run testing" — informally in conversation rather than through a skill. The verification was already a `[user]` work item ([working-mode-render-verification]) in Processed, but Claude drove it ad-hoc, and its findings surfaced as loose captures instead of through a structured flow.
**The insight (user):** when Claude finds itself wanting to run or drive testing/verification — especially `[user]` work — that urge is a signal to stop and route it into the proper skill (asking for a quick /plan to write/structure the lines, or /next to actually hand over an existing `[user]` work item), rather than running it informally in a post-close or inter-skill tail.
**Why it matters:** driving structured `[user]` work in loose conversation is the same failure family as under-filing `[user]` work items ([user-line-underfiling-asymmetry]) — user work handled informally instead of as tracked, structured lines. It also blurs the plan/execute boundary from the *other* side: not "planning during execution," but "executing structured work with no skill around it."
**Open design question (for /plan):** the exact trigger and destination — when does "I want to verify X" convert into "route this into a skill," and which skill? The verification items were already `[user]` work items, so part of the answer may simply be "run `[user]` verification through /next, not ad-hoc." Settle the routing at design.
**Files (rough):** plugin-behaviour.md (a rule on routing spontaneous testing/verification into a skill rather than a loose tail), possibly next.md / plan.md. Relates to [user-line-underfiling-asymmetry] and [post-close-tail-state].

#### Say which docset won when a stale host emits no routing directive — the one window the shipped redirect doesn't cover [skill-docset-override-unsanctioned]
**Mostly resolved by shipped work, narrowed at processing 2026-08-06 to what remains.**
**The unsanctioned move is now the sanctioned mechanism.** `session_start.py` emits a docset directive on every 5-series session: wherever a skill names a procedure doc under `docs/`, read the `docs-b/` version instead — carrying its own self-check, that docset B's docs are stamped `docset: B` while docset A's carry no frontmatter, so a missing stamp means the wrong docset was opened. So the exact manoeuvre this capture recorded as a judgment call nothing permits — the skill naming one path, Claude reading another — is now issued by the plugin itself. Verified by reading the hook at processing, and by this session's own injected directive.
**What remains is the narrow window the capture was born in: a host too old to emit the directive.** With no directive there is no sanction, and a session is back to improvising between the skill's named path and the project's recorded `Model:` field. That window opens at every stale-host moment and closes at the next reinstall, so it is real but self-curing.
**The answer, and it is inherited rather than invented.** The method already handles the neighbouring case: when the selected docset's folder is **missing** from the installed plugin, `session_start` falls back to the fuller docset and says so in a plain state line — which docset was picked, that its folder is absent from the installed plugin rather than from the project, and which is running instead. Never silently. Extend that same posture to the disagreement case:
```
host carries the docset the project's    ->  follow the PROJECT. Say so in one
  Model: field implies, but emitted           line: which docset is being read
  no directive (stale routing logic)          and that the host didn't route.

host does NOT carry that docset          ->  follow the HOST — it is all there
                                             is. Say so in one line.
```
**Why announce rather than halt, since halt was one of the three options.** A halt is heavy for a condition a reinstall cures, and it would fire hardest on exactly the user least able to diagnose it. The recorded failure was never that the wrong choice was made — it was that **the choice was invisible**: nothing recorded it, and the opposite call would have looked equally reasonable to a later reader. A stated line fixes the invisibility, which is the actual defect. Silently preferring either side is what is ruled out.
**What gets built.** `docs-b/plugin-behaviour.md` — the two-branch rule above, placed where the docset routing is described, phrased as one stated line rather than a question to the user. No hook change: the missing-folder fallback already exists and this is the no-directive sibling, governed by behaviour rather than by code, because a host too old to emit a directive is equally too old to carry a new check.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md`. Scope sibling of the other behaviour-doc items cleared this session. Relates to [plan-skill-did-not-engage] (the sibling observation from the same session), [retire-docset-a] (which removes the fallback this reasoning leans on — read them together), [session-start-silent-docset-fallback] (whose missing-folder state line this inherits).
**History below — the original capture; its fork is answered above.**
Captured by you — observed 2026-08-03 at this session's own close. Unlike /plan earlier in the session, the /done skill *did* engage — and it named `docs/done.md`, docset A, because the installed host (1.16.0-test2) predates phase 3 and has no docset routing. Claude read `docs-b/done.md` and `docs-b/done-plan.md` instead, reasoning from CLAUDE.md's Model target that an Opus 5 session uses docset B.
**That override was a judgment call nothing sanctions.** The skill stated a path; Claude read a different one. It was very likely correct — following docset A here would have closed an Opus 5 session under the frozen docset — but no rule permits it, nothing recorded it, and a session that made the opposite call would look equally reasonable. The pre-rezip window where the host and the project disagree is exactly when this arises.
**To settle at /plan:** whether the method should say what happens when the installed host's routing and the project's model target disagree — follow the host, follow the project, or halt and ask. Relates to [docset-routing-mechanism] (the detection that would make the disagreement visible), [plan-skill-did-not-engage], [opus5-docset-switch-live-verification].

#### Extend the check-the-documentation rule inward, so advice about the method's own capabilities is verified rather than described from memory [method-advice-invents-capabilities-on-domain-mapping]
**Split at processing, 2026-08-06 (the user's call). This item is now the rule half only; the recurring-intake feature question left as [recurring-intake-has-no-named-pattern].**
**The rule to extend already exists and is quietly scoped wrong.** `docs-b/plugin-behaviour.md`'s diagnosis-order rule says to check what a tool documents about itself before depending on its behaviour — and it is written throughout in terms of *external* tools, its justification reading that "the tool is used by very many people" and our own setup is the unverified thing. So it excludes the one tool Claude speaks about with the most authority and the least verification: **the method itself**, whose documentation is the procedure docs already loaded in the session. Nobody noticed the exclusion until the advice went wrong.
**The error's shape, which is why a general "be careful" would not have caught it.** Claude enforced the plan/build boundary correctly one paragraph before getting this wrong. What it got wrong was narrower and less visible: **where work items come from.** /next builds only what already sits in Processed above the readiness line; it cannot notice something in a folder and create work for itself. The advice silently deleted the queueing step, turning a two-command rhythm per batch into a one-command one. A rule aimed at the *boundary* would have passed; the check has to be against what the docs actually say the mechanism does.
**The severity argument, which is about who catches it.** This was caught because the user develops the method. **A consumer taking the same advice has no way to know it describes a capability that does not exist** — they would build their project's operating rhythm around it, and the failure would surface much later as the method seeming broken rather than as the advice having been wrong. The advisory surface is where the method's least-verifiable claims get made, to exactly the people least able to check them.
**[method-capability-misdescribed-during-its-own-fix] was folded in here at its processing, 2026-08-06 (the user's call), and it widens the trigger below — read this before building, because the narrow version is now known to be insufficient.**
**The instance.** Asked why the third overnight blitz reduced the queue by only one item, Claude answered that "the blitz can only build what /plan has cleared", and drew a conclusion from it: that the bottleneck had moved to planning throughput. The user challenged it. The claim is false — `resources/overnight-blitz-plan.md` Phase 2 and its softened-bar rule explicitly authorise the blitz to triage Unprocessed and build anything meeting that bar, a rule the user set when approving the first blitz, and the run in question used it for two of its four builds. Re-verified by grep at this processing, not carried on trust.
**Why it changes this item's design rather than merely evidencing it.** The slip happened **in the session that was processing this very item**, minutes after its rule had been authored. So having just written the rule did not prevent the behaviour — the same shape as [claude-reached-for-shell-write-against-rule], where a loaded explicit rule slipped three times and the conclusion was that prose is not the fix. That is evidence about the *strength* of the tendency, and it lands directly on where the trigger sits: the wrong claim was **not advice at all**, it was a one-clause explanation offered while answering an unrelated question about queue counts. A rule scoped to advisory moments would not have caught it.
**The second finding, kept because it is the half with consequences.** Claude built a further inference on the false claim — that planning throughput was now the limiting factor — and presented it as a finding. So a wrong premise stated in passing propagated into a recommendation about how the user should change their working rhythm. Same consequence shape this item already records from the consumer project: the user handed a false model of their own tooling to plan around.
**What gets built.**
1. `docs-b/plugin-behaviour.md` — extend the diagnosis-order rule's scope to name the method itself as a tool subject to it. **The trigger is any claim about what a skill, hook, plan or queue mechanism does — including one made in passing, inside an answer to a different question — not only claims made while advising.** Before making such a claim, read the doc that defines the mechanism. Placed on the existing rule rather than as a new one, so it inherits that rule's why.
2. `docs-b/plugin-behaviour.md` — and a clause for the second finding: an inference built on such a claim is not presented as a finding or a recommendation until the claim itself has been checked. This is the limb with real consequences, so it is stated separately rather than left implied by the first.
3. `docs-b/plan.md` — the same check where domain-mapping discussions actually happen, since /plan is where a user asks how their work maps onto the method.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md`, `plugin/si-plugin/docs-b/plan.md`. Scope siblings with the other behaviour-doc items cleared this session. No SPEC change — this constrains Claude's advice, not a described feature. Relates to [recurring-intake-has-no-named-pattern] (the pressure behind the invention, split out at processing — **read together, because fixing this half alone removes an outlet without removing the pressure, which the method's own anti-invention pattern predicts will simply produce a different invention**), [harness-misbehaviour-diagnosis-order] (the rule being extended).
**History below — the original capture, including the incident in full.**
Captured by you 2026-08-04 from a design discussion in a private consumer project, where you caught the error and corrected it.
**What happened.** The user asked how a recurring intake — new items arriving in an inbox, each needing the same decode-then-finalise treatment — should map onto the method. They proposed /plan scanning the inbox and processing them. Claude correctly pushed back on that half, citing that /plan decides what to work on and never does the work. It then proposed its own shape: recipes land in the inbox, **/next does the decoding as a build** and drives the finalisation walkthrough straight after, "one /next session rather than split across two commands." The user replied that nothing can run in /next without a /plan first, because /next doesn't write its own work items. That is correct, and Claude conceded it.
**Why the specific shape of the error matters.** Claude did not misunderstand the plan/next boundary — it enforced that boundary correctly one paragraph earlier. What it got wrong is narrower and less visible: **where work items come from.** /next builds only what already sits in Processed above the readiness line; it cannot notice something in a folder and create work for itself. Claude's advice silently deleted the queueing step, turning a two-command rhythm per batch into a one-command one.
**The consequence was structural, not cosmetic.** The user was designing their project's operating rhythm around Claude's description of how the tool behaves. Had it not been caught, the project would have been built expecting intake to be picked up automatically, and the failure would have surfaced later as the method seeming broken rather than as the advice having been wrong.
**This is the existing diagnosis-order rule, turned inward, and nobody noticed it applies.** The behaviour rules already say to check what a tool documents about itself before depending on the behaviour — written after a session built a capability on an input the documentation explicitly declined to guarantee. Here the "tool" is the method, its documentation is in the loaded procedure docs, and no check ran. The rule was authored for external tools and its scope quietly excludes the one tool Claude is always speaking with authority about.
**The severity argument, which is about who catches it.** This was caught because the user develops the method. A consumer taking the same advice has no way to know it describes a capability that doesn't exist — the advisory surface is where the method's least-verifiable claims get made, to exactly the people least able to check them.
**The second finding, and it is probably the cause.** The method has no story for **recurring intake** — a batch of similar things arriving regularly, each needing the same treatment. That shape is common well beyond recipes: receipts, evidence files, form submissions, inbound documents. Today every batch needs a fresh /plan to queue an item that says "do the same thing again," and nothing names that as the sanctioned answer. The method's own recorded pattern is that a real pressure with no stated legitimate path reliably produces an invented one — which is exactly what a proposal to have /next pick up its own work is. So the invention here may be a symptom rather than carelessness, and a rule telling Claude to check the docs before advising would not remove the pressure.
**May split at processing.** Two design calls live here: how to stop method capability being invented at advisory moments, and whether recurring intake earns real support. They are filed together because the second explains the first, and separating them risks fixing the symptom alone.
**Files (rough):** `docs-b/plugin-behaviour.md` (extend the check-the-documentation rule to cover the method itself, and the doc-routing rules where advisory claims are made), `docs-b/plan.md` (domain-shape discussions happen here), `SPEC.md` and a FAQ entry if recurring intake gains a named pattern. Relates to [harness-misbehaviour-diagnosis-order], [next-per-item-queue-removal].

#### Enumerate the editable set in the scope rule instead of naming it, settle "project docs" and "your own files" everywhere else [no-standard-names-for-workflow-docs-vs-user-files]
**The capture's central claim was checked at processing, 2026-08-06, and is FALSE — and what is actually true is worse, so the design below is not what the capture proposes.**
**The capture says the two terms "denote the same set". They denote two different sets, and they differ on the one file that matters.** Read from the hook and the docs rather than inferred:
```
"project docs"   (docs-b/setup.md)      ->  SPEC.md, QUEUE.md, LOG/
"method docs"    (hooks/pre_tool_use.py) ->  QUEUE.md, LOG/, _build.md, _plan.md
                                             — SPEC.md is EXPLICITLY EXCLUDED
```
The hook's own header states the reason: *"SPEC.md is not a method doc, so a build can edit it only when it's explicitly listed in Files: … so scope-lock alone keeps SPEC read-only for any build that doesn't name it."* That is the containment which stops a build silently rewriting what the project is meant to be.
**So this is a correctness problem in the description of a safety boundary, not a naming inelegance.** A reader who takes "method docs are always editable" to mean the three project documents concludes that a build may rewrite SPEC.md at will. The capture's framing — pick one good name for one thing — cannot fix that, because there is no one thing.
**And the capture's third meaning is real too, making three.** The method also ships its own procedure documents (`plan.md`, `next.md`, `plugin-behaviour.md`), and "method docs" reads at least as naturally as those. `docs-b/setup.md` already instructs /setup not to say "method docs" to users at all — so the term is *already* recognised as unsafe in front of a reader, without anyone noticing that it is unsafe in the rules too.
**The design (the user's call, 2026-08-06): in the scope rule, do not name the set — list it.**
1. **Enumerate, in `pre_tool_use.py`'s description and in every procedure-doc statement of what a build may edit:** the queue, the log, and the session's working file. Four more words, and there is no wrong reading available — **a name always invites one; a list has none.** This is the part that carries the safety consequence, and it is deliberately the least clever option.
2. **"Project docs" keeps its existing meaning** — SPEC.md, QUEUE.md, LOG/ — defined once and used consistently for the ordinary conversational case where no boundary is being asserted.
3. **"Your own files"** becomes the standard term for the user's own content — the photos, drafts, recipes, scanned documents, source files. It is deliberately plain: it needs no definition and cannot be inverted, which is exactly what the coined alternative failed at. The capture's evidence for that is kept below.
4. **Retire "method docs" as a collective term** wherever 1 or 2 has replaced it, rather than redefining it — a term with three live readings is not repairable by adding a fourth definition somewhere.
**Scope by grepping the literals at build time, per the hook-enforced-format tracing rule, not from this list.** The strings are `method doc`, `method docs`, `project docs`. Candidates found at processing: `docs-b/done.md`, `done-plan.md`, `next.md`, `next-audit.md`, `migrate-checklist.md`, `plugin-behaviour.md`, `setup.md`; `hooks/pre_tool_use.py` (the header contract and the `_is_method_doc` docstring); `hooks/session_start.py`; `scripts/scrub_sweep.py`; `templates/CLAUDE-TEMPLATE.md`, `templates/faq-template.md`; `skills/done/SKILL.md`. **Note the hook's function name `_is_method_doc` is internal and need not change** — the contract text a reader relies on is what matters.
**The docset-A call, to make deliberately at build rather than assume.** Docset A carries the same wording about a hook both docsets share. Renaming vocabulary is development, which the freeze bars; but describing a shared safety boundary wrongly is the correction shape the freeze permits. **Decide explicitly, and if A is touched, author fresh in A's register — never paste from B.** Same question as [docset-a-commit-msg-tmp-same-bug]; read them together and answer both the same way.
**Files:** `plugin/si-plugin/hooks/pre_tool_use.py`, the docs-b procedure docs and templates named above (final list from the grep), `SPEC.md` if its description of the scope-lock uses the term, plus `templates/faq-template.md` + `faq-index-template.md` — a user meeting "your own files" in one place and "project docs" in another needs the distinction stated once. `plugin/si-plugin/docs/` subject to the freeze call above. Relates to [background-vocabulary-list-not-protective] (the same readability goal, cleared above — that item's plain-substitutions limb should reuse the terms this one settles, so build them in that order), [docset-a-commit-msg-tmp-same-bug] (the same freeze question), [scope-file-editable-vs-locked] (which discusses the same boundary).
**History below — the original capture, whose same-set premise is corrected above.**
Captured by you 2026-08-04, from writing a site article that needed both terms and found only one.
**What's actually in the corpus, checked rather than assumed.** "Project docs" appears in `docs-b/plugin-behaviour.md`, `docs-b/setup.md`, `templates/CLAUDE-TEMPLATE.md` and `templates/faq-template.md`. "Method docs" appears in `docs-b/plugin-behaviour.md`, `setup.md`, `next.md`, `next-audit.md`, `done.md`, `done-plan.md` and `migrate-checklist.md`. Both denote the same set — SPEC, the queue and the log. Nothing declares them synonyms.
**"Method docs" is the worse of the two, because it has a plausible wrong reading.** The method also ships procedure docs — `plan.md`, `next.md`, `plugin-behaviour.md` — and "method docs" reads at least as naturally as those. So a rule saying edits are allowed to "method docs" can be read two ways, and one of them is much wider than intended. This appears in the scope-lock allowance, where the difference is not cosmetic.
**And there is no name at all for everything else in a project.** The recipes, the scanned documents, the photos, the drafts — the actual content a project is about. One incidental "your files" in a FAQ template is the whole vocabulary. Every project has both categories and every user has to talk about both, so the absence doesn't stay empty: it gets filled, differently, each time.
**The evidence that it gets filled badly.** A site article written this session reached for "material" as the term, defined it explicitly as *the user's own files, outside the four workflow documents* — and was immediately misread the other way round by the person who commissioned it, who took it to mean the project docs. That is about as fast as a coined term can fail. It was replaced with "your own files", which needs no definition and can't be inverted.
**Why this is worth doing rather than tolerating.** The two categories differ in who owns them, who writes them, and what the scope-lock does about them, and those are exactly the things a non-coder needs to get right early. A vocabulary that blurs them makes the clearest explanatory move available — *your own files are not your work* — impossible to state crisply. It also compounds: every new doc and FAQ entry picks whichever term is nearby.
**What to settle.** One name for the workflow's own documents, used everywhere, chosen so it cannot be read as the procedure docs. One name for the user's own content, plain enough to need no definition — "your own files" is the current candidate and has survived one real test. Then a corpus-wide pass, scoped by grepping the literal strings per the tracing rule, since the terms are scattered across seven procedure docs and two templates.
**Files (rough):** `docs-b/plugin-behaviour.md`, `docs-b/setup.md`, `docs-b/next.md`, `docs-b/next-audit.md`, `docs-b/done.md`, `docs-b/done-plan.md`, `docs-b/migrate-checklist.md`, `templates/CLAUDE-TEMPLATE.md`, `templates/faq-template.md`, `SPEC.md`, plus a FAQ entry — final list from the grep. Docset A is frozen, so procedure changes land in docs-b only. Relates to [method-advice-invents-capabilities-on-domain-mapping].

#### Make the background-vocabulary rule a test rather than a list, supply the plain substitutions, and forbid two names for one thing [background-vocabulary-list-not-protective]
Captured by you 2026-08-06 — you repeatedly had to ask what terms meant, and one exchange took four turns to resolve a single word. Processed 2026-08-06; the design below is settled, not open.
**What happened.** Across the session Claude used, without defining: **"the guard"** (an improvised synonym for the hook, which cost four turns to untangle — the user finally asked outright *"by 'that guard' do you mean the hook?"*), **"the routing half"** (the user: *"half of what"*), **"the fork"**, **"scope-lock"**, **"the capture"** as a bare noun, and **"routing"** generally. The user also twice asked for a plain summary of an item because the analysis had not been readable on its own, and once said plainly *"I just do not get anything you are talking about here."*
**Why it matters beyond this session.** `docs-b/plugin-behaviour.md` carries a background-only vocabulary list — terms that name scaffolding the user never sees and must be translated or omitted. **Not one of the terms above is on it.** The list names things like `Step N`, `pre-flight`, `[SILENT]`, procedure-doc filenames. So the list is a list of *known* leaks, and it protects against exactly those; it offers nothing against a term invented on the spot, which is what "the guard" was.
**The sharper reading, to weigh at processing: an enumerated list is the wrong shape for this rule.** Every term on it earned its place by leaking once. A new improvised synonym cannot be on a list written before it existed, so the list can only ever be complete for the past. Candidate replacement or supplement: a **test** rather than a list — before using a term for a piece of the machinery, would the user have met it in something they read? If not, name it in plain words. That is checkable against any term, including one just invented.
**A specific sub-case worth naming, because it caused the worst exchange:** using two different words for the same thing in one explanation. "The hook" and "the guard" were the same program, and alternating between them read as two mechanisms. Consistency of naming may matter more here than the choice of name.
**Note the interaction with [approval-flow-token-doubling-simplification]**, cleared this same session: that item's decision is that captures are written for Claude and summarised for the user. This finding is the same problem one layer up — the summary itself was not readable. A rule about summarising does not help if the summary uses the same untranslated vocabulary.
**What gets built — one file, four changes to the background-only vocabulary section.**
1. **The test becomes the rule.** Before using a term for a piece of the machinery, ask: would the user have met this word in something they actually read? If not, say it in plain words. This governs any term, including one invented in the moment, which is what the list structurally cannot do.
2. **The list is demoted to worked examples**, not deleted — it still carries the known leaks usefully, it just stops being the thing the rule *is*. This session's terms are added to it: "the guard", "the routing half", "the fork", "scope-lock", "the capture" as a bare noun, "routing".
3. **Supply the plain substitutions outright**, for the terms that recur — hook, scope-lock, capture, fork, routing. **This is the addition the capture did not propose, and it is the one that addresses the actual failure mechanism.** A test says *avoid this word* and leaves the replacement to be invented mid-sentence; inventing under that pressure is exactly what produced "the guard". A ready phrase removes the invention rather than forbidding its result.
4. **One name per thing, within an explanation.** Never alternate synonyms for the same mechanism. "The hook" and "the guard" were one program, and switching between them read as two — which is why that exchange cost four turns rather than one. Consistency matters more here than which name is picked.
**Stated honestly, so the build isn't over-expected: this is a wording rule, and wording rules slip.** [method-advice-invents-capabilities-on-domain-mapping] carries an instance of a rule failing minutes after it was authored, and [claude-reached-for-shell-write-against-rule] is the case where prose was judged not to be the fix at all. The difference here is that **no mechanical option exists** — nothing inspects Claude's prose, and no hook could judge whether a word is jargon — so a better-shaped rule is the only lever available rather than the lazy choice. Expect an improvement over the list, not a guarantee.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md`. No SPEC change — this constrains how Claude words things, not a described feature; no FAQ entry, since the user never meets the rule itself, only its effect. Docset A is frozen and this is new behaviour, so docs-b only. Scope sibling of the other behaviour-doc items cleared above. Relates to [approval-flow-token-doubling-simplification] (readable-for-the-user is the shared goal — that item decides captures are summarised for you, and this is why the summary itself has to be readable), [method-advice-invents-capabilities-on-domain-mapping] (the loaded-rule-slips evidence), [faq-backfill].

#### A slash command was rendered as a runnable bash code block, and on remote control that buries the actual ask [slash-command-rendered-as-runnable-bash-block]
Captured by you 2026-08-06 from remote control, with two screenshots of another project's session. That session ended two messages — *"Run /done here to record the build and commit it?"* and *"Run /done to record the build and commit it?"* — and beneath each put a fenced block tagged **Bash** containing `/done`. You reported it as: this session is outputting to code blocks what it normally would as a text chip.
**Checked rather than inferred, per the check-the-documentation rule.** The shipped docset B says nothing about how a slash command should be rendered — no rule requires the fence and no rule forbids it. `docs-b/plan.md`'s own close wording puts it in plain prose (*"Run /done and I'll record this and commit it"*), so the procedure text models the right shape without ever stating it as a rule.
**The two rules that collide to produce it.** The behaviour rules say fenced blocks are for code and shell commands, one per block; the harness instructs that a shell command the user might run goes in its own `bash`-tagged fence, because the app attaches a Run button to shell-tagged blocks. Neither rule excludes slash commands, so `/done` gets classified as a shell command it is not. `/done` is typed into the chat box; run in a terminal it would fail.
**Why it is worse than untidy, and worse on remote control specifically.** The fence is the loudest element on a phone screen and carries an affordance that does the wrong thing, while the actual ask sits above it in ordinary text. The method already has a rule that no part of an ask may live only in the reply suggestions, for exactly this reason — a stop must be answerable from its own words. This is the mirror failure: the ask is present but out-competed by a control that misleads. The screenshots show the input box reading "Queue feedback…" with a Run-style button, next to a block the user cannot usefully press.
**Processed 2026-08-06 and kept. The build is one clause in the fenced-code-block rule:** slash commands are named in prose — `/done`, `/next` — never fenced, because a fence means "a terminal runs this" and a slash command is typed into the chat box. The existing rule's two stated reasons for fencing (protecting characters whose exactness is the substance, and the Run button) both argue *against* fencing a slash command rather than for it, so this is a boundary the rule was always missing rather than a change of direction. The method's own procedure text already writes it this way, so nothing else has to move.
**The Claude Code half is filed and done: [anthropics/claude-code#84430](https://github.com/anthropics/claude-code/issues/84430)**, opened 2026-08-06 on your steer, duplicate-checked first (no prior report) and scrubbed by construction — no project name, path or slug in the text. It asks for either of two behaviours: suppress the shell/Run affordance when a block's content is a bare slash command, or render it using the slash-command chip the app already has. **Nothing in this item waits on it.** The method clause above is the fix that is ours to make, and it is correct whether or not the app ever changes — a slash command is not shell input, so it does not belong in a shell container regardless of how the container is rendered.
**Files (rough):** `docs-b/plugin-behaviour.md` (the fenced-code-block rule, one clause). Relates to [close-reliance-on-reply-chips] (the same failure of an ask being unanswerable from its own words on remote control), [keep-step-recommendation-has-no-length-discipline] (the same session-shape readability family).

#### Research is never queued for /next: it can only produce captures, so it is conducted in the /plan that needs it [research-never-queued-for-next]
Captured by you 2026-08-05, raised while processing the reply-chips item, whose draft carried "research first" as the first limb of a queued work item. Your rule: research must never be planned as next-work, because its only output is findings — captures, corrected premises, informed designs — never a build. Queuing it defers the very information the planning discussion needed, so the item gets designed on an unverified premise and the research lands later as a stale errand. Research that turns out to be needed in /plan is conducted then and there, in /plan. The procedure already leans this way ("/plan resolves what it can in-session"); this makes it a rule with a tell: a work item whose first step is "research X" is the sign that /plan skipped its own job.
**Processed 2026-08-06. Checked against the shipped docs before deciding, not assumed:** `docs-b/plan.md` already carries the resolve-now-versus-capture block, and `/next` has no research flavor at all — the flavors are build, `[audit]` and `[user]`. So research has no legitimate route into a run today, and what is actually missing is the **tell** and the **boundary**, not a new prohibition. That makes this a cheap sharpening of text that already leans the right way.
**Both open questions resolve, and both answers go into the rule.**
1. **Measurement infrastructure is not research.** The adherence harness builds a runner, a scenario set and constraint checkers — files that exist when it finishes. The discriminator to state plainly: **does anything survive the work except knowledge?** If yes it is a build, whatever its subject. That keeps the rule from swallowing every investigative-sounding build item.
2. **A genuinely multi-session investigation stays in Unprocessed, and its *processing* is the research** — across as many /plan sessions as it takes. It never becomes /next work, and it never needs a new container.
**Naming that second answer is not optional, and this item must not ship without it.** The method's own recorded pattern is that a prohibition at a moment of real pressure reliably produces an invented escape route, and an invented move is worse than the thing prohibited because nothing recognises or records it. Three separate rules have been walked into for exactly this reason. "Never queue research" creates precisely that pressure — the research is real and has to go somewhere — so the legitimate home is stated in the same breath as the prohibition.
**Files:** `docs-b/plan.md` (the resolve-now block gains the tell and the two boundaries) and `docs-b/plugin-behaviour.md` (the routing table, where research sits on the findings side — a record to be READ — rather than the work side). Docset A is frozen and this is new behaviour, so docs-b only.
**No FAQ entry, and the reason is recorded rather than left as a silent skip:** the user never meets this rule. What they experience is research happening in the planning session that needed it instead of being deferred, which needs no explanation. Relates to [docset-b-register-decision-weaker-than-recorded] (the harness — the worked example of infrastructure that is a build, not research).

#### Move the missing-setting catch-up out of the session-start hook and into /plan's opening read [setting-topup-mid-close]
Captured by you 2026-08-05, same incident. session_start's missing-setting directive tells the session to open its first reply with the question; that session's first command was /done, so "Which .md editor do you work in here?" got wedged into an already stop-heavy close as an unrelated ask at the worst moment. And the setting it solicited — Editor — has since been retired by the method, so a stale host was asking for an answer nothing current reads. The same thing happened on this project the same day: this session's 1.18.0 host told it to collect the same retired setting. Weigh: the top-up defers to a planning-shaped moment rather than riding whatever command comes first; and how a top-up stays safe when the setting list has changed under an older installed host.
**Third sighting, 2026-08-06 (captured by you): the same ask now gates a /next run.** In another project on remote control, a fresh session's /next opened with "Which .md editor do you work in here?" and held a queued run — four builds plus a walk-through — behind it. Your reaction, kept as the design input: "why do you need to know that?" — the ask solicits a setting whose purpose you no longer even remember, which is what an execution-gating question must never be. (The purpose, for the record: the Editor field let the plugin point at a doc instead of re-pasting it; it was retired because links open in the app's own viewer regardless, so nothing reads the answer.) Strengthens both halves: the top-up must never gate an execution command, and a stale host keeps soliciting settings the method has retired.
**Deferred at the 2026-08-06 morning processing (user's call), then processed at that day's later session.** What the deferral established, kept because it is still the factual base: the Editor ask itself is fully retired on main (field, /setup question, and catch-up check all deleted); only stale hosts still ask, and updates cure them. The user's lean going in was that the catch-up ask should perhaps be retired outright.
**Re-verified at processing rather than trusted.** `hooks/session_start.py` does carry an empty `setting_checks = []` list with the machinery around it intact — so the mechanism is live but dormant. And the text it injects when it fires reads *"Bring it up to date now, before /next or /plan."* **So the run-gating you hit on remote control was not the mechanism misfiring; it was the mechanism doing exactly what it instructs.** That reframes the fix: this is not a timing bug to be retimed.
**Decided: rehome it, which is the user's retirement of the hook mechanism with the capability kept.** Neither "retire it" nor "retime it inside the hook" is right, and the reason is structural. A session-start hook fires **before anything knows what the session is for**, so its question cannot choose a good moment — it can only attach itself to whichever command comes first. Retiming it in place would mean writing a rule instructing the hook not to do the thing its position makes it do, which is the weakest kind of fix.
**Why not delete it outright, despite the lean.** It is the only thing that reaches a project set up weeks ago when the method later adds a setting. Without it, a new setting arrives only if the user happens to re-run /setup — and that need is about to be concrete rather than hypothetical, since [provenance-credit-second-person-ambiguous] adds exactly such a field. Deleting the mechanism and then rebuilding it for that item is churn.
**The build:**
1. **`hooks/session_start.py` — delete the content-level top-up block outright**, including the empty `setting_checks` list, the `missing_settings` accumulation, and the injected "PROJECT MISSING NEWER SETTINGS … before /next or /plan" text. Leave the presence-based `missing_scaffold` path alone: that one detects whole files or folders absent, is a different check, and does not gate anything.
2. **`docs-b/plan.md` — the opening read gains the check.** /plan already reads project state and already folds what it finds into one narration, so this costs no new beat. It is also structurally incapable of holding a /next run, which is the whole point of moving it.
3. **Whatever asks must say why it wants the answer.** *"Which .md editor do you work in here?"* failed on two counts, and only one was timing: it was also unanswerable, because neither the user nor the session asking could say what the answer was for. Any catch-up question names the setting, what reads it, and what changes once it is answered.
**Files:** `hooks/session_start.py` (deletion), `docs-b/plan.md` (the opening read), `SPEC.md` if it describes the top-up as hook behaviour, and a FAQ entry — a user meets this question directly, so it earns one. Docset A is frozen; the hook is shared by both docsets, so deleting the injection there is a correction that reaches A for free without editing A. Relates to [provenance-credit-second-person-ambiguous] (the first real setting this will carry), [scaffolding-resync] (the deliberately-excluded harder case of rewriting changed template wording).

#### The audit close's routing step carries no response-shape tag while all three of its siblings do [next-audit-route-step-untagged]
By Claude, from the overnight blitz's method-compliance sweep (Lens 2, tag placement), 2026-08-06. Appended at the bottom — unrelated to its neighbours.
**The finding.** In `docs-b/next-audit.md`, "Present findings as one numbered set" is `[BRIEF, PROMPT]`, "Handle contested findings one at a time" is `[SEQUENCE, PROMPT]`, and "Close" is `[BRIEF, PROMPT]`. Between them sits "Route the approved set to Unprocessed", which carries nothing — and it is not a silent step: it appends captures to Unprocessed and the Captures placement rule requires narrating the placement in one line where judgment is exercised. So its output behaviour is left to chance in a doc where every neighbour's is pinned.
**It was captured rather than fixed because the choice is a real call, not a typo.** `[BRIEF]` matches the one-line placement narration the placement rule asks for. `[SILENT]` is defensible on the reading that the findings were presented and approved one step earlier, so re-narrating where each landed is noise the consolidate-the-scans rule would rather avoid.
**Decided at processing 2026-08-06: `[BRIEF]`, and the tie breaks on a rule rather than on preference.** The step's own text was read at processing, which is what settles it: it does not merely append. **It consolidates** — a dozen approved findings can collapse into one work item with numbered points, or stay standalone where they carry design calls the user must make. So the user approved *findings* and what they end up with is a *structure* they did not choose. Silence there means the shape of their own queue changed without a word.
**The second, independent reason, and it is the stronger one.** The Captures placement rule requires narrating placement in one line wherever judgment is exercised. A `[SILENT]` tag here would suppress a standing rule that applies everywhere else — and tags govern **how much is said, never which rules apply**. Reading a tag as a licence to skip a rule is a misuse of the tag system, so `[SILENT]` is not merely the weaker option here; it would be wrong in a way worth recording.
**Files:** `plugin/si-plugin/docs-b/next-audit.md` — the "Route the approved set to Unprocessed" heading line, nothing else. **Docset A is untouched even if it carries the same gap**: adding a tag is development, not correction, so the freeze bars it. Relates to the compliance checklist at `resources/method-compliance-audit-checklist.md`, which this sweep ran from.

#### A bundled double-ask mid-/plan caused a mis-approval — the one-item rule slipped in live use [bundled-double-ask-misapproval]
Filed at the 2026-08-06 planning close's wind-down re-scan, per the testing-outcome rule. Mid-session, Claude put a fresh capture confirmation and a pending work-item approval into one message; the user's "approve" was meant for the capture, and the work item was executed on it — caught only because the user said so ("I didn't realise you'd bundled the ask"). The one-item-at-a-time rule was loaded and explicit, and it still slipped at exactly the moment it protects: two live asks in flight after an interruption. Same family as [claude-reached-for-shell-write-against-rule] — a clearly-worded loaded rule slipping under local pressure (there a bulk deletion; here a pending ask interrupted by new input). Candidate fix to weigh at processing: a rule that an interrupted pending ask is always re-asked alone, in its own message, never bundled with the interruption's handling — or judge it a one-off not worth a rule.
**Processed 2026-08-06: kept, and NOT judged a one-off — the same condition recurred during that very processing.** Mid-session the user interrupted a pending work-item approval with two screenshots of an unrelated problem. That interruption was handled to completion, and the pending item was then re-presented alone before any decision was asked for. The right shape happened, but by judgment rather than by rule, since nothing in the docs names this case. **A fix that works only when the session happens to notice is not a fix.**
**Two properties make this worth a rule rather than vigilance.** A short affirmative — "ok", "approve", "go ahead" — **binds to the most recent ask**, so the instant two asks are live, every cheap approval word is ambiguous, and cheap approval words are exactly what a well-run session trains the user to give. And the failure is **silent**: an approval landing on the wrong ask leaves no trace anywhere. Last time it surfaced only because the user happened to say so, which is not a detection mechanism.
**The rule, narrow and stated as the hardest case of an existing one:** new input arriving while an ask is pending is **never answered in the same message as that pending ask**. Handle the interruption to completion, then re-ask the pending item on its own, restating what it is — because after a detour the user has lost the thread as well, so a bare "so, approve?" is not a re-ask.
**Why it is authored as a named case rather than a new rule.** The general one-item-per-message rule was loaded, explicit and being followed, and it still slipped — because in the moment it does not feel like two items. It feels like one item plus some housekeeping, and housekeeping does not feel like an ask. Naming that specific misperception is the whole value; restating the general rule more firmly would change nothing, since firmness was never what was missing.
**Files:** `docs-b/plugin-behaviour.md` — the one-item-per-message rule gains this case. Nothing else. **No FAQ entry:** what the user experiences is Claude re-asking something after a detour, which needs no explanation. Docset A is frozen and this is new behaviour, so docs-b only. Relates to [claude-reached-for-shell-write-against-rule] and [false-positive-recorded-as-settled-capability] (the same slip-shape — a clearly-worded loaded rule failing under local pressure, now recorded three times).

#### Name "one /plan per batch" as the sanctioned answer for recurring intake, beside the rule that forbids the shortcut [recurring-intake-has-no-named-pattern]
Split from [method-advice-invents-capabilities-on-domain-mapping] at its processing on 2026-08-06, the user's call. That item is the rule half — check the method's own documentation before advising about its capabilities — and this is the *pressure* that produced the invention in the first place. Appended at the bottom: nothing currently in Unprocessed relates to it.
**The shape.** A batch of similar things arrives regularly and each needs the same treatment — receipts, evidence files, form submissions, inbound documents, scanned recipes. Today every batch needs a fresh /plan to queue an item saying "do the same thing again", and **nothing in the method names that as the sanctioned answer.** The method's own recorded pattern is that a real pressure with no stated legitimate path reliably produces an invented one — which is exactly what "have /next pick up its own work" was. So the invention was plausibly a symptom rather than carelessness, and the rule half alone does not remove the pressure.
**The boundary any answer must respect, so it isn't rediscovered as a fork.** Executable work reaches /next through the queue and only through the queue; /next never reads a side document to find steps and never writes its own work items. That is deliberate, and an intake pattern that quietly breaches it is the same error in a nicer wrapper. The likely legitimate shapes are therefore queue-side — a work item designed to be re-added, a described repeatable procedure the user queues per batch, or simply naming "one /plan per batch" as the answer and saying so plainly — not a new intake mechanism.
**Processed 2026-08-06 and narrowed hard: build the SENTENCE, not a pattern.** The item's own text half-dismisses its answer — *"every batch needs a fresh /plan may simply be the correct answer stated out loud"* — and that is the answer. It is buildable today and needs no frequency evidence at all, because it invents nothing: it states what the method already requires.
**Why the narrow version is the whole win.** The queue-only rule is a prohibition sitting **exactly where real pressure builds** — a batch has arrived, it plainly needs doing, and the obvious shortcut is forbidden. This method's own recorded pattern is that a prohibition with no stated alternative reliably produces an invented one, and that is precisely what happened here: "have /next pick up its own work" was invented to fill the vacuum. So the invention was a symptom of a missing sentence, not carelessness, and the cure is naming the sanctioned path in the same breath as the ban.
**Designing an actual intake pattern is explicitly NOT kept, and the reason is recorded so it isn't re-proposed as an oversight.** The evidence is one observed instance in one private project. Building method surface on that is inventing for a need nobody has confirmed — the same failure in the opposite direction. If recurring intake is genuinely common, it will re-capture itself with better evidence attached, and this sentence will already have removed the pressure that produces bad workarounds in the meantime.
**What gets written:** one clause where the queue-only rule already lives, stating that recurring work is queued **per batch**, that this is the answer rather than a gap the method hasn't got to, and that a work item may be written to be re-added rather than re-derived each time. No new state, no new marker, no intake mechanism.
**Files:** `docs-b/plugin-behaviour.md` — the routing-and-discipline rule that executable work lives in the queue gains its positive limb — plus `templates/faq-template.md` and its `faq-index-template.md` index line. The FAQ entry earns its place: a user with recurring work asks this directly, and a plain answer is what stops them inventing a workaround. No SPEC change — nothing about the described product changes. Docset A is frozen and this is new text, so docs-b only. Relates to [method-advice-invents-capabilities-on-domain-mapping] (the rule half — read together), [next-per-item-queue-removal].

#### The keep-step's analysis ran long enough that the user asked for a summary [keep-step-recommendation-has-no-length-discipline]
Filed at the 2026-08-06 planning close's wind-down re-scan, as a testing outcome. Mid-session, after a recommendation message presenting three response options with reasoning for each, the user replied only *"please summarise"* — and the shortened version conveyed the same decision in about a fifth of the words. Earlier in the same session they twice asked for plain summaries of items because the analysis was not readable on its own, and once said plainly *"I just do not get anything you are talking about here."*
**Distinct from [background-vocabulary-list-not-protective], and the distinction is the point.** That item is about unfamiliar *words*; this is about *length and shape*. Both were live in the same session, and fixing the vocabulary alone would have left this untouched — a message can be entirely plain-worded and still be too long to act on.
**Where the gap is.** plan.md's keep-step is told to describe what would be built in both limbs — which files change and what changes inside them — and that requirement is load-bearing and should stay. What is absent is any discipline on the *accompanying reasoning*: nothing bounds how much analysis rides along, so it grows to fit whatever was thought about. The evidence from this session points at decision first, options as a short list, reasoning on request — which is simply the standing lead-with-the-decision rule, not being applied to this message type.
**It recurred at its own processing, 2026-08-06, which is what settles it as a pattern rather than one bad message.** Partway through that session the user again replied *"please summarise the capture to me"* — same moment in the loop, same shape: the analysis was longer than the decision required. Two sessions, three or more instances, one of them while processing the item about it.
**Decided at processing: take the cheaper option — name the moment in the existing rule; do NOT give the keep-step its own shape.** Two reasons, and the second is the one that would not be obvious later.
1. **The both-limbs requirement genuinely needs words and must not be squeezed.** Stating which files change and what changes inside them is load-bearing — it is the check that stops undesigned work reaching Processed. A bespoke length rule sitting at the same moment would pull directly against it.
2. **Two rules governing one moment is a design smell, because the one that fires is whichever was read most recently** — which is not a property anyone controls. Naming the moment inside the rule that already governs it leaves exactly one rule in force there.
**What is actually missing is ORDER, not a length cap, and the fix should say so.** The evidence points one way in every instance: **decision first, options as a short list, reasoning on request.** That is the standing lead-with-the-decision rule, simply never applied to this message type. Framing the fix as a cap would be wrong as well as unenforceable — the failing message was not over some limit, it was back-to-front.
**Files:** `docs-b/plugin-behaviour.md` — the lead-with-the-decision rule names the keep-step recommendation as a moment it governs. **No `plan.md` change:** the keep-step's own requirements are correct and stay exactly as written. Docset A is frozen and this is new behaviour, so docs-b only. No FAQ entry — this is how Claude writes a message, not a workflow the user needs explained. Relates to [background-vocabulary-list-not-protective] (the same session's readability problem, one layer down), [approval-flow-token-doubling-simplification] (whose whole decision is that the user reads a summary rather than the full text — this is that principle failing at a different moment).

#### The queue mover trips the file-changed-on-disk warning on nearly every kept item, and that warning must not become routine [mover-edit-sequence-normalises-a-safety-warning]
Filed at the 2026-08-06 planning close's wind-down re-scan, as a testing outcome. Twice this session an Edit to QUEUE.md returned *"the file had been modified on disk since you last read it"*, both times caused by this same session's earlier `reorder_queue.py` run, which rewrites the whole file. Both were benign, and both were verified as benign — `git status`, a structure count, section headings, marker position, and the queue lint.
**The concern is not the two instances; it is that the pattern is structural.** /plan moves an item with the mover and then edits the moved block in place, which is the prescribed sequence, so this fires on nearly every kept item. The file-safety rule correctly forbids reasoning past the warning — and states its own reason plainly: *"The innocent case trains the response to the dangerous one."* A warning that fires several times per session, always innocently, is exactly that training happening on schedule. The rule is right; the frequency is what makes it fragile.
**There is precedent for the fix, which is why this is worth doing rather than tolerating.** `done.md` already faces the same shape and solves it: the hash-backfill signature is named explicitly, and a matching dirty file is folded in with at most a one-line note rather than investigated file-by-file, on the stated grounds that re-investigating a known answer is *"pure delay for zero decision value."*
**Fresh evidence from its own processing session, 2026-08-06, and it is stronger than the two instances above.** That session processed seventeen dispositions, ran the mover on nearly every one, and the warning fired repeatedly — every single time caused by that session's own mover run minutes earlier. Every occurrence was checked properly (`git status`, item count, section headings, marker position) and every one was benign. **The session recorded the honest part: by the fourth identical occurrence the pull to skip the check was noticeable.** That is the training the file-safety rule warns about, observed rather than predicted, in the session that was processing the warning about it.
**Decided at processing: fix the SEQUENCE first; the named signature is the fallback, not the plan.** The two routes are not equal. A named benign signature is a **carve-out on a safety rule**, and every carve-out has to be drawn narrowly enough to keep catching the dangerous case — which here is real: a concurrent session's write once destroyed an item heading and reached a commit, and this warning is the layer that would have caught it. Writing an exception is the option that spends safety.
**Reversing the sequence spends nothing, because it removes the collision instead of excusing it.** Edit the block in place first, then move it — the warning never arises, so no exception has to exist and no judgment has to be made about whether *this* occurrence is the innocent kind.
**The obstacle, stated honestly and scoped.** The mover addresses blocks by slug, so an edit that changes the heading line the slug sits on would break a later move. That is narrow rather than fatal: slugs are immutable by design and survive reorders and renames, so the ordinary keep — rewrite the description and rationale, slug unchanged — is safe under edit-then-move. The build must confirm this against `reorder_queue.py`'s actual slug matching rather than assume it.
**So the build has an order and a fallback:**
```
1. reverse the sequence where the slug is untouched  ->  the warning stops existing
2. named benign signature, narrowly drawn            ->  ONLY where step 1 cannot
                                                         apply. Must state what it
                                                         does NOT cover, so a
                                                         concurrent-write warning
                                                         still stops work.
```
**Files:** `docs-b/plan.md` (the keep-step's move-then-edit sequence), `docs-b/done-plan.md` (the close reorder, same shape), and `docs-b/plugin-behaviour.md` (the file-safety warning rule) **only if step 2 turns out to be needed** — if the sequence fix covers every case, the safety rule is left exactly as it is, which is the better outcome. No FAQ or SPEC change: this is internal sequencing the user never sees. Relates to [concurrent-session-support] (the genuine danger this warning exists to catch — any carve-out must keep catching it).

#### Group work items by shared scope so a group moves into Processed and runs as one unit [plan-close-priorities-over-reorder]
Captured by you 2026-08-05, while processing [merge-and-fold-without-asking] — you raised it as the larger question sitting behind the scope-siblings collision.
**Your position:** the close reorder has been rather expensive, and you don't know that it earns its keep. What you'd rather have is broader **priorities** asked about or refreshed at the /plan close, with **groups** of work items prioritised accordingly — rather than a per-item reordering pass over both sections every session.
**Why it is worth taking seriously rather than defending the existing pass.** The close reorder walks both sections and re-derives order by unlock-potential and build-order. Its cost is paid every single session; its benefit only shows up when the order was actually wrong. The method has already conceded half of this once by making the *start-of-processing* reorder conditional and change-scoped — consider only what changed, and leave a still-correct order alone. The close reorder never got that treatment, so this may be the same correction arriving for the other pass.
**Two open questions the design must separate.** Whether the reorder is *too expensive* (a cost problem, fixable by making it conditional the way the start-of-processing pass already is), or whether per-item ordering is *the wrong unit* (a design problem, fixed by grouping). These have different answers and conflating them would produce a change that costs the same and explains less.
**What "priorities" would mean, which is genuinely undesigned.** Whether they are project-level themes the user names and refreshes ("the release, then the rename"), whether groups are drawn from those themes or from shared scope, and how a group's priority interacts with an individual item's dependencies. Note the user's global CLAUDE.md already carries a ranked project list, so priority-as-a-named-ordering is a shape this user already works in — but that is cross-project, and this would be within one queue.
**The constraint any answer must honour, from [merge-and-fold-without-asking].** Dependencies come first and are never displaced by grouping; unblock-potential second; clustering by shared scope is a tie-breaker only. A blocking item runs on its dependency order whatever group it belongs to. Whatever replaces or reshapes the reorder has to preserve that precedence, or blockers get starved invisibly.
**Skipped at processing, 2026-08-06 (the user's call), with three findings banked so the next pass starts here rather than re-deriving them.**
1. **The item's central premise is FALSE, verified by reading the shipped doc.** It says the close reorder "never got that treatment" — the conditional, change-scoped treatment the start-of-processing reorder received. It did. `docs-b/done-plan.md`'s reorder step opens, in bold: *"Conditional and change-scoped — not a full re-derivation every close"*, and carries an explicit branch for the common case: *"still satisfies the principles below -> silent no-op: change nothing, say nothing."* So the cost fix this item expected to propose is already built and shipped.
2. **Therefore the felt cost has an unidentified cause, and designing before identifying it would be building on the same broken premise twice.** Three candidates, not separable from a planning session: the rule exists but is not being *followed* at closes; the expense is really the close as a whole and the reorder is merely its visible part; or per-item ordering is genuinely the wrong unit and the grouping idea stands on its own merits, independent of cost. **The item could not be kept because none of these lets you say what would be built** — the keep-step's own test.
3. **The cheap next move is one observation, not research: at the next close, notice whether the reorder actually no-ops or walks the whole queue anyway.** That single data point splits the three candidates, and it costs nothing extra because the close happens regardless. Do that before designing anything here.
**The grouping idea was NOT rejected, and the re-offer instructed above was carried out on 2026-08-06 — which produced a sharper idea than the one this item was carrying.**
**Reframed by you at that re-offer: group by SHARED SCOPE, not by priority.** The item as captured was about *priority* groups — named themes the user refreshes. What you actually want is **scope** groups: work items whose Files lists overlap, gathered so the group **moves into Processed together and runs as one unit**. That is a different proposal and a better-founded one, so the item is retitled to it and the priority framing is kept below only as the history it is.
**Why it is worth building.** Items touching the same files build better together — one coherent pass over a file instead of three sequential ones — and if they move into the ready section as a unit, that is one decision instead of three. The method already half-agrees: scope-sibling clustering exists today, but only as a **tie-breaker applied where it is free**. This promotes it to the unit that work actually moves in.
**The risk the design must defeat, and it is the exact thing the existing tie-breaker bound was written to prevent.** If a group moves as a unit, an undesigned item rides in on a well-designed sibling's coat-tails because it happens to touch the same file. **Sharing a scope is not evidence that work is ready.** So the design must let the *group* move while every item in it still passes the keep-step on its own merits. The grouping changes the packaging, never the standard — and that sentence should survive into whatever is written.
**The precedence below is unchanged and must survive intact:** dependencies first whatever the file scope, unblock-potential second, grouping third. Otherwise a blocking item is starved because it belongs to a different group, invisibly.
**Two things this reframe closes.** The cost question is dead — finding 1 below establishes the close reorder is already conditional and change-scoped, and this item no longer rests on cost at all. And the "what are priorities" question is dropped rather than answered: groups are drawn from shared scope, which is mechanically derivable from Files lists, so nothing has to be named or refreshed by hand.
**Live evidence from the same session, worth carrying:** the start-of-processing reorder did fire conditionally and correctly — it moved exactly one item, the one whose relationship had changed, and left the rest alone. So the conditional machinery behaves as documented at that end, which is further reason the cost framing was the wrong one.
**Carried across from [next-start-order-handoff] when that item was deleted on 2026-08-06, because this reframe is what dissolved it.** That item wanted /plan to file a forward handoff carrying per-item placement rationale, so /next could *explain* the order rather than infer it. Under scope grouping there is no per-item placement rationale to carry: **the ordering unit is the group, and the group's reason is that its items share a file scope — derivable from the Files lists at the moment the run is presented.** So when /next presents a run, naming the group and its shared scope **is** the explanation: one line, derived on the spot, with no handoff artifact and no second advisory lifecycle to maintain. Build that line as part of this item. It must stay one line: the run-presentation step is `[BRIEF]` and carries a deliberate passage forbidding caveats, warnings and proposed pause points there, on the stated grounds that the expense is not the warning but the negotiation it invites.
**Prior art to read before designing, not after.** The reorder's rationale is recorded across `LOG/2026-07-14-plan-reorder-by-unlock-potential.md`, `2026-07-27-plan-processing-flow-redesign.md` and `2026-07-29-plan-step3-shadow-close.md`, and both earlier cost items ([reorder-cost-vs-dependency-model], [plan-close-reorder-latency]) have already shipped or been dropped, so their reasoning survives only in those entries. Read them so this doesn't relitigate a settled trade-off without knowing it.
**Files:** `docs-b/plan.md` (the keep-step, where scope siblings are grouped and where the readiness standard must be restated as per-item), `docs-b/done-plan.md` (the close reorder, which becomes group-aware), `docs-b/plugin-behaviour.md` (dependency ownership — the precedence list, promoting clustering from tie-breaker to unit while keeping dependencies first), `scripts/reorder_queue.py` if moving a group needs an invocation of its own, `SPEC.md` (user-visible ordering behaviour), plus a FAQ entry. Relates to [merge-and-fold-without-asking], [prioritise-without-reorder] (which settles the session-local-order question and carries the 2026-08-05 divergence instance, relocated there when [plan-session-order-diverges-from-file-order] was deleted on 2026-08-06 — **read it before designing here, since it decides that a session may work its own order and that the file holds the durable default**).

#### Claude volunteered memory as a destination instead of the project docs — correct by the rule, and nothing in the project can see what landed there [memory-destination-invisible-to-the-record]
Captured by you 2026-08-05, from a screenshot of a close in another project running the method. Recorded as an observation to settle at processing, not as a verdict — the behaviour may be exactly right, and the interesting part may be what it exposes rather than what it did.
**What happened.** At a close, Claude surfaced a discovery — a local build tool's daemon producing a loopback error that reads as a firewall block — and reasoned that it is a fact about **the machine**, not about that project: any project running a similar local build tool could hit it and get the same misleading diagnosis. It noted the fact was recorded in that project's CLAUDE.md, "which is right for this project, but a session in another project would never see it", and offered to save it to memory as "the one place that travels across all your projects". It asked rather than acting.
**On the rule as written, that is correct, and the reasoning matched the rule almost exactly.** The memory-boundaries rule says memory must never hold the project's records — behaviour observations, design decisions, project state, procedure gaps — and that it is a good home for what no project doc owns: user preferences, working style, **cross-project facts**. A machine-level tooling fact is the paradigm case of the last one. It also asked instead of writing, which is the shape the method wants.
**So the finding is not misbehaviour; it is a visibility gap the rule creates and does not address.** Memory does not travel with the project and the user cannot read it. The rule accepts that for things no project doc owns — but it never asks what happens when such a fact **later turns out to matter for this project**. Nothing in the queue, the spec or the log points at it, and no session can enumerate what memory holds. The record has a destination it cannot see into, and the rule licenses writing there without leaving a trace anywhere the project can find.
**Designed at processing, 2026-08-06 — and the abstract worry became a concrete one, because the fact in question was traced and found to be WRONG.**
**The decisive finding.** The fact Claude offered to save was that project's Gradle failure, described as a machine-level problem resembling a firewall block. That diagnosis was corrected two sessions later, and the corrected write-up contradicts it on **both** counts: *"it is not the machine, not a firewall"*, and *"It is specific to Claude, not to the machine."* The real cause is the Claude desktop app's MSIX sandbox ([desktop-app-sandbox-blocks-local-toolchains]). So the candidate cross-project fact was false in exactly the dimension that made it look cross-project in the first place.
**That is what turns this from a bookkeeping question into a correctness one.** Had it been saved, memory would now hold a false claim about the user's machine, presented to every future session in every project — and **the user cannot read memory to notice, and no session can enumerate it to correct.** Every other destination in this method is correctable because it is findable. Memory is the only one that is not, which means it is the one destination where being wrong is permanent by default. The against-argument the capture raised — that a LOG trace duplicates something deliberately routed elsewhere — is answered by this: the trace is not a copy of the content, it is the thread back to a fact that may later need correcting.
**What gets built, two parts.**
1. `docs-b/done.md` — a memory write that arose inside a project leaves **one line in that session's LOG entry**: that a cross-project fact was saved, and roughly what it concerns. **Never the content** — the content lives in memory, and duplicating it would re-create exactly the double-record the boundary rule exists to prevent. Enough that a human reading the log can see the destination was used and go looking.
2. `docs-b/plugin-behaviour.md` — **make "ask before writing to memory" an explicit rule.** Checked at processing: the memory-boundaries section says what memory must never hold and what it is free for, and says nothing about asking. The observed session asked anyway, which was good judgment — but good judgment is not a mechanism, and this is precisely the destination where the user cannot audit the outcome. Placed on the existing boundary rule so it inherits its why.
**One sharpening the incident argues for, to weigh at build rather than assume.** The falsehood here was a **diagnosis** — a conclusion about why something failed — and diagnoses are exactly the kind of "cross-project fact" that looks most worth saving and is least likely to have been verified. Whether the rule should say something specific about provisional diagnoses, or whether the trace line plus the existing it-ran-versus-it-worked rule already covers it, is a judgment for the build. **Do not let it grow into a general prohibition on saving anything uncertain** — that would recreate the too-strong blanket rule this method already retired once.
**Do not turn this into a prohibition on memory.** The boundary rule's own history is that a blanket "not to memory" was found both too strong and too weak, and was deliberately replaced. This item is about visibility, not about restricting the destination.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md` (the memory-boundaries rule — the ask, and the routing section), `plugin/si-plugin/docs-b/done.md` (the LOG trace line at the close). No SPEC change — this constrains where Claude writes and what the close records, not a described feature; a FAQ entry only if the trace line is something a user would meet and wonder about. Docset A is frozen and this is new behaviour, so docs-b only. Relates to [desktop-app-sandbox-blocks-local-toolchains] (the fact that was nearly saved, and was wrong), [false-positive-recorded-as-settled-capability] (the same wrong diagnosis promoted into a record — read together, since one is this failure into a project doc and the other is it into memory), [no-standard-names-for-workflow-docs-vs-user-files].

#### Gitignore the scaffolded `FAQ/` folder by default, since it's method material rather than the user's project [gitignore-scaffolded-faq]
Captured by you 2026-08-04 during a build. /setup copies `templates/faq-template.md` and `faq-index-template.md` into every adopted project as `FAQ/faq.md` and `FAQ/index.md`. Those files explain how the *method* works — they are not part of what the user is building, and they get committed into the user's own repo where they read as clutter.
**Designed at processing, 2026-08-06. The framing changed, and that change is what makes the build correct rather than merely tidy.**
**The FAQ is not a document in the project; it is a LOCAL COPY of something that ships with the plugin.** That reframing decides everything else: a copy is not committed, and a copy must be regenerable. Stating it as "gitignore the folder" alone would deliver the first half and silently break the second. It is also the same conclusion [faq-backfill] reached for this project's own copy — that it is a mirror refreshed from its source rather than an independently maintained document — arrived at from the opposite direction, which is worth noting as corroboration rather than coincidence.
**The guarantee that must ship WITH the ignore rule, or this is a regression.** If the FAQ is not in the user's history, a fresh clone on another machine has no FAQ at all — and the FAQ is what `session_start` points every session at. So the rule is **"never committed, and restored whenever absent"**, not "add a line to `.gitignore`". Recorded this emphatically because the ignore line is the obvious half and the restore is the half that would be dropped.
**The scope question is answered: the argument reaches the FAQ and nothing else.** SPEC.md, QUEUE.md and LOG/ are the user's own record and belong in their history; their CLAUDE.md is theirs too. The FAQ is the only scaffolded artifact that is purely an explanation of somebody else's tool, so there is no slope here to slide down. Answered explicitly so a later pass does not re-open it as "should we ignore the other scaffolding too".
**An already-committed FAQ needs a real action, not a rule.** Adding a `.gitignore` entry does nothing to files git is already tracking. /setup should detect that case and **offer** the untracking (`git rm --cached FAQ/`, which removes it from tracking while leaving the files on disk) rather than doing it silently or leaving the job half-done — it changes what is in the user's repository, so it is theirs to approve.
**This project is the exception and needs NO mechanism to protect it.** Here the FAQ *is* the product. But a self-hoster arrives by cloning the repo, not by running /setup, so the rule never fires in this project at all — the two-doors seeding design already covers it. **Written down so nobody adds a special case guarding against something that cannot happen**, and so that if [self-hosting-auto-detection] ever ships, its suppression list does not acquire this by reflex.
**What gets built.**
1. `docs-b/setup.md` — the scaffolding step writes the `FAQ/` entry into the project's `.gitignore` (creating the file if absent, and not duplicating an existing entry); restores `FAQ/faq.md` and `FAQ/index.md` from the shipped templates whenever they are missing, not only at first adoption; and detects an already-tracked FAQ, offering the untrack with a plain-English explanation of what it does and does not change.
2. `templates/CLAUDE-TEMPLATE.md` — one line recording that the FAQ folder is a local copy of the plugin's own help, not the user's work, and is deliberately not committed. Without this the ignore rule looks like an accident to anyone who finds it later.
3. `SPEC.md` — /setup's scaffolding behaviour is described there, and this changes what a new project gets. Plus a FAQ entry, since a user who notices their FAQ is untracked deserves the reason.
**Files:** `plugin/si-plugin/docs-b/setup.md`, `plugin/si-plugin/templates/CLAUDE-TEMPLATE.md`, `SPEC.md`, `templates/faq-template.md` + `faq-index-template.md`. Docset A's `setup.md` is **the one file the freeze excepts** — a /setup question or behaviour must change in both docsets or neither — so this likely lands in A as well, **authored fresh in A's register, never pasted from B**; settle it explicitly at build. Relates to [faq-backfill] (the mirror conclusion this matches), [faq-as-claude-readable-reference] (unaffected — the files stay on disk and readable either way, which that item already notes), [self-hosting-auto-detection].

#### Detect self-hosting at session start and suppress the consumer-only machinery that misfires in the method's own project [self-hosting-auto-detection]
**The open crux is answered, 2026-08-06 — by this session's own startup line rather than by argument.**
**The question was: what would detection switch on that ambient CLAUDE.md cannot?** The item listed three candidate payloads and called the trigger payload-less. The answer is the second candidate, and it now has an observed instance instead of a hypothesis.
**The instance.** The 2026-08-06 planning session opened with `session_start` reporting *"Plugin version changed since this project was last set up (1.12.0 → 1.19.0-test1) — an update has been installed."* That check exists for a consumer, to tell someone building their own app that their plugin updated. In this project it is **meaningless and permanently wrong**: `.si-version` records 1.12.0 while `plugin.json` carries 1.19.0-test1, and the two can never agree again, because this is the project that produces the versions. Every rezip widens the gap by design, so the line fires at the top of every session, forever, carrying no information.
**And it is not alone.** The missing-setting top-up is the same family — [setting-topup-mid-close] records it firing in this project and in another, soliciting a setting the method has since retired. Consumer machinery running where it does not apply is the pattern, not one stray line.
**Why this is the payload that earns detection, stated plainly because the item's whole difficulty was that nothing did.** Ambient CLAUDE.md is prose; prose cannot stop a program printing a line. Every other candidate payload — a self-hosting banner, guarding host/target confusion — is information that could simply be written in CLAUDE.md, so none of them needs detection. Suppression does, because the thing being changed is a hook's behaviour.
**The safeguard the item already carries, and it stays.** On the trigger firing (the plugin package living in the repo the session opened in), `session_start` checks for a recorded self-hosting marker. If absent, ask **once** — "this looks like you're developing the method itself; is that right?" — and record the answer: confirmed (self-hosting behaviours on) or declined (never ask again). If the marker is present, act silently on its value. The recorded sign is what turns a would-be per-session nag into a single ask, and it cleanly covers the detected-but-not-wanted case.
**What gets built, both limbs.**
1. `hooks/session_start.py` — the detection (the plugin package living inside the opened project), the one-time ask and its recorded marker, and the suppression itself: with self-hosting confirmed, the version-drift check and the missing-setting top-up do not run. Suppressed means silent, not "reported as suppressed" — the behaviour rules are explicit that a silenced check contributes nothing to the opening narration, including no mention that it was silenced.
2. `templates/CLAUDE-TEMPLATE.md` — nothing, deliberately: the marker belongs to the self-hosting project, which is seeded by cloning the repo rather than by /setup, so a consumer's scaffold must not carry the field. Recorded because the instinct is to add it to the template.
**Deliberately still out of scope: the seeding question.** How a self-hoster gets the rules is already solved by two doors — a self-hoster clones the repo and inherits CLAUDE.md; a consumer runs /setup and gets a scaffold carrying none of the self-hosting rules. Detection's value is not seeding, and this build must not reopen it.
**Files:** `plugin/si-plugin/hooks/session_start.py`, `SPEC.md` (the startup checks are described there as user-visible behaviour), plus a FAQ entry. Relates to [setting-topup-mid-close] (the second misfiring check, and the user's lean toward retiring that mechanism outright — read them together, since retiring it would remove half of what this suppresses), [self-hosting-path-portability].
**History below — the original capture and its open crux, now answered above.**
Captured by you; split from [self-hosting-reproducibility] on 2026-07-29 when its concrete portability half became [self-hosting-path-portability]. Open, undesigned question: today self-hosting is *ambient* — CLAUDE.md auto-loads so a cloner gets the rules, but nothing *detects* "this is SI developing SI" as a distinct mode. Should there be a dedicated detection signal (e.g. a hook that sees the plugin package living in the repo it's opened in → self-hosting), or is ambient CLAUDE.md enough?
**Gating premise dropped (user, 2026-08-01):** no longer deferred until "a genuine second developer needs it" — that condition is unpredictable, so gating on it means the item is never ready until it's already too late. Worth designing on its own merits.
**The open crux — trigger has no payload yet.** The item names the trigger (a hook noticing the plugin package lives in the repo it's opened in) but not what detection would *do* that ambient CLAUDE.md can't already. Candidate payloads to weigh at design: a session_start self-hosting banner (host/target distinction is live; rezip to test changes); suppressing/altering consumer-only machinery that misfires in the dev project; actively guarding host/target confusion. Detection only earns its keep if it switches on a concrete behavior ambient loading can't.
**One concrete payload the user named (2026-08-01):** self-hosting awareness should at minimum carry the understanding that **the consumer-facing FAQ (the shipped template) and the project's own FAQ are one and the same in self-hosting** — they are not two separate artifacts here. Relates to the FAQ-freshness thread and [self-hosting-path-portability], [migrate-skill].
**Seeding is already solved — it's not what detection is for (clarified 2026-08-01).** How the host Claude gets self-hosting instructions was murky; resolved into **two distinct seeding paths that must not be conflated.** (1) A **self-hoster** developing the method gets everything by **cloning/forking the repo** — CLAUDE.md is committed in the SI repo (it's what loads in a dev session), so they inherit SPEC/QUEUE/LOG/plugin/CLAUDE.md and therefore **do not run /setup** (nothing to scaffold). (2) A **consumer** building their own app installs the plugin and **runs /setup**, which scaffolds a fresh CLAUDE.md from CLAUDE-TEMPLATE.md carrying **none** of the self-hosting rules. So repo-clone seeds self-hosting; /setup seeds a consumer — two doors. This is *why* "host-only by residence in CLAUDE.md" works for host-only rules like [faq-sync-hard-gate]: the rule lives in the repo's CLAUDE.md, present only in clones-for-development, absent from a consumer's scaffold, so it sleeps for consumers with no detection needed. **Consequence for this item:** detection's value is NOT seeding (the clone handles that) — it's only switching on behaviors CLAUDE.md prose can't express (the open payload question above).
**Anti-nag design — a recorded acknowledgement sign (user, 2026-08-01):** if detection is built, it must not nag. Follow the one-time-acknowledgement pattern the method already uses (working-mode, editor fields): on the trigger firing (the plugin package living in the repo it's opened in), session_start checks for a **recorded self-hosting marker**. If absent, ask **once** — "this looks like you're developing the method itself; is that right?" — and record the answer: **confirmed** (they forked; self-hosting behaviors on) or **declined** ("I'd rather do it my own way"; record and never ask again). If the marker is already present, act silently on its value. The recorded sign is what turns a would-be per-session nag into a single ask, and it also cleanly covers the "detected the shape but the user doesn't want self-hosting mode" case.

#### Send the behaviour rules to unadopted folders too, and give /setup the precedence line its siblings carry [setup-runs-outside-behaviour-rules]
**The premise the capture flagged for checking has evaporated, confirmed at processing 2026-08-06 by reading the hook.**
**The exemption's stated reason is obsolete.** Both docsets' `setup.md` justify the omission by saying the behaviour rules "aren't loaded yet" because /setup runs before adoption. That described the **old delivery model**, where `session_start` inlined the rules into the session's context. It no longer does: the hook now emits a directive telling the session to **read** `plugin-behaviour.md` from the installed plugin. That file ships with the plugin and is readable whether or not anything has been adopted. So the rules are not absent because they are unavailable — they are absent because the unadopted branch does not point at them.
**And the same branch already accepts this exact argument for a different directive.** `session_start.py`'s unadopted branch deliberately emits the **docset directive**, with the reason written into the code: /setup lives in both docsets, so the redirect has to reach an unadopted folder or a 5-series session would scaffold from docset A while every later session ran docset B. That is the identical reasoning, already agreed, one directive away. This item is applying it to the rules directive rather than proposing anything new.
**Why the ungoverned window matters more than its size suggests.** It is the moment a folder is adopted and files are created — the highest-consequence moment in the method, and the only one a brand-new user ever sees. The response-shape tags in particular are plainly applicable there: a non-coder meeting the setup interview needs them more than anyone.
**What gets built.**
1. `hooks/session_start.py` — emit the behaviour-rules directive in the unadopted branch alongside the docset directive already there.
2. `skills/setup/SKILL.md` — add the precedence line its three siblings carry (the plugin-wide rules govern above the skill's own procedure).
3. `docs-b/setup.md` — remove the "aren't loaded yet" claim, which is now untrue, and state instead that the rules are read like any other session's.
**Two things kept deliberate rather than tidied away.**
- **/setup's local rules stay** — leave the user's content untouched, never overwrite, never blind-rename, use the user's words verbatim. They are not replaced by inheritance. They are specific to adopting a stranger's folder, and belt-and-braces is the right posture at the highest-consequence moment. Recorded because "these are now redundant" is the tempting and wrong read.
- **No subset of the rules is carved out as pre-adoption-inapplicable.** Rules referring to docs that do not exist yet are harmless; the communication and safety rules are exactly what that moment needs.
**The freeze call this carries, to settle at build rather than assume.** Docset A's `docs/setup.md` makes the same now-untrue claim. CLAUDE.md's freeze bars development but permits correction, and names **setup.md as the one file the freeze excepts** — a /setup question must change in both docsets or neither. This is correction-shaped (the claim is false against the shared hook), so it likely lands in both, **authored fresh in A's register, never pasted from B**. Decide it explicitly at build.
**Files:** `plugin/si-plugin/hooks/session_start.py`, `plugin/si-plugin/skills/setup/SKILL.md`, `plugin/si-plugin/docs-b/setup.md`, and `plugin/si-plugin/docs/setup.md` subject to the freeze call above. `SPEC.md` if the startup behaviour it describes changes. Relates to [session-start-payload-oversized-and-misordered] (the delivery change that dissolved the premise), [docset-a-completion-ask-inconsistency] (the same freeze question from another angle).
**History below — the original capture, whose premise is resolved above.**
By Claude, from the scope-lock audit at `resources/research/scope-lock-audit.md` (§7.4); filed 2026-08-04.
**The finding.** `skills/plan/SKILL.md`, `next` and `done` each carry a line stating that the plugin-wide behaviour rules govern the skill above its own procedure. `skills/setup/SKILL.md` omits it, and both docsets confirm the omission is deliberate — `docs/setup.md` and `docs-b/setup.md` both say the behaviour rules "aren't loaded yet" because /setup runs before a project is adopted.
**Why it is worth weighing rather than accepting.** The ungoverned window is the one where a folder gets adopted and files get created — the highest-consequence moment in the method, and the only one a brand-new user ever sees. /setup does compensate, with its own local rules: leave the user's content untouched, never overwrite, never blind-rename, use the user's words verbatim. But those are **re-derived locally rather than inherited**, so the compensation is a snapshot. Any behaviour rule added from here on reaches /plan, /next and /done and silently does not reach /setup.
**The premise should be checked, not assumed.** "The rules aren't loaded yet" describes the *old* injection model, where session_start inlined the rules into an adopted project's context. If [session-start-payload-oversized-and-misordered] replaces that with an instruction to read the rules file, then the rules become readable by any session at any time, adopted or not — and the stated reason for the exemption may simply evaporate. Settle that item first, or at least read this against it.
**What to weigh at design.** Whether /setup should carry the precedence line unconditionally; whether some subset of the rules is genuinely inapplicable pre-adoption (response-shape tags plainly are not — a non-coder meeting the setup interview needs them most); and whether the locally re-derived rules should be replaced by inheritance or kept as a deliberate belt-and-braces.
**Files (rough):** `plugin/si-plugin/skills/setup/SKILL.md` (the precedence line), `docs-b/setup.md` (the stated exemption and its local rules), `docs/setup.md` is frozen but carries the same claim — decide under [docset-a-completion-ask-inconsistency]'s freeze question. Relates to [session-start-payload-oversized-and-misordered], [docset-a-completion-ask-inconsistency].

#### Re-inject the behaviour rules after a compaction, so a compacted session doesn't run ungoverned with nothing noticing [no-session-end-or-compaction-hook]
**Narrowed at processing, 2026-08-06. The confirmation this item demanded has already been done, and the session-end half is rejected.**
**The events are confirmed** — `resources/research/claude-code-hook-events.md`, 2026-08-05. The set is larger than the capture assumed: `Stop`, `StopFailure`, `SessionEnd`, `PreCompact` **and `PostCompact`** all exist, and the plugin registers only `SessionStart`, `PreToolUse` and `PostToolUse`. **`PostCompact` was unanticipated by the capture and is probably the right home**: re-injecting *before* a compaction only hands it more material to discard, whereas restoring *after* one puts the rules back into the context that survived.
**The prerequisite that stays load-bearing, carried verbatim from that research: "the event exists" and "the event can act" are different claims.** Blocking and output capability were not checked per event beyond `SubagentStart`, which is the standing proof of the distinction. **Read the documentation for whatever event this builds on, and confirm it can actually inject context, before designing against it.** This is the same failure the method has already paid for twice — a capability built on an input the documentation declined to guarantee, and a hook whose output the harness silently discarded.
**Why the compaction half is the serious one, and worse than the capture makes it sound.** Every guarantee this plugin offers rests on the behaviour rules being in context. If a compaction drops them, the session keeps running and keeps looking correct — it is simply no longer governed, and **nothing anywhere detects it.** That is precisely the shape of the failure where `session_start` emitted a payload the harness discarded for weeks while sessions appeared to work fine, because Claude reconstructed plausible behaviour from other context. The method already treats *state* loss as expected — the unconditional `_build.md` re-read exists for it — but has no equivalent for *rule* loss.
**The session-end check is REJECTED, with its reason, so it is not re-proposed.** A `Stop` or `SessionEnd` hook asking "did you leave anything unrouted?" is exactly the end-of-session nag the method has spent months removing — the same family as the completion asks, the repeat lift-condition questions, and the top-up ambush. "Nothing unrouted survives a session" stays an instruction. If evidence ever shows work actually being lost this way, that is a new capture with real grounds; today there is none.
**What gets built.**
1. `hooks/hooks.json` — register the compaction event chosen after the capability check above.
2. A new hook script, or an extension of `session_start.py` if the re-injection can reuse its payload builder — emitting the same read-the-rules directive the session start sends, plus the short load-bearing state lines (active docset, an uncleared red flag, the build stamp) that the payload ordering already places ahead of bulk text for exactly this survivability reason.
3. `SPEC.md` — the hooks are described there; a fourth registered event is user-visible behaviour. Plus a FAQ entry only if the user would ever see it fire.
**Files:** `plugin/si-plugin/hooks/hooks.json`, a new hook script or `plugin/si-plugin/hooks/session_start.py`, `SPEC.md`, FAQ templates if user-visible. Relates to [session-start-payload-oversized-and-misordered] (whose payload ordering this reuses), [harness-misbehaviour-diagnosis-order] (the check-the-documentation rule this build must obey), [post-close-tail-state].
**History below — the original capture, whose event list was incomplete and whose session-end half is rejected above.**
By Claude, from the scope-lock audit at `resources/research/scope-lock-audit.md` (§7.5); filed 2026-08-04.
**The finding.** `hooks.json` registers `SessionStart`, `PreToolUse` and `PostToolUse` and nothing else. So two things the method treats as guaranteed are guaranteed only by instruction: that nothing unrouted survives a session, and that a session's rules survive compaction. Claude Code exposes `Stop` and `PreCompact` events; the plugin uses neither.
**Why compaction is the sharper half.** The method already treats memory loss as the expected case — the unconditional `_build.md` re-read exists precisely because a session may remember nothing. But compaction can also drop the *rules*, not just the state, and nothing detects that. This is the same family as the truncation finding: a rule that is no longer in context is not a rule, and today nothing checks either at delivery or after compaction.
**What to weigh at design.** Whether a `PreCompact` hook should re-inject the load-bearing state lines and the rules pointer (cheap, and the natural pairing with whatever [session-start-payload-oversized-and-misordered] settles); and whether a `Stop` hook should check for unrouted work, or whether that is the kind of end-of-session nag the method has deliberately removed elsewhere. **Confirm both hook events exist and what payload they carry before designing** — [harness-misbehaviour-diagnosis-order]'s rule, and this item asserts a capability from an audit reading rather than from the documented contract.
**Files (rough):** `plugin/si-plugin/hooks/hooks.json` (new event registrations), a new hook script or an extension of `session_start.py` if the re-injection reuses its payload builder. Relates to [session-start-payload-oversized-and-misordered], [harness-misbehaviour-diagnosis-order], [post-close-tail-state].

#### Name the post-/done tail in the close routing, so a session stops inferring which close applies [post-close-tail-state]
**Narrowed at processing, 2026-08-06 — this item had two halves and one of them is already fixed.**
**The safety half is CLOSED, and must not be reopened.** The capture below says the post-close tail runs with the file scope-lock fully off, so any file is editable and the user is never told. That was true when it was written on 2026-08-01. It stopped being true on 2026-08-04, when `pre_tool_use.py` gained its planning-session file gate: with no active build there is no agreed file list, so a write outside the files planning touches by design (QUEUE.md, SPEC.md, LOG/, the session's own notes, and the always-editable memory / research / scratchpad dirs) returns **ask** — never deny. The hook's own note gives the intent: *"visibility, not containment: such a write should not be stopped, it should be impossible to make unremarked."* A post-close tail has no active build, so it is covered like any other unbuilt stretch. Verified by reading the hook at processing rather than inferred from the commit. **Stated precisely, because the imprecise version invites a wrong reopening: containment is still off after a close — nothing is blocked — but visibility is now on. The item's actual complaint was that the tail had neither.** Whether an ask is *sufficient* protection is a different and much smaller question, and no evidence yet says it isn't.
**What remains is the naming half, and it is genuinely small.** `docs-b/done.md` does not name the post-close tail in its close routing, so a session reaching the end of one has to work out from scratch what kind of session it was and which close applies — today it lands on "a session that changed only method docs" by inference. Name the case explicitly so the routing states it instead of the session deriving it.
**The light-cue limb, kept bounded.** At most a one-line awareness cue that captures filed after the commit are on disk and will ride the next session's commit. **It must never read as "you must run /done again"** — the capture below is emphatic on this and the user was explicit: durability is already handled by the captures-filed-after-commit mechanism, and this item exists to let the tail happen *gracefully*, never to add a re-close requirement.
**What gets built, both limbs.**
1. `docs-b/done.md` — the close routing names the post-close tail as its own case, with the one-line cue attached and the never-a-prerequisite bound stated beside it so a later edit doesn't harden it.
2. A FAQ entry only if the cue ships as user-visible text; no SPEC change, since the described behaviour is unchanged — this names a case the docs already handle by inference.
**One insight carried across from [done-delta-close] when that item was deleted on 2026-08-06, because it is worth keeping and this is its proper home.** That item proposed a second, cheaper close path, and its hardest open question was *detection* — how a session would know it had already committed this session, which was going to need a session marker or a git-log comparison. **Naming the tail state dissolves that question rather than answering it.** If a cheaper close is ever wanted, it hangs off this named state as a *behaviour*, not as a second close path with detection machinery of its own. Recorded here so the idea survives without a queue item, and so nobody rebuilds the detection. It stays bounded by the never-a-prerequisite rule above: a lighter close is a thing the user may ask for, never a thing the method requires.
**Files:** `plugin/si-plugin/docs-b/done.md`, and `templates/faq-template.md` + `faq-index-template.md` if the cue ships. Relates to [shell-writes-bypass-file-scope-lock] (the other scope-lock hole, still open — shell writes bypass both rules).
**History below — the original capture; its safety half is superseded by the section above.**
Captured by you — filed after `fa2f8e5`. Observed live 2026-08-01: after a /done committed (`ea272f6`), the session ran a long tail — six captures filed to QUEUE.md, all uncommitted — and the user briefly lost track of being post-close. The method neither names nor handles the post-close tail. (This capture is itself a post-close capture — the same state, live.)
**Durability is already handled — this is NOT about forcing another /done (user, 2026-08-01).** Post-close captures are written to QUEUE.md, so they are on disk and not lost, and the existing mechanism already picks them up: a capture filed after the commit rides the next session's commit and converges into that session's LOG (plugin-behaviour.md captures-filed-after-commit; done.md). The design intent is to let the post-close tail happen *gracefully*, leaning on that pickup — never to add a re-close requirement. At most a *light* awareness cue ("these are filed and will be saved by your next session"), which must not read as "you must /done again."
**The two real gaps:**
- **Routing clarity.** What a post-close captures-only tail "counts as" is unclear — done.md routes it to done-plan.md as "a session that changed only method docs," but that's inferred, not named. Name the post-close tail explicitly in done.md's routing so a session doesn't have to reason it out.
- **Scope-lock is off (the safety point).** `pre_tool_use.py` enforces file-scope only while `_build.md` exists; once /done deletes it, the file scope-lock is fully off — any file is editable (git-safety and the subagent-ask still fire; only file containment drops). The user runs the whole post-close tail with no scope containment and isn't told.
**Open design questions (for /plan):** whether scope-lock-off is a bug to fix (re-enable some lighter post-close protection) or just to advise plainly; and how light the awareness cue should be so it never becomes a de-facto re-close prerequisite.
**Files (rough):** plugin-behaviour.md (a post-close-tail rule — the scope-lock-off advisory + at most a light awareness cue), done.md (name the post-close tail in routing), possibly pre_tool_use.py / session_start.py if post-close protection is added, SPEC/FAQ if user-visible. Relates to the captures-filed-after-commit mechanism.

#### Decide when a run may widen its own scope and say so, rather than halting — the scope file was never locked [scope-file-editable-vs-locked]
**The premise is corrected at processing, 2026-08-06, and the correction dissolves the fork as written.**
**`_build.md` is already editable during a build.** `pre_tool_use.py` classes it as a method doc alongside QUEUE.md and LOG/, so a build may write to it; the hook's own denial text says as much — *"only method docs and `_build.md` can be edited"*. So the capture's framing — that this method locks its scope file where the independently-built tool deliberately leaves its own editable — is **wrong about this method**. Mechanically the two designs already agree: the scope file is writable and an expansion lands in the diff where a human can read it. Verified by reading the hook rather than inferred.
**What actually enforces the halt is instruction, not the hook.** The procedure tells a build that needs more files to stop and ask; nothing mechanical stops it appending to its own list. That relocates the whole question from code to behaviour, and it means the change under discussion costs no hook work at all.
**So the real question, and it is a better one than the fork it replaces: when may a run widen itself and narrate it, and when must it genuinely stop?**
```
the candidate, now cheap because the mechanism already permits it:
    a run that discovers it needs a file APPENDS to its own Files: list,
    NARRATES the addition, and CONTINUES — the addition rides into the
    commit, where it is as readable as any other change.

what must still halt (to be settled at build, not assumed):
    a discovery that reveals the item was UNDERSPECIFIED rather than that
    the work grew — the method already separates these two cases, so there
    is an existing seam to hang the distinction on.
```
**The cost the strict reading carries, which is why this is worth doing.** A halt is an interruption, and /next is unattended in practice, so every legitimate scope discovery ends a run that could have continued. This is not hypothetical: CLAUDE.md records the red-flag-states build where /next's self-scoping *"caught it only by halting mid-run, the interruption an unattended run shouldn't need."*
**The other design's cost, kept so the trade stays honest:** it relies on someone actually reading the diff. That is a real dependency, and it is the reason the narration limb is not optional — an expansion that is permitted but unannounced is strictly worse than one that halts.
**What gets built.** `docs-b/next-build.md` — the widen-and-narrate path and the boundary against the underspecified case, written at the existing discovery-versus-underspecification seam. `SPEC.md` — its scope-lock paragraph already says the Files list is an approximation of the real boundary rather than the boundary itself; it needs the widening behaviour stated so the description stays true. No `pre_tool_use.py` change: **the hook was never the constraint, recorded explicitly so this is not reopened as a code question.**
**Files:** `plugin/si-plugin/docs-b/next-build.md`, `SPEC.md`, plus a FAQ entry if the narration is user-visible. Relates to [build-scope-vs-described-work-unenforced] (which observes that staying inside the Files list was never the real boundary anyway — read together), [shell-writes-bypass-file-scope-lock] (the genuine hook-level gap, unaffected by this), [plan-scope-lock-gap-and-emergency-path]. Source for the comparison: `resources/research/scopelock-independent-implementation/`.
**History below — the original capture, whose locked-versus-editable framing is corrected above.**
Captured by you 2026-08-04, from comparing this method's scope-lock against an independently-built one (source at `resources/research/scopelock-independent-implementation/`).
**The fork.** That tool makes its scope file **always editable**, deliberately. Its reasoning: scope expansion should be *visible*, not impossible — an unexplained edit to the scope file shows up in the diff, and its agent instructions forbid silent expansion, so the check is a human reading a diff rather than a hook refusing a write. This method takes the opposite line: `_build.md` and its `Files:` list are set at scope-lock and a build that needs more must halt and ask.
**Why it is a real question and not just a different taste.** Both designs are trying to stop the same thing — a build quietly growing past what was agreed. The disagreement is about *where the catch happens*: at the moment of the write, or at the moment someone reads what changed. This method's answer is the stricter one and it has a cost it has never priced — a halt is an interruption, and `/next` is meant to run unattended, so every legitimate scope discovery ends a run that could have continued. The other design pays a different cost: it relies on someone actually reading the diff.
**What to weigh.** Whether a *recorded, narrated* scope expansion mid-run is safe enough to be permitted — the run appends to its own `Files:` list, says so, and the addition lands in the commit where it can be read — or whether the halt is load-bearing precisely because an unattended run has nobody present to object. Note the method already distinguishes discovery from underspecification when a run finds work outside its scope, so the machinery for a considered mid-run response partly exists. And note this touches the same surface as [build-scope-vs-described-work-unenforced], which observes that staying inside the `Files:` list was never the real boundary anyway.
**Files (rough):** `plugin/si-plugin/hooks/pre_tool_use.py` (whether `_build.md` is writable during a build), `docs-b/next-build.md` (the halt-and-ask path), `SPEC.md` (the scope-lock's described behaviour). Relates to [shell-writes-bypass-file-scope-lock], [build-scope-vs-described-work-unenforced], [plan-scope-lock-gap-and-emergency-path].

#### Document the `/plan` name clash with Claude Code's own plan command, and name the qualified form that works [plan-skill-did-not-engage]
**Diagnosed at processing, 2026-08-06. It was never a silent failure — the harness said what was wrong and the message was read as noise.**
**The cause is in the capture's own evidence.** The 2026-08-02 session's error was *"plan is a UI command, not a skill."* That is Claude Code stating that the bare name `plan` resolves to one of its own built-in commands — it has a plan mode of its own. The plugin's skill was not skipped silently; its short name was taken.
**And the working form is already in use.** The 2026-08-06 planning session was invoked as **`/sovereign-implementer:plan`**, the plugin-qualified form, and the skill engaged normally. So there is a form that works, it is the one the app offers from its own menu, and nothing about the plugin is broken.
**Why it is still worth building rather than closing.** A consumer who types `/plan`, gets a Claude Code UI command, and has no idea why has no route to the answer — and this lands on the **install path**, which the release ritual already singles out as the one path nobody exercises after their first day, so it can break completely and stay broken. Naming the clash and the qualified form costs a paragraph and removes an unrecoverable first-day confusion.
**The detection half is recorded as a limit, not a plan.** The capture asked whether something should *notice* a procedure being followed with no skill around it. As far as this session can tell, the plugin's three hooks fire at session start and around tool use, and none receives a signal naming the invoking skill — so mechanical detection is probably unavailable. **Stated as a belief needing confirmation against the documented hook contract, per the check-what-the-tool-documents rule, and deliberately not designed against.** If a future hook event does expose it, this can be reopened.
**One consequence to carry:** the 2026-08-02 session followed the procedure by reading the doc directly, which worked because the docset directive was in context. That makes that session weak evidence about how /plan behaves when properly invoked — relevant to anything relying on its observations.
**What gets built.** `templates/faq-template.md` + `faq-index-template.md` — an entry naming the clash, the symptom (`plan is a UI command, not a skill`), and the qualified form; and `INSTALL.md` + `README.md`'s install section, where a first-day user actually looks. No code change: the skill is correctly named and the app's own command menu offers the qualified form.
**Files:** `plugin/si-plugin/templates/faq-template.md`, `plugin/si-plugin/templates/faq-index-template.md`, `INSTALL.md`, `README.md`. Relates to [skill-docset-override-unsanctioned] (the sibling observation from the same session-behaviour family), [faq-backfill] (which rebuilds the project's FAQ copy — this entry lands in the template, which that item mirrors from).
**History below — the original capture, whose "should something notice?" fork is answered above.**
Captured by you — observed at this session's own start, 2026-08-02. After a /clear, "/plan" arrived as plain text rather than as an invoked skill; attempting to invoke it returned "plan is a UI command, not a skill." The session proceeded by reading docs-b/plan.md directly and following it, which worked — the docset-B routing directive was in context, so the right procedure was read.
**Why it's worth recording rather than shrugging off:** the method's guarantees assume the skill engaged. Nothing detected that it hadn't, and nothing would have flagged it if the wrong docset had been read instead. It also means this session is weak evidence about how /plan behaves when properly invoked — relevant to [opus5-docset-switch-live-verification], which is meant to confirm exactly that.
**To settle at /plan:** whether this is a harness quirk with no method implication, or whether something should notice that a procedure is being followed without its skill around it. The second reading connects to [drive-testing-signals-skill-routing].

#### [user] Send one test report through flintcraft.tech/report and confirm it actually arrives [report-url-404]
**Cut back at processing, 2026-08-06, from "build the page" to "prove the page works" — the build half is done and three of this item's claims were stale.**
**What was checked, this session, by loading the page rather than asking.** flintcraft.tech/report is **live**. It serves a working form: an optional email box ("only if you'd like a reply"), a project dropdown listing Throughliner / TaskFlow / Something else, a report text area ("paste what Claude captured"), and a Send button. So the "site not republished, page not live" gap recorded below is gone.
**The second recorded gap is judged NOT a gap, and the objection is retired rather than carried.** The item says the form should be a single free-text box and objects to the email field and the dropdown. That misreads what the one-free-form-block rule constrains: the rule governs **what Claude drafts** — one prose block rather than labelled fields, so the user is never reconciling Claude's headings against different headings on the page. The page satisfies that exactly, because the report body *is* one box. The email is optional and sits outside the report; the dropdown routes between projects and is now genuinely useful, since it already lists more than one. **No page change is needed.**
**The third stale claim, and it changes how much this matters.** The paragraph below reasons that the channel blocks nobody because there is exactly one user of the method. That was fair when written. Since then a community member opened a substantial pull request on the public repo ([respond-to-rygel-opencode-pr]), the project's Discord holds five people, and a collaborator has been invited to another of the user's repos. The channel is no longer only theoretical, and a silently-dropping form is worse than the original 404: a 404 tells you it failed, a form that accepts and discards does not.
**So the whole of what remains is one test, and only the user can run it** — Claude cannot submit a form on the user's behalf, and cannot see wherever the submissions land.
**Walkthrough.**
1. Open **flintcraft.tech/report**.
2. In the project dropdown, choose **Throughliner**.
3. In the report box, paste something unmistakably identifiable, so it can be told apart from a real report at the other end — for example: `TEST — ignore. Verifying the feedback channel delivers. 2026-08-06.`
4. Put an email address in **only if you want to check the reply path too**; it is optional and the test works without it.
5. Press **Send bug report**, and note what the page does afterwards — a confirmation message, a redirect, an error, or nothing at all. **What it does is part of the result**, because a user who gets no feedback cannot tell a success from a silent failure.
6. Go to wherever these submissions are meant to arrive — the Netlify site's form submissions, or the notification email if one is configured — and confirm the test is actually there.
7. Report back what happened at both ends.
**Done when** the test submission is confirmed present at the receiving end. If it does not arrive, that failure is the finding, and it becomes a fresh capture describing exactly where it broke rather than being fixed inside this item.
**Completion is user-reported, and that gap is deliberate.** There is nothing observable from here — the receiving end sits behind an account Claude has no access to — so this cannot be checked against the world at the close. It is recorded rather than left implicit, per the `[user]` lifecycle's own note that where nothing observable exists, the item simply waits until the user mentions it.
**History below — the original capture and its stale progress notes, kept for the reasoning.**
Captured by you (2026-07-18), kept as a `[user]` work item 2026-07-27, returned to Unprocessed 2026-08-02. During the feedback channel's first real use, submitting the drafted report at flintcraft.tech/report hit a Netlify "Page not found" 404 — the URL doesn't exist, so no report can be filed at all, whatever its format. This is external website work on the Flintcraft.tech project (a separate site Claude can't build or deploy from the SI repo), so it's the user's to run. With [report-fields-mismatch-webpage] settling the method on one free-form block, the page needs only a single text box + submit. Done when: the page exists at flintcraft.tech/report and a real test submission actually lands.
**Progress found 2026-08-02:** the page now exists in the Flintcraft.tech repo at `site/report.html`, committed and pushed (`edd5e83`), with a working form. Two gaps remain: the site has not been republished, so the page is not live yet; and the form carries an email field and a project dropdown alongside the text box, where the method settled on a single free-text block. So this is closer to done than the original capture suggests, but not done.
**Returned to Unprocessed, deliberately deprioritised (user's call, 2026-08-02).** It sat below the cleared-to-run marker with a user-only lift-condition, which meant /plan's below-line revisit asked about it every single session. The user has repeatedly deprioritised it in favour of the Opus 5 work and was tired of being asked. The only sanctioned way to postpone is the bottom of Unprocessed, so that is where it now sits — not a below-line item with a condition, and emphatically not a "lift when the user raises it" condition, which would be a phantom shelf that depends on the user reading the queue. There is currently exactly one user of the method (the developer), so the broken feedback channel blocks nobody.

--- Cleared to run above this line ---

#### Merge the blitz branch into main once the soak has gone quietly [merge-blitz-2026-08-06-to-main]
By Claude, split out at the 2026-08-06 processing of [retire-docset-a] because that item's dependency turned out to have no durable home.
**Why this exists at all.** The branch `overnight-blitz-2026-08-06` carries the blitz plus the planning sessions that followed it — twenty-plus processed work items, deletions, a preserved session transcript, and the editing-state contract another of your projects is waiting on. `main` is untouched. The merge was tracked only inside a forward advisory, and advisories are transient by design and cleared at a close. Meanwhile the queue refers to `[merge-blitz-branch-to-main]` as though it were a live item; that was the **previous** branch's item and it is gone. So the one action several items wait on existed only in a note built to be thrown away — the under-filing shape the method already names.
**It is Claude-work, not `[user]` work — the tag was checked rather than assumed.** `git checkout main && git merge overnight-blitz-2026-08-06` is a command Claude runs, and `git` is present and working. What is yours is the **greenlight**, which is a decision, not an action — hence below the readiness line rather than tagged `[user]`. This is the distinction the over-tag guard exists for: "Claude can't decide this" is not "Claude can't do this".
**The trade, stated plainly because it has shifted and will keep shifting.** The branch's whole purpose is that you can live on the blitzed state before committing to it. But work done during the soak lands on the branch too, so a late revert discards the soak days' work along with the blitz. That cost was small on day one and is not small now. The balance therefore moves toward merging once the soak has gone quietly, rather than holding the branch open indefinitely to preserve an option that costs more every day it is kept.
**The build:** `git checkout main`, merge the branch, confirm the merge is clean and the queue's structure survives it (section headings, the readiness marker, the item count), push, then re-run the plugin update so the installed host is snapshotted from `main` rather than the branch. If it has **not** earned trust instead, the opposite path is checkout `main`, re-run the plugin update to re-snapshot it, and restart — and that decision is also yours.
**Lift-condition:** you are satisfied the soak has gone quietly — no misbehaviour traced to the blitzed build across ordinary sessions. Mechanically checkable in one respect only, and the revisit should check it: whether the branch still exists un-merged (`git branch --merged main`). Everything else is your judgment. **Greenlight it at any /plan close, or say so at any point.**
**Files:** none in the repo — this is a git operation plus a host reinstall. Relates to [retire-docset-a] (blocked by this), [concurrent-session-support] (whose branch-per-session design this is the second hand-run data point for), [advisory-soak-blitz-branch-then-merge] (the transient note this replaces).

#### Retire the frozen 4.8 docset A once its retirement conditions are met, and update the README accordingly [retire-docset-a]
Captured by you 2026-08-05. You proposed retiring A immediately, on the grounds that docset B is well established; after Claude cited the freeze decision (`d1bfeda`, 2026-08-02 — A frozen as the known-good fallback so a bad migration can never strand the project, recorded there as your non-negotiable), you chose to hold the freeze and file retirement as future work instead. So this item is the retirement, made deliberate: it happens when its conditions clear, not by drift.
**Why not now, recorded so the timing isn't re-litigated:** docset B has only been selected live since 2026-08-04 and the blitz's work is still soaking on an unmerged branch; the measurement harness ([docset-b-register-decision-weaker-than-recorded]) states its safe window is "while A still stands" — retiring A first closes that window before anything has been measured; and A costs no tokens in a 5-series session (only one docset loads), so keeping it is shelf space, not context.
Blocked by: [merge-blitz-2026-08-06-to-main]
**Why that field rather than prose:** the first condition below is "the blitz branch merged and soaked", and at this processing that action had no durable home — it lived only in a forward advisory, which is cleared at a close. It was split out into its own work item at the same moment, so the dependency now points at something real and the queue lint can check it.
**The three conditions, settled at processing 2026-08-06 rather than left open.**
1. **The branch merged and soaked** — now [merge-blitz-2026-08-06-to-main], the `Blocked by:` above.
2. **The harness, and it is SOFT.** The item's own wording allows "a deliberate decision that measuring isn't needed", so this never hard-blocks. What it does mean is that retiring A **closes the measurement window permanently** — [docset-b-register-decision-weaker-than-recorded] states its safe window is "while A still stands", because a register experiment is survivable only when a known-good fallback exists. So the choice at greenlight time is explicit: run the harness first, or decide on the record that the method will never measure this and accept that.
3. **The 4.8 / absent-model question COLLAPSES, and this is the part that was mistaken for design work.** The item reads as though session_start's routing needs redesigning. It doesn't. With one docset there is no choice to make, so the model-detection branch and the default-when-absent branch are **deleted, not rewritten** — every session loads `docs-b/` because there is nothing else to load. What survives is not a design question but a **fact to check at greenlight time**: are there any 4.8 users left? If yes, retiring A hands them a docset authored by subtraction for the 5-series, and the freeze decision says plainly that B's lighter register regresses 4.8's rule-following. That is a cost to accept deliberately, not a thing to design around. The no-strand guarantee is carried instead by the missing-docset state line inherited below.
**The build, once cleared:** remove docset A (`plugin/si-plugin/docs/`) from the package; **delete** session_start's docset-selection branch and its default-when-absent fallback, leaving an unconditional load of `docs-b/`; remove the docset-routing directive's substitution language, since there is nothing to substitute; update CLAUDE.md's Model target section (the two-docset architecture and the freeze rule both stop being true), SPEC.md's two-docsets paragraph, README, and the FAQ's docset entry. Scope the full ripple by grepping the docset literals (`docs/`, `docs-b/`, `docset`) per the hook-enforced-format tracing rule — this is exactly the shape that rule exists for, since the literals are read by a hook.
**Inherit, don't re-derive, the missing-docset behaviour from [session-start-silent-docset-fallback].** That item builds a state line for the case where the selected docset's folder is absent from the installed host. Retiring A removes the fallback that made such a case survivable, so with one docset a missing folder means no procedure docs at all — the state line stops being a diagnostic nicety and becomes the only safe response. Reuse its wording and its host-versus-working-tree distinction rather than writing a fresh one.
**Files:** `plugin/si-plugin/docs/` (removal), `hooks/session_start.py`, `CLAUDE.md`, `SPEC.md`, `README.md`, and the FAQ template pair — final list from the grep. Relates to [docset-b-register-decision-weaker-than-recorded] (the safe window this closes), [structural-delimiting-experiment], [session-start-silent-docset-fallback] (whose logic this inherits).
**Lift-condition:** its `Blocked by:` clears, **and** you greenlight it — the harness has either run or been consciously waived, and the 4.8-user question has been checked rather than assumed. Not a mechanical lift: the last two are judgments, so this rises when you say so at a /plan close, not on a check coming back true.

#### [user] Verify the merged two-section plugin behaves live [merged-plugin-live-verification]
By Claude. The post-merge dogfooding check — one live confirmation across real sessions rather than a pile of deferred observation items, per the standing rule that broken behaviour shows up in use and re-captures itself. **Checks rewritten at the 2026-08-05 planning session** (via [merged-plugin-verification-checklist-stale]): the old list tested for a provenance lint that was deliberately removed, and predated the readiness-marker, orphaned-prose, and inserted-heading checks. Run on the installed host (1.18.0-test1 or later) in real /plan, /next, and /done sessions on the real queue — most checks confirm themselves during the soak's ordinary sessions; only the /setup line needs a deliberate detour:
- **session_start:** an uncleared red-flag work item is surfaced first-thing; the docset directive matches the project's `Model:` field; the installed-version and build-stamp lines arrive.
- **The queue lint** (advisory, after any QUEUE.md edit): runs clean on the real two-section queue, and flags: a `#### ` heading with no `[slug]`; a red-flag marker whose state is neither cleared nor uncleared; a missing section heading; a readiness-marker problem (no marker while Processed holds work, two markers, or a marker sitting in Unprocessed); orphaned prose belonging to no item; a heading newly inserted under Processed while a build is active. It never flags provenance — that is by design, not a gap.
- **/next:** picks the top Processed items above the readiness line and builds several back-to-back; self-scopes _build.md's Files list; treats a missing readiness marker as nothing-cleared (halts rather than building everything); leads a `[user]` item with a live walk-through, one step at a time, with no completion ask; halts when the same error repeats three times on one item; closes a multi-item run with one summary commit.
- **/done:** routes by work-item flavor, names LOG files by slug, cross-checks shipped slugs against Processed.
- **/setup** (in a scratch project if convenient): scaffolds a two-section QUEUE.md plus `resources/research/`.
- Folds in the [device-access-consent] resolution check: the first build needing on-device verification asks permission before connecting.
Host-side — anything found broken routes to a fresh capture.
Set aside · 2026-08-05 · "I can't do that as I'm not at home, I'm on remote control" — reached: the passive checks confirmed themselves in the 2026-08-05 /next run (session_start version + build-stamp lines arrived, docset matched the Model field, multi-item run built top-down with self-scoped Files list, queue edits drew no lint flags, [user] walk-through led with no completion ask); the /done close check confirms at that session's own close — stopped by: the /setup detour needs a fresh session on a scratch folder, which needs the user at their desktop — retry: when the user is back at their computer.

#### Improve SI for concurrent session running [concurrent-session-support]
By Claude at the user's request. The user wants SI made safer for running concurrent sessions — e.g. a /plan and a build at the same time.
**Premise correction (2026-07-29):** /next "only appends" is not quite right — its *close* removes shipped items from Processed and repositions the cleared-to-run marker, so a /next close and a /plan session can both rewrite QUEUE.md structure at once. The contention is real.
**Reframe reached (2026-07-31):** Drive-sync is *Alex's personal environment*, not a method-general blocker. SI's users are mostly not on Google Drive, so for them there is no sync floor and in-repo concurrency is solvable in-method — a structural safeguard is worth building for the majority; Alex covers her own case by pausing Drive during sessions. The old "the hard half is outside SI's control (Drive conflict copies)" framing is Alex-specific and should be corrected — it is NOT a method-general truth.
**Detect-and-warn design (2026-07-31) — rejected, superseded by the merge direction below.** The earlier proposal was detect-and-warn: (1) liveness by working-file mtime; (2) a session_start awareness note; (3) a pre-write stop-and-ask recheck; (4) `.git/index.lock` retry/backoff. Recorded so it isn't re-proposed; replaced by the 2026-08-01 direction.
**Design progress banked (user + Claude, 2026-08-01) — continue next session, still undesigned:**
- **Drop staleness entirely (user).** A session should always be *closed* properly, never left to go "stale" — so detecting staleness by mtime is the wrong model. Design so an abandoned-live session doesn't happen, rather than detecting it after the fact. (Kills proposal #1.)
- **The session_start awareness note (#2) stays** — fine as-is.
- **New core direction — live/canonical queue, merge-at-close (user's idea, better than detect-and-warn).** Each session edits its own *live* copy of the queue; at close, its changes merge into the *canonical* version. **Use git if that makes it easier to build (user approved).** The merge is automatic and clean when two sessions touched *different* items/sections; a conflict arises only when both touched the *same* region. Claude resolves the merge **silently**, surfacing to the user **only** when a conflict genuinely can't be resolved safely — the user never sees raw conflict markers. Reframes the problem from *preventing* concurrent writes to *letting them diverge and merging at the end*. Real design work: (a) structure the queue so sessions rarely collide; (b) make Claude's silent conflict-resolution trustworthy.
- **`.git/index.lock` retry/backoff (#4) — minor robustness only.** Two simultaneous commits make git's brief lock file error one of them; retry-after-a-moment avoids it. A small nicety, not core.
- **Cloud-sync detection (user).** Google Drive / OneDrive / Dropbox syncing the folder is a *separate* failure mode from in-repo concurrency — these make their own duplicate "conflict copies" and file locks. Add a startup check for whether the project folder is being synced, and surface it. (Correctly re-homes the old Drive-specific worry as one instance of a general cloud-sync issue.)
- **Remote-control convergence.** Remote sessions *already* auto-branch (a git worktree); the manual pain has been merging them back. The merge-at-close machinery here removes that pain, which is why the separate worktree-override hook was deleted rather than built (2026-08-05) — its mechanism facts are carried below.
- **Desktop vs remote asymmetry — the open crux for next session.** Remote already forks (branch-per-session free; only auto-merge missing). Desktop shares the one main tree, so matching that isolation means deliberately branching each desktop session, or a separate-file fallback. Fork to resolve: branch *every* session, or isolate only where sessions overlap?
- **Scope distinction to resolve, not conflate (user, 2026-08-01):** full-project branching vs queue-only branching. The contention is fundamentally about QUEUE.md, so branching *just the queue* may suffice; full-project branching (what the remote worktree already does) is the heavier hammer. Fine to scale up to full-project *if* it solves both concurrency and remote-merge neatly — but name the two scopes so a future pass chooses deliberately rather than sliding between them unnoticed.
**Unnamed cost, added 2026-08-04 from the scope-lock audit (`resources/research/scope-lock-audit.md` §8.6):** the merge-at-close direction has Claude silently resolving conflicts in QUEUE.md. QUEUE.md is the artifact that *defines* scope — the cleared-to-run marker sets how much autonomous work is authorised, and the Files list of every item derives from it. So a silent automatic merge is an expansion of agent authority over the boundary itself, not just over a document, and the design above doesn't frame it that way. Weigh at design whether the queue's *structural* lines (the readiness marker, section headers, red-flag states) should be excluded from silent resolution and always surfaced, even when the merge is technically clean.
**A live instance, 2026-08-05 — and it is worse than this item's design discussion anticipates.** Two sessions ran concurrently **in the same folder**: an overnight-blitz session left open, and a planning session. The blitz filed a new capture and its write landed on the *heading line* of an existing item, destroying that item's heading and slug outright and leaving its rationale orphaned under the item above. It reached a commit. The planning session found it only by trying to move the item by slug and getting "not in section Unprocessed".
**Why it changes the design input.** The discussion above frames concurrency as *conflicting edits* to be merged — two sessions touching the same region, resolved cleanly or surfaced. This was not a conflict at all: one session's write simply consumed a structural line of another item, and every layer passed. Git saw a valid diff. The lint saw a valid queue. The commit succeeded. **So "merge cleanly when different regions were touched" is not sufficient protection, because the damage here was to a region nobody was editing.** It strengthens the note already in this item that the queue's *structural* lines should be excluded from silent resolution — and extends that set from the readiness marker and section headers to **item headings**, which are equally structural and were not previously named.
**Two mitigations that are not this item's to build,** noted so they aren't lost: the queue lint gains an orphaned-prose check (folded into [lint-blind-to-readiness-marker], and it earns its place independently of concurrency); and the Edit tool's "file modified on disk since you last read it" warning fired twice during the incident and was dismissed both times as the queue mover's doing — a session-behaviour lesson rather than a mechanism, that the first such warning should stop work rather than be reasoned past.
**Designed 2026-08-05. Both forks are resolved, and one reframe is what resolved them.**
**The reframe: isolation is the fix; merging is the price paid for it.** The live corruption happened in **one folder**, with two sessions sharing a single working tree. Git was never involved and there was no merge conflict, because there was nothing to merge — just two processes writing one file from stale reads. The design above framed the problem as *conflicting edits to be merged*, which put the emphasis on the wrong half and is why the fork stayed open so long. Branching is what prevents the damage; merge-at-close is the cost of having branched, not the mechanism that saves you.
**Fork 1 — RESOLVED: branch every session.** "Isolate only where sessions overlap" requires knowing another session is live. That requires liveness detection, which the user killed on the grounds that a session should always be *closed* properly rather than left to go stale. So the overlap option depends on the one mechanism already ruled out, and cannot be built without reversing that decision. Branch-per-session doesn't: it costs almost nothing when uncontended, because a merge with no divergence is a fast-forward and effectively invisible. **It is free when you are working alone and pays exactly when you are not** — which is the property you want, since concurrency is unpredictable and the protection has to already be in place when it happens.
**Fork 2 — RESOLVED: full-project branching, not queue-only.** Git gives whole-tree branching for free; isolating a single file would need bespoke machinery built for one document. And the premise that "the contention is fundamentally about QUEUE.md" is too narrow: a build session touches hooks, procedure docs, templates and SPEC, and two concurrent builds collide on source files, not just the queue. The planning session of 2026-08-05 alone touched QUEUE.md, its own working file, and would have touched CLAUDE.md. Queue-only branching would leave all of that unprotected while costing an invention.
**A property worth naming, because it is what makes abandoned sessions tolerable without liveness detection: branching converts an invisible failure into a visible one.** Today an abandoned or colliding session corrupts a file silently and every layer passes. Under branch-per-session, the worst residue is an unmerged branch sitting in `git branch` where anyone can see it. A leftover branch is a housekeeping chore; silent corruption reached a commit and was found only by accident. This is the honest answer to "what about sessions that are never closed" — they leave litter rather than damage.
**Consequence, acted on the same session: [worktree-override-hook] was DELETED rather than built.** That hook existed to *stop* remote sessions forking, because merging them back by hand hurt. With the merge automated and forking the design, it solved a problem this removes — and shipping it would have actively defeated the isolation, pointing every session back at the one shared tree, which is precisely how the queue got corrupted. Its mechanism facts were relocated here before it went (see below). The accepted risk, recorded so it is not a surprise: if this design stalls, the original remote-forking pain returns with nothing queued for it — acceptable because the hook was never built, so nothing working was lost, and unmet pain re-captures itself in use.
**The desktop/remote asymmetry dissolves.** Remote already forks a worktree per session; desktop shares one tree. With desktop branching too, both surfaces behave identically and the asymmetry that made this hard stops existing.
**The remaining build question, and it is the one that carries the risk: what happens on a GENUINE conflict.** Most merges will have no conflict at all, so this narrows to real concurrent edits to the same region. The scope-lock audit's warning stands and is sharpened by the live incident: QUEUE.md is the artifact that *defines* scope — the readiness marker sets how much autonomous work is authorised — so silently resolving it is an expansion of agent authority over the boundary itself, not merely over a document.
```
NEVER silently resolved — always surfaced to the user:
    the readiness marker          # sets what /next may run unattended
    section headings              # Processed / Unprocessed
    item headings and their slugs # extended after the live incident, where
                                  # the damage was to a heading nobody was
                                  # editing and every layer passed
    red-flag markers and states

MAY be resolved silently:
    prose-only changes inside DIFFERENT items
    appends to different sections
```
**Mechanism facts relocated here from [worktree-override-hook] when it was deleted, 2026-08-05 — they answer a build question this item already carries.** Claude Code exposes a **`WorktreeCreate` hook event** that replaces default worktree creation: it takes a name and returns a path, so a hook can decide where a new session's tree lives. Confirmed to still exist as of the 2026-08-05 check (`resources/research/claude-code-hook-events.md`); the surrounding constraints are in `resources/research/remote-control-limitations.md`. That is directly useful to the "worktree or plain branches" question below — the same lever that was going to *suppress* forking can be used to *direct* it. Also carried across: remote sessions disconnect unpredictably, which is why the user pre-makes standby sessions; branching does not fix that and must not be described as if it does.
**Still to settle at build:** where the merge runs (the /done close is the obvious home, since it is the one close that always fires); what a session does when it finds its branch already merged; whether `git worktree` or plain branches suit the desktop case better, now informed by the `WorktreeCreate` lever above; and the `.git/index.lock` retry, which stays a minor robustness nicety rather than core. **Cloud-sync detection stays in scope but is independent** — a startup check for whether the project folder is inside a Drive/OneDrive/Dropbox sync root, surfaced not fixed, since those produce their own conflict copies and file locks regardless of any git design.
**Live remote-control evidence, 2026-08-05 (captured by you): what the current shape costs.** A session left open on the desktop could not be continued from remote control — continuing meant either branching, which you avoid because of the stress tied to it, or having a helper at the desktop close the session and open a new one so it would appear in remote control. That close-by-proxy took roughly forty minutes across four relayed stops (the incident is recorded on [close-reliance-on-reply-chips] and its sibling captures). Whatever this item builds must either make the branch path genuinely stress-free or make continuing a session not require one; this incident is the measure to build against.
**Live evidence this is workable, from the same session that designed it.** The branch-per-session shape is being dogfooded right now: this project has run a full blitz and a planning session on `overnight-blitz-2026-08-05b`, and the *first* blitz already merged cleanly into main at `5127f1c` with eighteen items on it. So the pattern has completed a full cycle once, by hand, before being automated. [merge-blitz-branch-to-main] will supply a second data point.
**Lift-condition (placed below the marker at the 2026-08-05 close): held by choice, not by a blocker** — the same standing [history-rewrite-third-party-scrub] and [rename-to-throughliner] already carry. Nothing queued blocks it and the design is settled. What keeps it below is that it rewrites `session_start.py` and changes how **every** session begins, and the run it would otherwise join ends with [merge-blitz-branch-to-main] — so a concurrency build that went wrong would be merged into main automatically, unattended, by the same run that produced it. **Lifts when you open a session for it deliberately, or greenlight it at any /plan close.**
**Files:** `docs-b/done.md` and `docs-b/done-plan.md` (the merge at the close), `docs-b/plugin-behaviour.md` (safe-concurrency rules and the never-silently-resolved list), `hooks/session_start.py` (branch creation, the awareness note, and the cloud-sync check), `docs-b/plan.md` if the branch is created at session start rather than by the hook, SPEC.md + README (user-facing behaviour change), plus a FAQ entry. Relates to [worktree-override-hook] (whose fate this decides), [next-per-item-queue-removal] (which assumed the queue is freed early for other sessions — an assumption branching changes), [build-scope-vs-described-work-unenforced].

#### The `captured by you` credit is unreliable in two ways: it only resolves for one reader, and it gets applied wrongly with nothing to catch it [provenance-credit-second-person-ambiguous]
Captured by you 2026-08-03 — you asked who "you" refers to in a provenance line, then challenged a credit that turned out to be wrong.
The credit means the project's user, and the convention is deliberate: [provenance-labelling-reconsideration] shipped at `ea272f6`, dropping mandatory provenance for an asymmetric default-AI scheme where an unmarked item is Claude's and only a user-raised item carries a credit. **That decision is not in question** — the asymmetry is sound and cheap. Both problems below sit inside it.
**Problem 1 — it resolves only for one reader.** The credit is direct address, so it works only if the reader is the person it was written for. That always holds in a solo project. It stops holding for a repo that goes public ([scrub-gate-before-approval], [setup-repo-privacy-and-publishing]), for a project handed on, or for any second reader — where "captured by you" points at nobody identifiable.
**Problem 2 — and this one is worse — the credit gets applied wrongly, and nothing catches it.** Live instance the same day: [reorder-mover-marker-at-top-blind] carried a `captured by you` credit from filing. It records a script throwing an internal error during a /done close — something only Claude could have seen. The user had no way to observe it. The credit was then *inherited unchecked* through a full processing pass, and only caught because the user happened to say it didn't feel like theirs.
**Why that's the more serious half.** An ambiguous credit is a reading problem; a wrong credit corrupts the record. The queue's purpose is preserving who decided what, and a mis-credit silently attributes thinking to the wrong party — in the direction that flatters the user's involvement, which is exactly the direction nobody will question. It also survives processing, because a rewrite preserves the provenance line as given rather than re-deriving it.
**Measured at processing, 2026-08-05: 40 of the queue's 55 items carry a `captured by you` credit.** Framed carefully, because the number is suggestive rather than diagnostic — it is entirely consistent with being true, since there is one user and they are the developer, so they do raise most things. What it does establish is that the credit discriminates almost nothing in practice: a marker on three-quarters of items tells a reader little, and a single wrong credit is well camouflaged among them. That is the condition under which the known mis-credit survived a full processing pass unnoticed.
**Problem 1 is resolved by the user's decision, 2026-08-05: record a real first name, an alias, or another identifier at /setup, and credit captures to it.** Claude had recommended a one-line queue-header explanation instead, on the grounds that a name writes a person into a repo that may be public. **The user's framing already answers that** — offering an *alias* gives multi-user disambiguation with none of the exposure, so the objection applies to a name-only design and not to this one. The user's reason: "you" breaks down as soon as a project has more than one user, and the header line only ever worked because this project has exactly one.
```
rejected: "captured by the user"  -> unambiguous but colder, and the second
                                     person is part of how the queue reads as
                                     the user's own document
rejected: name-only, no alias     -> writes a real person into every item of a
                                     possibly-public repo
kept:     an identity recorded at /setup — real name, alias, or other, the
          user's choice, with the alias route offered plainly
```
**Three things the build must settle rather than assume.**
1. **The multi-user case the field exists for.** A single stored field records *one* identity, so where two people genuinely share a project, the second person's captures would be credited to the first — the exact failure this is meant to fix, reintroduced. Decide between a per-session confirmation, a field explicitly scoped to "the project's primary user", or something else. Do not leave this to inference.
2. **The ask is a solicited identifier, so it is governed by the private-information rules.** /setup must say plainly, at the moment of asking, that the answer is written into committed project docs — and offer the alias in the same breath. /setup already detects repo visibility, so a public repo should lead with the alias option rather than mentioning it as an afterthought.
3. **What happens to the 40 existing credits.** A new field protects new items and does nothing about those already written. Migration is [provenance-credit-audit]'s territory; this item must not silently assume it happens.
**Problem 2 — a filing-time test, plus re-derivation at the keep-step.** State the test in words: *could the user have observed this unaided?* And require the keep-step to re-derive the credit from the discussion rather than inherit the filed one, because inheritance is precisely how the known bad credit survived processing. No lint — this is a judgment a hook cannot make, so it is a wording fix.
**Files:** `plugin/si-plugin/docs-b/setup.md` (the new question and its privacy framing), `templates/CLAUDE-TEMPLATE.md` (the stored field), `docs-b/plugin-behaviour.md` (the Captures provenance convention and the filing-time test), `docs-b/plan.md` (the keep-step re-derivation), the QUEUE.md header prose in setup.md, SPEC.md (a new user-visible setup question), README feature-list sync, and a FAQ entry. Docset A: setup.md is the file the freeze excepts, so the question must disappear from or appear in both — see [docset-a-completion-ask-inconsistency].

#### Design proactive rationale-nudging: Claude asks for the *why* without nagging [throughline-feature-elevation]
Captured by you (2026-07-30); reframed 2026-08-01. Earlier threads are gone: thread (a) — the why-pipeline → "throughline" rename — folded into [rename-to-throughliner]; thread (b) — user-provenance on rationale — folded into [provenance-labelling-reconsideration] (2026-08-01) for one consistent provenance scheme. What remains is the genuinely-undesigned thread: **proactive rationale-nudging.** At certain moments (a decision made, a route chosen, an item kept), Claude could proactively ask the user for the *why* — actively extracting human intention rather than only recording rationale when it surfaces on its own. Because this is the titular feature — durable rationale that survives a forgetful session — it should be genuinely excellent. **Designed 2026-08-05 from that session's own evidence, which resolved the crux and changed the mechanism.**
**The finding: the productive nudge is a proposal, not a question.** Almost none of that session's rationale came from anyone asking "why". It came from Claude stating a recommendation *with its reasoning* and the user disagreeing. The clearest case is this queue's own [provenance-credit-second-person-ambiguous]: Claude recommended a queue-header line and gave its grounds; the user overrode it with "'you' breaks down when there are multiple users", and that sentence is now the item's rationale. The same shape produced the rationale on [lint-blind-to-readiness-marker], [docset-a-completion-ask-inconsistency] and [merge-and-fold-without-asking].
**Why that reframing dissolves the nagging problem rather than managing it.** A "why do you want that?" question arrives empty and makes the user do the work, which is exactly what nags. A proposal costs the user nothing when they agree — one word — and when they disagree, the reason arrives attached, because people justify corrections for free and resent interrogation. So "when can the nudge fire without nagging" is hard to answer for a question and easy for a proposal: wherever a decision is being made.
**The moments, now nameable:**
```
/plan keep-step    ->  a recommendation carries its reasoning explicitly.
                       Mostly making existing practice a rule, not a new beat.
the close          ->  where a decision's rationale is written down.
a user OVERRIDE    ->  the highest-yield moment of the three, and the one with
   of a proposal        no rule at all today. An override is rationale arriving
                        unbidden; nothing currently says to capture it as such.
```
**One suppression limb, non-negotiable:** don't propose where the answer is predictable. [merge-and-fold-without-asking] is the user saying plainly that a whole class of Claude's asks was redundant, filed the same day. A nudge design that ignores that reinvents the behaviour the user asked to stop.
**Rejected: sharpen-and-skip for a dedicated design pass.** Legitimate and offered; the user chose to keep it, because the crux is settled and the remaining questions are build-sized rather than design-sized. Recorded so the option isn't re-proposed as though it had been overlooked.
**Cost to weigh at build:** a proposal-with-reasoning spends tokens every time, and the same session established that approval-time token cost lands hardest on Pro users ([approval-flow-token-doubling-simplification]). The suppression limb is what keeps this affordable, so it is part of the feature rather than a refinement.
**Files:** `docs-b/plugin-behaviour.md` (the throughline section — the nudge's proposal form and its suppression rule), `docs-b/plan.md` (the keep-step: a recommendation carries its reasoning), `docs-b/done.md` (capturing rationale at a user override), SPEC.md if its description of the throughline changes, README feature-list sync, and a FAQ entry. Relates to [rename-to-throughliner] (which renames this concept), [provenance-labelling-reconsideration], [merge-and-fold-without-asking], [throughline-value-articulation].

#### [audit] Check every existing `captured by you` credit against what the user could actually have observed [provenance-credit-audit]
By Claude, split from [provenance-credit-second-person-ambiguous] at its processing on 2026-08-05. Filed as its own item rather than a limb of that build because /next routes by flavor, and an audit limb inside a build item would either not run or run as a build.
**Why it is worth doing.** 40 of the queue's 55 items carry the credit. A wording fix protects items written from now on and does nothing about those already there — and there is at least one known bad credit in the existing set: [reorder-mover-marker-at-top-blind] was credited to the user for a script error only Claude could have seen, and it survived a full processing pass because a rewrite preserves the provenance line as given rather than re-deriving it. One confirmed instance in a set of forty is a reason to look at the other thirty-nine.
**The test to apply, one item at a time:** could the user have observed this unaided? A hook error, a code read, a doc grep, an internal tool result — no. A design preference, a live observation of Claude's behaviour, a decision, a report from another project — yes. Where the answer is no, the credit is wrong.
**Report findings; change nothing.** This is an audit, so it lists the items whose credit looks wrong, with the reason for each, and those become captures or a consolidated repair item. The repair is deliberately not part of the audit.
**Sequencing note, not a `Blocked by:`.** Running this *after* [provenance-credit-second-person-ambiguous] ships would let one pass both correct wrong credits and migrate them to the new identity form, rather than touching the same 40 items twice. It is not blocked, though — the audit is useful on its own and its findings would inform that build's migration step. Order at a close, deliberately.
**Files:** none — an audit reads and reports. Reads QUEUE.md and, where a credit's origin is unclear, `LOG/index.md` and the entry that filed the item. Relates to [provenance-credit-second-person-ambiguous], [reorder-mover-marker-at-top-blind].
**Files (rough):** `docs-b/plugin-behaviour.md` (Captures — the provenance line format and the filing-time test), `docs-b/plan.md` (the keep-step, where an inherited credit should be re-derived), the QUEUE.md header template in `docs-b/setup.md`, `templates/CLAUDE-TEMPLATE.md` if the convention is described there. Relates to [provenance-labelling-reconsideration] (shipped — the scheme this sits inside), [reorder-mover-marker-at-top-blind] (the live mis-credit), [scrub-gate-before-approval], [setup-repo-privacy-and-publishing].

#### Backfill the method's own FAQ current with every user-facing change since 2026-06-18 [faq-backfill]
Captured by you — filed after `b6f2344`; processed 2026-08-01. Split from the [faq-staleness-and-sync-failure] capture. `FAQ/faq.md` + `FAQ/index.md` last edited 2026-06-18; a stack of user-facing method changes shipped since (through v1.16.0) with no FAQ entries. Bring both current: audit the LOG/SPEC deltas since 2026-06-18 for user-facing changes (new skills behaviour, working mode, readiness line, seeding, feedback channel, multi-item /next, etc.), draft the missing/updated FAQ entries, and reconcile the index.
**Sequence:** land [faq-sync-hard-gate] first so the backfilled FAQ doesn't immediately re-rot under the same broken mechanism.
**One entry named specifically, 2026-08-04.** Cover what the user can do while `/plan` works through the queue: that skipping an item and raising something of your own are available at any point and are stated once at the start rather than repeated; that each item closes on carry-on-or-stop; and that a capture you raise mid-session comes with the offer to process it now or leave it for its turn. This was asked for after the behaviour was observed and not recognised — the rule is working correctly and had simply been forgotten, which is a documentation gap rather than a design one.
**And the backfill is a correction job, not only an addition job.** The index currently advertises at least five retired concepts as current: Batches, a Red flags section at the top of QUEUE.md, a Deferred tests section, "Parked", and `/next freeform`. A reader following those is misinformed rather than under-informed, which makes this more urgent than a missing-entries backlog would suggest. Audit every existing entry against current behaviour before writing new ones.
**Files:** FAQ/faq.md, FAQ/index.md. Relates to [faq-sync-hard-gate]; note [rename-to-throughliner] will later rename identity strings across these entries (orthogonal to the content backfill).
**Audit findings, consolidated ([faq-audit-wrong-missing-and-shape], run 2026-08-05 in the overnight blitz — its blocker is spent; this item now builds from these findings once the user approves the recommendation):**
1. **The premise is confirmed.** The shipped template is the maintained document (edited continuously through 2026-08-04, 66 entries after the blitz's two additions); the project's `FAQ/faq.md` is the fossil — its base was last refreshed 2026-06-18 (`b5c5337`), and only three new-rule entries have been appended since (docset choice 2026-08-02; editor rescope and write-first 2026-08-04). So consumers get a current FAQ at /setup and this project's own copy — the one session_start points every session at — is two months behind.
2. **What is wrong in the project copy — worse than the five named concepts.** Eight of its 22 entries describe retired machinery as current: Batches and Build/Test/Audit subheadings; `/next freeform`; the Red flags section at the top of QUEUE.md; the Deferred tests section; "Parked" with its two reason-line formats; the spec-edit-batch answer to "Can I edit SPEC.md?" (SPEC is a normal doc now); `_plan.md` described as tracking promoted/parked/dropped; and "outside the current batch" scoping. A reader following these is misinformed, not under-informed.
3. **What is wrong in the template — almost nothing.** Its push-marker mention is deliberately historical (fine). Two entries use "parked" colloquially for below-the-line waiting ("stays parked in your queue", "parked below the line") — mild vocabulary residue from a retired concept, worth a wording pass, not wrong in substance.
4. **What is missing from the project copy:** roughly forty topics the template already covers — the two-section queue, the readiness line, `Blocked by:`, `[user]` walk-throughs and their no-completion-ask rule, build/audit flavors, working mode, the Editor field, red-flag states, the feedback channel, multi-item /next, pushing vs committing, the update path, seeding, the processing flow, subagent asks, the planning file-gate, and more. Nothing user-facing was found in LOG/SPEC that the template misses.
5. **Recommendation: rebuild, not patch — by replacing the project copy with the template.** Under a third of the project copy survives unchanged, and every survivor already exists in the template in newer form, so patching means hand-merging 22 entries into a document that already supersedes them. In self-hosting the shipped template and the project FAQ are the same artifact; the honest structure is to copy `templates/faq-template.md` → `FAQ/faq.md` and `faq-index-template.md` → `FAQ/index.md`, and state in CLAUDE.md that the project copy is a mirror refreshed from the template whenever the template changes.
6. **Shape judgment: the index needs grouping.** 66 links in accretion order is unnavigable for a non-coder under pressure. Recommend reorganising the index (both copies) under five or six plain headings — getting started · the queue · building · closing and saving · safety and privacy · updates and troubleshooting — with the answers file left in its current order; the index is the navigation surface, so grouping it is the whole win at a fraction of the churn.
8. **Who the FAQ is written for — measured 2026-08-06, and the answer is "the user", unchanged.** Relocated here when [faq-as-claude-readable-reference] was deleted at its processing, so the number survives and the question is not re-asked mid-rebuild. That item wondered whether entries should also be written for Claude to read, and recorded honestly that it was a hunch with no evidence. The measurement: across the 16 session transcripts then on disk, **18 opens of a FAQ file, spanning five projects** — this one, scrolly-thing, flintcraft.tech, Verso, and an old copy of this project. So the injected pointer *is* acted on; the capability was never missing. **Two limits stated rather than glossed:** most opens are in this project, where the FAQ is the artifact being maintained rather than consulted, so they are not evidence of the pointer landing; and a file path cannot say *why* a file was opened. The three opens in projects where the FAQ is merely help are the ones that count, and they are thin but real. **The conclusion for this rebuild: keep writing for the user.** Claude already holds the procedure docs, which say the same things more precisely; the FAQ's value to a session is that it is short, which is exactly what writing for two audiences would destroy.
9. **Why the existing close gate never caught this** (carried over from the duplicate capture [host-faq-stale-pre-redesign], dropped 2026-08-06). CLAUDE.md's FAQ-sync gate fires on a *change* — a session whose commits carry something user-facing must disposition the FAQ before closing. It therefore sees new drift and is blind to accumulated drift: nothing about a quiet session says the FAQ fell two months behind. /setup never re-scaffolds an existing FAQ either, so no mechanism re-syncs the project copy. That is the gap this backfill closes, and it is an argument for the mirror-from-template rule in point 5 rather than for tightening the gate — a gate that fires on change cannot detect the absence of change.

#### Rewrite the full public history to remove the third-party name and the author email, and remap all 475 LOG index hashes [history-rewrite-third-party-scrub]
Captured by you 2026-08-04, reversing the scrub-forward-only decision recorded at `68ca27e` the same day. The reason for the reversal is yours: the working tree being clean isn't enough while the name and the nature of the matter stay readable in the public history.
**The facts, established at capture rather than assumed.** The name appears in 12 commits, across roughly 20 files (LOG entries, `CLAUDE.md`, `AGENTS.md`, `IDEAS.md`). It is in **no** commit message and at **no** branch tip — the forward scrub was complete. The earliest affected commit is `39aa0f4` (2026-06-22), giving a rewrite span of 139 commits.**Second exposure, found 2026-08-04 and folded in deliberately.** Every commit in this repo carries `recyclobat@gmail.com` as author — all 385, from the first — in a repo that is public. Commit metadata is machine-readable on GitHub and routinely harvested, and unlike the name it appears in no file, so no edit or forward scrub touches it. This was folded into the same item rather than filed separately because the two fixes are one operation: `git filter-repo` applies a text replacement and a mailmap in a single pass, and running them as two rewrites would mean a second force-push whose span includes the first rewrite's own commits. Claude Code's own commits already use `noreply@anthropic.com`, so only the user's authored commits carry a real address.
**The collateral, which is the reason this is a build and not a command.** The email is in the very first commit, so the rewrite span is the **whole history**, not the 139 commits carrying the name. That widens the index remap from 123 references to **all 475** in `LOG/index.md`. The scale is the argument for doing it once: the remap is scripted from `git filter-repo`'s commit map either way, so the extra references are machine work rather than human work, whereas a second rewrite would repeat the whole force-push-and-verify cycle. That index is how the throughline is retrieved, so a rewrite that doesn't remap it trades a privacy exposure for a broken memory. Apply the map to `LOG/index.md` and to any entry file quoting a hash in a heading, then verify by re-resolving every rewritten reference.
**Correction, 2026-08-04: the orphan tag history is in scope after all.** The original text said it was clean and needed no rewrite. That is right about the third-party *name* — the 2026-08-04 scrub sweep covered it. It is wrong about the email, which appears on those commits like every other. So the earlier statement was true of the exposure it was checked against and was then read more broadly than it had been tested.
**What that history actually holds, which is the reason to be careful with it.** At tag `v157` (2026-06-01) there are 142 files under `_method/build-log/`, the pre-rebuild logging format — the project's entire record from May 2026, in a folder nobody looking in `LOG/` would find. It was located 2026-08-04 when the user asked where their pre-June logging had gone. This is not incidental history; it is the throughline for the method's first month.
**Third strand, folded in by the user's decision 2026-08-04: graft the two histories together.** Today the pre-rebuild graph is reachable only through 135 tags and is not an ancestor of `main`, so a clone shows no early history and the record reads as two unrelated projects. Connect them so the log reads continuously. The user's reason for folding this in rather than doing it separately: the rewrite already touches every commit and changes every hash, so a graft in the same `git filter-repo` pass costs one force-push and one verification instead of three passes over the same commits. That is the same argument the item already makes for folding in the email fix.
**This resolves the tag-clutter question without deleting anything.** The competing option was renaming the range to stop `v17` sorting beside `v1.16.0`. Rejected: it means creating 135 tags and then deleting 135 tags on a public remote, which is 135 repetitions of the one operation that could destroy this history. After the graft, the commits are reachable from `main`, so the tags stop being the only lifeline — but **keep them regardless**, as the version record they are. Deleting them was raised and is refused; the standing never-delete rule in CLAUDE.md holds.
**Branch coverage correction, 2026-08-05: there are THREE remote branches, not two.** `git branch -r` at this session shows `origin/main`, `origin/queue-redesign` **and `origin/claude/remote-session-capture-QxjcU`** — a remote-session branch this item has never named. A pushed branch outside the rewrite keeps both the third-party name and the author email intact on every commit it holds, so a rewrite that covers only the branches someone remembered is a rewrite that does not achieve its purpose. **Enumerate the branches with `git branch -r` at build time rather than working from this list**, since remote-control sessions can create more between now and then. Same correction shape as the orphan-tag one already recorded above: a statement that was true of what it had been checked against, then read more broadly than it had been tested.
**Three things the build must confirm rather than assume.** Whether `git filter-repo` performs a graft directly or whether that needs a replace-ref or parent-rewrite callback — read its documentation before designing the pass, don't infer it. That all 135 tags are re-pointed to their rewritten commits rather than left dangling at old hashes. And that `origin/queue-redesign`, which is still live on the remote, is either covered by the rewrite or deleted before it — a pushed branch outside the rewrite keeps both the name and the email intact.
**The sequence.** Commit the working tree first. Take a full backup copy of the repo folder before anything destructive. Install `git filter-repo` (not present; `filter-branch` is deprecated and unsuitable at this span). **Set `git config user.email` to a GitHub `noreply` address before the rewrite**, or the very next commit reintroduces the address the rewrite just removed and the whole operation has to be repeated. This is the one step that is cheap now and expensive at any later moment, which is exactly the shape of the finding that produced it. Decide branch coverage deliberately — this repo has two worktrees sharing one object store (`main` and `codex/si-port`; the stale `queue-redesign` worktree was deregistered 2026-08-05, though its branch still exists locally and on the remote), and rewriting only `main` would leave copies on any other branch that has been pushed. Rewrite, remap the index, verify no occurrence survives in any commit or message, then force-push.
**State the limits plainly to the user before the force-push, because they are not obvious.** Every commit hash changes. GitHub can retain unreferenced objects for a period after a force-push, so removal is not instant. Any existing fork or clone keeps the old history and nothing can reach into those. The honest claim is "removed from this repository going forward," never "removed from the internet." Every tag hash changes too, so any reference to a tag by hash elsewhere goes stale.
**The `--force` push is a deliberate, one-time exception to the file-safety rule that forbids it**, authorised by you here. Record it in the LOG entry as a departure, with what was done and why it couldn't wait.
Red flag · State: cleared — cleared by this decision: the risk is named, the user was told plainly what a rewrite can and cannot achieve, and chose the rewrite over scrub-forward-only and over making the repo private.
**Files:** the whole history in the rewrite span; `LOG/index.md` (the 123 remapped references) and any LOG entry file quoting an affected hash; `CLAUDE.md` if the worktree list turns out to be stale. Relates to [scrub-gate-before-approval] and [claude-md-old-history-claim-wrong], which carries the orphan-tag history and its clean sweep result.
**Lift-condition (rewritten 2026-08-04 — the `Blocked by: [log-scope-adjacent-work]` line is removed because that item has shipped):** nothing queued blocks this. It stays below the readiness line for one reason only, which is not a dependency: it force-pushes a rewrite of the entire history and needs the user present to approve that, so it must never sit above the line where an unattended run could reach it. **Lifts when the user opens a session for it deliberately** — this is the top priority of 2026-08-04's planning session and the recommended next session, not shelved work.

#### Release the docset-B work: version bump, pre-push sweep over 18 commits, GitHub Release [release-docset-b-work]
Captured by you 2026-08-03. Eighteen commits sit unpushed, spanning docset B's full authoring (`6ba51d3`), the fidelity audit and phase-3 session_start wiring (`b931278`), the fidelity restores (`f37e332`), and the surrounding /plan sessions. The last published release is v1.16.0; the working tree carries the `-test4` suffix from rezip testing. The method's one external user is running everything behind that.
**Run the Push ritual in CLAUDE.md as written** — it already specifies the steps. Two parts of it carry real work rather than being mechanical this time. The version bump is a **minor** (1.17.0), not a patch: two docsets picked automatically by model is a new capability, and the bump must drop the `-testN` suffix. And the pre-push consistency sweep is substantial at this size — Pass A reads eighteen commits' LOG entries, Pass B checks the templates, project docs and CLAUDE.md against what those commits changed.
**Ordering: run after [docset-b-inert-on-desktop-app] has shipped and been seen to work (re-pointed 2026-08-03).** The old clause waited on the session_start diagnostic and said the release would clear immediately if the failure turned out to be local. That diagnostic has now run, and the wording is actively misleading if left: it resolved, but *not* in the direction that clears this. The hook's output does reach sessions now — and with it working, the payload was found to contain no docset directive at all, because no `model` field arrives. So the release's headline feature, model-based docset selection, does nothing in this environment.
Shipping now would publish and announce a release whose main capability is inert. The genuine dependency is therefore the replacement selector: ship [docset-b-inert-on-desktop-app], confirm a real session loads docset B, then release. Two things also get easier by waiting — the release notes can describe a feature that works, and the payload-size problem may have dissolved by then, since docset B's payload is 3,524 characters against docset A's 92,112.
**Dependency satisfied 2026-08-04; now held by choice, not by blocker.** The condition above is met — [docset-b-inert-on-desktop-app] shipped in `f832385` and the /plan session of 2026-08-04 received the docs-b directive on host 1.16.0-test5, so the headline feature demonstrably works. It stays below the line because the user chose to hold it for a session that can watch it, and because the eight items now cleared above rewrite `session_start.py`, `pre_tool_use.py`, SPEC.md, CLAUDE.md, AGENTS.md and five procedure docs — releasing before those land would ship a build the very next run rewrites, and the Pass B consistency sweep would have to be redone. **Nothing blocks this: greenlight it at any /plan close, ideally the one following the cleared run.** Note the run also retires the push marker ([spec-diverges-from-shipped-docsets]), so the release's sweep must check CLAUDE.md and AGENTS.md for that change.
**Release notes draw from the LOG entries**, per the ritual's step 10, and this release has an unusual amount to say: the register decision and its later weakening, the 39% subtraction result, the fidelity audit. The Discord announcement written this session covers much of the same ground and is the natural source to adapt — the first real use of the artifact test from [log-scope-adjacent-work].
**Files:** `plugin/si-plugin/.claude-plugin/plugin.json` (version), `plugin/si-plugin.zip` and `plugin/zip-archive/` (repackage and prune), plus whatever the Pass B sweep turns up across templates, SPEC.md, README.md, QUEUE.md and CLAUDE.md. Relates to [session-start-output-not-reaching-sessions], [test-suffix-plugin-json-close-noise].

#### Build an adherence-measurement harness, and use the docset-B register question as its first experiment [docset-b-register-decision-weaker-than-recorded]
Captured by you 2026-08-03; re-scoped the same day after fresh research. The slug is unchanged from the original capture, which asked only whether to re-test pseudocode.
**Where this started.** `LOG/2026-08-02-opus5-docset-b-authoring.md` gives three reasons for rejecting typed pseudocode as docset B's register, and calls the third decisive: the format demoted a standing global rule into a comment inside one branch. That error was caught and fixed in the re-author and never shipped. A fixed authoring error is thin evidence about a format — any register lets a rule be put in the wrong place, and the fix is to move it. So the decision rests on two arguments, and neither was described as decisive.
**The two that stand.** The fences carried the mechanics that would be followed anyway, while the two most-slipped rules in the sample section stayed prose in the pseudocode draft too. And prose inside a fence is a costume — lines like `purpose = the one human checkpoint…` lose wrapping and gain no precision, which is bespoke semantics rather than bespoke syntax, the thing the Grammar-Prompting caveat actually warns about.
**The narrower defensible form of the discarded third reason,** kept rather than dropped: a branch structure *invites* scoping a rule to a branch, because branches are what the notation offers, whereas prose has no branch for a global rule to fall into. It was one instance generalised to a class when the call was made, but it has a second instance now — the capture line-format block that shipped in docset B asserted structure its own prose contradicted (point 1 of the fidelity restores, `f37e332`).
**Why the item's original re-test was dropped.** It proposed re-authoring one section in pseudocode and reading it on a 5-series model. That is the same instrument that produced the decision now in doubt, so it would yield a second impression rather than a second data point. Research 2026-08-03 (`resources/research/pseudocode-vs-prose-for-rule-adherence.md`) found no study anywhere testing prose against pseudocode for standing behavioural rules, and found the evidence *for* pseudocode thinner than assumed — Mishra et al. measure NLP task accuracy on BLOOM and CodeGen, one of them a code model, and Skill-as-Pseudocode measures agent task completion. Both sides of the argument thinned at once.
**What gets built.** A small measurement harness, host-only and not shipped in the plugin package. Four parts: a fixed scenario set; the rules that actually slip expressed as script-checkable constraints, following IFEval's verifiable-constraint design; a runner driving `claude -p` with `--plugin-dir` pointed at each docset build, `--model` fixed, `--output-format stream-json` captured, and `--max-budget-usd` capping the loop; and a violation count per docset. Feasibility confirmed against the installed CLI's own `--help` rather than assumed.
**Why the harness is the deliverable and the verdict is not.** Every register, wording and structure decision this project has made was judged by reading a doc. There is currently no way to tell whether *any* change to these docs helps. That is the real gap; the register question is only its first customer, and the harness outlives whatever answer it gives.
**The safe window, unchanged.** A register change is survivable now precisely because docset A exists as the known-good fallback, which CLAUDE.md calls a non-negotiable no-strand guarantee. Once A retires there is one docset and nothing to fall back to. So run this while A still stands, not after.
**Publish the result either way — a condition this build closes against, not an intention.** The raw run output and baseline numbers land in `resources/testing/`, because a later run is only meaningful against the first one's exact numbers. The finding and the decision it drove land in the LOG entry. The public write-up is derived from that LOG entry, the same way the Push ritual draws GitHub Release notes from LOG entries. This is written into the item deliberately: doc size is the method's most contested feature, the standing defence has only ever been "it works anyway", and if the harness reports that the size critics were right, the pull will be to quietly not write it up. A commitment made in chat will not survive that moment; a close condition will. Publishing an unfavourable result is the whole point of measuring rather than reading. Relates to [log-scope-adjacent-work].
**Deliberately not folded in:** the structural-delimiting experiment, filed separately. Same harness, different experiment — bundling several experiments into one build item is the consolidation trap.
**Lift-condition (below the line, 2026-08-03):** nothing gates this — it is held below only because the release should ship first, and because it is a multi-session build that shouldn't run unattended off the back of a planning session. Greenlight it at any /plan close once the release has gone out, or sooner if you want it built first.
**Files:** a new host-only harness directory under `resources/testing/`, holding the runner, the scenario set and the constraint checkers. No `docs-b` changes unless the experiment flips the register, which would be its own work. Relates to [opus5-docset-b-authoring], [docset-b-fidelity-restores], and the research at `resources/research/pseudocode-format-for-skill-docs.md` and `resources/research/pseudocode-vs-prose-for-rule-adherence.md`.

#### Test explicit structural delimiting on plugin-behaviour.md as a second harness experiment — the better-evidenced lever we hadn't considered [structural-delimiting-experiment]
By Claude, from the 2026-08-03 research (`resources/research/pseudocode-vs-prose-for-rule-adherence.md`). The register question has been framed throughout as prose versus pseudocode. That may be the wrong axis. The better-evidenced intervention for a document of this exact shape — long, multi-section, always loaded, carrying many independent rules — is explicit structural delimiting: XML-style section boundaries whose purpose is to stop an instruction in one section being read as applying to another.
**Why it's a distinct experiment rather than a variant of the register question.** It changes the document's boundaries, not its voice. No rule is reworded and no prose becomes pseudocode, so it costs a fraction of a register change and can be tried without re-authoring anything. That also makes it the cheaper first experiment of the two, which is worth weighing when ordering them.
**The evidence, and its limits.** Provider guidance from Anthropic, OpenAI and Google converges on tag-delimited sections for complex prompts. Practitioner testing is consistent about when the benefit appears: nothing measurable on short single-purpose prompts, gains on long multi-section ones. `plugin-behaviour.md` is markdown-sectioned rather than tag-delimited, and is the longest always-loaded document in the method — squarely the case the evidence describes. But none of that evidence measures behavioural rule adherence either, which is the same gap that undermined the pseudocode case, so this is a hypothesis to test rather than a fix to apply.
**Corroboration worth carrying:** AGENTIF (arXiv 2505.16944) finds that in agentic settings, models hold *format and style* constraints worst — which is what the response-shape rules are. That suggests the rules which keep slipping here do so for structural reasons rather than poor wording, and also that a format change alone may not fix them. Recorded so the experiment isn't over-expected.
Blocked by: [docset-b-register-decision-weaker-than-recorded]
**Why:** that item builds the adherence-measurement harness, which is the only thing that makes this measurable at all. Converted from a prose "depends on" to the proper field at processing, 2026-08-05 — a dependency held in prose is one nothing checks.
**The premise measured rather than asserted, 2026-08-05.** `docs-b/plugin-behaviour.md` is **1,148 lines / 62 KB** — roughly twice the next largest procedure doc (`done.md`, 698 lines / 34 KB) and the only one loaded in every session whatever the session is doing. So "long, multi-section, always loaded, many independent rules" is the measured description of this file, not an impression. Recorded so a later session doesn't have to re-derive it.
**A boundary this build must not cross.** CLAUDE.md decided deliberately, with its reasoning recorded at length, that docset B's `plugin-behaviour.md` stays a **single file** — progressive disclosure fails for standing behavioural rules, which have no trigger to fetch them. Delimiting is compatible with that decision: tags mark sections, they don't split a document. But "improve the structure of the big file" is exactly the banner under which a build drifts into splitting it, and the person building this may not be the person who read that decision. **Delimit; do not split.**
**Its own caveat stays prominent:** the delimiting evidence does not measure behavioural rule adherence either, which is the same gap that thinned the pseudocode case. A hypothesis to test, not a fix to apply.
**Ordering note (not a dependency):** this item argues it is the cheaper of the harness's two experiments, since it rewords no rule. Once the harness exists, decide deliberately which experiment runs first rather than defaulting to the register question because it was raised earlier.
**Files (rough):** the harness's scenario set and constraint checkers under `resources/testing/`, plus a delimited variant of `docs-b/plugin-behaviour.md` built for the run. No shipped change unless it wins.
Filed after `f37e332`

#### Detect a stale project format at session_start and halt with a migrate prompt; name /setup as the migration home and retire the separate /migrate skill [setup-as-migration-home]
Captured by you — testing outcome from the SA consumer project, filed after `9cd1e51`; processed 2026-08-01. SA validated that the migration recipe works and that /setup already re-scaffolds on version drift and loads `migrate-checklist.md` from setup.md Step 2C. So the migration machinery already lives in /setup; what's missing is *active detection* — today migration only happens if the user thinks to run /setup.
**The decision (user's call, 2026-08-01):** migration should be **detected and prompted**, not left to the user noticing drift. So build the stale-format halt that [migrate-skill] reserved as a deferred design question, home it in session_start + /setup, and **retire [migrate-skill]'s separate-`/migrate`-skill framing** — /setup is the migration home. The user's reason: a project silently on an old format wastes /plan and /next context on stale scaffolding, and shouldn't depend on the user spotting the drift.
**The build:**
1. **session_start.py — stale-format detection.** Add a **format-epoch** signal, kept distinct from `.si-version`/the plugin version (which bumps on every release, most of which change no format). The plugin package declares a current format-epoch; the project records its own. When the project's epoch is behind the plugin's, session_start halts the normal load and tells the user to run /setup to migrate *before* /plan or /next. This is the piece a plain "run /setup when you notice" convenience can't give.
2. **setup.md — clear the halt.** The migration path (Step 2C, already loading `migrate-checklist.md`) writes the current format-epoch into the project on completion, so a migrated project stops halting.
3. **Self-hosting bump discipline (host-only, → CLAUDE.md):** whenever a build changes the queue/doc format, it bumps the plugin's format-epoch — sibling to the README-sync / SPEC-sync triggers. Without this the halt never fires.
**Rejected alternatives:** a separate `/migrate` skill ([migrate-skill]) — heavier standing maintenance for a job /setup already does, and SA proved /setup carries it; and detection-by-structure (does QUEUE.md *look* two-section?) — fragile, so the explicit epoch marker wins.
**Residues from SA to carry (minor):** /setup left SA's top-level `research/` folder unreconciled against the scaffold's `resources/research/`; and old `By Claude` provenance survives on migrated items (harmless).
**Files:** hooks/session_start.py (epoch detection + halt state), setup.md (write epoch on migrate), plugin package (declare current format-epoch), SPEC.md (new user-visible hook behaviour), README feature-list sync, CLAUDE.md (the epoch-bump self-hosting rule), a FAQ entry, and the format-epoch marker's literal name grepped at build time. Supersedes [migrate-skill]; relates to [migration-checklist] (the recipe) and [migration-recipe-freshness].
**Lift-condition (below the line):** ready to build now — greenlight at /done close to lift above the marker for its own /next run, or leave below until you want it built.

#### Refresh the migration recipe whenever a build changes the queue/doc format, as a clause on the existing format-epoch bump [migration-recipe-freshness]
Captured by you — filed after `f593cd1`; processed 2026-08-02. The migration checklist (the recipe for converting an old-format project to the current two-section queue) drifts as the method's format keeps changing, and a stale recipe is worse than none. So it needs a refresh discipline.
**Its gate was stale and is removed.** The item said it was blocked on [migration-checklist] existing first. That shipped at `ea272f6` on 2026-08-01 — `docs/migrate-checklist.md` and `docs-b/migrate-checklist.md`, 130 lines, loaded on demand from setup.md Step 2C. Nothing blocks this now.
**Trigger decided: format-change, not "on rezip."** Rezip is a frequent local test build that usually changes nothing about the queue format, so a pass on every rezip is noise — and noise trains the step to be skipped. A format-change trigger fires exactly when drift is introduced. Same shape as the existing README-sync and SPEC-sync triggers, a pattern the method already runs.
**It rides an existing trigger rather than becoming a new one.** [setup-as-migration-home] already establishes a format-epoch bump whenever a build changes the queue/doc format — the same moment, already detected. So this is one more clause on that bump ("and refresh the migration recipe in the same session"), not a separate gate someone must remember. Build it after that item ships, so the clause has a bump to attach to.
**Rejected: the version-keyed migration-notes changelog** (the option rehomed here from the deleted [migrate-skill]). A second artifact to keep fresh, solving what the recipe already covers, and its own framing asked whether it earns its keep. Against a 130-line recipe refreshed at the moment of change, it doesn't. Recorded so it isn't re-proposed.
**Files:** CLAUDE.md (the format-epoch bump clause, host-only — consumers don't author formats), docs/migrate-checklist.md and docs-b/migrate-checklist.md as the refresh targets. Depends on [setup-as-migration-home]. Relates to [migration-checklist] (shipped).

#### Rename Sovereign Implementer → Throughliner: full technical identity + positioning, big-bang with a `renames` map [rename-to-throughliner]
Captured by you (2026-07-30); migration path resolved this /plan (2026-07-31) via research at resources/research/plugin-marketplace-rename-migration.md. **Rename the plugin from "Sovereign Implementer" to Throughliner**, built around its real distinctive value: the throughline of rationale that survives a forgetful session (an AI memory-of-reasoning tool), dropping the over-claiming "Sovereign" and the no-code-competitor framing. "Throughliner" (coined single word) chosen over "Throughline" because the bare word is crowded — getthroughline.com is itself software (name-availability research: resources/research/throughliner-name-availability.md, which found no product named "Throughliner"). Port-neutral: not "Throughline for Claude Code," so a future port won't break the name.
**Scope — both halves (user's call):** positioning/user-facing name (README title, marketplace description, pitch — lead with rationale-memory value); and technical identity (`sovereign-implementer` slug → `throughliner`, the GitHub repo, install/adopt commands, `.si-version`, every absolute path and doc mention).
**PREMISE CHANGED 2026-08-06 — step 1 below has already happened outside the method. Read [github-identity-changed-underneath-the-project] before building any of this.** The repo is already `FlintcraftTech/throughliner`; the user's GitHub login is now `its-coughfee` and the old name `FlintCraftTech` is now an organisation. So this item's migration sequence no longer starts where it thinks it does, and the remaining steps — the marketplace `renames` entry, the corpus-wide identity strings, the positioning copy — are what currently stand between a working redirect and a broken install path.
**Migration decided (big-bang, not a transition apparatus):** (1) rename the GitHub repo — GitHub's 301 redirect keeps existing `marketplace add` registrations resolving; **permanently reserve the old repo name** (reusing it later breaks the redirect). (2) Add a top-level **`renames`** map entry in marketplace.json (`sovereign-implementer` → `throughliner`) — Claude Code auto-migrates each consumer's settings and shows a one-line notice; append-only, keep the entry forever. (3) Net consumer cost is a single `/plugin install` (github remote → `plugin-cache-miss`), documented in the release notes + FAQ. Caveats to carry into the build: needs consumers on Claude Code ≥ 2.1.193; managed/policy-enabled installs don't auto-rewrite.
**Lean on the marketplace id (`flintcraft`): keep it.** Renaming the marketplace itself would force consumers to re-add the marketplace — a burden the plugin `renames` map does not cover. Confirm in the build, but the default is to leave `flintcraft` as-is.
**Hook-enforced-identity change — trace the ripple by grepping the literal old identity strings at build time** (per CLAUDE.md's rule): grep `sovereign-implementer`, `Sovereign Implementer`, `si-plugin`, `.si-version`, `flintcraft`, **and `why-pipeline` / `why pipeline`** across the whole repo so the file list is complete before the build starts. Candidates: plugin.json, marketplace.json, CLAUDE.md, README.md, SPEC.md, every docs/ procedure file (incl. plugin-behaviour.md's Why-pipeline section), templates, hooks, INSTALL.md, resources/.
**Includes the concept rename why-pipeline → "throughline"** ([throughline-feature-elevation] thread a, folded in and settled 2026-07-31): the product and its titular feature share a name, so the concept rename lands atomically with the identity rename rather than as a separate pass that could drift out of sync. Rename `why-pipeline` / `why pipeline` → "throughline" across all docs, including plugin-behaviour.md's Why-pipeline section heading and every reference.
**Naming clarity (resolved 2026-08-01, folds in the deleted [throughline-naming-referent]):** the throughline is the *reasoning spine* — the thread of *why* — running through capture → work item → log; the LOG is where it's recorded and retrieved from, **not** the throughline itself. State this distinction explicitly wherever the rename introduces "throughline" (the renamed plugin-behaviour.md section, SPEC positioning, FAQ), so "throughline" is never colloquially collapsed into "the LOG." The tell that they're distinct: a complete LOG can carry *no* throughline — every event recorded but the reasoning stripped out — which is exactly the failure the mechanism fights, so the two must stay separate.
**Coordination:** host-side and large — the SPEC/CLAUDE.md/README rename lands atomically with the technical identity change (the "large SPEC rework is its own build" case). Relates to [self-hosting-path-portability] (absolute-path fix overlaps the path-rename surface), [migration-checklist] (adds an identity-migration dimension), and [throughline-feature-elevation] (its threads b+c — rationale provenance and proactive nudging — stay separate and are NOT in this build).
**Lift-condition rewritten 2026-08-05 — the old one is spent, and nothing now blocks this.** It read "cleared to run once [user-work-surfacing-fix] (the emergency) has shipped, rezipped, and been verified live". That item shipped at `ae92d34` on 2026-07-31, so the condition has been satisfied for over a month and the revisit could not tell, because a met condition and a still-waiting one read the same way when nobody re-checks the event. **This item is now held by choice, not by a blocker** — the same standing this queue already records for [release-docset-b-work]. What holds it is that a repo-wide identity rename touching every doc, hook, template and the GitHub repo itself should be its own deliberate /next run with the user present, never swept up unattended behind other work. **Greenlight it at any /plan close when you want it to be the session's work.**
**The positioning copy this rename must write is folded in here, relocated whole from [throughline-value-articulation] on 2026-08-05 (the user's call).** That item was captured from drafting the Discord throughline help article, which found the current framing — "sometimes you can ask why something is the case" — undersells the feature as a minor lookup, not worth the perceived cost of approvals and log-writing. It was folded rather than kept separate because this item already rewrites the README title, positioning and feature list wholesale and already says to lead with rationale-memory value, so keeping the words elsewhere would mean writing the positioning twice and letting the two drift. The ordering fact underneath: the pitch cannot be written before the product's name is settled, and this item settles it.
**The headline to lead with:** the throughline's value isn't occasional why-lookup — it's that *the user's intent keeps running the project even after Claude has forgotten everything.* Claude's memory resets each session; the throughline is why a fresh, short, even cheaper-model session still builds the project the way the user meant, instead of re-deriving intent from code and guessing wrong. That is the product, not a side feature.
**Value points to draw from:**
- *For Claude:* it's how Claude stays aligned to intent (not just "what" but "why you want it that way"); rationale riding a rule is *why the rule is followed reliably* (bare rules slip) — so it steadies Claude's own behaviour, not just answers user questions; it lets Claude catch drift, because recorded intent makes contradictions visible.
- *The "branches" framing (the user's):* the throughline isn't one file — intent lives in SPEC, rationale rides every QUEUE item, history lives in LOG. SPEC and QUEUE are read *during* planning and building, so the throughline shapes every plan and build silently, not only on a "why?" query; the LOG is the deep archive pulled on demand.
- *For the user:* stops re-deciding settled things (records why a rejected option lost, so it isn't re-proposed); prevents silent regressions (see why something is that way before changing it); institutional memory for coming back after a break or handing the project on; keeps a growing project coherent instead of rotting; confidence that the project still reflects intent rather than hoping Claude "remembers."
- *Reframing the perceived overhead (put this in the article):* the approving is what makes the record genuinely *yours* and trustworthy — a reason you never vouched for is one you can't trust on read-back; the log-writing pays a small cost now against the big "why did we do this?" cost later; and externalised intent is exactly what lets short, cheap sessions work at all.
**The work-flow-vs-throughline distinction (for the help docs):** the current article describes the *flow of work* (capture → processed → built), which is the work pipeline, not the throughline. The throughline is the *why that travels alongside* that flow and is preserved. Help docs land harder separating the two: "here's how work moves" *and then* "here's the throughline — the reasoning that rides with it and survives Claude forgetting." Uses the settled naming clarity above (throughline = reasoning spine across SPEC/QUEUE/LOG; the LOG records it, isn't it).
**The FAQ half of that content is [faq-backfill]'s**, not this item's — that build rebuilds the FAQ wholesale, so a throughline entry written here would be overwritten there. Cross-referenced rather than duplicated.
**Add the Discord community link to the README in this same pass (2026-08-03).** The invite is recorded in CLAUDE.md's External communication section and Alex has consciously accepted it being public. It isn't added to the README now because this item rewrites the README title, positioning and feature list wholesale, so a link added today would be rewritten here anyway.
**Files:** determined by the build-time identity grep (candidates above), plus plugin-behaviour.md (Why-pipeline section rename), SPEC.md (name/positioning), README title + feature list + the Discord link, CLAUDE.md, marketplace.json (`renames` map), and a FAQ entry (user-visible identity change + the one-time `/plugin install`).

#### Finding a referenced queue item is hard work for the user, and the queue they are reading may not be the queue on disk [queue-pointer-hard-to-follow-and-possibly-stale]
Captured by you during the 2026-08-05 planning session, at the moment it happened: you went to find [faq-edit-trips-planning-file-gate] and could not, because the only recent reference to it was a bare slug a couple of turns back, and the queue you were scrolling did not appear to reflect the items already processed this session.
**Two problems, and they compound.** The first is navigation: a pointer is a plain `.md` link that lands at the **top** of a 460-line file, so every lookup is a scroll or a search — and once a checkpoint has passed, later messages refer to the item by bare slug only, with no heading text to search for. The rule that a pointer carries the target's exact heading text applies at the pointer; it does nothing for every subsequent mention.
**The second is worse, and it was tested in-session on 2026-08-05: the queue you read IS stale.** A real edit was made to this very item and confirmed on disk, and the user's already-open view of QUEUE.md did not show it — that view was still showing the file as of an earlier point, with [merge-and-fold-without-asking] as its newest content. The user had separately found that clicking to another document and back was what made earlier changes appear.
**That pair of observations identifies the cause and rules the other candidate out.** The desktop viewer serves a **cached snapshot** of an already-open document and refreshes it only on navigating away and back. It is not Drive sync lag: sync lag would survive a re-open, and a re-open shows current content immediately. Recorded as an established finding rather than a theory, because it was tested rather than reasoned about.
**Second test, same session: the staleness is NOT markdown-specific.** A `.py` file in the same folder, opened and then edited while the user watched it, also showed the old content. So the viewer caches every open document regardless of extension. This matters twice over: it rules out a file-format change as a fix (see [doc-filetype-switch-for-line-links]), and it makes anthropics/claude-code#84017 a general viewer bug rather than a markdown-rendering one.
**A note on how the first attempt at this test failed, because it is a method lesson and not just a mishap.** The test was first built as two files whose names differed only by extension — `viewer-refresh-ab-test.md` and `viewer-refresh-ab-test.py` — and the user was asked to open the right one from a link. They opened the wrong one and lost the observation. The project's CLAUDE.md records that the user scores in the bottom 10th–25th percentile on symbol search, which is exactly the skill "pick the correct one of two near-identical strings" demands. The redesign removed the choice entirely: one file, one link, one unmistakable name. **The general lesson: when a step needs the user to identify something, the design should remove the discrimination rather than rely on it.**
**The consequence for the method is immediate and larger than the annoyance.** Write-first-then-point assumes the user can read what was just written by following the pointer. If their open view is a snapshot, then a pointer emitted right after a write can land them on text that does not yet include it — so they may approve, or fail to find, a version that no longer exists. The remote-mode inline paste never had this failure. **Interim mitigation while this stands: pair every pointer with either an inline paste or an explicit instruction to click away and back.**
**The workaround is itself a cost, reported 2026-08-05.** Searching within the file flips the desktop viewer out of rendered display into code mode — a smaller, narrower font that is immediately harder to read — and switching back to the rendered view returns the document to the **top**, discarding the position the search just found. So the suggested remedy for "the link lands at the top" is a workaround that ends by putting you back at the top, in a less readable font. Filed as anthropics/claude-code#84018; it is harness behaviour, not method behaviour. The stale-viewer finding below is filed as anthropics/claude-code#84017. Both were duplicate-checked before filing and neither had a prior report.
**Why this matters more than the inconvenience.** The whole write-first-then-point rule rests on the user being able to read what was written, in place, and approve it there. If the pointer is hard to follow *and* the view can be stale, then approval-in-place is weaker than the method believes — the user may be approving from a version that no longer exists, which is exactly the failure pasting inline never had. That makes this a question about a load-bearing rule, not a UI annoyance.
**Narrowed 2026-08-05 to the harness bugs only.** The navigation half — how a pointer should be written so the user can find the item — is answered by [retire-working-mode-for-short-summary-default], which makes every pointer arrive with a short inline summary, so the user can act without following the link at all. What remains here is the three Claude Code bugs themselves, which bite whatever the method does.
**The three, with what was established about each:**
1. **#84017 — the viewer serves a stale snapshot** of an already-open document and refreshes only on navigating away and back. Tested rather than reasoned about, and **not markdown-specific**: a `.py` file in the same folder went stale identically, which rules out both Drive sync lag and any file-format fix.
2. **#84018 — searching within the file flips the viewer** from rendered display into code mode (smaller, narrower font), and switching back returns the document to the **top**, discarding the position the search just found. So the remedy for "the link lands at the top" ends by putting you back at the top, less readably.
3. **#83475 — a `.md` link with a `:N` line suffix is dead.** It fails silently while still looking clickable. `.py` and `.json` links with `:N` do open at the exact line, so this is a markdown-path bug, not a missing feature.
**Facts relocated here from the deleted [doc-filetype-switch-for-line-links], because they answer questions that will otherwise be re-asked.** Only two files are locked to the `.md` extension — each `SKILL.md` and `CLAUDE.md` — so the queue, spec, log, templates and procedure docs are all technically free to change. A switch was measured at **698 occurrences of `.md` across 35 files**, including all three hooks, and reverting would cost the same again. Extension does not change how Claude reads a document, so nothing about cross-item reasoning would fragment; line anchors are a viewer affordance for the human only. And a format switch would **not** fix staleness (see point 1), which was the main reason anyone wanted to expedite it. The last extension test was inconclusive rather than negative — the test vehicle was broken (new, untracked, repo-root files, where even `.py` failed) — so nothing was learned about whether a prose extension can line-anchor.
**One method lesson from that testing, worth keeping.** The first test asked the user to open the correct one of two files whose names differed only by extension. They opened the wrong one and the observation was lost. The project's CLAUDE.md records that the user scores in the bottom 10th–25th percentile on symbol search, which is exactly the skill that task demands. **When a step needs the user to identify something, remove the discrimination rather than rely on it** — one file, one link, one unmistakable name.
**Processed 2026-08-06: kept, and moved below the readiness line with a mechanically-checkable lift-condition.** As an Unprocessed capture it could only ever be processed into "check again later", which meant surfacing every session forever — the nagging shape this method has spent months removing. Below the line with an automatic check, it is silent until something actually happens.
**State re-checked at processing (2026-08-06): all three still OPEN, no movement.** #84017 last updated 2026-08-05, #84018 2026-08-05, #83475 2026-08-03. The related harness feature request #77134 was checked in the same pass and is also still open, last updated 2026-08-03.
**Only ONE of the three has a method change waiting on it, and saying so is what keeps the revisit honest.**
```
#83475  dead :N link on .md   ->  REAL work waiting. On a fix, pointers gain
                                  line anchors, and the standing rule that
                                  forbids emitting a :N suffix on a .md link
                                  (doc-bound-text, plugin-behaviour.md) is
                                  revisited rather than left over-cautious.

#84017  stale viewer snapshot  ->  MUCH weaker than when filed. The
                                  short-summary-with-every-pointer rendering
                                  was since decided as the default on its own
                                  merits ([approval-flow-token-doubling-
                                  simplification]), so a fix here is welcome
                                  but changes no rule.

#84018  search flips to code   ->  NO method consequence, either way. It is a
        mode, loses position       daily irritation for the user with nothing
                                   for the method to build. Tracked so it is
                                   not mistaken for pending work.
```
**Lift-condition, mechanical and requiring nothing of the user: has any of #84017, #84018 or #83475 been closed?** One `gh issue view <n> --repo anthropics/claude-code --json state` per issue, run silently by /plan's below-line revisit. Propose lifting only when **#83475** closes, since it is the only one carrying work; a close on either of the others is narrated in one line and the item stays put. **This is the set-the-trap pattern that broke the [statusline-context-reader] cycle** — a condition waiting on someone remembering to look will roll forward indefinitely, and a condition a command can answer will not.
**The escape route is CLOSED, decided 2026-08-06 — these three bugs are permanent until Anthropic fixes them.** This item previously said the bugs might be resolved "by leaving rather than by fixing", pointing at a move to the VS Code extension. That item was deleted the same day: the user settled that **the Claude Code desktop app is the platform, as a product decision** (now recorded in SPEC.md under Who it's for), and is building their own reader instead — see [mdreader-as-the-reader]. So there is no surface change coming, and waiting on one is not a strategy. The lift-condition below is now the only route, which makes its mechanical checkability the whole value rather than a convenience.
**Files (rough):** none — no method change is proposed here. Relates to [retire-working-mode-for-short-summary-default] (which answers the navigation half) and [mdreader-as-the-reader] (the reader direction that replaced the editor-switch route, and which addresses reading only — it changes nothing about the three bugs, since they live in the app's own viewer).

## Unprocessed

#### Last session advises building the cleared region soon rather than planning further, because the designs are fresh and staleness is the measured enemy [advisory-build-while-designs-are-fresh]
Filed at the second 2026-08-06 planning close. This is a reason, not a work item — queue position says *what* is next and cannot say *why now*.
**The finding this rests on.** Two consecutive planning sessions have now found roughly half their items resting on premises that had gone stale since capture — eight of sixteen at the previous close, seven of fifteen at this one, four of those seven outcome-reversing. The cause is structural: an item is written when its claims are true, then sits while the project moves under it, and nothing re-reads it until processing. **The cleared region is currently about twenty items, and every one of them was designed within the last two days.** That is the freshest this queue has been, and the value decays from here. Building now is worth more than planning more.
**Rewritten at the third 2026-08-06 planning close — the advice holds and is stronger, but its numbers and its caveats were spent.** That session processed seventeen dispositions, so the four captures this advisory used to list as non-blocking have all been processed and the note about them is gone. The cleared region is now **34 build items**, not twenty, and the whole of it was designed within the last three days.
**The strongest single reason to build now, added at that close: the run is deliberately remote-control-ready.** Every walk-through that needed the user at their desktop was moved below the readiness line, so the cleared region contains **no step that stops for a person at a computer**. The one `[user]` item left above the line ([report-url-404]) is a web form, doable from a phone. That state was arranged on purpose and nothing preserves it automatically — the next planning session could clear a desktop-dependent item above the line without noticing what it costs. **This is the fact queue position cannot carry, and the reason this advisory still earns its place.**
**One ordering note worth keeping.** [editing-state-signal-for-companion-apps] sits at the top deliberately: a separate project of the user's is queued and waiting on it, so building it early unblocks work outside this repo.
**One thing that genuinely would benefit from planning first, stated honestly rather than suppressed:** [show-then-write-survives-at-step-level] found a live contradiction in the shipped procedure docs — both the write-first and show-first rules are in force at once — and several cleared items edit those same docs. It does not block them, but a session that fixed the contradiction first would be editing a consistent document.
**Persist until the cleared region has been substantially built**, or until a session finds a reason the designs have moved.

#### Last session advises soaking the blitz branch before merging, and proving the new build's delivery at the restart [advisory-soak-blitz-branch-then-merge]
**Rewritten at the 2026-08-06 planning close to only what still stands** — three of its four points were consumed or superseded by that session, and a half-true advisory orients the next session wrongly in both directions.
**What persists: the branch decision — soak first, and that is the recommendation, not merely an option.** The blitz ran on branch **`overnight-blitz-2026-08-06`**; `main` is untouched. The whole point of the branch is that you can live on the blitzed state for a day or two before committing to it. The test build is snapshotted from the branch, so your ordinary sessions exercise the new behaviour with `main` untouched. Merging on the first morning stays available, but it is a gamble rather than the plan. Merge with `git checkout main && git merge overnight-blitz-2026-08-06` once it has earned trust; if it hasn't, check out `main`, re-run the plugin update to re-snapshot it, and restart. **One honest caveat, and it has grown again:** work done during the soak lands on the branch too, so a late revert discards the soak days' work along with the blitz. **Updated at the second 2026-08-06 planning close:** the branch now carries the blitz plus **two** full planning sessions — twenty-plus processed work items, three deletions, a preserved session transcript, and the editing-state contract another of your projects is waiting on. The revert side of this gate is no longer cheap in any sense, which shifts the balance toward merging once the soak has gone quietly rather than holding the branch open indefinitely to keep the option.
**Consumed 2026-08-06, recorded so it isn't re-raised:** the delivery proof (the planning session opened reporting host `1.19.0-test1`, matching the recorded string — the restart took); the status-line test (superseded — the probe is now installed and its lift-condition is mechanically checkable, so it no longer waits on anyone catching a restart); and "nothing waiting for /next", which is now false — the cleared region holds thirteen build items.
**Rewritten again at the third 2026-08-06 planning close, and most of it is now redundant.** The merge itself is a **real work item** — [merge-blitz-2026-08-06-to-main], filed at that session, sitting below the readiness line awaiting the user's greenlight, and carrying the command, the trade, and the revert path in full. So this advisory no longer needs to hold any of that, and holding a second copy would let the two drift.
**What is left that the work item cannot say:** only the reason it is still below the line rather than cleared — the soak is a judgment the user makes, not a condition anything can check. Once that greenlight happens, this advisory has nothing left at all.
**Persist until the branch has been merged or discarded** — at which point delete it rather than rewriting it again; its content now lives on the work item.

#### Last session advises doing the history rewrite as its own deliberate session [advisory-cleared-run-then-history-rewrite]
**Rewritten at the 2026-08-05 close to only what still stands** — the first partly-spent advisory handled under the new rule from [codex-advisory-retire-argument-unweighed], which says a half-consumed advisory is rewritten rather than cleared or kept whole. What was consumed: "run the five cleared items first" (the overnight blitz ran them), the note that a full app restart was due (done — the session ran on host 1.17.0-test1), and the flag that [release-docset-b-work] looked spent (confirmed at the close: v1.17.0 shipped at `5f1a79e`, and the item now sits below the line awaiting reprocessing).
**What persists:** [history-rewrite-third-party-scrub] is the only exposure in this queue that is actively growing rather than sitting still in a design. It is deliberately below the readiness line and must stay there — it force-pushes a rewrite of the entire history and needs the user present, so it must never be reachable by an unattended run. **Open a session for it deliberately.**
**Persist until the history rewrite has been done** — not spent by a planning session reading it, because the recommendation concerns work that has not happened yet.

#### A dispatching work item goes stale silently when the thing it sent for comes back [dispatched-item-not-updated-when-answer-returns]
Captured by you 2026-08-06, as method feedback from a consumer project.
**What happened.** A `[user]` work item said, in effect, "open the other project, have the signal format designed, then bring it back." That happened. The format came back and was filed as a fresh capture. But **nothing updated the dispatching item**, which still read "go and design it" — so running its walk-through would have asked for work that was already done.
**The gap, stated as the general shape rather than the one instance.** Nothing in the method links a **returned answer** back to the **item that requested it**. A work item can dispatch a question outward; when the answer arrives it arrives as new material with its own life, and the requesting item is not a party to that. So the request stands unamended and reads as still-outstanding, indefinitely.
**Why it is worth a rule rather than vigilance.** It was caught only because the wind-down re-scan happened to notice the contradiction between two things in the same session. That is luck, not a mechanism: had the answer arrived a session later, nothing would have been holding both halves at once, and the next /next would have walked the user through work already finished — which is exactly the failure the never-ask-if-it's-done rule assumes cannot happen, because it assumes an item's text is current.
**It has a near-sibling already in the method, which is the argument that the shape is real.** The `Set aside ·` marker exists precisely because a part-walked item needed somewhere to record *what was reached*, so the next run resumes rather than restarting. This is the same problem one step earlier: what was **returned**. The marker supplies knowledge the never-ask rule depends on; there is no equivalent for a dispatch coming home.
**Directions to weigh at processing, not settled here.** Whether the filing of an answer should carry a back-reference to the requesting slug and prompt an amendment; whether a dispatching item should be required to name what "the answer has come back" looks like, the way a lift-condition names an event; or whether this is really the `[user]` lifecycle missing an update-on-return step. Note the honest counter before designing: the returning answer is often filed by a *different session in a different project*, which cannot see this queue at all — so any design that relies on the answering side remembering to look back will not fire.
**Files (rough):** `docs-b/plugin-behaviour.md` (the Captures rules and the `[user]` lifecycle), possibly `docs-b/plan.md` (the below-line revisit, which is the existing routine that re-reads items). Relates to [editing-state-signal-for-companion-apps] (the dispatch this was observed on), [false-positive-recorded-as-settled-capability] (the other way a queued item ends up resting on something no longer true).

#### The skip-to-defer step hardcodes a repo path for the queue mover, so it cannot work in a consumer project [skip-step-hardcodes-dev-repo-script-path]
Captured by you 2026-08-06, as method feedback from a consumer project.
**The finding, and it is one line.** `docs-b/plan.md`'s skip-to-defer step instructs `python plugin/si-plugin/scripts/reorder_queue.py QUEUE.md Unprocessed --move <slug> BOTTOM`. That path resolves only inside this development project. A consumer project has no `plugin/` folder, so a session following the instruction literally fails, and the script must be located in the installed plugin's own folder instead.
**One half of the report was checked and is WRONG, recorded so the build doesn't hunt for a second bug.** The feedback also named `done-plan.md`'s close reorder as carrying the same path. It does not. That doc says in its own words to locate the script **under the plugin root**, and both its invocations use `<plugin-root>/scripts/…`. Docset A's copy is correct too. **The broken occurrence is exactly one**, verified by grepping every `reorder_queue.py` reference across both docsets at capture time.
**What makes it a slip rather than a pattern — and this is the useful part.** `plan.md`'s own keep-step, about a hundred lines above the broken line, already uses `<plugin-root>/scripts/…` correctly. So the correct form was established in the same file and then not carried to the second invocation.
**Why nothing caught it here.** In this project the hardcoded path **works**, because this project genuinely has that folder. So the one environment where the method is exercised most is the one environment where the bug is invisible by construction — it can only be found from a consumer project or by reading. That is worth carrying into the fix: the same reasoning applies to any path a procedure doc names.
**To weigh at processing:** the fix itself is one line, but consider whether a grep for repo-relative paths across the shipped docs belongs with it, since this class of error is silent in the dev project by definition and there is no reason to think one line is the only instance.
**Files (rough):** `plugin/si-plugin/docs-b/plan.md` (the skip-to-defer invocation). Relates to [self-hosting-path-portability] (the same class of problem — absolute or repo-relative paths that only resolve here).

#### The show-then-write rule survives at step level in five docs, contradicting the write-first rule the method decided on [show-then-write-survives-at-step-level]
Captured by you 2026-08-06 — you proposed the cause after the third instance of Claude announcing an unmade write, asking whether the slip was probably the method's, since the approval flow has been reversed at some point and leftovers might remain. It was checked by grep rather than assumed, and it holds.
**Both rules are live in docset B at once.** `plugin-behaviour.md`'s doc-bound-text rule says approval-time text destined for one of the method's docs is **written to that doc first**, verified by re-read, and then pointed at. But the step-level instructions still say the opposite:
```
plan.md:485   keep-step        "Show the draft as a blockquote under **Work item:**.
                                Don't write until approved."
plan.md:685   wind-down        "Present all candidates as ONE numbered set of
done.md:471   re-scan           fully-drafted captures for a single approval"
plan.md:212   queue seeding    "show for approval, append on approval"
done.md:135   LOG entries      "show them for approval before"
done.md:621                    "draft it and show it for approval"
setup.md:112  migration        "getting approval before writing"
```
**Why this is a strong explanation rather than a convenient one.** A session reads the general rule once, at the start, and then reads a step-level instruction at the moment of acting — and step-level instructions govern the act. All three of the session's slips happened at steps in this list: the keep-step (thirteen times) and the wind-down re-scan. The observed behaviour is not "forgot to write"; it is **"composed an approval message, as instructed, and then described it as though the doc had been written"** — which is precisely what a half-migrated procedure would produce.
**One entry is probably NOT a leftover and must be decided rather than swept up.** `migrate-checklist.md:36` says *"DRAFT the whole converted queue and show it for approval — never write first."* Writing an entire speculatively-rewritten queue file before approval is a materially different risk from writing one work item, so that instruction may be deliberate. The build decides it explicitly; a blind sweep would silently reverse a possibly-correct call.
**To weigh at processing.** Whether every remaining site converts to write-first-then-point, or whether some approval-time outputs genuinely stay show-first — the doc-bound-text rule already carves out non-doc-destined text (a commit message, a recommendation, options to choose between), so the sweep must apply that boundary per site rather than uniformly. And whether the general rule should say, in one line, that it **overrides any step-level instruction to show before writing**, since that is the collision that produced this.
**Files (rough):** `docs-b/plan.md`, `docs-b/done.md`, `docs-b/setup.md`, `docs-b/migrate-checklist.md` (the decision case), and `docs-b/plugin-behaviour.md` (the precedence line). Trace the ripple by grepping the literal phrases at build time rather than working from the list above, which was a point-in-time grep. Docset A is frozen — it predates the write-first decision entirely, so it is internally consistent and needs nothing. Relates to [claude-pointed-at-a-write-it-had-not-made] (the three instances this explains), [approval-flow-token-doubling-simplification] (the decision that introduced write-first).

#### Claude announced a write it had not made, twice in one session, including in the message capturing the first instance [claude-pointed-at-a-write-it-had-not-made]
Filed at the 2026-08-06 planning session's wind-down re-scan, as a testing outcome.
**What happened, twice.** Mid-session Claude told the user a capture had been filed and gave a pointer to its heading — **before writing it**. Caught on the next turn by Claude itself and corrected, and the text was then written. Later, at the wind-down re-scan, Claude presented three drafted captures saying *"I've written all three so you can see them in place"* — again before writing them. The second instance occurred **in the message that was capturing the first**.
**The rule was loaded, specific, and quoted the failure almost exactly.** The doc-bound-text rule says the pointer goes out only after the write returned success *and* a re-read confirms the content is there; that a pointer must never be emitted from the *intention* to write; that this has already gone wrong once before; and that a pointer misreporting a write is **worse** than pasting, because the user opens the doc, finds nothing, and has no copy in chat either.
**Why it is worth more than another instance on the pile.** The rule identifies the mechanism precisely — *"a pasted draft cannot be claimed without being produced; a pointer can"* — and that asymmetry is exactly what makes the slip available. Write-first-then-point removed the risk of the user approving text that was never written, and introduced the risk of Claude *reporting* text that was never written. The second instance is the stronger evidence: at that moment the failure was the session's active subject, which is as much local attention as any rule can get, and it still happened.
**To weigh at processing.** This is now the fourth recorded instance of a clearly-worded loaded rule slipping under local pressure ([claude-reached-for-shell-write-against-rule], [false-positive-recorded-as-settled-capability], [bundled-double-ask-misapproval], and this). **Unlike those three, this one may be mechanically catchable**, and that is the reason to look hard rather than write another rule: the claim is about a file's contents, and file contents are checkable. Weigh whether the announcement should be *produced by* the verification rather than written alongside it — a pointer emitted only from a confirmed re-read cannot be produced from an intention, because there is nothing to read. Note the honest counter: no hook can inspect prose Claude is about to emit, so the mechanism, if it exists, is a procedural one — the pointer is composed from the re-read's result — not an enforcement layer.
**THIRD instance, same session, and it changes the diagnosis — see the sibling item below.** Minutes after the second, Claude again announced a capture as written and on disk before writing it. Three occurrences in one session, two of them inside messages *about* the failure. At that point "a rule slipped under local pressure" stops being a sufficient explanation: no amount of local attention was going to fix something that recurred while it was the sole subject of attention.
**The user proposed the actual cause and it checks out: the method still tells sessions to show first.** See [show-then-write-survives-at-step-level] — the shipped docs carry BOTH rules, and the show-first version sits at the exact steps where all three slips happened. So the announcing-before-writing reflex is very likely being *followed from the procedure*, not merely leaking past a rule. **This item should be processed after that one, or folded into it**, because if the contradiction is the cause then removing it may be the entire fix, and any rule written here would be a second patch over a conflict that would still exist.
**Files (rough):** `docs-b/plugin-behaviour.md` (the doc-bound-text rule). Relates to [show-then-write-survives-at-step-level] (the probable cause — read first), [claude-reached-for-shell-write-against-rule], [false-positive-recorded-as-settled-capability], [bundled-double-ask-misapproval] (the same slip-shape), [approval-flow-token-doubling-simplification] (which made write-first-then-point the default and therefore created this exposure).

#### The queue mover silently repositions the readiness marker on a plain in-section move, clearing shelved work [mover-move-repositions-readiness-marker]
By Claude, observed live 2026-08-06 during the planning session's final action.
**What happened.** The command was `reorder_queue.py QUEUE.md Processed --move merged-plugin-live-verification AFTER concurrent-session-support` — a request to move one item, with **no `--marker-after` argument given**. The script reported `Processed reordered (50 items), marker placed` and moved the `--- Cleared to run above this line ---` marker as well. The result: [concurrent-session-support] — deliberately below the line because it rewrites `session_start.py` and changes how **every** session begins — ended up **above** it, cleared to run unattended. So did the moved item itself, which was the opposite of the intent.
**Why this is serious rather than untidy.** That marker is the method's authorisation boundary: it sets how much work an unattended /next run may build without the user present. A tool that moves it as a side effect of an unrelated request is silently widening what an autonomous run is allowed to do. The queue's own [concurrent-session-support] item already names this class of thing — the readiness marker is on its NEVER-silently-resolved list, on the reasoning that it defines scope rather than merely being a document. The same argument applies with more force to a script that moves it without being asked.
**Nothing would have caught it.** The script printed `marker placed`, which reads as reassurance rather than warning and does not say the marker *moved*. The queue lint checks structure, and the structure was valid — a marker in a legal position, all items present. `git diff` would show it, but the mover rewrites the whole file, so the diff is not something a session reads line by line. It was caught only because line numbers were checked immediately afterwards for an unrelated reason.
**The fix direction, to settle at processing.** The marker should be **invariant under any operation that was not asked to move it**: `--move` and `--move-section` preserve its position relative to the items around it, and only `--marker-after` moves it. Weigh what "preserve" means when the moved item crosses the marker — arguably the move should then be refused, or reported loudly, rather than resolved silently in either direction. And the success message should state the marker's position explicitly whenever it changed, since `marker placed` currently conveys nothing.
**One narrowing observed at the same session's close:** a `--move` within **Unprocessed** left the marker untouched. That section has no marker, so this is consistent with the fault being in whatever re-places the marker during a **Processed** operation, rather than in the move logic generally. A starting point for the build, not a conclusion.
**Check the same class of bug in `--move-section` before assuming it is confined to `--move`** — that variant moves items between sections and was used roughly thirteen times in this same session. No misplacement was observed, but it was not specifically watched for, and the absence of an observation is not evidence.
**Files (rough):** `plugin/si-plugin/scripts/reorder_queue.py`. Relates to [concurrent-session-support] (whose never-silently-resolved list names this exact marker and this exact reasoning), [mover-edit-sequence-normalises-a-safety-warning] (the other mover-behaviour item from the same session), [lint-blind-to-readiness-marker].

#### mdreader is the reader direction, and the editor-switch route is closed [mdreader-as-the-reader]
Captured by you 2026-08-06, reversing the 2026-08-05 decision that deleted [mdreader-as-throughliner-reader] in favour of moving to the VS Code extension. That earlier item was deleted the same day it was raised, once VS Code was researched; you have reversed it, and [move-to-vscode-extension] was deleted in its place at this session.
**The reversal is recorded rather than assumed, because the record held the opposite.** The 2026-08-05 reasoning was that VS Code removes the viewer the three bugs live in, at zero build cost, against a Windows app written from scratch by a first-time Windows developer. That argument was put to you again at this session before acting. **Your decision stands: mdreader only.**
**What made the editor-switch route untenable is bigger than a preference, and it is now product truth.** At this same session you settled that **the Claude Code desktop app is the platform** — a product decision, not a description of your habits — and that the terminal is never a route because it is not approachable for the people this method is for. That is written into SPEC.md under Who it's for. An editor switch is the same shape of answer as a terminal: it solves a problem by leaving the surface the audience is on.
**Three doors close with it, and they are written down so nobody follows a dead pointer.**
```
the Gradle/JVM sandbox   ->  no full-trust surface is coming. Solved WITHIN the
                             app, by naming the constraint and handing the user
                             back to Android Studio — see [desktop-app-sandbox-
                             blocks-local-toolchains], re-scoped at this session.
the status line          ->  the only route to Claude reading its own context
                             level. Stays shut; the desktop app ignores the
                             setting (anthropics/claude-code#41456).
the three viewer bugs    ->  #84017, #84018, #83475 are now permanent until
                             Anthropic fixes them. [queue-pointer-hard-to-follow-
                             and-possibly-stale] updated accordingly.
```
**What mdreader does and does not address, stated plainly so the scope is not inflated later.** It is a reader. It addresses reading method documents comfortably. It does **not** change where Claude Code runs, so it touches none of the three doors above. Anyone reading this later should not record mdreader as having solved them.
**To settle at processing:** whether this project has anything to build at all, or whether mdreader is simply your other project and this queue's only interest is that the editor-switch option is closed. The honest lean is the latter — the method's docs make no claim about which reader you use, and the one place a reader assumption ever lived (the retired `Editor:` field) is already gone. If that holds, this item's whole content is the three closed doors above, and it can be deleted once they are safely recorded elsewhere.
**Files (rough):** likely none in this repo. Relates to [queue-pointer-hard-to-follow-and-possibly-stale], [desktop-app-sandbox-blocks-local-toolchains], [retire-working-mode-for-short-summary-default].

#### A set-aside marker and a lift-condition on the same item silently cancel each other out [set-aside-silences-the-lift-condition]
Captured by you 2026-08-06 — you asked what would actually ask you *"are you back at the desktop yet?"*, and whether a lift-condition that isn't a capture or an advisory is blessed by the method at all. Checking the answer exposed the conflict.
**The answer to the question as asked is yes, the mechanism exists.** /plan's below-the-line revisit walks every below-line item and gathers the conditions only the user can answer into **one consolidated question, asked once per planning session**. So a lift-condition is not an orphan note; something does read it.
**But it stops being true the moment the same item also carries a `Set aside ·` marker, and nothing names that interaction.** The marker's entire purpose is to take an item **out** of that consolidated question, on the user's own not-now, with queue exhaustion as the only guaranteed way back. So an item carrying both has a condition that will never be asked about — which is precisely the silent shelf the method spent months eliminating, arrived at by following two correct rules at once.
**It was nearly created live.** At this session Claude was about to move a walk-through below the line with a lift-condition **and** record the user's words as a set-aside marker, treating both as obviously right. The user's question caught it before it was written. That is the shape worth noting: neither rule is wrong, and each is well-reasoned in isolation, so nothing about applying both together feels like an error.
**One live instance already exists and is not hypothetical.** [merged-plugin-live-verification] carries a set-aside marker from 2026-08-05 with a retry of "when the user is back at their computer", and was moved below the readiness line at this session. So nothing is currently asking about it, by design — correct behaviour, but the user should not have to discover that by asking.
**To weigh at processing.** Whether the two are simply exclusive — an item has a lift-condition **or** a set-aside marker, never both, with a stated rule for which wins when a session is about to write the second; or whether a set-aside marker should *carry* the condition forward so queue exhaustion is not the only route back. Note the honest tension: the marker exists precisely to stop repeat-asking, so any fix that quietly restores the asking rebuilds the nagging it removed.
**Files (rough):** `docs-b/plugin-behaviour.md` (the Set aside section and the below-the-line revisit), `docs-b/plan.md` (the revisit's classification table, which currently lists `set aside` and `user-only` as separate outcomes with no rule for an item that is both), `docs-b/done-plan.md` (where lift-conditions are written). Relates to [merged-plugin-live-verification] (the live instance), [queue-pointer-hard-to-follow-and-possibly-stale] (the counter-example — a mechanically-checkable condition needs no asking at all, which may be the real lesson).

#### /plan miscounted its own item list from memory while the list sat in its working file [plan-miscounted-its-own-item-list]
Filed at the 2026-08-06 planning session's wind-down re-scan, as a testing outcome. Appended beside its sibling: both are that session's own behaviour.
**What happened.** Claude presented an item as the session's last with one still remaining, then corrected itself on the next turn. The count was reconstructed from conversation memory. A numbered list of all sixteen items — written at the start of processing, for exactly this purpose — was sitting in `_plan.md` and was not re-read.
**Why it is worth filing despite costing nothing.** The consequence here was one wrong sentence. The shape is the method's central design assumption failing: **the working file exists so a session does not depend on remembering, and the session remembered instead.** The same reflex against a checkpoint's item, a disposition, or a skipped slug would produce a wrong queue rather than a wrong sentence, and nothing would catch it — the file would still say the right thing, unread. This session also proved the file works when consulted: every disposition was appended to it as it happened, and that record is intact.
**To weigh at processing.** Whether the checkpoint step should re-read `_plan.md` rather than continue from context — cheap, since the file is small and already open in the procedure — or whether this is one slip not worth a step. Note the general form, which may matter more than this instance: **wherever the method writes state to a file to survive compaction, it should say that the file is the source, not the backup.** A file consulted only when memory fails is not compaction-proof, because a session with intact-feeling memory never knows to consult it.
**Files (rough):** `docs-b/plan.md` (the planning state file and the checkpoint), possibly `docs-b/next-build.md` (`_build.md` has the identical relationship). Relates to [claude-pointed-at-a-write-it-had-not-made] (the same session's other self-observation), [concurrent-session-support].

