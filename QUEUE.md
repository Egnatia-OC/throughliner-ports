# QUEUE

Two sections. **Processed** — work discussed and agreed, ordered top-to-bottom; /next builds from above the `--- Cleared to run above this line ---` marker. **Unprocessed** — captured, not yet fully processed; the next /plan weighs each item. A work item is a `#### ` heading (its description) with a `[slug]` at the end of that line and its rationale in the block beneath. A leading `[audit]` / `[user]` tag names how it's executed; no tag means a build. A work item carrying a security or privacy risk gets a `Red flag · State: …` marker — the flag rides the work.

## Processed

#### [user] Confirm the three app-behaviour claims the method's most-used rules rest on [user-verify-app-ui-premise-claims]
Split from [audit-premise-claims-verification] at its processing, 2026-08-07. **The capability check was run rather than assumed:** these are claims about what the Claude Code desktop app does on screen — what a link does when clicked, whether a button appears, whether suggested replies render. Claude has no view of the app's interface from inside a session and no tool that would show it, so this is a genuine absence rather than a browser-shaped task handed over out of habit.
**Why it is worth your few minutes.** Each of these underpins a rule applied many times per session, and each was true when written and has never been re-checked. If one has changed, the rule resting on it is now either over-cautious or actively wrong.
**Walkthrough.**
1. **Line-anchored links.** Claude sends you two links to the same file — one plain, one with a line number on the end. Click each. Report what happens: does the plain one open the file at the top, does the line-numbered one do nothing, or has that changed? *The rule resting on this: pointers are forbidden from carrying line numbers on `.md` files, which makes every pointer land at the top of a long queue.*
2. **Fenced blocks.** Claude sends you a shell command in a code block and a long line of text in another. Report whether the shell one shows a Run button, and whether the long line wraps or runs off the side of the screen. *The rules resting on this: code goes in fences and prose never does — the second exists because a long draft in a fence once ran off-screen where you could not read it.*
3. **Suggested replies.** At the next stop where Claude asks you something, look at whether the app offers grey suggested replies in the input box. Report whether they appear at all, and whether they appear when you are on remote control. *The rule resting on this: every stop must name both replies in its own text, because those suggestions cannot be relied on — a close once took about forty minutes through a relay when half an ask lived only in a suggestion.*
4. **Line anchors on CODE files — added 2026-08-07 by [md-line-anchor-opens-at-top-not-dead].** Claude sends you a link to a `.py` or `.json` file with a line number on the end. Click it and report whether it opens **at that line** or at the top. *The rule resting on this: the docs state that code files honour their line anchors while `.md` files do not — and the `.md` half of that pair was just found to be wrongly described, so the code half should not be assumed right because its sibling was checked. If code files also ignore the anchor, the whole distinction goes, and with it the advice to keep line numbers on code links.*
**Done when** all four are reported. Any that has changed becomes a fresh capture naming the rule it undermines.
**Completion is user-reported and that gap is deliberate** — there is nothing observable from here, per the `[user]` lifecycle's own note that where nothing observable exists, the item waits until you mention it.
**Files:** none — this produces knowledge. Relates to [audit-premise-claims-verification] (the Claude-runnable half), [queue-pointer-hard-to-follow-and-possibly-stale] (which tracks the three filed Claude Code bugs, one of which is claim 1).

#### [user] Re-run the fence-wrapping and suggested-reply checks while on remote control [user-verify-remote-control-rendering]
By Claude, from the 2026-08-07 walkthrough of [user-verify-app-ui-premise-claims]. **The capability check was run rather than assumed:** this is what the Claude Code app renders on a phone or remote session, and no tool available from inside a session can see that surface — the browser tools drive a browser, not this app's own interface. A genuine absence, not a browser-shaped task handed over out of habit.
**Why it exists.** Two of that walkthrough's three claims were settled at the desk and left half-open, because **the failures they were written from both happened on remote control**. Leaving that as a sentence in a log entry is how user work evaporates, so it is a work item instead.
**What is already known, so this doesn't get re-run wholesale.** At the desk: a long sentence inside a fence **wraps** and is fully readable; a shell-tagged fence **does** show a Run button; suggested replies **did not appear at all**. Those are settled and need no repeating.
**Walkthrough — two checks, when you next happen to be working on remote control. There is no need to make a special trip.**
1. **Fence wrapping.** Ask for a long sentence inside a plain code block. Report whether you can read it to the end, or whether it runs off the side and needs sideways scrolling. *The rule resting on this: prose never goes in a code block — and "fences don't wrap in the app" is that rule's only stated reason. It is now known false at the desk. If it is false on remote control too, the rule has no reason left and something has to change; if it is true here, the rule survives with a much narrower reason than it currently states.*
2. **Suggested replies.** At any stop where Claude asks you something, report whether grey suggested replies appear in the input box. *The rule resting on this: every stop must name both possible answers in its own words. It is already the safe direction — the method assumes the suggestions are absent — so this confirms rather than risks anything.*
**THE TWO CHECKS ARE NOT EQUAL, and this is written in so a half-answer is not read as no answer.** Check 1 carries real work: [fences-wrap-so-prose-rule-reason-is-false] is `Blocked by:` this item and cannot be built without it, and the answer decides whether a rule dies, narrows, or stands. Check 2 confirms rather than risks anything — the method already assumes the suggestions are absent, and the desk run found they did not appear at all. **So if only one ever gets done, it is check 1, and reporting check 1 alone unblocks the dependent item.**
**The over-tag guard was re-run at processing 2026-08-07 rather than inherited.** The tools available from inside a session drive a *browser*; the Claude Code app's own interface is not a page any of them can reach. There is no tool to name, so this is a genuine absence.
**Kept above the readiness line deliberately, not because it is urgent.** It is opportunistic — no special trip — so it sits where /next can offer it during ordinary runs, giving it many chances to be caught in passing. Below the line it would surface only through the consolidated waiting-item question, where a single "not yet" would record a set-aside and silence it until the queue ran dry. Above the line costs nothing: you defer it with a word, and that defer records itself.
**Done when** both are reported, or check 1 alone if that is what the occasion allows. Anything that differs from the desk results becomes a fresh capture naming the rule it affects.
**Completion is user-reported and that gap is deliberate** — nothing here is observable from inside a session, per the `[user]` lifecycle's own note that where nothing observable exists, the item waits until you mention it.
**Files:** none — this produces knowledge. Relates to [fences-wrap-so-prose-rule-reason-is-false] (the rule waiting on check 1), [user-verify-app-ui-premise-claims] (the desk-side walkthrough this completes), [md-line-anchor-opens-at-top-not-dead].

--- Cleared to run above this line ---

#### Fenced blocks DO wrap at the desk, so the prose-never-in-a-fence rule has lost its only stated reason [fences-wrap-so-prose-rule-reason-is-false]
Captured by you 2026-08-07 — you ran the check in [user-verify-app-ui-premise-claims]'s walkthrough and reported that a long sentence inside a fence wrapped and was fully readable.
**What the docs say.** `docs-b/plugin-behaviour.md:170-175`: *"Prose never goes in a fence, even when the user will paste it elsewhere… The failure this replaces: **fences don't wrap in the app**, so a long draft ran off-screen — a user on remote control could not read the text they were being asked to approve."* That sentence is the rule's **entire** justification. The sibling fencing rule above it has two independent reasons (character-exactness and the Run button); this one has one, and it is a claim about rendering.
**What actually happens, observed live.** Sent a ~280-character single sentence inside a plain fence, you reported it **wraps** and the whole thing is readable without scrolling sideways. So on the desktop app as it stands, at the desk, fences wrap.
**Confirmed in the same check, and worth banking:** the Run button **does** appear on a shell-tagged fence. That half of the fencing rules stands, and it is the half that actually earns the "one command per block" discipline.
**The part that is genuinely still open, and must not be collapsed into the finding.** The recorded failure was specifically *"a user on remote control could not read the text"*. Remote control was **not** tested — you were at the desk. Remote control is a different rendering surface, and this method already knows it differs in at least one way (the reply suggestions never render there, which is claim 3 of this same walkthrough). So the honest state is: **falsified at the desk, unverified on remote control.** A build that deletes the rule on this evidence alone would be repeating the mistake this whole verification pass exists to catch — reading a claim more broadly than it was tested, the same shape already recorded twice against the orphan-tag history and the remote-branch list.
**What it means either way.** If fences wrap everywhere, the rule loses its reason entirely and should either be deleted or re-founded on something true. If they wrap at the desk but not on remote control, the rule survives with a corrected and much narrower reason — and that narrowing matters, because the rule currently reads as absolute and shapes how every long draft is presented. Settle the remote-control case before touching the rule.
Blocked by: [user-verify-remote-control-rendering]
**Why that field and why below the line.** The rule cannot be touched until the remote-control surface is checked, and that check is queued work rather than an external event — so it is a `Blocked by:`, and this item sits beneath its blocker as the lint requires. Kept below the readiness line deliberately: if it sat above, a /next run could reach it before the user had a chance to run the check, which is precisely the premature edit this item exists to prevent.
**THE RIPPLE WAS GREPPED AT PROCESSING, 2026-08-07: two sites, and the second is a different rule.**
```
docs-b/plugin-behaviour.md:170-175   "Prose never goes in a fence" — the
                                      no-wrapping claim is its ENTIRE
                                      justification                      -> FIX
docs/plugin-behaviour.md:20          docset A gives the SAME false reason
                                      for a DIFFERENT instruction: why
                                      approval-time output is a blockquote
                                      rather than a fence                -> FIX
```
Docset B's approval-time rule states the blockquote default **without** that reason, so B is unaffected there — the damage in B is confined to the prose-in-fence rule. Docset A's edit is a **correction** (a stated reason that is factually false), which the freeze permits; author it in A's own register rather than pasting B's.
**Evidence banked from the 2026-08-07 planning session, because it changes how much is at stake.** The user asked for a pasteable block and was given one — the rule's own escape hatch (*"if the user wants a paste block, offer one and produce it on request"*) working exactly as written. So the rule is **not** the absolute it reads as, and the practical cost of keeping it is lower than "its reason is false" suggests. This matters at build: the *instruction* may well survive on plainer grounds — a long draft reads better rendered than fenced, wrapping or not — even though the *reason* has to go either way.
**The three outcomes, so the build does not have to re-derive them.** If fences wrap on remote control too, the stated reason is dead everywhere and the rule is either deleted or re-founded on readability. If they do not wrap there, the rule survives with a corrected and much narrower reason, and that narrowing matters because it currently reads as universal. Either way **docset A's sentence is wrong today** and does not wait on the check — A claims fences do not wrap at all, which the desk test has already falsified.
**Files:** `plugin/si-plugin/docs-b/plugin-behaviour.md` (the prose-in-fence rule) and `plugin/si-plugin/docs/plugin-behaviour.md` (docset A's blockquote reason). Relates to [user-verify-remote-control-rendering] (the blocker), [md-line-anchor-opens-at-top-not-dead] (the sibling falsification from the same walkthrough, same two-docset shape), [user-verify-app-ui-premise-claims] (the desk walkthrough that found it).

#### Merge the blitz branch into main once the soak has gone quietly [merge-blitz-2026-08-06-to-main]
By Claude, split out at the 2026-08-06 processing of [retire-docset-a] because that item's dependency turned out to have no durable home.
**Why this exists at all.** The branch `overnight-blitz-2026-08-06` carries the blitz plus the planning sessions that followed it — twenty-plus processed work items, deletions, a preserved session transcript, and the editing-state contract another of your projects is waiting on. `main` is untouched. The merge was tracked only inside a forward advisory, and advisories are transient by design and cleared at a close. Meanwhile the queue refers to `[merge-blitz-branch-to-main]` as though it were a live item; that was the **previous** branch's item and it is gone. So the one action several items wait on existed only in a note built to be thrown away — the under-filing shape the method already names.
**It is Claude-work, not `[user]` work — the tag was checked rather than assumed.** `git checkout main && git merge overnight-blitz-2026-08-06` is a command Claude runs, and `git` is present and working. What is yours is the **greenlight**, which is a decision, not an action — hence below the readiness line rather than tagged `[user]`. This is the distinction the over-tag guard exists for: "Claude can't decide this" is not "Claude can't do this".
**The trade, stated plainly because it has shifted and will keep shifting.** The branch's whole purpose is that you can live on the blitzed state before committing to it. But work done during the soak lands on the branch too, so a late revert discards the soak days' work along with the blitz. That cost was small on day one and is not small now. The balance therefore moves toward merging once the soak has gone quietly, rather than holding the branch open indefinitely to preserve an option that costs more every day it is kept.
**The build:** `git checkout main`, merge the branch, confirm the merge is clean and the queue's structure survives it (section headings, the readiness marker, the item count), push, then re-run the plugin update so the installed host is snapshotted from `main` rather than the branch. If it has **not** earned trust instead, the opposite path is checkout `main`, re-run the plugin update to re-snapshot it, and restart — and that decision is also yours.
**Lift-condition:** you are satisfied the soak has gone quietly — no misbehaviour traced to the blitzed build across ordinary sessions. Mechanically checkable in one respect only, and the revisit should check it: whether the branch still exists un-merged (`git branch --merged main`). Everything else is your judgment. **Greenlight it at any /plan close, or say so at any point.**
**Files:** none in the repo — this is a git operation plus a host reinstall. Relates to [retire-docset-a] (blocked by this), [concurrent-session-support] (whose branch-per-session design this is the second hand-run data point for), [advisory-soak-blitz-branch-then-merge] (the transient note this replaces).

#### Retire the frozen 4.8 docset A once its retirement conditions are met, and update the README accordingly [retire-docset-a]
Captured by you 2026-08-05. You proposed retiring A immediately, on the grounds that docset B is well established; after Claude cited the freeze decision (`d1bfeda`, 2026-08-02 — A frozen as the known-good fallback so a bad migration can never strand the project, recorded there as your non-negotiable), you chose to hold the freeze and file retirement as future work instead. So this item is the retirement, made deliberate: it happens when its conditions clear, not by drift.
**Why not now, recorded so the timing isn't re-litigated:** docset B has only been selected live since 2026-08-04 and the blitz's work is still soaking on an unmerged branch; the measurement harness ([docset-b-register-decision-weaker-than-recorded]) states its safe window is "while A still stands" — retiring A first closes that window before anything has been measured; and A costs no tokens in a 5-series session (only one docset loads), so keeping it is shelf space, not context.
Blocked by: [merge-blitz-2026-08-06-to-main]
**Why that field rather than prose:** the first condition below is "the blitz branch merged and soaked", and at this processing that action had no durable home — it lived only in a forward advisory, which is cleared at a close. It was split out into its own work item at the same moment, so the dependency now points at something real and the queue lint can check it.
**The three conditions, settled at processing 2026-08-06 rather than left open.**
1. **The branch merged and soaked** — now [merge-blitz-2026-08-06-to-main], the `Blocked by:` above.
2. **The harness, and it is SOFT.** The item's own wording allows "a deliberate decision that measuring isn't needed", so this never hard-blocks. What it does mean is that retiring A **closes the measurement window permanently** — [docset-b-register-decision-weaker-than-recorded] states its safe window is "while A still stands", because a register experiment is survivable only when a known-good fallback exists. So the choice at greenlight time is explicit: run the harness first, or decide on the record that the method will never measure this and accept that.
3. **The 4.8 / absent-model question COLLAPSES, and this is the part that was mistaken for design work.** The item reads as though session_start's routing needs redesigning. It doesn't. With one docset there is no choice to make, so the model-detection branch and the default-when-absent branch are **deleted, not rewritten** — every session loads `docs-b/` because there is nothing else to load. What survives is not a design question but a **fact to check at greenlight time**: are there any 4.8 users left? If yes, retiring A hands them a docset authored by subtraction for the 5-series, and the freeze decision says plainly that B's lighter register regresses 4.8's rule-following. That is a cost to accept deliberately, not a thing to design around. The no-strand guarantee is carried instead by the missing-docset state line inherited below.
**The build, once cleared:** remove docset A (`plugin/si-plugin/docs/`) from the package; **delete** session_start's docset-selection branch and its default-when-absent fallback, leaving an unconditional load of `docs-b/`; remove the docset-routing directive's substitution language, since there is nothing to substitute; update CLAUDE.md's Model target section (the two-docset architecture and the freeze rule both stop being true), SPEC.md's two-docsets paragraph, README, and the FAQ's docset entry. Scope the full ripple by grepping the docset literals (`docs/`, `docs-b/`, `docset`) per the hook-enforced-format tracing rule — this is exactly the shape that rule exists for, since the literals are read by a hook.
**Inherit, don't re-derive, the missing-docset behaviour from [session-start-silent-docset-fallback].** That item builds a state line for the case where the selected docset's folder is absent from the installed host. Retiring A removes the fallback that made such a case survivable, so with one docset a missing folder means no procedure docs at all — the state line stops being a diagnostic nicety and becomes the only safe response. Reuse its wording and its host-versus-working-tree distinction rather than writing a fresh one.
**Files:** `plugin/si-plugin/docs/` (removal), `hooks/session_start.py`, `CLAUDE.md`, `SPEC.md`, `README.md`, and the FAQ template pair — final list from the grep. Relates to [docset-b-register-decision-weaker-than-recorded] (the safe window this closes), [structural-delimiting-experiment], [session-start-silent-docset-fallback] (whose logic this inherits).
**Lift-condition:** its `Blocked by:` clears, **and** you greenlight it — the harness has either run or been consciously waived, and the 4.8-user question has been checked rather than assumed. Not a mechanical lift: the last two are judgments, so this rises when you say so at a /plan close, not on a check coming back true.

#### [user] Verify the merged two-section plugin behaves live [merged-plugin-live-verification]
By Claude. The post-merge dogfooding check — one live confirmation across real sessions rather than a pile of deferred observation items, per the standing rule that broken behaviour shows up in use and re-captures itself. **Checks rewritten at the 2026-08-05 planning session** (via [merged-plugin-verification-checklist-stale]): the old list tested for a provenance lint that was deliberately removed, and predated the readiness-marker, orphaned-prose, and inserted-heading checks. Run on the installed host (1.18.0-test1 or later) in real /plan, /next, and /done sessions on the real queue — most checks confirm themselves during the soak's ordinary sessions; only the /setup line needs a deliberate detour:
- **session_start:** an uncleared red-flag work item is surfaced first-thing; the docset directive matches the project's `Model:` field; the installed-version and build-stamp lines arrive.
- **The queue lint** (advisory, after any QUEUE.md edit): runs clean on the real two-section queue, and flags: a `#### ` heading with no `[slug]`; a red-flag marker whose state is neither cleared nor uncleared; a missing section heading; a readiness-marker problem (no marker while Processed holds work, two markers, or a marker sitting in Unprocessed); orphaned prose belonging to no item; a heading newly inserted under Processed while a build is active. It never flags provenance — that is by design, not a gap.
- **/next:** picks the top Processed items above the readiness line and builds several back-to-back; self-scopes _build.md's Files list; treats a missing readiness marker as nothing-cleared (halts rather than building everything); leads a `[user]` item with a live walk-through, one step at a time, with no completion ask; halts when the same error repeats three times on one item; closes a multi-item run with one summary commit.
- **/done:** routes by work-item flavor, names LOG files by slug, cross-checks shipped slugs against Processed.
- **/setup** (in a scratch project if convenient): scaffolds a two-section QUEUE.md plus `resources/research/`.
- Folds in the [device-access-consent] resolution check: the first build needing on-device verification asks permission before connecting.
Host-side — anything found broken routes to a fresh capture.
Set aside · 2026-08-05 · "I can't do that as I'm not at home, I'm on remote control" — reached: the passive checks confirmed themselves in the 2026-08-05 /next run (session_start version + build-stamp lines arrived, docset matched the Model field, multi-item run built top-down with self-scoped Files list, queue edits drew no lint flags, [user] walk-through led with no completion ask); the /done close check confirms at that session's own close — stopped by: the /setup detour needs a fresh session on a scratch folder, which needs the user at their desktop — retry: when the user is back at their computer.

#### Improve SI for concurrent session running [concurrent-session-support]
By Claude at the user's request. The user wants SI made safer for running concurrent sessions — e.g. a /plan and a build at the same time.
**Premise correction (2026-07-29):** /next "only appends" is not quite right — its *close* removes shipped items from Processed and repositions the cleared-to-run marker, so a /next close and a /plan session can both rewrite QUEUE.md structure at once. The contention is real.
**Reframe reached (2026-07-31):** Drive-sync is *Alex's personal environment*, not a method-general blocker. SI's users are mostly not on Google Drive, so for them there is no sync floor and in-repo concurrency is solvable in-method — a structural safeguard is worth building for the majority; Alex covers her own case by pausing Drive during sessions. The old "the hard half is outside SI's control (Drive conflict copies)" framing is Alex-specific and should be corrected — it is NOT a method-general truth.
**Detect-and-warn design (2026-07-31) — rejected, superseded by the merge direction below.** The earlier proposal was detect-and-warn: (1) liveness by working-file mtime; (2) a session_start awareness note; (3) a pre-write stop-and-ask recheck; (4) `.git/index.lock` retry/backoff. Recorded so it isn't re-proposed; replaced by the 2026-08-01 direction.
**Design progress banked (user + Claude, 2026-08-01) — continue next session, still undesigned:**
- **Drop staleness entirely (user).** A session should always be *closed* properly, never left to go "stale" — so detecting staleness by mtime is the wrong model. Design so an abandoned-live session doesn't happen, rather than detecting it after the fact. (Kills proposal #1.)
- **The session_start awareness note (#2) stays** — fine as-is.
- **New core direction — live/canonical queue, merge-at-close (user's idea, better than detect-and-warn).** Each session edits its own *live* copy of the queue; at close, its changes merge into the *canonical* version. **Use git if that makes it easier to build (user approved).** The merge is automatic and clean when two sessions touched *different* items/sections; a conflict arises only when both touched the *same* region. Claude resolves the merge **silently**, surfacing to the user **only** when a conflict genuinely can't be resolved safely — the user never sees raw conflict markers. Reframes the problem from *preventing* concurrent writes to *letting them diverge and merging at the end*. Real design work: (a) structure the queue so sessions rarely collide; (b) make Claude's silent conflict-resolution trustworthy.
- **`.git/index.lock` retry/backoff (#4) — minor robustness only.** Two simultaneous commits make git's brief lock file error one of them; retry-after-a-moment avoids it. A small nicety, not core.
- **Cloud-sync detection (user).** Google Drive / OneDrive / Dropbox syncing the folder is a *separate* failure mode from in-repo concurrency — these make their own duplicate "conflict copies" and file locks. Add a startup check for whether the project folder is being synced, and surface it. (Correctly re-homes the old Drive-specific worry as one instance of a general cloud-sync issue.)
- **Remote-control convergence.** Remote sessions *already* auto-branch (a git worktree); the manual pain has been merging them back. The merge-at-close machinery here removes that pain, which is why the separate worktree-override hook was deleted rather than built (2026-08-05) — its mechanism facts are carried below.
- **Desktop vs remote asymmetry — the open crux for next session.** Remote already forks (branch-per-session free; only auto-merge missing). Desktop shares the one main tree, so matching that isolation means deliberately branching each desktop session, or a separate-file fallback. Fork to resolve: branch *every* session, or isolate only where sessions overlap?
- **Scope distinction to resolve, not conflate (user, 2026-08-01):** full-project branching vs queue-only branching. The contention is fundamentally about QUEUE.md, so branching *just the queue* may suffice; full-project branching (what the remote worktree already does) is the heavier hammer. Fine to scale up to full-project *if* it solves both concurrency and remote-merge neatly — but name the two scopes so a future pass chooses deliberately rather than sliding between them unnoticed.
**Unnamed cost, added 2026-08-04 from the scope-lock audit (`resources/research/scope-lock-audit.md` §8.6):** the merge-at-close direction has Claude silently resolving conflicts in QUEUE.md. QUEUE.md is the artifact that *defines* scope — the cleared-to-run marker sets how much autonomous work is authorised, and the Files list of every item derives from it. So a silent automatic merge is an expansion of agent authority over the boundary itself, not just over a document, and the design above doesn't frame it that way. Weigh at design whether the queue's *structural* lines (the readiness marker, section headers, red-flag states) should be excluded from silent resolution and always surfaced, even when the merge is technically clean.
**A live instance, 2026-08-05 — and it is worse than this item's design discussion anticipates.** Two sessions ran concurrently **in the same folder**: an overnight-blitz session left open, and a planning session. The blitz filed a new capture and its write landed on the *heading line* of an existing item, destroying that item's heading and slug outright and leaving its rationale orphaned under the item above. It reached a commit. The planning session found it only by trying to move the item by slug and getting "not in section Unprocessed".
**Why it changes the design input.** The discussion above frames concurrency as *conflicting edits* to be merged — two sessions touching the same region, resolved cleanly or surfaced. This was not a conflict at all: one session's write simply consumed a structural line of another item, and every layer passed. Git saw a valid diff. The lint saw a valid queue. The commit succeeded. **So "merge cleanly when different regions were touched" is not sufficient protection, because the damage here was to a region nobody was editing.** It strengthens the note already in this item that the queue's *structural* lines should be excluded from silent resolution — and extends that set from the readiness marker and section headers to **item headings**, which are equally structural and were not previously named.
**Two mitigations that are not this item's to build,** noted so they aren't lost: the queue lint gains an orphaned-prose check (folded into [lint-blind-to-readiness-marker], and it earns its place independently of concurrency); and the Edit tool's "file modified on disk since you last read it" warning fired twice during the incident and was dismissed both times as the queue mover's doing — a session-behaviour lesson rather than a mechanism, that the first such warning should stop work rather than be reasoned past.
**Designed 2026-08-05. Both forks are resolved, and one reframe is what resolved them.**
**The reframe: isolation is the fix; merging is the price paid for it.** The live corruption happened in **one folder**, with two sessions sharing a single working tree. Git was never involved and there was no merge conflict, because there was nothing to merge — just two processes writing one file from stale reads. The design above framed the problem as *conflicting edits to be merged*, which put the emphasis on the wrong half and is why the fork stayed open so long. Branching is what prevents the damage; merge-at-close is the cost of having branched, not the mechanism that saves you.
**Fork 1 — RESOLVED: branch every session.** "Isolate only where sessions overlap" requires knowing another session is live. That requires liveness detection, which the user killed on the grounds that a session should always be *closed* properly rather than left to go stale. So the overlap option depends on the one mechanism already ruled out, and cannot be built without reversing that decision. Branch-per-session doesn't: it costs almost nothing when uncontended, because a merge with no divergence is a fast-forward and effectively invisible. **It is free when you are working alone and pays exactly when you are not** — which is the property you want, since concurrency is unpredictable and the protection has to already be in place when it happens.
**Fork 2 — RESOLVED: full-project branching, not queue-only.** Git gives whole-tree branching for free; isolating a single file would need bespoke machinery built for one document. And the premise that "the contention is fundamentally about QUEUE.md" is too narrow: a build session touches hooks, procedure docs, templates and SPEC, and two concurrent builds collide on source files, not just the queue. The planning session of 2026-08-05 alone touched QUEUE.md, its own working file, and would have touched CLAUDE.md. Queue-only branching would leave all of that unprotected while costing an invention.
**A property worth naming, because it is what makes abandoned sessions tolerable without liveness detection: branching converts an invisible failure into a visible one.** Today an abandoned or colliding session corrupts a file silently and every layer passes. Under branch-per-session, the worst residue is an unmerged branch sitting in `git branch` where anyone can see it. A leftover branch is a housekeeping chore; silent corruption reached a commit and was found only by accident. This is the honest answer to "what about sessions that are never closed" — they leave litter rather than damage.
**Consequence, acted on the same session: [worktree-override-hook] was DELETED rather than built.** That hook existed to *stop* remote sessions forking, because merging them back by hand hurt. With the merge automated and forking the design, it solved a problem this removes — and shipping it would have actively defeated the isolation, pointing every session back at the one shared tree, which is precisely how the queue got corrupted. Its mechanism facts were relocated here before it went (see below). The accepted risk, recorded so it is not a surprise: if this design stalls, the original remote-forking pain returns with nothing queued for it — acceptable because the hook was never built, so nothing working was lost, and unmet pain re-captures itself in use.
**The desktop/remote asymmetry dissolves.** Remote already forks a worktree per session; desktop shares one tree. With desktop branching too, both surfaces behave identically and the asymmetry that made this hard stops existing.
**The remaining build question, and it is the one that carries the risk: what happens on a GENUINE conflict.** Most merges will have no conflict at all, so this narrows to real concurrent edits to the same region. The scope-lock audit's warning stands and is sharpened by the live incident: QUEUE.md is the artifact that *defines* scope — the readiness marker sets how much autonomous work is authorised — so silently resolving it is an expansion of agent authority over the boundary itself, not merely over a document.
```
NEVER silently resolved — always surfaced to the user:
    the readiness marker          # sets what /next may run unattended
    section headings              # Processed / Unprocessed
    item headings and their slugs # extended after the live incident, where
                                  # the damage was to a heading nobody was
                                  # editing and every layer passed
    red-flag markers and states

MAY be resolved silently:
    prose-only changes inside DIFFERENT items
    appends to different sections
```
**Mechanism facts relocated here from [worktree-override-hook] when it was deleted, 2026-08-05 — they answer a build question this item already carries.** Claude Code exposes a **`WorktreeCreate` hook event** that replaces default worktree creation: it takes a name and returns a path, so a hook can decide where a new session's tree lives. Confirmed to still exist as of the 2026-08-05 check (`resources/research/claude-code-hook-events.md`); the surrounding constraints are in `resources/research/remote-control-limitations.md`. That is directly useful to the "worktree or plain branches" question below — the same lever that was going to *suppress* forking can be used to *direct* it. Also carried across: remote sessions disconnect unpredictably, which is why the user pre-makes standby sessions; branching does not fix that and must not be described as if it does.
**Still to settle at build:** where the merge runs (the /done close is the obvious home, since it is the one close that always fires); what a session does when it finds its branch already merged; whether `git worktree` or plain branches suit the desktop case better, now informed by the `WorktreeCreate` lever above; and the `.git/index.lock` retry, which stays a minor robustness nicety rather than core. **Cloud-sync detection stays in scope but is independent** — a startup check for whether the project folder is inside a Drive/OneDrive/Dropbox sync root, surfaced not fixed, since those produce their own conflict copies and file locks regardless of any git design.
**Live remote-control evidence, 2026-08-05 (captured by you): what the current shape costs.** A session left open on the desktop could not be continued from remote control — continuing meant either branching, which you avoid because of the stress tied to it, or having a helper at the desktop close the session and open a new one so it would appear in remote control. That close-by-proxy took roughly forty minutes across four relayed stops (the incident is recorded on [close-reliance-on-reply-chips] and its sibling captures). Whatever this item builds must either make the branch path genuinely stress-free or make continuing a session not require one; this incident is the measure to build against.
**Live evidence this is workable, from the same session that designed it.** The branch-per-session shape is being dogfooded right now: this project has run a full blitz and a planning session on `overnight-blitz-2026-08-05b`, and the *first* blitz already merged cleanly into main at `5127f1c` with eighteen items on it. So the pattern has completed a full cycle once, by hand, before being automated. [merge-blitz-branch-to-main] will supply a second data point.
**Lift-condition (placed below the marker at the 2026-08-05 close): held by choice, not by a blocker** — the same standing [history-rewrite-third-party-scrub] and [rename-to-throughliner] already carry. Nothing queued blocks it and the design is settled. What keeps it below is that it rewrites `session_start.py` and changes how **every** session begins, and the run it would otherwise join ends with [merge-blitz-branch-to-main] — so a concurrency build that went wrong would be merged into main automatically, unattended, by the same run that produced it. **Lifts when you open a session for it deliberately, or greenlight it at any /plan close.**
**Files:** `docs-b/done.md` and `docs-b/done-plan.md` (the merge at the close), `docs-b/plugin-behaviour.md` (safe-concurrency rules and the never-silently-resolved list), `hooks/session_start.py` (branch creation, the awareness note, and the cloud-sync check), `docs-b/plan.md` if the branch is created at session start rather than by the hook, SPEC.md + README (user-facing behaviour change), plus a FAQ entry. Relates to [worktree-override-hook] (whose fate this decides), [next-per-item-queue-removal] (which assumed the queue is freed early for other sessions — an assumption branching changes), [build-scope-vs-described-work-unenforced].

#### The `captured by you` credit is unreliable in two ways: it only resolves for one reader, and it gets applied wrongly with nothing to catch it [provenance-credit-second-person-ambiguous]
Captured by you 2026-08-03 — you asked who "you" refers to in a provenance line, then challenged a credit that turned out to be wrong.
The credit means the project's user, and the convention is deliberate: [provenance-labelling-reconsideration] shipped at `ea272f6`, dropping mandatory provenance for an asymmetric default-AI scheme where an unmarked item is Claude's and only a user-raised item carries a credit. **That decision is not in question** — the asymmetry is sound and cheap. Both problems below sit inside it.
**Problem 1 — it resolves only for one reader.** The credit is direct address, so it works only if the reader is the person it was written for. That always holds in a solo project. It stops holding for a repo that goes public ([scrub-gate-before-approval], [setup-repo-privacy-and-publishing]), for a project handed on, or for any second reader — where "captured by you" points at nobody identifiable.
**Problem 2 — and this one is worse — the credit gets applied wrongly, and nothing catches it.** Live instance the same day: [reorder-mover-marker-at-top-blind] carried a `captured by you` credit from filing. It records a script throwing an internal error during a /done close — something only Claude could have seen. The user had no way to observe it. The credit was then *inherited unchecked* through a full processing pass, and only caught because the user happened to say it didn't feel like theirs.
**Why that's the more serious half.** An ambiguous credit is a reading problem; a wrong credit corrupts the record. The queue's purpose is preserving who decided what, and a mis-credit silently attributes thinking to the wrong party — in the direction that flatters the user's involvement, which is exactly the direction nobody will question. It also survives processing, because a rewrite preserves the provenance line as given rather than re-deriving it.
**Measured at processing, 2026-08-05: 40 of the queue's 55 items carry a `captured by you` credit.** Framed carefully, because the number is suggestive rather than diagnostic — it is entirely consistent with being true, since there is one user and they are the developer, so they do raise most things. What it does establish is that the credit discriminates almost nothing in practice: a marker on three-quarters of items tells a reader little, and a single wrong credit is well camouflaged among them. That is the condition under which the known mis-credit survived a full processing pass unnoticed.
**Problem 1 is resolved by the user's decision, 2026-08-05: record a real first name, an alias, or another identifier at /setup, and credit captures to it.** Claude had recommended a one-line queue-header explanation instead, on the grounds that a name writes a person into a repo that may be public. **The user's framing already answers that** — offering an *alias* gives multi-user disambiguation with none of the exposure, so the objection applies to a name-only design and not to this one. The user's reason: "you" breaks down as soon as a project has more than one user, and the header line only ever worked because this project has exactly one.
```
rejected: "captured by the user"  -> unambiguous but colder, and the second
                                     person is part of how the queue reads as
                                     the user's own document
rejected: name-only, no alias     -> writes a real person into every item of a
                                     possibly-public repo
kept:     an identity recorded at /setup — real name, alias, or other, the
          user's choice, with the alias route offered plainly
```
**Three things the build must settle rather than assume.**
1. **The multi-user case the field exists for.** A single stored field records *one* identity, so where two people genuinely share a project, the second person's captures would be credited to the first — the exact failure this is meant to fix, reintroduced. Decide between a per-session confirmation, a field explicitly scoped to "the project's primary user", or something else. Do not leave this to inference.
2. **The ask is a solicited identifier, so it is governed by the private-information rules.** /setup must say plainly, at the moment of asking, that the answer is written into committed project docs — and offer the alias in the same breath. /setup already detects repo visibility, so a public repo should lead with the alias option rather than mentioning it as an afterthought.
3. **What happens to the 40 existing credits.** A new field protects new items and does nothing about those already written. Migration is [provenance-credit-audit]'s territory; this item must not silently assume it happens.
**Problem 2 — a filing-time test, plus re-derivation at the keep-step.** State the test in words: *could the user have observed this unaided?* And require the keep-step to re-derive the credit from the discussion rather than inherit the filed one, because inheritance is precisely how the known bad credit survived processing. No lint — this is a judgment a hook cannot make, so it is a wording fix.
**Files:** `plugin/si-plugin/docs-b/setup.md` (the new question and its privacy framing), `templates/CLAUDE-TEMPLATE.md` (the stored field), `docs-b/plugin-behaviour.md` (the Captures provenance convention and the filing-time test), `docs-b/plan.md` (the keep-step re-derivation), the QUEUE.md header prose in setup.md, SPEC.md (a new user-visible setup question), README feature-list sync, and a FAQ entry. Docset A: setup.md is the file the freeze excepts, so the question must disappear from or appear in both — see [docset-a-completion-ask-inconsistency].

#### Design proactive rationale-nudging: Claude asks for the *why* without nagging [throughline-feature-elevation]
Captured by you (2026-07-30); reframed 2026-08-01. Earlier threads are gone: thread (a) — the why-pipeline → "throughline" rename — folded into [rename-to-throughliner]; thread (b) — user-provenance on rationale — folded into [provenance-labelling-reconsideration] (2026-08-01) for one consistent provenance scheme. What remains is the genuinely-undesigned thread: **proactive rationale-nudging.** At certain moments (a decision made, a route chosen, an item kept), Claude could proactively ask the user for the *why* — actively extracting human intention rather than only recording rationale when it surfaces on its own. Because this is the titular feature — durable rationale that survives a forgetful session — it should be genuinely excellent. **Designed 2026-08-05 from that session's own evidence, which resolved the crux and changed the mechanism.**
**The finding: the productive nudge is a proposal, not a question.** Almost none of that session's rationale came from anyone asking "why". It came from Claude stating a recommendation *with its reasoning* and the user disagreeing. The clearest case is this queue's own [provenance-credit-second-person-ambiguous]: Claude recommended a queue-header line and gave its grounds; the user overrode it with "'you' breaks down when there are multiple users", and that sentence is now the item's rationale. The same shape produced the rationale on [lint-blind-to-readiness-marker], [docset-a-completion-ask-inconsistency] and [merge-and-fold-without-asking].
**Why that reframing dissolves the nagging problem rather than managing it.** A "why do you want that?" question arrives empty and makes the user do the work, which is exactly what nags. A proposal costs the user nothing when they agree — one word — and when they disagree, the reason arrives attached, because people justify corrections for free and resent interrogation. So "when can the nudge fire without nagging" is hard to answer for a question and easy for a proposal: wherever a decision is being made.
**The moments, now nameable:**
```
/plan keep-step    ->  a recommendation carries its reasoning explicitly.
                       Mostly making existing practice a rule, not a new beat.
the close          ->  where a decision's rationale is written down.
a user OVERRIDE    ->  the highest-yield moment of the three, and the one with
   of a proposal        no rule at all today. An override is rationale arriving
                        unbidden; nothing currently says to capture it as such.
```
**One suppression limb, non-negotiable:** don't propose where the answer is predictable. [merge-and-fold-without-asking] is the user saying plainly that a whole class of Claude's asks was redundant, filed the same day. A nudge design that ignores that reinvents the behaviour the user asked to stop.
**Rejected: sharpen-and-skip for a dedicated design pass.** Legitimate and offered; the user chose to keep it, because the crux is settled and the remaining questions are build-sized rather than design-sized. Recorded so the option isn't re-proposed as though it had been overlooked.
**Cost to weigh at build:** a proposal-with-reasoning spends tokens every time, and the same session established that approval-time token cost lands hardest on Pro users ([approval-flow-token-doubling-simplification]). The suppression limb is what keeps this affordable, so it is part of the feature rather than a refinement.
**Files:** `docs-b/plugin-behaviour.md` (the throughline section — the nudge's proposal form and its suppression rule), `docs-b/plan.md` (the keep-step: a recommendation carries its reasoning), `docs-b/done.md` (capturing rationale at a user override), SPEC.md if its description of the throughline changes, README feature-list sync, and a FAQ entry. Relates to [rename-to-throughliner] (which renames this concept), [provenance-labelling-reconsideration], [merge-and-fold-without-asking], [throughline-value-articulation].

#### [audit] Check every existing `captured by you` credit against what the user could actually have observed [provenance-credit-audit]
By Claude, split from [provenance-credit-second-person-ambiguous] at its processing on 2026-08-05. Filed as its own item rather than a limb of that build because /next routes by flavor, and an audit limb inside a build item would either not run or run as a build.
**Why it is worth doing.** 40 of the queue's 55 items carry the credit. A wording fix protects items written from now on and does nothing about those already there — and there is at least one known bad credit in the existing set: [reorder-mover-marker-at-top-blind] was credited to the user for a script error only Claude could have seen, and it survived a full processing pass because a rewrite preserves the provenance line as given rather than re-deriving it. One confirmed instance in a set of forty is a reason to look at the other thirty-nine.
**The test to apply, one item at a time:** could the user have observed this unaided? A hook error, a code read, a doc grep, an internal tool result — no. A design preference, a live observation of Claude's behaviour, a decision, a report from another project — yes. Where the answer is no, the credit is wrong.
**Report findings; change nothing.** This is an audit, so it lists the items whose credit looks wrong, with the reason for each, and those become captures or a consolidated repair item. The repair is deliberately not part of the audit.
**Sequencing note, not a `Blocked by:`.** Running this *after* [provenance-credit-second-person-ambiguous] ships would let one pass both correct wrong credits and migrate them to the new identity form, rather than touching the same 40 items twice. It is not blocked, though — the audit is useful on its own and its findings would inform that build's migration step. Order at a close, deliberately.
**Files:** none — an audit reads and reports. Reads QUEUE.md and, where a credit's origin is unclear, `LOG/index.md` and the entry that filed the item. Relates to [provenance-credit-second-person-ambiguous], [reorder-mover-marker-at-top-blind].
**Files (rough):** `docs-b/plugin-behaviour.md` (Captures — the provenance line format and the filing-time test), `docs-b/plan.md` (the keep-step, where an inherited credit should be re-derived), the QUEUE.md header template in `docs-b/setup.md`, `templates/CLAUDE-TEMPLATE.md` if the convention is described there. Relates to [provenance-labelling-reconsideration] (shipped — the scheme this sits inside), [reorder-mover-marker-at-top-blind] (the live mis-credit), [scrub-gate-before-approval], [setup-repo-privacy-and-publishing].

#### Backfill the method's own FAQ current with every user-facing change since 2026-06-18 [faq-backfill]
Captured by you — filed after `b6f2344`; processed 2026-08-01. Split from the [faq-staleness-and-sync-failure] capture. `FAQ/faq.md` + `FAQ/index.md` last edited 2026-06-18; a stack of user-facing method changes shipped since (through v1.16.0) with no FAQ entries. Bring both current: audit the LOG/SPEC deltas since 2026-06-18 for user-facing changes (new skills behaviour, working mode, readiness line, seeding, feedback channel, multi-item /next, etc.), draft the missing/updated FAQ entries, and reconcile the index.
**Sequence:** land [faq-sync-hard-gate] first so the backfilled FAQ doesn't immediately re-rot under the same broken mechanism.
**One entry named specifically, 2026-08-04.** Cover what the user can do while `/plan` works through the queue: that skipping an item and raising something of your own are available at any point and are stated once at the start rather than repeated; that each item closes on carry-on-or-stop; and that a capture you raise mid-session comes with the offer to process it now or leave it for its turn. This was asked for after the behaviour was observed and not recognised — the rule is working correctly and had simply been forgotten, which is a documentation gap rather than a design one.
**And the backfill is a correction job, not only an addition job.** The index currently advertises at least five retired concepts as current: Batches, a Red flags section at the top of QUEUE.md, a Deferred tests section, "Parked", and `/next freeform`. A reader following those is misinformed rather than under-informed, which makes this more urgent than a missing-entries backlog would suggest. Audit every existing entry against current behaviour before writing new ones.
**Files:** FAQ/faq.md, FAQ/index.md. Relates to [faq-sync-hard-gate]; note [rename-to-throughliner] will later rename identity strings across these entries (orthogonal to the content backfill).
**One entry must be settled BEFORE this rebuild runs, added 2026-08-07.** [retired-faq-entry-strands-legacy-users] may require a *retirement entry* for the `--- Plan session here: … ---` marker — retired 2026-08-07, deleted from every shipped doc and template, absent from the migration checklist, and silently exempted by the queue lint's structural-line rule, so a user whose queue still carries one has no answer anywhere. Because this item rebuilds the FAQ wholesale from the template, an entry written after the rebuild would be overwritten and an entry written before it must be in the template, not the project copy. **Check that item's disposition before rebuilding**; if it has not been decided, the rebuild proceeds and the entry is added to the template afterwards.
**Audit findings, consolidated ([faq-audit-wrong-missing-and-shape], run 2026-08-05 in the overnight blitz — its blocker is spent; this item now builds from these findings once the user approves the recommendation):**
1. **The premise is confirmed.** The shipped template is the maintained document (edited continuously through 2026-08-04, 66 entries after the blitz's two additions); the project's `FAQ/faq.md` is the fossil — its base was last refreshed 2026-06-18 (`b5c5337`), and only three new-rule entries have been appended since (docset choice 2026-08-02; editor rescope and write-first 2026-08-04). So consumers get a current FAQ at /setup and this project's own copy — the one session_start points every session at — is two months behind.
2. **What is wrong in the project copy — worse than the five named concepts.** Eight of its 22 entries describe retired machinery as current: Batches and Build/Test/Audit subheadings; `/next freeform`; the Red flags section at the top of QUEUE.md; the Deferred tests section; "Parked" with its two reason-line formats; the spec-edit-batch answer to "Can I edit SPEC.md?" (SPEC is a normal doc now); `_plan.md` described as tracking promoted/parked/dropped; and "outside the current batch" scoping. A reader following these is misinformed, not under-informed.
3. **What is wrong in the template — almost nothing.** Its push-marker mention is deliberately historical (fine). Two entries use "parked" colloquially for below-the-line waiting ("stays parked in your queue", "parked below the line") — mild vocabulary residue from a retired concept, worth a wording pass, not wrong in substance.
4. **What is missing from the project copy:** roughly forty topics the template already covers — the two-section queue, the readiness line, `Blocked by:`, `[user]` walk-throughs and their no-completion-ask rule, build/audit flavors, working mode, the Editor field, red-flag states, the feedback channel, multi-item /next, pushing vs committing, the update path, seeding, the processing flow, subagent asks, the planning file-gate, and more. Nothing user-facing was found in LOG/SPEC that the template misses.
5. **Recommendation: rebuild, not patch — by replacing the project copy with the template.** Under a third of the project copy survives unchanged, and every survivor already exists in the template in newer form, so patching means hand-merging 22 entries into a document that already supersedes them. In self-hosting the shipped template and the project FAQ are the same artifact; the honest structure is to copy `templates/faq-template.md` → `FAQ/faq.md` and `faq-index-template.md` → `FAQ/index.md`, and state in CLAUDE.md that the project copy is a mirror refreshed from the template whenever the template changes.
6. **Shape judgment: the index needs grouping.** 66 links in accretion order is unnavigable for a non-coder under pressure. Recommend reorganising the index (both copies) under five or six plain headings — getting started · the queue · building · closing and saving · safety and privacy · updates and troubleshooting — with the answers file left in its current order; the index is the navigation surface, so grouping it is the whole win at a fraction of the churn.
8. **Who the FAQ is written for — measured 2026-08-06, and the answer is "the user", unchanged.** Relocated here when [faq-as-claude-readable-reference] was deleted at its processing, so the number survives and the question is not re-asked mid-rebuild. That item wondered whether entries should also be written for Claude to read, and recorded honestly that it was a hunch with no evidence. The measurement: across the 16 session transcripts then on disk, **18 opens of a FAQ file, spanning five projects** — this one, scrolly-thing, flintcraft.tech, Verso, and an old copy of this project. So the injected pointer *is* acted on; the capability was never missing. **Two limits stated rather than glossed:** most opens are in this project, where the FAQ is the artifact being maintained rather than consulted, so they are not evidence of the pointer landing; and a file path cannot say *why* a file was opened. The three opens in projects where the FAQ is merely help are the ones that count, and they are thin but real. **The conclusion for this rebuild: keep writing for the user.** Claude already holds the procedure docs, which say the same things more precisely; the FAQ's value to a session is that it is short, which is exactly what writing for two audiences would destroy.
9. **Why the existing close gate never caught this** (carried over from the duplicate capture [host-faq-stale-pre-redesign], dropped 2026-08-06). CLAUDE.md's FAQ-sync gate fires on a *change* — a session whose commits carry something user-facing must disposition the FAQ before closing. It therefore sees new drift and is blind to accumulated drift: nothing about a quiet session says the FAQ fell two months behind. /setup never re-scaffolds an existing FAQ either, so no mechanism re-syncs the project copy. That is the gap this backfill closes, and it is an argument for the mirror-from-template rule in point 5 rather than for tightening the gate — a gate that fires on change cannot detect the absence of change.

#### Rewrite the full public history to remove the third-party name and the author email, and remap all 475 LOG index hashes [history-rewrite-third-party-scrub]
Captured by you 2026-08-04, reversing the scrub-forward-only decision recorded at `68ca27e` the same day. The reason for the reversal is yours: the working tree being clean isn't enough while the name and the nature of the matter stay readable in the public history.
**The facts, established at capture rather than assumed.** The name appears in 12 commits, across roughly 20 files (LOG entries, `CLAUDE.md`, `AGENTS.md`, `IDEAS.md`). It is in **no** commit message and at **no** branch tip — the forward scrub was complete. The earliest affected commit is `39aa0f4` (2026-06-22), giving a rewrite span of 139 commits.**Second exposure, found 2026-08-04 and folded in deliberately.** Every commit in this repo carries `recyclobat@gmail.com` as author — all 385, from the first — in a repo that is public. Commit metadata is machine-readable on GitHub and routinely harvested, and unlike the name it appears in no file, so no edit or forward scrub touches it. This was folded into the same item rather than filed separately because the two fixes are one operation: `git filter-repo` applies a text replacement and a mailmap in a single pass, and running them as two rewrites would mean a second force-push whose span includes the first rewrite's own commits. Claude Code's own commits already use `noreply@anthropic.com`, so only the user's authored commits carry a real address.
**The collateral, which is the reason this is a build and not a command.** The email is in the very first commit, so the rewrite span is the **whole history**, not the 139 commits carrying the name. That widens the index remap from 123 references to **all 475** in `LOG/index.md`. The scale is the argument for doing it once: the remap is scripted from `git filter-repo`'s commit map either way, so the extra references are machine work rather than human work, whereas a second rewrite would repeat the whole force-push-and-verify cycle. That index is how the throughline is retrieved, so a rewrite that doesn't remap it trades a privacy exposure for a broken memory. Apply the map to `LOG/index.md` and to any entry file quoting a hash in a heading, then verify by re-resolving every rewritten reference.
**Correction, 2026-08-04: the orphan tag history is in scope after all.** The original text said it was clean and needed no rewrite. That is right about the third-party *name* — the 2026-08-04 scrub sweep covered it. It is wrong about the email, which appears on those commits like every other. So the earlier statement was true of the exposure it was checked against and was then read more broadly than it had been tested.
**What that history actually holds, which is the reason to be careful with it.** At tag `v157` (2026-06-01) there are 142 files under `_method/build-log/`, the pre-rebuild logging format — the project's entire record from May 2026, in a folder nobody looking in `LOG/` would find. It was located 2026-08-04 when the user asked where their pre-June logging had gone. This is not incidental history; it is the throughline for the method's first month.
**Third strand, folded in by the user's decision 2026-08-04: graft the two histories together.** Today the pre-rebuild graph is reachable only through 135 tags and is not an ancestor of `main`, so a clone shows no early history and the record reads as two unrelated projects. Connect them so the log reads continuously. The user's reason for folding this in rather than doing it separately: the rewrite already touches every commit and changes every hash, so a graft in the same `git filter-repo` pass costs one force-push and one verification instead of three passes over the same commits. That is the same argument the item already makes for folding in the email fix.
**This resolves the tag-clutter question without deleting anything.** The competing option was renaming the range to stop `v17` sorting beside `v1.16.0`. Rejected: it means creating 135 tags and then deleting 135 tags on a public remote, which is 135 repetitions of the one operation that could destroy this history. After the graft, the commits are reachable from `main`, so the tags stop being the only lifeline — but **keep them regardless**, as the version record they are. Deleting them was raised and is refused; the standing never-delete rule in CLAUDE.md holds.
**Branch coverage correction, 2026-08-05: there are THREE remote branches, not two.** `git branch -r` at this session shows `origin/main`, `origin/queue-redesign` **and `origin/claude/remote-session-capture-QxjcU`** — a remote-session branch this item has never named. A pushed branch outside the rewrite keeps both the third-party name and the author email intact on every commit it holds, so a rewrite that covers only the branches someone remembered is a rewrite that does not achieve its purpose. **Enumerate the branches with `git branch -r` at build time rather than working from this list**, since remote-control sessions can create more between now and then. Same correction shape as the orphan-tag one already recorded above: a statement that was true of what it had been checked against, then read more broadly than it had been tested.
**Three things the build must confirm rather than assume.** Whether `git filter-repo` performs a graft directly or whether that needs a replace-ref or parent-rewrite callback — read its documentation before designing the pass, don't infer it. That all 135 tags are re-pointed to their rewritten commits rather than left dangling at old hashes. And that `origin/queue-redesign`, which is still live on the remote, is either covered by the rewrite or deleted before it — a pushed branch outside the rewrite keeps both the name and the email intact.
**A THIRD string joins the replacement set, added 2026-08-07 by [session-start-writes-payload-sample-into-repo] as it shipped.** Alongside the third-party name and the author email, the rewrite must replace **the project folder's absolute path, which includes the machine account name**. It was published in a tracked JSON file that `session_start.py` wrote silently into `resources/research/`; the write has been removed and the file deleted from the working tree, but every commit that carried it still holds the string, which is precisely why it rides this pass rather than needing its own. **Derive the literal at build time** — read it from the deleted file in git history (`git show <commit>:resources/research/session-start-payload-sample.json`) rather than from anything written here, because writing the path into this queue would republish in a public repo exactly what the rewrite exists to remove. Grep the whole history for it, not just that one file: a path string can appear anywhere a session pasted one.

**The sequence.** Commit the working tree first. Take a full backup copy of the repo folder before anything destructive. Install `git filter-repo` (not present; `filter-branch` is deprecated and unsuitable at this span). **Set `git config user.email` to a GitHub `noreply` address before the rewrite**, or the very next commit reintroduces the address the rewrite just removed and the whole operation has to be repeated. This is the one step that is cheap now and expensive at any later moment, which is exactly the shape of the finding that produced it. Decide branch coverage deliberately — this repo has two worktrees sharing one object store (`main` and `codex/si-port`; the stale `queue-redesign` worktree was deregistered 2026-08-05, though its branch still exists locally and on the remote), and rewriting only `main` would leave copies on any other branch that has been pushed. Rewrite, remap the index, verify no occurrence survives in any commit or message, then force-push.
**State the limits plainly to the user before the force-push, because they are not obvious.** Every commit hash changes. GitHub can retain unreferenced objects for a period after a force-push, so removal is not instant. Any existing fork or clone keeps the old history and nothing can reach into those. The honest claim is "removed from this repository going forward," never "removed from the internet." Every tag hash changes too, so any reference to a tag by hash elsewhere goes stale.
**The `--force` push is a deliberate, one-time exception to the file-safety rule that forbids it**, authorised by you here. Record it in the LOG entry as a departure, with what was done and why it couldn't wait.
Red flag · State: cleared — cleared by this decision: the risk is named, the user was told plainly what a rewrite can and cannot achieve, and chose the rewrite over scrub-forward-only and over making the repo private.
**Files:** the whole history in the rewrite span; `LOG/index.md` (the 123 remapped references) and any LOG entry file quoting an affected hash; `CLAUDE.md` if the worktree list turns out to be stale. Relates to [scrub-gate-before-approval] and [claude-md-old-history-claim-wrong], which carries the orphan-tag history and its clean sweep result.
**Lift-condition (rewritten 2026-08-04 — the `Blocked by: [log-scope-adjacent-work]` line is removed because that item has shipped):** nothing queued blocks this. It stays below the readiness line for one reason only, which is not a dependency: it force-pushes a rewrite of the entire history and needs the user present to approve that, so it must never sit above the line where an unattended run could reach it. **Lifts when the user opens a session for it deliberately** — this is the top priority of 2026-08-04's planning session and the recommended next session, not shelved work.

#### Release the docset-B work: version bump, pre-push sweep over 18 commits, GitHub Release [release-docset-b-work]
Captured by you 2026-08-03. Eighteen commits sit unpushed, spanning docset B's full authoring (`6ba51d3`), the fidelity audit and phase-3 session_start wiring (`b931278`), the fidelity restores (`f37e332`), and the surrounding /plan sessions. The last published release is v1.16.0; the working tree carries the `-test4` suffix from rezip testing. The method's one external user is running everything behind that.
**Run the Push ritual in CLAUDE.md as written** — it already specifies the steps. Two parts of it carry real work rather than being mechanical this time. The version bump is a **minor** (1.17.0), not a patch: two docsets picked automatically by model is a new capability, and the bump must drop the `-testN` suffix. And the pre-push consistency sweep is substantial at this size — Pass A reads eighteen commits' LOG entries, Pass B checks the templates, project docs and CLAUDE.md against what those commits changed.
**Ordering: run after [docset-b-inert-on-desktop-app] has shipped and been seen to work (re-pointed 2026-08-03).** The old clause waited on the session_start diagnostic and said the release would clear immediately if the failure turned out to be local. That diagnostic has now run, and the wording is actively misleading if left: it resolved, but *not* in the direction that clears this. The hook's output does reach sessions now — and with it working, the payload was found to contain no docset directive at all, because no `model` field arrives. So the release's headline feature, model-based docset selection, does nothing in this environment.
Shipping now would publish and announce a release whose main capability is inert. The genuine dependency is therefore the replacement selector: ship [docset-b-inert-on-desktop-app], confirm a real session loads docset B, then release. Two things also get easier by waiting — the release notes can describe a feature that works, and the payload-size problem may have dissolved by then, since docset B's payload is 3,524 characters against docset A's 92,112.
**Dependency satisfied 2026-08-04; now held by choice, not by blocker.** The condition above is met — [docset-b-inert-on-desktop-app] shipped in `f832385` and the /plan session of 2026-08-04 received the docs-b directive on host 1.16.0-test5, so the headline feature demonstrably works. It stays below the line because the user chose to hold it for a session that can watch it, and because the eight items now cleared above rewrite `session_start.py`, `pre_tool_use.py`, SPEC.md, CLAUDE.md, AGENTS.md and five procedure docs — releasing before those land would ship a build the very next run rewrites, and the Pass B consistency sweep would have to be redone. **Nothing blocks this: greenlight it at any /plan close, ideally the one following the cleared run.** Note the run also retires the push marker ([spec-diverges-from-shipped-docsets]), so the release's sweep must check CLAUDE.md and AGENTS.md for that change.
**Release notes draw from the LOG entries**, per the ritual's step 10, and this release has an unusual amount to say: the register decision and its later weakening, the 39% subtraction result, the fidelity audit. The Discord announcement written this session covers much of the same ground and is the natural source to adapt — the first real use of the artifact test from [log-scope-adjacent-work].
**Files:** `plugin/si-plugin/.claude-plugin/plugin.json` (version), `plugin/si-plugin.zip` and `plugin/zip-archive/` (repackage and prune), plus whatever the Pass B sweep turns up across templates, SPEC.md, README.md, QUEUE.md and CLAUDE.md. Relates to [session-start-output-not-reaching-sessions], [test-suffix-plugin-json-close-noise].

#### Build an adherence-measurement harness, and use the docset-B register question as its first experiment [docset-b-register-decision-weaker-than-recorded]
Captured by you 2026-08-03; re-scoped the same day after fresh research. The slug is unchanged from the original capture, which asked only whether to re-test pseudocode.
**Where this started.** `LOG/2026-08-02-opus5-docset-b-authoring.md` gives three reasons for rejecting typed pseudocode as docset B's register, and calls the third decisive: the format demoted a standing global rule into a comment inside one branch. That error was caught and fixed in the re-author and never shipped. A fixed authoring error is thin evidence about a format — any register lets a rule be put in the wrong place, and the fix is to move it. So the decision rests on two arguments, and neither was described as decisive.
**The two that stand.** The fences carried the mechanics that would be followed anyway, while the two most-slipped rules in the sample section stayed prose in the pseudocode draft too. And prose inside a fence is a costume — lines like `purpose = the one human checkpoint…` lose wrapping and gain no precision, which is bespoke semantics rather than bespoke syntax, the thing the Grammar-Prompting caveat actually warns about.
**The narrower defensible form of the discarded third reason,** kept rather than dropped: a branch structure *invites* scoping a rule to a branch, because branches are what the notation offers, whereas prose has no branch for a global rule to fall into. It was one instance generalised to a class when the call was made, but it has a second instance now — the capture line-format block that shipped in docset B asserted structure its own prose contradicted (point 1 of the fidelity restores, `f37e332`).
**Why the item's original re-test was dropped.** It proposed re-authoring one section in pseudocode and reading it on a 5-series model. That is the same instrument that produced the decision now in doubt, so it would yield a second impression rather than a second data point. Research 2026-08-03 (`resources/research/pseudocode-vs-prose-for-rule-adherence.md`) found no study anywhere testing prose against pseudocode for standing behavioural rules, and found the evidence *for* pseudocode thinner than assumed — Mishra et al. measure NLP task accuracy on BLOOM and CodeGen, one of them a code model, and Skill-as-Pseudocode measures agent task completion. Both sides of the argument thinned at once.
**What gets built.** A small measurement harness, host-only and not shipped in the plugin package. Four parts: a fixed scenario set; the rules that actually slip expressed as script-checkable constraints, following IFEval's verifiable-constraint design; a runner driving `claude -p` with `--plugin-dir` pointed at each docset build, `--model` fixed, `--output-format stream-json` captured, and `--max-budget-usd` capping the loop; and a violation count per docset. Feasibility confirmed against the installed CLI's own `--help` rather than assumed.
**Why the harness is the deliverable and the verdict is not.** Every register, wording and structure decision this project has made was judged by reading a doc. There is currently no way to tell whether *any* change to these docs helps. That is the real gap; the register question is only its first customer, and the harness outlives whatever answer it gives.
**The safe window, unchanged.** A register change is survivable now precisely because docset A exists as the known-good fallback, which CLAUDE.md calls a non-negotiable no-strand guarantee. Once A retires there is one docset and nothing to fall back to. So run this while A still stands, not after.
**Publish the result either way — a condition this build closes against, not an intention.** The raw run output and baseline numbers land in `resources/testing/`, because a later run is only meaningful against the first one's exact numbers. The finding and the decision it drove land in the LOG entry. The public write-up is derived from that LOG entry, the same way the Push ritual draws GitHub Release notes from LOG entries. This is written into the item deliberately: doc size is the method's most contested feature, the standing defence has only ever been "it works anyway", and if the harness reports that the size critics were right, the pull will be to quietly not write it up. A commitment made in chat will not survive that moment; a close condition will. Publishing an unfavourable result is the whole point of measuring rather than reading. Relates to [log-scope-adjacent-work].
**Deliberately not folded in:** the structural-delimiting experiment, filed separately. Same harness, different experiment — bundling several experiments into one build item is the consolidation trap.
**Lift-condition (below the line, 2026-08-03):** nothing gates this — it is held below only because the release should ship first, and because it is a multi-session build that shouldn't run unattended off the back of a planning session. Greenlight it at any /plan close once the release has gone out, or sooner if you want it built first.
**Files:** a new host-only harness directory under `resources/testing/`, holding the runner, the scenario set and the constraint checkers. No `docs-b` changes unless the experiment flips the register, which would be its own work. Relates to [opus5-docset-b-authoring], [docset-b-fidelity-restores], and the research at `resources/research/pseudocode-format-for-skill-docs.md` and `resources/research/pseudocode-vs-prose-for-rule-adherence.md`.

#### Test explicit structural delimiting on plugin-behaviour.md as a second harness experiment — the better-evidenced lever we hadn't considered [structural-delimiting-experiment]
By Claude, from the 2026-08-03 research (`resources/research/pseudocode-vs-prose-for-rule-adherence.md`). The register question has been framed throughout as prose versus pseudocode. That may be the wrong axis. The better-evidenced intervention for a document of this exact shape — long, multi-section, always loaded, carrying many independent rules — is explicit structural delimiting: XML-style section boundaries whose purpose is to stop an instruction in one section being read as applying to another.
**Why it's a distinct experiment rather than a variant of the register question.** It changes the document's boundaries, not its voice. No rule is reworded and no prose becomes pseudocode, so it costs a fraction of a register change and can be tried without re-authoring anything. That also makes it the cheaper first experiment of the two, which is worth weighing when ordering them.
**The evidence, and its limits.** Provider guidance from Anthropic, OpenAI and Google converges on tag-delimited sections for complex prompts. Practitioner testing is consistent about when the benefit appears: nothing measurable on short single-purpose prompts, gains on long multi-section ones. `plugin-behaviour.md` is markdown-sectioned rather than tag-delimited, and is the longest always-loaded document in the method — squarely the case the evidence describes. But none of that evidence measures behavioural rule adherence either, which is the same gap that undermined the pseudocode case, so this is a hypothesis to test rather than a fix to apply.
**Corroboration worth carrying:** AGENTIF (arXiv 2505.16944) finds that in agentic settings, models hold *format and style* constraints worst — which is what the response-shape rules are. That suggests the rules which keep slipping here do so for structural reasons rather than poor wording, and also that a format change alone may not fix them. Recorded so the experiment isn't over-expected.
Blocked by: [docset-b-register-decision-weaker-than-recorded]
**Why:** that item builds the adherence-measurement harness, which is the only thing that makes this measurable at all. Converted from a prose "depends on" to the proper field at processing, 2026-08-05 — a dependency held in prose is one nothing checks.
**The premise measured rather than asserted, 2026-08-05.** `docs-b/plugin-behaviour.md` is **1,148 lines / 62 KB** — roughly twice the next largest procedure doc (`done.md`, 698 lines / 34 KB) and the only one loaded in every session whatever the session is doing. So "long, multi-section, always loaded, many independent rules" is the measured description of this file, not an impression. Recorded so a later session doesn't have to re-derive it.
**A boundary this build must not cross.** CLAUDE.md decided deliberately, with its reasoning recorded at length, that docset B's `plugin-behaviour.md` stays a **single file** — progressive disclosure fails for standing behavioural rules, which have no trigger to fetch them. Delimiting is compatible with that decision: tags mark sections, they don't split a document. But "improve the structure of the big file" is exactly the banner under which a build drifts into splitting it, and the person building this may not be the person who read that decision. **Delimit; do not split.**
**Its own caveat stays prominent:** the delimiting evidence does not measure behavioural rule adherence either, which is the same gap that thinned the pseudocode case. A hypothesis to test, not a fix to apply.
**Ordering note (not a dependency):** this item argues it is the cheaper of the harness's two experiments, since it rewords no rule. Once the harness exists, decide deliberately which experiment runs first rather than defaulting to the register question because it was raised earlier.
**Files (rough):** the harness's scenario set and constraint checkers under `resources/testing/`, plus a delimited variant of `docs-b/plugin-behaviour.md` built for the run. No shipped change unless it wins.
Filed after `f37e332`

#### Detect a stale project format at session_start and halt with a migrate prompt; name /setup as the migration home and retire the separate /migrate skill [setup-as-migration-home]
Captured by you — testing outcome from the SA consumer project, filed after `9cd1e51`; processed 2026-08-01. SA validated that the migration recipe works and that /setup already re-scaffolds on version drift and loads `migrate-checklist.md` from setup.md Step 2C. So the migration machinery already lives in /setup; what's missing is *active detection* — today migration only happens if the user thinks to run /setup.
**The decision (user's call, 2026-08-01):** migration should be **detected and prompted**, not left to the user noticing drift. So build the stale-format halt that [migrate-skill] reserved as a deferred design question, home it in session_start + /setup, and **retire [migrate-skill]'s separate-`/migrate`-skill framing** — /setup is the migration home. The user's reason: a project silently on an old format wastes /plan and /next context on stale scaffolding, and shouldn't depend on the user spotting the drift.
**The build:**
1. **session_start.py — stale-format detection.** Add a **format-epoch** signal, kept distinct from `.si-version`/the plugin version (which bumps on every release, most of which change no format). The plugin package declares a current format-epoch; the project records its own. When the project's epoch is behind the plugin's, session_start halts the normal load and tells the user to run /setup to migrate *before* /plan or /next. This is the piece a plain "run /setup when you notice" convenience can't give.
2. **setup.md — clear the halt.** The migration path (Step 2C, already loading `migrate-checklist.md`) writes the current format-epoch into the project on completion, so a migrated project stops halting.
3. **Self-hosting bump discipline (host-only, → CLAUDE.md):** whenever a build changes the queue/doc format, it bumps the plugin's format-epoch — sibling to the README-sync / SPEC-sync triggers. Without this the halt never fires.
4. **Migration reconciles the project's `.gitignore`, not only its doc scaffolding — added 2026-08-07 from [throughliner-folder-not-gitignored-in-old-projects].** That item established the shape by measurement: `setup.md` writes entries for `.throughliner/` and `FAQ/`, and this development project has **neither**, with `FAQ/` actually tracked — not because /setup lacks a step (it has one, including an offer to untrack an already-committed FAQ) but because **/setup has never been re-run here since those steps shipped, and nothing prompts a re-run**. That is exactly the gap this item exists to close, so the migration path must reconcile ignore entries as well as documents.
**Three findings converged on this item on 2026-08-07, which is the argument for building it rather than a coincidence.** Each needs a step that only runs when an existing project is brought up to date: this `.gitignore` reconciliation; removing a retired queue marker from a user's file ([retired-faq-entry-strands-legacy-users]); and converting plain section preambles to blockquotes so the lint stops false-flagging ([lint-flags-scaffolded-section-preambles]). None is buildable without the detection this item provides.
**Rejected alternatives:** a separate `/migrate` skill ([migrate-skill]) — heavier standing maintenance for a job /setup already does, and SA proved /setup carries it; and detection-by-structure (does QUEUE.md *look* two-section?) — fragile, so the explicit epoch marker wins.
**Residues from SA to carry (minor):** /setup left SA's top-level `research/` folder unreconciled against the scaffold's `resources/research/`; and old `By Claude` provenance survives on migrated items (harmless).
**Files:** hooks/session_start.py (epoch detection + halt state), setup.md (write epoch on migrate), plugin package (declare current format-epoch), SPEC.md (new user-visible hook behaviour), README feature-list sync, CLAUDE.md (the epoch-bump self-hosting rule), a FAQ entry, and the format-epoch marker's literal name grepped at build time. Supersedes [migrate-skill]; relates to [migration-checklist] (the recipe) and [migration-recipe-freshness].
**Lift-condition (below the line):** ready to build now — greenlight at /done close to lift above the marker for its own /next run, or leave below until you want it built.

#### Refresh the migration recipe whenever a build changes the queue/doc format, as a clause on the existing format-epoch bump [migration-recipe-freshness]
Captured by you — filed after `f593cd1`; processed 2026-08-02. The migration checklist (the recipe for converting an old-format project to the current two-section queue) drifts as the method's format keeps changing, and a stale recipe is worse than none. So it needs a refresh discipline.
**Its gate was stale and is removed.** The item said it was blocked on [migration-checklist] existing first. That shipped at `ea272f6` on 2026-08-01 — `docs/migrate-checklist.md` and `docs-b/migrate-checklist.md`, 130 lines, loaded on demand from setup.md Step 2C. Nothing blocks this now.
**Trigger decided: format-change, not "on rezip."** Rezip is a frequent local test build that usually changes nothing about the queue format, so a pass on every rezip is noise — and noise trains the step to be skipped. A format-change trigger fires exactly when drift is introduced. Same shape as the existing README-sync and SPEC-sync triggers, a pattern the method already runs.
**It rides an existing trigger rather than becoming a new one.** [setup-as-migration-home] already establishes a format-epoch bump whenever a build changes the queue/doc format — the same moment, already detected. So this is one more clause on that bump ("and refresh the migration recipe in the same session"), not a separate gate someone must remember. Build it after that item ships, so the clause has a bump to attach to.
**Rejected: the version-keyed migration-notes changelog** (the option rehomed here from the deleted [migrate-skill]). A second artifact to keep fresh, solving what the recipe already covers, and its own framing asked whether it earns its keep. Against a 130-line recipe refreshed at the moment of change, it doesn't. Recorded so it isn't re-proposed.
**Files:** CLAUDE.md (the format-epoch bump clause, host-only — consumers don't author formats), docs/migrate-checklist.md and docs-b/migrate-checklist.md as the refresh targets. Depends on [setup-as-migration-home]. Relates to [migration-checklist] (shipped).

#### Retiring a shipped FAQ entry leaves the users who still have the retired thing with no answer [retired-faq-entry-strands-legacy-users]
By Claude, noticed while building [audit-plan-marker-implement-or-retire] on 2026-08-07, and captured rather than folded in, because acting on it would have been a build changing its own described work.
**What happened.** That item's decision was to retire the planning-gate marker and **delete** its FAQ entry, along with the index line. The deletion was carried out as decided. But the entry was the only place that answered "what is this line in my queue?", and the people most likely to ask are precisely the ones whose queue still carries one — a marker placed by an older session, in a project that has not been touched since. They open the FAQ, search for it, and find nothing at all. Nothing about the retirement removes the line from their file.
**The general shape, which is why this is worth a rule rather than one restored entry.** Retiring a feature and retiring its *documentation* are different operations, and the method currently treats them as one. A shipped FAQ is read by users of every past version, not only the current one — it ships fresh to new projects and is restored from the templates whenever it is missing, so it is always the current copy, serving queues of every age. Deleting an entry therefore removes the answer for exactly the population that still has the question.
**The alternative that was considered and not taken, recorded so it is weighed rather than re-derived.** A retirement entry: same heading, so a search still finds it, with an answer saying the thing is retired, what replaced it, and that an existing one is safe to delete. It costs a paragraph and it self-obsoletes — after a version or two nobody has the artifact and the entry can go for real. The counter is FAQ bloat: every retirement leaving a tombstone eventually produces a document that is mostly history. Neither was weighed at the time because the question never came up.
**To settle at processing.** Whether retirement entries become the default for any *user-visible artifact* that can persist in a user's own files (a queue marker, a doc section, a settings field) while remaining a plain delete for things a user never sees. And if so, whether they carry an explicit expiry — "delete this entry once the migration checklist no longer converts queues carrying it" — so the bloat counter is answered by construction rather than by hoping someone prunes.
Blocked by: [migration-recipe-freshness]
**Why that field, and why below the line.** The primary limb below rides the format-epoch bump that [migration-recipe-freshness] establishes, and that item in turn waits on [setup-as-migration-home]. The dependency is on queued work, so it is a `Blocked by:` rather than a prose condition — and this item sits beneath its blocker, as the lint requires.
**Reframed at processing, 2026-08-07: the primary fix REMOVES the artifact; the FAQ entry only covers the window before it is removed.** The capture frames the choice as tombstone-entry versus nothing. There is a third option it did not consider, and the method already has the mechanism: **the migration checklist**, whose whole job is converting an old-format project to current. A retired queue marker is precisely a format change, so the honest answer to "there is a line in my queue that means nothing" is to delete the line, not to document it in perpetuity.
**The instance was verified at processing and is worse than the capture states.** The planning-gate marker is gone from every shipped doc and template — no FAQ entry, no index line — and `migrate-checklist.md` says nothing about it either. Meanwhile `post_tool_use.py`'s orphaned-prose check exempts any `^---\s.*---$` line by design, so a stale `--- Plan session here: … ---` in a user's queue is **silently tolerated**: no lint flag, no answer, no migration step. It simply sits there meaning nothing, and nothing anywhere will ever mention it.
**THE BUILD, two clauses in two different homes — and the split is load-bearing because one ships and one does not.**
1. **Shipped: the migration clause.** When a build retires a user-visible artifact that can persist in a user's own files — a queue marker, a doc section, a settings field — the migration recipe gains a step removing it. This is one more clause on the format-epoch bump [migration-recipe-freshness] already establishes, not a new gate someone must remember, and it reaches consumers because migration is shipped machinery.
2. **Host-only: the FAQ tombstone rule**, beside CLAUDE.md's FAQ-sync gate, because consumers do not maintain the method's FAQ. **A tombstone is NOT the default for every retirement** — it is required only where migration cannot remove the artifact, or where no migration is expected to run. Same heading as the retired entry so a search still finds it; an answer saying the thing is retired, what replaced it, and that an existing one is safe to delete; and an **explicit expiry** ("delete once the migration recipe no longer converts queues carrying it") so the bloat counter is answered by construction rather than by hoping someone prunes.
**Why the bloat counter is genuinely answered rather than waved at.** The capture's objection was that every retirement leaving a tombstone eventually produces a document that is mostly history. Two things defuse it: tombstones are now the exception rather than the default, because migration handles the common case; and each one carries a deletion condition tied to the migration recipe, so it self-prunes on a date something else already determines.
**One coordination point, and it is time-sensitive.** [faq-backfill] rebuilds the project FAQ wholesale from the template. If the planning-gate marker warrants a tombstone, it must be specified before that rebuild runs, or the entry gets written and immediately overwritten. A pointer has been added to [faq-backfill] so this survives independently of what happens to this item.
**Files:** `CLAUDE.md` (the FAQ tombstone rule, beside the FAQ-sync gate), `plugin/si-plugin/docs-b/migrate-checklist.md` and `docs/migrate-checklist.md` (the removal step — note the freeze permits this in A as a correction, since a recipe that leaves a retired marker in place is a recipe that does not do its job). Relates to [audit-plan-marker-implement-or-retire] (the retirement that surfaced this), [faq-backfill] (the rebuild this must reach first), [setup-as-migration-home] (the detection that makes migration actually run), [lint-flags-scaffolded-section-preambles] (the same lint whose structural exemption hides the stale marker).

#### Rename Sovereign Implementer → Throughliner: full technical identity + positioning, big-bang with a `renames` map [rename-to-throughliner]
Captured by you (2026-07-30); migration path resolved this /plan (2026-07-31) via research at resources/research/plugin-marketplace-rename-migration.md. **Rename the plugin from "Sovereign Implementer" to Throughliner**, built around its real distinctive value: the throughline of rationale that survives a forgetful session (an AI memory-of-reasoning tool), dropping the over-claiming "Sovereign" and the no-code-competitor framing. "Throughliner" (coined single word) chosen over "Throughline" because the bare word is crowded — getthroughline.com is itself software (name-availability research: resources/research/throughliner-name-availability.md, which found no product named "Throughliner"). Port-neutral: not "Throughline for Claude Code," so a future port won't break the name.
**Scope — both halves (user's call):** positioning/user-facing name (README title, marketplace description, pitch — lead with rationale-memory value); and technical identity (`sovereign-implementer` slug → `throughliner`, the GitHub repo, install/adopt commands, `.si-version`, every absolute path and doc mention).
**PREMISE CHANGED 2026-08-06 — step 1 below has already happened outside the method. Read [github-identity-changed-underneath-the-project] before building any of this.** The repo is already `FlintcraftTech/throughliner`; the user's GitHub login is now `its-coughfee` and the old name `FlintCraftTech` is now an organisation. So this item's migration sequence no longer starts where it thinks it does, and the remaining steps — the marketplace `renames` entry, the corpus-wide identity strings, the positioning copy — are what currently stand between a working redirect and a broken install path.
**FOLDED IN 2026-08-07 at your approval: the per-platform naming convention, from [throughliner-for-platform-naming-convention] (now deleted). The fold added no files** — this item already lists every identity surface the convention would touch.
**The idea, in your words from the Discord message about the closed OpenCode PR:** *"Mine probably needs to be named 'Throughliner for Claude Code' or something like that so others can follow in that convention."* What it adds beyond this item's single new name is a **shape**: the product is Throughliner, and each build of it is qualified by the platform it runs on — so a port somebody else hosts becomes "Throughliner for OpenCode" and reads as part of a family rather than an unaffiliated fork. It was said to a contributor rather than to yourself, which is the point: the project has just declined a port on the reasoning that ports live downstream as their own repos, and that reasoning otherwise leaves the ecosystem with no way to signal that two separately-hosted repos are the same method for different platforms.
**Why it was folded rather than kept separate, and this is the load-bearing reason.** This item settles every identity string in the corpus. If the two ran separately and this one went first, it would ship a name the convention changes, and the convention's entire content would become a **second** identity pass over the same surfaces. The capture's own text said to read them together; folding is the version of that which cannot be missed.
**The recommendation to carry into the build, made at processing 2026-08-07 rather than left open.** The qualifier belongs in **prose and positioning only** — the README title, the repo description, the marketplace description, the pitch — and **not** in the technical identity strings. The plugin name, the marketplace id and the install target stay bare `throughliner`. Three reasons: the install target is what consumers type and what the `renames` map must migrate, so churning it costs real user disruption for a naming gesture; the marketplace already scopes the plugin, so the qualifier adds no disambiguation anywhere a machine reads it; and a qualifier is a *positioning* claim about which platform a build serves, which is what prose carries and slugs should not.
**That resolves the capture's second open question for free: no further repo rename.** `throughliner` stays the repo name, so nothing compounds the redirect fragility already recorded on [github-identity-changed-underneath-the-project].
**And its third, answered honestly rather than talked up:** a convention announced by one project is not a convention until someone adopts it. It costs nothing to state, and it earns its keep at the moment it is used — making the downstream-hosting offer more attractive to the person receiving it. That is the whole case for it, and it is enough.
**The redirect check the capture asked for needs no home here** — it is already on [audit-premise-claims-verification]'s list as a one-`gh`-call check of the marketplace install path after the rename, including what the old path does now the old username belongs to an organisation.
**Migration decided (big-bang, not a transition apparatus):** (1) rename the GitHub repo — GitHub's 301 redirect keeps existing `marketplace add` registrations resolving; **permanently reserve the old repo name** (reusing it later breaks the redirect). (2) Add a top-level **`renames`** map entry in marketplace.json (`sovereign-implementer` → `throughliner`) — Claude Code auto-migrates each consumer's settings and shows a one-line notice; append-only, keep the entry forever. (3) Net consumer cost is a single `/plugin install` (github remote → `plugin-cache-miss`), documented in the release notes + FAQ. Caveats to carry into the build: needs consumers on Claude Code ≥ 2.1.193; managed/policy-enabled installs don't auto-rewrite.
**Lean on the marketplace id (`flintcraft`): keep it.** Renaming the marketplace itself would force consumers to re-add the marketplace — a burden the plugin `renames` map does not cover. Confirm in the build, but the default is to leave `flintcraft` as-is.
**One deliberate EXCLUSION from that grep, agreed 2026-08-07:** the editing-state marker's `producer` field carries the constant `throughliner` (see [gitignore-does-not-stop-drive-sync-of-throughliner]). It is a published **format value** other applications key on, not an identity string, and it was chosen at version 2 precisely so this rename would not touch it. Changing it here would break a published contract and force a further version bump. Leave it alone.
**Hook-enforced-identity change — trace the ripple by grepping the literal old identity strings at build time** (per CLAUDE.md's rule): grep `sovereign-implementer`, `Sovereign Implementer`, `si-plugin`, `.si-version`, `flintcraft`, **and `why-pipeline` / `why pipeline`** across the whole repo so the file list is complete before the build starts. Candidates: plugin.json, marketplace.json, CLAUDE.md, README.md, SPEC.md, every docs/ procedure file (incl. plugin-behaviour.md's Why-pipeline section), templates, hooks, INSTALL.md, resources/.
**Includes the concept rename why-pipeline → "throughline"** ([throughline-feature-elevation] thread a, folded in and settled 2026-07-31): the product and its titular feature share a name, so the concept rename lands atomically with the identity rename rather than as a separate pass that could drift out of sync. Rename `why-pipeline` / `why pipeline` → "throughline" across all docs, including plugin-behaviour.md's Why-pipeline section heading and every reference.
**Naming clarity (resolved 2026-08-01, folds in the deleted [throughline-naming-referent]):** the throughline is the *reasoning spine* — the thread of *why* — running through capture → work item → log; the LOG is where it's recorded and retrieved from, **not** the throughline itself. State this distinction explicitly wherever the rename introduces "throughline" (the renamed plugin-behaviour.md section, SPEC positioning, FAQ), so "throughline" is never colloquially collapsed into "the LOG." The tell that they're distinct: a complete LOG can carry *no* throughline — every event recorded but the reasoning stripped out — which is exactly the failure the mechanism fights, so the two must stay separate.
**Coordination:** host-side and large — the SPEC/CLAUDE.md/README rename lands atomically with the technical identity change (the "large SPEC rework is its own build" case). Relates to [self-hosting-path-portability] (absolute-path fix overlaps the path-rename surface), [migration-checklist] (adds an identity-migration dimension), and [throughline-feature-elevation] (its threads b+c — rationale provenance and proactive nudging — stay separate and are NOT in this build).
**Lift-condition rewritten 2026-08-05 — the old one is spent, and nothing now blocks this.** It read "cleared to run once [user-work-surfacing-fix] (the emergency) has shipped, rezipped, and been verified live". That item shipped at `ae92d34` on 2026-07-31, so the condition has been satisfied for over a month and the revisit could not tell, because a met condition and a still-waiting one read the same way when nobody re-checks the event. **This item is now held by choice, not by a blocker** — the same standing this queue already records for [release-docset-b-work]. What holds it is that a repo-wide identity rename touching every doc, hook, template and the GitHub repo itself should be its own deliberate /next run with the user present, never swept up unattended behind other work. **Greenlight it at any /plan close when you want it to be the session's work.**
**The positioning copy this rename must write is folded in here, relocated whole from [throughline-value-articulation] on 2026-08-05 (the user's call).** That item was captured from drafting the Discord throughline help article, which found the current framing — "sometimes you can ask why something is the case" — undersells the feature as a minor lookup, not worth the perceived cost of approvals and log-writing. It was folded rather than kept separate because this item already rewrites the README title, positioning and feature list wholesale and already says to lead with rationale-memory value, so keeping the words elsewhere would mean writing the positioning twice and letting the two drift. The ordering fact underneath: the pitch cannot be written before the product's name is settled, and this item settles it.
**The headline to lead with:** the throughline's value isn't occasional why-lookup — it's that *the user's intent keeps running the project even after Claude has forgotten everything.* Claude's memory resets each session; the throughline is why a fresh, short, even cheaper-model session still builds the project the way the user meant, instead of re-deriving intent from code and guessing wrong. That is the product, not a side feature.
**Value points to draw from:**
- *For Claude:* it's how Claude stays aligned to intent (not just "what" but "why you want it that way"); rationale riding a rule is *why the rule is followed reliably* (bare rules slip) — so it steadies Claude's own behaviour, not just answers user questions; it lets Claude catch drift, because recorded intent makes contradictions visible.
- *The "branches" framing (the user's):* the throughline isn't one file — intent lives in SPEC, rationale rides every QUEUE item, history lives in LOG. SPEC and QUEUE are read *during* planning and building, so the throughline shapes every plan and build silently, not only on a "why?" query; the LOG is the deep archive pulled on demand.
- *For the user:* stops re-deciding settled things (records why a rejected option lost, so it isn't re-proposed); prevents silent regressions (see why something is that way before changing it); institutional memory for coming back after a break or handing the project on; keeps a growing project coherent instead of rotting; confidence that the project still reflects intent rather than hoping Claude "remembers."
- *Reframing the perceived overhead (put this in the article):* the approving is what makes the record genuinely *yours* and trustworthy — a reason you never vouched for is one you can't trust on read-back; the log-writing pays a small cost now against the big "why did we do this?" cost later; and externalised intent is exactly what lets short, cheap sessions work at all.
**The work-flow-vs-throughline distinction (for the help docs):** the current article describes the *flow of work* (capture → processed → built), which is the work pipeline, not the throughline. The throughline is the *why that travels alongside* that flow and is preserved. Help docs land harder separating the two: "here's how work moves" *and then* "here's the throughline — the reasoning that rides with it and survives Claude forgetting." Uses the settled naming clarity above (throughline = reasoning spine across SPEC/QUEUE/LOG; the LOG records it, isn't it).
**The FAQ half of that content is [faq-backfill]'s**, not this item's — that build rebuilds the FAQ wholesale, so a throughline entry written here would be overwritten there. Cross-referenced rather than duplicated.
**Add the Discord community link to the README in this same pass (2026-08-03).** The invite is recorded in CLAUDE.md's External communication section and Alex has consciously accepted it being public. It isn't added to the README now because this item rewrites the README title, positioning and feature list wholesale, so a link added today would be rewritten here anyway.
**Files:** determined by the build-time identity grep (candidates above), plus plugin-behaviour.md (Why-pipeline section rename), SPEC.md (name/positioning), README title + feature list + the Discord link, CLAUDE.md, marketplace.json (`renames` map), and a FAQ entry (user-visible identity change + the one-time `/plugin install`).

#### Finding a referenced queue item is hard work for the user, and the queue they are reading may not be the queue on disk [queue-pointer-hard-to-follow-and-possibly-stale]
Captured by you during the 2026-08-05 planning session, at the moment it happened: you went to find [faq-edit-trips-planning-file-gate] and could not, because the only recent reference to it was a bare slug a couple of turns back, and the queue you were scrolling did not appear to reflect the items already processed this session.
**Two problems, and they compound.** The first is navigation: a pointer is a plain `.md` link that lands at the **top** of a 460-line file, so every lookup is a scroll or a search — and once a checkpoint has passed, later messages refer to the item by bare slug only, with no heading text to search for. The rule that a pointer carries the target's exact heading text applies at the pointer; it does nothing for every subsequent mention.
**The second is worse, and it was tested in-session on 2026-08-05: the queue you read IS stale.** A real edit was made to this very item and confirmed on disk, and the user's already-open view of QUEUE.md did not show it — that view was still showing the file as of an earlier point, with [merge-and-fold-without-asking] as its newest content. The user had separately found that clicking to another document and back was what made earlier changes appear.
**That pair of observations identifies the cause and rules the other candidate out.** The desktop viewer serves a **cached snapshot** of an already-open document and refreshes it only on navigating away and back. It is not Drive sync lag: sync lag would survive a re-open, and a re-open shows current content immediately. Recorded as an established finding rather than a theory, because it was tested rather than reasoned about.
**Second test, same session: the staleness is NOT markdown-specific.** A `.py` file in the same folder, opened and then edited while the user watched it, also showed the old content. So the viewer caches every open document regardless of extension. This matters twice over: it rules out a file-format change as a fix (see [doc-filetype-switch-for-line-links]), and it makes anthropics/claude-code#84017 a general viewer bug rather than a markdown-rendering one.
**A note on how the first attempt at this test failed, because it is a method lesson and not just a mishap.** The test was first built as two files whose names differed only by extension — `viewer-refresh-ab-test.md` and `viewer-refresh-ab-test.py` — and the user was asked to open the right one from a link. They opened the wrong one and lost the observation. The project's CLAUDE.md records that the user scores in the bottom 10th–25th percentile on symbol search, which is exactly the skill "pick the correct one of two near-identical strings" demands. The redesign removed the choice entirely: one file, one link, one unmistakable name. **The general lesson: when a step needs the user to identify something, the design should remove the discrimination rather than rely on it.**
**The consequence for the method is immediate and larger than the annoyance.** Write-first-then-point assumes the user can read what was just written by following the pointer. If their open view is a snapshot, then a pointer emitted right after a write can land them on text that does not yet include it — so they may approve, or fail to find, a version that no longer exists. The remote-mode inline paste never had this failure. **Interim mitigation while this stands: pair every pointer with either an inline paste or an explicit instruction to click away and back.**
**The workaround is itself a cost, reported 2026-08-05.** Searching within the file flips the desktop viewer out of rendered display into code mode — a smaller, narrower font that is immediately harder to read — and switching back to the rendered view returns the document to the **top**, discarding the position the search just found. So the suggested remedy for "the link lands at the top" is a workaround that ends by putting you back at the top, in a less readable font. Filed as anthropics/claude-code#84018; it is harness behaviour, not method behaviour. The stale-viewer finding below is filed as anthropics/claude-code#84017. Both were duplicate-checked before filing and neither had a prior report.
**Why this matters more than the inconvenience.** The whole write-first-then-point rule rests on the user being able to read what was written, in place, and approve it there. If the pointer is hard to follow *and* the view can be stale, then approval-in-place is weaker than the method believes — the user may be approving from a version that no longer exists, which is exactly the failure pasting inline never had. That makes this a question about a load-bearing rule, not a UI annoyance.
**Narrowed 2026-08-05 to the harness bugs only.** The navigation half — how a pointer should be written so the user can find the item — is answered by [retire-working-mode-for-short-summary-default], which makes every pointer arrive with a short inline summary, so the user can act without following the link at all. What remains here is the three Claude Code bugs themselves, which bite whatever the method does.
**The three, with what was established about each:**
1. **#84017 — the viewer serves a stale snapshot** of an already-open document and refreshes only on navigating away and back. Tested rather than reasoned about, and **not markdown-specific**: a `.py` file in the same folder went stale identically, which rules out both Drive sync lag and any file-format fix.
2. **#84018 — searching within the file flips the viewer** from rendered display into code mode (smaller, narrower font), and switching back returns the document to the **top**, discarding the position the search just found. So the remedy for "the link lands at the top" ends by putting you back at the top, less readably.
3. **#83475 — a `.md` link with a `:N` line suffix is dead.** It fails silently while still looking clickable. `.py` and `.json` links with `:N` do open at the exact line, so this is a markdown-path bug, not a missing feature.
**Facts relocated here from the deleted [doc-filetype-switch-for-line-links], because they answer questions that will otherwise be re-asked.** Only two files are locked to the `.md` extension — each `SKILL.md` and `CLAUDE.md` — so the queue, spec, log, templates and procedure docs are all technically free to change. A switch was measured at **698 occurrences of `.md` across 35 files**, including all three hooks, and reverting would cost the same again. Extension does not change how Claude reads a document, so nothing about cross-item reasoning would fragment; line anchors are a viewer affordance for the human only. And a format switch would **not** fix staleness (see point 1), which was the main reason anyone wanted to expedite it. The last extension test was inconclusive rather than negative — the test vehicle was broken (new, untracked, repo-root files, where even `.py` failed) — so nothing was learned about whether a prose extension can line-anchor.
**One method lesson from that testing, worth keeping.** The first test asked the user to open the correct one of two files whose names differed only by extension. They opened the wrong one and the observation was lost. The project's CLAUDE.md records that the user scores in the bottom 10th–25th percentile on symbol search, which is exactly the skill that task demands. **When a step needs the user to identify something, remove the discrimination rather than rely on it** — one file, one link, one unmistakable name.
**Processed 2026-08-06: kept, and moved below the readiness line with a mechanically-checkable lift-condition.** As an Unprocessed capture it could only ever be processed into "check again later", which meant surfacing every session forever — the nagging shape this method has spent months removing. Below the line with an automatic check, it is silent until something actually happens.
**State re-checked at processing (2026-08-06): all three still OPEN, no movement.** #84017 last updated 2026-08-05, #84018 2026-08-05, #83475 2026-08-03. The related harness feature request #77134 was checked in the same pass and is also still open, last updated 2026-08-03.
**Only ONE of the three has a method change waiting on it, and saying so is what keeps the revisit honest.**
```
#83475  dead :N link on .md   ->  REAL work waiting. On a fix, pointers gain
                                  line anchors, and the standing rule that
                                  forbids emitting a :N suffix on a .md link
                                  (doc-bound-text, plugin-behaviour.md) is
                                  revisited rather than left over-cautious.

#84017  stale viewer snapshot  ->  MUCH weaker than when filed. The
                                  short-summary-with-every-pointer rendering
                                  was since decided as the default on its own
                                  merits ([approval-flow-token-doubling-
                                  simplification]), so a fix here is welcome
                                  but changes no rule.

#84018  search flips to code   ->  NO method consequence, either way. It is a
        mode, loses position       daily irritation for the user with nothing
                                   for the method to build. Tracked so it is
                                   not mistaken for pending work.
```
**Lift-condition, mechanical and requiring nothing of the user: has any of #84017, #84018 or #83475 been closed?** One `gh issue view <n> --repo anthropics/claude-code --json state` per issue, run silently by /plan's below-line revisit. Propose lifting only when **#83475** closes, since it is the only one carrying work; a close on either of the others is narrated in one line and the item stays put. **This is the set-the-trap pattern that broke the [statusline-context-reader] cycle** — a condition waiting on someone remembering to look will roll forward indefinitely, and a condition a command can answer will not.
**The escape route is CLOSED, decided 2026-08-06 — these three bugs are permanent until Anthropic fixes them.** This item previously said the bugs might be resolved "by leaving rather than by fixing", pointing at a move to the VS Code extension. That item was deleted the same day: the user settled that **the Claude Code desktop app is the platform, as a product decision** (now recorded in SPEC.md under Who it's for), and is building their own reader (mdreader) as a separate project of their own. So there is no surface change coming, and waiting on one is not a strategy. Note the reader addresses **reading** only — it changes nothing about these three bugs, which live in the app's own viewer. The lift-condition below is now the only route, which makes its mechanical checkability the whole value rather than a convenience.
**Files (rough):** none — no method change is proposed here. Relates to [retire-working-mode-for-short-summary-default] (which answers the navigation half). The mdreader direction that replaced the editor-switch route is the user's own separate project and carries no work item here — [mdreader-as-the-reader] was deleted at the 2026-08-07 planning session once its three closed doors were recorded in SPEC.md, the shipped FAQ, and this project's CLAUDE.md.

## Unprocessed

#### Last session advises refreshing the installed host before trusting any host-side judgment, and settling the merge only after that [advisory-refresh-host-before-merge-judgment]
Filed at the 2026-08-07 planning close. Three state facts with a recommendation attached; none can be read off queue position, and one of them undercuts a decision that was about to be made on the old picture.
**One: the running host is not the working tree, and the gap is unmeasured.** The target's build stamp is `e224c1490fa5` and it matches **none** of the five installed builds. So the soak that [merge-blitz-2026-08-06-to-main] rests on has been exercising *a* build from this branch, not *this* branch. That does not make the soak worthless; it makes it evidence about something adjacent to what would be merged, and nobody can currently say how adjacent.
**Two: the signal that would normally answer that question is itself in doubt.** The stamp `session_start` reported at this session's start matched nothing on disk — not the target, not any cached build, including the one whose version it named as its own. Recorded as [build-stamp-reported-matches-nothing-on-disk] with no cause offered, because none has been tested. **Until that is explained, treat every host-side liveness judgment as unreliable in both directions** — the mechanism replaced asking the user what is installed, so a wrong answer arrives looking exactly like a right one.
**Three: the release now demonstrably waits on the merge.** [release-ritual-silent-on-branches] was processed this session and resolved the fork: the release trigger fires from `main`, never from a branch, with "merge sooner" as the escape route when a held release carries an actively-spreading fix. The currently-held release contains exactly that — the removal of a hook that was writing the user's absolute project path into a public repository. So the merge is no longer only about docset retirement; it is what gets a privacy fix to consumers.
**The recommendation, in order.** Rezip and fully restart the app so the host *is* the branch; run `resources/testing/hook_schema_check.py` before the restart and ask the first fresh session **what it actually received in its context** after it — the wording matters, because a dead hook and a live one are indistinguishable when Claude can reconstruct plausible behaviour from other context. Then judge the soak on a build that matches what would be merged, and merge. Building the cleared region first is also defensible and loses nothing, since a build edits the working tree either way — but see the host caveat on [advisory-build-while-designs-are-fresh] before reading a quiet run as proof.
**The merge question was asked at this session's opening and never answered** — `git branch --merged main` confirms the branch is still unmerged. It is the user's judgment and it is still open.
**Persist until the host has been refreshed and the stamp discrepancy explained.** The merge half is spent once the merge happens or is deliberately declined.

#### Last session advises building the cleared region soon rather than planning further, because the designs are fresh and staleness is the measured enemy [advisory-build-while-designs-are-fresh]
Filed at the second 2026-08-06 planning close. This is a reason, not a work item — queue position says *what* is next and cannot say *why now*.
**The finding this rests on.** Two consecutive planning sessions have now found roughly half their items resting on premises that had gone stale since capture — eight of sixteen at the previous close, seven of fifteen at this one, four of those seven outcome-reversing. The cause is structural: an item is written when its claims are true, then sits while the project moves under it, and nothing re-reads it until processing. **The cleared region is currently about twenty items, and every one of them was designed within the last two days.** That is the freshest this queue has been, and the value decays from here. Building now is worth more than planning more.
**Rewritten again at the THIRD 2026-08-07 planning close.** The thirty-one-item cleared region it described was **built and drained** by the /next run that followed. The region has since been refilled with **eleven items designed at this session**, so the freshness argument stands but points at different work. One thing is genuinely new and cuts against building immediately — see the host caveat below.
**The finding still stands, and there are now four consecutive data points for it.** Each planning session keeps finding a large fraction of its items resting on premises that had gone stale since capture. This session added its own: a capture's confident "docset A's copy is correct too" was false and the same bug sat in both docsets; a mover defect described as a fix direction to settle turned out to be reproducible in two minutes with the correct code already present elsewhere in the same file; and an item's cited line numbers had all drifted. The cause is structural — an item is written when its claims are true, then sits while the project moves under it, and nothing re-reads it until processing.
**A FIFTH data point arrived this session, and the first from OUTSIDE this project:** a consumer project's own planning session found an item whose stated prerequisite had quietly cleared. So the load-bearing-claims check is not just a local habit — it ships, it fires in consumer projects, and it keeps paying.
**What is fresh now:** eleven cleared items, all designed at the 2026-08-07 planning session against a corpus that has not moved since. Most are small, independently-scoped doc repairs in named files, so a run degrades gracefully if interrupted. Two `[user]` walk-through items sit at the end of the cleared region and are deliberately doable anywhere — clicking links, reading a code block, noticing whether suggested replies render.
**THE HOST CAVEAT, which is new and cuts the other way — read it before treating a /next run as proof of anything.** The target's build stamp matches **no installed build**, so the running host is behind the working tree by an unknown amount, and [build-stamp-reported-matches-nothing-on-disk] records that the stamp the hook *reports* matches nothing on disk either. Several cleared items edit hooks and procedure docs. Those changes can be *built* and committed safely — the working tree is what a build edits — but **nothing built this way can be verified as live until a rezip and a full app restart**, and the usual signal for judging that is itself in question. Build if you want the work done; do not read a quiet run as evidence the changes took.
**Persist until the cleared region has been substantially built**, or until a session finds a reason the designs have moved.

#### Last session advises doing the history rewrite as its own deliberate session [advisory-cleared-run-then-history-rewrite]
**Rewritten at the 2026-08-05 close to only what still stands** — the first partly-spent advisory handled under the new rule from [codex-advisory-retire-argument-unweighed], which says a half-consumed advisory is rewritten rather than cleared or kept whole. What was consumed: "run the five cleared items first" (the overnight blitz ran them), the note that a full app restart was due (done — the session ran on host 1.17.0-test1), and the flag that [release-docset-b-work] looked spent (confirmed at the close: v1.17.0 shipped at `5f1a79e`, and the item now sits below the line awaiting reprocessing).
**What persists:** [history-rewrite-third-party-scrub] is the only exposure in this queue that is actively growing rather than sitting still in a design. It is deliberately below the readiness line and must stay there — it force-pushes a rewrite of the entire history and needs the user present, so it must never be reachable by an unattended run. **Open a session for it deliberately.**
**Persist until the history rewrite has been done** — not spent by a planning session reading it, because the recommendation concerns work that has not happened yet.

#### The build stamp session_start reports matches none of the installed builds on disk, so the one signal that replaced asking the user may be unreliable [build-stamp-reported-matches-nothing-on-disk]
By Claude, found 2026-08-07 while gathering evidence for the merge decision on [merge-blitz-2026-08-06-to-main]. Filed rather than explained, per the compare-never-explain rule: the discrepancy is stated, no cause is offered.
**What was measured, and the commands are recorded so this is reproducible rather than a recollection.** Running `content_stamp()` from `plugin/si-plugin/hooks/session_start.py` (invoked with `PYTHONDONTWRITEBYTECODE=1` so the read leaves no residue) over the target and over every installed build in `~/.claude/plugins/cache/flintcraft/sovereign-implementer/`:
```
TARGET  plugin/si-plugin        e224c1490fa5
host    1.17.0-test1            84b3052e30df
host    1.18.0                  e949b5282949
host    1.18.0-test1            b1530a5574b4
host    1.19.0                  0c2415a80e01
host    1.19.0-test1            0982cf79340c
session_start REPORTED at this session's start:   3f6c1a662fdc
```
**Two separate findings, and they must not be collapsed.**
1. **The target matches no installed build.** That is expected and benign in itself — it simply means the host has not been reinstalled since the latest target changes, which is exactly what the stamp exists to tell us. Its consequence is real though: **the branch's plugin changes are not live in the host**, so the soak has been exercising something other than the branch's current state.
2. **The stamp the hook REPORTED matches nothing on disk at all** — not the target, and not any of the five cached builds, including `1.19.0-test1`, which is the version the same hook named as its own in the same message. **No cause is offered here.** Candidate explanations exist (the running host may not be one of these cached directories; the hook may stamp a different root than the directory name suggests; something may have changed between session start and the measurement) and none has been tested, so listing them as though one were likely would be the invented-rationale failure this method already records.
**Why the second finding is the serious one.** The build stamp is not a convenience — it is the mechanism that **replaced asking the user what is installed**. /plan compares the reported stamp against the target's to decide whether host-side changes are live, and both answers are acted on: a match means host-side tests are meaningful, a mismatch means deferring them. If the reported value corresponds to nothing on disk, that comparison has been returning confident answers that may be wrong **in either direction** — and the wrong answer looks exactly like the right one.
**A SECOND OBSERVATION, taken at the same session's rezip, and it is the strongest lead so far: a cached build's stamp is not stable.** The directory `1.19.0-test1` stamped `0982cf79340c` early in the session and `71f0ca224f84` later, **with no reinstall of that directory in between**. Only a prune of older siblings and an install of a *new* sibling (`1.19.0-test2`) happened between the two readings. So the stamp over an installed build changes while it sits there — which would explain the reported value matching nothing, without any of the earlier guesses. **This is a lead, not a conclusion.** What must be established: which files inside a cache directory change at runtime, and whether `content_stamp`'s exclusions (`__pycache__` directories and `.pyc` files) are missing a generated artifact. The rezip confirmed the mechanism works when both sides are freshly measured — the new build `1.19.0-test2` stamped `ab5400b8d618`, exactly matching the target — so whatever moves the stamp appears to act on a build *after* installation rather than corrupting the computation itself.
**The first thing a build must do is establish which directory the running host actually is**, rather than assuming it is the cache entry matching the version string. Everything else depends on that. Note the hook computes its stamp over `CLAUDE_PLUGIN_ROOT`, which is supplied by the harness — so what that variable actually holds at runtime is a fact to capture from a live payload, not to infer from the directory layout.
**Do not treat this as disproving the stamp mechanism.** It shows one reported value not matching one set of directories. That is enough to stop relying on the comparison until explained, and not enough to conclude the design is wrong.
**Files (rough):** `plugin/si-plugin/hooks/session_start.py` (`content_stamp` and the state line that reports it), possibly `resources/testing/hook_schema_check.py` for an assertion once the correct root is known. Relates to [merge-blitz-2026-08-06-to-main] (the decision this was gathered for), [setup-as-migration-home] (which adds a second host-side signal and should not be built on an unreliable one), [verification-tooling-can-lie-confidently] (the same day's other instance of a confident-looking measurement being wrong).

#### Correspondence with a companion project has no method support, so the user is left deciding what to relay back [companion-project-correspondence-unsupported]
Captured by you 2026-08-07 — surfaced at the wind-down re-scan from your own words mid-session: *"sorry, I'm not sure what to pass on to the other project. if you have anything to say to that chat please let me know."*
**What happened, and it happened three times in one session.** Messages arrived from a companion project, were processed into queue changes, and then the user had to work out what — if anything — went back. Each time the reply had to be asked for rather than offered. Three substantial replies were drafted, each carrying commitments about a published format another application is being built against, and each needing to be recorded in the LOG in full so a later session can match returning answers to what was actually asked.
**Why it is a gap rather than a one-off.** SPEC's External communication section covers **announcements** — Discord posts about the method, written for an audience. It says nothing about **correspondence**: a two-way exchange with a specific project, where a message arrives, changes work here, and warrants an answer. The method has a well-developed inbound half (captures, verification of incoming claims, dispatch notes) and no outbound half at all. So the reply happens only if the user thinks to ask for one, which is the same evaporation shape the `[user]` under-filing rule exists to prevent — except here it is Claude's work evaporating, not the user's.
**What the session did by hand, which is the candidate design.** After each exchange: file what the message changed; note in the item what a returning answer will look like; draft a reply for approval; record the sent text in the LOG in full. That worked well enough that both dispatched questions came home the same session and landed on the right items. Nothing about it was written down, so it survives only in this entry.
**To settle at processing.** Whether this is a rule or merely this project's situation — the honest counter is that most consumers have no companion project, so a shipped rule would rarely fire. If it is kept, whether the trigger is "a message arrives from outside" or the narrower "a message changes work here"; and where the sent text lives, since the LOG is currently the only home and three full replies is a lot of entry.
**Files (rough):** `docs-b/plugin-behaviour.md` (the research-and-evidence triage, where the incoming half already lives) or `SPEC.md`'s External communication section; possibly `CLAUDE.md` if it stays host-only. Relates to [dispatch-return-check-blind-across-projects] (the incoming half, kept this session), [editing-signal-runtime-semantics-undocumented] and [gitignore-does-not-stop-drive-sync-of-throughliner] (the two items carrying hand-run dispatch notes), [log-scope-adjacent-work] (the artifact test that says the sent text belongs in the LOG).

#### "Greenlight it at any /plan close" names a step that does not exist, so below-line work waits on a decision nothing ever asks for [greenlight-step-does-not-exist]
Captured by you 2026-08-08 — you said you had been trying to greenlight the merge across several sessions and it kept not happening, and asked to be told plainly what stage the cycle was on. The investigation that followed is this item. **Your report was the only thing that could have surfaced it:** every individual session behaved correctly, so nothing internal was ever going to notice.
**What was found, by grep rather than by reasoning.** Seven Processed items below the readiness line instruct the user to *"greenlight it at any /plan close"* — including [merge-blitz-2026-08-06-to-main], [retire-docset-a], [concurrent-session-support], [release-docset-b-work], [rename-to-throughliner], [setup-as-migration-home] and [docset-b-register-decision-weaker-than-recorded]. **The word "greenlight" appears nowhere in the procedure docs as an action.** `done-plan.md`'s close has no such step: it positions the readiness marker, clears the advisory, writes the entry and commits. Nothing asks.
**Where the question actually fires, and why that is the wrong end of the session.** The only place a below-line item's condition is raised is `plan.md` **Step 1** — the consolidated user-only question at the session's **opening**. So the instruction points at the *end* of a session and the machinery fires at the *beginning*. This is the retired push marker's exact shape — stated in one place, implemented in none — and it is worth naming as such, because that precedent is the argument for treating it as a defect rather than a wording slip.
**The deeper mismatch, which is the part a simple "move the ask to the close" would not fix.** The revisit is built for **external events**: "has the thing you were waiting on happened yet?" A greenlight is not an event. It is a **judgment about whether things have been going well enough to proceed** — and asking it at a session's opening asks the user to judge a soak at the moment they have the least information about it. By the close, when they have a session's worth of evidence, nothing asks again. The two are different question shapes and the machinery only has one.
**A live demonstration, from the session that found it.** The consolidated question was asked correctly at this session's opening. A consumer bug report arrived in the next message, the question was never returned to, and later in the same session Claude supplied a *fresh* reason to defer the merge. Every step was individually correct. The outcome was another session where the greenlight did not happen.
**And a compounding risk in the existing machinery, which must be checked before anything is built.** The revisit's rule is that a "no change" answer records a `Set aside ·` line and stops asking. If a greenlight question is ever answered "not yet", that would silence it under a mechanism designed for external waits — turning a judgment the user actively wants to make into one they are no longer offered. Establish whether that has already happened to any of the seven.
**To settle at processing.** Whether the fix is a greenlight step at the /plan close (where the evidence is), a distinct condition *type* separating judgments from events, or removing the phrase from the seven items and pointing them at whatever mechanism actually exists. Note the honest secondary question: whether a cycle whose reconcile phase keeps adding work to the same branch has a defined terminating condition at all — the audit's own repair captures *were* cleared, so this is a question rather than a finding.
**Files (rough):** `docs-b/done-plan.md` (the close, if the step lands there), `docs-b/plan.md` (the revisit and its condition types), `docs-b/plugin-behaviour.md` (the below-the-line revisit section), `CLAUDE.md` (the branch-cycle section, which names the merge gate). The seven items' own prose needs the phrase corrected either way — grep `greenlight` across QUEUE.md before finalising. Relates to [merge-blitz-2026-08-06-to-main] (the item that surfaced it), [set-aside-silences-the-lift-condition] (the silencing mechanism named above), [pre-merge-differential-audit] (the cycle this gates).

#### A two-hook relay may give compaction re-injection the trigger it was said not to have [compaction-reinjection-via-postcompact-flag]
By Claude, from [audit-premise-claims-verification] run on 2026-08-07. The audit was asked to establish two documented facts and to report rather than design; this is the report, filed as work because it revives something currently recorded as unbuildable.
**Both facts the audit was asked for, read from `code.claude.com/docs/en/hooks` rather than inferred.** **(a)** The set of events that can return `hookSpecificOutput.additionalContext` still includes `UserPromptSubmit` and `PostToolUse`, and still excludes `PreCompact`, `PostCompact` and `SessionEnd`. So [compaction-reinjection-has-no-usable-event]'s core premise **holds**: no compaction event can inject context. **(b)** `transcript_path` is present in the hook payload, and the documentation gives **no** way for a mid-session hook to learn from its own payload that a compaction has happened — it also warns the transcript file is written asynchronously and may lag the live conversation, so reading it to infer compaction would be racing a file that is behind by design.
**The finding the question did not anticipate, which is why this is a capture and not just a log line.** `PostCompact` cannot *inject*, but it does **fire**, and side-effect-only events can still write to disk. So the missing trigger may exist as a **relay across two hooks**: `PostCompact` writes a small flag file; the next `UserPromptSubmit` sees the flag, emits the re-injection as `additionalContext`, and deletes the flag. That yields exactly the once-per-compaction firing the shipped item wanted, using only documented behaviour of each event, and it sidesteps the specific objection recorded against the fallback — that a `UserPromptSubmit` hook would otherwise fire on **every** prompt, since here it emits nothing unless the flag is present.
**Recorded honestly, because the audit's own discipline is not to design from a document skim.** This is a *route*, not a verified mechanism. Three things are unestablished and must be read or tested before anything is built: whether `PostCompact` is actually registrable in the shipped `hooks.json` shape this plugin uses; whether it fires reliably in the **desktop app**, which is this method's platform and where the `model` field's absence has already shown that documented payloads do not always arrive; and where a flag file could live without violating the method's own rules — it is transient local session state carrying no project meaning, so the `.throughliner/` folder (already gitignored, already transient, already the home of the editing-state markers) is the obvious candidate rather than anything tracked.
**The honest counter, which belongs with the proposal.** Even working perfectly, this re-injects rules **one prompt late** — the first turn after a compaction runs without them. Whether that is worth two hooks and a flag file on disk is a design call nobody has made, and it is not this item's to make.
**Skipped to the bottom at the 2026-08-07 planning session, with two of its three unknowns resolved here so the next pass starts further along.** It could not be kept: the keep-step requires stating what would change inside which files, and whether there is anything to build at all still turns on the third unknown.
1. **Registrability — resolved, and it is not an obstacle.** `plugin/si-plugin/hooks/hooks.json` registers events as a plain event-name-to-handler map with an optional `matcher`, and does nothing per-event beyond that. So nothing in this plugin's own file would need reshaping to add `PostCompact`; the only open half is whether Claude Code accepts that event name, which is a documentation read rather than a design question.
2. **The flag file's home — resolved, and the item's stated reason was FALSE.** This item chose `.throughliner/` on the grounds that it is "already gitignored". Checked: in this project it is not — `git check-ignore` returns nothing for it and `.gitignore` carries only `*.zip`, `__pycache__/`, `*.pyc` and `.claude/`. The folder is still the right *kind* of home (transient, session-scoped, already the editing-state markers' home), but the premise that made it obviously safe does not hold wherever /setup ran before the editing-state feature shipped. See [throughliner-folder-not-gitignored-in-old-projects], filed from this check.
3. **A naming constraint arrived from the consumer side 2026-08-07 and must be honoured whatever else changes.** The companion reader is **designed** to watch `.throughliner/` and key on the `editing-*.json` glob specifically — designed, not built; that project has no code yet, so this is a stated design intent rather than observed behaviour. Any file whose name does not match is ignored safely — but a flag file named to match (anything starting `editing-` and ending `.json`) would be parsed as an editing marker. **Name the flag file so it cannot match that glob**, and treat this as part of the published contract rather than a courtesy: the reader is built against the shipped format, and a name collision would make it misread our own flag as an edit in progress.
4. **The folder's privacy reasoning is weaker than this item assumed, on a second axis.** Beyond not being gitignored in older projects, gitignoring does not stop *syncing* — see [gitignore-does-not-stop-drive-sync-of-throughliner]. A flag file added here inherits that, so weigh what the flag's contents would be; a file carrying no paths is much safer than one that does.
5. **Desktop-app firing — STILL UNCHECKED, and it is the one that decides whether this exists at all.** Whether `PostCompact` actually fires in the Claude Code desktop app, which is this method's platform. The `model` field has already demonstrated that a documented payload can simply not arrive here.
**Where that third question was going to go, and why it did not.** The intended route was the standing verification item, per [route-audit-shaped-questions-to-the-audit]. **That item no longer exists** — [audit-premise-claims-verification] ran and was consumed at `5993a10` on 2026-08-07, and the eleven remaining mentions of its slug in this queue are all cross-references in other items' prose. So the route this item's sibling was skipped under is currently unavailable, which is itself evidence for [route-audit-shaped-questions-to-the-audit]'s open question of whether a standing audit item should be a maintained fixture rather than an item that drains like any other. **Do not process this item until that question has a route and the firing check has an answer** — a third pass reaching the same wall is the roll-forward failure [compaction-reinjection-has-no-usable-event] was filed to record.
**Why it is filed rather than folded into the existing item.** [compaction-reinjection-has-no-usable-event] records a correct finding — the build as designed is unavailable — and that finding stands unchanged. This is a different construction reached from the same facts, so overwriting that item would erase a conclusion that is still true.
**Files (rough):** none until the three unknowns are settled; then `hooks/hooks.json`, a new or extended hook, and `docs-b/plugin-behaviour.md` if the behaviour becomes user-visible. Relates to [compaction-reinjection-has-no-usable-event] (whose premise this confirms and whose dead end this may route around), [audit-premise-claims-verification] (the audit that produced it), [no-session-end-or-compaction-hook].

#### The shell-write matcher passes scripted writes to IN-SCOPE files, so it would not have caught either of the two real slips [shell-write-matcher-blind-to-in-scope-targets]
By Claude, established 2026-08-08 by the first run of `resources/testing/test_pre_tool_use_shell_writes.py`, built for [shell-heredoc-write-immediately-after-authoring-the-rule]. Filed after that item's own commit; the finding is what its test was built to establish.
**What the test established.** `pre_tool_use.py`'s structured-write matcher denies a scripted write only when the target is **outside** the active build's Files list (its denial text names the scope-lock as its reason; method docs, memory, research and scratchpad also pass by design). Both recorded live slips — the heredoc append to `templates/faq-template.md` and the multi-site substitution in `docs-b/setup.md` — wrote files that were **in scope** for their run. So the mechanical check that item hoped "would have stopped both instances regardless of what any doc said" stops neither. The doc rules are currently the only protection for the in-scope case, and they are the rules that slipped three times in one day.
**The design question, which is a hook-behaviour call and therefore not decided overnight.** The denial's two stated reasons split: the scope-lock reason genuinely doesn't apply to an in-scope target, but the stale-mount-overwrite reason applies to every scripted write regardless of scope — and the user's own global instruction forbids shell writes unconditionally for exactly that reason. Extending the denial to in-scope targets would close the gap the real slips went through; the counter is that the sanctioned scripted case (bulk queue edits) relies on method docs passing, and QUEUE.md already passes via the method-doc exemption, so the extension may cost nothing. Weigh whether the sanctioned-case carve-outs (method docs, scratchpad) are sufficient escape routes before extending.
**The test pins current behaviour deliberately** (case 4, named in its header): if the hook is extended, that assertion flips as part of the same build.
**Files (rough):** `plugin/si-plugin/hooks/pre_tool_use.py` (the structured-write check's scope condition), `resources/testing/test_pre_tool_use_shell_writes.py` (flip case 4). Relates to [shell-heredoc-write-immediately-after-authoring-the-rule] (the item whose test found this), [shell-writes-bypass-file-scope-lock].

#### Untrack FAQ/ from this repo with `git rm --cached` — the half of the gitignore repair that needs your explicit yes [untrack-faq-needs-your-yes]
By Claude, split out 2026-08-08 when the overnight blitz built [throughliner-folder-not-gitignored-in-old-projects]. Part 1 shipped: `.throughliner/` and `FAQ/` are now in this project's `.gitignore`. Part 2 — untracking the already-committed `FAQ/faq.md` and `FAQ/index.md` — was that item's own condition: it removes committed files from the repository, so it must not happen on an inference during an unattended run. Filed here so the deferred half stays tracked rather than living only in a log entry.
**Why it is safe by design, carried over from the parent item:** SPEC has the FAQ restored from the shipped templates whenever it is missing, so a fresh clone regenerates it; `git rm --cached` keeps the files on disk. And [faq-backfill] should know: once untracked, that rebuild refreshes a local mirror and its close should not expect a diff there.
**The command, once you say yes:** `git rm --cached FAQ/faq.md FAQ/index.md`, then commit.

