# QUEUE

## Batches

Worked top to bottom. Each batch of changes or tests is one /next session of either type:

- build session where changes are applied, then claude runs all tests it is able to do itself
- test session where the user runs any testing only they can do

**Verbatim-copy strings in fenced blocks; combine commit approval**
In the desktop app, Ctrl+C copies the whole assistant message, so strings the user needs to lift verbatim (commit message, commit body, paste-ready prompts) only work as copy targets when each one sits in its own fenced code block. Commit message and body are the most frequent case — every /done. Currently they're presented and approved as separate steps, which is redundant; they can be displayed together as two adjacent copyable blocks and approved in one go. A general rule in plugin-behaviour.md keeps this from drifting back as other copy-need cases surface.

Build:
- plugin-behaviour.md: add a rule that strings the user is meant to copy verbatim must be presented in fenced code blocks. Cover the why (desktop app Ctrl+C copies the whole message, so only fenced blocks give a clean copy affordance). Examples: commit messages, commit bodies, paste-ready prompts.
- done.md Build close-out Section 2.4 (commit step): rewrite to present the commit message title and commit body each in its own fenced code block in the same message, then ask for a single approval covering both.
- done.md Plan close-out Section 3 (commit step): same change.

**Tag the no-test-section decision as [SILENT]**
When /plan drafts a batch without a Test section, the absence currently gets narrated ("No Test section because the change is verifiable..."). That narration is noise — the user wrote the rationale and knows what kind of change it is. The existing tag system covers this case; using `[SILENT]` rather than prose keeps the procedure doc consistent with how output behaviour is described elsewhere.

Build:
- plan.md Step 3 Test section: tag the omission case as `[SILENT]` so the decision to skip a test section doesn't get narrated to the user. Use the tag, not a prose substitute.

**Promote recommendation must name concrete outputs**
The current /plan procedure asks for promote/park/drop before the batch entry is drafted, so the user approves a direction without seeing what would actually get built. This session that gap was bridged by Claude's style — concrete outputs got named at recommendation time organically. Style isn't a guarantee; a different model could recommend promote in abstract terms, leaving the user to approve blind. Codifying it structurally — promote must describe what would actually get built, in terms the user can recognize as the work product — closes the hole regardless of model. If the interview hasn't yielded enough to describe it concretely, the recommendation isn't ready; keep interviewing.

Build:
- plan.md Step 2 sub-step 2 (Recommend): change the Promote bullet so it requires describing what would actually get built — in terms the user can recognize as the work product. Add a forcing-function clause: if sub-step 1's interview hasn't yielded enough to describe it concretely, the recommendation isn't ready — keep interviewing. Park and Drop bullets unchanged.

**Tighten Why-pipeline preserve and retrieve rules**
plugin-behaviour.md's Why-pipeline section already covers preserving rationale as prose and retrieving it from LOG when a user asks why-questions. Two gaps blunt the rule. Preserve doesn't call out the common collapse-shapes (one-line summaries, dedicated why-fields, typed taxonomies) that look reasonable to a future doc or skill designer but lose meaning silently — without naming them, the same mistake gets remade. Retrieve doesn't mention LOG/index.md — so a why-search reads the full log files when the one-line-per-entry index would point to candidates first, faster and more accurately. Both edits serve the same end: when the user asks "why is the app like this?", the answer needs to exist as preserved prose AND be findable.

Build:
- plugin-behaviour.md Why-pipeline > Preserve: expand the existing rule to explicitly name the three collapse-shapes — one-line summaries, dedicated why-fields, typed taxonomies (e.g. "UX reason / functionality reason"). The expansion must include its own why woven in as inline prose (not a labelled field) so the rule models what it asks for: each shape loses meaning differently — a line truncates the reasoning behind a decision; a taxonomy is never complete and forces nuance into the closest pre-defined slot.
- plugin-behaviour.md Why-pipeline > Retrieve: update the search instruction to use LOG/index.md first (the one-line-per-entry summary) to find candidate entries, then read the full prose in LOG/log.md or LOG/log-v*.md. The Prior decisions section already references this rule and inherits the change.

**Stage sweep edits at push; warn on dirty plugin tree at session start**
push-and-rezip step 8 stages a fixed list (zip, archive, plugin.json, LOG/) that doesn't include whatever the pre-push sweep modified. Sweep edits — prose tightening in plugin/si-plugin/ to keep templates and skill docs aligned with the procedure changes being pushed — fall out of the commit and sit orphaned in the working tree across sessions. The next /next can then layer build edits on top of orphaned sweep changes, mixing unrelated work into one commit. Two complementary fixes addressing the two failure modes: at push, stage every dirty path in plugin/si-plugin/ rather than a named list (catches sweep edits automatically); at session start, when no build is in progress, warn if plugin/si-plugin/ has uncommitted state (catches existing orphans before a new build layers on top). Both edits land in this project's CLAUDE.md — the push-and-rezip workflow lives there, not in the shipped plugin.

Build:
- CLAUDE.md push-and-rezip step 8: replace the fixed stage list ("zip, archive changes, plugin.json, LOG/ changes") with an instruction to stage every dirty path in `plugin/si-plugin/` (via `git status --porcelain plugin/si-plugin/`) plus the zip in `plugin/`, archive changes in `plugin/zip-archive/`, and LOG/ changes. Sweep edits get caught automatically.
- CLAUDE.md (this project's, root): add a session-start dirty-tree check. When a session starts with no `_build.md` present in the project root, run `git status --porcelain plugin/si-plugin/` and warn the user if non-empty, listing the dirty paths. Surfaces orphaned sweep edits before /next layers build changes on top.

**Fix /clear-before-/done close-out order**
next.md Step 7 and plan.md Step 4 close-outs both tell the user "Run /done to record this and commit, or keep adjusting. Run `/clear` first to keep context clean." The "first" places /clear *before* /done, but /done reads the conversation to write a faithful LOG entry — clearing first strips exactly what /done draws on. The /clear advice already lives correctly at the end of /done itself, where it recommends clearing before the next skill. Fix is to drop the misplaced sentence from both offering close-outs; the post-/done placement carries the advice in the right spot.

Build:
- plugin/si-plugin/docs/next.md Step 7 (line 142): remove the "Run `/clear` first to keep context clean." sentence from the close-out. Keep the /done offer.
- plugin/si-plugin/docs/plan.md Step 4 (line 85): same change — drop the /clear sentence, keep the /done offer.

**Speed up LOG hash backfill with `git log -S`**
The backfill instruction in plan.md and next.md suggests `git log --diff-filter=A` or blame, both of which return a wider set of commits that Claude has to scan and match to entry titles by eye, plus often reading the full log files for orientation. `git log -S "<entry title>" --pretty=%h -- LOG/` returns the hash mechanically per placeholder — no scanning, no matching, no log reading. Pair it with a batch-read of every file containing `[HASH]` upfront so Edit's read-first rule is satisfied in one round-trip instead of one per placeholder. The instruction lives in two places (plan.md Step 1, next.md Step 1) and gets the same rewrite in both.

Build:
- plugin/si-plugin/docs/plan.md Step 1 "Backfill LOG hashes first": rewrite the instruction. New shape: (1) batch-read every file in LOG/ that contains `[HASH]` upfront; (2) for each placeholder, run `git log -S "<entry title>" --pretty=%h -- LOG/` and use the returned hash; (3) replace `[HASH]` in place. Drop the `--diff-filter=A` and blame fallbacks.
- plugin/si-plugin/docs/next.md Step 1 "Backfill LOG hashes": same rewrite, same shape.

### Parked

- Sizing gates rework — research filed at resources/research/batch-sizing-research.md. Three changes slated: (1) reframe "name concrete outputs" as the readiness gate (what differentiates batch-ready from still-a-capture), (2) remove the 5-test verification-burden rule, (3) replace with coherence test ("can Claude explain the batch in one sentence without multiple 'and's"). Further research needed on session-length as a mid-build split indicator — scroll bar length correlates with quality drop / auto-compact; is this because higher communication quality makes session length mirror cognitive load? Could a simple metric (word count, turn count) work as a split yardstick both mid-build and at planning time when actual session length isn't yet known?

## Captures

Captured outside /plan. Picked up and routed during the next /plan session.


- Repo installation guide for Claude — add a subpage (or top-level doc) to the GitHub repo aimed at a Claude Code session that a user has pointed at the repo URL. When a non-coder pastes the repo address into Claude and says "install this for me," Claude should be able to read this page and walk them through getting SI installed in their desktop app. Covers: what SI is in one line, that it's a desktop-app plugin (not a CLI install), where to get the zip (plugin/si-plugin.zip in the repo), and the desktop-app install steps. Audience for the page is Claude reading on the user's behalf, not the user directly.

- Claude is reluctant to volunteer research because doing so means admitting it doesn't know something — a face-losing move it avoids. Develop prompting around the research mechanism that reframes volunteering to research as the smart, capable move rather than an admission of ignorance, so Claude offers it at the right moments instead of waiting to be reminded the internet exists. Three angles to build the reframe on: (1) Neutral, blame-free framing — AI and software move fast, so any time the work depends on an external system, library, or API, checking the latest guidelines / release notes is just normal diligence, not a knowledge gap. (2) Stakes for the user — building on stale or under-researched assumptions risks the user spending a week building the wrong thing, which is very costly to undo; research protects against that. (3) How it reads to the user — Claude constantly needing reminders that research is possible looks bad; volunteering research at the right moments is NOT evidence of weak training and doesn't make Claude look dumb, it looks smart and capable — like it has tools and knows when to use them. The reframe needs to land all three so Claude internalizes research-volunteering as face-saving, not face-losing.

- Put the options Claude offers on a spectrum so the user can see there's more than what's listed, without forcing Claude to exposit endless build options. The problem: Claude typically gives a bounded range of options — that's deliberate (exhaustively listing every file type / approach would get tiresome and overload the user), but the cost is the user isn't in the driver's seat; they only steer among what Claude chose to surface. Idea: let Claude hint that other file types / workarounds / approaches exist by arranging the options it *does* give along a spectrum (e.g. easy → hard, or some other axis, possibly more than one axis), and signalling that there could be further options off one end or both ends. This keeps the visible list short while telling the user "there's more this way if you want it," putting them back in control of how far to push. Scale by altitude: for component-level choices, a single easy→hard spectrum. When reasoning about whole strategies / applications / fields (not just components), hint across two or three spectra at once, laid out in a tabular format. Research hook: Claude should be explicitly instructed to web-search whenever asked to expand the spectra (push past the ends / add more axes) — the off-the-ends options are exactly where Claude's own knowledge is thinnest, so expansion should pull from current sources. Overlaps with the research-volunteering capture above (same instinct to reach for the web at the right moment); the spectrum mechanism could be one concrete trigger for that behaviour.

- Rethink the line/character limits on LOG index entries — the "index entries must be ~one line" instinct is probably a wrong move. Asking, not deciding. It traces to forgetting that Claude doesn't read an index the way a human does: a human needs terse, scannable titles; Claude doesn't, so index entries don't need to be tiny. This matters beyond LOG formatting — the short-index constraint is part of what justifies keeping builds small for "higher retrieval granularity" (the limit-builds → limit-index-title chain that feeds the coherence test in the parked sizing-gates item). If index entries can be longer, that justification weakens. Open question is the right limit *mechanism*, two candidate shapes: (a) absolute — index entries capped at ~10 lines of some character width, log entries capped at roughly double that; (b) relational — index entries no longer than ~30% the size of the log entry they represent. The relational version likely needs much less thinking from Claude for a similar result, which is itself a reason to favour it. Pick the limit that yields good retrieval with the least cognitive overhead.

- Unify all length-limiting under the incoming batch-splitting logic, and retire the 5-test rule. Decision: the 5-test verification-burden rule goes. Batch-splitting becomes the single mechanism Claude is ever advised to use to limit length, applied uniformly to both build and test batches. Extend the same logic to /plan sessions, which tend to drag out: at the start of a plan session Claude decides up front the point at which it'll suggest /compact — not /clear, because clearing mid-session is the wrong tool when Claude needs to keep reasoning across many accumulated choices, whereas compact trims size while preserving that working context. Open design piece: what signal Claude uses to set that compact point at session start, before actual session length is known (ties to the word-count / turn-count yardstick question in the parked sizing-gates item).

- Reframe "name concrete outputs" as the batch-readiness gate — the test that distinguishes batch-ready from still-a-capture. Extracted from the parked sizing-gates item as a standalone, concrete change in its own right. Pending review and roll-in to a new batch.

- Coherence test survives, but reframed and re-justified. Keep it as a batch prerequisite, but drop the weird indirect framing ("explain the batch in one sentence without multiple 'and's") — state it directly to Claude as what it actually is: a test of whether the build can be summarised into an index entry. Mechanism bonus: that summary can be pre-generated pre-build and then reused as the index entry (valid as long as the build went exactly as planned). The real justification — and the part to preserve — is that the reasoning behind the existence of each product feature must stay findable via the index; the coherence test exists to protect that findability. Surface that justification explicitly and let it filter up in true why-pipeline fashion (capture → batch → log/index), rather than leaving it implicit in a one-sentence rule.

- done.md "Recommend next" templates (Plan close-out Step 4, Build close-out Phase 3) hard-code "Run /next when ready" whenever batches exist, with no branch for dependency-aware recommendations. plugin-behaviour.md says Claude owns sequencing, but the done.md template silently encourages the deferring-to-user shape — recommendation collapses to /next regardless of what unprocessed captures sit in the queue or how they relate to the top batch. Two-part fix: (1) before recommending, check if any unprocessed captures touch the same files/area as the top batch — if so, recommend /plan first and name the overlap; (2) when a captured item or a batch belongs in a different position based on dependency order, the recommendation must include offering to reorder the queue — captures, batches, or both — so the right item lands in the right place. Captures have order too; moving a capture up so it gets processed earlier in /plan, or moving a batch up so /next picks it first, are both valid reorderings and both Claude's call to offer. Recognising a dependency creates the obligation to make the queue reflect it, not just name it verbally. /next has a related but narrower scan (blocker gate looks for captures that "contradict, invalidate, or would benefit") — done.md's recommendation should mirror or extend that logic, and add the reorder-offering step on top.

- done.md Build close-out Phase 4 ("Push and context") is getting routinely skipped across multiple sessions. Observed pattern: Phase 3 ("Recommend next") finishes its own turn, the user replies, and Claude treats the build as closed — Phase 4's "Push to remote? (yes / not yet)" prompt never fires. This session it took the user explicitly asking "why didn't you offer to push?" to surface the miss. The skip looks structural, not random: Phase 3 reads like a natural close-out (recap-style sentence + recommendation), so once it lands, the procedure feels finished and Phase 4 falls off the end. The fix probably needs to lean on whatever mechanism makes Phase 3 reliably its-own-turn — perhaps an explicit tag (`[SEQUENCE]`?) marking Phase 3 → Phase 4 as a forced two-step, or rewording Phase 3 so it doesn't read as terminal. Also worth checking how often this miss happens versus how often the user actually wants to push then — if "push now" is the common case, the offer matters; if it's rare, the skip is mostly invisible damage but still erodes trust in the procedure. (Parallel concern already captured: Plan close-out Step 5 push prompt shouldn't exist at all; this capture is about Build close-out, which legitimately needs the prompt.)

- Remove all `/clear` references from done.md and any other skill that mentions it (e.g. plan.md, next.md close-outs). Currently done.md Build close-out Phase 4 and Plan close-out Step 5 both end with "Either way, run `/clear` before the next skill," and similar language may live elsewhere. The /clear advice doesn't belong in skill procedure docs — when to clear context is a user judgment call about session continuity, not a procedural step Claude should issue at the end of every build. Surfacing it as a routine close-out instruction trains both Claude and the user to treat clearing as default behaviour, when in practice it depends on what's coming next (more work in the same area benefits from kept context; a new area benefits from a fresh slate). The fix: strip every `/clear` mention from skill docs entirely. If a context-management rule is worth keeping at all, it belongs in plugin-behaviour.md as one principle, not as repeated close-out boilerplate. Worth a sweep: grep the plugin for `/clear` and audit each hit.

- done.md Plan close-out Step 5 ("Push and context") shouldn't exist. /plan sessions only touch project-level method docs (QUEUE.md, SPEC.md, REGISTRY.md, LOG/) — there's nothing to ship. The Plan close-out should end at Step 4 (Recommend next), with Step 3 (Git commit) as the only git operation. The "Push to remote?" prompt and the "run /clear before the next skill" line both crept in recently as accidental carryovers from Build close-out (where push and the rezip workflow do make sense because the build changed plugin source). The fix: delete Step 5 entirely from the Plan close-out. Plan commits accumulate locally and ride up the next time a real build runs and a Build close-out push happens. Also worth checking: when this crept in (`git log -S "Push and context" -- plugin/si-plugin/docs/done.md`) — the answer points to whether other recent changes to done.md need a sanity pass.

- Procedure docs don't affirmatively define what a batch is. plan.md Ground rules has the negative form ("Never queue thinking work as a batch") but no positive statement that batches are only build work or test work. The Step 3 template implicitly shows Build + Test subheadings as the shape but doesn't say "these are the only two kinds." The gap is what lets framings like "a planning-session batch" read as plausible options. Fix: add an affirmative line to plan.md Ground rules stating batches are build, test, or both — nothing else. Planning, audits, reviews, etc. run inside /plan and never become batches themselves.

- /next has no instruction for what to do when, mid-build, it surfaces that user testing is needed that wasn't planned. Currently the discovery has no defined home — Claude might inline-prompt the user, might queue it ad hoc, might forget. Fix: add a routing rule to next.md — when /next discovers user-runnable testing is needed beyond what the batch's Test section specifies, route it through Captures (so it becomes a test-only batch in a future /plan), don't attempt it inline and don't extend the current batch.

- /plan doesn't explicitly prompt Claude to think about testing when authoring a batch. Test sections currently appear as a structural slot in the template, but there's no instruction telling Claude to actively consider what would need verifying (Claude-runnable vs user-runnable) at the moment of drafting. The omission lets Test sections get skipped silently when they shouldn't, or get authored thinly. Fix: add a step (or sub-step) to plan.md Step 3 — when drafting a batch, explicitly think through what testing the work would need, split Claude-runnable from user-runnable, and either populate the Test section or note explicitly why one isn't needed.

- Design an /audit skill. Audit work (reviewing procedure docs systematically for repetition, prose-where-tag-belongs, drift, etc.) has been stuck in Captures for 6-7 /plan rounds — every /plan session, the new incoming captures crowd out the accumulated audit work, and the dialogic capture-by-capture flow doesn't fit the read-many-propose-many shape audits actually have. A dedicated skill would: take a target on invocation (which docs to audit, against what criteria), read those docs systematically, propose moves/changes one at a time (or grouped) for user approval, and output captures or direct batch entries. Boundary: /plan handles incoming raw captures; /audit is invoked deliberately on a defined audit target. Design needed before building — open questions include exact procedure shape, how the output integrates with QUEUE.md (new captures vs direct batch entries), and how to scope an audit (single-doc, multi-doc, full-sweep).

- The "Captures append to the bottom" rule in plugin-behaviour.md is in tension with the same file's "Claude owns sequencing — ordering, dependencies, what happens first." Append-to-bottom enforces mechanical FIFO and removes any room for Claude to exercise the ordering judgment the other rule says it owns. Same problem applies to batches. Proposed reconciliation, applied to both captures and batches: ordering goes first as directed by Claude, then oldest-first as the fallback. Operationally: before appending, Claude checks whether dependency considerations apply (does this item block or unblock others, should it be processed earlier or later), and places it accordingly; if no ordering rationale applies, it appends. Fix: rewrite the append rule in plugin-behaviour.md (and the parallel framing in plan.md's batch placement instructions) to state both halves — Claude-directed where applicable, oldest-first as default — so the two rules live together instead of contradicting.

- When /plan is processing captures, Claude should pause after each capture is fully disposed (promoted/parked/dropped and removed from Captures) and ask "ready for the next capture?" before opening the next one. Currently Step 2 of plan.md sequences captures back-to-back, which forces the user to interrupt Claude mid-presentation of the next item if they want to wrap the session (e.g. /done). The checkpoint already exists mid-capture (the "anything else?" at the end of the interview) — the gap is between captures. Fix: add a between-captures checkpoint to plan.md Step 2 that mirrors the entry-question pattern from Step 1 ("Anything else, or ready for [next/Captures]?"). Same principle: don't auto-advance past a user-visible decision point.

- Speed up LOG hash backfill further than the already-queued `git log -S` rewrite. Two simplifications: (1) In the common case there's one new entry — one [HASH] in log.md + one in index.md, same hash — so a single `git log -n 1 --pretty=%h -- LOG/log.md` gives the right hash for both without per-placeholder lookups. (2) Detect cleanly with `git grep -l '\[HASH\]' -- LOG/` before reading any files; if nothing matches, the step is a true no-op with zero reads. Also: restrict the scan to `LOG/log.md` and `LOG/index.md` only, or use a stricter pattern (`^## \[HASH\]` / `^- \[HASH\]`) — old archived log files (log-v1.6.1.md, log-v1.6.2.md) contain the literal string `[HASH]` in prose discussing the placeholder mechanism, and the current procedure would false-positive on those. Roll these into the existing "Speed up LOG hash backfill" batch rather than queueing a separate one — same area, cleaner as one change.

- next.md Step 4 ("User raises something out of scope") currently has an "Adding to scope instead" sub-section that lets Claude offer to add a raised item to _build.md as a new entry, framed as a workaround. This is the wrong default — it lets out-of-scope ideas leak into the active build's commit and log entry, polluting what should be one coherent change. Rule should be: anything raised mid-/next that isn't already in the batch goes to Captures, full stop. The only exception is when the raised idea is genuinely part of the same coherent change — fits the why-pipeline naturally, would share the build's log entry and index line, makes the batch easier (not harder) to find later. That exception is narrow and Claude evaluates it against the why-pipeline rules, not against user convenience. Fix: rewrite Step 4 to drop the general "add to scope" offer; replace with the narrower exception keyed to why-pipeline coherence.

### Parked

- Trickle-up audit: review all procedure docs (setup.md, plan.md, next.md, done.md) for rules that are repeated across multiple docs or aren't skill-specific. Move them to plugin-behaviour.md so they're stated once and apply everywhere. Parked pending the /audit skill being designed and built — this item has been stuck in active Captures for 6-7 /plan rounds without progressing, which is what motivated the /audit skill capture.

- Output tag overhaul audit: review all procedure docs (setup.md, plan.md, next.md, done.md) for prose that describes output behaviour where a tag ([SILENT], [BRIEF], [PROMPT], [DISCUSS], [SEQUENCE]) should be used instead. Includes: _build.md entry ticking in next.md should be [SILENT] (crash-recovery bookkeeping, not a status report). Parked pending the /audit skill being designed and built — same reason as the trickle-up audit capture.

- Batch cohesion ordering heuristic — the build log as decision log creates pressure for discrete builds, and batch ceremony is friction for users. Queue ordering rule: builds touching an area with no related batches yet should sink (absent urgency or dependency), giving time for more captures to accumulate and make the batch worthwhile. Batching also touches context window management, not just coherent logging — Claude needs to think about one area of related concerns at a time. Needs design work to sharpen "no friends" and "related" into a mechanical rule. (The companion /next-time check — recommend switching to /plan if related captures exist for the top batch — shipped as the next.md blocker-gate capture scan.)
- Cruise control skill — a skill that runs build→commit→build→commit through a batch (or multiple batches) unattended, stopping only when it hits something requiring user input. Key design concerns: (1) wording that doesn't pressure Claude to push through uncertainty, (2) dependency management when Claude decides when to wrap a batch, (3) /done judgment steps can't get skipped for speed. Parked: depends on stabilizing the skills it would chain.
- Self-hosting support during /setup — if the user says they're rebuilding SI with SI (or building any Claude Code plugin with the plugin), scaffold the self-hosting workflow into their CLAUDE.md: push-and-rezip steps, host/target distinction, pre-push consistency sweep, version bumping. Could be an additional /setup question ("Are you building a Claude Code plugin?" → yes triggers self-hosting scaffolding).
- /done spec check — after writing the **Why:** field, Claude checks whether any reasoning constitutes a product decision that should update SPEC.md. Retrospective check (at decision time) vs the current /plan pipeline gate (prospective, at planning time). Both mechanisms need more real usage before deciding how they relate.
- Threshold-based context management — if hooks ever gain token usage fields (e.g. `context_usage_pct`), the plugin could recommend `/compact` or `/clear` based on actual utilization instead of rule-based triggers. Research filed at resources/research/context-window-hook-access.md. Parked: depends on Anthropic adding token data to hook event input.
- Pre-push sweep could be lighter if /done flagged host-side impacts at build time — accumulate a manifest of "this change affects X in project docs" during /done while context is fresh, then the push sweep checks flagged spots instead of re-reading everything. Sweep stays as safety net but gets cheaper. Design question: cross-build staleness (build 3 invalidates something build 1 touched) still needs the sweep to catch it.
