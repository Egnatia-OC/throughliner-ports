# QUEUE

Two sections. **Processed** — work discussed and agreed, ordered top-to-bottom; /next builds from above the `--- Cleared to run above this line ---` marker. **Unprocessed** — captured, not yet fully processed; the next /plan weighs each item. A work item is a `#### ` heading (its description) with a `[slug]` at the end of that line and its rationale in the block beneath. A leading `[audit]` / `[user]` tag names how it's executed; no tag means a build. A work item carrying a security or privacy risk gets a `Red flag · State: …` marker — the flag rides the work.

**Authoring a rule for the method's own text? Run [`resources/self-authoring-rules.md`](resources/self-authoring-rules.md) first.**

## Processed

#### [user] Restart the app and report what the status-line marker file shows [statusline-restart-test]
Captured by you; filed after `8e65a12` while building [below-line-revisit-nags-by-another-route], which found it buried as a prose lift-condition inside [statusline-context-reader] where nothing had ever acted on it. Processed 2026-08-09 and reduced to the user's part only.
**Why it is yours to run.** The test only means anything observed across a full application restart. Claude cannot quit and relaunch the desktop app, and cannot see what a session on the other side of that restart receives. Everything else — writing the probe, wiring it up, reading the marker afterwards — is Claude's, and is now [statusline-test-script].
**The probe is wired, as of 2026-08-10.** The script is `resources/testing/statusline_probe.py`; it is set as the `statusLine` command in `.claude/settings.local.json`; and the marker file it appends to is **`resources/testing/statusline-marker.log`**. That file currently holds only a header block explaining what counts — anything below that block was written by the app. The script was smoke-tested by hand (fed a payload on stdin, wrote its line correctly), so a silent file after the restart means the app never ran it, not that the script is broken.

**Walkthrough.**
1. Claude confirms the probe is wired and tells you the marker file's path. — done 2026-08-10, path above.
2. You fully quit the desktop app — confirming the process has actually exited, since a normal quit on Windows can leave it running, Task Manager if needed.
3. You relaunch it and open a session.
4. You tell Claude the session is open; Claude reads the marker file and reports what is in it.
**Observable check at close:** the marker file's contents. Claude reads the file rather than taking the outcome on report, per the check-the-world rule.
**Run once on 2026-08-10, and it did NOT resolve — this item stays open.** The user quit and relaunched, opened a fresh session, and the marker file was still empty but for its header. On the three-way reading below that is the third outcome, and it is deliberately **not** being taken: `.claude/settings.local.json` was edited while the app was already running, so the app that quit had never seen the `statusLine` entry and the app that relaunched was the first launch with it on disk. If the desktop app reads settings at launch — the same explanation the 2026-07-31 attempt reached for after an identical empty marker — a silent file here is expected and says nothing about support. "The script is broken" *is* excluded: it was smoke-tested by hand before the marker was cleared. Full reasoning in `LOG/2026-08-10-statusline-restart-test.md`.
**What the next attempt needs, and it is nothing new.** The setting is now on disk and has been through one full launch, so a *second* quit-and-relaunch discriminates: still empty means the timing explanation is spent and the third outcome stands. No new script, no new wiring — one more restart. Do not delete [statusline-context-reader] on the strength of the first run.
**What the outcome decides.** Real percentages → the mechanism works and [statusline-context-reader] is buildable, with an offset threshold designed for the known undercount. `null` values → a payload-timing problem. File never written at all → the desktop app does not support status-line commands and the mechanism is dead in this environment.
**The fallback was rewritten 2026-08-11**, when [session-sizing-and-break-lines] was deleted. There is no manual session-break line to fall back to and there never will be: the readiness line **is** the run bound, set at /plan, and a run that must stop early stops on behaviour rather than on a number.
Cheap to pair with a restart that is happening anyway — any rezip-and-restart is a free moment for it. That convenience is exactly what once kept it written as a rider on someone else's item rather than as work in its own right, so it is noted as convenience only, not as a reason to wait for one.
**Cleared 2026-08-10.** It had been blocked by [statusline-test-script] — the probe had to exist and be wired before a restart could test anything. That shipped, and the wiring details are recorded above, so nothing now holds this.

#### [user] Run one queue-mover command in your own console and report whether the heading echoes back readably [mover-encoding-fix-verification]
Filed 2026-08-11 at processing, split from [mover-console-encoding-mangles-output] so the fix and its observation are separate work. Mixed authorship: you chose the verified route over fixing it and hoping; the decomposition is Claude's.
**Why it is yours to run.** The mangling never reproduced in either shell Claude has — Git Bash and the PowerShell tool both printed em-dashes and `→` correctly — so the only console that shows the failure is the one you are looking at, and Claude cannot see it. The capability check was run rather than assumed: `read_terminal` was named as the tool that might have done this and ruled out, along with the PowerShell tool.
**Walkthrough.**
1. Claude ships the UTF-8 fix and rezips, then tells you it is installed.
2. You run any mover command that echoes an item heading containing an em-dash — the simplest is a no-op reorder Claude will give you at the time.
3. You report what the echo line looks like: the em-dash intact, or a replacement character.
**Observable check at close:** your report of the echoed characters. There is nothing on disk to read, which is precisely why this is a `[user]` line rather than a check Claude runs.
**If it still mangles**, the stream reconfiguration is not reaching the console and the next suspect is the app's own rendering of tool output rather than Python's encoding — do not close it as fixed on the strength of the fix having shipped.
**Cleared 2026-08-11.** It had been blocked by [mover-console-encoding-mangles-output] — there was nothing to observe until the UTF-8 fix shipped. That fix landed in `08c885b`, so the verification is now runnable.

--- Cleared to run above this line ---

#### Give every session its own git branch, merged back at /done, so concurrent sessions stop colliding [concurrent-session-support]
By Claude at the user's request; designed across 2026-07-29, 2026-08-01 and 2026-08-09. The user wants SI safe for running concurrent sessions — a /plan and a build at the same time. The contention is real rather than theoretical: /next's close removes shipped items from Processed and repositions the cleared-to-run marker, so a /next close and a /plan session can both rewrite QUEUE.md's structure at once.
**Direction settled 2026-08-09 — full-project branching, not queue-only.** Two options were weighed. Queue-only branching would give each session its own copy of QUEUE.md merged at close: a much smaller blast radius, and the contention really is about QUEUE.md. It lost because it means writing and then trusting a bespoke merge for one file, and it does nothing about the remote-merge pain, so [worktree-override-hook] would survive it. Full-project branching borrows git's merge instead of hand-rolling one, and it collapses two queue items into one. The user chose this option after both were put side by side. The risk carried forward: branch-switching under a live app, in a folder a cloud client is syncing, is where this could bite a non-coder.
**Mechanics.**
- `session_start` creates the session's branch; naming is automatic (`session/<date>-<id>`) so the user never types or sees a branch name unless something goes wrong.
- Uncommitted hand-edits present at session start come along onto the new branch rather than being stashed. Stashing is where a non-coder loses their own work.
- `/done` merges the branch back into main and deletes it. Claude resolves conflicts silently and surfaces only what it cannot settle safely — the user never sees raw conflict markers.
- On an unresolvable conflict the branch is left in place and the user is told plainly that their work is safe on it.
- A session that ends without /done leaves an unmerged branch. This is recoverable, since the work is committed and the branch is named, but `session_start` needs a check that notices leftover branches and offers to merge them, or they accumulate silently.
- `.git/index.lock` retry-and-backoff: two closes landing at the same instant make git's brief lock error one of them. Minor robustness, but it belongs here.
- Cloud-sync detection: a startup check for whether the project folder is being synced by Drive, OneDrive or Dropbox, surfaced to the user. These clients make their own conflict copies and file locks, which is a separate failure mode from in-repo concurrency and is not fixed by branching.
- The session_start awareness note — telling a session that another is live — stays as previously designed.
**No blocking between sessions, recorded because it was the user's question and the answer is not obvious.** Each session merges its own branch whenever it closes; one session can /done while another is still running, and neither waits on the other. A session only feels the other at all when both edited the same region of the same file, and then it resolves at its own close rather than waiting.
**Rejected designs, kept so they are not re-proposed.** (1) Detect-and-warn — liveness by working-file mtime, a pre-write stop-and-ask recheck — superseded by merge-at-close, which lets sessions diverge instead of policing writes. (2) Staleness detection generally: the user's position is that a session should always be *closed* properly rather than left to go stale, so design for that instead of detecting it after the fact. (3) The old framing that the hard half is outside SI's control because of Drive conflict copies — that is the user's personal environment, not a method-general truth, and most users are not on Drive.
**Relates to** [worktree-override-hook], which names this as its blocker: if merge-at-close makes merging painless then forking is fine, and that item gets deleted rather than built. Decide its fate as part of this build.
**Files (confirm at build):** `hooks/session_start.py` (branch creation, leftover-branch check, cloud-sync detection, the awareness note), `docs-b/done.md` + `docs-b/done-plan.md` + `docs-b/done-build.md` (merge-at-close), `docs-b/plugin-behaviour.md` (safe concurrency), `SPEC.md`, `README.md` feature list, plus a FAQ entry.
**Two findings folded in 2026-08-10, both surfaced while processing [restore-plan-file-gate] and both bearing on whether this design works as written. The user asked the question that found the first; the reasoning is Claude's.**
**Finding 1 — branch-per-session does not isolate concurrent sessions, which is the case it was designed for.** A branch checkout is per-working-tree: one folder has one `HEAD`, so two sessions open on the same project folder are always on the same branch, whatever `session_start` creates. The design therefore isolates *sequential* sessions and does nothing for *concurrent* ones. Real isolation of two live sessions needs separate worktrees — which is [worktree-override-hook], the item this design was going to delete as unnecessary. **Answer this before building.** The design was reached across three sessions and cleared, so the premise failing is worth surfacing at planning time rather than at the first build.
**Finding 2 — `_build.md` is project-level, and that is already wrong today.** The behaviour rules permit a planning session in one chat alongside a build in another. `_build.md` sits in the project root, so the planning session sees it present, concludes it is inside a build, and the scope-lock applies the build's file list to writes that session never agreed to. This predates any branching work and does not wait on it.
**The fix, which this item should carry:** hooks receive `session_id`, so the working file is session-scoped and every check that keys on it asks the question it actually means — is there a build *for this session*. That is a rename with ripple rather than a redesign. Ripple to trace by grep at build: `plugin/si-plugin/docs-b/next.md`, `done.md` and the done family, `plugin/si-plugin/hooks/pre_tool_use.py` (the scope-lock's existence check), and the behaviour rules' one-build-at-a-time and parallel-sessions text. [restore-plan-file-gate] keys its own gate on the same file, so the two must agree on whatever it is called.
**Returned unbuilt 2026-08-10** when the /next run carrying it was stopped after two items.
**Held below the line 2026-08-11, and it must not be built as written.** Research that session (`resources/research/worktree-isolation-and-desktop-sessions.md`) established that session isolation belongs to the harness rather than to us: a worktree exists so edits in one session never touch another's, Claude Code enforces that by blocking edits into the main checkout, and the desktop app documents a worktree per new session. Together with Finding 1 above — one working tree has one `HEAD`, so branch-per-session cannot isolate concurrent sessions — the design's stated purpose is answered by something we do not control. The direction reversed twice inside one discussion, which is itself the reason this is held rather than rewritten in passing.
Blocked by: [isolation-model-is-not-ours-to-choose]
**Re-process this — its blocker has now shipped, and it should lose branch creation entirely. Instruction written 2026-08-11 by that item's build.** Branch-per-session cannot isolate concurrent sessions (one working tree, one `HEAD`), and where isolation does exist the harness already provides it — session_start now detects and reports which model is in force. What survives with independent value is **merge-at-close, and only for the isolated case**. The fate decision is the user's at /plan and is not taken here.
**Finding 2 has already shipped and should be dropped from this item's scope when it is re-processed.** It became [session-scope-the-working-file] and was built on 2026-08-11: working files are now `_build-<session-id>.md` / `_plan-<session-id>.md`, so the defect this item recorded is fixed and is no longer part of what it carries.

#### Backfill the method's own FAQ current with every user-facing change since 2026-06-18 [faq-backfill]
Captured by you — filed after `b6f2344`; processed 2026-08-01. Split from the [faq-staleness-and-sync-failure] capture. `FAQ/faq.md` + `FAQ/index.md` last edited 2026-06-18; a stack of user-facing method changes shipped since (through v1.16.0) with no FAQ entries. Bring both current: audit the LOG/SPEC deltas since 2026-06-18 for user-facing changes (new skills behaviour, working mode, readiness line, seeding, feedback channel, multi-item /next, etc.), draft the missing/updated FAQ entries, and reconcile the index.
**Sequence:** land [faq-sync-hard-gate] first so the backfilled FAQ doesn't immediately re-rot under the same broken mechanism.
**Files:** FAQ/faq.md, FAQ/index.md. Relates to [faq-sync-hard-gate]; note [rename-to-throughliner] will later rename identity strings across these entries (orthogonal to the content backfill).
**Cleared 2026-08-09.** It had been blocked by [faq-sync-hard-gate], because backfilling the FAQ before that gate shipped would refill the doc under the same broken mechanism that let it rot. That gate shipped in `4f5e167`, so the blocker is gone and nothing else in the queue holds this.
**Returned unbuilt 2026-08-10, and its premise needs re-examining before it is built.** A /next run read both FAQ files and confirmed the staleness is worse than the item describes: five of the eighteen entries document retired mechanisms outright (Batches and Captures, `/next freeform`, the Red flags section, the Deferred tests section, the spec-edit batch), and several others carry the retired "batch" vocabulary. The build was stopped before writing, on the user's point that this is the same failure as the merge cycle and the rule corpus — a thing fixed by hand whenever someone notices, with no cycle maintaining it. Their words: it keeps going around in circles, and they have repeated for months that it needs updating cyclically, but a cycle is never applied.
The item's own stated precondition is met on paper and does not address this: [faq-sync-hard-gate] shipped in `4f5e167`, but it fires when a session lands a user-facing change, so it cannot catch drift in an area nobody has touched — which is exactly how the FAQ reached this state. Build this after [rule-lifecycle-system] has been designed, and treat FAQ freshness as one of the things that design must account for, rather than backfilling now and leaving the same gap behind.
**Held below the line 2026-08-11 to match that instruction.** It had been sitting in the cleared region while its own prose said to build it after the lifecycle design — so a /next run could have picked it up and refilled the FAQ under the mechanism that let it rot. The dependency was already written; only the position contradicted it.
Blocked by: [rule-lifecycle-system]

#### /plan reads the five most recent LOG index lines at Step 1, so a session starts knowing what just happened [plan-reads-recent-log-index]
Processed 2026-08-10. The problem is the user's, in their own words: they regularly find themselves asking Claude at the start of a session to look at recent sessions for context, and there is a session-start context hole where mid-session Claude knows what is happening and start-of-session Claude does not. Reading recent history would not carry all necessary context, but it sets upcoming work against past work and makes the session aware of the LOG from the start.
**The build.** At plan.md's Step 1 read-state, read the most recent five lines of `LOG/index.md` alongside QUEUE.md and SPEC.md, and fold anything relevant into the opening narration that already fires. No separate output and no summary of the log for its own sake — it is orientation, not a report.
**Why the index and not the entries.** The index is already built for exactly this: the behaviour rules define an index line as Claude-facing, existing so a session can decide which entry to open without reading prose, and require each line to carry the artifact touched and the nature of the change. Reading the top few costs a fraction of the entries and leaves the full retrieve path untouched.
**Why /plan only, and not every session.** `resources/research/legislative-review-cycles.md` finds that a broadly-scoped obligation is the one that goes unperformed — 7.6% coverage where a duty claimed 344 Acts. A narrow read at one skill's opening is the shape that survives. It also lands in `plan.md`, which is fetched when /plan runs rather than always-loaded, so it spends nothing against the instruction ceiling.
**The cost, and why this waits.** Measured at roughly 2,000–2,500 tokens for five lines today. That is bounded only if index lines are bounded, and they are explicitly uncapped today, so this would be an unbounded read that grows as entries get more verbose. That is why it waits on the proportional cap rather than shipping alongside it.
**Files:** `plugin/si-plugin/docs-b/plan.md` (Step 1 read-state), plus a FAQ entry if the opening narration visibly changes.
Blocked by: [index-line-length-proportional-cap]

#### A status-line-command context-% reader that /next polls at decision points [statusline-context-reader]
Captured by you (2026-07-30). The only surface the harness feeds live context-% to is the status-line command (`context_window.used_percentage` / `remaining_percentage`); hooks receive no context data at all (resources/research/context-window-hook-access.md). So: a small script set as the status-line command writes the current context-% to a file on each status-bar refresh, and /next reads that file at discrete decision points — after each built item — to decide continue-or-wrap. Not a daemon: point-in-time file reads, no continuous polling. This is the *sole available mechanism* for context-% session sizing, precisely because hooks are blind to it — the "automation a hook genuinely cannot do" that the 2026-06-23 hook-vs-CLI research (resources/research/hook-context-injection-vs-companion-cli.md) said would justify a companion tool.
**Scope now: just the context-% reader** (user's call, 2026-07-30). Not framed as broader companion infrastructure yet — if other features later want a companion tool, revisit then.
**Its consumer was deleted 2026-08-11, so this item now stands or falls on its own.** It was built as infrastructure for [session-sizing-and-break-lines], whose premise [arbitrary-run-length-capping] removed: the readiness line is the run bound, /next no longer proposes a subset of it, and run-size judgments are out of the advisory. So a context-% reader no longer has a feature waiting on it. What it could still serve is a **behaviour-based stop** — a run noticing it is degrading rather than counting items — which is the shape that decision explicitly left open. Weigh that at the next processing: if nothing wants the number, the reader is research rather than work, and this item should be deleted alongside its consumer rather than kept out of momentum.
**Research (2026-07-31, resources/research/statusline-context-reader-feasibility.md):** the mechanism is CONFIRMED at the Claude Code level — a custom status-line command exists and receives `context_window.used_percentage` via stdin, and can write a file as a side effect. Two caveats now baked in: **(1) `used_percentage` understates real usage** — it excludes tool definitions, CLAUDE.md, MCP configs, and the system prompt (Anthropic issue #17959), which for SI (large plugin-behaviour.md + CLAUDE.md) means a naive threshold fires far too late; the wrap threshold MUST offset the undercount. **(2) The desktop-app run was NOT observed in-session:** a live test this session (a python status-line script wired into settings.local.json) did not run — the marker file stayed empty — consistent with the desktop app loading settings only at launch. So desktop-app support is still unconfirmed and needs a restart to settle.
Blocked by: [statusline-restart-test]
Recorded 2026-08-09; converted to the lint-checked field form 2026-08-09. That item is the restart-based desktop-app test, and its outcome decides whether this is buildable at all: real percentages → buildable, and the offset threshold gets designed; `null` values → a payload-timing problem; the file never written → the desktop app doesn't support status-line commands, this mechanism is dead in this environment, and this item is deleted.
This blocker used to live as a prose lift-condition inside this item, which is precisely why nothing ever acted on it — the test was a sentence, not work. It was split out as its own `[user]` line on 2026-08-09, and this item is the worked example behind [below-line-means-blocked-by-an-item].
**Files:** a new status-line script in the plugin package; next.md (the read-and-decide logic); SPEC.md if it becomes a user-facing feature; plus a FAQ entry. Relates to [next-context-self-detection].

#### Ship a default-off WorktreeCreate hook that reuses the existing tree, so remote sessions don't fork [worktree-override-hook]
Captured by you; designed 2026-07-29. Starting a new session from remote control forks a fresh git worktree, and merging it back from the desktop is painful — so the user can only start new sessions from the desktop and pre-makes standby sessions (remote sessions disconnect unpredictably). Verified lever (research: resources/research/remote-control-limitations.md): Claude Code's `WorktreeCreate` hook replaces default worktree creation (takes a name, returns a path), so it can point a new session at the existing tree instead of forking.
**Design (decided this /plan):**
- **Ships in the plugin, default-OFF.** The hook reaches every project the user has SI on — a global change — so it never activates unasked. It's inert until enabled.
- **Opt-in offered at /setup.** /setup explains the tradeoff in plain English (reuse the existing tree so remote sessions don't fork, vs. the ordinary per-session worktree) and writes the choice to a setting the hook reads. Enabling later means re-running /setup or asking Claude.
- **When enabled,** the hook returns the existing tree path instead of forking; when off, it defers to default worktree creation.
**Caveats carried:** fixes only the worktree-forking, not the disconnections; needs host-side testing before trusting (returning the existing path *should* work, but git's actual behavior must be verified live). Held below the cleared line with a host-side test gate — don't clear to run until the hook is tested live after reinstall.
Blocked by: [concurrent-session-support]
Recorded 2026-08-01, restated 2026-08-09, converted to the lint-checked field form 2026-08-09. That item's live/canonical git-merge direction may make this hook unnecessary: the hook exists to avoid forking *because* merging hurts, so if merge-at-close makes merging painless, forking is fine and this item gets deleted rather than built. Design the two together; don't build this in isolation.
**Files:** a new `WorktreeCreate` hook in the plugin, session_start/settings plumbing for the opt-in flag, setup.md (the opt-in question), SPEC.md (new hook is a user-facing feature), README feature-list sync, plus a FAQ entry.
**Re-process this — it is now a probable delete. Instruction written 2026-08-11 by the build of [isolation-model-is-not-ours-to-choose], which shipped the detection this item's premise depended on.** That research found Claude Code documents refusing a worktree path that resolves into the main checkout, which is exactly what this hook would return. The fate decision is the user's at /plan and is not taken here; what the build establishes is that the mechanism this item proposes is likely to be refused by the harness, so the item should be re-examined rather than built.

## Unprocessed

#### /plan never created its `_plan.md` across a twenty-item session, and nothing noticed until the close needed it [plan-file-never-created]
Filed 2026-08-11 by Claude at the /done close, from its own failure minutes earlier.
**What happened.** `plan.md` Step 2 opens: "Planning state file: `_plan.md`. Create it when processing begins, to hold the item list, the current item, and the beat reached. Update it at each beat transition, and append each item with its disposition — kept / deleted / skipped-this-session — with slug, one line each." The 2026-08-11 /plan processed roughly twenty items and **never created the file at all.** The omission surfaced only at the close, when `done-plan.md`'s LOG step reached "if a `_plan.md` exists, read its disposition list … so the entry doesn't have to be reconstructed from memory" and there was nothing to read.
**What it cost, concretely.** The close reconstructed the entry's Queue-changes and Work-processed lines from conversation memory — exactly what the file exists to prevent, and exactly what a fresh or compacted session could not have done. Two of the file's other jobs happened not to matter: nothing was skipped, so the skipped-slug record was moot, and the session was never interrupted, so the resume path was never needed. That is luck, not mitigation.
**Why this instance differs from the day's other three, and is more tractable.** [write-first-report-without-write], [invented-rationale-compounds-past-the-shipped-rule] and [subagent-ask-rule-slipped] all require Claude to notice something about its own reasoning — the weakest kind of enforcement, and the subject of [what-would-answer-this-as-a-shared-trigger]. **This one is a plain procedural step with a mechanically checkable outcome: does the file exist?** No judgment, no self-assessment. That it was skipped anyway is worse in one sense — the easy case failed — and better in another, because unlike the others it is enforceable.
**To settle at processing.** Whether a hook can carry it: `post_tool_use` already fires on QUEUE.md edits and could notice a queue being edited with no `_plan.md` present, which is close in shape to the existing lint. Weigh against the admission gate's question 4 — a hook adds standing friction every session forever, and the honest counter is that the file's value is realised mainly at the close and at a resume, so a mid-session nag may cost more than the failure does. Also weigh simply repealing the requirement: if a twenty-item session completed without it and lost only the close's mechanical record, the file may be worth less than its rule.
**Files (rough):** `plugin/si-plugin/docs-b/plan.md` (Step 2's state-file instruction), possibly `plugin/si-plugin/hooks/post_tool_use.py` if the hook route is taken. Relates to [what-would-answer-this-as-a-shared-trigger] and the three shipped-but-not-firing rules named above.

#### Last session advises processing rename-item-needs-reshaping next [forward-advisory]
Filed at the 2026-08-11 plan close. **[rename-to-throughliner] must not be built until it is reshaped**, and that is the one thing a run could get wrong: its migration plan opens with a step already done — the remote is already `FlintcraftTech/throughliner` — and a build-time grep found files its list misses, including a folder rename rather than a text substitution. That is [rename-item-needs-reshaping]. It also depends on an account action nobody has run, [throughliner-repo-rename], which must land first or consumers hold a marketplace registration pointing at a repo describing a different plugin id.
It was moved to the end of the cleared builds so a run reaches everything else before it, but placement is not the safeguard — the condition above is. Everything else cleared is unaffected by anything still unprocessed.
Stated as conditions rather than counts or positions deliberately: the previous advisory said this item "sits ninth", which a single build run made false before anyone read it ([advisory-falsified-by-intervening-run], decided this session).

#### Claude ran the wind-down re-scan three times in one session, twice at moments no document names [wind-down-rescan-run-out-of-procedure]
Captured by you 2026-08-11 at the /done close. Your words: those other wind-down scans were out of procedure — you had not run /done and yet they happened. The account below is Claude's, from re-reading both documents.
**What the documents actually say.** `plan.md` places one wind-down re-scan "after every item is processed and before recommending /done" — a single beat at the end of processing. `done.md` runs one at every close, and states plainly that when /plan already ran its own this session, the /done one is a harmless no-op. So the design is **one substantive re-scan per session**, with a no-op backstop at the close.
**What happened.** Three ran. The first fired after a partial batch with hours of processing still to come; a second fired later, also mid-flow; the third ran at the close, where by then it should have been the no-op. Only the third sat at a moment either document names, and it did not behave as the document describes.
**Why it is worth capturing rather than noting.** It produced real captures, which is exactly what makes it easy to excuse — a step that yields something looks like a step that belonged. But the cadence was invented: no trigger in either doc fires at "we have done a few items", and a session that invents a beat is a session running a procedure nobody can predict or audit. It also spends the user's turns: each re-scan is `[DISCUSS, PROMPT]`, so each one stopped and asked.
**The likely pull, to test rather than assume.** The re-scan is defined by a *position* — after all items — and this session never reached that position, because processing continued in batches at the user's direction rather than running to an empty queue. With the anchor absent, the step attached itself to the nearest thing that felt like a pause. If that reading is right, the defect is that the trigger is positional in a session shape the position never occurs in, which is the same failure recorded the same day in [advisory-falsified-by-intervening-run]: a positional claim in a queue that moves.
**To settle at processing.** Whether the trigger is restated as an event rather than a position — the user signalling the end of processing, or the close being reached — and whether a re-scan run early should be recorded so a later one knows it has already happened, which is `_plan.md`'s job and was itself missing this session ([plan-file-never-created]). Weigh also whether a mid-session re-scan is genuinely harmful or merely unscheduled; if it is harmless, the honest fix may be to permit it explicitly rather than to police it.
**Files (rough):** `plugin/si-plugin/docs-b/plan.md` (the wind-down re-scan step) and `docs-b/done.md` (the file-only version and its no-op clause). Relates to [plan-file-never-created] (the missing record that would have made a repeat visible) and [advisory-falsified-by-intervening-run] (a positional trigger in a moving queue).

#### Delete the shelved Codex port from this repository's history [delete-codex-port-from-history]
Captured by you 2026-08-11, after `git worktree list` surfaced it. Rides this session's commit.
The shelved Codex port is not merely a sibling folder: it is a **git worktree of this repository**, checked out at `../Sovereign Implementer - Codex port` on branch `codex/si-port`. So the port's history is carried inside this repo, which is public.
**Why it is worth doing rather than leaving.** The port has been dormant since 2026-07-28 and `CLAUDE.md` already describes it as read-only history with no live relationship to maintain. Carrying a dead branch in a public repository is cost with no benefit, and it makes `git worktree list` misreport what this project is doing — which is how it was found.
**To settle at processing, and it needs care.** Removing a branch is easy; removing something from *history* is not the same operation, and the two must not be confused. Decide which is actually wanted: dropping the branch ref and the worktree (cheap, reversible, leaves commits unreachable but present until gc), or rewriting history to remove the commits (expensive, rewrites hashes, breaks every existing clone and any reference to those hashes). Note `CLAUDE.md` points at the port's folder as a place to look up what an old Codex-side slug meant, so whatever is removed, that pointer is corrected in the same change.
**Files (rough):** `CLAUDE.md` (the Cross-platform ports section), plus the git operations themselves. Related: the `v17`–`v157` orphan tags are a separate pre-rebuild artefact and are **not** in scope unless deliberately added.

#### Claude spawned a subagent without asking, and it is unknown whether the hook's cost gate fired [subagent-ask-rule-slipped]
Captured by you 2026-08-11 at your instruction, after Claude reported the slip. Your framing: capture it as the rules slipping on researching without a subagent. Rides this session's /done commit.
**The instance.** Mid-/plan, Claude needed to know whether Claude Code exposes an end-of-response hook and spawned the `claude-code-guide` subagent to find out — without asking first. It cost about 28,000 subagent tokens. The answer was good and the finding was filed, which is not the point.
**The rule is shipped, always-loaded and unambiguous.** `skill-nonspecific-rules.md` says to ask before spawning a subagent and to name the cost, because one run can exhaust the user's session usage; and separately, that a plain research request gets inline reading and searching first. Claude read those rules at session start and broke them a few hours later. **This is the third instance today of a correctly-worded always-loaded rule failing to fire**, alongside [write-first-report-without-write] and [invented-rationale-compounds-past-the-shipped-rule] — which is the pattern worth capturing, more than the one lapse.
**The second half, which is a live unknown rather than a diagnosis.** `pre_tool_use.py` carries a subagent cost ask-gate, and `hooks/hooks.json` registers it against the matcher `Task|Agent`, so it should have fired on this call. The user saw no dialog they remarked on, and Claude cannot see from inside whether the ask was raised and auto-accepted or never raised at all. The code's own comment records that this gate was **silently dead once before**, matching only `Task` while current Claude Code names the tool `Agent` — a gate that is registered, firing, and matching nothing looks identical to a gate that works.
**What settling it needs.** An observation, not reasoning: spawn a subagent deliberately and have the user report whether a dialog appears. That is user-observable only, so it decomposes into a `[user]` line if the answer is not obtained more cheaply. Note the same session found [plan-quiet-list-case-mismatch], where a different gate in the same file was inert on Windows for a case-sensitivity reason — so a second silent-gate finding here is not unlikely.
**To settle at processing.** Whether the ask-before-subagent rule needs anything beyond what is written (it does not read as a wording problem), or whether the honest answer is that the hook gate is the enforcement and the wording is only a reminder — in which case the priority is proving the gate alive rather than sharpening prose.
**Files (rough):** `plugin/si-plugin/hooks/pre_tool_use.py` and `hooks/hooks.json` if the gate turns out to be inert; otherwise none. Relates to [write-first-report-without-write] (whose `Stop`-hook design is the sibling mechanism for a claim Claude makes) and [behaviour-rules-read-is-enforceable].

#### The keep-check has no rule for a mixed item — half fully specified, half not designable yet — so a session invents its own framing [keep-check-has-no-mixed-item-rule]
Captured by you 2026-08-11 after catching the mishap that exposed it. Mixed authorship: you identified that the wrong question had been asked and asked whether a rule was at fault; the diagnosis below is Claude's.
**What happened.** Processing [write-first-report-without-write], Claude found one half fully specified (three docs contradicting the write-first rule) and one half not designable (why a shipped, correctly-worded rule fails to fire). Instead of putting the real choice to you, Claude asked whether to split the item or keep it whole — a filing question — and you corrected it: the question was whether to design the mechanism now or defer that half to a later /plan.
**Why this is a gap in the doc rather than only a slip.** `plan.md`'s keep-check has two limbs — name the files that change and what changes inside them — and an item failing either is sharpened or skip-deferred, **never kept**. Its skip-to-defer recommendation likewise addresses an item that will not design out this session. Both are written about a **whole** item. Neither says what to do when one item contains both states, which is common. With no rule to reach for, the session invented a framing, and the invented framing hid the decision that was actually the user's.
**The analogous rule that already exists, and is the model for the fix.** The same document decomposes a mixed Claude-prep-plus-user-action item: Claude-doable parts become build items, the irreducible user action becomes a single `[user]` line, cross-referenced by slug. There is no equivalent for buildable-plus-undesigned.
**What it let through, which is the concrete cost.** The item was kept with its second half failing the second limb, papered over by a close condition requiring the unbuilt half to be re-filed as a capture at close. That is a workaround for a check that should have stopped the keep.
**To settle at processing.** Whether the rule is that a mixed item is always split at the keep-step (buildable half kept, undesigned half returned to Unprocessed with its design progress written in), or whether it may be kept whole with the user deciding first whether to design the remainder in-session — and if the latter, that the decision is put to the user in those words rather than left to the session's framing.
**Files (rough):** `plugin/si-plugin/docs-b/plan.md` (the keep-check's two limbs, the skip-to-defer recommendation, and the decomposition sub-step). Relates to [write-first-report-without-write] (the instance) and [merged-plugin-verification-remainder] (what happens when an unbuilt half rides a closed item's prose).

#### The spec-sync trigger reads as decision-time but SDD atomicity is behaviour-time — say which, because the two disagree [spec-sync-trigger-decision-vs-behaviour]
Filed after `72f4fe9` at the /plan close that hit the ambiguity. plan.md's ground rule says a planning decision that changes what SPEC says — "a new capability, a scope change, a reworded rule" — is edited into SPEC *in that same /plan session*, and the /done spec-sync gate enforces it. Read strictly, that fires on the **decision**.
**Why that reading is wrong, and why it isn't obviously wrong.** SPEC is product truth: what the thing *is*. This session decided to retire working mode, but nothing shipped — the retirement is a cleared work item. Editing SPEC now would make it claim the product has no working mode while the installed plugin still does, which is a false SPEC rather than a synced one. The gate's own stated why is spec-driven development's contract that the spec moves *in the same commit as the behaviour change* — behaviour-time, not decision-time. So the rule's wording and the rule's rationale point at different moments.
**What was done this session, so a later reader isn't guessing.** The gate was judged to pass: SPEC still described the shipped product accurately, and both affected items ([remote-control-opening-mention], [doc-link-rendering-settled]) carry `SPEC.md` in their file lists so SPEC moves when the behaviour does. That is a defensible call, not a settled rule — which is exactly why it needs settling.
**To settle at processing:** whether the trigger is behaviour-time (SPEC moves at the build, and the /plan-close gate only checks that a SPEC-affecting item *lists* SPEC.md), or decision-time with a way to describe decided-but-unshipped truth. The first is simpler and matches SDD; the second is what the current wording implies. Whichever wins, the wording and the rationale must agree afterwards.
**Files (rough):** `docs-b/plan.md` (the ground rule), `docs-b/done.md` (the spec-sync gate), `docs-b/done-plan.md` (the plan-close delta). Relates to [remote-control-opening-mention] and [doc-link-rendering-settled] as the items that triggered it.

#### /next was recommended at a build close when nothing sat above the cleared-to-run line, where /next can only soft-stop [recommend-next-empty-cleared-region]
Captured by you — testing outcome, filed after `fa24343`. At this session's build /done close, the three user-line-cluster fixes shipped and were removed from Processed, leaving the `--- Cleared to run above this line ---` marker at the top with nothing above it. Claude still pointed toward /next as a live next step. That's wrong: with nothing above the marker, /next's pre-flight hits the "nothing cleared" early exit and soft-stops (recommending /plan) — so recommending /next sends the user into a dead end.
**The fix to weigh (for /plan):** done-build.md's Phase 3 "Recommend next" should check whether the cleared region is empty — top of Processed is the marker, or Processed holds only below-line work — and, when it is, recommend /plan to greenlight the next work rather than /next. Today Phase 3's branch 2 ("Processed work exists → name the next item") keys only on Processed being non-empty; it doesn't distinguish work *above* the marker (runnable by /next) from work *below* it (needs /plan to clear). That gap is what let /next be recommended into an empty cleared region.
**Files (rough):** done-build.md (Phase 3 recommend-next), possibly the shared recommend logic in done.md. Relates to [merged-plugin-live-verification].

#### The `-testN` plugin.json bump is surfaced as an out-of-scope dirty file on every close between a rezip and the next push — give it a recognized-signature skip like the hash backfill [test-suffix-plugin-json-close-noise]
Captured by you — filed after `fa24343`. Every rezip bumps plugin.json's `-testN` test suffix (CLAUDE.md Rezip ritual) as a working-tree-only change never committed until the next real Push (which drops the suffix and commits a clean version). So between a rezip and the next push, plugin.json sits dirty, and the commit core's out-of-scope-dirty-path check surfaces it on *every* close — predictable noise. The method already special-cases exactly this shape for the hash backfill ("recognise the hash-backfill signature first, and skip the investigation for it").
**The fix to weigh:** add a sibling recognized-signature carve-out — a plugin.json diff whose only change is the `-testN` test suffix is the known rezip artifact; fold or ignore it silently, at most a one-line note, never a per-close question.
**Files (rough):** done.md (Commit core out-of-scope-dirty-path detection — add the test-suffix signature beside the hash-backfill one). Host-only (the `-testN` scheme is self-hosting; consumers don't rezip).

#### Setup should settle repo privacy & publishing posture: gitignore option, licensing question, optional public-repo setup [setup-repo-privacy-and-publishing]
Captured by you — raised mid-/done, filed after `9cd1e51`. Throughliner content (QUEUE/LOG/SPEC) is sensitive and a user's repo may be public, so /setup should help settle the privacy/publishing posture up front rather than leaving it implicit.
**Components to design (user, 2026-08-01):**
- **Gitignore-all-TL option** — offer to gitignore all Throughliner content so it never enters the repo at all; the maximally-private posture, for users who don't want planning/logs tracked or published.
- **Licensing question** — ask about the project's license as part of setup.
- **Optional public-repo setup, non-pressuring** — framed off the license choice: "since you chose this license, do you want this on your GitHub? We can do it now, or I can capture it for a later /plan." Offer, never push; the "capture for later" branch is the graceful decline.
- **Open-source → open-logging is an unsettled opinion, NOT a decided default (user's own caveat):** the user leans toward open source implying open logging but isn't sure it's substantiated enough in the real world to *suggest* in setup. Weigh whether setup surfaces this at all, and if so as a neutral option — don't bake the opinion into a recommendation without grounding.
**Depends on [scrub-gate-before-approval]:** a public-repo offer is only safe once the scrub gate guarantees artifacts are clean — don't encourage publishing before the leak risk is closed.
**Files (rough):** setup.md (new questions), plugin-behaviour.md (privacy posture), SPEC/FAQ, `.gitignore` handling. Relates to [scrub-gate-before-approval].

#### [user] Correct and post the announcement about rationale moving out of operative rules — the draft tells readers the why lives in session logs they can't reach [announcement-rationale-split-correction]
Captured by you, 2026-08-09 — split out of [rationale-relocation-invisible-to-consumers] when that capture was deleted as otherwise satisfied. The Discord draft written on 2026-08-09 says the reasoning "lives in the session logs where you can go and read it." That is false for every reader who is not developing the method: the plugin package ships neither `LOG/` nor `resources/`. Posting it as written would tell users to go and read something they have no access to.
**Why this is the user's line:** Claude can draft the corrected wording, but only the user can post to Discord. Under the capability test, drafting is Claude-work and posting is not — so the line covers the posting, with the draft prepared for it.
**Walkthrough:**
1. Claude drafts the corrected announcement, replacing the session-logs claim with the actual split: why the method behaves a certain way is in the FAQ that ships with the plugin; why a rule is worded as it is stays in the development log.
2. The user reads it and says what to change.
3. The user posts it to Discord.
4. The user says it's posted, and it gets recorded and the line removed.
Rough draft, to sharpen at step 1 rather than now — the announcement's other content wasn't reviewed this session, so check the whole thing rather than only the one false sentence. Also worth deciding at step 1 whether this is still worth announcing at all, given the week that has passed.
**Files:** none — the artifact is a Discord post. Relates to [self-authoring-rules].

#### Articulate the throughline's full value for SPEC positioning and help docs — it's undersold as "you can ask why" [throughline-value-articulation]
Captured by you — from drafting the Discord throughline help article, filed after `9cd1e51`. The current help framing undersells the throughline: "sometimes you can ask why something is the case" reads as a minor lookup feature, not worth the perceived cost of approvals and log-writing. The real value needs stating plainly, and feeds SPEC/README positioning (fold in when [rename-to-throughliner] rewrites positioning) and the help/FAQ docs.
**The headline to lead with:** the throughline's value isn't occasional why-lookup — it's that *the user's intent keeps running the project even after Claude has forgotten everything.* Claude's memory resets each session; the throughline is why a fresh, short, even cheaper-model session still builds the project the way the user meant, instead of re-deriving intent from code and guessing wrong. That is the product, not a side feature.
**Value points to draw from:**
- *For Claude:* it's how Claude stays aligned to intent (not just "what" but "why you want it that way"); rationale riding a rule is *why the rule is followed reliably* (bare rules slip) — so it steadies Claude's own behaviour, not just answers user questions; it lets Claude catch drift, because recorded intent makes contradictions visible.
- *The "branches" framing (user's):* the throughline isn't one file — intent lives in SPEC, rationale rides every QUEUE item, history lives in LOG. SPEC and QUEUE are read *during* planning and building, so the throughline shapes every plan and build silently, not only on a "why?" query; the LOG is the deep archive pulled on demand.
- *For the user:* stops re-deciding settled things (records why a rejected option lost, so it isn't re-proposed); prevents silent regressions (see why something is that way before changing it); institutional memory for coming back after a break or handing the project on; keeps a growing project coherent instead of rotting; confidence that the project still reflects intent rather than hoping Claude "remembers."
- *Reframing the perceived overhead (put this in the article):* the approving is what makes the record genuinely *yours* and trustworthy — a reason you never vouched for is one you can't trust on read-back; the log-writing pays a small cost now against the big "why did we do this?" cost later; and externalised intent is exactly what lets short, cheap sessions work at all.
**The work-flow-vs-throughline distinction (for the help docs):** the current article describes the *flow of work* (capture → processed → built), which is the work pipeline, not the throughline. The throughline is the *why that travels alongside* that flow and is preserved. Help docs land harder separating the two: "here's how work moves" *and then* "here's the throughline — the reasoning that rides with it and survives Claude forgetting." Uses the settled naming clarity (throughline = reasoning spine across SPEC/QUEUE/LOG; the LOG records it, isn't it).
**Files (rough):** SPEC.md + README positioning (fold in at [rename-to-throughliner]), FAQ/help entries. Relates to [rename-to-throughliner]. The nudging design this once expected from [throughline-feature-elevation] is not coming — that item was retired on 2026-08-09 and replaced by [rationale-misattribution], so the value articulation stands on the throughline as it already works rather than on a planned feature.

#### Design a /plan→/next ordering handoff: carry the order's rationale forward so /next explains (not infers) it, with a predictable, not-over-narrated shape and optional user reorder-input before the run [next-start-order-handoff]
Captured by you — filed after `bebffe7`.
Today /plan reorders both queue sections and /next delivers the cleared region in that order. Two gaps: /next can't *explain* why the order is what it is (it would have to infer the rationale), and an easy-going user is fine letting it run but some users will want a say in the order at the **start of /next**, before anything runs.
**The idea:** have /plan file a forward handoff (advisory-style) every time that carries the **ordering logic** — why this before that — so at /next's start Claude can state the order *and its reasoning* without inferring, and offer the user a chance to adjust before the run.
**Design goals to settle when processed:**
- **Predictable shape.** The start-of-/next "run roundup + rationale recall" should have a fixed, recognisable form, not free-form narration that varies each time.
- **Not TOO narrated.** Explaining the order pulls against the anti-overwhelm / consolidate-narration rules — the design must balance "explain enough that a user could object" against "don't bury the Ready ask in a wall of rationale." Likely: terse order + one-line why per non-obvious placement, detail on request.
- **Optional reorder input.** Preserve the easy-going path (confirm and run) while giving users who want it a clean way to change the order at /next start — without turning it into a mandatory gate.
**Don't conflate two lifecycles.** The existing forward-recommendation advisory is a **/plan→/plan** orientation handoff, consumed and cleared at the next *planning* session. This is a distinct **/plan→/next** handoff of *ordering rationale*, read at build start — settle whether it's a new artifact or an extension of the advisory, and its own clear/lifecycle.
**Builds on:** next.md Step 1.3 (the existing "present the run — Ready?" step that already lets a user redirect to /plan). **Relates to:** [reorder-cost-vs-dependency-model] (the per-session cost of reorder reasoning — this adds a consumer of that reasoning) and [user-work-surfacing-fix] (both touch what /next surfaces to the user at run start).
**Skipped this session (2026-07-31)** — its surfaces (next.md Step 1 run-presentation, the reorder) are being reshaped by [user-work-surfacing-fix] and [reorder-cost-vs-dependency-model]; design it fresh once those land.
**Blockers cleared + author-once note (2026-08-01):** its two reshaping dependencies ([user-work-surfacing-fix], [reorder-cost-vs-dependency-model]) have both shipped, so this is now ready to design. But its home doc (next.md Step 1) is about to be reworked wholesale in the docset-B authoring ([fable-docset-model-detection] phase 2). Design and author this *as part of that B work* so the /next-start handoff lands once in docset B, rather than authored into the about-to-freeze docset A and redone. The design (handoff shape, lifecycle, optional-reorder mechanism) is durable and can be settled independently of which docset carries it.
**Files (rough):** plan.md (file the ordering handoff at close), done-plan.md (where the reorder + advisory are written), next.md Step 1 (read + present + optional reorder), plugin-behaviour.md (the advisory lifecycle + the consolidate-narration rule this must respect), FAQ.

#### Name the post-/done tail as an explicit state: route it cleanly and advise that scope-lock is off — without adding a re-/done prerequisite [post-close-tail-state]
Captured by you — filed after `fa2f8e5`. Observed live 2026-08-01: after a /done committed (`ea272f6`), the session ran a long tail — six captures filed to QUEUE.md, all uncommitted — and the user briefly lost track of being post-close. The method neither names nor handles the post-close tail. (This capture is itself a post-close capture — the same state, live.)
**Durability is already handled — this is NOT about forcing another /done (user, 2026-08-01).** Post-close captures are written to QUEUE.md, so they are on disk and not lost, and the existing mechanism already picks them up: a capture filed after the commit rides the next session's commit and converges into that session's LOG (plugin-behaviour.md captures-filed-after-commit; done.md). The design intent is to let the post-close tail happen *gracefully*, leaning on that pickup — never to add a re-close requirement. At most a *light* awareness cue ("these are filed and will be saved by your next session"), which must not read as "you must /done again."
**The two real gaps:**
- **Routing clarity.** What a post-close captures-only tail "counts as" is unclear — done.md routes it to done-plan.md as "a session that changed only method docs," but that's inferred, not named. Name the post-close tail explicitly in done.md's routing so a session doesn't have to reason it out.
- **Scope-lock is off (the safety point).** `pre_tool_use.py` enforces file-scope only while `_build.md` exists; once /done deletes it, the file scope-lock is fully off — any file is editable (git-safety and the subagent-ask still fire; only file containment drops). The user runs the whole post-close tail with no scope containment and isn't told.
**Open design questions (for /plan):** whether scope-lock-off is a bug to fix (re-enable some lighter post-close protection) or just to advise plainly; and how light the awareness cue should be so it never becomes a de-facto re-close prerequisite.
**Files (rough):** plugin-behaviour.md (a post-close-tail rule — the scope-lock-off advisory + at most a light awareness cue), done.md (name the post-close tail in routing), possibly pre_tool_use.py / session_start.py if post-close protection is added, SPEC/FAQ if user-visible. Relates to the captures-filed-after-commit mechanism.

#### Add a lightweight post-close "done delta" — commit everything since the last /done without a full-session rescan [done-delta-close]
Captured by you — filed after `fa2f8e5`. /done is expensive: the full close re-scans the whole session's discussion (wind-down re-scan), runs the spec-sync gate, reorder, marker positioning, advisory management, LOG authoring, and staleness sweep. When a /done has already committed this session and the tail only filed a few captures, the full ritual is overkill — most steps no-op, but Claude still pays the reading/reasoning cost to *reach* each no-op. (Seen live 2026-08-01: a second /done banked six post-close captures; nearly every step was a silent no-op, yet the full path was still walked.)
**The idea:** a delta close that handles only *everything since the last /done commit* — stage the new changes, write a light delta LOG entry, commit — while **bounding the rescan to since-the-last-close** instead of the whole session, and skipping reorder/marker/advisory steps when nothing relevant changed.
**This coexists with, doesn't replace, the free path:** post-close captures already ride the next session's commit ([post-close-tail-state]), so doing nothing is still valid. The delta close is the explicit "bank it now, cheaply" option for when the user wants it.
**Design questions (for /plan):**
- **Trigger:** separate mode/command, or auto-detected? Auto-detect is cleaner — /done notices a commit already happened this session and switches to the delta path — but "already committed this session" needs a reliable signal (a session marker, or git log since session start).
- **Keep vs skip:** commit + light LOG entry kept; whole-session wind-down shrinks to since-last-commit; reorder/marker/advisory fire only if the delta touched them. Preserve the full close's safety (nothing unrouted) without re-paying its cost.
- **LOG shape:** a fresh delta entry (as this session did with render-verify-and-captures.md) vs appending to the prior entry.
**Relates to:** [post-close-tail-state], the done-family dedup audit findings (done.md is already heavy — a delta path should reuse the shared core, not duplicate it), and captures-filed-after-commit.
**Files (rough):** done.md (the delta-close path + its trigger), possibly session_start.py if a session-commit marker is needed, SPEC/FAQ if user-visible.

#### Claude spontaneously driving testing/verification in a loose tail is a signal to route into a skill, not run it informally [drive-testing-signals-skill-routing]
Captured by you — filed after `fa2f8e5`. Observed 2026-08-01: this session's whole post-close tail was triggered by Claude initiating the render-verification — "having the user run testing" — informally in conversation rather than through a skill. The verification was already a `[user]` line ([working-mode-render-verification]) in Processed, but Claude drove it ad-hoc, and its findings surfaced as loose captures instead of through a structured flow.
**The insight (user):** when Claude finds itself wanting to run or drive testing/verification — especially `[user]` work — that urge is a signal to stop and route it into the proper skill (asking for a quick /plan to write/structure the lines, or /next to actually hand over an existing `[user]` line), rather than running it informally in a post-close or inter-skill tail.
**Why it matters:** driving structured `[user]` work in loose conversation is the same failure family as under-filing `[user]` lines ([user-line-underfiling-asymmetry]) — user work handled informally instead of as tracked, structured lines. It also blurs the plan/execute boundary from the *other* side: not "planning during execution," but "executing structured work with no skill around it."
**Open design question (for /plan):** the exact trigger and destination — when does "I want to verify X" convert into "route this into a skill," and which skill? The verification items were already `[user]` lines, so part of the answer may simply be "run `[user]` verification through /next, not ad-hoc." Settle the routing at design.
**Files (rough):** plugin-behaviour.md (a rule on routing spontaneous testing/verification into a skill rather than a loose tail), possibly next.md / plan.md. Relates to [user-line-underfiling-asymmetry] and [post-close-tail-state].

#### Should SI auto-detect "it's pointed at itself" and engage a self-hosting mode? [self-hosting-auto-detection]
Captured by you; split from [self-hosting-reproducibility] on 2026-07-29 when its concrete portability half became [self-hosting-path-portability]. Open, undesigned question: today self-hosting is *ambient* — CLAUDE.md auto-loads so a cloner gets the rules, but nothing *detects* "this is SI developing SI" as a distinct mode. Should there be a dedicated detection signal (e.g. a hook that sees the plugin package living in the repo it's opened in → self-hosting), or is ambient CLAUDE.md enough?
**Gating premise dropped (user, 2026-08-01):** no longer deferred until "a genuine second developer needs it" — that condition is unpredictable, so gating on it means the item is never ready until it's already too late. Worth designing on its own merits.
**The open crux — trigger has no payload yet.** The item names the trigger (a hook noticing the plugin package lives in the repo it's opened in) but not what detection would *do* that ambient CLAUDE.md can't already. Candidate payloads to weigh at design: a session_start self-hosting banner (host/target distinction is live; rezip to test changes); suppressing/altering consumer-only machinery that misfires in the dev project; actively guarding host/target confusion. Detection only earns its keep if it switches on a concrete behavior ambient loading can't.
**One concrete payload the user named (2026-08-01):** self-hosting awareness should at minimum carry the understanding that **the consumer-facing FAQ (the shipped template) and the project's own FAQ are one and the same in self-hosting** — they are not two separate artifacts here. Relates to the FAQ-freshness thread and [self-hosting-path-portability], [migrate-skill].
**Seeding is already solved — it's not what detection is for (clarified 2026-08-01).** How the host Claude gets self-hosting instructions was murky; resolved into **two distinct seeding paths that must not be conflated.** (1) A **self-hoster** developing the method gets everything by **cloning/forking the repo** — CLAUDE.md is committed in the SI repo (it's what loads in a dev session), so they inherit SPEC/QUEUE/LOG/plugin/CLAUDE.md and therefore **do not run /setup** (nothing to scaffold). (2) A **consumer** building their own app installs the plugin and **runs /setup**, which scaffolds a fresh CLAUDE.md from CLAUDE-TEMPLATE.md carrying **none** of the self-hosting rules. So repo-clone seeds self-hosting; /setup seeds a consumer — two doors. This is *why* "host-only by residence in CLAUDE.md" works for host-only rules like [faq-sync-hard-gate]: the rule lives in the repo's CLAUDE.md, present only in clones-for-development, absent from a consumer's scaffold, so it sleeps for consumers with no detection needed. **Consequence for this item:** detection's value is NOT seeding (the clone handles that) — it's only switching on behaviors CLAUDE.md prose can't express (the open payload question above).
**Anti-nag design — a recorded acknowledgement sign (user, 2026-08-01):** if detection is built, it must not nag. Follow the one-time-acknowledgement pattern the method already uses (working-mode, editor fields): on the trigger firing (the plugin package living in the repo it's opened in), session_start checks for a **recorded self-hosting marker**. If absent, ask **once** — "this looks like you're developing the method itself; is that right?" — and record the answer: **confirmed** (they forked; self-hosting behaviors on) or **declined** ("I'd rather do it my own way"; record and never ask again). If the marker is already present, act silently on its value. The recorded sign is what turns a would-be per-session nag into a single ask, and it also cleanly covers the "detected the shape but the user doesn't want self-hosting mode" case.

#### Keep the migration recipe current via a maintenance pass as the method's format evolves [migration-recipe-freshness]
Captured by you — filed after `f593cd1`. The migration checklist (the recipe for converting an old-format project to the current two-section queue) will drift as the method's queue/doc format keeps changing — a stale recipe is worse than none. So it needs a maintenance pass that keeps it current.
**Trigger to settle at processing:** you suggested doing it "on rezip." Worth weighing against a tighter alternative — rezip is a frequent local test-build, so a pass on *every* rezip risks becoming noise, whereas a **format-change trigger** (whenever a build changes the queue/doc format, refresh the recipe in the same session) targets exactly the moment drift is introduced — the same family as the existing README-sync and SPEC-sync triggers. Decide between them (or a hybrid) when processed.
**Gated on [migration-checklist] existing first** — the recipe this keeps fresh isn't built yet (it sits below the cleared-to-run line). So this can't be built until that ships.
**Relates to** [migration-checklist] (the recipe it maintains). **Option to weigh when processed (rehomed from the deleted [migrate-skill], 2026-08-01):** a **migration-notes doc** — a version-keyed running record of what structurally changed between which format versions, refreshed whenever a build changes the format. Same refresh trigger as [setup-as-migration-home]'s format-epoch bump discipline, so design the two together; decide whether a full changelog earns its keep or the recipe-refresh alone suffices.

#### The harness "surface already-produced text" primitive + a method ordering-flip could collapse approval-time token doubling — reshaping the working-mode machinery [approval-flow-token-doubling-simplification]
Captured by you — filed after `ea272f6`. Insight reached 2026-08-01 while reviewing the user's Claude Code feature request (anthropics/claude-code#77134). **The root cost:** showing text in chat and writing text to a file are *both* the model producing those tokens (a Write/Edit tool call carries the full text as model-generated input). So any text that both appears in chat and lands in a file is produced twice. Approval-heavy workflows (capture drafts, LOG entries, commit messages) hit this constantly.
**Two directions, same waste:** (A) write-to-file first, then re-emit to show for approval — what issue #77134 asks the harness to fix (surface a Write/Edit's content verbatim, no second model pass). (B) the method's *actual* dominant flow — author the draft in chat first (the approval), then write after the user approves — where the doubling is the *Write* re-emitting the just-shown text.
**The ordering half is decided and gone from here (2026-08-09).** [write-first-approval-flow] settles the flip to write-first-then-report, ungated by the harness, with one test deciding which moments flip: does a revert fully undo it. What remains in this item is only the harness-dependent half — how a report renders once a Write's content can be surfaced without a second model pass — plus the convergence note below. Do not re-decide the ordering here.
**The resolution (design at /plan, don't act yet):** don't ask the harness for a second (Direction-B) primitive — it's the murkier one (writing a prior chat message to disk). Instead, once the Direction-A primitive exists, **flip the method's ordering to write-first-then-surface-for-approval**, turning every approval moment into Direction A. Trade-off to weigh: write-first means the file briefly holds unapproved content, so it leans on revert-on-reject — which is why the method currently shows-first (the "never write to QUEUE.md without showing first" rule). Whether that safety is worth trading for the token saving is the call.
**Convergence:** this and issue #77134 circle the same area — the view-in-doc machinery may simplify further once the harness can surface already-produced text. Note that much of the convergence has already happened: [doc-link-rendering-settled] and [remote-control-opening-mention] shipped 2026-08-09 and between them retired the working mode field, the Editor field and the line-anchored-link promise, so what remains to converge is smaller than this note originally described.
**Files (rough):** plugin-behaviour.md (the View-in-doc rendering section + the write-first report rules across plan.md/next-build.md/done.md), gated on issue #77134 landing. External dependency: anthropics/claude-code#77134.

#### Routing communication feedback to memory masks the method defect that produced it — the memory-boundaries rule needs the exception [memory-masks-method-defects]
Captured by you — raised 2026-08-09 at a /done close, filed after `115f851`. The user's words: if Claude's replies are affected by memory, then the method can't truly be tested.
**The conflict, which the shipped rule does not see.** plugin-behaviour.md's memory-boundaries rule lists what memory is free for: user preferences, working style, **communication feedback**, cross-project facts. Separately, this project's CLAUDE.md states that all use of the plugin to develop the plugin is testing the plugin, and that any moment session memory covers for something the docs should carry is itself a mandatory capture. Nobody applied that second rule to *persistent* memory, and the two now disagree: saving communication feedback to memory is explicitly permitted, and doing it here contaminates the only test the method gets.
**Why it is sharper than a self-hosting quirk.** Communication feedback is very often *evidence about the method's own narration rules*. This session is the demonstration: the user twice said there was too much text, which is evidence that the /plan checkpoint is too long — filed as [done-invoked-when-user-meant-continue]. Had the same feedback also gone to memory, Claude would simply have behaved better from then on, the queue item would have stopped mattering, and the defect would have survived in the shipped docs with nothing to surface it. Memory would have quietly bought a fix for one user while every consumer kept the bug.
**So the routing test is not "is this a preference?" but "is this evidence about the method?"** A preference that no method rule governs — a name, a timezone, a tool the user likes — is memory's, unchanged. Feedback that a *method-produced behaviour* was wrong is a testing outcome and belongs in the queue, whether or not it also reads as a preference. The overlap is the whole problem: the feedback is genuinely both, and the current rule names only the branch that silences it.
**To settle at processing:** the exact wording of the exception in the memory-boundaries rule; whether it is general or scoped to projects that are testing the method; and whether the shipped rule should say plainly that feedback about Claude's own narration is a capture first and a preference second. Weigh against the admission gate — this may be an amendment to the existing memory-boundaries rule rather than a new rule, which is the cheaper shape.
**Files (rough):** `plugin/si-plugin/docs-b/plugin-behaviour.md` (Routing and discipline, Memory boundaries), possibly `CLAUDE.md` if the self-hosting half needs stating separately. Relates to [done-invoked-when-user-meant-continue] (the live instance) and [self-hosting-auto-detection] (which asks what self-hosting detection would switch on — this is a candidate payload).

#### Three layers assert the method's communication rules, so self-hosting cannot test them — global CLAUDE.md and the output style mask whatever plugin-behaviour.md says [communication-rules-untestable-here]
Captured by you — raised 2026-08-09 at a /done close, immediately after [memory-masks-method-defects], when the user asked whether anything else lingering might be hiding the real face of the plugin and the model. Project memory turned out to be genuinely empty — the directory has never existed — but the search for it surfaced something larger.
**The finding.** The user's global `~/.claude/CLAUDE.md` loads in every session on this machine, this project included, and independently instructs: one thing at a time with the count stated first and no previewing later items; lead with the decision and skip recaps; one step per message for walkthroughs; offer a web search rather than guessing at an external fact; and explain in plain English to a non-coder. Every one of those has a twin inside `docs-b/plugin-behaviour.md`. The active output style asserts a third copy of the same set.
**Why that is a testing problem and not a redundancy problem.** This project is the method's only real test bed, and the rule that all use of the plugin to develop the plugin is testing the plugin depends on the plugin being the thing producing the behaviour. Where three layers assert the same rule, no session can tell which one it followed, and a rule that is weak, badly worded, or missing from `plugin-behaviour.md` still produces correct behaviour here — supplied by a layer consumers do not have. The defect ships and this project never sees it. So self-hosting tests the method's *mechanics* well (hooks, queue structure, the skills, the mover, the lint) and its *communication rules* barely at all, which is precisely the half that has been rewritten most often.
**The same shape as its sibling capture.** [memory-masks-method-defects] found that routing communication feedback to memory hides the method defect that produced it. This is that failure one layer up and already in force: not a future risk from something Claude might save, but a standing condition that has applied to every session this project has ever run.
**To settle at processing, and it is genuinely open.** The obvious move — strip the duplicated rules out of the global file — is not obviously right: those instructions serve the user across every other project, and removing them to improve one project's test fidelity trades real everyday benefit for a diagnostic. Options to weigh: leave it and simply record the limitation honestly, so nobody mistakes this project's good behaviour for evidence the rules work; test the communication rules somewhere else, in a consumer project without the global overrides, which is what an E2E project is for; or make the overlap visible rather than removing it — enumerate which rules are doubled, so a session reasoning about narration knows which of its behaviour is unattributable. The third is the cheapest and loses nothing.
**Don't let this become an argument for weakening the global file.** The user is a non-coder and those instructions exist because they help; the problem is what they conceal about the method, not that they are wrong to have.
**Files (rough):** likely none in the plugin — the honest output may be a note in this project's `CLAUDE.md` recording the limitation, plus an entry in `resources/method-compliance-audit-checklist.md` so a compliance audit knows not to treat observed narration here as evidence. Relates to [memory-masks-method-defects], [self-hosting-auto-detection], and [self-authoring-rules-compliance-audit] (whose corpus is exactly the doubled text).

#### An outbound INBOX message is fire-and-forget, with no way to know it was ever seen [inbox-delivery-unconfirmed]
Filed 2026-08-10 at the /done close, from delivering a real message. The Understudy project had no `INBOX/` folder at all, so ours created it. If their installed method predates INBOX scaffolding, nothing in their session start surfaces waiting mail, and the message sits unread indefinitely with nothing on this side ever knowing.
**Why it is worth deciding rather than leaving implicit.** The INBOX design says a project reads only its own mailbox and never goes looking through others, which is a sound privacy boundary and is probably why no acknowledgement exists. But the consequence has not been written down: a sender has no way to distinguish "read and acted on" from "never seen", and the more useful the message the worse that is. This session's message asks a question whose answer would change how the method renders links — a question that may simply never arrive.
**Options, none obviously right.** Accept fire-and-forget and say so plainly in the docs and FAQ, so a user knows what sending does and does not guarantee. Or have the recipient's archive step write a receipt back into the sender's INBOX, which is symmetric with the existing move and needs no new mechanism, but doubles message traffic and means a project writes into another unprompted. Or have the sender check for the recipient's `INBOX/` before writing and warn when it has to create one, which is cheap and catches exactly this case without any acknowledgement protocol.
**A completed round trip, 2026-08-10, which bounds the worry without removing it.** The message this item was filed about was read and answered: Understudy replied on the link-rendering question, confirmed they read our v2 marker format, and corrected the date error at its source. So delivery worked, and the answer that "may simply never arrive" did arrive. What the round trip did *not* provide is any acknowledgement mechanism — it worked because the other project happened to read its mailbox and chose to reply, which is the same fire-and-forget property described above, with a good outcome rather than a guarantee. Note also what happened on this side the same day: the arrival was not surfaced at session start and was found only because the user said to look. That was investigated on 2026-08-11 and the hook's scan turned out to be correct — the messages simply arrived *after* that session had opened, and session start is its only firing moment. The receiving half is therefore not broken, but it is bounded in a way the design never stated: mail arriving mid-session waits for the next one. Carried by [inbox-mail-has-no-place-in-the-ladder].
**Files (rough):** `plugin/si-plugin/docs-b/plugin-behaviour.md` (Cross-project INBOX), `SPEC.md` if the guarantee is stated to users, plus a FAQ entry.

#### A close swept an unattributed hand edit into its commit, and the out-of-scope dirty-path check never fired [close-sweeps-unattributed-hand-edit]
Filed 2026-08-10 in the post-close tail, from a live instance minutes earlier. The user edited QUEUE.md by hand during the close — cutting the rationale out of a header line Claude had written — and saved it moments before Claude staged and committed. The edit went into `a556e5c` with no mention, no attribution, and no separate record. Nothing was lost, and that is not the point.
**Why the existing guard missed it.** done.md's commit core requires detecting out-of-scope dirty paths and surfacing any change no build staged. It compares `git status --porcelain` against the active build's file list — but a planning close has no build file list, and QUEUE.md is the file a planning session edits by design. So the user's edit was invisible: it arrived inside a file Claude already considered its own, and the check has no way to distinguish a line Claude wrote from a line the user rewrote in the same file.
**The confusion it caused, which is the real cost.** Immediately afterwards the user asked Claude to look at their change, and Claude reported it did not exist — `git status` was clean, correctly, because the edit was already committed. A clean tree cannot distinguish "no edit was made" from "the edit is already committed". The user was close to re-doing work that already existed, and separately their companion app was reporting a stale last-updated time, so two independent signals agreed on the wrong answer.
**To settle at processing.** Whether a close should diff its own writes against the file's state at staging time, so a hand edit inside a Claude-owned file is detectable at all; or whether the honest answer is cheaper — a close that stages a method doc says in one line that it is doing so, which is enough to make a swept edit visible without any new mechanism. Weigh against the admission count; this may be an amendment to the existing dirty-path check rather than a new rule.
**Files (rough):** `plugin/si-plugin/docs-b/done.md` (Commit core, out-of-scope dirty-path detection). Relates to [post-close-tail-state] and [concurrent-capture-during-next-costs-interruptions] — all three concern work arriving at a moment the close does not expect it.

#### The behaviour-rules read can be hook-enforced after all, and the reason it wasn't is documented wrongly [behaviour-rules-read-is-enforceable]
Filed 2026-08-10 during /plan; rides this session's /done commit. Mixed authorship: the user refused Claude's assertion that a forced read was not possible and asked for it to be checked; the research and the findings below are Claude's, from the Claude Code hooks reference. Full evidence, including the verbatim JSON shapes a build would need, is at `resources/research/hook-enforced-doc-reading.md`.
**Finding 1 — the shipped design's stated premise is wrong.** `session_start.py:360-384` explains that the rules are pointed at rather than pasted because the file is tens of kilobytes and "blew the cap by a wide margin". The 10,000-character cap is documented as **per hook command, not aggregate**, so several `SessionStart` commands could carry the rules in directly. The docstring's reasoning holds only for a single command and does not say so. Undocumented and unverified: whether the harness concatenates multiple SessionStart outputs cleanly and in a stable order.
**Finding 2 — a read-gate is constructible.** `PreToolUse` receives `transcript_path` and can return `permissionDecision: "deny"` (or exit 2). So a hook rule can scan the transcript for a completed Read of the behaviour doc and deny or ask until it finds one — genuine enforcement rather than steering, the same class as the existing scope-lock. Unverified: the transcript's on-disk format, whether a Read's target path is reliably recoverable from it, and how the gate behaves on resume, after compaction, and inside a subagent.
**Finding 3 — nothing can force it at session start.** `SessionStart` hooks are context-only and cannot block. Any enforcement necessarily fires at the first tool call, not before the first reply.
**Sharpened at processing 2026-08-10, and left here deliberately rather than kept.** The docstring correction was split out as [session-start-cap-docstring-wrong] and cleared. What remains cannot be described as a build, because its file list would be the design's own output — the keep-check's second limb fails by construction. Two things must be **measured** before anyone can say what gets built, and neither is a lookup:
1. Does the harness concatenate multiple `SessionStart` outputs cleanly, in a stable order, with no separate aggregate limit further up? The reference is silent, so this is an experiment.
2. Is a completed Read's target path reliably recoverable from the transcript `.jsonl`, and does that survive resume, compaction, and running inside a subagent?
Both need a rezip and an application restart to observe, so the experiment is Claude's work and the restart is the user's — the decomposition already in the queue as [statusline-test-script] plus [statusline-restart-test]. Do not build that structure until the prior question is answered: whether enforcement is wanted at all. That belongs with [rule-lifecycle-system]'s born stage, which owns what makes a gate actually run.
**The ordering established in the session that filed this, kept because it is the frame for that decision:** a filename steers weakest, a first-line admission test steers better, a required close-time artifact leaves a trace, and a hook denial is the only one that cannot be skipped. The costs run the other way — a transcript scan on every tool call is the most expensive of the four.
Relates to [extract-skill-nonspecific-rules] (whose new doc inherits the same unenforced directive), [rule-lifecycle-system] (whose "born" stage carries the same visible-but-unenforced gate problem) and [restore-plan-file-gate] (the other hook rule recovered on an ask-never-deny design).

#### The shipped provenance rule is not holding — rationale keeps growing with reasoning the user never gave, and every pass copies it forward [invented-rationale-compounds-past-the-shipped-rule]
Captured by you 2026-08-10, filed mid-/next after `a556e5c`. Your words: Claude keeps inventing rationale the user didn't give or agree to, adding more and more.
**The rule already shipped and is live.** [rationale-misattribution] built at `f9326dc` and its wording sits in `plugin/si-plugin/docs-b/plugin-behaviour.md` in two places — the Captures provenance bullet (a `captured by you` credit requires the user's own words as its source; mixed authorship names who did which part; the same bar binds reason-shaped sentences in the prose) and the Why-pipeline's rationale-provenance clause. Both were loaded in the installed host during the session that filed this. So this is not a missing rule and not a rule lost to the revert — it is a shipped, always-loaded, deliberately sharpened rule that is still not holding.
**Why that is worse than the original finding, and why the obvious remedies are already spent.** [rationale-misattribution] ruled a hook out on a stated argument: the lint sees only text, and nothing in a finished work item distinguishes reasoning the session produced from reasoning the user gave, so there is no signal to match on. That left sharper wording as the only route, and sharper wording is exactly what shipped. Both candidate fixes have now been tried. Whatever this needs is a third thing, and finding it is the work.
**The new half you supplied: it compounds, and the compounding has a measurable cost.** Rationale is re-authored forward at every stage — capture to processed item to LOG — and each pass adds. `/next`'s scope-lock then copies every run item's rationale text verbatim into `_build.md`. In the run that filed this, that copy was about eight thousand tokens for fifteen items, all of it text already read from QUEUE.md minutes earlier. So invented rationale is not only a record that misrepresents the user; it is a bill the project pays again on every run that touches those items, and it grows.
**The live instance, in this session's own writing.** The capture filed minutes before this one opens "Captured by you" over a body that is mostly Claude's reasoning about where to search and what the retrieval would settle. The credit line is accurate — the instruction was the user's — but the surrounding prose is the shape the rule was written to catch, and it was written anyway with the rule loaded.
**Sharpened and deferred 2026-08-10; two halves split out and cleared.** The compounding cost became [build-file-rationale-pointer]. The evidence question became [rationale-growth-audit], which runs on git history rather than waiting for future sessions. What is left here is the provenance failure itself, and it does not design out yet.
**One candidate rejected at processing, so it is not re-proposed.** A cap — *a re-author pass may only carry forward and reword, never add* — was weighed and refused: processing legitimately does add reasoning, since a decision reached in the interview or an alternative rejected is exactly what the throughline exists to preserve. That rule would fix the symptom by deleting the feature.
**New evidence from the session that deferred this, and it points somewhere different.** Across nine items processed on 2026-08-10, attribution held — user and Claude contributions were named separately, and defeated Claude proposals were recorded as Claude's — and rationale volume grew anyway. So authorship and volume are separable failures, and the volume half may not be a provenance problem at all. [rationale-growth-audit] is what tests that.
Relates to [rationale-misattribution] (shipped, the rule this reports failing), [log-entry-cost-at-run-scale] and [arbitrary-run-length-capping] (both concern what a large run costs at close), and [communication-rules-untestable-here].
**Measured 2026-08-10 by [rationale-growth-audit]; the numbers now live at `resources/research/rationale-growth-measurement.md`.** They were five separate queue items until 2026-08-11, when they were consolidated into that file and deleted from here — none of them was work, so each would have failed the keep-check's second limb and halted a run that reached it. The two that bear hardest on this item: growth happens almost entirely at the capture→processed step and barely at all afterwards, so **the compounding framing above is wrong as stated**; and the worst-growing item in the corpus was later cut by 56% by hand, which is evidence the growth is compressible rather than load-bearing. Read §5 of that file with any figure taken from it — word counts cannot separate invented reasoning from decisions genuinely reached, which is the question this item actually asks.

#### Four checks the merged-plugin verification could not exercise, kept as work rather than left in a closed item's prose [merged-plugin-verification-remainder]
Filed 2026-08-10 at the close of [merged-plugin-live-verification]. Rides this session's commit.
That item verified most of its checklist by a /next run being the observation. Four things it could not reach, each needing something the run did not have:
1. **The three-strikes error halt.** No error repeated three times on one item, so nothing triggered it. Unexercised, not passed.
2. **/setup scaffolding a two-section queue plus `resources/research/`.** Needs a fresh folder. This is Claude work, not user work — it just sat outside the run's described scope.
3. **The queue lint's other three flags** — a slugless line, missing provenance, a missing section heading, and a red-flag state that is neither cleared nor uncleared. Only the blocked-by flag fired, because the real queue contained no instances of the others. Exercising them means writing bad queue lines on purpose, which is a build with a fixture, not an observation.
4. **The /done family** — flavor routing, LOG files named by slug, the shipped-slug cross-check, one summary commit across a multi-item run. This is exercised by the very next close, so it may already be answered by the time anyone reads this; check the LOG before building it.
**Why these are filed rather than noted.** The item they came from is closed and removed from the queue, so its prose is now only in git history and a LOG entry, neither of which /plan reads at planning time. An unexercised check left there is a check nobody runs. Note items 3 and 4 in particular are cheap.
Relates to [merged-plugin-live-verification].

#### Work that finishes in another project has no way to tell this one, so an item can sit ready long after it is done [cross-project-work-completes-invisibly]
Filed 2026-08-10 during a /next walk-through, at your instruction. Mixed authorship: you raised the question that found it — asking why the site was being edited from here when it belongs to a separate project — and Claude's is the diagnosis below. Rides this session's commit.
**What happened.** [report-url-404] was a `[user]` line waiting on a page existing at flintcraft.tech/report, which is work on a different project. The page had been stood up some time earlier. Nothing in this project learned that, and the item sat in the cleared region as ready work until a /next walk-through fetched the URL and found it already live.
**No harm this time, and that is not the point.** The item recorded an observable check, so the walk-through ran the check instead of asking whether the work was done — the check-the-world rule working exactly as designed, turning a stale item into a one-line confirmation. The cost was one wasted slot in a run and a moment of reasonable confusion about whether Claude was reaching into another project.
**The general shape.** Any item whose completion happens outside this repo is invisible here until someone looks. An observable check rescues the cases where "done" leaves a trace a fetch or a file test can find. It rescues nothing where completion is only visible to the user or inside another project's records — and that is the larger class.
**To settle at processing.** Whether the answer is an INBOX message from the doing project (the mechanism exists, and this is close to what it is for), or whether the honest answer is that observable checks are as far as this goes and the docs should say so plainly. Weigh against [inbox-delivery-unconfirmed], which records that an INBOX message is fire-and-forget in both directions — a notification nobody is guaranteed to read would not fix this, it would move it. Note also that this project cannot write into another project's queue as work; the most it can do is post a message that project has to read.
Relates to [inbox-delivery-unconfirmed], [inbox-mail-has-no-place-in-the-ladder] and [report-url-404] (the instance).

#### Starting a session from remote control has no live queue item, now that the worktree-override hook is probably dead [remote-session-start-has-no-owner]
Filed 2026-08-11 at the /plan close, from the wind-down re-scan, and approved by you. Rides this session's commit.
Your problem, in your own words this session: you cannot start sessions from remote control without creating a fuss, so you make them at home and run /clear — and the one time you tried, the work touched nothing in the project and Claude still raised a merge to deal with.
**The only item addressing this was [worktree-override-hook]**, whose design is a `WorktreeCreate` hook returning the existing tree. Research this session (`resources/research/worktree-isolation-and-desktop-sessions.md`) found the documented refusal that most likely forbids exactly that: a directory whose git metadata resolves into the main checkout, or that contains the protected checkout, is refused as an isolation worktree. That item is now probably-delete, and it was the only thing pointed at this problem.
**So the need is unowned.** The remaining honest options are to accept the fork and make the *merge* painless at close — merge-at-close being the one half of [concurrent-session-support] with independent value — or to establish that a hook-created path is genuinely refused before abandoning the override route. Note that the disconnections which made remote sessions unreliable are a separate platform issue no hook touches.
Relates to [worktree-override-hook], [concurrent-session-support] and [isolation-model-is-not-ours-to-choose].
**Left in Unprocessed deliberately on 2026-08-11, decided rather than overlooked.** Today's work gave the need a probable owner: [isolation-model-is-not-ours-to-choose] instructs that [concurrent-session-support] keeps merge-at-close as the one part with independent value, and painless merging is exactly the remote-start answer — if a forked session merges back cleanly, forking stops mattering. Two options were weighed. Holding this below the line behind [concurrent-session-support] would state the dependency in the field built for it, but it would assert this item is designed and merely blocked, which is false, and it would create a three-deep chain — remote-start → concurrent-session-support → isolation-model — which is signal 3 of the detector designed the same day in [nothing-audits-the-cleared-region]. Leaving it here says the true thing: it is undesigned. Re-process it **after** [concurrent-session-support] is reshaped, at which point either merge-at-close covers it and this is a clean delete, or it does not and it becomes work with a describable build. The risk accepted: nothing mechanically reminds anyone, where a `Blocked by:` would have. That is judged the lesser cost against asserting a design that does not exist.

#### An "unattended" /next run is not unattended: it stops for [user] items and never closes itself [unattended-run-is-not-unattended]
Captured by you 2026-08-11 in the post-close tail. Your words: an unattended run is not actually an unattended run — it stops for user items and doesn't automatically run /done on itself; we can design that, but it needs consideration of where user items land.
**The claim as it currently stands.** `SPEC.md` calls multi-item /next "unattended in practice — the autonomous build mode the Principles name", and the Principles say execution sessions trend toward pure execution, "stopping only for what genuinely needs the user". In practice a run stops in at least three ways: a `[user]` item is walked through live, one step at a time; a `[freeform]` item halts it outright; and at the end the run simply stops, because **/done is a command the user runs** — nothing closes the session on its own. So a run left alone finishes its builds and then sits there, uncommitted, until someone comes back.
**Why this is worth more than a wording fix.** The word shapes real decisions. `[user]` and `[audit]` lines are placed end-preferred *because* a stop inside a contiguous build run interrupts an otherwise unattended sequence — that rule only makes sense if the run is meant to be left alone. And the readiness line was made the run's sole bound on the same reasoning. If "unattended" is aspirational rather than true, several settled decisions rest on an overstatement.
**What a design has to settle, and your condition is the hard part.** Where `[user]` items land. Today end-preferred placement batches them at the end of the cleared region, which is the right instinct but only a tie-adjustment — a genuine dependency still puts a `[user]` item mid-run, and then the run stops in the middle whatever the word claims. Options to weigh: whether a truly unattended run must refuse to include `[user]` items at all and leave them to a separate walk-through session; whether the readiness line grows a second meaning (build up to here unattended, then stop for the human); or whether the honest answer is to stop calling it unattended and describe it as what it is — a run that clears vetted work and pauses where a person is genuinely needed.
**The self-closing half carries a real tension, not just work.** A run that ran /done on itself would commit without the user present, and the commit message is one of only two things the method still shows before it happens — precisely because a commit is hard to unwind and never becomes file content. So auto-closing either overrides that rule or needs a form of it that survives nobody being there. That has to be settled before, not after.
**Files (rough):** `SPEC.md` (the Multi-item /next paragraph and the Principles), `plugin/si-plugin/docs-b/next.md` (the run's stopping behaviour), `docs-b/done-plan.md` (end-preferred placement, whose rationale rests on this), plus an FAQ entry — a user reading "unattended" and walking away is exactly who this misleads. Relates to [arbitrary-run-length-capping] and [processed-reorder-mostly-unnecessary] (which keeps end-preferred placement on this same reasoning).

#### [user] Rename the GitHub repository and permanently reserve the old name, before the identity rename lands in the repo [throughliner-repo-rename]
Filed 2026-08-11 by Claude, during the /next run that reached [rename-to-throughliner] and found its first step was not Claude's to take. The decomposition is Claude's; the rename decision and its migration design are already recorded in [rename-to-throughliner] and are not reopened here.
**Why it is yours to run.** Renaming a GitHub repository is an account action on a service Claude has no authorised path to perform, and reserving the freed-up old name means creating a second repository under your account. Claude checked for a route before tagging this — the `gh` CLI can rename a repo where it is installed and authenticated, but that is still an irreversible change to a published project's public identity, made under your account, which is yours to authorise and yours to do.
**Why it goes first, and this is the whole reason the item exists separately.** The in-repo half of the rename changes the plugin's slug and adds the marketplace `renames` map. If that lands while the repository is still at the old URL, the marketplace registration consumers already hold points at a repo whose contents now describe a different plugin id — a half-renamed state that is worse than either end of it. The repository rename is what makes the 301 redirect exist, and the redirect is what keeps existing `marketplace add` registrations resolving.

**Walkthrough.**
1. Rename the repository `FlintcraftTech/throughliner` — the current repo is already named that, so confirm first whether a rename is needed at all, or only the plugin slug inside it. Claude flagged this: the repo name and the plugin slug are two different strings and the queue item treats them as one job.
2. If a rename is needed, do it in the repository's Settings page on GitHub.
3. **Create a new empty repository under the old name immediately afterwards**, and never delete it. GitHub's redirect from the old name breaks the moment somebody else claims that name, so reserving it is what keeps the redirect permanent.
4. Tell Claude it is done; Claude reads the remote back and confirms what it points at.
**Observable check at close:** `git remote -v` and a fetch against the remote — Claude runs these rather than taking the outcome on report, per the check-the-world rule.
**Files:** none in this repo. Its output is a precondition for [rename-to-throughliner], not an edit.

#### Four of the five stale FAQ entries [faq-backfill] names were corrected on 2026-08-11, so its premise needs re-reading before it is built [faq-backfill-partly-overtaken]
Filed 2026-08-11 by Claude, from work done in the same run. Not a claim that [faq-backfill] is done — a correction to the evidence it currently reasons from.
**What changed.** [revert-induced-doc-drift-sweep] rewrote four of the five entries that item names as documenting retired mechanisms: Batches and Captures, the Red flags section, the Deferred tests section, and the retired batch vocabulary in the /plan-/next-/done answer. The fifth, `/next freeform`, was already corrected before this run. The shipped template's "Plan session here" entry was corrected in the same pass.
**Why it matters rather than being bookkeeping.** [faq-backfill] is held below the line behind [rule-lifecycle-system], and its stated reason for waiting is that backfilling now would refill a document under the mechanism that let it rot. That reasoning is untouched and this does not lift it. What changes is the *size and shape* of the remaining job: the outright-wrong entries are gone, so what remains is coverage of changes shipped since 2026-06-18 that the FAQ never described at all — a different task from correcting falsehoods, and one whose scoping should start from a fresh read rather than from the five-entry list.
**Deliberately not lifting it.** The hold is on the lifecycle design, not on the staleness count, and a smaller job behind the same blocker is still behind it. Relates to [faq-backfill], [revert-induced-doc-drift-sweep] and [rule-lifecycle-system].

#### A finding filed as a queue item sits in Unprocessed indefinitely, and the placement-contradiction detector only watches Processed [findings-in-unprocessed-never-flagged]
Filed 2026-08-11 by Claude from the wind-down re-scan, approved by you.
**The instance.** Five rationale-growth measurements were filed as Unprocessed work items on 2026-08-10 and sat there until 2026-08-11, when processing established that none of them was work: ask the keep-check's second limb what changes inside which files and the answer is nothing. They were consolidated into `resources/research/rationale-growth-measurement.md` and deleted. Nothing surfaced them in the intervening day; they were found only because a session went looking through Unprocessed for a cluster to work on.
**Why the detector designed the same morning does not cover this.** [nothing-audits-the-cleared-region] flags an item whose placement contradicts its own text, and signal 2 is exactly this shape — an item whose `Files:` line says it has none, or whose prose says its build list is the design's output. But that detector watches **Processed**, on the reasoning that the cleared region is what a build run consumes. Unprocessed is unwatched, and Unprocessed is precisely where a findings-shaped item lands and stays: it never reaches a build, so it never halts anything, so nothing ever complains.
**Why it is worth catching rather than tolerating.** A finding in the queue is not merely untidy. It is counted in every queue-length figure, it is read and skipped by every /plan opening that skims Unprocessed, and it competes for processing order against real work. The five above also carried a caveat that only worked when bound to the numbers — split across five items, four were quotable alone and the fifth was the one nobody opened, which is a live misreading risk rather than a filing preference.
**To settle at processing.** Whether the existing signal 2 simply extends to Unprocessed, which is nearly free since the detector already parses the whole file; and what the flag should *say* there, since the answer is not "this is misplaced" but "this may be a finding rather than work, and its home is `resources/` or the LOG". Note the method already has the routing rule — the three-way triage in `skill-nonspecific-rules.md` — so the flag points at an existing decision rather than introducing one.
**Files (rough):** `plugin/si-plugin/scripts/queue_digest.py` or `plugin/si-plugin/hooks/session_start.py`, wherever [nothing-audits-the-cleared-region] puts the three signals. Relates to [nothing-audits-the-cleared-region] (whose detector this extends) and [hook-emits-counts-not-the-graph].

#### The throughput floor counts Unprocessed blockers while the below-line lift fires on shipped, and nothing states the difference [floor-and-lift-use-different-tests]
Captured by you 2026-08-11 — you stated, as a rule, that a below-line item should only ever be blocked by something in Unprocessed, because a blocker sitting in Processed would never be processed and the held item would never clear. The diagnosis below is Claude's.
**The inference was wrong, and it was reasonable.** The below-line revisit lifts on **shipped per LOG**, not on processed: a blocker in Processed-above is built by /next, leaves the queue, and the next /plan's revisit lifts the held item without /plan ever touching the blocker. That is not theoretical — three items lifted at the opening of this very session by exactly that route, and four more are one /next run from lifting.
**Where the inference comes from, which is why this is a documentation defect rather than a user error.** The throughput floor genuinely does count blockers **in Unprocessed**, and correctly so: those are the ones a *planning* session can move, where a blocker in Processed needs a build. So the method has two tests keyed on different sections — one for what a /plan session should prioritise, one for when a held item may be lifted — and **no document puts them side by side or says they are different questions.** Reading either alone, the other's criterion looks like an inconsistency or an error.
**The real failure mode is one section over, and it is worth naming in the same place.** What genuinely strands work is a blocker that is *itself held below the line* — a chain — which is why chains are signal 3 of [nothing-audits-the-cleared-region]'s detector. A chain that terminates is slow; one that loops never resolves.
**To settle at processing.** Whether one short statement covers it — the floor asks what this session can unblock, the lift asks what has already shipped, and a chain is the case to watch — and where it lives, noting `plan.md` holds both the floor derivation and the revisit, so it can be said once in the document that already contains both halves. Weigh against the admission count: this is plausibly a clarifying clause on an existing step rather than a rule.
**Files (rough):** `plugin/si-plugin/docs-b/plan.md` (the start-of-processing floor derivation and Step 1's below-line revisit), possibly a FAQ entry since a consumer meeting a held item would ask the same question. Relates to [nothing-audits-the-cleared-region] and [hook-emits-counts-not-the-graph], whose fourth bucket makes the absent-blocker case visible.

#### Reshape [rename-to-throughliner]: the repo rename is already done, and its ripple is wider than the item states [rename-item-needs-reshaping]
Filed 2026-08-11 by Claude, from the /next run that reached the item and stopped short of building it.
**Step 1 is spent.** The item's migration plan opens with "rename the GitHub repo". `git remote -v` shows the remote is already `FlintcraftTech/throughliner`. What actually remains is the *plugin's* identity inside the repo — the `sovereign-implementer` slug, the `plugin/si-plugin/` folder name, the marketplace `renames` map, and the positioning rewrite. The item treats repo name and plugin slug as one job; they are two strings and only one of them is still wrong.
**The build-time grep found files the item's candidate list misses:** `plugin/si-plugin/output-styles/concise-sovereign.md` (whose own filename carries the old identity), `plugin/si-plugin/scripts/scrub_sweep.py`, `.claude/launch.json` and `.claude/settings.local.json`, plus the `plugin/si-plugin/` folder rename itself — a file move, not a text substitution.
**What must not be swept in:** `resources/research/*`, `resources/plugin-behaviour-retired.md` and the LOG are history. Renaming identity strings inside them would falsify the record.
Relates to [throughliner-repo-rename] (the remaining GitHub-side step) and [rename-to-throughliner].

#### [user] Discord post: how much stronger a session is from its start once /plan opens by reading recent LOG index lines [discord-post-session-start-strength]
Captured by you 2026-08-11. The observation is yours, in your own words: before, it always felt shaky for the first few items, and starting out with log-awareness from the start plus some maybe-relevant context massively boosts the start of sessions. The angle is yours; the correction below is Claude's.
**It cannot be written yet, and that is the whole reason this is a queue item rather than a draft.** You asked for the post believing the feature was live. It is not: `plan.md`'s Step 1 reads QUEUE.md and SPEC.md only, and the three `LOG/index.md` mentions in that document are targeted lookups — has this already been decided — not an orientation read. The feature is [plan-reads-recent-log-index], which is held below the line behind [index-line-length-proportional-cap].
**Your experience was real; the mechanism you credited was the wrong one.** What actually steadied the session that produced this was the below-line revisit reading LOG to check two blockers, plus the previous session's forward advisory naming where to start. Both are live. Neither is the five-recent-lines read. Worth carrying into the post, because "the thing that helped was not the thing I thought" is a better story than the one originally intended.
**The post's content, to draft when it ships.** The shaky-first-few-items problem and what causes it; what the orientation read changes; and the honest scope — it does not carry all necessary context, it sets upcoming work against past work. The cost bound is worth including too, since it is why the feature waited: five index lines is an unbounded read until index lines are capped, which is what [index-line-length-proportional-cap] fixes.
**Constraints:** 2000 characters, the Discord limit. Not posted until *everything* the post describes has shipped — the standing rule recorded in `CLAUDE.md`, adopted 2026-08-11.
**Walkthrough.** 1. The feature ships. 2. Claude drafts the post inside the character limit. 3. You read it and say what to change. 4. You post it — Claude has no route to Discord. 5. You confirm, and the line is closed.
Blocked by: [plan-reads-recent-log-index]

#### Rename Sovereign Implementer → Throughliner: full technical identity + positioning, big-bang with a `renames` map [rename-to-throughliner]
Captured by you (2026-07-30); migration path resolved this /plan (2026-07-31) via research at resources/research/plugin-marketplace-rename-migration.md. **Rename the plugin from "Sovereign Implementer" to Throughliner**, built around its real distinctive value: the throughline of rationale that survives a forgetful session (an AI memory-of-reasoning tool), dropping the over-claiming "Sovereign" and the no-code-competitor framing. "Throughliner" (coined single word) chosen over "Throughline" because the bare word is crowded — getthroughline.com is itself software (name-availability research: resources/research/throughliner-name-availability.md, which found no product named "Throughliner"). Port-neutral: not "Throughline for Claude Code," so a future port won't break the name.
**Scope — both halves (user's call):** positioning/user-facing name (README title, marketplace description, pitch — lead with rationale-memory value); and technical identity (`sovereign-implementer` slug → `throughliner`, the GitHub repo, install/adopt commands, `.si-version`, every absolute path and doc mention).
**Migration decided (big-bang, not a transition apparatus):** (1) rename the GitHub repo — GitHub's 301 redirect keeps existing `marketplace add` registrations resolving; **permanently reserve the old repo name** (reusing it later breaks the redirect). (2) Add a top-level **`renames`** map entry in marketplace.json (`sovereign-implementer` → `throughliner`) — Claude Code auto-migrates each consumer's settings and shows a one-line notice; append-only, keep the entry forever. (3) Net consumer cost is a single `/plugin install` (github remote → `plugin-cache-miss`), documented in the release notes + FAQ. Caveats to carry into the build: needs consumers on Claude Code ≥ 2.1.193; managed/policy-enabled installs don't auto-rewrite.
**Lean on the marketplace id (`flintcraft`): keep it.** Renaming the marketplace itself would force consumers to re-add the marketplace — a burden the plugin `renames` map does not cover. Confirm in the build, but the default is to leave `flintcraft` as-is.
**Hook-enforced-identity change — trace the ripple by grepping the literal old identity strings at build time** (per CLAUDE.md's rule): grep `sovereign-implementer`, `Sovereign Implementer`, `si-plugin`, `.si-version`, `flintcraft`, **and `why-pipeline` / `why pipeline`** across the whole repo so the file list is complete before the build starts. Candidates: plugin.json, marketplace.json, CLAUDE.md, README.md, SPEC.md, every docs-b/ procedure file (incl. plugin-behaviour.md's Why-pipeline section), templates, hooks, INSTALL.md, resources/.
**Includes the concept rename why-pipeline → "throughline"** ([throughline-feature-elevation] thread a, folded in and settled 2026-07-31): the product and its titular feature share a name, so the concept rename lands atomically with the identity rename rather than as a separate pass that could drift out of sync. Rename `why-pipeline` / `why pipeline` → "throughline" across all docs, including plugin-behaviour.md's Why-pipeline section heading and every reference.
**Naming clarity (resolved 2026-08-01, folds in the deleted [throughline-naming-referent]):** the throughline is the *reasoning spine* — the thread of *why* — running through capture → work item → log; the LOG is where it's recorded and retrieved from, **not** the throughline itself. State this distinction explicitly wherever the rename introduces "throughline" (the renamed plugin-behaviour.md section, SPEC positioning, FAQ), so "throughline" is never colloquially collapsed into "the LOG." The tell that they're distinct: a complete LOG can carry *no* throughline — every event recorded but the reasoning stripped out — which is exactly the failure the mechanism fights, so the two must stay separate.
**Coordination:** host-side and large — the SPEC/CLAUDE.md/README rename lands atomically with the technical identity change (the "large SPEC rework is its own build" case). Relates to [self-hosting-path-portability] (absolute-path fix overlaps the path-rename surface), [migration-checklist] (adds an identity-migration dimension), and [rationale-misattribution] (which replaced [throughline-feature-elevation] on 2026-08-09 — the proactive-nudging thread is retired, and the rationale-provenance thread became that item; neither is in this build).
**Cleared 2026-08-10.** It had been blocked by [merged-plugin-live-verification]. The chain: the old condition named [user-work-surfacing-fix] shipping, rezipping and being verified live; it shipped at `ae92d34`, leaving the verification as the outstanding half, and that verification became its own queue item. It ran on 2026-08-10 and confirmed the merged plugin live, so nothing in the queue holds this now.
The old condition also said a repo-wide identity rename "should be its own deliberate /next run". That reasoning is kept as guidance for whoever runs it — this is a large atomic change and shouldn't be swept up mid-run — but under the below-the-line rule it is **not** what holds the item down. Only the unrun verification does.
**Files:** determined by the build-time identity grep (candidates above), plus plugin-behaviour.md (Why-pipeline section rename), SPEC.md (name/positioning), README title + feature list, CLAUDE.md, marketplace.json (`renames` map), and a FAQ entry (user-visible identity change + the one-time `/plugin install`).

#### The always-loaded rule corpus is over its ceiling — eviction is due [rule-corpus-over-ceiling]
Filed 2026-08-11 by the rule-lifecycle board's MEASURED signal, at the close of the build run that created it. This is the board's designed behaviour rather than a judgment: a signal that fires files one capture and gets out of the way, so the work lands where all work lands instead of standing as a notice nobody acts on.
The count is 234 structural rule-statements across `plugin/si-plugin/docs-b/skill-nonspecific-rules.md`, against a ceiling of 200 expressed in the proxy's own units. The ceiling derives from the 2026-08-09 hand count of ~218 instructions against the 150–200 research figure in `resources/research/instruction-file-bloat-and-subtraction.md`; the proxy is not the true instruction count and does not claim to be.
The techniques are in `resources/rule-maintenance.md` — codification, consolidation, recasting, and the staleness test. Reproduce the number with `python resources/rule_signals.py .` before and after, since a genuine consolidation lowers it and that is the check that the pass did something.
**The signal counts as satisfied while this capture is open**, so it will not re-file every session.

#### The three-lens compliance sweep is due, on its new trigger [three-lens-compliance-sweep-due]
Filed 2026-08-11 by the board's AUDITED signal. Until this run the sweep in `resources/method-compliance-audit-checklist.md` had no trigger at all and ran only when a /plan happened to file an `[audit]` item, which is how the corpus reached ~218 before anyone counted. It is now keyed to the same over-ceiling number MEASURED reads.
Reads the same input as [rule-corpus-over-ceiling] but is not downstream of it: both compute the number independently, and if the counter were deleted this would compute it itself. Sharing an input is not the dependency the design forbids.
This is an `[audit]`-shaped piece of work when it is processed — it reads and reports, and its findings become captures.

#### Recent rule-bearing commits carry no gate disposition, and the obligation only just shipped [rule-gate-dispositions-missing]
Filed 2026-08-11 by the board's BORN signal, which reports 15 of the last 15 commits touching `plugin/si-plugin/docs-b/`, `resources/self-authoring-rules.md` or `CLAUDE.md` with no `Rule gate:` line in any LOG entry.
**Expected, and that is the point of filing it rather than suppressing it.** The disposition obligation shipped in this same session, so every commit before it predates the rule. The decision this item carries is narrow: backfill dispositions for recent history, or start clean from this commit and accept that the signal reports a fixed set of pre-rule commits until they age out of the window.
Claude's view, for the user to accept or reject: start clean. A backfilled disposition is written by someone reconstructing what a past session decided, which is exactly the handoff-provenance problem — it would look like evidence and not be.

#### Live rules still name 44 retired terms, and the signal's precision needs a pass [live-rules-name-retired-terms]
Filed 2026-08-11 by the board's REPEALED signal. Some hits are genuine and worth fixing: `Blocks:` and `Depends on:` survive in `done-plan.md` and `post_tool_use.py` though both fields are retired in favour of `Blocked by:`.
**Others are probably not faults, and the item carries that honestly rather than claiming 44 defects.** The detector skips a line that says a term is retired, but a doc can name a retired mechanism correctly in other ways. Two pieces of work here: fix the genuine stale references, and tighten the detector against what the pass learns — its guard list is one constant in `resources/rule_signals.py`.
Relates to [rule-corpus-over-ceiling]: both are eviction work, and doing them in one pass is likely cheaper than two.

#### The run that built the corpus measure grew the corpus by 24 statements [this-run-grew-what-it-measured]
Filed 2026-08-11 by Claude at the build close, from re-running the board before and after. The always-loaded count went 210 → 234 across this session.
**Not an argument that the work was wrong** — the run shipped 21 agreed items and several genuinely amended always-loaded rules. It is the observation that a 21-item run has a measurable cost in the one currency the method says is binding, and nothing surfaced that cost while the run was happening. The board now would, but only at the *next* session's start.
Two specific suspicions worth checking in the eviction pass, both Claude's: several additions this run wrote reasoning into the operative rule (the parallel-sessions block, the capability-check split, the prior-decisions widening) where `resources/self-authoring-rules.md` says the why belongs in the LOG entry that decided it; and the isolation and capability-check blocks may be consolidatable now that both are written out in full.
Relates to [rule-corpus-over-ceiling] — this is the diagnosis, that is the work.

#### Every new script rediscovers the UTF-8 console fix by failing [new-scripts-rediscover-the-utf8-fix]
Filed 2026-08-11 by Claude, from three instances in one session.
`queue_digest.py` crashed outright on its first run — `UnicodeEncodeError` on a `→` inside a queue heading — and was fixed by copying `reorder_queue.py`'s stream reconfiguration verbatim. `session_start.py`'s new subprocess read of the board mangled every em-dash until an explicit `encoding="utf-8"` was added, which is the same defect one layer up: there it is the write, here the read. And twice Claude misread its own console's *display* of a correct string as a data fault, once building a wrong diagnosis before checking the raw bytes.
**The fix is known and written down; what is missing is anything that applies it without being rediscovered.** Candidates: a tiny shared module the scripts import, a line in the authoring notes for new scripts, or accepting the copy and making the comment in `reorder_queue.py` the canonical one that new scripts are pointed at. The third is cheapest and matches how the hooks already duplicate `_working_file` deliberately.
The display-versus-data half is a separate lesson worth keeping: check `ascii()` or raw bytes before diagnosing an encoding fault seen through a console.

#### [user] A [user] walkthrough handed over a command that could not produce the observation [user-walkthrough-command-cant-produce-the-observation]
Filed 2026-08-11 by Claude, from a live instance minutes earlier. Mixed authorship: the user's request to run the command themselves is what exposed it; the finding is Claude's.
Walking [mover-encoding-fix-verification], Claude gave the user a `reorder_queue.py … --move <slug> TOP` command. That path prints `Processed reordered (2 items)` and echoes no heading at all — so it could not have shown whether an em-dash renders. The observation the item exists to collect was impossible from the command handed over. `--delete` is the path that echoes a heading.
**It was caught only because the user asked Claude to run it first**, which the walk-through rules do not require and which will usually not happen — the user is meant to run these. So the catch was luck.
**The fix to weigh: before handing a `[user]` step over, confirm the step can actually produce the observation the item names.** That is cheap where Claude can run the command harmlessly (a scratch fixture, as here), and it is a different check from the capability check, which asks whether Claude could do the *work* — this asks whether the *user's step* yields the *evidence*. Tagged `[user]`-shaped for now because the instance sits inside a `[user]` walkthrough; whether the rule belongs in next.md's walk-through branch or in plan.md's walkthrough-authoring step is the design question.

#### The 20% index-line cap was derived from three entries, and 87 existing lines break it [index-cap-sample-was-three-entries]
Filed 2026-08-11 by Claude at the close of the run that shipped [index-line-length-proportional-cap], from measuring the whole index rather than a sample.
**What the measurement says.** Applying the cap across `LOG/index.md` finds **87 lines over 20%**, with the worst at 48%. The pattern is clear and was invisible in the original sample: the violations are overwhelmingly SHORT entries carrying ordinary-length lines — a 96-word entry with a 46-word line reads at 48% while saying nothing excessive. The three entries measured at processing were 968, 1,055 and 1,738 words, all long, which is why they all passed comfortably.
**This is not an argument that the cap is wrong, and the distinction matters.** The cap was written as a ceiling that freezes current practice, and against long entries it does exactly that — every one of the 21 entries written in this run came in under it, most between 13% and 18%. What the corpus shows is that the derivation's *sample* was unrepresentative of entry length, not that a proportion is the wrong shape.
**Three options, none taken here because this is a fate decision.** Re-derive the figure against the full distribution rather than three cases, which is what [derivation-required-for-limits] would actually require of a limit stated today. Or scope the cap to entries above some length, which reintroduces a bare number and should be resisted. Or accept that 87 historical lines are grandfathered and the cap binds only new ones — cheapest, and honest so long as it is written down rather than left as drift.
**The uncomfortable reading, recorded because it is the one a later auditor should test.** A limit whose own corpus violates it 87 times is close to the failure [derivation-required-for-limits] was written to prevent — a number that fires against correct work. It was measured, which is more than the 1,200-word ceiling ever was; it was just measured on too little.
Relates to [index-line-length-proportional-cap] (the cap itself), [derivation-required-for-limits] (the rule that admits it) and [rule-corpus-over-ceiling] (the other measure this session found firing on its own corpus).

