# QUEUE

Two sections. **Processed** — work discussed and agreed, ordered top-to-bottom; /next builds from above the `--- Cleared to run above this line ---` marker. **Unprocessed** — captured, not yet fully processed; the next /plan weighs each item. A work item is a `#### ` heading (its description) with a `[slug]` at the end of that line and its rationale in the block beneath. A leading `[audit]` / `[user]` tag names how it's executed; no tag means a build. A work item carrying a security or privacy risk gets a `Red flag · State: …` marker — the flag rides the work.

**Authoring a rule for the method's own text? The rule gate is in `CLAUDE.md`, always loaded — run it before you write.**

## Processed

--- Cleared to run above this line ---

## Unprocessed

#### [user] Discord post: the planning session now offers you the work that is cheapest right now [discord-post-context-adjacency]
Captured by you 2026-08-12; the angle is yours — the ladder and how it drastically improves the workflow.
**It cannot be written until [ladder-rung-for-context-adjacent-items] has shipped.**
**What the post has to convey, and the difficulty is making it vivid.** When nothing is left to prioritise by, /plan now says so and offers the items closest to what the session has already read, rather than working blindly down the file. The value is concrete: in the session that produced this, one item needed SPEC.md read in full — 6,159 words — which the session had already loaded, so the measurement that made the item decidable cost nothing. A later session pays that read again to reach the same point.
**A judgment for you at drafting time.** The before-picture is a real failure — a session grinding through items in file order while cheap adjacent work sat unnoticed, and you asking for it by hand across several sessions. Whether that goes in a public post is your call; it is what makes the improvement legible.
**Walkthrough.** 1. The feature ships. 2. Claude drafts inside 2,000 characters. 3. You read it and say what to change. 4. You post it. 5. You confirm, and this line closes.
**Unblocked 2026-08-13.** [ladder-rung-for-context-adjacent-items] shipped, and has since been refined once — the offer now leads with a recommendation rather than a flat menu (`LOG/2026-08-12-context-adjacency-offer-is-a-flat-menu.md`, `e5d169b`). Draft against the refined behaviour, not the original. Sat blocked unnoticed; see [blocked-by-in-unprocessed-is-never-revisited].

#### [user] Discord post: Claude now knows how the work cycle actually fits together [discord-post-cycle-awareness]
Captured by you 2026-08-12; the angle is yours — awareness of the build cycle, so Claude knows exactly what is going on.
**It cannot be written until [cycle-summary-at-every-skill-opening] has shipped.**
**The honest before-picture, which is also the strongest material.** The pieces were all documented — what an audit does, what a capture is, what /plan may process, what /next may build — and nothing stated the loop. Claude assembled it wrong twice in consecutive sessions, both times confidently, reasoning that a planning session sitting between an audit's findings and their build meant the findings could not reach the build. That is the cycle working, not an obstacle to it. Your words on the second occasion: this is the second planning session in a row where you have had to explain it.
**The design point worth including.** A flat list of stages would not have prevented either failure, because both were about a loop *closing*. What shipped names the return edges explicitly.
**Same judgment as the sibling post:** the before-picture is Claude getting it wrong, and whether that appears publicly is yours to decide.
**Walkthrough.** 1. The feature ships. 2. Claude drafts inside 2,000 characters. 3. You read it and say what to change. 4. You post it. 5. You confirm, and this line closes.
**Unblocked 2026-08-13.** [cycle-summary-at-every-skill-opening] shipped — its LOG entry is `LOG/2026-08-12-cycle-summary-at-every-skill-opening.md`. Ordinary ready work. Sat blocked unnoticed; see [blocked-by-in-unprocessed-is-never-revisited].

#### Setup should settle repo privacy & publishing posture: gitignore option, licensing question, optional public-repo setup [setup-repo-privacy-and-publishing]
Captured by you — raised mid-/done, filed after `9cd1e51`. Throughliner content (QUEUE/LOG/SPEC) is sensitive and a user's repo may be public, so /setup should help settle the privacy/publishing posture up front rather than leaving it implicit.
**Components to design (user, 2026-08-01):**
- **Gitignore-all-TL option** — offer to gitignore all Throughliner content so it never enters the repo at all; the maximally-private posture, for users who don't want planning/logs tracked or published.
- **Licensing question** — ask about the project's license as part of setup.
- **Optional public-repo setup, non-pressuring** — framed off the license choice: "since you chose this license, do you want this on your GitHub? We can do it now, or I can capture it for a later /plan." Offer, never push; the "capture for later" branch is the graceful decline.
- **Open-source → open-logging is an unsettled opinion, NOT a decided default (user's own caveat):** the user leans toward open source implying open logging but isn't sure it's substantiated enough in the real world to *suggest* in setup. Weigh whether setup surfaces this at all, and if so as a neutral option — don't bake the opinion into a recommendation without grounding.
**Depends on [scrub-gate-before-approval]:** a public-repo offer is only safe once the scrub gate guarantees artifacts are clean — don't encourage publishing before the leak risk is closed.
**Files (rough):** setup.md (new questions), plugin-behaviour.md (privacy posture), SPEC/FAQ, `.gitignore` handling. Relates to [scrub-gate-before-approval].

#### [user] Correct and post the announcement about rationale moving out of operative rules — the draft tells readers the why lives in session logs they can't reach [announcement-rationale-split-correction]
Captured by you, 2026-08-09 — split out of [rationale-relocation-invisible-to-consumers] when that capture was deleted as otherwise satisfied. The Discord draft written on 2026-08-09 says the reasoning "lives in the session logs where you can go and read it." That is false for every reader who is not developing the method: the plugin package ships neither `LOG/` nor `resources/`. Posting it as written would tell users to go and read something they have no access to.
**Why this is the user's line:** Claude can draft the corrected wording, but only the user can post to Discord. Under the capability test, drafting is Claude-work and posting is not — so the line covers the posting, with the draft prepared for it.
**Walkthrough:**
1. Claude drafts the corrected announcement, replacing the session-logs claim with the actual split: why the method behaves a certain way is in the FAQ that ships with the plugin; why a rule is worded as it is stays in the development log.
2. The user reads it and says what to change.
3. The user posts it to Discord.
4. The user says it's posted, and it gets recorded and the line removed.
Rough draft, to sharpen at step 1 rather than now — the announcement's other content wasn't reviewed this session, so check the whole thing rather than only the one false sentence. Also worth deciding at step 1 whether this is still worth announcing at all, given the week that has passed.
**Files:** none — the artifact is a Discord post. Relates to [self-authoring-rules].

#### Design a /plan→/next ordering handoff: carry the order's rationale forward so /next explains (not infers) it, with a predictable, not-over-narrated shape and optional user reorder-input before the run [next-start-order-handoff]
Captured by you — filed after `bebffe7`.
Today /plan reorders both queue sections and /next delivers the cleared region in that order. Two gaps: /next can't *explain* why the order is what it is (it would have to infer the rationale), and an easy-going user is fine letting it run but some users will want a say in the order at the **start of /next**, before anything runs.
**The idea:** have /plan file a forward handoff (advisory-style) every time that carries the **ordering logic** — why this before that — so at /next's start Claude can state the order *and its reasoning* without inferring, and offer the user a chance to adjust before the run.
**Design goals to settle when processed:**
- **Predictable shape.** The start-of-/next "run roundup + rationale recall" should have a fixed, recognisable form, not free-form narration that varies each time.
- **Not TOO narrated.** Explaining the order pulls against the anti-overwhelm / consolidate-narration rules — the design must balance "explain enough that a user could object" against "don't bury the Ready ask in a wall of rationale." Likely: terse order + one-line why per non-obvious placement, detail on request.
- **Optional reorder input.** Preserve the easy-going path (confirm and run) while giving users who want it a clean way to change the order at /next start — without turning it into a mandatory gate.
**Don't conflate two lifecycles.** The existing forward-recommendation advisory is a **/plan→/plan** orientation handoff, consumed and cleared at the next *planning* session. This is a distinct **/plan→/next** handoff of *ordering rationale*, read at build start — settle whether it's a new artifact or an extension of the advisory, and its own clear/lifecycle.
**Builds on:** next.md Step 1.3 (the existing "present the run — Ready?" step that already lets a user redirect to /plan). **Relates to:** [reorder-cost-vs-dependency-model] (the per-session cost of reorder reasoning — this adds a consumer of that reasoning) and [user-work-surfacing-fix] (both touch what /next surfaces to the user at run start).
**Skipped this session (2026-07-31)** — its surfaces (next.md Step 1 run-presentation, the reorder) are being reshaped by [user-work-surfacing-fix] and [reorder-cost-vs-dependency-model]; design it fresh once those land.
**Blockers cleared + author-once note (2026-08-01):** its two reshaping dependencies ([user-work-surfacing-fix], [reorder-cost-vs-dependency-model]) have both shipped, so this is now ready to design. But its home doc (next.md Step 1) is about to be reworked wholesale in the docset-B authoring ([fable-docset-model-detection] phase 2). Design and author this *as part of that B work* so the /next-start handoff lands once in docset B, rather than authored into the about-to-freeze docset A and redone. The design (handoff shape, lifecycle, optional-reorder mechanism) is durable and can be settled independently of which docset carries it.
**Files (rough):** plan.md (file the ordering handoff at close), done-plan.md (where the reorder + advisory are written), next.md Step 1 (read + present + optional reorder), plugin-behaviour.md (the advisory lifecycle + the consolidate-narration rule this must respect), FAQ.

#### Name the post-/done tail as an explicit state: route it cleanly and advise that scope-lock is off — without adding a re-/done prerequisite [post-close-tail-state]
Captured by you — filed after `fa2f8e5`. Observed live 2026-08-01: after a /done committed (`ea272f6`), the session ran a long tail — six captures filed to QUEUE.md, all uncommitted — and the user briefly lost track of being post-close. The method neither names nor handles the post-close tail. (This capture is itself a post-close capture — the same state, live.)
**Durability is already handled — this is NOT about forcing another /done (user, 2026-08-01).** Post-close captures are written to QUEUE.md, so they are on disk and not lost, and the existing mechanism already picks them up: a capture filed after the commit rides the next session's commit and converges into that session's LOG (plugin-behaviour.md captures-filed-after-commit; done.md). The design intent is to let the post-close tail happen *gracefully*, leaning on that pickup — never to add a re-close requirement. At most a *light* awareness cue ("these are filed and will be saved by your next session"), which must not read as "you must /done again."
**The two real gaps:**
- **Routing clarity.** What a post-close captures-only tail "counts as" is unclear — done.md routes it to done-plan.md as "a session that changed only method docs," but that's inferred, not named. Name the post-close tail explicitly in done.md's routing so a session doesn't have to reason it out.
- **Scope-lock is off (the safety point).** `pre_tool_use.py` enforces file-scope only while `_build.md` exists; once /done deletes it, the file scope-lock is fully off — any file is editable (git-safety and the subagent-ask still fire; only file containment drops). The user runs the whole post-close tail with no scope containment and isn't told.
- **What Claude may drive in the tail, folded in from [drive-testing-signals-skill-routing] on 2026-08-14 when that item was deleted into this one.** Observed the same day, 2026-08-01, from the same session: the whole post-close tail was set off by Claude initiating a render-verification informally in conversation — "having the user run testing" — when the verification was already a `[user]` line sitting in Processed. Its findings then surfaced as loose captures rather than through any structured flow. The user's insight, in her words: when Claude finds itself wanting to run or drive testing or verification, especially `[user]` work, that urge is a signal to stop and route it into the proper skill — a quick /plan to structure the lines, or /next to hand over an existing `[user]` line — rather than running it informally in a post-close or inter-skill tail. It is the same failure family as under-filing a `[user]` line: user work handled informally instead of as a tracked, structured line. And it blurs the plan/execute boundary from the far side — not planning during execution, but executing structured work with no skill around it.
**Why that arrived here rather than staying its own item.** Its rule has no subject until this one lands: "a loose tail" is not something the method names yet, so there is nowhere to hang what Claude may not do in one. It also has nowhere to live as a free-standing rule — it must fire when *no* skill is running, which is the one case the always-loaded file's all-four-skills admission test rejects. As a clause of the tail's own definition, both problems disappear.
**Open design questions (for /plan):** whether scope-lock-off is a bug to fix (re-enable some lighter post-close protection) or just to advise plainly; how light the awareness cue should be so it never becomes a de-facto re-close prerequisite; and the folded question of the exact trigger and destination for routing a verification urge into a skill.
**Files (rough):** `plugin-behaviour.md` is named here and no longer exists — it was retired on 2026-08-10, so this list needs re-deriving at design rather than following. Its successor for a rule of this shape is `docs-b/skill-nonspecific-rules.md`, subject to the admission problem noted above. Otherwise: done.md (name the post-close tail in routing), possibly pre_tool_use.py / session_start.py if post-close protection is added, SPEC/FAQ if user-visible. Relates to the captures-filed-after-commit mechanism and to [drive-testing-signals-skill-routing], now folded in and deleted.

#### Add a lightweight post-close "done delta" — commit everything since the last /done without a full-session rescan [done-delta-close]
Captured by you — filed after `fa2f8e5`. /done is expensive: the full close re-scans the whole session's discussion (wind-down re-scan), runs the spec-sync gate, reorder, marker positioning, advisory management, LOG authoring, and staleness sweep. When a /done has already committed this session and the tail only filed a few captures, the full ritual is overkill — most steps no-op, but Claude still pays the reading/reasoning cost to *reach* each no-op. (Seen live 2026-08-01: a second /done banked six post-close captures; nearly every step was a silent no-op, yet the full path was still walked.)
**The idea:** a delta close that handles only *everything since the last /done commit* — stage the new changes, write a light delta LOG entry, commit — while **bounding the rescan to since-the-last-close** instead of the whole session, and skipping reorder/marker/advisory steps when nothing relevant changed.
**This coexists with, doesn't replace, the free path:** post-close captures already ride the next session's commit ([post-close-tail-state]), so doing nothing is still valid. The delta close is the explicit "bank it now, cheaply" option for when the user wants it.
**Design questions (for /plan):**
- **Trigger:** separate mode/command, or auto-detected? Auto-detect is cleaner — /done notices a commit already happened this session and switches to the delta path — but "already committed this session" needs a reliable signal (a session marker, or git log since session start).
- **Keep vs skip:** commit + light LOG entry kept; whole-session wind-down shrinks to since-last-commit; reorder/marker/advisory fire only if the delta touched them. Preserve the full close's safety (nothing unrouted) without re-paying its cost.
- **LOG shape:** a fresh delta entry (as this session did with render-verify-and-captures.md) vs appending to the prior entry.
**Relates to:** [post-close-tail-state], the done-family dedup audit findings (done.md is already heavy — a delta path should reuse the shared core, not duplicate it), and captures-filed-after-commit.
**Files (rough):** done.md (the delta-close path + its trigger), possibly session_start.py if a session-commit marker is needed, SPEC/FAQ if user-visible.

#### Keep the migration recipe current via a maintenance pass as the method's format evolves [migration-recipe-freshness]
Captured by you — filed after `f593cd1`. The migration checklist (the recipe for converting an old-format project to the current two-section queue) will drift as the method's queue/doc format keeps changing — a stale recipe is worse than none. So it needs a maintenance pass that keeps it current.
**Trigger to settle at processing:** you suggested doing it "on rezip." Worth weighing against a tighter alternative — rezip is a frequent local test-build, so a pass on *every* rezip risks becoming noise, whereas a **format-change trigger** (whenever a build changes the queue/doc format, refresh the recipe in the same session) targets exactly the moment drift is introduced — the same family as the existing README-sync and SPEC-sync triggers. Decide between them (or a hybrid) when processed.
**Gated on [migration-checklist] existing first** — the recipe this keeps fresh isn't built yet (it sits below the cleared-to-run line). So this can't be built until that ships.
**Relates to** [migration-checklist] (the recipe it maintains). **Option to weigh when processed (rehomed from the deleted [migrate-skill], 2026-08-01):** a **migration-notes doc** — a version-keyed running record of what structurally changed between which format versions, refreshed whenever a build changes the format. Same refresh trigger as [setup-as-migration-home]'s format-epoch bump discipline, so design the two together; decide whether a full changelog earns its keep or the recipe-refresh alone suffices.

#### Show-first approval moments produce their text twice [approval-flow-token-doubling-simplification]
Captured by you (2026-08-01), while reviewing your Claude Code feature request anthropics/claude-code#77134. Rescoped at your direction on 2026-08-13 from a much larger item about approval-time doubling generally.
**The cost, narrowed to where it still exists.** Showing text in chat and writing it to a file are both the model producing those tokens, so text that does both is produced twice. That used to hit every approval moment. It no longer does: write-first shipped, and the report after a write is one line naming what landed, never a re-paste — so the dominant flow produces nothing twice.
**What remains is the show-first set only** — the moments the write-first test deliberately keeps showing first, because the previous version is not recoverable without the user: a commit message, anything leaving the machine, a wholesale conversion of a document the user already owns. There the text is composed in chat, approved, and then produced again to be used.
**Why it is not buildable yet.** The saving needs the harness to surface an already-produced Write's content verbatim, with no second model pass — issue #77134, which has not landed. Until it does there is no build to describe, so this stays in Unprocessed rather than being kept as designed work. Re-examine when the issue ships.
**Two things settled and not to be re-opened here.** The write-first ordering flip is decided and shipped. The convergence note about view-in-doc machinery is spent — the working-mode field, the Editor field and the line-anchored-link promise were all retired on 2026-08-09.
External dependency: anthropics/claude-code#77134.

#### Routing communication feedback to memory masks the method defect that produced it — the memory-boundaries rule needs the exception [memory-masks-method-defects]
Captured by you — raised 2026-08-09 at a /done close, filed after `115f851`. The user's words: if Claude's replies are affected by memory, then the method can't truly be tested.
**The conflict, which the shipped rule does not see.** plugin-behaviour.md's memory-boundaries rule lists what memory is free for: user preferences, working style, **communication feedback**, cross-project facts. Separately, this project's CLAUDE.md states that all use of the plugin to develop the plugin is testing the plugin, and that any moment session memory covers for something the docs should carry is itself a mandatory capture. Nobody applied that second rule to *persistent* memory, and the two now disagree: saving communication feedback to memory is explicitly permitted, and doing it here contaminates the only test the method gets.
**Why it is sharper than a self-hosting quirk.** Communication feedback is very often *evidence about the method's own narration rules*. This session is the demonstration: the user twice said there was too much text, which is evidence that the /plan checkpoint is too long — filed as [done-invoked-when-user-meant-continue]. Had the same feedback also gone to memory, Claude would simply have behaved better from then on, the queue item would have stopped mattering, and the defect would have survived in the shipped docs with nothing to surface it. Memory would have quietly bought a fix for one user while every consumer kept the bug.
**So the routing test is not "is this a preference?" but "is this evidence about the method?"** A preference that no method rule governs — a name, a timezone, a tool the user likes — is memory's, unchanged. Feedback that a *method-produced behaviour* was wrong is a testing outcome and belongs in the queue, whether or not it also reads as a preference. The overlap is the whole problem: the feedback is genuinely both, and the current rule names only the branch that silences it.
**To settle at processing:** the exact wording of the exception in the memory-boundaries rule; whether it is general or scoped to projects that are testing the method; and whether the shipped rule should say plainly that feedback about Claude's own narration is a capture first and a preference second. Weigh against the admission gate — this may be an amendment to the existing memory-boundaries rule rather than a new rule, which is the cheaper shape.
**Files (rough):** `plugin/throughliner/docs-b/plugin-behaviour.md` (Routing and discipline, Memory boundaries), possibly `CLAUDE.md` if the self-hosting half needs stating separately. Relates to [done-invoked-when-user-meant-continue] (the live instance). It used to be offered as a candidate payload for [self-hosting-auto-detection], deleted 2026-08-14 — the absence of any payload only detection could switch on is what settled that item.

#### The behaviour-rules read can be hook-enforced after all, and the reason it wasn't is documented wrongly [behaviour-rules-read-is-enforceable]
Filed 2026-08-10 during /plan; rides this session's /done commit. Mixed authorship: the user refused Claude's assertion that a forced read was not possible and asked for it to be checked; the research and the findings below are Claude's, from the Claude Code hooks reference. Full evidence, including the verbatim JSON shapes a build would need, is at `resources/research/hook-enforced-doc-reading.md`.
**Finding 1 — the shipped design's stated premise is wrong.** `session_start.py:360-384` explains that the rules are pointed at rather than pasted because the file is tens of kilobytes and "blew the cap by a wide margin". The 10,000-character cap is documented as **per hook command, not aggregate**, so several `SessionStart` commands could carry the rules in directly. The docstring's reasoning holds only for a single command and does not say so. Undocumented and unverified: whether the harness concatenates multiple SessionStart outputs cleanly and in a stable order.
**Finding 2 — a read-gate is constructible.** `PreToolUse` receives `transcript_path` and can return `permissionDecision: "deny"` (or exit 2). So a hook rule can scan the transcript for a completed Read of the behaviour doc and deny or ask until it finds one — genuine enforcement rather than steering, the same class as the existing scope-lock. Unverified: the transcript's on-disk format, whether a Read's target path is reliably recoverable from it, and how the gate behaves on resume, after compaction, and inside a subagent.
**Finding 3 — nothing can force it at session start.** `SessionStart` hooks are context-only and cannot block. Any enforcement necessarily fires at the first tool call, not before the first reply.
**Sharpened at processing 2026-08-10, and left here deliberately rather than kept.** The docstring correction was split out as [session-start-cap-docstring-wrong] and cleared. What remains cannot be described as a build, because its file list would be the design's own output — the keep-check's second limb fails by construction. Two things must be **measured** before anyone can say what gets built, and neither is a lookup:
1. Does the harness concatenate multiple `SessionStart` outputs cleanly, in a stable order, with no separate aggregate limit further up? The reference is silent, so this is an experiment.
2. Is a completed Read's target path reliably recoverable from the transcript `.jsonl`, and does that survive resume, compaction, and running inside a subagent?
Both need a rezip and an application restart to observe, so the experiment is Claude's work and the restart is the user's — the decomposition already in the queue as [statusline-test-script] plus [statusline-restart-test]. Do not build that structure until the prior question is answered: whether enforcement is wanted at all. That belongs with [rule-lifecycle-system]'s born stage, which owns what makes a gate actually run.
**The ordering established in the session that filed this, kept because it is the frame for that decision:** a filename steers weakest, a first-line admission test steers better, a required close-time artifact leaves a trace, and a hook denial is the only one that cannot be skipped. The costs run the other way — a transcript scan on every tool call is the most expensive of the four.
Relates to [extract-skill-nonspecific-rules] (whose new doc inherits the same unenforced directive), [rule-lifecycle-system] (whose "born" stage carries the same visible-but-unenforced gate problem) and [restore-plan-file-gate] (the other hook rule recovered on an ask-never-deny design).

#### The shipped provenance rule is not holding — rationale keeps growing with reasoning the user never gave, and every pass copies it forward [invented-rationale-compounds-past-the-shipped-rule]
Captured by you 2026-08-10, filed mid-/next after `a556e5c`. Your words: Claude keeps inventing rationale the user didn't give or agree to, adding more and more.
**The rule already shipped and is live.** [rationale-misattribution] built at `f9326dc` and its wording now sits in `plugin/throughliner/docs-b/skill-nonspecific-rules.md` in two places (it was authored into `plugin-behaviour.md`, which was retired on 2026-08-10 and split; the reference was corrected on 2026-08-13) — the Captures provenance bullet (a `captured by you` credit requires the user's own words as its source; mixed authorship names who did which part; the same bar binds reason-shaped sentences in the prose) and the Why-pipeline's rationale-provenance clause. Both were loaded in the installed host during the session that filed this. So this is not a missing rule and not a rule lost to the revert — it is a shipped, always-loaded, deliberately sharpened rule that is still not holding.
**Why that is worse than the original finding, and why the obvious remedies are already spent.** [rationale-misattribution] ruled a hook out on a stated argument: the lint sees only text, and nothing in a finished work item distinguishes reasoning the session produced from reasoning the user gave, so there is no signal to match on. That left sharper wording as the only route, and sharper wording is exactly what shipped. Both candidate fixes have now been tried. Whatever this needs is a third thing, and finding it is the work.
**The new half you supplied: it compounds, and the compounding has a measurable cost.** Rationale is re-authored forward at every stage — capture to processed item to LOG — and each pass adds. `/next`'s scope-lock then copies every run item's rationale text verbatim into `_build.md`. In the run that filed this, that copy was about eight thousand tokens for fifteen items, all of it text already read from QUEUE.md minutes earlier. So invented rationale is not only a record that misrepresents the user; it is a bill the project pays again on every run that touches those items, and it grows.
**The live instance, in this session's own writing.** The capture filed minutes before this one opens "Captured by you" over a body that is mostly Claude's reasoning about where to search and what the retrieval would settle. The credit line is accurate — the instruction was the user's — but the surrounding prose is the shape the rule was written to catch, and it was written anyway with the rule loaded.
**Sharpened and deferred 2026-08-10; two halves split out and cleared.** The compounding cost became [build-file-rationale-pointer]. The evidence question became [rationale-growth-audit], which runs on git history rather than waiting for future sessions. What is left here is the provenance failure itself, and it does not design out yet.
**One candidate rejected at processing, so it is not re-proposed.** A cap — *a re-author pass may only carry forward and reword, never add* — was weighed and refused: processing legitimately does add reasoning, since a decision reached in the interview or an alternative rejected is exactly what the throughline exists to preserve. That rule would fix the symptom by deleting the feature.
**New evidence from the session that deferred this, and it points somewhere different.** Across nine items processed on 2026-08-10, attribution held — user and Claude contributions were named separately, and defeated Claude proposals were recorded as Claude's — and rationale volume grew anyway. So authorship and volume are separable failures, and the volume half may not be a provenance problem at all. [rationale-growth-audit] is what tests that.
Relates to [rationale-misattribution] (shipped, the rule this reports failing), [log-entry-cost-at-run-scale] and [arbitrary-run-length-capping] (both concern what a large run costs at close), and [communication-rules-untestable-here].

**Design progress 2026-08-14, made while processing [provenance-restated-in-plan-and-done] alongside this.** Reviewed together, and this item still does not design out — but one thing about it is now settled rather than open. The corpus states this rule in three places: canonically in the always-loaded rules, in `plan.md`'s processing discipline, and inside `done.md`'s LOG entry template. Only the third sits at the moment the failure actually occurs, since re-authoring an item's rationale into an entry is where reasoning gets re-attributed. That copy is therefore protected — the paired item cuts `plan.md`'s and explicitly leaves `done.md`'s alone.

**What that rules out, which is the useful part.** "State the rule again, or state it harder" is no longer a candidate direction: the rule is already stated at the failure site and was broken there anyway, and this item's own evidence from nine items on 2026-08-10 is that attribution held while volume grew. So more text is not the missing ingredient, and a fix has to come from somewhere other than restatement. Whether that is a check on the written entry, a different authoring order, or something not yet seen is still open — which is why this skips again rather than being kept.
**Measured 2026-08-10 by [rationale-growth-audit]; the numbers now live at `resources/research/rationale-growth-measurement.md`.** They were five separate queue items until 2026-08-11, when they were consolidated into that file and deleted from here — none of them was work, so each would have failed the keep-check's second limb and halted a run that reached it. The two that bear hardest on this item: growth happens almost entirely at the capture→processed step and barely at all afterwards, so **the compounding framing above is wrong as stated**; and the worst-growing item in the corpus was later cut by 56% by hand, which is evidence the growth is compressible rather than load-bearing. Read §5 of that file with any figure taken from it — word counts cannot separate invented reasoning from decisions genuinely reached, which is the question this item actually asks.
**A fourth instance of the pattern this item owns — a correctly-worded always-loaded rule failing to fire — carried across from [subagent-ask-rule-slipped] at /plan on 2026-08-13, when that item was deleted.** Mid-/plan on 2026-08-11, Claude spawned the `claude-code-guide` subagent to answer a research question without asking first, at roughly 28,000 subagent tokens. The rule is always-loaded, unambiguous, and says to ask before spawning one and to name the cost; Claude read it at session start and broke it hours later. That item listed it as the third instance alongside [write-first-report-without-write] and this one. It is recorded here rather than kept as its own item because **its buildable half is now closed**: the subagent cost ask-gate was confirmed alive by reading `hooks/hooks.json` (matcher `Task|Agent`) and `pre_tool_use.py` (`if tool_name in ("Task", "Agent")`), both naming the current and the legacy tool name, with a code comment recording the earlier silent death when only `Task` matched. The gate returns `ask`, never `deny`. So the enforcement is intact and only the prose-does-not-fire pattern survives, which is this item's subject.
**Skipped at /plan on 2026-08-13, with a fifth thing now in play that this item predates.** The output style gained a rule on 2026-08-13 telling Claude to match a written file's length to what the task needs, naming queue rationale and LOG entries explicitly. That is a third remedy after the two this item records as spent, it aims at the volume half rather than the provenance half, and it has never been tested. So the next move here is to observe whether rationale volume falls now that the rule exists — not to design a fourth mechanism on top of an untested third. Re-examine once a few sessions have written queue rationale under it.
**One methodological lesson from that deletion, worth more than the instance.** The deleted item asserted that settling the gate question needed a user observation and would decompose into a `[user]` line. It did not: "did the gate fire?" is answered by reading what the gate matches on. That is exactly the reframe the over-tag guard exists for — asking *what would answer this?* rather than *how do I observe it?* — and without it a `[user]` line would have gone to the user unnecessarily.

#### Work that finishes in another project has no way to tell this one, so an item can sit ready long after it is done [cross-project-work-completes-invisibly]
Filed 2026-08-10 during a /next walk-through, at your instruction. Mixed authorship: you raised the question that found it — asking why the site was being edited from here when it belongs to a separate project — and Claude's is the diagnosis below. Rides this session's commit.
**What happened.** [report-url-404] was a `[user]` line waiting on a page existing at flintcraft.tech/report, which is work on a different project. The page had been stood up some time earlier. Nothing in this project learned that, and the item sat in the cleared region as ready work until a /next walk-through fetched the URL and found it already live.
**No harm this time, and that is not the point.** The item recorded an observable check, so the walk-through ran the check instead of asking whether the work was done — the check-the-world rule working exactly as designed, turning a stale item into a one-line confirmation. The cost was one wasted slot in a run and a moment of reasonable confusion about whether Claude was reaching into another project.
**The general shape.** Any item whose completion happens outside this repo is invisible here until someone looks. An observable check rescues the cases where "done" leaves a trace a fetch or a file test can find. It rescues nothing where completion is only visible to the user or inside another project's records — and that is the larger class.
**To settle at processing.** Whether the answer is an INBOX message from the doing project (the mechanism exists, and this is close to what it is for), or whether the honest answer is that observable checks are as far as this goes and the docs should say so plainly. Weigh against [inbox-delivery-unconfirmed], which records that an INBOX message is fire-and-forget in both directions — a notification nobody is guaranteed to read would not fix this, it would move it. Note also that this project cannot write into another project's queue as work; the most it can do is post a message that project has to read.
Relates to [inbox-delivery-unconfirmed], [inbox-mail-has-no-place-in-the-ladder] and [report-url-404] (the instance).

#### An "unattended" /next run is not unattended: it stops for [user] items and never closes itself [unattended-run-is-not-unattended]
Captured by you 2026-08-11 in the post-close tail. Your words: an unattended run is not actually an unattended run — it stops for user items and doesn't automatically run /done on itself; we can design that, but it needs consideration of where user items land.
**The claim as it currently stands.** `SPEC.md` calls multi-item /next "unattended in practice — the autonomous build mode the Principles name", and the Principles say execution sessions trend toward pure execution, "stopping only for what genuinely needs the user". In practice a run stops in at least three ways: a `[user]` item is walked through live, one step at a time; a `[freeform]` item halts it outright; and at the end the run simply stops, because **/done is a command the user runs** — nothing closes the session on its own. So a run left alone finishes its builds and then sits there, uncommitted, until someone comes back.
**Why this is worth more than a wording fix.** The word shapes real decisions. `[user]` and `[audit]` lines are placed end-preferred *because* a stop inside a contiguous build run interrupts an otherwise unattended sequence — that rule only makes sense if the run is meant to be left alone. And the readiness line was made the run's sole bound on the same reasoning. If "unattended" is aspirational rather than true, several settled decisions rest on an overstatement.
**What a design has to settle, and your condition is the hard part.** Where `[user]` items land. Today end-preferred placement batches them at the end of the cleared region, which is the right instinct but only a tie-adjustment — a genuine dependency still puts a `[user]` item mid-run, and then the run stops in the middle whatever the word claims. Options to weigh: whether a truly unattended run must refuse to include `[user]` items at all and leave them to a separate walk-through session; whether the readiness line grows a second meaning (build up to here unattended, then stop for the human); or whether the honest answer is to stop calling it unattended and describe it as what it is — a run that clears vetted work and pauses where a person is genuinely needed.
**The self-closing half carries a real tension, not just work.** A run that ran /done on itself would commit without the user present, and the commit message is one of only two things the method still shows before it happens — precisely because a commit is hard to unwind and never becomes file content. So auto-closing either overrides that rule or needs a form of it that survives nobody being there. That has to be settled before, not after.
**Files (rough):** `SPEC.md` (the Multi-item /next paragraph and the Principles), `plugin/throughliner/docs-b/next.md` (the run's stopping behaviour), `docs-b/done-plan.md` (end-preferred placement, whose rationale rests on this), plus an FAQ entry — a user reading "unattended" and walking away is exactly who this misleads. Relates to [arbitrary-run-length-capping] and [processed-reorder-mostly-unnecessary] (which keeps end-preferred placement on this same reasoning).

#### [user] Discord post: how much stronger a session is from its start once /plan opens by reading recent LOG index lines [discord-post-session-start-strength]
Captured by you 2026-08-11. The observation is yours, in your own words: before, it always felt shaky for the first few items, and starting out with log-awareness from the start plus some maybe-relevant context massively boosts the start of sessions. The angle is yours; the correction below is Claude's.
**It cannot be written yet, and that is the whole reason this is a queue item rather than a draft.** You asked for the post believing the feature was live. It is not: `plan.md`'s Step 1 reads QUEUE.md and SPEC.md only, and the three `LOG/index.md` mentions in that document are targeted lookups — has this already been decided — not an orientation read. The feature is [plan-reads-recent-log-index], which is held below the line behind [index-line-length-proportional-cap].
**Your experience was real; the mechanism you credited was the wrong one.** What actually steadied the session that produced this was the below-line revisit reading LOG to check two blockers, plus the previous session's forward advisory naming where to start. Both are live. Neither is the five-recent-lines read. Worth carrying into the post, because "the thing that helped was not the thing I thought" is a better story than the one originally intended.
**The post's content, to draft when it ships.** The shaky-first-few-items problem and what causes it; what the orientation read changes; and the honest scope — it does not carry all necessary context, it sets upcoming work against past work. The cost bound is worth including too, since it is why the feature waited: five index lines is an unbounded read until index lines are capped, which is what [index-line-length-proportional-cap] fixes.
**Constraints:** 2000 characters, the Discord limit. Not posted until *everything* the post describes has shipped — the standing rule recorded in `CLAUDE.md`, adopted 2026-08-11.
**Walkthrough.** 1. The feature ships. 2. Claude drafts the post inside the character limit. 3. You read it and say what to change. 4. You post it — Claude has no route to Discord. 5. You confirm, and the line is closed.
**Unblocked 2026-08-13.** [plan-reads-recent-log-index] shipped — its LOG entry is `LOG/2026-08-12-plan-reads-recent-log-index.md`, and /plan's read-state step now opens by reading the five newest index lines. Ordinary ready work. This was the fourth item found sitting behind a shipped blocker; see [blocked-by-in-unprocessed-is-never-revisited].

#### The hook-payload "under half the cap" assertion is a bare number with no derivation [half-cap-assertion-has-no-derivation]
Filed 2026-08-12 by Claude at /plan, relocated out of [session-start-payload-over-its-headroom] when that item was deleted as already fixed. The question is the deleted item's; the reading of the test file is Claude's.
**Why the parent was deleted and this survived it.** That item reported two failing assertions in `resources/testing/hook_schema_check.py`. Both were run at /plan on 2026-08-12 and both pass: the payload is back under the headroom line, and the project-state line leads the payload again, which `2f0dfdf` records fixing after it had drifted 2,289 characters in. So the item's own build had nothing left to do. But it raised a third point the passing tests do not answer, and that point is this item.
**What the test file actually says, read rather than assumed.**
```
HOOK_OUTPUT_CAP = 10_000        # sourced: hooks reference + two GitHub issues
size < HOOK_OUTPUT_CAP // 2     # "keeps headroom" — no derivation given
```
The 10,000-character cap is properly grounded — the Claude Code hooks reference, `anthropics/claude-code#44086` and `#70460`, with a note that past the cap the harness saves the text to a file and injects a roughly 2KB preview plus a path, so the model never sees the remainder. **The half is grounded in nothing.** No comment, no measurement, no reference.
**Why that matters here specifically.** A bare threshold with no derivation is what [derivation-required-for-limits] bans, and this project has now deleted two of them for exactly that reason: the rule-corpus ceiling removed from `rule_signals.py`, and the 150–200 instruction count being removed from the self-authoring gate. A third one sitting inside a test suite is the same defect in a place nobody looks, and it has already fired once — the parent item existed because this assertion failed.
**The separate 2KB figure is NOT in scope and should not be swept in.** `check("SessionStart: project state within the first 2KB", 0 <= state <= 2048)` is a different number with a different job, and whether it has a derivation was not examined. Anyone building this should check it or leave it alone deliberately, not assume it shares this one's fate.
**To settle at processing.** Whether the half-cap assertion has a derivation nobody wrote down — ask what would answer this before concluding it does not, since the answer may be in a LOG entry or in the commit that added it. If it has one, record it in the file. If it does not, the options are to derive one honestly, to replace the assertion with a growth report carrying no threshold (which is what the board did when its ceiling went), or to delete the assertion and keep only the hard 10,000 cap. Weigh that a test suite is a place a threshold can sit unexamined for a long time, which argues for the growth-report shape over a new number.
**Files (rough):** `resources/testing/hook_schema_check.py`. Host-only — the testing directory is not in the plugin package. Relates to [derivation-required-for-limits], [index-cap-sample-was-three-entries] (the same defect in the index-line rule, kept in this session), [board-reports-one-audience-not-two] and [gate-still-declares-the-old-ceiling] — the two bare numbers already removed.

#### The tick means "built" and "confirmed" at once, so a build can ship code nothing ever ran [tick-conflates-built-and-confirmed]
Filed 2026-08-13 by Claude from an INBOX message sent by the Understudy project (`INBOX/archive/2026-08-13-from-understudy-parking-outcome-followup.md`), a follow-up to the parking report already filed as [in-session-parking-has-no-home].
**How they closed the parking case, which is the evidence rather than the ask.** /done's completion check offers two paths — finish with /next, or close partial — and neither fitted. Seven items had their code written and about to be committed; a partial close leaves those in Processed, so the next run would present them as unbuilt and rebuild over code already there. The queue would have been lying, in the direction that wastes a whole run. Instead the seven were ticked and removed, because the building genuinely happened, and the outstanding confirmations were filed as two new items — a `[user]` line for the judgements that are the user's, and an ordinary build item for the confirmation sweep that is Claude's. A dependent installer item was moved below the line naming that sweep as its blocker.
**The general shape, in their words: a build that ships code it never ran is not rare enough to leave to judgement, and the tick is currently the only signal, which means it has to mean both "built" and "confirmed" at once.** Their eleven LOG entries each said plainly what was and was not confirmed — but only because a session chose to write it there. Nothing in the close would have caught the omission.
**One incidental observation, offered rather than argued.** A message can be read and archived while the sending session is still running, so "sent" and "final" are the same instant, and nothing warns a sender that follow-up in place is now impossible. That may be entirely correct — mail is not a document — but it is worth deciding rather than leaving as a side effect. Note it fires against [inbox-delivery-unconfirmed], which is cleared to run and states what sending does and does not guarantee.
A reply to Understudy is owed. Relates to [in-session-parking-has-no-home] (the same correspondence), [unattended-run-is-not-unattended] and [inbox-delivery-unconfirmed].

#### A project's CLAUDE.md keeps describing a queue structure the method has retired, and nothing compares the two [adopted-claude-md-describes-retired-structure]
Filed 2026-08-13 by Claude from the same Understudy message, their finding 4, observed on their own project.
**What they found.** Their CLAUDE.md described QUEUE.md as holding batches with Build/Test/Audit subheadings, a Deferred tests section, and captures split by `---`, with items carrying `Parked:` headers. All four are retired. The file was written at /setup and has been read at the start of every session since, and nothing compares it against the current structure. A session the previous day went looking for the Deferred tests section, did not find it, and had to establish from our docs that it was gone.
**Why it is ours rather than theirs.** The staleness is generated by the method's own scaffolding — CLAUDE-TEMPLATE.md wrote that description — so every project adopted before those changes likely carries the same paragraph. They are repairing their own copy and reporting the pattern.
**This is a known gap rather than a new one, and processing should start from that.** SPEC says the missing-setting top-up is add-only and that "the riskier case — reconciling content whose template wording changed — is deliberately out of scope for now". The format epoch and its migration halt cover documents whose *shape* the hooks parse; a prose paragraph describing the queue is neither missing nor structurally wrong, so all three existing checks pass it. This is the first recorded instance of that deliberate gap costing a real session real time, which is what makes it worth re-opening.
**One thing to check rather than assume at processing:** whether the current `CLAUDE-TEMPLATE.md` still emits the retired description, or whether it was fixed and only already-adopted projects carry it. Those need different fixes, and the second is the harder one.
A reply to Understudy is owed. Relates to [migration-recipe-freshness] and [shipped-spec-maintenance-rules].

#### The filing-claim hook fires on a slug that was only cited, not filed [stop-hook-fires-on-cited-slugs]
Filed 2026-08-13 by Claude from a live instance in this /plan session, after the last committed close, so it belongs to no committed session record.
**What happened.** A message cited `[nothing-runs-the-hook-tests-at-a-close]` while reasoning about an already-built item — quoting a planning entry's kept-list and naming that item's LOG file. The stop hook read the slug as a filing claim, found no matching `#### ` heading in QUEUE.md, and blocked with "the write did not happen". Nothing had been written, and nothing was meant to be.
**Why the detector cannot tell them apart as written.** A slug in square brackets is the method's only cross-reference notation, and the always-loaded rules positively require citing other items by slug in prose. So the hook's signal — a slug appearing in a message — is present in the ordinary compliant case as well as the failure case. The false fire is not a tuning miss; the two shapes are identical at the level the detector reads.
**This is the second recorded shape, and the pair is what makes it structural.** [stop-hook-fires-on-drafted-not-filed-captures] records the hook firing on a capture presented as a draft for approval, which is also a shape the method itself specifies. Both fire on conduct the rules mandate.
**A harder case the fix must survive:** an item that genuinely was built this session is absent from QUEUE.md for the correct reason — /next consumes it — so absence from the queue can never on its own mean a write was missed.
**One thing to check rather than assume at processing:** whether the detector distinguishes a slug in a reporting sentence from one in a citation, and whether it can consult LOG filenames before concluding a slug is unfiled. Relates to [stop-hook-fires-on-drafted-not-filed-captures] and [disposition-detector-is-format-brittle].

#### A correctly held item is as invisible to the user as a wrongly held one [held-items-invisible-during-normal-use]
Filed 2026-08-13 by Claude from the same Understudy correction, the part they say is not a rules problem at all.
**What happened on their project.** Their user asked why a feature she had raised repeatedly "isn't queued". It was queued, fully designed, and held below the readiness line for a correct reason. She could not see it during ordinary use.
**Why the correction sharpens this rather than dissolving it.** This cost was originally reported as collateral damage from a wrong placement, so fixing the placement looked like fixing the visibility. The placement turned out to be right, and the user still could not see her work. That makes the invisibility independent of whether any given hold is justified — which is what promotes it from a symptom to its own item.
**What is actually invisible.** /next presents the cleared region and builds from it; the held region is read at /plan's below-the-line revisit, which is deliberately silent when an item is still blocked. So a user who runs /next and /done and does not sit through a /plan revisit has no moment where held work is named at all, and the silence is by design rather than by oversight.
**To settle at processing:** whether anything should surface held work outside /plan, and if so where — a /next pre-flight line naming the count and what each waits on is the obvious candidate and is also exactly the kind of per-run noise this method keeps removing. Weigh that before scoping. Relates to [no-field-for-ordering-preference] and [built-unverified-check-only-at-the-close].

#### A user-named subset of items, once processed, has no stated shape — so the session reaches for the empty-queue gate and leans toward stopping [subset-done-has-no-stated-shape]
Filed 2026-08-13 by Claude from a live instance in this /plan session, caught by you. Filed after the last committed close, so it belongs to no committed session record.
**What happened.** You named three items at the opening. When the third was processed, Unprocessed still held 35 items, and the session offered "anything else to capture or discuss, or shall we close out?" — `plan.md`'s **neutral end-of-queue gate**, which is specified for the case where Unprocessed *empties*. You had to ask why continuing was not offered.
**The gap, which is a missing case rather than a misread one.** `plan.md` states the four routes once at the start of processing, and specifies the end-of-queue gate for an empty queue. Between those sits an unhandled state: a user-named subset is exhausted while the queue is not. Nothing says what the session does there, so the nearest available gate gets reached for — and the nearest one names closing.
**Why that particular substitution is worse than a neutral miss.** The end-of-queue gate is carefully worded not to lean toward closing *given an empty queue*. Applied to a full one it stops being neutral: it silently reclassifies 35 unprocessed items as nothing left to do. The same failure the checkpoint recital was stripped back for — an offer naming closing, at the end of a piece of finished work, reading as an announcement that closing is what happens next.
**To settle at processing:** whether the fix is a stated shape for subset-exhausted (return to the ladder and present the next item, continuing as the default), or whether the end-of-queue gate should test that the queue is actually empty before it may be used. Relates to [close-invites-same-session-next] and [plan-gates-say-close-out-for-a-retired-phase] — three records of a close being offered where continuing was the better default.

#### A `Blocked by:` line in Unprocessed is read by nothing, so an item can stay blocked long after its blocker shipped [blocked-by-in-unprocessed-is-never-revisited]
Captured by you (2026-08-13), from your standing instruction that nothing may be held without something existing to surface it again. The three live instances below are the evidence.
**What was found.** Three `[user]` Discord-post items in Unprocessed — [discord-post-spec-inversion], [discord-post-context-adjacency] and [discord-post-cycle-awareness] — each carried a `Blocked by:` naming a slug absent from the queue. All three blockers were checked against LOG and **all three had shipped**, two of them on 2026-08-12 with LOG entries named after their slugs. The posts had been ready for a day with nothing saying so, and they were found only because you asked why work goes unsurfaced.
**The mechanism, which is a coverage gap rather than a bug.** `plan.md`'s below-the-line revisit is the thing that asks "has this blocker shipped?" and it walks **Processed below the marker only**. Nothing walks Unprocessed for the same field. The queue digest does resolve the reference and prints `-> ABSENT`, but ABSENT reads identically for a blocker that shipped and one that never existed, and no step is told to act on it.
**Why this is worse than the region it does cover.** An item held below the line in Processed is at least designed, cleared-adjacent and looked at by a step that exists. An item sitting in Unprocessed behind a stale blocker is invisible to that step, invisible to /next, and its blocker is invisible to the throughput floor once it has shipped and left the queue. Nothing anywhere is looking.
**To settle at processing:** whether the revisit simply widens to both regions, or whether `Blocked by:` should be barred from Unprocessed altogether — an Unprocessed item is by definition not yet designed, so the field may be claiming a precision it cannot have. Either way the digest already resolves the reference, so the check is cheap. Relates to [runs-alone-at-the-end-can-be-starved] (found in the same exchange) and [held-items-invisible-during-normal-use].

#### A `Runs alone` item placed last can be overtaken indefinitely and never reached [runs-alone-at-the-end-can-be-starved]
Filed 2026-08-13 by Claude, from your question about why the rename had not happened. Filed after the last committed close, so it belongs to no committed session record.
**The live instance.** [rename-to-throughliner] is cleared to run and has been for some time. It is not held, not blocked, and nothing is wrong with its placement — it carries `Runs alone` and sits last, which is exactly what `plan.md` prescribes for unrelated work so a run clears the buildable items before stopping. You had assumed it was stuck below the line; it never was.
**Why correct placement produces starvation anyway.** A run reaches a last-placed item only after everything ahead of it is built. Every planning session adds newly cleared work *ahead* of it, because that is where kept items land. So the queue position that guarantees a clean run also guarantees the item recedes each time the queue is worked. This session alone placed three items ahead of it.
**What makes it invisible.** Nothing reports how long an item has been cleared, and nothing may — a bare age threshold is the kind of invented number this project bans. The item is not flagged by the digest, not held by any rule, and reads as perfectly healthy at every inspection. It surfaced only because you asked after it by name.
**To settle at processing, and the obvious answers are both weak.** Placing `Runs alone` work first inverts the problem — it stops every run at the top. Reporting cleared-age needs a threshold nobody can defend. Worth weighing whether the honest fix is narrative rather than mechanical: /plan naming, when it places work into an already-cleared region, that it has just placed something ahead of a `Runs alone` item. Relates to [blocked-by-in-unprocessed-is-never-revisited] and [held-items-invisible-during-normal-use].

#### What ends a message becomes the suggestion chip, so message endings should be designed rather than left to fall where they land [message-ending-drives-the-suggestion-chip]
Captured by you (2026-08-13), from watching the chips over many sessions. The design below is yours; the confirming instance and the doc-tracing are Claude's.
**Your observation, in your words:** the chips are useful and you do not want them gone — the recommendation is often hiding in the chip, and that is how you find what Claude is actually suggesting. The problem is *which* command lands there. Your proposal has three parts: mid-session asks should not end on the close option — bury it mid-sentence and put continue last, since continue is the commonly taken route; recommendations should surface last so they are both clearer and more likely to be picked up; and the close's recommendation should say to run /clear or start a new session before /next, so the chip generator has no /next to grab.
**The mechanism is already documented here, which is why this is not speculation.** `done.md`'s Recommend next records it plainly: the harness offers the slash command it just saw as a tab-completion, and a message ending in a question whose answer looks like a command is one keystroke from being run by accident. A user was already caught by exactly that. What is new is your finding that the same mechanism runs in the *useful* direction too — the chip is a channel, not only a hazard, and nothing has ever tried to aim it.
**A live instance, from the close that preceded this capture.** `done.md` rung 2 says to name the next item as information, with **no question and no command string ending the message**. That close ended on a bolded "Start /next when you're ready", and the chip then offered /next — the outcome the rule exists to prevent, produced by a rule shipped hours earlier in this same project. So part three of the proposal is not a new rule so much as an unmet one, and the queue item that shipped it, [close-invites-same-session-next], did not survive its first real test.
**Where parts one and two stand.** Both are close to rules that already exist — the always-loaded rules put the single user-facing ask in bold at the end, and lead with the decision. So the work here is mostly making the existing endings deliberate rather than authoring new ones: which option ends the sentence when several are offered, and whether "continue" is stated last by default.
**The one thing to weigh honestly at processing.** This shapes the method's own prose around a third-party heuristic that is undocumented and can change without notice. That is defensible only because parts one and two are independently right under the method's existing rules and would stand if the chips vanished tomorrow. Part three is a compliance fix. If a future proposal contorts wording *only* to steer the chip, that is where the line is.
**Files (rough):** `plugin/throughliner/docs-b/done.md` (Recommend next, rung 2's wording — the /clear-or-fresh-session phrasing) and the ask-shaping rule in `skill-nonspecific-rules.md`. Shipped, not host-only. Relates to [close-invites-same-session-next] (built, and violated at its first test), [subset-done-has-no-stated-shape] and [plan-gates-say-close-out-for-a-retired-phase].

#### The project's two Claude Code config files point at a folder layout that has not existed for months [claude-config-points-at-dead-layout]
Filed 2026-08-13 by Claude during the identity-rename build. Filed after the last committed close, so it belongs to no committed session record.
**What was found, and it is not what the rename item predicted.** [rename-to-throughliner] listed `.claude/launch.json` and `.claude/settings.local.json` among the files the folder rename would invalidate. Neither was invalidated, because neither pointed at `plugin/si-plugin` in the first place. They point at a much older layout — a top-level `sovereign-implementer/` folder with `planning/`, `build-log/` and `Dev/Resources/` inside it — under a user path (`C:\Users\Alex\...`) that is also no longer this machine's. That folder does not exist and has not for a long time.
**What this means in practice.** `launch.json` declares one dev-server configuration serving `sovereign-implementer/crash-course`, a directory that is absent, so the configuration cannot start. `settings.local.json` carries roughly fifteen permission allowlist entries naming absolute paths into that dead layout; a permission entry for a path that cannot occur simply never matches, so they are inert rather than harmful. Two live entries in the same file *did* name the real `plugin/si-plugin/scripts/reorder_queue.py` and were corrected in the rename build, since those would genuinely have stopped matching.
**Why it is captured rather than fixed here.** The rename item's described work is the identity change, and none of this is the identity change — the strings are stale for an unrelated reason and were stale before the rename started. Fixing them means deciding what `launch.json` should serve now, or whether the project needs one at all, which is a decision rather than a substitution.
**To settle at processing.** Whether `launch.json` is deleted or repointed; whether the dead allowlist entries are pruned (harmless but they make the file hard to read, and this user's own stated difficulty is scanning dense lists); and whether anything else in `.claude/` assumes the old layout.
**Files (rough):** `.claude/launch.json`, `.claude/settings.local.json`. Host-only — a consumer's `.claude/` is their own.

#### A one-line reply cannot be told apart from a suggestion chip, so every user-credit resting on one is unverifiable [chip-replies-are-indistinguishable-from-user-authorship]
Captured by you (2026-08-13), in your words: those are ALL chip answers, i don't write like that. The metadata check and the containment test below are Claude's.
**What was being checked.** You asked where `Runs alone` came from, saying you did not remember okaying it. The queue item that introduced it recorded two user-credits: that you directed settling it before the next run, and that the marker itself was "the route, chosen by the user". Claude read the raw transcript, judged the first credit accurate and the second wrong, and told you so with confidence. You then said all of those replies were chips.
**The metadata settles it, and it settles it against the record rather than for it.** Every user message in `.claude/projects/<slug>/*.jsonl` carries `origin: {kind: "human"}` and `promptSource: "sdk"`. Messages you plainly typed — including ones carrying your own typos — are byte-for-byte indistinguishable in that metadata from one-line affirmatives. The transcript records who **sent** a message and never who **composed** it. So the primary evidence this project relies on for provenance cannot answer the provenance question at all.
**The loop, which is why this is structural rather than a discipline failure.** Claude proposes a design; the harness renders Claude's own proposal as a suggestion chip; the user clicks it to move on; Claude records "chosen by the user, in their own words". Claude's recommendation is laundered into the user's authorship, and the resulting artifact is indistinguishable from a real user decision — to a later session, to an audit, and to the session that wrote it. It was believed and asserted twice in the exchange that discovered it.
**The one signal that still works is content, not metadata.** A real user message contains something Claude's preceding message did not: a disagreement, a new fact, a redirect, a question, a correction. A reply wholly contained in what Claude just proposed cannot evidence authorship, because the chip could have supplied every word. That is checkable with no harness support and no new field.
**To settle at processing:** whether the credit rule in the always-loaded rules gains that containment test explicitly; whether existing credits can be audited by it or should simply be treated as unreliable in bulk; and whether a wrongly-credited decision needs re-deciding rather than only re-labelling — the `Runs alone` marker is shipped and may be entirely right on its merits, which is a separate question from who chose it.
**What this does NOT establish.** That the decisions were bad. `Runs alone` may be correct; the objection is to the record claiming an authorship it cannot support.
**Files (rough):** the provenance rules in `plugin/throughliner/docs-b/skill-nonspecific-rules.md` (Captures, provenance; and the rationale-provenance clause). Shipped, not host-only — every consumer's queue carries these credits. Makes [invented-rationale-compounds-past-the-shipped-rule] structural rather than a discipline problem, and relates to [message-ending-drives-the-suggestion-chip] (the same chip mechanism, aimed at output rather than input) and [runs-alone-at-the-end-can-be-starved].

#### `[freeform]` is documented as a queue tag for planned work, but it is really a logging type for ad-hoc sessions [freeform-documented-as-the-wrong-thing]
Captured by you (2026-08-13), in your words: freeform is not for running things alone, it is for big sessions or things that characteristically can't run within /next, for whatever reason; the vast majority of freeform sessions are run by the user ad hoc and not planned in /plan, but they just need a type for logging so that /done knows what it is looking at.
**What the shipped docs say instead.** `skill-nonspecific-rules.md` and `plan.md` both describe `[freeform]` as a work-item flavor marking work /next must **not** build, whose "defining case is a repair to the machinery /next itself uses — the queue mover, the scope-lock, the lint — where running the broken mechanism to build past it is the failure." `plan.md` further tells /plan where to *place* it in the cleared region. All of that describes a tag on a planned queue item.
**The divergence, stated plainly.** Your account is that the common case never goes through /plan at all: you do the work by hand, and the tag exists so /done can recognise what kind of session it is closing. The shipped account is that /plan tags an item and /next declines to build it. Those are different mechanisms with different primary users, and the shipped text would not lead a reader to your version.
**Why this is more than a documentation tidy.** `[freeform]` was proposed and rejected as the vehicle for solo work at two separate decision points, both times on the ground that it "means something else" — see [runs-alone-premise-never-tested]. Those rejections were reasoned from the shipped definition. If the shipped definition is not what the tag is for, both rejections were argued from a false premise, and the mechanism built in their place inherits that.
**To settle at processing:** whether the shipped description is rewritten toward the ad-hoc-session-type meaning, whether both meanings are real and need separating into two things, and what `done.md`'s router actually needs in order to recognise a freeform session — since on your account that is the tag's main job and the shipped docs barely mention it.
**Files (rough):** `plugin/throughliner/docs-b/skill-nonspecific-rules.md` (the flavor table and the `[freeform]` paragraph), `plan.md` (the placement rules), `done.md` (the router, if freeform needs recognising there). Shipped, not host-only. Relates to [runs-alone-premise-never-tested] and [chip-replies-are-indistinguishable-from-user-authorship].

#### "Nothing blocks it" is read as a dead end instead of an instruction to file the blocker, and no amount of restating the rule has fixed it [nothing-blocks-it-read-as-a-dead-end]
Captured by you (2026-08-13), in your words: a capture is placed, blocking them, so that processing will pick up that capture and lift the items again — if there is no item in queue, MAKE ONE. You state you have explained this five to ten times in the last month and were close to giving up on getting it across.
**The rule is already shipped, which is what makes this a real finding rather than a request.** `plan.md`'s keep-step says "if that blocker is not already a queue item, write it into Unprocessed first". `skill-nonspecific-rules.md` says the same for something waited on in the world: file it as its own item in Unprocessed first, then name it. Both are unambiguous. The failure is not a missing rule.
**The exact sentence where it failed, from `d73b53f`:** "below-the-line needs a named blocker and nothing blocks the rename." That reasoning closed the route and led directly to designing the `Runs alone` marker — a new shipped field, wired into five files. In that specific case a blocker was already available with no invention at all: [rename-to-throughliner] is itself a queue item, so anything that must not share its run could have named it and been lifted when it shipped.
**Why restating the rule will not fix it, which is the actionable part.** The rule is written from the *placement* side — "when placing an item below the line, name its blocker; file one if it does not exist." A session does not arrive from that side. It arrives holding a problem — *I need to isolate this work* — asks whether below-the-line is available, concludes "nothing blocks these, so that route is closed", and begins designing a new mechanism. It never reaches the placement rule, because it has already ruled the route out. The rule sits one step past where the decision is actually made, so it is read at authoring time and never at deciding time. This is the same shape as [close-cost-depth-line-never-surfaces] and [rung-change-is-never-narrated]: a correct rule with no site at the moment it is needed.
**The recurrence is the evidence and should not be softened at processing.** The user has given this correction five to ten times in about a month. A rule needing that is not being under-explained; its firing site is wrong.
**One refinement rather than an objection.** A manufactured blocker must be real work — "verify the rename landed cleanly" is genuine, and dependents can legitimately wait on it. An item filed purely to hold a position is the misuse [no-field-for-ordering-preference] already flags, so the fix must not read as licence to invent placeholder items.
**To settle at processing:** where the rule needs a second firing site — the likely one being the moment a session concludes that a queue mechanism is unavailable, which is exactly when it should be asking whether it can file the missing piece instead. Whether that can be stated without becoming a general "consider filing an item" nag is the design question.
**Files (rough):** `plugin/throughliner/docs-b/plan.md` and `skill-nonspecific-rules.md`. Shipped, not host-only. Relates to [runs-alone-premise-never-tested] (the mechanism this failure produced), [no-field-for-ordering-preference], [blocked-by-in-unprocessed-is-never-revisited] and [invented-rationale-compounds-past-the-shipped-rule].

#### The superseded-research flag cannot tell a citation of the fallen part from a citation of the surviving part [superseded-flag-has-no-section-granularity]
Observed 2026-08-13 while filing [written-deliverable-length-unaddressed], which cites `instruction-file-bloat-and-subtraction.md`. The digest flagged it correctly by its own rule and the flag was, for this item, a false alarm.
**A `Superseded by:` line can say the file fell only in part, and this one does.** `instruction-file-bloat-and-subtraction.md` records that §1's instruction-count figure was re-validated and found roughly an order of magnitude too tight, and that its other sections "stand". The item that triggered the flag relies only on the subtraction argument, which survived.
**The flag has no way to know that, because it matches on the filename.** So an item citing a surviving section is flagged exactly as loudly as one built on a fallen premise, and the only way to tell them apart is for a human to open both documents and read.
**Why this is worth more than a shrug.** A check that fires on correct work is the failure mode this project has already named twice — at the repealed index-line cap, and in the standing rule that a check which over-claims makes the corpus look guarded when it is only partly guarded. The same argument applies to over-firing: a flag that is usually a false alarm gets learned past, and then the one real one is skimmed too.
**The mitigation used this time, offered as a possible shape rather than the answer:** the item's own prose now names which section fell and which it relies on, so the next reader gets the answer without re-deriving it. That is a convention, not a mechanism, and nothing enforces it.
**To settle at processing:** whether the fix is a convention (an item citing a partly-superseded file states which part it rests on), a format change (`Superseded by:` naming sections, and the digest reporting them), or nothing at all — the flag's text already says it is a prompt to re-read the premise rather than a verdict, so this may be working as designed and simply needing the convention. Weigh doing nothing seriously; it is a real candidate.
**Files (rough):** `plugin/throughliner/scripts/queue_digest.py`, and `skill-nonspecific-rules.md`'s superseded-research paragraph. Shipped.

#### Clearing a conversation does not cancel a slash command queued behind it, and nothing says so [clear-does-not-cancel-a-queued-command]
Filed 2026-08-13 by Claude at its own /done close. Captured by you in substance — you asked, mid-run, whether Claude had seen the `/clear` you ran from remote control, having reasonably read the clear as cancelling what followed.
**What happened.** A `/clear` and a `/next` were sent together. The clear wiped the conversation; the `/next` behind it ran normally against an empty context. From your side the old text was still on screen, so it looked as though the session had ignored the clear — when in fact it had honoured it exactly, and the scrollback you could see was the app's, not Claude's context.
**Why it is worth a queue line rather than a shrug.** Nothing in the method or the app tells a user that a clear is a context operation and not a queue operation, and the two readings are equally natural. The cost is real: a `/next` running immediately after a clear starts a build with none of the planning conversation behind it, which is exactly the fresh-short-session case the method designs for — but only if the user *meant* it.
**To settle at processing, and the routing genuinely is open.** This may be Claude Code's to fix rather than ours, in which case the destination is a GitHub issue rather than a build. What is plausibly ours is smaller: /next's opening could say, where the context is visibly empty of prior discussion, that it is starting cold. Weigh whether that is detectable at all before designing it — Claude cannot reliably tell an empty context from a forgotten one, which is the same blindness recorded in the wind-down re-scan's caveat.
**Files (rough):** none decided. Relates to the fresh-short-session design target in `CLAUDE.md`.

#### `py -c` escapes the script-write guard, and it is the invocation this project's own rules recommend [py-dash-c-escapes-the-script-write-guard]
Filed 2026-08-13 by Claude from a live instance in its own post-close tail. Filed after the last committed close, so it belongs to no committed session record.
**What happened.** During a rezip, the version in `plugin/throughliner/.claude-plugin/plugin.json` was bumped by running `py -c "..."` with a `open(p,'w')` inside it, rather than by using the editing tools. `pre_tool_use` is supposed to block exactly that and did not. The file was read back and was correct, so no damage was done — the finding is the silence, not the outcome.
**The cause, read off the hook rather than guessed.** `PY_INVOCATION` is `\bpython[0-9.]*\b|\bpy\s+-[0-9]`. That catches `python`, `python3`, `python3.13` and `py -3.13`, and misses `py -c`. The write-call detection downstream never ran, because the invocation half of the test failed first.
**Why this is worse than an ordinary pattern gap.** This project's own scripting constraints, in `CLAUDE.md`, tell sessions to use `py` rather than `python` on this machine, because `python` resolves to Inkscape's bundled interpreter. So the guard covers the invocation the rules steer sessions *away* from and misses the one they steer sessions *towards*. And SPEC states the block unconditionally — "whatever the file is, and whether or not it's in the current work's scope" — so a promised guarantee is not being kept.
**The likely fix looks one-line and should still be checked against the false-positive cost.** Widening the alternation to something like `\bpy\s+-` or `\bpy\b` would catch `-c`, but `py` is a short token and the hook's own comments record that this family of patterns has been tuned to avoid firing on harmless commands. Weigh whether `py` alone over-fires before adopting it. `resources/testing/test_pre_tool_use_shell_writes.py` gains a case either way — the suite passed throughout, which is the second half of why this stayed invisible.
**Files (rough):** `plugin/throughliner/hooks/pre_tool_use.py`, `resources/testing/test_pre_tool_use_shell_writes.py`. Shipped, so its close runs the hook suites. Relates to [rule-gate-has-no-site-in-next] — both are cases of a stated guarantee with nothing actually enforcing it.

#### The push's version-clean makes the content stamp report the host as stale immediately after every rezip [push-clean-breaks-the-content-stamp]
Filed 2026-08-13 by Claude, measured live in its own post-close tail. Filed after the last committed close, so it belongs to no committed session record. Host-only.
**The two rules that collide, both correct in isolation.** The rezip bumps `plugin.json` to a `-testN` version and installs that snapshot; the push then resets the suffix to a clean version so a test marker never reaches the remote. Separately, `session_start` publishes a content stamp of the installed plugin, and CLAUDE.md tells sessions to compare it against `content_stamp()` over `plugin/throughliner/` — a match meaning host-side changes are live.
**The measurement, taken this session rather than reasoned about.** Immediately after installing, source and installed stamps both read `b4bb37b9c1b6`. After the push's version-clean and no other edit, source read `654c88680de8` against an installed `b4bb37b9c1b6`. Nothing about the host changed; the only differing byte is the version string inside a file the stamp hashes.
**Why it matters rather than being cosmetic.** The mismatch is documented to mean "it hasn't been reinstalled since the most recent host-side change, so host-side tests aren't live yet" — which is false here, and false in exactly the sessions most likely to be checking, namely the ones right after a rezip. A check that reports a wrong answer in its most common case is the cry-wolf shape this project has repealed measures for before.
**Why it may have gone unnoticed until now.** A rezip is usually followed by a release, which bumps to a clean version *and* reinstalls, so the stamps re-converge. The rezip-then-push-without-release path is what exposes it, and this session took exactly that path.
**To settle at processing, with the obvious fix and its objection.** The natural answer is to exclude `plugin.json` from `content_stamp()`, since the version string is the one field the two rituals deliberately disagree about. The objection is that the stamp is meant to catch edits that bump no version, and excluding a file wholesale means a real change to `plugin.json` — a renamed plugin, an altered description — would then be invisible to it. Weigh excluding only the `version` key against excluding the file.
**Files (rough):** `plugin/throughliner/hooks/session_start.py` (`content_stamp`), `CLAUDE.md` (the stamp's description), `resources/release-ritual.md`. Host-only — consumers never rezip. Relates to [py-dash-c-escapes-the-script-write-guard], filed from the same tail.

#### The plugin ships an output style that the project's own map never mentions [output-style-missing-from-the-project-map]
Captured by you (2026-08-13), in your own words: you have literally never heard of the output style before or seen it in the folder structure. Confirmed against the files in the same exchange.
**What is missing.** `CLAUDE.md`'s "Where things live" tree lists `.claude-plugin/`, `hooks/`, `skills/`, `templates/` and `docs-b/`. It does not list `output-styles/`. The Architecture section above it enumerates "3 project docs, 4 skills, 3 hooks" and never mentions the style at all. `plugin/throughliner/output-styles/concise-throughliner.md` is nonetheless installed with every copy of the plugin and shapes the wording of every message the user reads.
**Why it matters beyond tidiness.** The user has been developing this plugin for months and did not know the component existed. A session opening on this project reads the same map and would not know either, so it can reason about narration behaviour without ever considering the layer that most directly controls it. That is how [concision-build-removed-the-asks] happened: a rule landed in a file outside the map, outside the rule gate's trigger list, and outside the board's growth report.
**The build.** Add `output-styles/` to the "Where things live" tree in `CLAUDE.md` with a one-line description of what it is in plain terms — the instructions that shape how Claude writes in chat. Add it to the Architecture enumeration so the component count is honest. Check whether `SPEC.md`'s feature list and `README.md`'s "What it does" also omit it; if a user-facing feature is missing from either, the SPEC-sync and README-sync triggers apply.
**Files:** `CLAUDE.md` (the "Where things live" tree and the Architecture enumeration), and `SPEC.md` / `README.md` if the check finds the same omission. Amends this project's own always-loaded text, so the build runs the rule gate at authoring time. Relates to [concision-build-removed-the-asks] — the same blind spot, found from the other end.

#### The rule board reports clean while real rule defects ship, so it measures paperwork rather than health [rule-board-measures-paperwork-not-health]
Raised by you (2026-08-13). Your words: these rules have had zero effect, and you asked of what relevance approval is when you have literally never been asked. The evidence assembled below is Claude's, from this session.
**The test this session accidentally ran.** The board was run live and returned 0 of 4 signalling — every rule-bearing commit carrying a gate disposition, no contradictions, no near-duplicates, no retired-term references. In the same session, five real defects in the rule corpus were found by conversation: a bare number shipped into the output style, the turn-by-turn asks removed from every session, the rule gate having no site in the build procedure, the board itself having no trigger, and an entire shipped component missing from the project map. The board caught none of them and reported clean throughout.
**What that means, stated without softening.** The four signals check whether a required artifact exists, whether two artifacts contradict, whether two rules read alike, and whether a rule names a retired word. None of them asks whether a rule is correct, whether it fires, or whether it made anything better. So a clean board is evidence the paperwork was completed, not that the rulebook is in good health — and this session is the demonstration.
**The approval problem underneath it, which is the user's point and is not answerable by tightening the board.** The gate's author and its approver are the same party. Claude writes the rule and Claude writes the line recording that the rule was admitted. The user has never been asked about a single one. `CLAUDE.md` already concedes that neither BORN nor CONTRADICTED can tell an honest disposition from a dishonest one; what this adds is that the user, the one party with an independent view, is structurally outside the loop.
**What to weigh at processing, with no answer assumed.** Options are genuinely open and at least one is repeal. Retiring the board entirely is a serious candidate: it costs maintenance and its clean readings are misleading, which is worse than no reading. Against that, the four checks are cheap and two of them (retired terms, near-duplicates) do catch real staleness when it exists. A third option is to keep the mechanical checks but stop calling the result a health verdict — rename what it reports so a clean board never reads as "the rules are working". A fourth is to put the user in the loop for rules that change what they see, which is the only route that addresses the approval half.
**Vocabulary, which is a real defect and not a quibble.** "Board", "signals", "dispositions" and "BORN / CONTRADICTED / MAINTAINED / REPEALED" are all internal names that reached the user's always-loaded `CLAUDE.md` without ever being explained, and the user was never the intended audience of the output. Whatever survives processing must say plainly who it is for and what, if anything, the user is meant to do when it fires.
**The board reproduced the failure it was built to avoid, and shipped a clause forbidding reconsideration.** `CLAUDE.md` records the board being chosen over a positional cycle, on the grounds that a cycle "is what failed before, with compression permanently downstream of machinery that never ran" — and it adds an instruction not to re-propose the cycle. The board then never ran either, for its whole life, because nothing triggered it. So the argument that defeated the alternative applies to the winner with equal force. The don't-re-propose clause must come out as part of whatever is decided here: it locks out an option on reasoning that has since been falsified, which is precisely what this project's rules about preserving rejected alternatives are meant to prevent.
**Files (rough):** `resources/rule_signals.py`, `CLAUDE.md` (the board's description, its vocabulary, and the don't-re-propose-the-cycle clause). Host-only. Relates to [rule-lifecycle-board-has-no-trigger] — that item gives the board a site, and this one asks whether it should have one at all, so they must be processed together or one will undo the other.

#### The rule-lifecycle board has no trigger, so its silence has never been evidence [rule-lifecycle-board-has-no-trigger]
Captured by you (2026-08-13). Your words: you have no idea how the board works and are concerned it is not working, because you never hear about it. Host-only.
**What was measured, not assumed.** The board was run this session — `py resources/rule_signals.py .` — and it works: 0 of 4 signals firing, all seven rule-bearing commits since the baseline carrying a gate disposition, no contradictions, no near-duplicates, no live references to retired terms. So the silence was accurate. That is the good half of the answer.
**The defect is that nobody could have known that.** Nothing runs the board. It is not in a hook, not in any skill's procedure, and not in the close ritual — it fires only when a person types the command, and CLAUDE.md describes it without ever saying when to run it. So a clean board and a board that has not been run for weeks are indistinguishable from the outside, which is exactly the condition the board exists to remove for rules. It is the siteless-rule pattern this project has now recorded four times, applied to the mechanism that watches rules.
**Settled at processing 2026-08-13: give it the close as its site, on the rule-gate's own trigger.** The close is where commits happen, and BORN and CONTRADICTED read commits, so it is the one moment the board has anything new to say. The hook-suite step already reads `git status` at the close to decide whether to run the test suites, so the shape is proven and needs no new detection.
**The trigger, which is mechanical and needs no judgment.** The close runs the board when the staged paths include `plugin/throughliner/docs-b/`, `resources/self-authoring-rules.md`, `resources/rule-maintenance.md`, or `CLAUDE.md` — the same set the rule gate fires on. Running it at every close was rejected: the board is meaningless on a commit that touches no rules, and this project has repealed measures for crying wolf.
**It reports one line even when quiet, and that is the point rather than a nicety.** Today's run came back clean, and nobody could have known that, because a clean board and an unrun board look identical from outside. A close that says "rule board: four signals, all quiet" turns silence into evidence. A close that says nothing recreates the exact condition that produced this capture.
**Where a signal does fire, the existing behaviour stands unchanged** — it files a capture under the slug the board prints, and is satisfied while an open capture with that slug exists. Nothing here changes what firing means.
**Files:** `plugin/throughliner/docs-b/done.md` and whichever close-family doc carries the hook-suite trigger, plus `CLAUDE.md` — the board's description there gains its trigger, which is the thing it has never had. Possibly a quiet-summary output mode in `resources/rule_signals.py` if the current output is too long for a one-line close report; check before adding one. Host-only — consumers never author method rules and have no board. Relates to [rule-gate-has-no-site-in-next] — the same siteless-mechanism failure, one layer down.

#### There is no way to tell where you are in a long processing run [no-position-signal-in-a-processing-run]
Captured 2026-08-14 at a /plan close, from the user losing her place live. Her words across three turns: "what is going on did we resolve the last item into work or not", "in the middle of what item? what item are we on? that's all I'm asking", and "then what was all that stuff you just did!".
**What happened.** Ten items were handled in one session. Each one produced several turns of discussion, and several produced side-work — a reply drafted and delivered, a capture filed mid-item, a rule gate opened, references repaired after a delete. From outside, that side-work is indistinguishable from work on the item, so a single-word instruction like "delete" was followed by four further exchanges and the user could no longer tell whether the item had been resolved, what item was current, or whether anything was still open.
**A running count is NOT the fix, and the first draft of this capture had it backwards.** It claimed the count was stated once at the start and never again, and offered a running position line as the obvious answer. The user corrected both at the close, in her own words: the count was in fact being stated at random points through the run for no apparent reason, it has since gone, and it is better gone. Claude had been emitting unprompted tallies — "that's five items processed and two skipped" — at moments no procedure asks for, which is noise dressed as orientation. Recorded rather than quietly amended, because a running counter is the intuitive fix and would otherwise be re-proposed by the next session to read this.
**What is actually missing is narrower.** Not how many items, but whether the item in hand is finished and whether what Claude is now doing still belongs to it. Every one of her three questions was that question, never a request for a total.
**What to weigh at processing.** Whether side-work should be announced as side-work when it starts, so a stretch of tool calls after a one-word instruction reads as "still finishing the delete" rather than as something new. Whether a plain "that item is closed" line at the moment an item ends would do it. Neither should reintroduce a tally. Relates to [concision-build-removed-the-asks], [subset-done-has-no-stated-shape] and [claude-md-vocabulary-is-unexplained] — all four are the user unable to read her own position from what the session says.

#### A user has no answer to what happens when Claude deletes something from their queue [no-faq-entry-on-deletion-and-recovery]
Captured 2026-08-14 at a /plan close, from the user asking mid-session what git history does and what was happening to her queue. She is the method's most experienced user and still had to ask.
**The gap.** Deleting a work item is a routine, approved planning move, and the method's own reasoning leans on deletion being safe because git history keeps the text. That premise is never explained anywhere the user reads. A non-coder approving a delete is being asked to agree that something is not worth doing, with no way to know whether "delete" means gone.
**Why it is not covered.** The FAQ has entries on the workflow's moving parts, none on what deletion does or how anything is recovered. SPEC states that git history keeps a deleted item, but SPEC is product truth for the method, not user help.
**What to weigh at processing.** Whether one entry covers it or two — deletion specifically, and the more general "is anything I approve ever unrecoverable" — and whether the answer should name a recovery command or simply say to ask Claude, which is the honest route for a user who does not use a terminal. Ships to consumers, so this is an ordinary FAQ entry rather than host-only. Relates to [own-faq-diverged-from-shipped-template] — decide which FAQ the entry lands in before writing it.

#### This project's own FAQ and the shipped FAQ template have diverged, and the authoring rule sends new entries to only one of them [own-faq-diverged-from-shipped-template]
Filed 2026-08-14 by Claude while processing [self-hosting-auto-detection], from checking a claim that item made rather than from any symptom.
**What was measured.** `FAQ/faq.md` is 413 lines; `plugin/throughliner/templates/faq-template.md` is 460. They are not the same file and their contents differ. The deleted item asserted, in the user's words from 2026-08-01, that in self-hosting the consumer-facing FAQ and the project's own FAQ "are one and the same — they are not two separate artifacts here." That was true of the intent and is false of the files.
**Why it matters more than a stale copy.** `CLAUDE.md`'s FAQ-entry rule tells a batch to carry an entry into `templates/faq-template.md` plus its index line. It does not mention `FAQ/faq.md`. So every FAQ entry authored under that rule lands in the shipped template and not in the FAQ this project actually reads — which is the one a session opens when a workflow question comes up here. The divergence is therefore not drift that happened once; it is produced by the rule, every time it fires.
**The FAQ-sync close gate does not catch it.** That gate requires a disposition line in the LOG saying the FAQ was updated or why it was not needed. A session that updates the template satisfies it honestly and leaves this project's own FAQ untouched.
**What to weigh at processing.** Whether the two should be one file with the shipped copy generated from it, whether the authoring rule should name both, or whether this project's FAQ should simply be replaced by the template on a re-copy — the method already has a re-copy discipline for method-shipped boilerplate. Also worth reading the 47-line difference before deciding: some of it may be consumer-only material that should never be here.
**Files (rough):** `CLAUDE.md` (the FAQ-entry rule), `FAQ/faq.md`, `FAQ/index.md`, possibly `templates/faq-template.md`. Host-only. Relates to [migrate-recipe-freshness] and to the FAQ-sync gate.

#### migrate-checklist.md's show-first explanation, about 60 words — the closest call in the set [cut-rationale-migrate-checklist]
Audit finding 8. "Why this shows before it writes, when nearly everything else writes first" (around line 16). It fails the test cleanly — the instruction to draft the conversion and get approval before writing stands complete without it.
**Why it is flagged as contestable rather than filed as an ordinary cut.** Its working function is to stop a later session reading this as an exception to write-first and "correcting" it; the paragraph says in terms that this is the general test applied, not an exception to it. That is a real risk, and the doc is fetched rather than always-loaded, so the saving is small. A keep here would be defensible.
**Files:** `plugin/throughliner/docs-b/migrate-checklist.md`. Source: the audit run under [rationale-written-into-shipped-docs].

#### [audit] A second rationale pass over the shipped docs, for the shapes the first pass could not see [rationale-audit-second-pass]
Captured by you 2026-08-14, in your own words: make a capture describing an audit that might pick up some more. Filed after the last committed close, so it belongs to no committed session record.
**Why a second pass rather than a re-run.** The first pass, run in this same session, applied one systematic criterion across all thirteen files under `docs-b/` — bolded why-paragraphs and defeated-alternative markers ("was rejected", "was refused", "recorded so it is not…", "Note what this replaces"). That is the shape this defect takes most often and it found about 1,050 words. It is not the only shape, and the audit said so at the time rather than presenting the eight findings as a complete inventory. Re-running the same criterion would return the same set.
**What the first criterion cannot see, which is what this pass is for.** Rationale written as an unmarked paragraph with no signature phrase. Rationale folded into the second half of a sentence that begins as a rule. Worked examples standing in for reasoning — the gate names those as the growth engine and excludes them from its own text, but nothing has checked the shipped docs for them. Evidence sentences attached to a rule as a subordinate clause. And rationale inside fenced blocks, which the first pass did not read as prose at all.
**Same test, applied per paragraph:** delete it and read what remains. A complete, correctly applicable instruction means what was deleted was rationale; an unfinished or misapplicable one means it was operative and stays. Where a sentence is genuinely needed to apply the rule, it is reclassified as operative text rather than exempted.
**Weigh at processing whether it runs whole-corpus or scoped.** Reading all thirteen files paragraph by paragraph is roughly 42,000 words and the judgement is per paragraph, which is why the first pass chose a criterion instead. Scoping it to `skill-nonspecific-rules.md` alone would cost a fraction and capture most of the value, since that file is the only one loaded in every session — but it would leave the fetched docs unexamined by either pass.
**Files:** reads `plugin/throughliner/docs-b/` (all files); the test is in the rule gate in `CLAUDE.md`. Edits nothing; output is captures. Relates to [rationale-written-into-shipped-docs], whose first pass this follows, and to the eight cut items filed alongside it.

#### EDITING-STATE-CONTRACT.md has a live consumer, no maintenance trigger, and an unexamined justification [editing-state-contract-status]
Captured by you 2026-08-14, mid-/next, from your proposal to delete the file. Filed after the last committed close, so it belongs to no committed session record. Three questions, yours, kept together because they are weighed against each other rather than separately.
**What was established before the questions, so they are not re-derived.** Your premise at the time was that the document is vestigial material from when MANIFEST was a doc. It is not: it documents a shipped feature — `pre_tool_use.py` writes a marker before every editing-tool call and clears it after, `session_start.py` sweeps stale ones — and it has a live consumer. Understudy's `src/main.js` (around line 264, under "Throughliner's editing signal") reads `.throughliner/`, scans every `editing-*.json` rather than one file, applies the published reader rule that editing is happening if any marker is active and fresh, and reads the `producer` field with `throughliner` as its fallback. An archived message in Understudy's INBOX shows the v2 format change was communicated deliberately.
**But a live consumer does NOT put a delete off the table, which is your correction and it defeats the first version of this capture.** Claude wrote that it did; you pointed out that what Understudy has is *code* that reads the markers, and that Understudy's Claude can read the hook that writes them. That is the stronger source in every respect: `pre_tool_use.py`'s marker-writing function **is** the format, so it cannot drift, while the document can and has nothing checking it — which is question 2 below. The document would only be load-bearing if Understudy could not reach this repository, and it can: they are sibling folders on the same machine, and the v2 change in fact travelled by INBOX message rather than by anyone reading the contract. So a delete is live, and question 3 is now most of the answer rather than one of three.
Your other point stands untouched and nothing here disputes it: users will not read it, and it is not written for them.
**Question 1 — does the dependency get to be visible from both ends?** Today the citation runs one way: this project publishes a contract and does not know who consumes it, and Understudy's code names the format without anything on this side recording that it does. That is the same one-way-citation shape the superseded-research rule was built for, one project apart. A pointer from Understudy's side, or a note here naming the consumer, would make a v3 change reach the code that depends on it. Weigh against the standing rule that this project does not scan other projects and works only on the folder it opened in — any pointer is something a person writes, never something a session goes looking for. **Note this question survives a delete and may be the only part that does:** knowing who reads the markers matters whether the format is documented in prose or read out of the hook, and it is the one thing neither the code nor the document currently records.
**Question 2 — when does it get maintained, or is it quietly decaying?** Your question, in your words. There is no trigger of any kind on it: it is not in the rule gate's path list, not in the board's growth report, not in the FAQ-sync trigger, and no close reads it. The hooks could drift from the published format and nothing would say so — which is the exact shape of the output-style failure built earlier in this same run, where an always-loaded layer sat outside every watcher and an underived number reached it in silence. Worth noting that a conformance check here is mechanically possible in a way most of this method's checks are not: the fields the hook writes are literal strings, so a test could assert the marker matches the document.
**Question 3 — is a published interface contract standard practice, or our own invention? This is now the load-bearing question.** Yours, and with the consumer objection defeated it carries most of the decision. If it is standard practice, defended by sources, it stays and gains a trigger. If it is our invention, it duplicates what is already in the repository in executable form — the hook code *is* the format, Claude can read it, and it cannot drift the way prose can — and this project has spent this whole session removing second copies of things. Your words for why the code is the better source: it is probably way more accurate too. **This turns on an external fact, so it needs a web search rather than a judgement here** — whether published field-level contracts for local file-based signals are a recognised practice, and what maintains them where they are.
**Files:** unknown until question 3 is settled — possibly `EDITING-STATE-CONTRACT.md` (deleted, or gaining a maintenance trigger), `CLAUDE.md` and `resources/rule_signals.py` (if it gains one), `resources/testing/` (if a conformance test is the answer). Host-only in every branch: consumers neither publish nor read this contract. Relates to [rule-lifecycle-board-has-no-trigger] and to the superseded-research one-way-citation problem.

#### SPEC still describes the output style's cadence rule as "three moments" after the count was removed from the rule itself [spec-names-a-count-the-style-dropped]
Filed 2026-08-14 by Claude at a /done close, from a mismatch noticed during that run's spec check-against and carried to the close rather than acted on.
**What it is.** `SPEC.md`'s output-style paragraph says the style "names the three moments worth speaking at" and lists them. The build in that run removed the count from the style itself, on the ground that "three moments" was a bare number deriving from nothing — the same defect as the repealed index-line cap. The style now names the occasions without counting them.
**Why it was not fixed in the run, and why it is not filed as a contradiction.** SPEC's sentence is still true: the style does name three occasions, and SPEC is describing a list rather than declaring a limit the method must obey. The derivation rule governs limits the method's own text declares, and a descriptive numeral in a product description is not one. So the check passed and nothing halted.
**Why it is worth a look anyway.** The whole subject of that session was a bare figure surviving in a place nobody was watching, and SPEC is read at every planning session and now at every build run too. A numeral sitting in the description of a rule that deliberately stopped stating one is at least worth a deliberate keep-or-cut rather than an unexamined survival.
**The cheap outcome is probably a keep**, recorded so the item is not read as assuming a change: rewriting SPEC to avoid an accurate numeral would be churn, and the honest fix might be one word rather than a paragraph.
**Files:** `SPEC.md`, the output-style paragraph. Host-side but shipped-adjacent — SPEC is this project's own. Relates to [concision-build-removed-the-asks], which removed the count from the style.

#### The rezip's version-bump rule picks a number that already exists whenever a push has run since the last rezip [rezip-bump-collides-after-a-push]
Filed 2026-08-14 by Claude while running a rezip, from the rule misfiring on that very run. Filed after the last committed close, so it belongs to no committed session record.
**The two rules disagree, and each is right on its own.** The rezip's first step says to read the current version and increment the test suffix, "or start at `-test1` if the base carries no suffix". The push's first step deliberately strips the suffix, so the committed version always carries none. Put together, every rezip that follows a push reads a bare version and takes the start-at-`-test1` branch — which names a build that already exists in the plugin cache.
**What that costs, which is worse than an untidy number.** The rezip exists to make the installed host match the source, and the update command matches on the **version string**. Re-installing a string the CLI has already seen is precisely the silent no-op the bump rule was written to prevent: it reports success, re-snapshots nothing, and the session carries on believing it is running new code. That failure has already happened once on this project, on 2026-08-09, and cost most of a session.
**How it was avoided this time, which is not a fix.** The cache directory was listed before choosing a number — `1.20.0-test1`, `-test2` and `-test3` were all present, so `-test4` was used instead of the `-test1` the rule names. That worked because the cache was checked, and nothing in the ritual says to check it. The content-stamp comparison at step 6 would have caught the resulting no-op afterwards, which is a real backstop but a late one.
**What to weigh at processing.** Whether step 1 should read the highest existing suffix from the plugin cache rather than from `plugin.json` (the cache is what the CLI matches against, so it is the authoritative source for this question); or whether the reset should preserve the suffix somewhere the next rezip can read; or whether the start-at-`-test1` branch should simply be deleted, since after the first rezip on any release line it is never the right answer. Also worth deciding whether the release bump resets the test counter, since a new release line genuinely has no prior test builds.
**Files (rough):** `resources/release-ritual.md`, the rezip's step 1. Host-only — consumers never rezip. Relates to the push's version-reset clause in `CLAUDE.md`, which is the other half of the disagreement.

#### The slug in a work item's heading appeared with nothing ever explaining it [slug-never-explained-to-the-user]
Captured by you 2026-08-14, in your own words: you have never actually known what the bracketed part of the title is for — it just randomly emerged one day and you didn't have time to deal with it.
**The gap.** The slug is load-bearing structure: it is what `Blocked by:` resolves against, what the queue lint checks, what a LOG entry names to say which item it built, and what Claude uses to refer to an item exactly in chat. None of that is stated anywhere the user reads. It appeared in their own documents and they worked around it for weeks.
**Why this is not solved by the slug being harmless to ignore.** It is harmless to ignore only once you know you may ignore it. Until then it is unexplained structure in a document the user is asked to read and approve, and the method's own standard is that unreadable is unapprovable.
**The likely shape, to settle at processing.** Either the queue file's own header sentence gains a clause saying what the bracketed name is for and that the user never has to write one, or it becomes an FAQ entry, or both. The queue header is read by anyone who opens the file, which the FAQ is not — so the header is the stronger site and the FAQ is the fuller answer. Note this depends on [faq-entry-criteria] if the FAQ route is taken.
**Files (rough):** `plugin/throughliner/docs-b/setup.md` (which authors the queue header) and the queue header text itself; possibly an FAQ entry. Shipped — every consumer meets the same unexplained brackets. Relates to [faq-entry-criteria].

#### Work-item headings are written long enough to be useless in the outline the user navigates by [headings-truncate-in-the-outline]
Captured by you 2026-08-14, from your screenshot of the queue open in a Markdown reader with an outline sidebar. **Your observation that you navigate by the section titles is what surfaced it; the diagnosis below is Claude's.**
**What the evidence shows.** In the reader's outline every heading is truncated mid-phrase — "Add a lightweight post-close 'done del…'", "Routing communication feedback to me…", "The hook-payload 'under half the cap' …". That outline is how the user actually finds an item; the slug plays no part in their navigation.
**Claude is making it worse, with an instance from the same session.** A heading was rewritten this session to "Retiring a step leaves its artifacts behind, and /plan's build prohibition is drawn by content type instead of destination" — two clauses, and the second one is the half that would identify it. Truncated to the width of that sidebar it says almost nothing.
**Why no existing rule catches it.** The index-line rules govern `LOG/index.md` and say specificity beats brevity, correctly, because that line is read whole. A queue heading is read *truncated*, in a viewer, so the constraint is different and nothing states it: the distinguishing words have to come early, because the end of the line may never be seen.
**What must not be concluded.** That headings get a length cap. This project repealed a proportional cap on measurement and bans bare numbers, and the same reasoning applies — the requirement is front-loading the distinguishing words, not shortening the line.
**To settle at processing.** Where the rule lives (the capture line-format block in `skill-nonspecific-rules.md` is the likely parent, since that is where the heading's shape is specified), and whether the existing long headings are rewritten in the same pass or left alone. Relates to [slug-never-explained-to-the-user] — both are about the queue as the user actually reads it.

#### Claude has no awareness that the user reads these documents in a viewer with an outline [reader-context-in-the-work-cycle]
Captured by you 2026-08-14, in your own words: we need basic awareness of the reader's role attached to the build-cycle information as well.
**The gap.** The work-cycle block in `skill-nonspecific-rules.md` describes how work moves — captured, processed, built, recorded — and says nothing about how the user *reads* any of it. So every rule about writing queue items is authored as though the file were read top to bottom in full, when in practice it is opened in a Markdown reader and navigated by an outline of headings. [headings-truncate-in-the-outline] is one consequence; there are likely others in how blocks are ordered within an item.
**A routing question this must resolve before it is built, and it is the whole difficulty.** The specific reader is one of the user's own projects, and naming it in the shipped rules would put a tool no consumer has into every consumer's session. The shipped version has to be generic — these documents are read in a viewer, navigated by headings, and written accordingly. The specific fact that *this* user reads them in *that* reader is true across all their projects, not just this one, which by the method's own doc-routing test makes it memory rather than any project's `CLAUDE.md`.
**So this is plausibly two things, and processing should say so:** a small generic clause in the shipped work-cycle orientation, and a memory entry for the user-specific fact. Weigh whether the generic clause earns an always-loaded slot at all, or whether it is better carried by the heading rule in [headings-truncate-in-the-outline] alone — which would make this item redundant, and that is an acceptable outcome.

#### The two-limb keep check pushes research into build items, beating the rule that says research is done in planning [research-packaged-as-build-work]
Captured by you 2026-08-14, from a live instance in another project minutes earlier: a planning session proposed splitting an item into "one build item where I do the research and write the findings into `resources/research/`" plus a slimmer `[user]` line. Your framing is the finding — research is never planned into work items, and if research can be done now in /plan then it is. **You also observed this is suddenly happening a lot, which is what the diagnosis below set out to explain.**
**It happened three times in one day.** Twice in this session — research queued into [faq-entry-criteria], and research folded into [shipped-spec-maintenance-rules] — and once in the other project. Both of this session's instances were corrected on your instruction; the SPEC one turned out to need no research at all, because `resources/research/spec-document-standards.md` already answered it.
**Nothing was deleted, and the rule is intact.** `plan.md` still carries "/plan resolves what it can in-session; capture is only for what it can't", with `research` listed first among what /plan resolves now. The defect is not a missing rule.
**Three things make it lose, and the third explains the timing.** It is not stated as a rule about research — it is one word inside a fenced list of six things, so "research belongs in planning" has to be inferred rather than read. It is explicitly hedged by the sentence immediately after it, "A default, not an absolute", which is a standing licence to make the exception exactly where an item seems to need one. And the keep-check pushes the other way with far more force.
**The mechanism, which is the substance of this capture.** To keep an item, `plan.md` requires stating the build in both limbs — which files change and what changes inside them — and calls it a blocking check. When an item's answer is not yet known, the cheapest way to make it pass both limbs is to write "research X, then change Y", which reads as a fully specified build. So the check that exists to keep undesigned work out of Processed is the same check that rewards packaging research as build work. It fires hardest precisely where the answer is unknown, which is where research is needed.
**The timing, confirmed from git rather than assumed.** The two-limb check was hardened on 2026-08-10 in `f8b03ea`, which introduced the wording "This is a blocking check, not a prompt to try harder", the instruction to state both limbs before recommending keep, and the warning that a bare file list is what undesigned work looks like. Before that it existed in a softer form; after it, a keep can be refused. Four days later the pattern is visible across projects. An earlier `git log -S` attributed the change to `989c38b` — that was a false positive, because `989c38b` is the rename that moved the whole package folder and therefore shows every string as newly added. Recorded so the same trace is not run twice with the same wrong result.
**What this is not.** Not a case for weakening the keep check. It stopped undesigned work reaching Processed, which was a real failure with real instances behind it. Two correct rules were put into conflict and the stronger one won where they disagree.
**The shape of the fix, to settle at processing.** The clause belongs on the keep check itself, where the pressure lands, not on the research bullet, which is already there and already ignored: an item that cannot state its build *because the answer is not known yet* routes to doing the research now, in this session, and only what /plan genuinely cannot resolve is captured. Weigh also whether "A default, not an absolute" should go — it is the sentence that licenses the exception, and by this project's own standard a rule qualified into a default is a rule that loses every contest.
**Files (rough):** `plugin/throughliner/docs-b/plan.md` — the keep-check sub-step, and the resolve-now block's hedge. Shipped, not host-only: this fires in every consumer's planning session, and the instance that prompted it was in another project.

#### done.md still says the forward advisory is cleared at the /done close, after that step was repealed [done-md-names-a-repealed-clear-step]
Filed 2026-08-14 by Claude at the wind-down re-scan, from reading both docs in full during this session's close.
**The contradiction.** `done.md`'s advisory section says the advisory "is read at the next /plan's opening to orient where that session starts, and cleared at that session's /done close (done-plan.md)". `plan.md` now clears it at the read that surfaces it, and `done-plan.md` carries no clear step at all — the repeal shipped in `47966bb`, whose index line records it. So a shipped doc names a step in another shipped doc that no longer exists.
**Why it matters rather than being cosmetic.** A session reading `done.md` at its close is told the clear is its job. It will either look for a step that isn't there, or perform a clear that already happened at the opening — and the second is silent. This is the same class of defect as the stale sentence found in `CLAUDE.md` this session about zips: prose left behind when the mechanism it described was retired, which is [retiring-a-step-leaves-its-artifacts] one level up.
**Files (rough):** `plugin/throughliner/docs-b/done.md` — the advisory paragraph's cross-reference and the `clear IF … / keep IF …` block beneath it, which also reads as instructions to the close. Check whether the persist-condition branch is now stated in two places or none.

#### The Claude Code GitHub app cannot be installed for an org from the flow that demands it [claude-code-github-app-org-install-dead-end]
Filed 2026-08-14 by Claude at the wind-down re-scan. **The observation is the user's, from hitting it live while trying to start a cloud session; the diagnosis is Claude's.**
**What happened.** Starting a cloud session against an org-owned repository popped "GitHub app not installed — the Claude GitHub app must be installed on your repository". The app *was* installed, on the user's personal account. Its "Install GitHub app" button led to the personal account's app settings page, which offers no route to install for the organisation that owns the repo. The org's own installed-apps page showed none, and searching the GitHub Marketplace for Claude Code returned no results. The only working route was the app's direct URL, `github.com/apps/claude`, which nothing in the flow mentions.
**Why it is worth reporting rather than just fixing locally.** A non-coder hits a popup that contradicts what their settings page says, follows the button it offers, lands somewhere that cannot resolve it, and has no next step. The user is a non-coder and did stop. By the three-way routing test this is Claude Code itself, so it belongs as a GitHub issue on `anthropics/claude-code`, not as a method capture and not as app work.
**What the work is.** Search existing issues first — knowing the adjacent reports is what lets a new one distinguish itself — then draft the issue and show the exact text before anything is posted. Nothing is filed without the user's explicit yes. Note the account name and repository name are identifying and are scrubbed or generalised in the draft.

#### A file's date in Google Drive means "last synced", not "last edited", and both you and Claude reasoned from it as if it meant edited [drive-dates-are-not-edit-dates]
Captured by you 2026-08-14 at the /done close. **The observation is yours** — you reported that two folders had both been updated today and asked which was real; **the diagnosis is Claude's.** Filed after the last committed close, so it belongs to no committed session record.
**What happened.** You read the project through Google Drive on your phone, saw `docs-b/` and `skills/` both showing as modified today, and reasonably concluded both were live and being edited. The filesystem and git both disagreed: `skills/` was last written on 2026-08-13, three of its four files on 2026-08-10, with no modification in git and no commit since the rename build. The whole project sits inside My Drive, so a sync pass or a re-upload refreshes what Drive displays without any byte inside the file changing.
**Why this is a method finding rather than a one-off confusion.** You work from Drive on your phone routinely, so this will recur, and it produces a report that is honestly made and factually wrong — the worst kind for Claude to receive, because nothing about it looks uncertain. Claude then took the report at face value and answered the wrong question twice before checking the filesystem, which cost several exchanges and visible frustration. The general shape: **a file browser's presentation is not evidence about a repository's contents, and both parties treated it as if it were.** Third instance in one session of that shape, after `throughliner.zip` looking live in Explorer and again looking dead to the fix.
**The likely fix, to settle at processing.** Something small on the handoff-claim provenance rule, which already covers Claude-authored claims but not user-reported file state: where the user reports that a file changed, was updated, or looks new, check `git log` and the filesystem before reasoning from it — and say what was checked rather than contradicting them flatly. Weigh whether this belongs in the always-loaded rules at all or in this project's CLAUDE.md, since it is about how *this* user works. Note the asymmetry that makes it worth having: checking costs one command, while not checking cost this session several exchanges.

#### A working file's `Depth:` lines are not bound to their items, so two written together silently attach to the wrong one [depth-field-has-no-binding-to-its-item]
Filed 2026-08-14 by Claude at the /done close, from a slip in this session's own build working file. Filed after the last committed close, so it belongs to no committed session record.
**What happened.** The `Depth:` field is written under an item's Progress tick, and its binding to that item is positional — nothing names the item it describes. Two items were ticked in the same edit and their depth lines landed together, so `[setup-mid-session-is-unhandled]` carried two depth lines and `[spec-ask-in-build-reads-as-a-violation]` carried none. The close spotted it and read the second line as belonging to the second item, which was almost certainly right — but that was a reconstruction from context, not something the file recorded.
**Why it matters more than it looks.** `done-build.md` instructs the close to read each item's depth field rather than judge it, and to treat a missing field as short while noting the omission as a discipline slip. Both behaviours are defeated here: the field was not missing, it was misattached, so the "missing means short" rule would have written the wrong form of entry for one item and the slip would have been reported against the wrong cause. A fresh short session, which is the design target, has no conversation memory to reconstruct from and would simply have believed the file.
**The likely fix, to settle at processing.** Bind the field to its item explicitly — `Depth: <slug> — short` — so position stops carrying meaning, which is the same principle the queue already applies in refusing to let position encode a relationship. Weigh the cheaper alternative of instructing the close to flag any item whose depth-line count is not exactly one, which detects the fault without changing the format. Relates to [tick-conflates-built-and-confirmed] and [close-cost-scales-with-run-size], both about what the tick does and does not record.

#### The `docs-b` folder name and the `docset: B` stamp outlive the two-docset model they were named for [docs-b-name-outlives-the-two-docset-model]
Captured by you 2026-08-14 at the /done close, in your own words: the docs-b folder is still present, and you thought docset B was no longer needed and had been absorbed as the main method. Filed after the last committed close, so it belongs to no committed session record.
**You are right about the model and the name is the residue.** There is one docset. Docset A was retired on 2026-08-09 and `CLAUDE.md` states plainly that `docs-b/` is it, that nothing picks between docsets, and that no model detection remains. What survives is the naming: the folder is still `docs-b/`, and every file in it still carries a `docset: B` frontmatter line. Both are shaped by a fork that no longer exists — "B" only means anything if there is an A.
**This is the same shape as the zip, on the same day, which is the point worth carrying.** [retiring-a-step-leaves-its-artifacts] found a retired step's output still sitting in place looking live; this is a retired *model's* vocabulary still sitting in every shipped file. Both are the eviction gap that item's amendment now covers for rules and artifacts, and neither is covered for names. Third instance the same day if the `si-plugin` folder name inside the archived zips is counted.
**What makes it work rather than a tidy-up, and why it must not be done casually.** The `docset: B` stamp has a live consumer: `session_start`'s behaviour-rules directive tells a session to self-check that the file it opened carries `docset: B`, so removing the stamp without changing that check breaks the one guard that proves the rules were loaded. `CLAUDE.md` says outright the stamp stays for that reason and that removing it would be churn — so this item is not licensed to remove it by pointing at the name alone. The folder name is the wider change: every skill's `SKILL.md` points at `docs-b/`, `session_start.py` and the other hooks resolve paths into it, `CLAUDE.md` and `SPEC.md` name it repeatedly, and the release ritual zips it. A rename is a hook-enforced-path change, so its scope is traced by grepping the literal string across the repo before any build starts, per this project's own ripple rule.
**To settle at processing.** Whether the folder is renamed at all, given the cost lands on a path every skill resolves and the benefit is clarity rather than behaviour; whether the frontmatter stamp is renamed to something that does not imply a sibling (it needs a value the self-check can assert either way); and whether these are one item or two, since the stamp has a live consumer and the folder name does not. Weigh also doing nothing and writing one sentence in `CLAUDE.md` saying the "B" is historical — the cheapest option, and the one that leaves every future reader asking this same question.
Relates to [retiring-a-step-leaves-its-artifacts] (the rule this is the next instance of) and to `resources/retired-terms.md`, which covers retired terms but does not reach a term embedded in a path.

#### The close costs more the bigger the run, because one LOG entry per item is unconditional while the build's own cost per item is not [close-cost-scales-with-run-size]
Filed 2026-08-14 by Claude, measured at the close of a seventeen-item run after the user asked where that turn's tokens had gone. Filed after the last committed close, so it belongs to no committed session record.
**What was measured.** The close wrote sixteen build entries plus one walk-through entry, 7,650 words in total, and prepended seventeen index lines of roughly 1,800 words. Before writing any of it, it read `done.md` (742 lines) and `done-build.md` in full, and re-read the 134-line build working file as the router requires. So the close's output was dominated by per-item records, and its input by two procedure docs read whole.
**Why this is a finding rather than a complaint.** The per-item entry rule is deliberate and its reason is good: a work item's queue text is consumed when it builds, so the entry becomes the only surviving record, and combining them would trade away per-slug retrievability. Nothing here argues against it. The observation is about **shape**: the close's cost scales linearly with run size and cannot be reduced by anything the run does, while the build's cost per item varies with how much each item actually changes. A seventeen-item run of small edits therefore has a close that costs more than several of its builds put together.
**What might follow, none of it decided.** Whether a short-depth item's entry could be genuinely shorter than it is now — the depth field already distinguishes them, and the seven short entries here still ran 120–200 words each. Whether the index line and the entry are duplicating each other at small sizes, since for a two-line cut the index line nearly restates the entry. Whether `done.md` at 742 lines needs the same subtraction pass the shipped docs just had, given it is read in full at every close of every session. **What must not follow** is a run-size cap: the method has refused that repeatedly and for good reasons, and this is not new evidence for it.
Relates to [rationale-written-into-shipped-docs] (the audit that just cut 104 lines from the shipped docs, of which `done.md` was one) and to [tick-conflates-built-and-confirmed].

#### An outbound report can describe a problem that was fixed days earlier, because nothing requires a live check before sending [outbound-report-not-checked-against-the-world]
Filed 2026-08-14 by Claude from inbound INBOX mail (`2026-08-14-report-page-already-live.md`), which was opened and archived during a /next pre-flight. Filed after the last committed close, so it belongs to no committed session record.
**What the message says.** The flintcraft.tech project reports that `flintcraft.tech/report` has been live and working since 2026-08-06, when the site was first connected to Netlify — and that two invisible Netlify defaults had to be fixed alongside it (form detection off by default since April 2023, and no email notification unless configured). Its correction to us: a report this project sent was dated 2026-08-09, three days after the fix, so it described a problem that no longer existed.
**Why it is a method finding rather than a one-off.** `[report-url-404]` itself was closed correctly on 2026-08-10 by fetching the URL — the check-the-world rule working exactly as designed. The gap is on the *outbound* side: the walk-through lifecycle requires an observable check before recording a `[user]` item as complete, but nothing requires the same check before sending a report to another project describing a problem. So the strong rule guards the closing of work and the weak path guards the sending of claims, which is the wrong way round: a wrong close costs one queue line here, a wrong outbound report costs another project's time.
**The likely shape, to settle at processing.** One clause on the outbound-send flow in `feedback-and-inbox.md`: where a report's claim has an observable check — a URL, a file, a branch — run it at the moment of drafting and say in the report what was observed and when. Weigh whether this is the same rule as the walk-through's check-the-world clause extended to a second site, in which case it is an amendment and spends no slot.
Relates to [cross-project-work-completes-invisibly] (the same blindness in the other direction) and [report-url-404] (the instance).

#### A closing ask that ends on a slash command pre-fills the user's composer, so plan.md's deliberately neutral gate leans toward closing anyway [closing-ask-ends-on-a-command]
Filed 2026-08-14 by Claude from inbound INBOX mail (`2026-08-14-hexboard-trailing-slash-command.md`), raised by Hexboard's user after seeing it live in a /plan session. Filed after the last committed close, so it belongs to no committed session record.
**What the message reports.** The desktop app lifts a trailing slash command out of an assistant message and pre-fills the composer with it. A message ending "…or shall we run /done?" leaves `/done` sitting in the input box as though the user had typed it, one keystroke from being sent.
**Why it is a method defect rather than an app quirk.** `plan.md` specifies the end-of-queue gate as "anything else to capture or discuss, or shall we run /done?" and, in the same breath, requires that ask to be neutral and not lean toward closing. The app turns that exact wording into a pre-filled close. So the doc's stated intent and its own specimen wording are in conflict once the rendering is taken into account.
**The strongest part of the report is that the fix already exists one doc away.** `done.md`'s recommend-next ladder had this same defect and it was fixed: rung 2 now states the next item and stops, explicitly because "a message that ends by asking a question whose answer looks like a command is one keystroke from being run by accident", and a user was caught by it. That reasoning is written down, it is about this precise mechanism, and it never reached `plan.md`. This is a fix applied at one site and not at its siblings.
**Three sites named in the report:** the end-of-queue gate, the checkpoint off-ramp on the last item ("anything else to capture or discuss, or run /done?"), and the rung-6 offer ("or we can stop and run /done").
**Confirmed live in the session that received this.** This /plan session ended two messages on "or shall we run /done?" before the mail was opened, so the specimen behaves as reported from inside this project too.
**Three candidate fixes offered by the sender, least disturbing first:** put the command earlier in the sentence rather than last; drop the slash and name the action in words ("or we can close the session and commit"); or order the options so the non-command branch lands last. Settling which — and whether the answer is a general wording rule rather than three edits — is processing's job.
**Relates to [message-ending-drives-the-suggestion-chip]**, which makes the general point that what ends a message becomes the suggestion chip. This is the specific, evidenced instance of it, with three named sites and a precedent fix; weigh at processing whether they are one item or two.

#### plan.md documents the queue mover's command line without the section argument the script requires [plan-md-mover-invocation-omits-section]
Filed 2026-08-14 by Claude at a /done close, from a command that failed live in this session. Filed after the last committed close, so it belongs to no committed session record.
**What happened.** `plan.md`'s below-the-line lift step gives the invocation as `reorder_queue.py <QUEUE.md path> --move <slug> AFTER <anchor> --marker-after <slug>`. Run exactly as written it exits non-zero with a usage message: the script's own grammar is `reorder_queue.py <queue_path> <section> --move <slug> BEFORE|AFTER <anchor>`, with the section name required before `--move`. The lift ran only after the section was added by hand.
**Why it matters more than a typo.** This session lost one round trip to it, which is trivial. What is not trivial is that `plan.md` presents these as copy-and-run commands and the always-loaded rules class a verbatim command as a paste target — so a session following the doc precisely is the session that fails, and a session improvising succeeds. It also failed at the below-the-line revisit, which is a step /plan runs at every opening.
**Scope to check at processing rather than assume.** The `--move-section` and `--delete` forms in the same doc were run this session and worked as documented, so this may be isolated to the within-section `--move`. Check every invocation `plan.md` and `done-plan.md` print against the script's usage text, rather than fixing the one that was caught.
**Files (rough):** `plugin/throughliner/docs-b/plan.md`, and `done-plan.md` if its invocation has the same defect. Shipped — consumers run these commands too.

#### The step that guarantees INBOX mail gets opened has nothing enforcing it, and it was skipped in the session that wrote this [inbox-open-step-not-enforced]
Filed 2026-08-14 by Claude at its own /done close, as a testing outcome from using the plugin to build the plugin.
**What happened.** `plan.md`'s Step 1 names itself the guaranteed moment mail is read: `session_start` surfaces that messages are waiting and `feedback-and-inbox.md` says what to do with one, but without this step nothing says *when* a message gets opened. This /plan session ran its full opening — digest, recent log lines, below-the-line revisit, placement flags — and did not open the waiting message. It was opened at the /done close instead, only because the close listed the mailbox.
**Why the miss matters beyond one session.** The doc's own stated failure is mail being surfaced every session and read in none of them. That is exactly what happened here, in the project that authors the rule, with the rule loaded. And the message was not incidental: it reported a defect in `plan.md`'s own end-of-queue gate that this session then reproduced twice before reading it.
**The shape of the problem, for processing to weigh.** This is the siteless-rule failure the method already names — except the site exists and was skipped, which is worse evidence than a missing site. `session_start` already knows the mailbox is non-empty, so an unopened message is one of the few things here that is mechanically detectable, unlike most of this project's obligations. Weigh a hook-side check against the standing rule that a gate crying wolf gets worked around, and against whether the /plan opening simply has too many checks competing.
**Relates to [rule-gate-has-no-site-in-next]** (a rule whose site did not fire) and to [closing-ask-ends-on-a-command], which is the message that went unread.
**Files (rough):** `plugin/throughliner/docs-b/plan.md`, possibly `plugin/throughliner/hooks/session_start.py`. Shipped — consumers receive INBOX mail too.

#### Writing a LOG entry can silently destroy an existing one, and the collision is only visible in git status [log-entry-write-can-clobber-an-existing-entry]
Filed 2026-08-14 by Claude at its own /done close, from destroying two committed LOG entries during that close. Captured by you in substance — you asked for it to be filed after it was reported.
**What happened.** This close wrote its entry to `LOG/2026-08-14-plan.md` without checking whether the name existed. It did: a committed entry from an earlier planning session the same day, 110 lines, overwritten in full. The restore was attempted at `LOG/2026-08-14-plan-2.md`, which was also taken by a second earlier session. Both were recovered intact with `git checkout HEAD --`, and the entry finally landed at `-plan-3.md`.
**The proximate cause is Claude skipping a rule that exists.** `done.md`'s LOG-entry-files section already states it: `name already taken -> append -2, -3, …`. Nothing about the method was missing. This item is not a request to add that rule again — restating a rule that was skipped is the failure shape this project has already named twice.
**What is worth fixing is that the collision is SILENT.** A successful overwrite looks exactly like a successful create: the write reports success, the file exists, the index line resolves, and the entry reads correctly because it is the one just written. The only trace is `git status` showing ` M` where `??` was expected — one character, in a list of twenty-odd paths, at a step whose stated job is detecting *out-of-scope* dirt rather than checking the close's own writes. It was caught here by chance.
**Why this project collides more than most.** The filename is derived from the close date plus the session type, so every planning session on a given day competes for the same name. This day had three. A consumer running one session a day would never see it; a self-hosting day, or any day with a morning and an afternoon session, hits it immediately.
**Two shapes to weigh at processing.** A check at the write — confirm the target does not exist before writing, and take the next free suffix — which is mechanically trivial and needs no judgment. Or a check at staging: the close knows which entry files it just created, so a created-file appearing as modified rather than untracked is a detectable contradiction. The first prevents it; the second catches it. Weigh whether both are wanted or whether the first alone is enough, and against the standing preference for not adding steps that fire on correct work.
**One observation recorded without a claim attached.** The Write tool is documented to refuse an overwrite of an existing file that has not been read in the session, and it did not refuse here. Whether that contract works differently than described, or the file counted as read for some reason, is unknown and was not investigated — do not build on this sentence without checking it.
**Files (rough):** `plugin/throughliner/docs-b/done.md` (the LOG entry files section, and possibly the commit core's staging check). Shipped — consumers write LOG entries the same way.

#### The close produces several commits every time, so "closing" is not one event and the tree is never left clean [close-produces-multiple-commits-every-time]
Captured by you 2026-08-14, in your own words: the close "is getting very messy every time with many commits". Filed at the close it describes.
**The observation is about recurrence, not one bad session.** Your word was *every time*, so this is not a complaint about today — it is that the shape repeats, and it has been visible in this project for a while.
**What this session did, as the specimen.** One commit for the close proper. Then a reply drafted and delivered after it, which changed a file and triggered the append-to-the-entry offer, which is a second commit. This capture is a third piece of work arriving after the commit that was supposed to end the session. The tail and this capture were folded into one commit deliberately, so the specimen is two commits rather than three — but only because it was noticed.
**Three designed behaviours produce it, and each is individually defensible.** First, `done.md` states that a capture filed in the post-commit tail is written as a working-tree edit with no separate commit and "rides into the next session's commit" — so a close is *specified* to leave the tree dirty. Second, the session-start hash backfill edits the entries the previous close wrote, which guarantees every session opens dirty regardless. Third, the post-close append offer exists precisely because work continues after the commit, and each append is another commit. This session opened with eighteen uncommitted files from a previous session, which is that design working as written.
**Why it is worth treating as a defect rather than as the cost of the design.** A close that does not end with a clean tree means no session ever begins from a known state, and the "uncommitted changes from a previous session" line at every session start becomes noise nobody reads — the cry-wolf shape this project has repealed measures for before. It also makes the close's own staging check weaker, because that check exists to spot dirt the session did not create, and there is always dirt the session did not create.
**Relates to three open items, and they may merge.** `[post-close-tail-state]` names the post-/done tail as an unnamed state; `[done-delta-close]` proposes a lightweight second close that commits everything since the last one; `[close-cost-scales-with-run-size]` concerns what a large close costs. This item is the user-visible symptom the other three circle; weigh at processing whether it is the parent they should hang off, or a fourth view of one problem.
**Do not resolve it by forbidding post-close work.** Work genuinely does arrive after a commit — a question answered, a reply sent, an observation like this one — and the method already decided that recording it beats pretending the session ended. The problem is the number of commits that produces, not that the work happens.

#### done-plan.md still restates the never-ask rule twice after it was promoted to the always-loaded doc [never-ask-restated-in-done-plan]
Filed 2026-08-14 by Claude during the build of [promote-never-ask-user-item-done], as adjacent-work discovery. Rides this session's commit.
**What was found.** That item counted six statements of the rule — four in `next.md`, one in `plan.md`, one in `done.md` — and its file list named only those three docs. `done-plan.md` carries two more that neither the source audit nor the processing session saw: "Identify completed items from what the session can already see — never by asking. There is no completion ask anywhere in a `[user]` item's life", and, at the end of its gap paragraph, "a completion ask is exactly what this removed. Don't reintroduce one under any wording."
**Why it was missed, which is the part worth keeping.** The source audit stated as its own limit that it was never supplied `done-plan.md` and four sibling sub-docs. So this is not a new kind of miss — it is the known blind spot producing exactly what [redundancy-audit-did-not-cover-subdocs] predicted, and that audit item should find it independently.
**What it needs.** The two prohibitions come out; `done-plan.md` keeps its detection table, its observable-check paragraph and the statement that the gap is the fallback rather than a hole to plug, all of which are site-specific. The promoted always-loaded rule now states the action positively, so a prohibition restated here is the shape the promotion was meant to end.
**Not built in the run that found it**, because `done-plan.md` was not in that item's file list and folding it in would have been scope the queue never agreed.

#### done-build.md and done-audit.md carry near-identical route-to-Unprocessed steps, restating each other and the canonical write-first test [route-to-unprocessed-duplicated-across-close-subdocs]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 1. Rides this session's commit.
**What was found.** `done-build.md:47–60` and `done-audit.md:27–44` both open their routing step with the same justification — that a capture in a git-tracked QUEUE.md is recoverable without the user's help, so it is a write-first case, the user contests by number after the fact, and the step does not wait before writing. Both then carry an identical fenced "the RECORD this step sweeps" block, down to the line saying conversation memory is a same-session bonus pass and never a source the step depends on.
**Why this is the worst ratio the sub-doc pass found.** It is duplication twice over: the two sub-docs restate each other, and the justification is separately canonical in the always-loaded write-first rule, which carries the recoverability test the copies assert without stating.
**What is genuinely site-specific and must survive.** `done-build.md` sweeps the build working file's notes and any fix a build check surfaced; `done-audit.md` sweeps observations outside the audit's criteria and process issues, and states that the findings themselves were already appended during the audit. Those differ and are the reason two steps exist at all.
**To settle at processing.** Whether the shared half moves into `done.md` as one step both point at, or is cut to a pointer at the canonical write-first rule in each. The first is more work and removes more text.

#### The depth field is defined in three places [depth-field-defined-three-times]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 2. Rides this session's commit.
**What was found.** `next.md`'s per-item completion step, `next-build.md:144–148` and `done-build.md:116–125` each state that every ticked item records `Depth: short` or `Depth: full — <trigger>`, that short is the default, and that the field is written at the tick rather than loosely during the build.
**What each uniquely carries.** `next.md` is the site where the field is actually written. `done-build.md` adds the only genuinely new half: that a ticked item with no depth field is read as short and noted at the close as a discipline slip rather than passing silently. `next-build.md`'s copy adds nothing the other two do not.
**Why it is worth doing.** This is the shape the audit predicted at the delegation boundary and could not check. Three statements of one field is also the drift shape this project fights — each copy is a chance for the values to diverge silently.

#### next-audit.md restates the bulk-approval inversion's justification [next-audit-restates-inversion-justification]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 3. Rides this session's commit.
**What was found.** `next-audit.md:66–69` explains that presenting findings as one numbered set is the bulk-approval inversion of one-at-a-time, and why: the findings are a deterministic result set produced by criteria the user already approved. The always-loaded inversions block states exactly that, and names an audit's findings as its worked example.
**What is site-specific.** The mechanics — state the count upfront, list each with number, observation, file:line and why it matters, ask for whole-set approval or contested numbers — plus the observation that this keeps the always-show rule intact, since the user reads every finding's wording before any of it is filed. Those stay; the argument for why it is an inversion is what duplicates.
**Weigh at processing whether the contested-findings step below it is a second instance**, since the canonical block already says contested items then go one at a time.

#### done-build.md restates next.md's no-subset rule in full [done-build-restates-no-subset-rule]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 4. Rides this session's commit.
**What was found.** `done-build.md:153–158` and `next.md`'s present-the-run step carry the same instruction, the same reason (the cleared-to-run line is the run bound, the user sets it at /plan, and Claude has no gauge of context filling at all) and the same exception (a behaviour-based no-progress stop, never a number).
**The complication that makes this a judgment rather than a straight cut.** The two fire at genuinely different moments — `next.md` forbids proposing a subset at a run's start, `done-build.md` forbids writing one into the forward advisory at a close. A session at one moment does not read the other doc. So this may be the promote-to-always-loaded shape rather than the delete-one shape, and the two candidates should be weighed against each other rather than assuming a cut.

#### done-plan.md restates the retired-headers rule [done-plan-restates-retired-headers]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 5. Rides this session's commit.
**What was found.** `done-plan.md:110–113` says not to reintroduce `Blocks:` / `Depends on:` headers, that the one dependency field is `Blocked by: [slug]` written on the item that is held, and that everything else stays prose slug-references. The always-loaded work-item-states section states all of that, more compactly.
**The one new half.** That the field is lint-checked precisely so it cannot go stale the way those headers did. Worth keeping at the site where the marker is placed.
**Rated low.** Four lines. Worth doing in a pass that is already open in this file, not on its own.

#### done-plan.md restates the four-way routing discriminator [done-plan-restates-routing-discriminator]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 6. Rides this session's commit.
**What was found.** `done-plan.md:208–215` carries a table for what goes above the marker when nothing blocks an item, what to do when an item waits on something in the world, and what stays in Unprocessed because you cannot yet say what it would build. That reproduces both the canonical work-item-states discriminator and the don't-under-file half of the `[user]` matched pair.
**What is site-specific.** This is where the `Blocked by:` line is actually written, and the paragraph beneath the table — that the slug is what makes the below-the-line revisit a single check rather than an interpretation, and that it closes the failure where an item waited on something nobody had filed — is `done-plan.md`'s own and must survive.
**Weigh carefully rather than cutting flat.** The table is the operative form at the moment of placement, which is the same argument that saved `next.md`'s routing map from the flavor-table cut.

#### done-plan.md restates dependency ownership [done-plan-restates-dependency-ownership]
Filed 2026-08-14 from the `[redundancy-audit-did-not-cover-subdocs]` audit, finding 7. Rides this session's commit.
**What was found.** `done-plan.md:126–128` says Claude reorders and narrates rather than asking, and that the user owns keep/delete and scope but not order. Both halves are canonical in the always-loaded dependency-ownership section.
**The site-specific half.** That order is low-stakes and reversible, so the narration is the catch-point where the user can redirect. That is an argument about this step, not a restatement.
**Rated low**, and in the same file as findings 5 and 6, so all three suit one pass.

#### A reversibility claim settled at processing was never checked against the world, and the build hit the exception [processing-asserts-reversibility-without-checking]
Filed 2026-08-14 by Claude at its own /done close, as a testing outcome from using the plugin to build the plugin. Host-only in its example, general in its shape.
**What happened.** [delete-codex-port-from-history] was settled at processing with a careful, well-reasoned paragraph choosing the cheap operation over a history rewrite, and it recorded that the cheap one "is also the reversible one" because dropped commits stay recoverable from the reflog until garbage collection. That is true of commits. The worktree being deleted held 722 lines of uncommitted work across 24 files plus two untracked files, none of which is a commit and none of which the reflog holds. The build halted, surfaced it, and the user chose to discard the work after being told plainly it could not be recovered.
**Why the processing session could not have known, and why that is the point.** Nothing in the keep-step asks a session to look at the thing it is about to destroy. The two-limb buildability test asks whether the item says what changes inside the files it names — which this item did, precisely and correctly. A reversibility claim is a claim about the *world*, not about the item's own specification, and the method has no check that reaches it.
**The shape it shares with other findings here.** [runs-alone-premise-never-tested], built in this same run, is the same failure one layer up: a plausible sentence about what git would or would not recover, written at processing, quoted forward for days, and refuted the moment anyone tested it. Two instances now, both about git recoverability specifically.
**To settle at processing, and the obvious fix may be too broad.** A rule requiring every destructive item to inspect its target before being cleared would fire on a great deal of work that destroys nothing. Weigh instead whether the trigger is narrower: an item whose own prose *asserts* that an operation is reversible or recoverable earns a check of that assertion before it clears. That keys on something visible in the item's text rather than on judging what counts as destructive.
**Do not read the build's halt as the system working.** It worked because the build happened to run `git status` in the worktree before removing it, which no step required.

#### `sed -i` reaches the working tree despite the rule banning it and the hook meant to stop script-writes [sed-i-escapes-the-script-write-guard]
Filed 2026-08-14 by Claude at its own /done close, as a testing outcome. Rides this session's commit.
**What happened.** During this run a shell command was issued that began `sed -i '' -e '' QUEUE.md`, as an accidental inclusion alongside the queue-mover invocation. `CLAUDE.md`'s file-safety rules name `sed -i` explicitly among the writes that must never be used, and `pre_tool_use` blocks writing a project file through a script rather than through the editing tools. Neither stopped it. The command ran, rewrote QUEUE.md in place, and was harmless only because the empty scripts made no change — verified afterwards by diffing the file, which showed only the intended item deletion.
**Why this is worth an item rather than a note.** The stated protection is two-layered — a rule Claude reads and a hook that blocks — and both layers missed the same invocation. A rule that is read and then violated in the same session is evidence about the rule's reach; a hook that permits the exact command its rule names is evidence about the hook's pattern.
**Relates to [py-dash-c-escapes-the-script-write-guard]**, already in Unprocessed, which reports the same guard being escaped by a different invocation. Weigh at processing whether these are one item: two instances of one gap is a stronger case than either alone, and the fix — whatever detects a write-shaped shell command — is likely shared.
**Note what is not claimed.** Nothing here says a hook could catch every such invocation. The narrower question is whether the two named cases are reachable.

#### A halt written for a non-coder used four pieces of jargon in its first sentence [halt-narration-used-unexplained-jargon]
Mixed authorship: the failure was Claude's, and you reported it in your own words — "I don't understand". Filed 2026-08-14 at the /done close, as a testing outcome.
**What happened.** A build halted to surface that a folder about to be deleted held unrecoverable work. The message opened with "the Codex port worktree has uncommitted work in it", then used *reflog*, *commits*, and *branch ref*, and offered three options phrased in the same vocabulary. You said you did not understand it. The second attempt — the same decision explained as "a second working copy of this same project, sitting next to it", with the loose edits described as existing "only as loose files on your disk" — was understood immediately and you decided in one turn.
**Why the guard did not fire.** The always-loaded vocabulary rule asks whether a term names something in *this user's* world, something you could show them. In a project whose subject matter is the method, that test resolves generously — a worktree genuinely is a folder that can be opened. So the rule permitted every term. What it does not weigh is that a *halt* is the one moment the user has no context to lean on: the run stopped, something is wrong, and a decision is needed immediately.
**The shape to weigh at processing.** Possibly a clause on the vocabulary rule: text written at a halt or a stop states the situation in terms that need no method vocabulary, because a user meeting a halt is being asked to decide rather than to follow along. Weigh against the risk of a rule that fires on every alarming-sounding moment.
**Worth noting the cost was one extra turn**, which is cheap. The reason to file it anyway is that a consumer meeting the same halt has strictly less context than you do, and no way to ask a question that gets a second attempt from someone who knows the material.

