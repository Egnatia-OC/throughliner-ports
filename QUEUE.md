# QUEUE

Two sections. **Processed** — agreed work, ordered top-to-bottom; /next builds from above the `--- Cleared to run above this line ---` marker. **Unprocessed** — captured, not yet processed; the next /plan weighs each item. Every entry in either section is a `#### ` heading (its description) with a `[slug]` at the end of that line and its rationale beneath; an entry in Unprocessed is a **capture**, and it becomes a **work item** when /plan keeps it into Processed. A leading `[audit]` / `[user]` tag names how it's executed; no tag means a build. An item carrying a security or privacy risk gets a `Red flag · State: …` marker — the flag rides the work.

**Authoring a rule for the method's own text? The rule gate is in `CLAUDE.md`, always loaded — run it before you write.**

## Processed

#### Refer to a third person in committed docs by their GitHub-published identity; unpublished names still scrub out [github-identity-naming]
**Your idea, 2026-08-22; scoped 2026-08-24 on your decision to the narrow buildable rule**, with the full multi-user identity layer split off to [multi-user-identity-layer]. Your reasoning: first-name habit was designed for the private desktop environment, but the method's documents are committed and can be public, so the register should follow what each person has chosen to publish on GitHub — username, pronouns where supplied, first name only where they put it there. More consistent with people's wishes where privacy is concerned.
**The rule, as an amendment to the scrub checklist:** when a session needs to refer to a person other than the project's own user in a committed document, it uses that person's GitHub-published identity as they publish it; anything they have not published — including a real name GitHub doesn't show — is still rewritten away at the same usefulness, exactly as the checklist already requires. The test is "did they publish this themselves?", which is what makes writing it consistent with the checklist's purpose rather than an exception to it.
Rule gate: run — amendment to the scrub checklist's personal-names line in skill-nonspecific-rules.md; no freestanding rule is added and nothing else is evicted. The grep for the line's distinctive words found every carrier; `resources/plugin-behaviour-retired.md` also carries it and is excluded as history.
--- Build block ---
Changes: `plugin/throughliner/docs/skill-nonspecific-rules.md` — the scrub checklist's "personal names" line gains the published-identity arm: a third person is referred to by their GitHub-published identity; names and details they have not published are rewritten away as before. `plugin/throughliner/templates/faq-template.md` (and its index line in `faq-index-template.md` if one names the scrub entry) — the scrub-gate FAQ entry gains one sentence saying collaborators appear by their published GitHub identity, never by private names; re-copy templates into `FAQ/`. Check `plugin/throughliner/scripts/scrub_sweep.py`'s docstring mentions of personal names and update the wording only if it describes the checklist's rule; its shape-matching behaviour is untouched.
Acceptance: the checklist line carries both arms (published identity written as published; unpublished rewritten away); FAQ/ files byte-match the templates; scrub_sweep.py behaviour unchanged.
Refused: reading identity from git config or the remote automatically — that is the identity layer, split to [multi-user-identity-layer]; making published-identity mandatory for the project's own user — this rule reaches third persons only.
--- End build block ---
**Files:** `plugin/throughliner/docs/skill-nonspecific-rules.md`, `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md` (check at build), `FAQ/faq.md`, `FAQ/index.md`, `plugin/throughliner/scripts/scrub_sweep.py` (docstring wording only, if needed). SPEC.md is excluded: its clause is written by this planning session.

#### Ordering ask stops naming an order it may not run, and leads with the red flag when one tops the queue [ordering-ask-wording]
**Captured by you 2026-08-24, processed the same session.** Two facets, both yours; the replacement wording is Claude's. First: answering "most-unblocking-first" feels like not quite approving the real order — the named default often isn't what runs, since when nothing unblocks anything the order comes from the fallback rules, so the yes approves an order that wasn't going to happen, and there is no consumer-safe name for the fallback ("ladder" is internal vocabulary). Second, from a live instance in another project: when an uncleared red flag tops the order, the neutral "start with the flag, or something else you'd rather prioritise?" reads too symmetric — the ask should state the flag comes first unless something else is definitely more important.
**The fix, agreed:** the default ask becomes "Anything you want to prioritise, or shall I pick the order?" — approval attaches to Claude choosing, which is what actually happens, and the floor narration that already fires names the picked order in plain words. The red-flag case gets its own asymmetric wording: the flag stated as what comes first, escape worded stronger than a preference. The mail variant keeps its shape and drops the order name.
**SPEC sentence written this session** (processing-flow paragraph), per product-truth-ahead-of-the-build.
Rule gate: run — amendment to plan.md's beat-2 ordering ask, rewording it in place; the old wording is repealed by the same edit, so no new rule is added and nothing else is evicted. The grep for "most-unblocking" found every carrier site; the Files line is derived from it.
--- Build block ---
Changes: `plugin/throughliner/docs/plan.md` — the beat-2 ordering ask, the Step 1 specimen message, and the mail variant all reworded: default ask "Anything you want to prioritise, or shall I pick the order?"; add the red-flag arm (when rung 1 is live, the ask leads with the flag as what comes first unless the user names something definitely more important); mail variant "process that first, or shall I pick the order?". `plugin/throughliner/docs/feedback-and-inbox.md` — the mail-variant sentence matches. `plugin/throughliner/templates/faq-template.md` (and `faq-index-template.md` if its line quotes the question) — the entry explaining the opening question rewritten: the default is Claude picking and naming the order after, with the red-flag exception; then re-copy templates into `FAQ/`.
Acceptance: grep for "most-unblocking-first" returns no hits in shipped docs, templates or FAQ; plan.md's beat-2 carries both arms; FAQ/ files byte-match the templates.
Refused: naming the fallback order inside the question — no consumer-safe name exists, and a named order the session doesn't run is the defect being fixed.
--- End build block ---
**Files:** `plugin/throughliner/docs/plan.md`, `plugin/throughliner/docs/feedback-and-inbox.md`, `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md` (check at build), `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its sentence is written by this planning session, not the build.

#### Merge rule states incorporation in its operative text: the host carries the folded facts, a pointer is never the incorporation [merge-rule-incorporation]
**Raised by you 2026-08-24, processed the same session.** The instance, recorded here because the admission test requires one: a consumer report ([multi-person-attribution-gap]) was folded into [multi-user-identity-layer] as a summary plus a pointer to the archived mail file, and you corrected it — folding is incorporation of the actual facts, since reasoning across two items' content fails once the facts sit in a file the queue read never opens. The merge rule as shipped says "REWRITE the host item" and can be satisfied by a rewrite that points instead of carrying, which is what happened.
**The wording is designed to survive the delete-and-read maintenance test:** the requirement rides the instruction itself — "rewrite the host so it carries the folded item's facts in its own text; a pointer to another file is extra information, never the incorporation" — so deleting the clause leaves a loosened rule and the test classifies it as operative, not rationale. The "because Claude can't reason across two files" sentence stays out of the rule and lives in this item and the session record.
Rule gate: run — amendment to plan.md's merge/supersession block, reworded in place; no new rule is added and nothing is evicted. The operative clarification is admitted on the recorded misapplication above, per the reclassify-don't-exempt rule.
--- Build block ---
Changes: `plugin/throughliner/docs/plan.md` — in the fold/merge block of the recommend step, the MERGE arm becomes: rewrite the host item so it carries the folded item's facts in its own text — a pointer to another file is extra information, never the incorporation — and state what came out.
Acceptance: the merge arm's delete-and-read result — removing the new clause leaves a rule satisfiable by a pointer, so the clause reads as operative; no "because" clause appears in the block.
Refused: a separate why-sentence in the rule ("Claude can't reason across two files") — rationale belongs to the record, and the reclassified clause carries the requirement without it.
--- End build block ---
**Files:** `plugin/throughliner/docs/plan.md`. No FAQ entry: the change alters how Claude performs a merge, not anything a user does. No SPEC sentence: SPEC does not describe the merge mechanics.

#### Walk a just-filed `[user]` line immediately where it clears a red flag or unblocks the session — sanction made explicit for weaker models [walk-user-line-at-filing]
**Raised by you 2026-08-24, from two live instances in another project running this method:** during /plan, the session filed a `[user]` line and immediately walked you through its steps (locking a Discord channel that closed a live exposure), twice in one conversation. You liked it and asked how the method allows it without clobbering the file-every-user-work rule — and then named the sharper concern: the current sanction is composed by inference across three rules (the lifecycle bullet, the keep-step's trying-is-free note, sequencing ownership), which the strongest model performed and a weaker model may not. The method's design target is the weaker model, and this failure mode is invisible by construction — filing-and-waiting looks correct, so no failure would ever be recorded.
**The clause, agreed:** the always-loaded lifecycle bullet gains one sentence — a filed `[user]` line may be walked the moment it is filed, with the user present, where walking it now clears a red flag or unblocks work this session is doing; the line is written before its first step is driven, so an interrupted walk survives in the queue. File-first is operative text, not rationale; "with the user present" keeps it out of unattended runs; nothing existing is removed, so a session that never takes the permission still behaves correctly.
**SPEC sentence written this session** (processing-flow paragraph), per product-truth-ahead-of-the-build.
Rule gate: run — amendment to skill-nonspecific-rules.md's walk-whenever-reached lifecycle bullet; no freestanding rule, nothing evicted. Admitted on the two live instances plus the design-for-the-weaker-model principle; the usual more-than-once failure test is answered by the failure being invisible by construction, stated rather than waived silently.
--- Build block ---
Changes: `plugin/throughliner/docs/skill-nonspecific-rules.md` — the `[user]` lifecycle bullet ("Walk a `[user]` item through whenever it is reached...") gains the sentence: "A filed `[user]` line may be walked the moment it is filed, with the user present, where walking it now clears a red flag or unblocks work this session is doing; the line is written before its first step is driven, so an interrupted walk survives in the queue." `plugin/throughliner/templates/faq-template.md` (index line in `faq-index-template.md`) — a short entry: Claude may walk you through a step during planning, right after writing it into the queue, when it clears a risk or unblocks the session; the queue line is written first so nothing depends on the conversation. Re-copy templates into `FAQ/`.
Acceptance: the bullet carries the new sentence with file-first and user-present as operative conditions; FAQ/ files byte-match the templates; unattended-run behaviour is untouched (next.md needs no edit — the reached-in-order rule stands).
Refused: siting the clause in plan.md alone — the bullet fires in all skills and /done can meet the same moment; a general permission without the red-flag/unblocking limbs — unbounded, it would invite walking every filed line immediately and re-create mid-session interruption.
--- End build block ---
**Files:** `plugin/throughliner/docs/skill-nonspecific-rules.md`, `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its sentence is written by this planning session.

#### Walk-throughs read the record before acting: check LOG/ for the item's slug, resume after what it shows done [walkthrough-repeats-completed-steps]
Found by Claude 2026-08-23, in the run that hit it; **kept 2026-08-24 on your agreement to the two-part design.** The [competition-comparison-article] walk-through re-ran its drafting step from scratch — the draft had been written, approved by you and delivered to the site project the previous day, all recorded in `LOG/2026-08-22-competition-comparison-article.md` and on `INBOX/sent.md`. Roughly an hour of drafting and three rounds of your editing were spent, and the collision surfaced only because `INBOX/sent.md` was opened for an unrelated reason. The item was legitimately still open (its last steps are yours), so a multi-step `[user]` item can be partly done — and nothing sent the session to the record first.
**The design, and why the heavy direction lost.** A walked item's LOG entry already appends actions as they happen (the walk-through record section), so progress IS recorded; the gap is only that nothing reads it before acting. A separate progress-tracking artifact was refused: it would duplicate what the record holds, and this method has repeatedly refused stored position in favour of reading the record. The two light parts, combined:
1. the always-loaded `[user]` lifecycle bullet gains the record check — before walking, list `LOG/` for records under the item's slug, read any found, say which steps the record shows done, and resume at the first that isn't;
2. the generated build view carries, for a `[user]` item, a "records on file under this slug" line — the digest already resolves slugs against LOG filenames, so the machinery exists, and the unattended run sees the flag in the one artifact it reads.
**SPEC sentence written this session** (the no-completion-asks paragraph): the record joins the observable check as a thing a session reads instead of asking.
Rule gate: run — amendment to the same walk-whenever-reached lifecycle bullet [walk-user-line-at-filing] amends (both ride one parent, built in one run); no freestanding rule, nothing evicted. Admitted on the recorded 2026-08-23 instance.
--- Build block ---
Changes: `plugin/throughliner/docs/skill-nonspecific-rules.md` — the `[user]` lifecycle bullet gains: "Before walking, list `LOG/` for records under the item's slug and read any found; say which steps the record shows done, and resume at the first that is not." `plugin/throughliner/scripts/generate_build_view.py` — for a `[user]` item, emit a "Records on file: <filenames>" line in its block when LOG/ holds records under its slug (reuse the digest's slug-resolution, including kind and numeric suffixes). `plugin/throughliner/docs/next.md` — where the walk-through lifecycle opens, one cross-reference to the record check (check at build whether its opening already defers to the bullet; add nothing that duplicates it). `plugin/throughliner/templates/faq-template.md` (index line in `faq-index-template.md`) — short entry: a walk-through picks up where the record shows you left off rather than re-doing steps; re-copy templates into `FAQ/`.
Acceptance: the bullet carries the check as operative text; the view shows a Records-on-file line for a `[user]` item with a matching LOG record and none otherwise (exercise both against this project's own queue); FAQ/ files byte-match the templates.
Refused: a walk-through progress artifact or per-step state file — duplicates the LOG entry's walk-through record and stores position, which this method refuses; relying on the run remembering the rule without the view line — the view is what an unattended run actually reads.
--- End build block ---
**Files:** `plugin/throughliner/docs/skill-nonspecific-rules.md`, `plugin/throughliner/scripts/generate_build_view.py`, `plugin/throughliner/docs/next.md` (cross-reference only, check at build), `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its sentence is written by this planning session. Relates to [log-record-kind-suffix] (shipped), whose suffix rule the slug-resolution must honour.

#### Claim-testing at the keep-step is bounded to the smallest settling observation — producing the deliverable is the build, not verification [plan-claim-testing-proportionality]
**Raised by you 2026-08-24, from a live instance earlier the same day in another of your projects:** during /plan, the session completed a logo SVG design in full to test its claims, then reverted it and queued the item as a build. Your reaction, and both halves are costs: it dangles finished work in front of the user and takes it away, and it was token-heavy for something resulting in no surviving work. You do not want claim-testing prohibited — checking an item's claims before keeping it stays right.
**The diagnosis.** The keep-step requires checking a claim an item rests on, and names /plan as the site where trying is free ("where running the command is harmless, run it"). Nothing bounds how much trying settles a claim, and the two-limb check asks for a *description* of the build, never a demonstration — but that distinction is written nowhere, so a model in the over-verifying direction proves buildability by building. The 5-series research records exactly this failure direction for Opus 5.
Rule gate: run — amendment to plan.md's keep-step trying note ("where running the command is harmless, run it"), bounding it in place; no freestanding rule, nothing evicted. Admitted on the recorded instance above, under the reworded single-instance admission test kept this session ([admission-test-single-instance]).
--- Build block ---
Changes: `plugin/throughliner/docs/plan.md` — the keep-step's trying note is bounded: trying is the smallest observation that settles the claim; a try that produces the item's own deliverable has become the build, and stops — the two-limb check asks for a description of the build, never a demonstration.
Acceptance: the note carries the bound as operative text; delete-and-read on the new clause leaves a loosened rule, classifying it operative; no prohibition on claim-testing is introduced.
Refused: prohibiting claim-testing in /plan — checking claims before keeping is load-bearing and stays; a token budget for trying — a bare number with no derivation.
--- End build block ---
**Files:** `plugin/throughliner/docs/plan.md`. No FAQ entry: the change alters how Claude verifies during planning, not anything a user does. No SPEC sentence: SPEC does not describe keep-step verification mechanics.

#### Admission test drops the more-than-once count: one pointable failure admits, speculation still stops [admission-test-single-instance]
**Your decision, 2026-08-24: the second-failure requirement is arbitrary and blocks needed work.** The count fell to the gate's own standard — the method bans limits with no derivation, and "more than once" is a bare count nobody derived. The evidence from the session that decided it: the requirement blocked or nearly blocked two admissions in one day ([walk-user-line-at-filing], [plan-claim-testing-proportionality]), and both times the disposition wrote around it rather than obeying it, which is a rule already dead in practice.
**What is kept, on Claude's recommendation and your agreement: the question itself survives with the count dropped** — "Has this actually failed, in a way you can point to? A speculative rule stops here." The pointable-instance requirement is the anti-speculation guard; removing the whole question would admit rules written for imagined failures, which is the line the corpus most defends. One recorded instance admits; imagination still does not.
Rule gate: run — amendment to the rule gate's own admission question 1 in CLAUDE.md, reworded in place; no freestanding rule, nothing evicted. Admitted on the two same-day instances above — which also satisfy the outgoing test on its own terms, so the change is valid under either reading.
--- Build block ---
Changes: `CLAUDE.md` — rule-gate question 1 becomes "Has this actually failed, in a way you can point to? A speculative rule stops here." Check `resources/self-authoring-rules.md` for any statement of the more-than-once form and update it to match, recording the repeal there per that file's role as the gate's history.
Acceptance: grep for "more than once" in CLAUDE.md returns nothing; the question retains the pointable-instance clause; self-authoring-rules.md carries the repeal with its date and reason.
Refused: removing question 1 entirely — the pointable-instance guard is what stops speculative rules, and no instance of it wrongly blocking a needed rule is on record.
--- End build block ---
**Files:** `CLAUDE.md`, `resources/self-authoring-rules.md` (check at build). No FAQ entry: host-only — consumers never author method rules. No SPEC sentence: the gate is not product truth.

#### Multi-person sessions: a keep is a state change, and fate decisions belong to the execution-authority holder [multi-user-approval-authority]
**Raised by you 2026-08-24, ahead of the first live multi-person use of the method:** demoing Throughliner in coaching sessions on the project's Discord and collaborating with a friend, through a bridge project that puts several people into one session with one person holding execution authority. Your concern, sharpened in discussion: the bridge gates "state-changing stuff" by owner, but nothing in a plan session looks truly state-changing — so things might get okayed through on that logic which then land in /next where state truly changes.
**The frame that settles it, Claude's, agreed by you: the queue IS state.** A keep at the keep-step is a deferred build authorization — /next exists precisely so cleared work builds unattended with no second approval — so the keep is the state change and /next is its delivery. Gating only file-writes lets a non-owner's "yes, keep it" load the cleared region for the owner's next run: authority crossing the plan→next boundary unnoticed.
**The rule, an amendment to the fate-ownership bullet:** in a session with more than one person, the decisions the rules assign to "the user" — keep or delete, scope expansion, clearing a red flag, approving anything that leaves the machine — belong to the execution-authority holder; anyone present may file captures, since Unprocessed is un-vetted by design and is the safe free-for-all zone. A disposition folds only on the authority holder's own turn, whoever else spoke between — the fold and containment tests key on the reply, and turn-taking (your batched-yes style) is otherwise unaffected.
**Interim for sessions before this ships:** the single-user reading stands — the authority holder is "the user".
**SPEC sentence written this session** (Who-it's-for section).
Rule gate: run — amendment to the dependency-ownership fate bullet ("The user owns whether an item is kept or deleted"), plus a clause on plan.md's fold conditions; nothing evicted. Admission: no recorded failure yet — admitted on your direction to prepare for a scheduled first use, a known imminent case rather than an imagined one, said plainly rather than dressed as an instance.
Relates to [multi-user-identity-layer] — identity says who is named; this says who may decide. Two bricks of the same territory, buildable independently: this one needs no identity machinery, only the definition of whose yes counts.
--- Build block ---
Changes: `plugin/throughliner/docs/skill-nonspecific-rules.md` — the Dependency ownership bullet "The user owns whether an item is kept or deleted, and whether a build expands its scope" gains the multi-person clause: with more than one person in the session, those decisions — and red-flag clearing and approval of anything leaving the machine — belong to the execution-authority holder, while filing captures is open to anyone present. `plugin/throughliner/docs/plan.md` — the fold conditions gain: in a multi-person session, agreement that folds is the authority holder's own turn (check at build whether the containment test's wording needs the same clause or already covers it). `plugin/throughliner/templates/faq-template.md` (index line in `faq-index-template.md`) — entry: several people can share a session; anyone can add ideas to the queue, and one named person's yes is what keeps, deletes, clears risks, or sends; re-copy templates into `FAQ/`.
Acceptance: the bullet carries the clause as operative text; single-user sessions read unchanged (the clause conditions on more than one person present); FAQ/ byte-match the templates.
Refused: gating by file-writes alone — the keep is the state change, which is the gap this closes; blocking non-owners from capturing — Unprocessed is un-vetted by design and participant ideas are the method working; any identity detection — that is [multi-user-identity-layer]'s territory.
--- End build block ---
**Files:** `plugin/throughliner/docs/skill-nonspecific-rules.md`, `plugin/throughliner/docs/plan.md`, `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its sentence is written by this planning session. An INBOX message to the bridge project carrying the gate-the-keep finding is handled in this same session under the send rules, not by this build.

#### Absence claims check a listing that shows hidden files — one clause on the keep-step's read-the-mechanism limb [absence-claims-check-hidden-files]
**Admitted 2026-08-24 on a recorded instance, the first use of the single-instance admission test after its count was dropped — you caught the outgoing count still steering the recommendation.** The instance: [inbox-address-book-absent] was filed on the claim that no address book exists in this project's INBOX; the file exists at `INBOX/.address-book.md` and the filing session's plain folder listing hid the dotfile. The false capture survived a day as plausible work and cost a processing slot; the keep-step's read-the-mechanism rule caught it at processing, which is why the fix is one clause on that limb rather than a new rule or a filing-time check — captures stay cheap and unverified by design, and processing stays the filter. The same inference in a consumer project could spawn migration work for a file that exists.
Rule gate: run — amendment to plan.md's read-the-mechanism limb ("where an item asserts how a mechanism behaves, read the mechanism before describing the build"); no freestanding rule, nothing evicted. Admitted on the instance above under the single-instance test.
--- Build block ---
Changes: `plugin/throughliner/docs/plan.md` — the read-the-mechanism limb gains one clause: an assertion that a file or folder is absent is checked with a listing that shows hidden files, since absence is the one claim a normal listing gets wrong silently.
Acceptance: the clause rides the existing limb as operative text; delete-and-read on it leaves the looser rule, classifying it operative; no filing-time verification is added.
Refused: verifying claims at capture time — captures are cheap by design and processing is the filter; a new freestanding rule — the existing limb already owns this class of check.
--- End build block ---
**Files:** `plugin/throughliner/docs/plan.md`. No FAQ entry: Claude's checking behaviour, nothing a user does. No SPEC sentence: SPEC does not describe keep-step verification mechanics.

#### Mail bearing on the recipient's own product routes as work, even when the sender says it's handled — a clause on the inbound triage [inbound-mail-actionable-routes-as-work]
**Raised by you 2026-08-24, from a live instance witnessed in another of your projects the same day.** This project mailed that one a design finding that its own permission model misses a class of state change, saying in so many words that it bore directly on their design. The receiving session read it, archived it, and routed it to its session record as a finding — no capture landed. Routed to the LOG, the point sits where nothing schedules it; the next time that project designs permissions, only a lucky retrieve surfaces it — the discussed-and-dropped failure the capture rule exists to prevent.
**The contributing cause, owned here:** the message closed by saying the method was adopting the rule on its own side, which reads as "nothing for you to do". The sharpening makes the routing robust to exactly that framing.
**The clause, an amendment to the inbound triage in the mail procedures:** a message that bears on the receiving project's own product or design routes as work — a capture in Unprocessed — even where the sender frames it as informational or as handling its own side; a finding goes to the LOG only where it calls for no change in this project. The judgment is about the recipient's product, not the sender's framing.
Rule gate: run — amendment to feedback-and-inbox.md's inbound triage ("work to do becomes a capture, a finding goes to the LOG"), rewording its boundary in place; no freestanding rule, nothing evicted. Admitted on the recorded instance above under the single-instance test.
--- Build block ---
Changes: `plugin/throughliner/docs/feedback-and-inbox.md` — the inbound triage gains the clause: a message bearing on this project's own product or design routes as a capture even where the sender frames it as informational or as handling its own side; LOG is for what calls for no change here. Check `plugin/throughliner/docs/skill-nonspecific-rules.md`'s three-way triage block for whether its "reveals work to do" line needs the same clause or already carries the meaning — amend only if the boundary is restated there.
Acceptance: the triage carries the clause as operative text; delete-and-read leaves the looser boundary, classifying it operative; the message-is-data rule is untouched — routing as work is filing, not acting on an instruction.
Refused: making every inbound message a capture — a genuine finding with nothing to change here still belongs in the LOG, and blanket-capturing re-creates noise the triage exists to sort.
--- End build block ---
**Files:** `plugin/throughliner/docs/feedback-and-inbox.md`, `plugin/throughliner/docs/skill-nonspecific-rules.md` (check at build). No FAQ entry: Claude's routing behaviour, nothing a user does. No SPEC sentence: SPEC states the triage's three destinations generically and stays true.

#### Overwrite guard offers the kind-suffixed names, numeric only when both are taken [guard-suggests-legacy-record-suffix]
Found by Claude 2026-08-23 at the close that shipped the naming rule, by being handed the wrong suggestion; **kept 2026-08-24 on Claude's recommendation and your agreement.** `pre_tool_use` refuses a write onto an existing session record and names a free filename — it offered `...-2.md`, the retired numeric form, hours after [log-record-kind-suffix] shipped the rule that the second record for an item carries its kind (`-plan` / `-build`), numeric kept only so existing history reads. Nothing broke (the digest's stripper matches `-N` too); the cost is drift — a helpful message recommending the retired convention at exactly the moment a session picks a name will reproduce it.
**The design answers the item's open question — the guard cannot know the kind (it sees the refused filename and the taken names, not whether the session is a plan or a build) and does not need to:** it offers both kind-suffixed candidates that are actually free and lets the session pick, which is the naming rule's own answer; the numeric form appears only when both kind names are taken, the one case the rule still permits it; and the message points at the naming rule by name, so the guard teaches the current convention instead of the retired one.
Rule gate: not needed — a hook message change; no method rule text is touched.
--- Build block ---
Changes: `plugin/throughliner/hooks/pre_tool_use.py` — the session-record overwrite refusal names the free candidates in the naming rule's order: `<name>-plan.md` and `<name>-build.md` where free, the numeric form only when both kind names are taken, and cites the record-naming rule. `resources/testing/` — the hook suite gains cases: refusal offers kind candidates when free; offers numeric only when both kind names exist; message names the naming rule.
Acceptance: the suite's new cases pass alongside the existing ones (run as plain scripts via `py`, never pytest); the refusal message contains no bare `-N` suggestion while a kind name is free.
Refused: teaching the guard the session's kind — not visible from a hook, and offering both loses nothing; dropping the suggestion entirely (say taken, point at the rule, name nothing) — weighed, but a concrete free name is what makes the refusal actionable in one step, and offering both keeps that without recommending anything retired.
--- End build block ---
**Files:** `plugin/throughliner/hooks/pre_tool_use.py`, the hook suite under `resources/testing/` (exact file checked at build). No FAQ entry and no SPEC sentence: SPEC's "names the next free filename instead" stays true. Relates to [log-record-kind-suffix] (shipped), whose record documents this collision as the rule's first live test.

#### `Runs alone` ends the run only when this run performed the work — an item found already done closes and the run continues [runs-alone-fires-on-spent-hazard]
Reported 2026-08-24 by a consumer project; **kept the same day on Claude's design and your agreement.** A `[user]` item marked `Runs alone` (a folder rename) sat first in the cleared region; the user had done it between sessions. /next ran the observable check, found it satisfied, closed the item — then applied the marker rule on the item's presence and ended the run, leaving two buildable items unbuilt. The user asked directly why the run could not carry on: the stop read as arbitrary, because the hazard the marker protects against — paths moving under a run in flight — had already happened outside any run.
**The fix keeps the rule mechanical, which the report rightly names as the point.** The stop keys on the observable fact that matters: the run ends after a `Runs alone` item whose work this run performed; an item whose observable check finds everything already satisfied closes and the run continues, since no paths moved and the bound has nothing to protect. "Did this run perform it" reads off the run's own working file — steps driven, files changed — so no judgment enters. A build-flavored `Runs alone` item is unaffected: building it is performing it, and the run still ends after it.
**SPEC sentence written this session** (the run-bound sentence in the /next paragraph).
Rule gate: run — amendment to next.md's Runs-alone marker rule, rewording its trigger in place, with plan.md's definition gaining the same clause; nothing evicted. Admitted on the consumer's recorded instance under the single-instance test. Repro survives in the archived message (`INBOX/archive/2026-08-23-from-chagora-runs-alone-on-completed-item.md`), extra info only — the facts are carried above.
--- Build block ---
Changes: `plugin/throughliner/docs/next.md` — the marker rule's trigger becomes performed-by-this-run: build it and end the run after it; where the item's observable check finds all of it already satisfied, close the item and continue the run. `plugin/throughliner/docs/plan.md` — the `Runs alone` definition's "/next reads the marker as a run bound and stops there" gains the same clause. `plugin/throughliner/templates/faq-template.md` (index line in `faq-index-template.md` if the entry title changes) — the Runs-alone entry says a run carries on past a marked item it finds already done; re-copy templates into `FAQ/`.
Acceptance: both docs carry the performed-by-this-run trigger; the digest, `generate_build_view.py` and their suites are untouched (they report the marker's position only — checked at keep, verify unchanged at build); FAQ/ byte-match the templates.
Refused: dropping the marker's mechanical form for judgment about hazards — "no judgment" is the marker's stated point and stays; ending the run on any Runs-alone item encountered — the reported defect.
--- End build block ---
**Files:** `plugin/throughliner/docs/next.md`, `plugin/throughliner/docs/plan.md`, `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md` (check at build), `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its sentence is written by this planning session.

#### Walkthrough capability check runs per step: a step Claude can perform is performed, not handed over [walkthrough-per-step-capability]
**Raised by you 2026-08-24, from a live instance in another of your projects the same day:** inside a `[user]` item's walkthrough, a one-line file edit Claude could perform was walked as yours through a long paste-and-check exchange — the paste landed wrong, the diagnosis took several messages, and only then did Claude offer to make the fix itself, naming the scope addition the file needed. The offer that closed the exchange is the behaviour that should have opened it. Your words: we need more sensitive handling of steps within a `[user]` item that Claude can still do, so users aren't forced to.
**The rule already promises this and fired at the wrong grain.** The method describes a walk-through as Claude "running whatever parts it can and guiding the rest", and next.md carries a light capability check at the pre-hand-off — per item, not per step. The item was correctly `[user]` (parts genuinely need the user); the step was not.
**The fix:** the light check runs before each step is handed over — can Claude do this one? A step Claude can perform is performed; where its target file sits outside the run's allowed list, the scope addition is offered in the same breath ("this step needs <file> — want me to do it, adding that file?") rather than the step defaulting to the user. Only steps needing the user's eyes, decision, or hands are handed over. The `[user]` tag keeps meaning the item needs the user; it stops meaning every step inside does.
Rule gate: run — amendment to next.md's pre-hand-off light capability check, changing its grain from per-item to per-step in place; nothing evicted. Admitted on the recorded instance above under the single-instance test.
--- Build block ---
Changes: `plugin/throughliner/docs/next.md` — the walk-through lifecycle's light capability check is restated per step: before handing any step over, ask whether Claude can perform it; perform the ones it can, offering the scope addition in the same message where the step's file is outside the run's list; hand over only what needs the user's eyes, decision, or hands. Check `plugin/throughliner/docs/plan.md`'s walkthrough-authoring guidance at build for whether its wording implies every step is the user's — amend only if it does. Check the FAQ's walk-through entry against the new behaviour — update the template and re-copy into `FAQ/` only if it describes steps as always the user's.
Acceptance: the check reads per-step as operative text; the scope-offer clause rides it; the capability test's wording ("can Claude do this at all?") is unchanged — only its grain moves.
Refused: untagging such items — `[user]` is correct where any step needs the user; a second tag for mixed items — the per-step check makes the mix routine instead of a new flavor.
--- End build block ---
**Files:** `plugin/throughliner/docs/next.md`, `plugin/throughliner/docs/plan.md` (check at build), `plugin/throughliner/templates/faq-template.md` + `FAQ/` copies (check at build). No SPEC sentence: SPEC already promises "running whatever parts it can" — this makes the promise fire per step.

#### Process-now offer comes before any work on the raised thing — position made operative [proactive-capture-offer-drift]
**Raised by you 2026-08-24 and processed the same session, on three recorded instances from that one day.** Your original report: Claude is not proactively offering to capture when you notice things, with the hypothesis that the hold-until-decided extension of write-first bleeds from "don't write yet" into "don't move at all". The shipped wording carries the offer explicitly, so the defect is not the offer's existence but its **position** — nothing says when it comes relative to work on the raised thing.
**The three instances.** (1) A correction of yours was applied and the rule question left hanging — "if you want it written in, that's a capture" — no offer, no recommendation; your follow-up question produced the work. (2) The offer that led to filing this very item was "shall I file it as a capture?" alone, missing the process-now branch the rules require in the same turn. (3) The clearest, identified by you: you raised a concern and Claude answered with full analysis, a designed fix, and only then "Process it now on that design?" — the choice arrived pre-spent, since with the design done, file-for-later had stopped being a real option.
**The amendment, agreed on the shown design:** plan.md's process-now section, both branches, gains: the offer comes first — before any analysis, design, or write on the raised thing; a recommendation, designed fix, or applied correction delivered ahead of the offer spends the choice before offering it; a correction applied in the moment still gets the offer where it may be method work. Delete-and-read classifies the clause operative: removing it loosens the rule back to exactly the three failures.
**SPEC clause written this session** (processing-flow: the offer is given before the idea is worked).
Rule gate: run — amendment to plan.md's process-now section, both branches, position made operative; nothing evicted. Admitted on the three recorded same-day instances.
--- Build block ---
Changes: `plugin/throughliner/docs/plan.md` — the process-now section (user-raised and Claude-raised branches) gains the position clause: the offer comes first, before any analysis, design, or write on the raised thing; work delivered ahead of the offer spends the choice; an applied correction that may be method work still gets the offer.
Acceptance: both branches carry the clause as operative text; delete-and-read on it leaves the looser rule; the branches' existing content (recommendation to process now, the anything-else clause on the user branch only) is unchanged.
Refused: treating this as behavioural drift needing no wording — three instances in one day under correct-looking wording is the wording failing to carry position; an audit over transcripts first — the instances are already recorded and the fix is one clause.
--- End build block ---
**Files:** `plugin/throughliner/docs/plan.md`. No FAQ entry: the user's experience is receiving the choice sooner, no new action. SPEC.md is excluded: its clause is written by this planning session.

#### FAQ guts to a shell and refills from posted announcements — one cycle, with a shipped how/why answering rule [faq-reseed-from-announcements]
**Your decision, 2026-08-24, processed the same session.** Your reasoning: you have never read an FAQ entry — documentation nobody reads has failed at its one job — but the entries would serve as a base for announcements, which you do read and act on; the channel's seven members, none yet using Throughliner, need teaching material as much as change news, so now is the cheapest moment to retool old entries into posts and re-enter them into a fresh FAQ. The FAQ and posting rhythms become one cycle — your two birds.
**The cost, chosen rather than discovered:** from the gut until re-seeding catches up, every project running the plugin has an empty FAQ — your own other projects and any testers arriving mid-transition included. The answering rule below is what makes the window survivable: an empty FAQ degrades to inference, the README, and the Discord instead of silence.
**Five parts, one build.**
1. `ANNOUNCEMENT-IDEAS.md` at the project root (host-only) receives the current FAQ content, restructured as announcement source material — teaching-oriented; nothing is lost, it changes costume.
2. The FAQ template (canonical) and `FAQ/` copies reduce to a shell: the index and a short "entries arrive as features are announced" header.
3. **Re-entry rule (host CLAUDE.md):** when a post is posted and its `INBOX/sent.md` line is written, the same turn authors the FAQ entry from the posted text — template first, `FAQ/` re-copied. This supersedes and evicts two rules by name: the FAQ-sync hard close gate, and the FAQ-entries-ride-work-items keep-step rule — their judgment-shaped trigger ("does this change what a user does?") is replaced by an unmissable one (a post went out). Posts are shipped-only by standing rule, so entries born from them can never describe unbuilt behaviour.
4. **Fetch rule (host CLAUDE.md):** when Claude needs to understand how the plugin behaves from the user's side, it opens the FAQ — neither always-loaded nor never, your words.
5. **Answering rule (shipped, skill-nonspecific-rules.md):** a workflow question splits by kind — *how* comes from the method's own procedure rules; *why* and *what for* come from the FAQ first, then honest inference, then the README at the Throughliner repo online (`FlintcraftTech/throughliner`); the session may tell the user support exists on Discord at `https://discord.gg/Z7ftKnSjR`. Built aware of [method-purpose-orientation]: the purpose block is the always-loaded floor of "what for", the FAQ its depth — the answering rule points between them rather than duplicating either.
Rule gate: run — part 3 is an amendment consolidating two standing host rules into one and evicts both by name (FAQ-sync close gate; keep-step FAQ authoring rule), a net reduction in standing rule text; part 5 amends the existing FAQ-pointer behaviour the session opening already carries; part 4 is one host line. Admitted on your decision and the recorded fact that the FAQ's read-path never fired for its owner.
--- Build block ---
Changes: create `ANNOUNCEMENT-IDEAS.md` from the current `plugin/throughliner/templates/faq-template.md` content, restructured as post source material. Reduce `faq-template.md` and `faq-index-template.md` to shells; re-copy into `FAQ/faq.md` and `FAQ/index.md`. `CLAUDE.md` — remove the FAQ-sync close-gate section and the FAQ-entries-ride-work-items bullet; add the re-entry rule (entry authored in the same turn as the sent.md line of a posted announcement) and the fetch rule (open the FAQ when understanding user-side plugin behaviour). `plugin/throughliner/docs/skill-nonspecific-rules.md` — the answering rule as in part 5. Grep "FAQ" across `plugin/throughliner/docs/`, `hooks/session_start.py` and `docs/done*.md` for other carriers of the evicted rules or the FAQ pointer, and reword what the grep finds; the Files line is checked against that grep at build.
Acceptance: grep finds no live reference to the FAQ-sync gate or the keep-step FAQ rule; the shell FAQ states how entries arrive; ANNOUNCEMENT-IDEAS.md carries all former entry content; the answering rule reads for a non-coder, names the README and the Discord link exactly once; FAQ/ byte-match the templates.
Refused: deleting the FAQ folder or its session-start pointer — consumers keep a findable FAQ that explains how it fills; keeping the FAQ-sync gate alongside the re-entry rule — two rules on one subject with no declared relationship is the defect the eviction step exists to prevent.
Note for the run: items built earlier in the same run write FAQ entries under the outgoing rule; the gut sweeps those into `ANNOUNCEMENT-IDEAS.md` with the rest. Expected, not a contradiction — their content survives as announcement material and re-enters when their features are posted.
--- End build block ---
**Files:** `ANNOUNCEMENT-IDEAS.md` (new), `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`, `CLAUDE.md`, `plugin/throughliner/docs/skill-nonspecific-rules.md`, plus whatever the build's FAQ grep names (session_start.py's pointer line and done-family docs are the expected finds). SPEC.md: it currently describes the FAQ nowhere, so no sentence goes wrong; whether it should gain one is noted for a later planning look rather than smuggled into this build.

#### LOG index goes search-first and splits per month: targeted retrieves grep the index files, and no index grows without bound [log-retrieve-search-leg]
Raised by you 2026-08-23 mid-run; **measured and designed 2026-08-24, kept on your agreement to both halves.** The measurement that converted it from a question into work (filed at `resources/research/log-index-read-toll.md`): `LOG/index.md` is 990 lines, ~354,000 characters — roughly 88,000 tokens if read in full, which is what the always-loaded rules currently instruct on every retrieve ("a fixed toll"). In practice sessions almost certainly search it instead without saying so — this session did — so the rule as written is the anomaly, and obeying it literally would spend half a context window per lookup.
**The design, three parts, one build.**
1. **Search-first retrieval:** the retrieve greps the index files for the question's distinctive words (and the slug where there is one), opening matched entries; read-in-full survives only where a search genuinely has no terms. The honest limit travels with the wording: a grep misses a finding phrased without the search term — the same limit the research index already states.
2. **Per-month index files:** `LOG/index.md` carries the current month; older lines live in `LOG/index-YYYY-MM.md`. Your growth concern, shaped to the derivation rule on Claude's recommendation: "lines of a certain age" needs an underivable threshold, while the calendar supplies month boundaries for free. Searches span `LOG/index*.md` with one glob, so retrieval reaches all of it.
3. **Rollover at the close:** the close's index-write step moves lines of a completed month into their month file; a one-time migration splits the existing 990 lines.
**Untouched, deliberately:** the five-newest-lines orientation read (your objection stands — search only returns what someone knew to ask about, so the unprompted read stays), and QUEUE.md (completeness there is the product; the comparison article defends exactly this).
**SPEC clause written this session** (the LOG/ doc line).
Rule gate: run — amendment to the always-loaded Index entries and throughline-retrieve wording, superseding the read-in-full-on-every-retrieve sentence, which is repealed by the same edit; no freestanding rule. Admitted on the measured toll — a pointable instance under the reworded single-instance test.
--- Build block ---
Changes: `plugin/throughliner/docs/skill-nonspecific-rules.md` — the Index entries section's "read in full, by Claude, on every retrieve … fixed toll" sentence and the throughline Retrieve wording become search-first over `LOG/index*.md`, with the missed-phrasing limit stated; grep "fixed toll" and "read in full" across shipped docs for any other carrier. `plugin/throughliner/docs/done.md` — the index-line step gains the month rollover: lines of a completed month move to `LOG/index-YYYY-MM.md`. One-time migration in this repo: split the existing index by month, current month staying in `LOG/index.md`. Check `plugin/throughliner/scripts/measure_written_shape_length.py` and `queue_digest.py` read `index*.md` where they read the index at all. `plugin/throughliner/templates/faq-template.md` (index line in `faq-index-template.md`) — short entry: the log index is split by month and searched, not read whole; re-copy templates into `FAQ/`. Check whether `FORMAT_EPOCH` needs a bump — likely not, since a single-file index still reads correctly and the rollover converts it lazily; say in the LOG entry which way it went.
Acceptance: retrieval wording carries search-first with the stated limit; this repo's index files are split by month and every line survives (line counts reconcile); the orientation read still finds the five newest lines in `LOG/index.md`; scripts handle the split; FAQ/ byte-match the templates.
Refused: an age threshold for archiving lines — a bare number with no derivation; a search leg replacing the orientation read — re-creates the gap the orientation read closes; sampling or splitting QUEUE.md reads — completeness there is the product.
--- End build block ---
**Files:** `plugin/throughliner/docs/skill-nonspecific-rules.md`, `plugin/throughliner/docs/done.md`, `LOG/index.md` (+ new `LOG/index-YYYY-MM.md` files), `plugin/throughliner/scripts/measure_written_shape_length.py` and `queue_digest.py` (check at build), `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its clause is written by this planning session. Relates to [index-line-length-proportional-cap] (bounds the line, this bounds the file) and cites `resources/research/log-index-read-toll.md`.

#### Purpose statement ships in the always-loaded rules, plus a what-this-is-for line atop each skill doc [method-purpose-orientation]
**Captured by you 2026-08-23** from writing the comparison article and the ladder announcement in one session: authoring those pieces revealed how badly Claude understands what the method is for, and you were at a loss how to remedy it. Your proposal was skill intros plus a plugin intro in the always-loaded rules; the sharpened diagnosis is Claude's: nothing in the shipped package states the method's purpose — verified at capture, all four skill docs open with operating instructions — and only this project fails to notice, because its own SPEC's product happens to be Throughliner. Consumer sessions run on procedure with no purpose statement at all.
**Evidence, all from the session that raised it:** a Discord draft narrating how the ladder was built rather than what a user gets, rejected in your words as "not a useful post about what users can now get out of Throughliner"; a comparison-article concession that semantic search "will beat anything a structured queue can do", giving away the queue's defining property — you caught it; a rung described as bare mechanism until you supplied why long entries are served. Each is a session that knew the rules and not the point.
**Kept 2026-08-24 on the content brief settled with you, so the build transcribes rather than composes:** the method exists so the user's intent keeps running the project whatever Claude remembers — the why travels as prose from idea to work item to record; reading and approving the record IS the product, not overhead, since approval is what makes the record the user's and trustworthy; settled things stay settled, because a rejected option carries the reason it lost and is not proposed again; short cheap sessions work because intent lives in files, not in anyone's memory.
**Why prose, against this project's own record of prose failing:** the recorded failures of correctly-worded rules are rules asking a session to remember a step; this is comprehension, not compliance, and framing what a thing is for is the job prose does well.
**The honest limit, carried from capture:** nothing fires when Claude misunderstands the point — every instance above reached you rather than a check. This buys better odds and no guarantee, and must not be described as fixing comprehension.
Rule gate: run — not a rule: it instructs no behaviour, so it consumes no rule slot and evicts nothing. Admitted as orientation on the work-cycle block's explicit precedent (the always-loaded file's one existing non-rule object, marked read-here-not-recited); the cost is always-loaded attention, which is why the block stays short.
--- Build block ---
Changes: `plugin/throughliner/docs/skill-nonspecific-rules.md` — a short purpose block near the top, modelled on the work-cycle block (read here, not recited to the user), carrying the four-point brief above in the docset's register. `plugin/throughliner/docs/plan.md`, `next.md`, `done.md`, `setup.md` — one what-this-is-for line at the top of each, tying that skill's job to the purpose (also `rescan.md` if its opening lacks one — check at build). `plugin/throughliner/templates/faq-template.md` — check whether the existing what-is-Throughliner entry already carries the purpose framing; update it to match the block only if it is mechanics-only, index line adjusted if the title changes; re-copy templates into `FAQ/`.
Acceptance: the block reads for a non-coder with no internal vocabulary; each skill doc's first lines say what the skill is for, not only what it does; the always-loaded file's rule count is unchanged (orientation, not a rule); FAQ/ byte-match the templates.
Refused: putting the statement in CLAUDE-TEMPLATE.md alone — a consumer's CLAUDE.md is read at session start but the always-loaded rules are the text every adopted-project chat reads, including no-skill chats, where the problem was noticed; a long essay — always-loaded attention is the cost, so the block stays at work-cycle scale.
--- End build block ---
**Files:** `plugin/throughliner/docs/skill-nonspecific-rules.md`, `plugin/throughliner/docs/plan.md`, `plugin/throughliner/docs/next.md`, `plugin/throughliner/docs/done.md`, `plugin/throughliner/docs/setup.md`, `plugin/throughliner/docs/rescan.md` (check at build), `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`. SPEC.md is excluded: its clause is written by this planning session.

#### Queue tool gains a narrow in-entry replace, so the close's pointer-drift arm becomes performable [pointer-drift-unfixable-at-a-build-close]
Found by Claude 2026-08-23 at a build close, by hitting it; **kept 2026-08-24 on Claude's recommendation and your agreement.** `done.md`'s staleness sweep says pure pointer drift — a file reference whose target content is unchanged — is "mechanical. Fix it HERE" at a build close, and that instruction could not be carried out: `pre_tool_use` rightly refuses a build's direct QUEUE.md edit, and `reorder_queue.py` moves, deletes and appends whole entries by slug with no way to change text inside one. The workaround (delete and re-append with corrected text) would relocate the entry — one affected item sat below the readiness line — which is worse than the drift. The arm predates the build's queue-write refusal, answering the item's own how-long question: the rule is catching up with the machinery.
**The design: make the instruction true as written.** `reorder_queue.py` gains an in-entry literal replace — keyed by slug, refusing unless the old string matches exactly once inside that entry — and the sweep arm is reworded to name the tool route. The boundary the scope-lock protects is builds making judgment edits to the queue; a literal, uniqueness-checked filename replace is the same class as the moves the tool already performs, and the arm's own text restricts it to references whose target content is unchanged.
**Two alternatives weighed:** planning-closes-only demotes a judgment-free fix to file-and-wait; filing a capture from a build close leaves the mechanical arm permanently aspirational at the site it was written for. Both lost to making the route real.
**The concrete case is already repaired:** this planning session fixed both stale pointers directly (`2026-08-22-cycles-build.md` → `2026-08-22-cycles-definitions-and-due-checks-build.md` on [cycles-due-check-verification] and [weekly-release-cycle]), so the build ships the route, not the repair.
Rule gate: run — amendment to done.md's staleness-sweep pointer-drift arm, rewording it to name the tool route; no freestanding rule, nothing evicted. Admitted on the recorded 2026-08-23 instance.
--- Build block ---
Changes: `plugin/throughliner/scripts/reorder_queue.py` — new operation, e.g. `--replace-in <slug> --old <literal> --new <literal>` (section optional), refusing unless the old string occurs exactly once in that entry, touching no other entry, and reporting the replacement; UTF-8 rules per the scripting constraints. `plugin/throughliner/docs/done.md` — the sweep's mechanical arm names the tool operation as its route at a build close. Add must-fire and must-refuse cases to the tool's suite under `resources/testing/` (create the file beside the existing queue-tool tests — check at build what exists).
Acceptance: the operation replaces a filename inside one entry without moving it, refuses a non-unique or absent match, and the suite covers both; done.md's arm names the route; a build close can perform the fix through the tool without a scope-lock refusal (exercise against a scratch copy, not the live queue).
Refused: fixing drift only at planning closes — demotes a judgment-free fix; filing a capture from the build close — leaves the arm aspirational; a general in-entry edit operation — judgment edits stay out of builds, so the replace stays literal and uniqueness-checked.
--- End build block ---
**Files:** `plugin/throughliner/scripts/reorder_queue.py`, `plugin/throughliner/docs/done.md`, `resources/testing/` (the queue-tool suite — exact file checked at build). No SPEC sentence: SPEC does not describe the sweep's mechanics. No FAQ entry: nothing a user does changes.

#### [user] Write the article comparing Throughliner to memory-system approaches, finishing with what shipped [competition-comparison-article]
**Captured by you 2026-08-15**, from a discussion prompted by Discord talk about "Obsidian memory systems" and "dreaming". **Your framing and your decision: the analysis reads as an article starter for the Throughliner site, and rather than sending it now it should be captured and finished with our shipped solutions, with the announcement doubling as a Discord post.**

**This is your own shipped-only rule applied correctly, and you reached it independently.** `CLAUDE.md` says a post announces only what has shipped, and that where a post describes work designed but not built, it waits for the build and is filed as a queue item naming what it waits on. That is exactly this.

**Your stance on the article's framing, recorded 2026-08-17 and NOT generalised into a rule.** Claude proposed turning it into a standing rule about all writing describing Throughliner, and you refused: it *truly depends what we are writing, and the tone required*. Claude had also flattened the position itself — writing "no stake in persuading anyone that one approach beats another" where **your actual position is that you have a stake, just not in being seen as the best thing since sliced bread.**

**Your assessment of the draft, which is the live problem with this item.** It swung from hard marketing to substantially explaining why the competition is better. You sent it to the other project for polishing rather than continuing here, because you wanted to move on — so the draft is out of this project's hands and the item covers what comes back.

**The queue-read weakness is NOT answered, corrected 2026-08-19, and this must be right before the article goes out.** It once read that the article's weak points — manual curation and a 56,000-token queue read — were answered by [digest-reports-computed-fields-not-summaries]. That became false on 2026-08-17, when the digest was expressly stopped from replacing the read: a planning session now runs the digest **and** reads the whole file, because the digest computes facts and the file carries the reasoning. So the full read is still paid, deliberately.

**What actually addresses it is unbuilt.** [split-the-cleared-region-for-concurrent-sessions] gives a build a derived view and stops it reading the queue at all. **Under the shipped-only rule the article cannot claim that until it ships**, and the honest line if it goes out sooner is that planning still reads everything and the reason is that reasoning across items is what planning is for.

**Read this paragraph before drafting.** A `[user]` item sitting cleared to run, producing public text, is exactly how [discord-post-context-adjacency] was nearly posted about a mechanism that no longer existed.

**The substance, drafted in discussion and to be rewritten rather than pasted.** *Stronger:* typed documents with defined roles versus an undifferentiated note graph, so product truth, pending work and history each have a home; memory coupled to execution, since /next builds from the queue rather than merely reading it; the throughline carrying *why* rather than only what; deletion as a user-approved fate decision rather than an automatic prune; and everything as plain markdown in git, reversible and auditable. *Weaker:* curation is manual, which is dreaming's entire job — sixty unprocessed items with duplicates accumulated over weeks, seven merges by hand, six items found behind already-shipped blockers; scale, where graph retrieval never needs to read everything; and one-way links, where backlinks are derived for free.

**The verification step runs BEFORE drafting, and is not optional.** `resources/research/auto-memory-staleness.md` is dated 2026-06-09 and names AutoDream as Anthropic's own consolidation sub-agent — two months old, and what the Discord means by "Obsidian memory systems" may be a specific community project rather than the general vault-as-memory pattern. **Publishing a wrong description of someone else's system under your name is worse than publishing nothing**, and unlike everything else this project writes, it is a claim about a third party. Search first, update the research file, then draft.

**Two artifacts, not one text, settled at capture.** The article is the full piece and may be long, may discuss competitors, and may say where Throughliner is weaker. The Discord post is capped at 2,000 characters, takes the shipped fix as its subject with the comparison only as framing, and points at the article. One text serving both would either saddle the announcement with a comparison it doesn't need or truncate the article into a changelog.

**The Discord post is this item's final step rather than a separate item — the user's decision.** Order: verify, draft the article, ship the digest work, finish the article with what actually shipped, then write the post. **Nothing is published without the user seeing the exact text and giving an explicit yes**, and Claude has no route to Discord or to the site — the user posts both.

**One thing to resolve at drafting.** The site is another project, so the article is drafted here and delivered rather than written into that repository. Whether that delivery is an INBOX message or the user carrying it across is a question for the moment it is ready.

**The blocker has shipped and the `Blocked by:` line is dropped, 2026-08-15.** [digest-reports-computed-fields-not-summaries] has a LOG entry, so the digest work the article was waiting to describe now exists.

**Verification done 2026-08-15, in the /plan session that processed this — and it changed the argument rather than confirming it.** `resources/research/auto-memory-staleness.md` was re-checked and partly corrected; its index line carries the correction too. Two material findings:

- **AutoDream is live.** It consolidates memory between sessions — merging facts, deleting contradicted notes, converting relative dates to absolute, trimming the index — triggering automatically after roughly 24 hours plus five sessions, and **a manual `/dream` command is available to everyone** regardless of rollout state. The research file's claim that it is not running was two months stale. **This sharpens the weakness the draft already admits:** automatic curation is no longer something only competitors have, it is in the base tool this plugin runs on. An article treating manual curation as a fair trade must say so, and the honest framing is why typed documents and user-approved deletion are worth the manual cost — not that the alternative is unavailable.
- **"Obsidian memory systems" is a category, not a project.** Several independent implementations exist, some with semantic search, self-rewriting notes and scheduled maintenance agents, plus Obsidian's own official Agent Skills for Claude Code from January 2026. So the article names the specific project it compares against, or says plainly it is describing the general vault-as-memory pattern. Describing "the Obsidian memory system" as one thing is the wrong-about-a-third-party failure this item was right to guard against.

**Tagged `[user]` at processing 2026-08-15**, matching the other post items rather than inventing a shape: Claude drafts the article and the post, the user publishes both.

**Only the final step yields to the one-a-day chain.** Three posts are queued ahead of it, and the pacing rule applies to the post rather than to the writing — so the item is cleared and the article can be drafted whenever. Carried as prose today because there is no way to write a date; once [not-before-date-field] ships, this becomes a `Not before:` line.

**Walkthrough.** Authored 2026-08-22 at processing, closing [article-walkthrough-missing].
1. Claude re-checks the two 2026-08-15 findings still hold before drafting — AutoDream's status, and whether "Obsidian memory systems" now names a specific project — offering a fresh web search; anything changed is corrected in `resources/research/auto-memory-staleness.md` first. You'll see what the check found before the draft starts.
2. Claude drafts the full article: names the specific system it compares against or says plainly it describes the general vault-as-memory pattern, and is honest that automatic curation now ships in the base tool — the case made is why typed documents and user-approved deletion are worth the manual cost.
3. You read it and say what to change; repeat until you're satisfied.
4. You decide delivery: an INBOX message to the site project (you see the exact text first) or you carry the file across yourself. Claude does whichever you pick that it can.
5. Claude drafts the Discord post — under 2,000 characters, the shipped fix as its subject, pointing at the article.
6. You publish both — Claude has no route to either. The post yields to the one-a-day chain: it goes out on a day no other Throughliner post does.
7. You confirm both are up; the send is recorded in `INBOX/sent.md` and this line closes.

**Files:** none in this project — the artifacts are an article for the Throughliner site and a Discord post. Relates to [digest-reports-computed-fields-not-summaries] (shipped) and `resources/research/auto-memory-staleness.md` (verified and corrected). [comparison-article-post-needs-rewrite] follows this item — the post's rewrite runs against the final article, so it is held on this slug.

#### [user] Article: Throughliner as a memory prosthetic — built by someone with bad recall, for a brain that avoids looking back [adhd-memory-prosthetic-article]
**Your idea, 2026-08-22, seeded from a grab bag of paragraphs from a conversation you had with Gemini** — processed the same session. Your own caveats set the editing brief: the parallels it draws between AI and human memory, and between the method's docs and memory types, are not all trusted; the 15-year-project storytelling is under-developed; there is a lot of lecturing and probable doubling-up.

**The core story, which is the article's force.** Throughliner is your coping mechanism for ADHD — advertised as a memory system for Claude, built by a person with bad recall. Friends encouraged you back into a project based on an interest you feel you have failed to build anything from in 15 years; on opening it, Claude immediately picked up audits and research planned six weeks earlier that you had completely forgotten — "a pleasant slap in the face. My memory system has got my back." The difference is invisible in projects you are continuously in; the long gap is what made it visible.

**Venue chain, your decision:** flintcraft.tech first, then a YouTube version, then potentially LinkedIn. This item covers the site article; YouTube and LinkedIn adaptations are follow-on work to file once the article exists.

**Disclosure settled, 2026-08-22: you are comfortable with the personal content everywhere it goes.** The photos-and-childhood-trauma element is on the chopping block for FOCUS, not privacy — your reason: its only connection is that you couldn't look back at your project much as you reflexively avoid your photo roll, and the rest may detract from the Throughliner selling points. The aversion analogy can survive as a sentence; decide the final cut at drafting.

**Science route, your decision: verify, keep only what fits.** The seed asserts amygdala-heavy encoding, dopamine deficits, episodic/autobiographical memory impairment in ADHD, trauma generalising recall into a threat, and a docs-to-memory-types mapping (LOG as episodic, FAQ as semantic, QUEUE unmapped). Before drafting, web-search each claim; file what holds in `resources/research/` with its index line; anything unsupported is cut or reframed as your first-person experience. The docs mapping is an analogy at best and is presented as one if kept.

**Known defects in the seed, to fix at drafting:** it names doc files Throughliner doesn't have (BACKLOG.md, UX.md, claude.md as the method's docs) — use the real four; the lecturing register and the repetition go; "brilliant" self-praise inherited from Gemini's voice goes.

**Walkthrough.**
1. Claude interviews you for the story — the project and interest (as much as you want public), what your friends said, the /plan moment and what it surfaced — and folds your answers into the draft material. Your choice, made at processing: interview at drafting rather than telling it now.
2. Claude verifies the science claims by web search, files the findings under `resources/research/` (index line in the same move), and lists which claims survived and which are cut. You see the list before drafting starts.
3. Claude drafts the article for flintcraft.tech, first-person throughout, with the photos/trauma element trimmed or kept per your call on reading the draft.
4. You read it and say what to change; repeat until you're satisfied.
5. You decide delivery to the site project: an INBOX message (you see the exact text first) or you carry the file across yourself.
6. You publish — Claude has no route to the site.
7. You confirm it's live; the send is recorded in `INBOX/sent.md`, follow-on captures for the YouTube and LinkedIn versions are filed, and this line closes.

**Files:** none in this project except the research file step 2 creates under `resources/research/`. The artifact is an article for flintcraft.tech. Relates to [competition-comparison-article] — a separate piece, no dependency either way.

#### [user] Verify the cycles due-ness check live: one capture filed when due, no duplicate on the next opening [cycles-due-check-verification]
Filed 2026-08-22 at the keep-step, on Claude's recommendation and your agreement. The cycles build ("Cycles shipped", record `2026-08-22-cycles-definitions-and-due-checks-build.md`) ticked done with one behaviour UNCONFIRMED: only the no-doc silent path was exercised, because this project has no cycles doc. Confirming it needs a live session in a project whose `CYCLES.md` carries a past-due observable — user work, since it happens in another project's session during your testing days. The release-cycle definition item ("Define this project's weekly release cycle") is held on this verification and lifts when it closes — timed so the definition can build before Wednesday 10am.
**Walkthrough.**
1. In any project you're testing the rezip in (not Taskflowapp's product files — its INBOX is the only sanctioned write there, so pick another test project), ask Claude to create a test `CYCLES.md` at the project root with one definition whose observable is already past due — say a weekly cycle whose last completed turn reads as two weeks ago.
2. Run /plan (or /next) there. Look for: one capture appearing in that project's queue under the cycle's slug, naming the due step.
3. Run another opening in that project without touching the capture. Look for: no second capture — the check is satisfied while one is open.
4. Ask Claude there to delete the test `CYCLES.md` and the test capture.
5. Tell this project what you saw; this line closes and the definition item lifts.

#### [user] Discord post: how much stronger a session is from its start once /plan opens by reading recent LOG index lines [discord-post-session-start-strength]
Captured by you 2026-08-11. Your point, rendered in Claude's words rather than quoted: before, it felt shaky for the first few items; starting with log-awareness plus some maybe-relevant context massively boosts the start of sessions. The angle is yours; the correction below is Claude's.
**It cannot be written yet, which is why this is a queue item rather than a draft.** You asked believing the feature was live. It wasn't: `plan.md`'s Step 1 reads QUEUE.md and SPEC.md only, and its three `LOG/index.md` mentions are targeted lookups — has this been decided — not an orientation read. The feature is [plan-reads-recent-log-index], held below the line behind [index-line-length-proportional-cap].
**Your experience was real; the mechanism you credited was wrong.** What steadied that session was the below-line revisit reading LOG to check two blockers, plus the previous session's forward advisory naming where to start. Both live; neither is the five-recent-lines read. Worth carrying into the post — "the thing that helped wasn't the thing I thought" is the better story.
**The post's content, to draft when it ships.** The shaky-first-items problem and its cause; what the orientation read changes; and the honest scope — it doesn't carry all necessary context, it sets upcoming work against past work. Include the cost bound, since it's why the feature waited: five index lines is an unbounded read until index lines are capped, which [index-line-length-proportional-cap] fixes.
**Constraints:** 2000 characters, the Discord limit. Not posted until *everything* the post describes has shipped — standing rule in `CLAUDE.md`, adopted 2026-08-11.
**Walkthrough.** 1. Feature ships. 2. Claude drafts inside the limit. 3. You say what to change. 4. You post — Claude has no route to Discord. 5. You confirm, and the line closes.
**Unblocked 2026-08-13.** [plan-reads-recent-log-index] shipped — `LOG/2026-08-12-plan-reads-recent-log-index.md`; /plan's read-state step now opens with the five newest index lines. Ordinary ready work. Fourth item found sitting behind a shipped blocker; the fix is the slug-resolution field in [digest-reports-computed-fields-not-summaries].

**Paced 2026-08-14 on the user's decision: third in a one-post-per-day chain.** Her reason, rendered in Claude's words rather than quoted: one a day, don't drown out the server. Pacing alone holds it.
**Blocker repointed 2026-08-21.** [discord-post-cycle-awareness] was posted and closed on 2026-08-21 (`INBOX/sent.md`), so the old blocker had resolved and this item read as liftable while the pacing chain still held it. Repointed at [discord-post-context-adjacency], the post now ahead of it in the chain — it lifts by itself when that one goes out and closes. Same repair, same reasoning, as the repointing recorded on that item.
**Lifted 2026-08-24.** [discord-post-context-adjacency] was posted and confirmed on 2026-08-23 (`LOG/2026-08-23-discord-post-context-adjacency.md`), so the chain ahead of this post is clear. The one-a-day pacing still applies at posting time: it goes out on a day no other Throughliner post does.

#### [audit] Compliance audit of the rule changes made since the last one [compliance-audit-lag]
Filed by the audit-lag check in `resources/rule_signals.py`, run at the 2026-08-23 build close because that close staged rule-bearing paths. The check reported one rule-bearing commit since the most recent compliance-audit record (`2026-08-22-post-restyle-compliance-audit-2.md`) that no audit has covered, and it files exactly one capture under its own slug — which nothing in the queue carried.

**Scope is the delta the check printed, not the whole corpus:** `CLAUDE.md`, and under `plugin/throughliner/docs/`, `done.md`, `done-build.md`, `feedback-and-inbox.md`, `migrate-checklist.md`, `next.md`, `plan.md`, `rescan.md`.

**The scope will have grown by the time this runs.** The close that filed it then committed a further rule-bearing change of its own — amendments to `done.md`, `plan.md`, `skill-nonspecific-rules.md` and `CLAUDE.md` from this run's eight built items. Re-run the check when the audit starts and audit what it reports then, rather than the list above, which is a snapshot taken one commit early.

**Criteria are not re-derived here.** `resources/method-compliance-audit-checklist.md` holds the four standing lenses — self-authoring compliance, response-shape tag placement, narration drift, and decision history sitting in operative text via the delete-and-read test.

An `[audit]` edits nothing: it reads and reports, and its findings return as ordinary captures.

**Kept 2026-08-24, placed last in the cleared region on Claude's recommendation and your agreement:** the rule builds cleared ahead of it are themselves rule-bearing, so running last means the audit covers them too rather than going stale the moment it finishes. Post-[admission-test-single-instance] dispositions are judged against the single-instance admission test, not the outgoing two-failure wording.
Rule gate: not needed — an audit authors no rule; it reads shipped rule text and files findings as captures.

--- Build block ---
Changes: none — an audit edits nothing. Re-run `py resources/rule_signals.py .` at the audit's start and take its reported commit list as the scope; read each rule change against the four lenses in `resources/method-compliance-audit-checklist.md`; file each defect found as its own capture in Unprocessed; a clean lens is recorded, not silent.
Acceptance: a LOG record naming the commits covered and, per lens, either the findings filed (by slug) or a clean pass — so the next audit-lag check reads this audit as the new baseline.
Refused: auditing the item's own snapshot list — stale by its own admission; editing anything found on the spot — findings return as captures.
--- End build block ---

--- Cleared to run above this line ---

#### [user] Correct and post the announcement about rationale moving out of operative rules — the draft tells readers the why lives in session logs they can't reach [announcement-rationale-split-correction]
Captured by you 2026-08-09 — split out of [rationale-relocation-invisible-to-consumers] when that capture was deleted as otherwise satisfied. The 2026-08-09 Discord draft says the reasoning "lives in the session logs where you can go and read it." False for every reader not developing the method: the plugin package ships neither `LOG/` nor `resources/`. Posting it as written points users at something they cannot access.
**Why this is the user's line:** Claude can draft the correction; only the user can post. Under the capability test, drafting is Claude-work and posting is not.
**Walkthrough:**
1. Claude drafts the corrected announcement, replacing the session-logs claim with the actual split: why the method behaves a certain way is in the shipped FAQ; why a rule is worded as it is stays in the development log.
2. The user says what to change.
3. The user posts it.
4. The user confirms; it's recorded and the line removed.
Rough draft, to sharpen at step 1 — the announcement's other content wasn't reviewed, so check the whole thing, not only the false sentence. Also decide at step 1 whether it's still worth announcing, given the week that has passed.
**Paced 2026-08-14 on the user's decision: last in a one-post-per-day chain.** Her reason, rendered in Claude's words rather than quoted: one a day, don't drown out the server. Last because it corrects an already-posted announcement rather than being news, so it yields to the three new posts. It lifts when [discord-post-session-start-strength] is posted and closed.
Blocked by: [discord-post-session-start-strength]

**Files:** none — the artifact is a Discord post. Relates to [self-authoring-rules].

#### Define this project's weekly release cycle, and amend the release model to run on it [weekly-release-cycle]
**Your decision, 2026-08-22: releases move from purely on-request to a weekly Wednesday cycle.** The pick costs no judgment — release the newest rezip at least a week old, so every change in it has had seven days of continuous dogfooding inside its successors; your correction that no single rezip runs a week is what led there. Due when the latest GitHub release is over seven days old and a week-old rezip exists — both observables. **Extended the same session by the three-channel model on [beta-tester-pathway]:** a Wednesday turn produces two events — this week's pick becomes the new beta, and last week's beta promotes to the stable release — so the cycle definition's steps carry both once those items are kept; the definition here stays buildable on the release half alone.

**The rule change this carries, gate-run at the build from this disposition.** CLAUDE.md's release section currently says a release runs when Alex asks and at no other time (your decision of 2026-08-09, made after stopping an automatic release twice). This narrows it: on request, or when the weekly release cycle falls due — and the reason the old failure does not recur is that the cycle asks no readiness question: the calendar and the git log settle which rezip goes, retrospectively, where the rejected middle option asked "is this good enough?" prospectively. On-request stays; the pre-rejected pause-before-publishing middle option stays rejected.

Rule gate: run — amendment to the Release section of CLAUDE.md, naming and superseding its at-no-other-time clause; the 2026-08-09 reasoning is outweighed on the stated ground rather than called wrong.

--- Build block ---
Changes: create this project's cycles doc with the release-cycle definition (artifact: the GitHub release; steps: pick newest week-old rezip, run the release ritual; cadence: weekly, Wednesday, declared; observable: the latest GitHub release's published date, plus the rezip log for a week-old candidate). `CLAUDE.md` — amend the Release section: a release runs when Alex asks, or when the weekly release cycle falls due; the at-no-other-time sentence is reworded to carry the cycle.
Acceptance: the cycles doc parses under the shipped check; CLAUDE.md's release section names both routes and still bars the pause-before-publishing middle option; release-ritual.md needs no change (the ritual itself is untouched).
Refused: choosing among candidate rezips each Wednesday — newest week-old wins, no judgment.
--- End build block ---
**Blocker repointed 2026-08-22:** the cycles machinery is built (`2026-08-22-cycles-definitions-and-due-checks-build.md`) with one behaviour unconfirmed, so the fact holding this item is the verification, not the build — the field now names the `[user]` verification line, and this lifts by itself when that closes.
Blocked by: [cycles-due-check-verification]
**Files:** `CLAUDE.md`, the new cycles doc. The dependency is host-side: the checks that read the definition must ship first.

#### The beta channel: each Wednesday's pick offered via Discord and a GitHub pre-release [beta-tester-pathway]
**Your idea, 2026-08-22, designed in the same session into a three-channel model — the standard release-channel shape (Chrome/Firefox), adopted on your terminology question.** Your day-to-day rezips are dev builds, yours alone and unchanged by this. Each Wednesday's pick becomes the **beta**: announced on the Throughliner Discord, hosted as a GitHub pre-release (Discord cannot host an install; the release ritual already builds and attaches zips), and offered to willing testers while it soaks for a week — you as the only tester at first, which is better than nothing and still a beta channel. After its week it promotes to **stable** and goes to the community listing ([marketplace-submission]). This superseded the earlier two-route question (repo-at-HEAD versus per-rezip artifacts): the weekly-pick artifact route won because it gives testers your chosen moments rather than every commit, and it reuses release machinery rather than adding a publish step to every rezip.
**Your sequencing, 2026-08-22, revised the same day: the channels launch together rather than beta-then-listing** — the community listing is itself part of how testers arrive, so the chain is beta channel + community listing (honestly framed as early), then YouTube videos pointing at them. Written on both items per the known-ordering rule.

**Kept 2026-08-22, held behind [weekly-release-cycle].** The three things the capture left open are settled. **Install route: a ref-pinned marketplace add** — research done at processing (`resources/research/claude-marketplace-listing-paths.md`, beta-channel section): a marketplace-add pins to a branch via `#ref`, so a `beta` branch fast-forwarded to each Wednesday's pick serves testers through the README's existing ask-Claude install shape, and no tester touches a zip; the pre-release zip stays as the release artifact. The research caveat travels: some ref-handling behaviour is covered by open feature requests, so the walkthrough is smoke-tested on a real second machine before any tester gets it. **Naming, your decision at processing: one cycle, called the release cycle** — beta is a step inside the Wednesday turn, not a sibling cycle; this build amends the one definition rather than adding another. **Offer wording:** drafted in the build, honestly-early testing framing; the launch announcement is the `[user]` line [beta-launch-announcement], filed with this keep.

--- Build block ---
Changes: amend the release-cycle definition in the cycles doc so one Wednesday turn carries both events — fast-forward the `beta` branch to the newest week-old rezip's commit, and promote last week's beta to the stable release; create the `beta` branch. `README.md` — add a beta-channel section: what beta means (honestly early), the tester install walkthrough (ask Claude to add the marketplace `FlintcraftTech/throughliner#beta` and install), and how updates arrive. Draft the Discord beta-offer announcement text into [beta-launch-announcement]'s walkthrough.
Acceptance: the cycles doc still parses under the shipped check with the two-event turn; the `beta` branch exists; README's beta section reads for a non-coder and matches the walkthrough smoke-test caveat (not offered to testers until smoke-tested); the announcement draft is under 2,000 characters.
Refused: a separate beta cycle with its own cadence — one cycle, beta as a step (the user's call); zip-download installs for testers — the ref-pinned marketplace add replaces it; a separate beta repo — a branch suffices.
--- End build block ---
**Understudy ordering, your decision 2026-08-22: the beta launch does not wait for it** — Understudy debuts as the standard companion app with the YouTube videos, after this channel and the listing; until then the beta materials carry the one-line caution against editing project docs while a run writes. Written on both this item and the marketplace item per the known-ordering rule.
Blocked by: [weekly-release-cycle]
**Files:** the cycles doc, `README.md`, QUEUE.md (the announcement item's walkthrough). The dependency is real, not just conceptual: the definition this amends is created by [weekly-release-cycle]'s build.

#### [user] Post the beta-channel launch announcement on the Throughliner Discord [beta-launch-announcement]
Filed 2026-08-22 with the keep of [beta-tester-pathway], which drafts the announcement text into this walkthrough as part of its build. The offer is framed honestly early — a testing invitation, not a product launch — and yields to the one-a-day posting chain like every other post. Launches alongside the community listing per your sequencing recorded on [beta-tester-pathway] and [marketplace-submission].
**Walkthrough.**
1. The draft is in this item once [beta-tester-pathway] builds; Claude walks you through any final edits.
2. Before posting, the tester install walkthrough must have been smoke-tested on a second machine — confirm that happened; do not post an install route nobody has run.
3. You post it on Discord, on a day no other Throughliner post goes out — Claude has no route to Discord.
4. You confirm; the send is recorded in `INBOX/sent.md` with what it claimed, and this line closes.
Blocked by: [beta-tester-pathway]
**Files:** none — the artifact is a Discord post.

#### [user] Approved Discord post about the comparison article now describes a superseded draft [comparison-article-post-needs-rewrite]
Found by Claude 2026-08-23 while walking the comparison-article item. A Discord post was drafted and approved on 2026-08-22 and has not gone out — it is recorded on `INBOX/sent.md` as approved and not yet posted, with its text verbatim in `LOG/2026-08-22-competition-comparison-article.md`. Its second paragraph announces the article and describes it as closing "on the coherence-over-scale trade", which was true of the 2026-08-22 draft.

That draft is superseded. The 2026-08-23 rewrite names a specific project rather than a category, adds a section on Papi as the nearest comparable tool, and ends on a shipped mechanism instead of a general trade-off — roughly 1,400 words against 900. A hold-note has already gone to the site project asking that the old one not be published.

**So the post cannot go out as approved.** Its first paragraph, about builds reading a generated view rather than the queue, is unaffected and still true. Only the second needs rewriting, and it needs rewriting after the article settles — it is currently out for review with the maker of one of the tools it names, and that review may change what the article ends on.

**Walkthrough.** 1. The article settles (external review back, revised text final). 2. Claude rewrites the post's second paragraph against the final article and shows the whole post. 3. You say what to change. 4. You post it, with the live article URL pasted in — Claude has no route to Discord. 5. You confirm, `INBOX/sent.md` is updated from approved-not-posted to posted, and this line closes.

**This is the repeal-falsifies-an-announcement case caught before it fired**, rather than after: the claim was recorded on the sent register, and the register is what made the collision findable when the article changed. It is also why the register records what a post claimed rather than merely that one exists.

**Kept 2026-08-24, held below the line on Claude's recommendation and your agreement.** As a capture this carried its ordering in prose because captures have no `Blocked by:`; as a held work item it carries the field, so it lifts by itself when the article item closes instead of being re-offered every session while the external review is out. The same ordering sentence is written on the article item per the known-ordering rule.
Blocked by: [competition-comparison-article]

## Unprocessed

#### Show-first approval moments produce their text twice [approval-flow-token-doubling-simplification]
Captured by you (2026-08-01) while reviewing your Claude Code feature request anthropics/claude-code#77134. Rescoped at your direction 2026-08-13 from a larger item about approval-time doubling generally.
**The cost, narrowed to where it still exists.** Showing text in chat and writing it to a file are both the model producing those tokens, so text doing both is produced twice. That used to hit every approval moment; it no longer does — write-first shipped, and the post-write report is one line naming what landed, never a re-paste.
**What remains is the show-first set only** — the moments write-first deliberately keeps showing first, because the previous version isn't recoverable without the user: a commit message, anything leaving the machine, a wholesale conversion of a document the user already owns. There the text is composed in chat, approved, then produced again to be used.
**Why it is not buildable yet.** The saving needs the harness to surface an already-produced Write's content verbatim with no second model pass — issue #77134, which hasn't landed. Until it does there's no build to describe. Re-examine when the issue ships.
**Two things settled, not to be re-opened here.** The write-first ordering flip is decided and shipped. The convergence note about view-in-doc machinery is spent — working-mode field, Editor field and line-anchored-link promise all retired 2026-08-09.
External dependency: anthropics/claude-code#77134.

**Checked 2026-08-19 and still open** — filed 2026-07-13, labelled `enhancement`, `area:cost`, `area:tools`, `area:core`, no maintainer response and no close date. The disposition is unchanged: nothing to build, re-examine when it ships. **What the check buys is that the next session reads a date rather than re-running the lookup**, which is the whole reason it is written here.

**Two things in the discussion are worth having when this does become buildable.** A comment dated 2026-08-01 sets out the mirror direction — author-in-chat, approve, then write — and argues it needs no second primitive, because a workflow that can show a Write's content verbatim can adopt write-first ordering and get the same saving so long as rejection reverts. That is this project's shipped model described from the outside. **It looks like yours, on the date and the reasoning, but nothing in the record here says so — worth confirming rather than assuming.** A later comment proposes generalising the primitive to `show_file(path, range?)`, which would also let Claude surface parts of *existing* files without re-emitting them — that reaches the view-in-doc pointer and the inline-text offer, not just the three show-first cases, so it would widen this item rather than merely unblocking it.

**Surfaced 2026-08-19 by the decay rung, on its first firing since the interleave was adopted.** It had been the oldest entry in the queue at 17 days and nothing in the ladder had ever reached it.

**Dated 2026-08-21 with your approval — the field's first use, as this item predicted.** It waits on `anthropics/claude-code#77134`, which nothing in this queue can resolve; five weeks open with no maintainer movement, so a month out is when there is plausibly news. Not offered again before then.
Not before: 2026-09-21

**Skipped again 2026-08-19, and it is the item that produced the fix for its own condition.** Presented, found unchanged, and in being presented it made the pattern visible: three entries in one session waiting on something outside this project, none able to name a blocker, all re-offered every session. That is [not-before-reaches-unprocessed], kept and cleared in the same session. **This is its first candidate** — once `anthropics/claude-code#77134` ships, or a date is worth guessing at, the field goes here and the re-offering stops. Until the field is built there is nothing to write, so the skip stands.

#### A personal bridge pushing `[user]` items into Taskflow as tasks, and reading completions back [taskflow-personal-bridge]
**Raised by you 2026-08-19**, from executing in another of your projects where the work is mostly `[user]` items. Your framing: Throughliner becomes an executive layer over projects Claude only half-implements, and sometimes you do not want to complete things in conversation — you want the to-do list. **The mapping is yours: a `[user]` item is a task, and its steps are subtasks.** The assessment below and the decomposition are Claude's.

**Your mapping is Taskflow's own model rather than an approximation, which is what makes the completion half work.** Taskflow's subtasks inherit their parent's Project, date and placement, and a parent has no checkmark of its own — it is complete only when every subtask is, and un-checking any child pulls the parent back out of the completed tray. So completion arrives as one derived signal per item, and a half-done item cannot read as done.

**Two decisions of yours, in your own words.** Route: *"design this against the file-based route now, with MCP as the later transport."* Scope: *"agreed, personal bridge to start."* So this is host-only tooling in this project, assuming your own Drive setup, and it ships nothing to consumers; promoting it to a shipped feature is a later decision this does not pre-empt.

**Why file-first rather than MCP.** Taskflow's paid tier already designs the channel — Claude reaching Taskflow through a remote MCP server — so Throughliner would be a client of a route Taskflow intends rather than an integration Taskflow's local-first principle forbids. But `[0020-remote-mcp-server]` and `[0019-ai-choice-flow-and-mcp-setup]` are unbuilt over there and cloud sync is their precondition. The same reasoning is already on their queue in `[strategy-doc-preview]`, in your words there: *"We're just here, so we don't need the MCP."*

**Two hazards found by reading their SPEC rather than assumed.** Taskflow runs on the phone against a local Room database, so a file route means producing a file that is carried onto the device, not writing where Taskflow reads. And their existing `[0014-json-export-and-import]` is a whole-database export and restore — pushing tasks through it would replace every other task in the app. What this needs is an **additive** import, which Taskflow has neither built nor designed.

**Not designable here yet, and that is why this is a capture.** The file contract, which Taskflow Project a pushed item lands in, and when a push happens all depend on what Taskflow agrees to build — so the second limb cannot be stated. It waits on [taskflow-bridge-request].

**Dated 2026-08-21 with your approval.** It waits on Taskflow's reply to the three questions mailed 2026-08-20, which nothing in this queue can produce; a week is when there is plausibly news, since Taskflow is your own project. Not offered again before then.
Not before: 2026-08-28

**One thing to settle at processing regardless of their answer:** a `[user]` item's text can name real people or client details, so what crosses the boundary needs the scrub the queue already gets, and a pushed task is leaving this project's records.

#### Submit Throughliner to Anthropic's community marketplace, as step one toward in-app browsability [marketplace-submission]
**Your goal, 2026-08-22: actual release to the Claude marketplace so people can browse for it inside the desktop app.** The research (`resources/research/claude-marketplace-listing-paths.md`) found two routes: the official marketplace is the only one browsable in-app by default, is curated at Anthropic's discretion, and has no self-serve path — the submission form feeds the community marketplace instead. So the realistic sequence is community first: submission via the clau.de/plugin-directory-submission form, automated security scanning plus human review, a public listing at claude.com/plugins pinned to a commit SHA; then official at Anthropic's discretion.
What a keep must settle: ending the pre-release posture CLAUDE.md declares ("in active testing, not ready for the Claude marketplace") — the user's decision; version-consistency discipline (plugin.json, changelog, git tags — the commonest reported rejection cause; the weekly release cycle [weekly-release-cycle] supplies the cadence for it, and a changelog does not yet exist); and confirming the name is final, since a marketplace slug is immutable once published and a rename breaks every install. The submission itself is a `[user]` step — a web form Claude cannot submit.
Runs behind [weekly-release-cycle] in spirit — a regular release rhythm is what makes the version discipline real — carried as this sentence rather than a blocker, since the submission decision is independently the user's.
**Reframed 2026-08-22, same session: the listing is the stable channel of the three-channel model settled on [beta-tester-pathway].** The research question this paragraph used to flag is answered — see below.
**Your sequencing, 2026-08-22, revised the same day: the listing launches alongside the beta channel rather than after it** — your first thought was beta testers before any listing, revised when it emerged the listing may be the only realistic way testers arrive; the listing is framed honestly as early instead. YouTube videos come after both, on your reasoning that videos without a listing would look bad to viewers while YouTube may bring the very first users. Written on both items per the known-ordering rule.
**Update-cadence research answered, 2026-08-22** (`resources/research/claude-marketplace-listing-paths.md`, listing-updates section): the listing's commit pin updates **only after re-review**, and no turnaround is documented anywhere — so the Wednesday stable promotion cannot push to the listing. The realistic shape: the weekly stable channel lives on this repo, and the listing is updated on a slower submit-and-wait rhythm — monthly, or when something worth announcing lands — worded as "submit the update".
**Your decision, 2026-08-22: the not-ready-for-the-marketplace posture ends.** You are ready to remove it; the one thing genuinely holding the submission is company registration, which is [abr-identity-and-address] on the flintcraft.tech project's queue — designed there with its research done. A dependency note was sent to that project's INBOX the same day (recorded in `INBOX/sent.md`); it asks no new work, only flags that a second project now waits. Whether the submission form itself actually requires registered-business details is unverified — check at keeping.
**Dated 2026-08-22 with your approval.** It waits on the ABR work in another project, which nothing in this queue can build; a month out is when there is plausibly news. Not offered again before then. Still to settle at the eventual keep: the changelog, and confirming the Throughliner name is final (the slug is immutable).
**Understudy ordering, your decision 2026-08-22: the launch does not wait for it.** Understudy debuts as the standard companion app with the YouTube videos (already last in the chain); the listing stays silent on it until it is real. Until a companion app honouring the editing-state contract is out, launch materials carry one honest line: don't edit the project docs while a run is writing them. A dependency note went to Understudy's own project INBOX the same day (recorded in `INBOX/sent.md`). Written on both this item and the beta-channel item per the known-ordering rule.
Not before: 2026-09-22

#### Multi-user identity layer: who is in the session, how they're identified, and how attribution reaches provenance and LOG [multi-user-identity-layer]
**Split 2026-08-24 from [github-identity-naming] on your decision: the narrow naming rule builds now, this remainder is captured and designed later.** Multi-user Throughliner has never been developed — SPEC assumes one user throughout, the provenance conventions ("captured by you") and the lint's possessive list hard-code a single addressee. Design questions carried from the original item: where identity is read from (git config? the GitHub remote? asked at /setup?), what happens when a participant has no GitHub profile, and how per-person identity meets the scrub rule now that the published-identity arm exists.
**A consumer report, folded in 2026-08-24, gives this a second concrete gap: attribution.** A project running this method has several people prompting one session from a shared channel, one of them holding execution authority. The provenance rule is binary — Claude's work unmarked, the human's credited to "you" — and has no answer with three humans: a capture one person raised, a decision argued between two, a rationale one supplied and another corrected all read as one undifferentiated "you", or as nothing. The LOG has the same gap — the throughline survives but cannot say which person made a call, and attribution is the part a person notices missing about their own contribution. The report names the hard half: recording a name is easy; deciding whose idea something was mid-session, without interrogating people, is not — and the containment test (a reply contained in Claude's preceding message evidences no origin claim) gets harder when the preceding message may have been answering someone else. The sender proposes no fix, deliberately — the shape of the answer is this project's to decide — and carries on under the single-user reading meanwhile; sent for continuation, so no reply is owed. Full report text, as extra info only: `INBOX/archive/2026-08-23-from-chagora-multi-person-attribution.md`. Design the two together: naming (this item's first half) is what attribution would write down.

#### Close asks the user whether to delete the generated build view [build-view-delete-ask-at-close]
Reported by you 2026-08-24, from a demo build session's /done, filed for a later /plan. At the close, before committing, Claude presented: the view file is throwaway, regenerated by /next from the queue each run, never committed so deleting is unrecoverable from git but nothing is lost — ending on "Shall I delete BUILD-VIEW.md?" This is machinery leaking into the close's narration: the generated view's lifecycle should never be the user's problem, and a question at the close is exactly the over-asking the method removes. To settle at processing: what the view machinery actually specifies for the file's lifetime (cleaned by the close silently, gitignored and left, or regenerated over), and whether the demo session's ask means the lifecycle was never specified — in which case the fix is specifying it, not just deleting the question.

#### Plan session verified by reading code and narrated the code at the user [plan-code-verification-narration]
Reported by you 2026-08-24, from a live plan session in another of your projects, filed for a later /plan. While working a `[user]` item's walkthrough, the session read the page's source against the walkthrough and reported its findings in code-level terms — "the checkbox label really is…", "the saving code is wrapped so that if the browser refuses storage it fails quietly". Two concerns, distinct and both yours: whether verification of this kind is supposed to happen in a plan session at all, and that code-level narration reached a non-coder either way — output-facing text is supposed to read cleanly for the method's audience whatever was read to produce it.
Worth noting at processing: the reading itself may be sanctioned — plan.md's keep-step directs confirming a walkthrough step can produce the observation it names, and /plan is named as the site where trying is free — so the defect may be narration register rather than the act. That boundary is [plan-claim-testing-proportionality]'s territory (cleared, unbuilt), and this item should be processed after that build lands, so the two rules are written against each other rather than past each other.

#### End-of-queue gate: the refill-reset is unstated, and a declared stay-open intent has no standing effect [end-of-queue-gate-refill-and-standing-intent]
Raised by you 2026-08-24 in the planning session that filed this, in two halves, both left with the process-or-file offer open at the close — filed by the wind-down re-scan.
First half: the once-per-rest bound says the wrap-up question fires again "only after further work has filled the queue and emptied it once more", but nothing states plainly that this resets the ask — this session hit two such refills and wrongly ended plainly both times, suppressing questions the rule owed, and its "plain" endings drifted into close-leaning framing ("whenever you're ready, the close records…"), which nudges toward the exit the gate is worded not to nudge toward.
Second half: you declared you were keeping the session open to capture while testing in other sessions, and the method has no notion of that standing intent — another session told the same thing kept presenting the pre-close message. A declared capture-inbox mode should silence the wrap-up ask for the rest of the chat; the intent is chat-held, like the inline-text switch, so nothing is stored.

#### Keep approvals rest on a post-write reading the user isn't doing [keep-approval-reading-burden]
Raised by you 2026-08-24: kept items felt un-okayed — "Claude skips an entire turn, doesn't present anything for approval and just keeps something". The offer-position half became [proactive-capture-offer-drift]; this is the other half, unfiled until the close's wind-down re-scan caught it. Write-first's contract is that the user reads the written item in QUEUE.md after the one-line report and rejects what's wrong; you report you generally don't open the file, so the approval the design assumes rests on a reading that isn't happening, and the fold rule (agreement in-exchange skips the standalone recommend turn) compounds it by removing the one moment the item's substance would otherwise be re-stated. To weigh at processing: whether the report should carry more of the item's substance for keeps specifically, whether show-first should be offered when a session's keeps run high, or whether this is accepted as the trade write-first already states — the record shows the trade was stated ("the risk it introduces is not rejection but the user not noticing"), so this may be that stated risk arriving rather than a new defect.

