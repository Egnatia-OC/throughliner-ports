# PATTERNS — Test Project

## Authentication flow

All API calls require a valid session token. The token is obtained via the login endpoint and expires after 24 hours.

## Error handling

Errors are displayed inline below the input that caused them. Toast notifications are reserved for system-level failures.

## Proposed edits pending

*Sovereign Implementer — Version 52.*
