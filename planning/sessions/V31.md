# V31 — NO-CODE-METHOD.md retirement / cleanup

## Goal

Confirm every rule in `NO-CODE-METHOD.md` has a home in a plugin component. Retire the file or replace it with a one-line pointer.

## Inputs

- V18–V30 outputs (V29 folded the *New-project* and *Existing-docs migration* routes into `/adopt`'s case branches; V30 migrated `DOC-STRUCTURE.md` into the plugin — both shape what's left to retire)
- `NO-CODE-METHOD.md` (post-V30 state)
- `planning/INVENTORY.md` (source-of-truth for what's been distributed where)

## Outputs

- Coverage map: every section/rule in V17's `NO-CODE-METHOD.md` cross-referenced to its plugin component(s)
- `NO-CODE-METHOD.md` retired or replaced with a one-line pointer (decide this session)
- Rules with no plugin home flagged and either backfilled or scheduled for follow-up
- Commit tagged `v31`

## Success criteria

- A project using the plugin no longer needs `NO-CODE-METHOD.md` — governance comes from plugin components
- Coverage map is complete
- Removing `NO-CODE-METHOD.md` from a test Taskflow project doesn't break anything

## Open questions

- Keep `NO-CODE-METHOD.md` as a stub for backward compatibility (older projects), or remove fully?
- If kept as a pointer, where does it point — plugin's bundled docs, marketplace listing, GitHub README?

## Risks / dependencies

- Coverage map may surface missed rules needing backfill — add follow-up session if non-trivial
- Existing projects (Taskflow) may still reference `NO-CODE-METHOD.md` — decide migration path
