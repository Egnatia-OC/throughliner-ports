# V36 — Doc-only: TEST-LOG ordering, planning's BACKLOG authority, plan-panel research

## Goal

Three small open-question resolutions, all doc-only or research, bundled because they don't interact and none warrants its own session. A light follow-up after V35's E2E so low-friction wins ship without competing with V35's refinement-list sessions for substantive scope.

1. **Flip TEST-LOG.md to newest-first** (resolves OPEN-QUESTIONS *TEST-LOG ordering — newest at top vs bottom*). Matches BUILD-LOG ordering and the no-coder's natural read order. Current bottom-first convention traces to the dev-internal TEST-LOG argument; the consumer-side TEST-LOG has different access patterns (drift check 4, planning read-back) where recency matters. **Deliberately split from the paired TEST-LOG row-pruning question** — pruning needs real Taskflow row-count data before deciding a cutoff; ordering doesn't. Pruning stays parked for V37+.

2. **Explicit assertion of planning's structural authority over BACKLOG.md in *During planning*** (resolves OPEN-QUESTIONS *NO-CODE-METHOD.md → During planning doesn't explicitly assert planning's structural authority over BACKLOG.md*). Shape A from the entry — one-line addition to *During planning*'s opening paragraph. Smallest defensible move; the assertion fits in a sentence. **Slight deviation from the entry's "parked until *During planning* next needs an edit" framing** — pulled in here because the session already opens *During planning* for #1's edits, so piggybacking is cheaper than waiting indefinitely for an unrelated trigger.

3. **Web-search check on Claude Code plan-panel writability** (partially advances OPEN-QUESTIONS *Surface the current build sequence in Claude Code's plan panel*). Not a full resolution — just clears the research blocker. Two outcomes: panel isn't programmatically writable → question collapses to a `Crash course.md` note about the limitation (fold in this session); panel is writable → entry stays parked for V37+ paired with the vocabulary-collision question, with *Working notes* updated to record the finding. **The design-decision half stays parked** — V36 only does the research, regardless of the search outcome.

## Inputs

- `plugin/docs/DOC-STRUCTURE.md` and root `DOC-STRUCTURE.md` (both, per two-write) — *TEST-LOG.md structure* → append rule and ordering language
- `plugin/templates/TEST-LOG-TEMPLATE.md` and `templates/TEST-LOG-TEMPLATE.md` (both, per two-write) — HTML format-reminder comment position moves from bottom to top
- `plugin/agents/after-build.md` — where TEST-LOG rows are appended (operating procedure inline post-V32); ordering language updates
- `plugin/agents/planning.md` — where TEST-LOG rows are read for test-session close and drift check 4; ordering language updates
- `plugin/hooks/universal-behaviour.md` and root `NO-CODE-METHOD.md` (both, per two-write) — *During planning* section, opening paragraph for #2
- `Crash course.md` — possible fold destination for #3 if the panel isn't writable
- `planning/OPEN-QUESTIONS.md` — TEST-LOG ordering and BACKLOG-authority entries already removed at V36 promotion (V34 commit); plan-panel entry will be updated with research outcome at session close
- The original OPEN-QUESTIONS entry text for each (accessible via `git show v33:planning/OPEN-QUESTIONS.md` if the V34 removals have already landed)

## Outputs

- TEST-LOG ordering flipped in both `DOC-STRUCTURE.md` copies, both `TEST-LOG-TEMPLATE.md` copies, `after-build.md`, `planning.md`. Existing rows in any project's live TEST-LOG: not migrated — newest-first is the rule going forward; older rows stay where they are. Transition stance documented in the changed `DOC-STRUCTURE.md` rule.
- One-line BACKLOG-authority assertion added to *During planning*'s opening paragraph in `plugin/hooks/universal-behaviour.md` and root `NO-CODE-METHOD.md` (two-write).
- Web-search outcome for plan-panel writability captured. If not writable: short `Crash course.md` note about the limitation, plan-panel OPEN-QUESTIONS entry removed. If writable: entry's *Working notes* updated with the finding, entry stays parked with revised *Next step* pointing at V37+ pairing with vocabulary collision.
- `BUILD-LOG.md` V36 entry. Footer bumps to V36 across the plugin-side and docs-only sets per `BUILD-METHOD.md` → *Footer bumps* (the *During planning* edit alone qualifies as a substantive method change).
- Commit tagged `v36`.

No smoke test required — V36 is doc-only with no testable code change. (See `BUILD-METHOD.md` → *Doc-only sessions*.)

## Success criteria

- Both TEST-LOG-TEMPLATE.md files have the format-reminder comment at the top; the append rule in both DOC-STRUCTURE.md copies reads newest-first; after-build's append step and planning's read-back step describe the new ordering consistently.
- *During planning*'s opening paragraph in both canonical doc locations asserts planning's structural authority over BACKLOG.md in one sentence, without redundant restatement elsewhere.
- Plan-panel question's status in OPEN-QUESTIONS reflects the research outcome — either the entry is gone (collapsed to a `Crash course.md` note) or *Working notes* records what the search found and *Next step* is revised.
- Doc-code parity audit at close finds no ghost references to the old TEST-LOG ordering and no inconsistency between the two-write copies.

## Open questions for this session

- **#1 transition stance phrasing.** "Existing rows stay put; new appends go on top" — how prominently does this need stating? Likely a one-line note under the changed rule in DOC-STRUCTURE.md. Confirm at session open.
- **#2 exact wording.** The OPEN-QUESTIONS entry leaves "structural authority over BACKLOG.md" as the substance but doesn't pre-write the sentence. Draft at session start; one-line target.
- **#3 web-search prompt shape and timing.** Per project `CLAUDE.md` → *Working with me*, the search needs to be a Claude-Sonnet-paste prompt in a fenced block. Decide whether the search runs at session open (so its outcome shapes the rest of the session) or at session close (so #1 and #2 ship even if the search comes back ambiguous).

## Risks / dependencies

- **Frame-correction sweep risk on #1.** Flipping TEST-LOG ordering is a frame change for future-Claude reading old BUILD-LOG entries that mention "the bottom of TEST-LOG" or similar positional language. Run the sweep per `BUILD-METHOD.md` → *Session close* step 2. Existing BUILD-LOG entries reference TEST-LOG row IDs (stable) rather than positional language, so the sweep surface is likely small — confirm empirically at close, don't assume.
- **Two-write rule discipline on #1 and #2.** Both edits touch plugin-side and docs-only canonical docs. The two-write rule (`BUILD-METHOD.md`) requires both sides bumped together. Easy to half-finish; the close-time parity audit catches it.
- **V35 refinement collision.** V35 will produce a refinement list that may itself touch TEST-LOG ordering, *During planning*, or plan-panel territory. If V35 refinements end up wanting any of these, V36 either folds the refinement in or reshapes. Triage at V35 close before V36 starts.
- **#3 may produce nothing actionable.** Web search could be inconclusive (sparse docs, ambiguous feature shape). If so, plan-panel entry stays parked, *Working notes* records "searched [date], inconclusive — re-check post-V37." Session still ships #1 and #2.
- **No plugin-code changes.** V36 doesn't exercise hooks, subagents-as-runtime, or `--plugin-dir`. No smoke test, but the doc-code parity audit still runs at close.
