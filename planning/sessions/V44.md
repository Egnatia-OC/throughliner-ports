# V44 — /adopt UX + per-project opt-out

> **Renumbered from V42 → V43 in session v40, then V43 → V44 in session v41 (2026-05-21)** when the v41 scoping session took the V41 slot. Content unchanged except for the v41 fold-in note below.

## Goal

Two sides of the same coin: (1) make the `/adopt` experience navigable for new users who DO want the method (permission-prompt surface under marketplace install, narration at each detection step, CLI vs. desktop-app input parity), and (2) give users who DON'T want the method in a given project an obvious, intuitive off-ramp (the plugin fires globally after marketplace install; the current opt-out path — running `/adopt` and picking the leave-alone case — is non-obvious and confusingly named).

## Inputs

- `plugin/agents/adopt.md` — the /adopt subagent (five case branches)
- `plugin/hooks/session_start.py` — unadopted-folder advisory wording
- `plugin/hooks/pre_tool_use.py` — adoption enforcement gate
- `Crash course.md` — install section, first-run guidance
- `.no-code-method-skip` marker mechanism (written by /adopt case 2/3 leave-alone)
- Claude Code's `claude plugin disable --scope project` (platform-level alternative)

## Outputs

- **Test results:** `/adopt` end-to-end via marketplace install on a fresh, never-adopted folder — both CLI and desktop app if possible
- **Permission-prompt surface comparison:** does marketplace install change the per-tool prompt surface vs. `--plugin-dir`?
- **Opt-out UX decision:** is `.no-code-method-skip` via `/adopt` leave-alone sufficient, or does the plugin need a more prominent off-ramp? Options: clearer wording in the SessionStart advisory itself ("run /adopt to set up, or /adopt to opt out" is confusing); a `/skip` or `/opt-out` alias; a one-line "not interested" path surfaced directly in the advisory without requiring `/adopt`.
- **Narration improvements:** plain-English preamble at each `/adopt` detection step so the user knows what they're approving and why
- **CLI vs. desktop-app parity:** verification that every `/adopt` dialogue step is navigable without pre-fills (desktop app has no pre-fill; CLI does)
- Updated `adopt.md`, SessionStart advisory wording, Crash course as needed

## Success criteria

- A new user running `/adopt` on a fresh folder can navigate every step without the Crash course open beside them
- A user who doesn't want the method can opt out in one obvious step from the unadopted-folder advisory — without needing to know that `/adopt` has a leave-alone branch
- CLI and desktop-app paths both work without confusion
- No regression in existing `/adopt` case branches (cold-start, migration, refresh, already-adopted)

## Open questions for this session

- **Does marketplace installation change the permission-prompt surface?** If the user approved the plugin at install time, do individual tool calls (Bash, python one-liners, file reads) still prompt individually? This determines whether narration improvements are needed or whether the friction is already gone.
- **Should there be a `/skip` or `/opt-out` command?** Separate skill-command that just writes `.no-code-method-skip` and confirms — no detection steps, no case logic. Lighter than routing through `/adopt`.
- **Should the SessionStart advisory offer the opt-out directly?** Current advisory says "run `/adopt` before doing anything else." Could instead say "run `/adopt` to set up, or `/skip` to opt out." Requires the `/skip` command to exist.
- **`/adopt` naming.** The command name implies "start using it," which is misleading when half its cases are about NOT using it. Rename to `/setup`? Or keep `/adopt` and add `/skip` as the complement?

## Risks / dependencies

- Desktop-app behaviour may differ from CLI in ways not discoverable without hands-on testing. This session may need Alex to run both and report back.
- Changes to SessionStart advisory wording are in `session_start.py` — same file as foundational reads and routing logic. Edit carefully.
- No hard dependency on V39–V42. Could run in any order. Placed after V42 because V39–V42 are higher priority for method substance; this is UX polish.
- **Consider folding V46 (`cd`-shifts-cwd marker-walk-up fix) into this session.** V46 (renumbered from V47 in session v43) is small (single-file, decisions pre-made) and touches the same adoption-gate hook logic (PreToolUse's `has_opt_out_marker`). If V44's actual surface comes in lighter than expected, V46 is a natural add-on — decide at session start. Filed v41 scoping session, 2026-05-21; renumbered v43, 2026-05-22.

---

## V44 mid-session handoff (session v46, 2026-05-22)

Session ran out of context. All changes below are **uncommitted** — no git commit, no tag, no footer bumps yet.

### Decisions made

1. **Q1 (marketplace permission surface) — RESOLVED.** Research confirmed no difference between marketplace and `--plugin-dir` at the permission-prompt level. Written to `research/marketplace-install-permission-surface.md`.

2. **Q2–Q4 (naming, opt-out mechanism, advisory wording) — PARTIALLY RESOLVED.**
   - Alex supports renaming `/adopt` → `/setup`. **Not yet implemented** — the rename touches many files and was deferred so the marker removal could land cleanly first.
   - The `.no-code-method-skip` marker architecture has been **removed from the public-facing plugin**. The plugin no longer offers to write or clear the marker file. User-facing opt-out is now Claude Code's built-in per-project plugin disable: `/plugin` → Installed → toggle off.
   - `/skip` command is **not needed** — the built-in plugin disable replaces it.
   - `/plugin` toggle only works for marketplace-installed plugins. `--plugin-dir` plugins don't appear in the `/plugin` UI. The dev project's existing `.no-code-method-skip` file is kept as a legacy escape hatch — `is_unadopted_with_work()` still honours it via `_LEGACY_SKIP_MARKER`.

### What's been changed (uncommitted)

**Marker architecture removal — DONE across all nine files:**

| File | What changed |
|---|---|
| `plugin/scripts/project_state.py` | Removed `OPT_OUT_MARKER_NAME`, `has_opt_out_marker()`, `ADOPT_CASE_OPTED_OUT` (case 5). Added `_LEGACY_SKIP_MARKER` check in `is_unadopted_with_work()` for dev-project compat. Kept `.no-code-method-skip` in `INFRA_NAMES`. `detect_adopt_case()` now returns 1–4 (four cases). |
| `plugin/hooks/session_start.py` | Removed opt-out imports. Updated advisory text: points at `/setup` and `/plugin` toggle. Removed opted-out references from comments and docstrings. |
| `plugin/hooks/pre_tool_use.py` | Removed `OPT_OUT_MARKER_NAME` import. Removed marker from `SCAFFOLD_NAMES`. Updated deny messages: point at `/plugin` toggle instead of marker. Updated comments/docstrings. |
| `plugin/agents/adopt.md` | Removed Case 5 (opted out) entirely. Cases 2 and 3 cancel/leave-alone options no longer write a marker — they confirm "no changes made" and point at `/plugin` toggle. Frontmatter updated: four cases, not five. |
| `plugin/skills/adopt/scripts/scaffold.py` | Removed `ADOPT_CASE_OPTED_OUT`, `has_opt_out_marker`, `OPT_OUT_MARKER_NAME` imports. Removed case 5 from `CASE_NAMES`. `cmd_detect_case` no longer emits `opt_out_marker_present`. |
| `plugin/skills/adopt/SKILL.md` | Updated description: four cases, removed opt-out language. Recap list updated. |
| `plugin/hooks/universal-behaviour.md` | Updated unadopted-folder routing: removed marker reference, added `/plugin` toggle guidance. |
| `plugin/docs/VOCABULARY.md` | Updated "Unadopted folder" entry: removed marker, added `/plugin` toggle. |
| `Crash course.md` | Added "Disabling the plugin in a specific project" paragraph in install section. Updated safety-net section: removed marker references, removed Case 5, added `/plugin` toggle paragraph. |

**Other changes:**

| File | What changed |
|---|---|
| `research/marketplace-install-permission-surface.md` | NEW FILE — Q1 research findings. |
| `CLAUDE.md` (this project's) | Added "Dev-project marker file" note explaining why `.no-code-method-skip` stays at repo root. |

### What's NOT done yet

1. **`/adopt` → `/setup` rename.** Alex approved the rename but it hasn't been implemented. This touches: `plugin/skills/adopt/` directory (rename to `plugin/skills/setup/`), `SKILL.md` frontmatter (`name: adopt` → `name: setup`), `plugin/agents/adopt.md` (all `/adopt` references → `/setup`), `plugin/hooks/session_start.py` (advisory text already says `/setup` in some places from the marker removal — needs a consistency pass), `plugin/hooks/pre_tool_use.py` (`ADOPT_SUBAGENT_TYPE` constant), `plugin/hooks/universal-behaviour.md`, `plugin/docs/VOCABULARY.md`, `Crash course.md` (throughout), `plugin.json` if it registers the skill name. **Note:** some files already reference `/setup` from the marker-removal edits (session_start.py advisory text). Others still say `/adopt`. The rename pass needs to make everything consistent.

2. **V46 fold-in decision.** V46 scope was about making the marker walk up directories when `cd` shifts cwd. With the marker architecture removed from the public plugin, V46's scope is largely moot for marketplace users. The legacy `_LEGACY_SKIP_MARKER` in the dev project could still benefit from a walk-up, but this is lower priority now. Decide whether to fold, defer, or close V46.

3. **OPEN-QUESTIONS items 1, 2, 7 fold-in.** V44 scope mentioned potentially folding items from "Post-adopt and mid-loop UX friction" in OPEN-QUESTIONS. Not yet addressed.

4. **Session close procedure.** No footer bumps, no BUILD-LOG entry, no doc-code parity audit, no commit, no tag. All pending for the continuation session.

5. **Narration improvements and CLI vs. desktop-app parity** — not yet addressed (V44 outputs 4 and 5).

### Where to start the next session

Pick up at the `/adopt` → `/setup` rename. It's the next concrete task — all the marker-removal edits are done and consistent. After the rename, do a grep sweep for any remaining `/adopt` references that should be `/setup`, then move to the remaining V44 outputs and session close.
