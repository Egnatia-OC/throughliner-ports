# V29 — DOC-STRUCTURE.md content migration + Crash course coherence pass

## Goal

Move `DOC-STRUCTURE.md`'s content into the plugin (decision: skill body or bundled reference doc — deferred from Chunk A in V17). Update `Crash course.md` to reflect the plugin architecture so the human-facing reference doc stays current.

## Inputs

- V18–V28 outputs
- `DOC-STRUCTURE.md`
- `Crash course.md`
- `planning/INVENTORY.md` (to know the final plugin shape Crash course should describe)

## Outputs

- Plugin component containing DOC-STRUCTURE content (skill body in `skills/doc-structure/SKILL.md` OR bundled reference doc under `docs/` — decided this session)
- Updated `Crash course.md` reflecting plugin architecture
- Commit tagged `v29`

## Success criteria

- A planning or migration session loads DOC-STRUCTURE content from the plugin (not from a project file)
- Crash course reads cleanly as a description of the plugin-based method, not the markdown-based one
- A first-time reader of Crash course can install the plugin and use the method without needing the original `NO-CODE-METHOD.md`

## Open questions for this session

- Skill body vs bundled reference doc for DOC-STRUCTURE content — which is more useful at decision time? (Skill body: Claude reads on demand. Bundled doc: a hook script can grep it without invoking Claude.)
- How much of Crash course needs rewriting vs incremental update — full rewrite or surgical pass?
- Should Crash course move into the plugin's bundled docs entirely, or stay in the method-development project for human authors?

## Risks / dependencies

- Crash course rewrite may surface plugin design gaps that require backtracking to earlier sessions
- DOC-STRUCTURE content destination affects how migration (V28's `/adopt` case 3) loads the spec — may need to revisit V28 if the destination changes
