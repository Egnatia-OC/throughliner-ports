# V28 — NO-CODE-METHOD.md retirement / cleanup

## Goal

Confirm that every rule in `NO-CODE-METHOD.md` has a home in a plugin component. Retire the original method file or replace it with a one-line pointer to the plugin.

## Inputs

- V18–V25 outputs
- `NO-CODE-METHOD.md`
- `planning/INVENTORY.md` (the source-of-truth for what's been distributed where)

## Outputs

- A coverage map: every section/rule in V17's `NO-CODE-METHOD.md` cross-referenced to its plugin component(s)
- `NO-CODE-METHOD.md` retired entirely OR replaced with a one-line pointer (decided this session)
- Any rules that have no plugin home are flagged and either backfilled (small additions to existing components) or scheduled for a follow-up session
- Commit tagged `v28`

## Success criteria

- A project using the plugin no longer needs `NO-CODE-METHOD.md` — all governance comes from plugin components
- The coverage map is complete: every rule is accounted for
- Removing `NO-CODE-METHOD.md` from a test Taskflow project doesn't break anything

## Open questions for this session

- Should `NO-CODE-METHOD.md` remain as a stub for backward compatibility (existing projects on older method versions), or fully removed?
- If kept as a pointer, where does it point — the plugin's bundled docs URL, the marketplace listing, the GitHub README?

## Risks / dependencies

- Coverage map may surface missed rules requiring backfill — add a small follow-up session if backfill is non-trivial
- Existing projects (Taskflow specifically) may still reference NO-CODE-METHOD.md — decide migration path for those
