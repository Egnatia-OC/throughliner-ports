# Build log

A running record of decisions, changes, and reasoning. Newest first. Written for a friend skimming, not an auditor — half a page per session, less when possible.

For format details, see `BUILD-METHOD.md` → *BUILD-LOG entry shape*.

---

## V27 — 2026-05-17 — After-build subagent + test-confirmation gate hook + SessionStart TEST-LOG tripwire + [Requested]/[Suggested] labels in BACKLOG.md

**What shipped.** The test-confirmation gate machinery whose prose V26 shipped, now wired end-to-end. New `plugin/agents/after-build.md` owns the *After every build* phase as a standalone subagent — silent MANIFEST.md update (fully automatic per V27 Q2), plain-English build recap with `[Requested]`/`[Suggested]` labels read off BACKLOG.md (per V27 Q3), test-session-open by appending blank-Status rows to TEST-LOG.md, prompts to refresh-test-and-bring-outcomes-to-planning. Idempotent — if TEST-LOG already has rows for the current session, it exits with a brief "test session already open" note (covers Stop-hook re-fires when a user types after after-build's first run). `plugin/agents/batch-executor.md` had the *After every build* responsibilities V25 absorbed into its completion path **stripped out** — batch-executor's turn now ends with a brief completion note when the last file ticks, and Stop hook routes to after-build from there. `plugin/agents/planning.md` extended with two additions: per-row read-back of pending TEST-LOG rows as the first sub-step of *During planning* (Rule 2, with push-back-on-bulk-confirmations and Skipped-requires-reason enforcement); and inline-label-writing on every change-list bullet added to BACKLOG.md (`- [Requested] Fix drag-to-postpone overshoot on tablet`). `plugin/agents/before-build.md` extended with a label-preservation rule for halt-C splits (labels travel with their items into new batches; no re-classifying to `[Suggested]` because items moved). `plugin/hooks/pre_tool_use.py` gained check (f): test-confirmation gate on Task → batch-executor invocations. The `hooks.json` PreToolUse matcher extended from `Edit|Write|MultiEdit` to `Edit|Write|MultiEdit|Task`; the hook script dispatches on `tool_name`. Check (f) reads TEST-LOG.md, identifies the previous batch's session from BUILD-LOG.md (path-block lookup first, then project-root fallback, then strict fallback when BUILD-LOG is missing OR unparseable — per V27 Q4, the unparseable-vs-missing distinction is named in the deny message). `plugin/hooks/session_start.py` got the TEST-LOG tripwire (V27 Q5): when previous-batch rows have `Confirmed Explicitly: No`, the SessionStart `additionalContext` injects a routing override directing main Claude to the planning subagent regardless of opener classification, with the row IDs and Test Descriptions inline so the subagent can lead the read-back from the first row. `SPINE_FILENAMES` extended to include `TEST-LOG.md`; `PLUGIN_METHOD_VERSION` bumped to 27 with its "Bumped each session" comment rewritten to reflect the V20+ session-tag/method-version decoupling. `plugin/hooks/stop.py` gained the after-build routing path: when the parser returns no top unticked batch but the Build batches section has at least one `- [x]` bullet, the hook compares BACKLOG.mtime to TEST-LOG.mtime — if BACKLOG is newer, after-build is presumed pending and the hook redirects to it (otherwise silent). `plugin/commands/build.md` updated to reflect the batch-executor-completion-note vs. after-build-recap split. DOC-STRUCTURE.md → *Build batches* got a new sub-section *Change list — `[Requested]`/`[Suggested]` labels* spelling out the inline-prefix shape, the planning/before-build/after-build chain that maintains it, and why labels attach to changes-not-files. NO-CODE-METHOD.md got two structural edits: a new *Handoff to after-build* paragraph in *At session start* paralleling the existing planning and batch-executor handoffs (also tightened the batch-executor handoff to say "completion note" instead of "recap"); and the *During planning* labels rule extended with the BACKLOG.md-inline-persistence convention plus a DOC-STRUCTURE.md cross-reference. Crash course.md updated at narrative altitude — the *After build* paragraph in *The session shape* now attributes the recap to the after-build subagent with the labels-read-off-BACKLOG mechanism named; the test-confirmation gate paragraph in *Four disciplines* updated to describe both hooks (PreToolUse gate + SessionStart tripwire). INVENTORY.md updated end-to-end: SessionStart hook description gains TEST-LOG tripwire; PreToolUse check (f) promoted from "V27 implementation per V26 spec" to "Shipped V27" with full description; Stop hook description gains the after-build routing path; planning, before-build, batch-executor, after-build subagent entries all updated for V27 changes; a stale "NO-CODE-METHOD.md... retired in V27" entry corrected to reflect that the prose file stays. BACKLOG-TEMPLATE.md (both copies) examples now show `[Requested]`/`[Suggested]` labels on change-list bullets. BUILD-METHOD.md → *Footer bumps* list explicitly adds `after-build.md` alongside the other subagent bodies. Footer bumps Version 26 → 27 across 19 files. `plugin.json` 0.26.0 → 0.27.0. All three V26 carry-forward cleanups (SPINE_FILENAMES, PLUGIN_METHOD_VERSION comment, pre_tool_use.py docstring count) absorbed and resolved. **No smoke tests run from this Cowork session** — Windows integration smoke testing via `claude --plugin-dir` is owed to Alex post-commit. The ~13 distinct smoke-test checks the V27 design implies (5 around the gate, 3 around after-build, 2 around planning extension, 2 around labels, 1 around DOC-STRUCTURE) get logged into TEST-LOG.md when Alex runs them on her Windows machine.

**Decisions taken and why.**

- **Q3 placement correction: labels live on change-list items, not Files: sub-section.** V27.md scope's lean was a "small label addition in the *Files: sub-section* item shape" — pushed back at design-lock time as a category error. A `[Requested]`/`[Suggested]` label describes the provenance of a *change*, not of a *file*: one change touches many files, one file absorbs edits from multiple changes. Files-level labels would force an arbitrary assignment when a `[Requested]` and `[Suggested]` change touch the same file. Moving the labels to the change list above the `Files:` sub-section keeps provenance with what it actually describes; DOC-STRUCTURE.md got a new explicit sub-section for it.

- **Mid-session scope expansion: fold the label work into V27 rather than a follow-up.** The Q3 resolution implied three small touches beyond after-build alone (DOC-STRUCTURE edit, planning label-write, before-build label-preserve). Considered splitting into V27a/V27b. Chose to fold in: the touched files (planning.md, before-build.md, DOC-STRUCTURE.md) were already in V27's scope for other reasons, so the additional edits cost less than reopening them in a follow-up session, and shipping after-build without the labels would have meant after-build had a dead-code "fallback for missing labels" branch covering a state that should never exist once V27 is whole.

- **Scope discovery mid-build: batch-executor must shed *After every build*.** V27.md scope listed building after-build as a new subagent but didn't call out that batch-executor's V25-era completion path *also* does MANIFEST + recap + prompts. Surfaced while designing after-build's prompt — leaving both subagents doing the same work would mean duplicate recaps and duplicate prompts. Added the scope-reduction edit; batch-executor now ends with a completion note only, Stop hook does the rest.

- **Stop hook "after-build pending" heuristic: BACKLOG.mtime vs. TEST-LOG.mtime.** Considered a state file, a session-tag comparison, and a "no rows for today" check. Each had failure modes around same-day second batches. Mtime comparison is simple, robust to the same-day case (planning advances BACKLOG.mtime when it removes the completed batch; the second batch advances it again; TEST-LOG.mtime stays at the first batch's after-build write — so BACKLOG > TEST-LOG when the second batch completes), and Lenient on stat failures.

- **TEST-LOG tripwire as `additionalContext` routing override, not just a status flag.** SessionStart already injects a state summary that *informs* main Claude's routing; the V27 tripwire goes further — it explicitly overrides main Claude's classification, saying "regardless of opener, spawn planning with this prompt." The override is necessary because a user opening a session with a feature request while pending rows exist would otherwise be routed to planning-as-feature-request flow, not the read-back-first flow. The override carries the row IDs and Test Descriptions so the planning subagent can open with a specific row immediately.

**Pivots and surprises.**

- **Stale "NO-CODE-METHOD.md... retired in V27" in INVENTORY.** Found during the doc-code parity audit. INVENTORY had a row saying the prose file would be retired in V27 — aspirational note from earlier sessions, not part of V27's actual scope. Updated to describe the current architecture: NO-CODE-METHOD.md is the source-of-truth prose; subagents (now planning, before-build, after-build) read it at session start via the read-spec-on-entry pattern.
- **batch-executor's "Flags surfaced in your response" section needed restructuring.** Originally written assuming batch-executor produces the recap. Post-V27 scope-reduction, batch-executor's turn ends without a recap, so flags noticed during the build need a different surface. Updated wording: red flags get written to BACKLOG.md's Red flags section (where after-build will see them in the recap); out-of-scope improvements need to be prominent in batch-executor's in-turn output because they're chat-only signal that after-build can't see.
- **Crash course's "Four disciplines" paragraph already said "A hook is the load-bearing gate"** — aspirational at V26 (the hook hadn't shipped). With V27 it's now true. Lightly tightened to name both the gate hook and the tripwire hook.

**Carried forward.**

- **Smoke testing in `claude --plugin-dir` on Windows** — Alex runs the 13-check sweep post-commit; TEST-LOG.md rows for each get appended in that session, not this one. See *What shipped* for the rough check breakdown.
- **`planning/sessions/V27.md`** deleted in this commit per its transient lifecycle (BUILD-METHOD.md → *Planning artefacts*).
- **OPEN-QUESTIONS.md entries** — none added this session; none of the existing entries named V27 as their fold-in target, so the entries stay parked per their *Next step* lines.
- **Helper-code duplication across `pre_tool_use.py` and `session_start.py`.** Both hooks now carry near-identical `parse_test_log_rows`, `is_row_confirmed`, and BUILD-LOG identification logic. Extraction to a shared module is the natural next refactor — same hook-side pattern as the existing `parse_backlog.py` shared parser. Not pursued this session because the duplication is mechanical and the refactor is its own session worth doing properly. → consider for a future packaging/refactor session.
- **The Q3 correction (labels on change-list items, not Files: sub-section)** highlights that the V27.md scope file had a placement category error that wouldn't have been caught without the design-lock walk. Worth keeping the design-lock-first rhythm for future sessions where the scope file calls a specific structural location.

---

## V26 — 2026-05-17 — TEST-LOG.md mechanism + Drafts in flight convention + V25 carry-over bugfixes

**What shipped.** New consumer-side TEST-LOG.md doc class (8 columns: # / Date / Session / Component / Test Description / Status / Confirmed Explicitly / User Notes; phase-based pruning), with templates in both `templates/` and `plugin/templates/` and structural spec in DOC-STRUCTURE.md. Five protocol rules placed across NO-CODE-METHOD.md phases — Rule 1 (Never infer completion) in *Required of Claude*; Rule 2 (test-session-close read-back) as new first sub-step of *During planning*; Rule 3 (No new build until test session closed, hybrid hook+subagent enforcement) in *Prohibited of Claude*; Rule 4 (Pass/Fail/Skipped definitions) in *Vocabulary*; Rule 5 (Retest after change, judgement + reasoning trail) as new fourth drift check in *During planning*. Phase edits: *After every build* gains a test-session-open step (writes blank-Status TEST-LOG rows); *During planning* gains the test-session-close read-back. CLAUDE-TEMPLATE.md (both copies) gains TEST-LOG.md in the JSON path block; scaffold.py extended to scaffold TEST-LOG.md as the fifth spine template. Crash course.md updated (TEST-LOG bullet in *The files*; *Three disciplines* → *Four*). V27.md scope reframed: split into 3 components (after-build subagent for test-session-open; planning subagent extension for test-session-close; PreToolUse hook for the gate) plus main-Claude session-open TEST-LOG tripwire. **Three V25 carry-over bugfixes** absorbed: parse_backlog.py placeholder detection (TEST-LOG #041 resolved); BACKLOG-TEMPLATE.md de-collision in both copies (TEST-LOG #042 resolved); plugin/agents/before-build.md parser invocation fix (TEST-LOG #044 resolved). One sweep-bonus: plugin/commands/build.md had the same parser-path bug as before-build (caught during the V25-carry-over sweep) — fixed inline. **Session-open recovery**: the dev-side `CLAUDE.md` (at the No code method project root, outside the `sovereign-implementer/` repo — the local file Cowork loads at session-start; not part of this commit) gained *Vxx.md inputs must be in the repo* (session-open scan rule); BUILD-METHOD.md gained *Drafts in flight* sub-section under *Planning artefacts* (close-time prevention rule); `planning/drafts/` folder created with V26 itself as the inaugural example via `planning/drafts/test-log-mechanism.md` (consumed and deleted at session close per the lifecycle). Doc-code parity audit: 17 edits across 8 files closing gaps from TEST-LOG additions (doc-listing sections updated in NO-CODE-METHOD + Crash course; "three drift checks" mentions corrected to four; INVENTORY's after-build subagent entry rewritten to forward-reference V27 revival; INVENTORY PreToolUse hook gains (f) sub-bullet for the test-confirmation gate; pre-existing "5 templates" off-by-one in INVENTORY + SKILL.md fixed). Footer bumps V25→V26 across 21 files (15 method-side .md + 3 plugin agents + planning/INVENTORY + 2 version trackers); BUILD-METHOD's *Footer bumps* list extended for the two new TEST-LOG-TEMPLATE files. No smoke tests this session — V26 is doc-only on the test-confirmation gate; the gate's real test runs when V27 ships and a fresh `~/v27-scratch` exercises the end-to-end flow.

**Decisions taken and why.**

- **Rule 2 relocation, mid-session.** V26.md scope put Rule 2 (test-session-close read-back) inside *After every build*. Mid-session realisation: *After every build* can't span the testing window — the user tests between sessions, after `/clear`. Relocating Rule 2 to be the first sub-step of *During planning* aligns the rule with when test outcomes actually arrive (the next planning session, when the user pastes test notes). *After every build* keeps the test-session-open step; the close happens in the next planning session. Reshapes V27: after-build subagent owns open, planning subagent owns close, hook fires between.

- **Hybrid enforcement of Rule 3.** Pure subagent runs into the ~30% drift rate (Crash course → Caveats). Pure hook is hostile without a walk-through. Hybrid: hook is the load-bearing gate (PreToolUse on Task → batch-executor); planning subagent's first sub-step is the UX. Hook fallback when no BUILD-LOG: "any unconfirmed row blocks" — strict but safe.

- **Q2 punted to Claude's judgement + reasoning trail.** Rule 5's "substantially changed" could be mechanical (file-touch count, line threshold) or judgemental. Mechanical needs the MANIFEST schema extension parked in OPEN-QUESTIONS. Judgemental keeps V26 scoped — and the reasoning trail makes the call auditable. If real builds show too much noise, the data forces a schema decision later.

- **Drafts in flight convention added mid-session as recovery.** V26.md cited "the Sonnet draft produced in V20 planning" as input; nothing in the repo matched, nothing on Alex's machine. Verified via prompt-to-previous-session that the draft was never committed. Root cause: scope files were writing "Alex has the file locally" without an integrity check. Fix: `planning/drafts/<topic>.md` (committed when "good enough to walk away from"; deleted in the commit of the session that consumes it) + CLAUDE.md session-open scan rule (halt on out-of-repo references in `Vxx.md` *Inputs* sections). V26 itself is the inaugural example.

**Pivots and surprises.**

- **V20→V26 "lives locally" failure as session-opener.** Discussed above. Recovery work added mid-session; convention written into BUILD-METHOD.md with the V20→V26 case as a worked example so future-Claude understands the rule's origin.
- **`/build` slash command parser-path bug.** OPEN-QUESTIONS noted `/build` did "correctly" handle the parser; the praise was about BACKLOG.md path resolution, but the script path itself (`python plugin/scripts/parse_backlog.py`) was wrong (project-relative; plugin isn't in the project tree). Caught by the V25 carry-over sweep that the before-build fix prompted; fixed inline.
- **Pre-existing "5 templates" off-by-one.** INVENTORY and SKILL.md both said "5 templates" with 4 listed (CLAUDE/UX/BACKLOG/MANIFEST) since at least V19. V26's TEST-LOG addition makes the count actually 5 — coincidental fix to a pre-existing parity bug.
- **Parity audit surfaced more drift than expected** — 17 edits across 8 files. Pattern worth keeping: when a new doc class lands, sweep doc-listing prose + count-mentions across NO-CODE-METHOD, Crash course, INVENTORY, README, and all subagent bodies. Easy to miss otherwise.

**Carried forward.**

- **`SPINE_FILENAMES` in `plugin/hooks/session_start.py`** still lists three docs (UX/BACKLOG/MANIFEST). V26 added TEST-LOG.md as a spine doc — should be in this list for detection consistency. Not a runtime bug (detection is lenient on any-one-match). → V27 (will touch the hook file anyway for the test-confirmation gate hook).
- **`PLUGIN_METHOD_VERSION` comment** says "Bumped each session" — outdated since the V20+ session-tag-vs-method-version decoupling. → V27.
- **`pre_tool_use.py:5` docstring** says "Runs three checks" — INVENTORY lists 5 actual (a-e); V27 will add (f). → V27.
- **Three V25 carry-over OPEN-QUESTIONS entries** absorbed and resolved this session — removed from OPEN-QUESTIONS.md in this commit.
- **`planning/sessions/V26.md`** + **`planning/drafts/test-log-mechanism.md`** deleted in this commit per their lifecycles.

---

## V25 — 2026-05-17 — Build orchestration core: Stop hook + batch-executor + before-build + slash commands + spec rewrites

**What shipped.** The build orchestration core — the two main motivating examples for the plugin. `plugin/scripts/parse_backlog.py` is the new shared parser that walks `BACKLOG.md`, finds the top unticked build batch, and emits a JSON payload (batch heading, change_list, Files: list with tick state, Serves lines); lenient on malformed input (`{}` and exit 0 on any failure). Three call sites consume it — the Stop hook embeds the payload in its redirect reason; the `/build` slash-command embeds the same payload when the user invokes a build manually; the PreToolUse hook calls the parser at edit-time to look up the current top batch's `Files:` list. `plugin/hooks/stop.py` is the build sequencer: after batch-executor finishes a turn, it parses BACKLOG.md, finds the next batch, and returns `{"decision": "block", "reason": "<payload>"}` to redirect into the next batch-executor invocation. Respects `stop_hook_active` — one redirect per user turn, matches D1's explicit user gating. `plugin/hooks/pre_tool_use.py` got the (c) batch file-list boundary check: parses BACKLOG.md at edit-time, looks up the top batch's `Files:` list, blocks Edit/Write/MultiEdit on any file not on the list. The originally-scoped (d) MANIFEST/UX read-before-edit check is deferred — the MANIFEST.md path-mapping schema gap (no way to resolve "which entry covers this file" given the current flat-glossary schema) blocks it; recorded as an OPEN-QUESTIONS entry. `plugin/agents/batch-executor.md` is the build-time subagent — receives the JSON payload via its invocation prompt, reads each unticked file in Files: order, makes the change, ticks BACKLOG.md per-file (partial-completion-safe), produces a build recap with `[Requested]`/`[Suggested]`/`[Prerequisite, not in plan]`/`[Re-batch, not in plan]` labels. Halt-and-confirm protocols cover the two exceptions under NO-CODE-METHOD.md → *Prohibited of Claude* (prerequisite carve-out, re-batching carve-out). Rules inlined in the agent body, intentional divergence from planning.md's read-spec-on-entry. `plugin/agents/before-build.md` is the new pre-build subagent — validates the top batch (parser parses; Serves UX.md: names resolve), enumerates the Files: list and writes it into BACKLOG.md, estimates verification burden, halts-and-confirms if the Batch-sizing principle requires a split. Rules read at runtime from NO-CODE-METHOD.md → *Before build* (read-spec-on-entry, intentional divergence from batch-executor). `plugin/commands/before-build.md` and `plugin/commands/build.md` are the first two slash-commands in the newer commands-directory pattern (`description:` + `allowed-tools:` frontmatter + prose body) — `/before-build` spawns before-build via Task; `/build` reads CLAUDE.md's path block, runs the parser, embeds the JSON payload into a batch-executor invocation, handles the empty-backlog case. NO-CODE-METHOD.md got substantive spec work: *Before build* rewritten from 8 chat-mediated steps to 8 subagent-aware steps (validate → enumerate Files: → review → verification burden → Batch-sizing application → conflict flags → switch out of plan mode + run /build); *After every build* rewritten from 5 chat-mediated steps to 5 with explicit carve-out-label and flag-taxonomy references; the *Batch-sizing principle* sub-section added (split-when-test-list-long, bundle-no-behaviour, never-fragment-arbitrarily); *Pre-build verification estimate* added to *Before build* step 4; *Mid-build re-batching carve-out* added to *Prohibited of Claude → Two exceptions* as sibling of the prerequisite carve-out; four new Vocabulary entries (Files: sub-section, Batch-sizing principle, Pre-build verification estimate, Halt-and-confirm protocol); *At session start* gained two new handoff paragraphs (Handoff to before-build, Handoff to batch-executor) paralleling the existing planning handoff. DOC-STRUCTURE.md gained the *Files: sub-section* spec as a bolded sibling of the `Serves UX.md:` name-matching paragraph, and the "unfinished portion" sentence in *Build batches* was tightened to reference tick state. Crash course.md got three paragraph rewrites in *The session shape* (Before build mentions /before-build + Batch-sizing principle inline; During build mentions /build + halt-and-confirm carve-outs + Stop-hook auto-continuation; After build attributes the recap to batch-executor) plus the existing *Before build* paragraph already referenced verification burden from the V25-prior session work. Both `templates/BACKLOG-TEMPLATE.md` and `plugin/templates/BACKLOG-TEMPLATE.md` had their example batches updated to show the Files: sub-section shape (mirrors kept in sync). INVENTORY.md got six restructure passes during the doc-code parity audit (subagent entries for before-build and batch-executor expanded from one-line placeholders to full descriptions including invocation paths, halt-and-confirm coverage, and rule-loading patterns; after-build subagent entry annotated "Not shipped as separate subagent — absorbed into batch-executor's completion path"; slash-commands section restructured to describe both the commands-directory and skill-with-flags patterns with shipped-V annotations; slash-command list entries for /before-build and /build updated from "Pending V23" to "Shipped V25"; templates path corrected from `skills/init-project/templates/` to `plugin/templates/`; `parse_backlog.py` added under Bundled artefacts as a shared utility; Stop hook + PreToolUse (c) entries annotated "Shipped V25"). OPEN-QUESTIONS.md gained four entries across the session — at the previous session's pause: MANIFEST.md path-mapping schema gap + Stop-hook 8-block cap; this turn: subagent rule-loading pattern divergence + a now-resolved entry on "Before build steps 1–2 written for chat-mediated planning" which was removed when the in-session rewrite landed. Footer bumps Version 23 → 25 across 16 files (3 spec docs + 5 templates × 2 locations + INVENTORY + 2 subagent bodies — planning.md and before-build.md). `plugin.json` 0.23.0 → 0.25.0; `session_start.py` `PLUGIN_METHOD_VERSION` 23 → 25. `batch-executor.md` gained a Version 25 footer (didn't ship with one; added during the bump pass for consistency with sibling subagents and to satisfy the SessionStart hook's footer tripwire). BUILD-METHOD.md → *Footer bumps: the full list* updated to add `before-build.md` and `batch-executor.md` alongside `planning.md`. Smoke-tested at pre-validation tier via CLI runs from Cowork bash (15/15 parser, 8/8 Stop hook, 9/9 PreToolUse boundary + V19/V22 regression coverage); see TEST-LOG #031–033. Windows integration smoke test in `claude --plugin-dir` still owed to Alex post-commit.

**Decisions taken and why.**

- **Doc-code parity audit escape-clause override: NO-CODE-METHOD.md *Before build* / *After every build* rewrites landed in-session, not deferred to V26.** BUILD-METHOD's escape clause recommends deferring spec rewrites whose work would dominate the session. The audit surfaced both as substantial rewrites — the existing chat-mediated wording was the V22-era doc never updated for the subagent split, and the spec rewrites would not be trivial. Defer was the default lean. Override reasoning: the spec gap was load-bearing for V25's shipped subagents — before-build's read-spec-on-entry pattern *depends on* the *Before build* spec being operationally correct, and batch-executor's recap rules reference the *After every build* labels. Leaving the spec at V22-era wording would have the V25-shipped agents reading a spec that didn't match what they actually do. Worth the extra session length to keep the shipped agents and the spec they cite in alignment from day one.

- **before-build subagent: validate-only first move + read-spec-on-entry rule loading.** Original design had a "reorganise pass" as standalone first step, matching the spec's *Before build* steps 1–2 verbatim. Validation review (fresh Sonnet session reading the actual files in the project) caught that planning has full BACKLOG.md edit authority since V22, making the reorganise pass dead weight in ~99% of cases and silently absorbing the only realistic case (out-of-band manual edits) where flagging would matter. Subagent design changed to validate-only (parse + Serves names exist); reorganise authority survives only as the response to the verification-burden split halt. On the rule-loading question: original draft proposed inline rules (matching batch-executor on "mechanical not branching" reasoning). Validation reframed as "stable rules vs. fresh rules" — batch-executor inlined rules that had been stable since V18 (prerequisite carve-out, recap shape, MANIFEST update); before-build's load-bearing rules were V25-introduced (Batch-sizing principle, Pre-build verification estimate, Mid-build re-batching trigger) and likely to churn as Alex actually runs Taskflow builds under them. Read-spec-on-entry kills the doc-code parity drift risk on the freshest rules. Flipped the recommendation.

- **`/build` argument-less per V25 Q3.** Out-of-order batches are handled by reordering BACKLOG.md during planning, not by a build-time chooser. Single mental model with the Stop-hook auto-continuation path: both entry points (slash-command, hook redirect) always pick the top unticked batch. An argument would have introduced a parallel "what does the user mean by `/build batch-3`?" interaction surface that adds nothing planning can't already do.

- **Ideation-session edits bundled into V25's commit rather than separated at staging.** The previous uncommitted ideation session left rule-sharpening and rule-additions in `plugin/hooks/universal-behaviour.md`, `NO-CODE-METHOD.md`, `Crash course.md`, and `planning/PLAN.md`. V25's parity audit added its own edits to two of those four files. The plan at session resume was to use `git add -p` at commit time to ship V25 and ideation as separate tags. At commit time, the procedural cost (`git add -p` is a tool Alex hadn't used before, and unpacking the bundle would have been the most friction-heavy step of the close-out) outweighed the historical-record value of clean tag separation — BUILD-LOG carries the conceptual narrative for both sets of work. Bundled. The ideation work itself (now in this commit): wording sharpen on the existing "push back rather than agree" rule across `NO-CODE-METHOD.md` and `universal-behaviour.md` (now "push back rather than simply agreeing"); a new "Walkthroughs one step at a time; alternatives all at once" rule appended to *Required of Claude* in both files; the `[SEQUENCE]` response-shape tag definition expanded with the no-previewing clause and a Method-contract cross-reference in NO-CODE-METHOD.md; a new fourth load-bearing example in Crash course's *What's editable, what's not* (counter changed from "three above" to "four above"); a new V28 row in `planning/PLAN.md` for "Walkthrough-mode for non-UI testing" (pushing previous V28–V31 to V29–V32). The ideation work doesn't get its own session number or tag.

**Pivots and surprises.**

- **Validation-via-sister-Sonnet-session was the right intervention for substantive design choices.** When the before-build subagent draft hit the three open questions (halt scenarios, reorganise-pass scope, inline vs. read-spec), spinning up a separate Claude Code session in the actual project with a self-contained validation prompt produced sharper pushback than continuing the design conversation inside Cowork would have. Two recommendations flipped (reorganise pass dropped, inline → read-spec), one halt scenario dropped (stale Files: detection — over-engineered), one missing scenario surfaced (verification-burden-triggered split as a peer halt). The pattern worth keeping: when a draft hits a design question with real downside risk, write the prompt for an independent Claude and paste it into a sister session before drafting.

- **The "after-build subagent" was a ghost reference in INVENTORY.md.** V17's architecture listed a separate after-build subagent. V25's batch-executor design absorbed those responsibilities (MANIFEST.md update, build recap, user prompts) into batch-executor's completion path on the principle that the recap is most accurate when produced in the same context as the build itself. INVENTORY hadn't been updated; doc-code parity audit caught it. Resolved in the same audit pass — entry annotated "Not shipped as a separate subagent — absorbed into batch-executor."

- **Slash-commands convention changed mid-roadmap.** INVENTORY described slash commands as "skills with `disable-model-invocation: true`, `user-invocable: true`, `agent: <subagent>`" — the older pattern from when Claude Code v2.1.101 merged commands into skills. V25's `/before-build` and `/build` landed in the newer commands-directory pattern (`plugin/commands/<name>.md` with `description:` + `allowed-tools:` frontmatter and a prose body). Both patterns now coexist (init-project still uses the older pattern; the new commands use the new). INVENTORY restructured to describe both, with shipped-V annotations clarifying which command uses which.

- **parse_backlog.py is lenient on failure (always exit 0, `{}` on malformed input) — the `/build` slash-command's original draft misread this.** First draft told main Claude to detect the empty case via "exit non-zero or empty payload." Re-read of the parser docstring before landing caught it: parser always exits 0, even on parse failure. Detection has to be on output content (`{}` or empty), not exit code. Corrected before the Edit went through, but worth recording — the parser's lenience is load-bearing for downstream call-sites and should be the first thing each consumer's design reckons with.

- **Cowork bash-mount staleness recurred — pre_tool_use.py + BACKLOG.md affected.** Same phantom-state issue as V22's note: bash sees a stale snapshot, Read/Edit see the current state. Worked around by running CLI smoke tests against `outputs/` copies of the relevant files rather than the canonical sources, then validating the canonical state via the file tools. Pre-validation tier; the real validation is Alex's Windows integration smoke test post-commit. Memory entry already exists (`reference_cowork_git_mount_phantoms.md`).

- **The Edit tool's "must Read first" requirement bit on every file I'd only Grep'd.** Footer bumps required reading 8 templates I hadn't yet Read in this session even though I'd Grep'd them for the footer line. Two-pass workaround: parallel Reads first, then parallel Edits. Same pattern came up earlier when I tried to edit NO-CODE-METHOD.md and BUILD-METHOD.md after only doing offset/limit reads — a Read with offset/limit counts for editing the read range, but a fresh Read seems to reset the harness state for the whole file. No method/code issue; tool affordance to remember.

**Carried forward.**

- **Windows integration smoke test in `claude --plugin-dir`** — run 2026-05-17 against a pre-populated `v25-scratch` outside the sovereign-implementer repo. See TEST-LOG #034–050. End-to-end flow works (index.html generated, renders matching the UX entry's stated behaviour); three bug findings logged as OPEN-QUESTIONS entries for V26 fix: Stop-hook template-placeholder misfire (#041), BACKLOG-TEMPLATE's instructional `[FOLD-IN PENDING]` ambiguity (#042), before-build subagent body's parser-path spec (#044).

- **Subagent rule-loading pattern divergence** — OPEN-QUESTIONS.md entry. before-build and planning use read-spec-on-entry; batch-executor uses inline rules. Should we converge? Defer until V26–V31 ship and the rate of NO-CODE-METHOD.md changes settles into something predictable. Revisit promotion if a doc-code parity audit flags meaningful drift in batch-executor.

- **MANIFEST.md path-mapping schema gap** — OPEN-QUESTIONS.md entry (from V25's first session). Blocks PreToolUse (d) read-before-edit enforcement; (d) deferred from V25 scope as a result. Five candidate shapes sketched in the entry; promote to a planning session in the V26+ range when MANIFEST.md schema work fits naturally.

- **Stop-hook 8-block cap** — OPEN-QUESTIONS.md entry (from V25's first session). Parked — current design respects `stop_hook_active` so chain length is always 1; cap can't trigger. Revisit only if a future workflow proposes multi-batch-per-turn auto-continuation.

- **`/plan` slash command** — still pending per INVENTORY, not pursued in V25. Auto-route via main-Claude classification remains the only planning invocation path.

- **Slash-command footer convention** — not pursued this session. Slash-commands (`plugin/commands/<name>.md`) didn't carry footers because the convention isn't load-bearing yet (the SessionStart footer-tripwire reads spine docs and subagent bodies, not slash-commands). If a future hook or audit needs to fingerprint slash-command files by method version, set the convention then; until then it's noise.

---

## V24 — 2026-05-16 — BUILD-METHOD.md and TEST-LOG.md added; session-tag and method-version decoupled

**What shipped.** Two new dev-internal docs and a slim of the project root `CLAUDE.md`. `sovereign-implementer/BUILD-METHOD.md` consolidates the working manual — session structure (open, three middle shapes, close), the doc-code parity audit (now with a sixth item, *Ghost references*), testing semantics (corrected), the BUILD-LOG / OPEN-QUESTIONS / new TEST-LOG entry shapes, footer-bump rules, and the planning artefact lifecycle. Lifted out of the old CLAUDE.md and expanded where the old version was thin or wrong. `sovereign-implementer/TEST-LOG.md` is new — 30 backfilled rows from V18 (3), V19 (11), V21 (10), V22 (6), one row marked Fail (the V22 INVENTORY ghost-command catch, plus a separate row recording its in-session retest as Pass), three Skipped with reasons. V20 and V23 had no testable code; this session does either. Project root `CLAUDE.md` slimmed from 240 to 100 lines — orientation, environment, file ops, terminology cheat sheet, Taskflowapp lock-down, and command-prompt experience level only; everything procedural moved to BUILD-METHOD. PLAN.md table renumbered: V24 is this session; old V24-V30 shifted to V25-V31 (V20 precedent). Session-scope files renamed accordingly, headings bumped, internal cross-refs shifted. OPEN-QUESTIONS.md entries 1-3 had their forward-looking refs shifted; entry 4's dated historical notes left intact. INVENTORY.md got mechanical V## shifts on 6 forward refs. **No method-version footer bumps anywhere this session** — `plugin.json` stays 0.23.0, `PLUGIN_METHOD_VERSION` stays 23, all method-side file footers stay V23. First session to ship under the new dev-internal-doesn't-bump rule.

**Decisions taken and why.**

- **BUILD-METHOD.md as a new file, not a CLAUDE.md restructure (option a, not b).** The dev project's working manual deserves its own home, separate from CLAUDE.md's orientation / environment / personal-collab material. Lets CLAUDE.md be terse and gives the manual room to grow (TEST-LOG entry shape, ghost-references audit item, footer-bumps explicit list — none of which would have fit cleanly inside the old CLAUDE.md). Option b (in-place restructure) would have preserved the single-file convenience but lost the conceptual separation; the project's working method is something a future reader will want to read once and reference repeatedly, not skim alongside personal preference rules every session.

- **Session tag and method-version footer decoupled going forward.** The old convention bumped footers every session regardless of whether the consumer-facing method had changed. V20 (Crash course promotion) and V23 (Cowork mentions strip) both bumped despite arguably-doc-only scopes — recording an interpretive mismatch (footers asserting a new method version when nothing functional in the method had changed). The new rule: footer + `plugin.json` + `PLUGIN_METHOD_VERSION` only bump when the session substantively changes the method or plugin. Session tag still increments per session. The two numbers will drift between method-changing sessions; the V21 footer-mismatch tripwire stays silent because `PLUGIN_METHOD_VERSION` moves in tandem with the footers. Historical V18-V23 numbers stay as-is; no retroactive corrections.

- **TEST-LOG.md created this session, not deferred to V26's consumer-side TEST-LOG ship.** Alex pushed back on a draft that proposed deferring. The failure mode this session diagnosed (CLAUDE.md asserting "the plugin has never been installed in Claude Code" while V18/V19/V21/V22 BUILD-LOG entries all describe `claude --plugin-dir` smoke tests with hooks firing) is precisely what TEST-LOG fixes — making test outcomes queryable by future sessions rather than buried in BUILD-LOG prose. Deferring would have left the gap open and the same recurring failure pattern in place. Backfill from BUILD-LOG was straightforward; ~30 minutes of work.

**Pivots and surprises.**

- **Initial framing wasted a round-trip on "live install + back-test."** I opened by proposing live-install + back-test as the next session before doing the read-pass, citing the CLAUDE.md *Plugin install status* paragraph asserting "never installed." Alex's pushback was sharp and correct: (1) V23 was a terminology sweep with nothing testable, (2) tests have happened — the BUILD-LOG records them — the recording isn't visible to future sessions, (3) the proposed session has no current pathway because the plugin isn't packaged for global install. Saved as feedback memory (`feedback_dont_propose_live_install_session.md`). The proposal pattern was the V23-era CLAUDE.md text feeding back into recurring recommendations; the root fix is BUILD-METHOD's corrected *Testing* section, not just the memory.

- **The old footer-bump convention had quietly drifted across V20 and V23.** When I drafted BUILD-METHOD's first version I treated "every session bumps the footer" as the existing rule. Alex called it out: the rule itself was the problem, at least for doc-only sessions. The decoupled-numbering rule is partly retroactive acknowledgement that V20 and V23 shouldn't have bumped footers either; the historical record stays as-shipped, with the *Historical note* in BUILD-METHOD recording the discontinuity.

- **INVENTORY.md's forward-pointers are semantically stale.** During the renumber pass, several INVENTORY references (Crash course "Updated in V25", DOC-STRUCTURE "deferred to V25", `/new-project` "Pending — V26") look like they're pointing at the wrong sessions even pre-this-renumber. Did the mechanical +1 shift only this session. A content audit of INVENTORY's forward-pointers would be useful in a future doc-code parity pass.

- **The renumber surfaced that V28.md and V31.md bodies still reference a "live-install session" deferred from V27/V28.** That framing was superseded by this session's BUILD-METHOD *Testing* section. Not rewritten this session — the natural fix is during V28's planning session, when the planner reads the active BUILD-METHOD and updates the scope file. Flagging for awareness, not preempting.

- **BUILD-LOG header pointer updated mid-entry.** The header said "see CLAUDE.md → *Build log*" — that section moved to BUILD-METHOD this session. Updated as part of this entry's commit; small doc-code parity catch.

**Carried forward.**

- **V23 carry-forwards remain valid.** Soft-discipline risk for mid-build SoT edits (the consumer-method-level concern about users editing UX.md during builds without hook protection); OPEN-QUESTIONS' cross-version reconciliation entry still has Cowork-first vocabulary in its working notes; V22's smoke-test items (a) and (e) from the V22 BUILD-LOG carry-forward — not in TEST-LOG because BUILD-LOG didn't explicitly attest them. Worth re-running and recording when Alex next opens Claude Code.

- **INVENTORY.md forward-pointers** — semantic audit deferred to a future doc-code parity pass.

- **`sessions/V24.md` hand-delete** — not applicable. This session was directed live by Alex without a scope file; no V24.md was ever created, so step 7 of session close is a no-op. Recording the precedent: when a session is directed live without a scope file, step 7 is skipped.

- **V28.md and V31.md body's "live-install session" references** — superseded by BUILD-METHOD's *Testing* section. Address during V28's planning session, not preemptively.

---

## V23 — 2026-05-17 — Remove Cowork mentions from method docs; Claude Code becomes explicitly required

**What shipped.** Cowork-removal sweep across the method spec docs, templates, and plugin component bodies. `NO-CODE-METHOD.md` gained an explicit "the method requires Claude Code" line at the top, removed the "Claude-Code fallback" framing of the new-project route, simplified the *Detect template state* tripwire to recommend starting the new-project route directly (no longer recommends switching tools), and replaced every "in Cowork" / "during their next Cowork session" with "by hand during planning sessions" — the fold-in mechanism stays the same (the user edits source-of-truth docs by hand; the Claude Code agent doesn't), but the location moves from a separate Cowork tool to in-Claude-Code planning sessions. `DOC-STRUCTURE.md` got the two-line equivalents in its *Fold-ins pending* and *Planning batches* sections. `Crash course.md` got a bigger rewrite — the "Where each tool fits" section reframed from two tools to two phases (planning, build), the "Day one — starting from scratch" walkthrough rewrote around Claude Code's new-project route instead of Cowork drafting, and the editing-surfaces table collapsed from two columns (Cowork | Claude Code) to one ("Claude (the agent) edit access"). Templates: `ADDITIONAL-DOC-TEMPLATE.md` and `BACKLOG-TEMPLATE.md` in both `templates/` and `plugin/templates/` got their Cowork mentions replaced. Plugin components: `plugin/agents/planning.md`, `plugin/hooks/pre_tool_use.py` (the read-only-doc deny message), `plugin/hooks/session_start.py` (the template-state recommendation), and `plugin/skills/init-project/SKILL.md` (a "drafted in Cowork" example) all got their Cowork references stripped. This project's outside-the-repo `CLAUDE.md` stripped its "this project's work happens in Cowork by default" specification per user direction — left unspecified, not replaced with Claude Code, to preserve flexibility during Alex's personal migration. `OPEN-QUESTIONS.md`'s prose-only-rewrite entry got a V23 planning note appended (during V23 prep yesterday) recording the framing shift: target audience moves from "Cowork novices from a future Podia course" to "people using the method outside the sovereign-implementer plugin." `PLAN.md` table was renumbered during V23 prep — V23 (new) is this Cowork-removal session, V24–V30 are the +1-shifted versions of the previous V23–V29 sessions. Version footers bumped V22 → V23 across all method-side files (3 spec docs + 5 templates × 2 locations + INVENTORY + planning.md subagent). `plugin.json` 0.22.0 → 0.23.0; `session_start.py` `PLUGIN_METHOD_VERSION` 22 → 23.

**Decisions taken and why.**

- **Claude Code is required, not recommended.** The user's direction was unambiguous: the method ships as a Claude Code plugin and can't run anywhere else, so the docs make Claude Code mandatory rather than "preferred." This is a stronger commitment than the V17 method's "Cowork-first, Claude Code for builds" framing. The prose-only rewrite (still parked in `OPEN-QUESTIONS.md`) is the carve-out for users who want the method's planning/build disciplines without the plugin — that audience exists, but it's not the audience for the plugin-based method.

- **Replacement wording: "during planning sessions" + "by hand."** The Cowork mentions weren't pure mentions — they were the *location* of fold-ins (user folds in their next Cowork session). Stripping without replacement would break the sentences. The natural replacement keeps the mechanism (user does the editing, not Claude) and updates only the location (planning sessions inside Claude Code, not separate Cowork sessions). Adding "by hand" makes the user-vs-agent distinction explicit: the user opens the file in their editor and types; the Claude Code agent can't write to source-of-truth docs (PreToolUse hook enforced).

- **This project's own `CLAUDE.md` is unspecified, not Claude-Code-prescribed.** Alex is in the middle of a personal migration from Cowork to Claude Code with no fixed timeline. Pinning the dev location to Claude Code would force a date she doesn't have; the original Cowork specification is being removed. Solution: strip the location specification entirely, leave the dev tool to Alex's discretion. The CLAUDE.md still describes what kinds of work happen in this project (markdown editing, plugin code, planning artefacts) but doesn't name the tool.

- **Internal planning artefacts deliberately left alone.** `PLAN.md`, `V27.md` (the renumbered new-project session that still has a Cowork-question open), and the `OPEN-QUESTIONS.md` cross-version reconciliation entry (still framed around "Cowork-first") retain Cowork references. The V23 scope was strict mentions-strip on user-facing method content, not a full audit of internal-planning vocabulary. Each will resolve naturally when its session runs. `Archive/` folders and `BUILD-LOG.md` are historical record; not touched.

**Pivots and surprises.**

- **Crash course rewrite was bigger than a mentions-strip.** The "Where each tool fits" section was load-bearing structural framing — two tools used in sequence — and the "Day one" walkthrough described the user starting in Cowork and switching to Claude Code. After V23 both framings are wrong, so both got rewritten rather than just edited. Within scope per V23.md's anticipated "minor rephrasing for sentences that break without Cowork," but worth noting: Crash course's narrative voice changed measurably this session, not just its vocabulary.

- **The "edit by hand during planning sessions" framing implies a soft discipline that didn't exist in the Cowork-tool era.** Under the original method, source-of-truth docs were edited in Cowork (different tool, no file editor in builds at all), so the user *physically couldn't* edit `UX.md` during a Claude Code build session. Under V23's framing, the user is in Claude Code throughout — they have a file editor available at all times — and the discipline of "don't edit UX.md mid-build" is now a *soft* rule (not hook-enforced; only the Claude agent's edits are blocked). Surfacing as a small drift risk that didn't exist before: a user who edits `UX.md` mid-build would silently violate the build-scope-locked principle. Not addressed in V23 (scope was mentions-strip, not redesign), but recording for future-me in case it surfaces in real use.

- **Cowork session corruption from parallel sessions.** During V23 prep (yesterday), Alex hit a working-tree corruption issue when running parallel Cowork sessions on the same project folder — Linux mount vs. Windows filesystem fight produced CRLF mangling across many files and silent truncation of `Archive/Version 10/NO-CODE-METHOD.md`. Recovered via `git restore .` after closing all parallel sessions. Logged as a memory entry (`reference_cowork_parallel_sessions_corrupt.md`) for future Claude. Not directly a V23 thing — happened during prep — but worth recording.

- **"Instructions" vocabulary miss.** Mid-session, Claude (this Claude) repeatedly failed to recognise that "the instructions" in Alex's vocabulary is Cowork's UI label for `CLAUDE.md`. She had to send a screenshot to disambiguate. Logged as a feedback memory; recurring sessions should not repeat the miss.

**Carried forward.**

- **Live install + back-test of V18–V23.** Per the existing `OPEN-QUESTIONS.md` entry ("Plugin live-install + back-test for V18–V23 — when as a session?"). V23's changes — especially the rewritten `pre_tool_use.py` deny message and `session_start.py` template-state recommendation — should be exercised in a real Claude Code session before the next session ships. The pre-install era's script-level validation isn't enough for the user-facing prompt strings.

- **Soft-discipline risk for mid-build SOT edits** — see *Pivots and surprises* above. If real use surfaces this as a real failure mode, consider a soft-warning hook (PostToolUse on a manual SOT edit during a build session) or document the discipline explicitly in `NO-CODE-METHOD.md` → *Method contract*. Not pursued this session.

- **`OPEN-QUESTIONS.md`'s Cowork-first cross-version reconciliation entry** still uses Cowork-first vocabulary. The entry's substance (tripwire half folded in V21, worker half pending in V27) is still valid; just the framing is stale. Sweep when V27 ships or when `OPEN-QUESTIONS.md` is next refreshed.

- **`planning/sessions/V23.md` hand-delete.** Per CLAUDE.md's session-close rule and the V21/V22 precedent — Alex deletes via Windows Explorer before staging the commit; the Cowork bash mount can't delete it due to file ACLs.

- **Personal migration from Cowork to Claude Code (Alex's workflow).** Not encoded as a method recommendation, but informs this project's CLAUDE.md (now dev-tool-unspecified). Decoupled from any session; happens on Alex's timeline.

---

## V22 — 2026-05-14 — Planning subagent + Serves-line PreToolUse hook

**What shipped.** Two plugin components landed and four method-side docs caught up. `plugin/agents/planning.md` is the first subagent in the plugin — its system prompt covers inputs (the `primary_intent` line main Claude passes at handoff), the always-on first action (read CLAUDE.md, path block, BACKLOG/MANIFEST/UX, plus NO-CODE-METHOD.md → *During planning* and DOC-STRUCTURE.md → *BACKLOG.md structure*), the three planning flows (test notes / feature request / scope question), the mixed-input secondary sort that catches "by the way, can we add dark mode?" items even when the primary flow is test notes, the always-run drift checks, the BACKLOG editing protocol with the Q3 Serves-line caveat, Discoveries → planning batches promotion, and the recap-output contract. Tools allowlisted: `Read, Edit, Write, Glob, Grep` — no Bash (the subagent doesn't run scripts), no Task (subagents can't spawn subagents per the V17 architectural revision). `plugin/hooks/pre_tool_use.py` gained the Q3 Serves-line check: parses every `Serves UX.md: <names>.` line in proposed Edit/Write/MultiEdit new content, matches each name against UX.md's `## Functionalities` section's `### ` entries with case-insensitive exact match after whitespace-trim, and denies with a redirect message that lists unmatched names plus a sample of known entries so Claude can spot a typo or recognise it needs to fold-in first. `Serves <ADDITIONAL>.md:` lines for additional source-of-truth docs are out of scope and pass through unchecked. NO-CODE-METHOD.md → *At session start* gained a new "Handoff to the planning subagent" paragraph defining the four `primary_intent` values (`test notes`, `feature request`, `scope question`, `mixed (primary: <one of the above>)`) and the rule that main Claude relays the subagent's recap rather than re-doing its work. The drift-check skip clause in *During planning* was tightened to "always run; only skip case is 'nothing has been built yet'" — the Q2 decision now explicit in the spec. Crash course gained a one-paragraph mention of the subagent split (load-bearing change, narrative altitude). DOC-STRUCTURE.md gained a Serves-line matching note. INVENTORY.md's planning-subagent and PreToolUse entries were filled in with the V22 shipped state, and D3 was structurally corrected (SessionStart hooks inject context — they can't launch subagents; main Claude is the launcher after reading the SessionStart state summary plus the opener). OPEN-QUESTIONS' direct-edit-users entry got a V22 note: shape #1 (tighten drift detection) is partially folded — file-level manual edits are caught by drift check 2 now that it runs every session, but in-file content changes inside still-tracked files remain uncovered. Footer bumps Version 21 → 22 across 15 files (3 spec docs + 5 templates × 2 locations + INVENTORY + the new planning.md). `plugin.json` 0.21.0 → 0.22.0; `session_start.py` `PLUGIN_METHOD_VERSION` 21 → 22.

**Decisions taken and why.**

- **Q1 — main Claude classifies primary intent at handoff; the subagent still does mixed-input sort.** The classification step (is this test notes? a feature request? a scope question?) is cheap and main Claude has to read the opener anyway to decide whether to route to planning at all. Doing the classification once in main Claude keeps the subagent's system prompt tight — it doesn't need a classification step at the top, just the three flows. Mixed-input openers still require the subagent to look at the opener a second time during the sort (catching the dark-mode mention even when primary_intent is test notes), so option (a)'s "classify once" doesn't eliminate the subagent's own opener-scan — it just hints which flow to start in. Option (b) — subagent classifies itself — was the alternative; it would put the classification logic and the flows in the same place but at the cost of a longer subagent prompt. (a) wins on clarity-per-token.

- **Q2 — drift checks run every planning session; only skip case is "nothing has been built yet."** A "skip when nothing built since last planning" rule would need a reliable signal for "since last planning," and there isn't one — `MANIFEST.md` last-modified time isn't, the BACKLOG isn't, and a git-log check would mean teaching the subagent how to work out a session boundary. More important: drift check 2 (`MANIFEST.md` ↔ codebase) is exactly the diff that surfaces manual code edits made outside Claude's awareness — the direct-edit-user case in OPEN-QUESTIONS. A skip rule would defeat that. Cost when no diff exists is low (subagent reads files, finds nothing, moves on); cost when a diff is missed is silent compounding drift. Asymmetric in favour of always-running.

- **Q3 — case-insensitive exact match after whitespace-trim; no fuzzy/typo-tolerant matching.** The Serves-line hook's job is to catch "Claude proposed a build batch with no matching UX.md entry," which usually means Claude skipped the planning-batch → fold-in step. Fuzzy matching would let that slip past whenever the made-up name *resembles* a real entry (e.g. `Search filter` near-matching `Search` or `Search filters`). The match needs to be strict enough to fail-fast. Case-insensitive-after-trim handles the only harmless variation — capitalisation choices in markdown headers (`### Dark Mode` vs the line `Serves UX.md: dark mode`) — without opening the door to ambiguous near-matches. Real typos by Claude are rare (Claude reads UX.md before writing the Serves line) and the deny message gives a one-round-trip fix.

- **Slash command `/plan` not shipped this session.** V22.md's success criteria require the auto-route path (paste test notes → main Claude classifies → planning subagent fires). A user-facing `/plan` slash command is listed in INVENTORY but isn't in V22's scope; it can land later if the auto-route turns out to be insufficient in real use, or stay deferred indefinitely if it's not needed.

**Pivots and surprises.**

- **Web-search mid-build for the subagent file format.** I started V22 not knowing the YAML frontmatter shape for plugin subagents, whether `subagent_type` was bare or plugin-namespaced, or whether `agents/` files auto-register. Wrote a structured prompt for Alex to paste into Sonnet with web search on; she paste-backed `plugin-subagent-reference.md`. Key facts: `subagent_type` is `<plugin-name>:<agent-name>` (so `no-code-method:planning`); `description` drives auto-delegation; `hooks`, `mcpServers`, and `permissionMode` are silently ignored in plugin subagents for security; `agents/*.md` auto-register, no plugin.json declaration needed. None blocking, but worth knowing upfront — guessing wrong here would have wasted a smoke-test cycle.

- **D3 in INVENTORY.md described a mechanism that doesn't exist.** The V17 design said "SessionStart hook auto-launches the planning subagent." Hooks inject context — they can't launch subagents via any documented Claude Code mechanism. V21's actual implementation was correct (the hook surfaces structural state; main Claude decides what to do), but INVENTORY hadn't been updated to match. Fixed in this session's parity pass. The auto-route promise from V17 still holds — it's just main-Claude-mediated rather than hook-mediated.

- **Cowork bash mount stuck on the pre-edit snapshot of pre_tool_use.py.** Bash saw 174 lines / May 13 timestamp; the Read tool saw the current 455 lines. Same phantom-state issue as the existing memory note about git locks on this mount. Means I couldn't run `python -c "ast.parse(...)"` to verify syntax in-session. Eyeball review used instead; smoke test is the real validation.

- **The planning subagent's system prompt deliberately delegates rule detail to NO-CODE-METHOD.md rather than restating it.** Saved against Opus risk #5 (long subagent prompts eat the subagent's own context). The prompt names the doc sections to read first, then lists only the V22-specific clarifications (primary_intent contract, mixed-input nuance, always-run drift rationale, Serves-line caveat) and behavioural protocol. Tightness vs. self-containment is a real tension here; preferring tightness for V22 and revisiting if the subagent surfaces "I didn't know I was supposed to do X" failures in real use.

- **INVENTORY's slash-commands list described unshipped commands as if they existed — caught mid-smoke-test.** When the smoke test opened Claude Code in Taskflow, the planning subagent confidently recommended Alex run `/migrate` to bring Taskflow up to spec. `/migrate` doesn't ship until V26, but INVENTORY listed it (and five other future slash commands) without any "pending" annotation; Claude reasonably read the doc and assumed all listed commands existed. Fold-in fix landed in the same V22 commit: every entry in INVENTORY's *Slash commands* section now carries a "**Shipped Vxx**" or "*Pending — Vxx*" annotation, plus a top-of-section preface stating that only `/init-project` is shipped as of V22 and the rest are roadmap items. Doc-code parity audit pass missed this in the first sweep (I checked V22-specific stale refs but didn't audit the broader shipped/unshipped status of components listed across INVENTORY); the smoke test was the catch. Same precedent as V21's mid-smoke-test plugin/templates footer catch — the audit can miss things the smoke test exposes.

- **The smoke test's auto-route check (Check 2) couldn't run in Taskflow.** Taskflow is in tier 2 — its `CLAUDE.md` path block uses the pre-V18 bullet-list format and its spine docs lack the method-aware footer. V21's SessionStart hook correctly classified the project as tier 2 and main Claude correctly read the gap flag and declined to auto-route to planning. That's all working behaviour, but it means Checks 2–4 (auto-route, Serves-line deny, Serves-line case-insensitive accept) can't be exercised in Taskflow. The plan was to pivot to a `v22-scratch` folder set up via `/init-project`; the actual setup ran into multiple side-quests (Alex initially ran `/init-project` from Taskflow's session by mistake; Claude-in-Taskflow recommended the non-existent `/migrate` command; a loop-detection menu appeared on the third invocation). Resolved by closing Taskflow's session entirely and starting fresh in a new empty folder.

**Carried forward.**

- **Smoke test for V22** — for Alex post-commit. Suggested cases: (a) after `/reload-plugins`, confirm `plugin/agents/planning.md` shows up in `/agents`; (b) in Taskflow, paste a short batch of test notes — verify main Claude classifies, spawns the planning subagent (visible in `/agents` activity), and the recap comes back; (c) in Taskflow's planning context, ask Claude to add a build batch with a deliberately-broken `Serves UX.md: NonExistentEntry.` line — verify the PreToolUse hook denies with the V22 redirect message; (d) verify a `Serves UX.md: <real entry, different case>` edit succeeds (case-insensitive match working); (e) confirm V21's SessionStart hook still fires correctly (universal rules + state summary in a tier-3 project).

- **`planning/sessions/V22.md` hand-delete.** Per CLAUDE.md's session-close rule and the V21 precedent — Alex deletes via Windows Explorer before staging the commit; the Cowork bash mount can't delete it due to file ACLs.

- **`/plan` slash command** — not pursued this session. Future session can add it (one skill file in `plugin/skills/plan/`) if the auto-route proves insufficient, or it stays deferred indefinitely.

- **Cowork bash-mount staleness on pre_tool_use.py** — reproducible. No action required beyond awareness; the file on disk is correct, and the smoke test will validate syntax-in-flight.

- **Direct-edit-users OPEN-QUESTIONS entry** — V22 partially folded shape #1. Shape #2 (developer-mode entry point), shape #3 ("Who this is for" doc section), and the residual gap (in-file content changes inside still-tracked files) remain. Revisit if direct-edit users actually surface in the wild or if public release approaches.

---

## V21 — 2026-05-14 — SessionStart hook extension: three-tier detection + foundational reads + footer tripwire

**What shipped.** `plugin/hooks/session_start.py` extended from ~60 lines (V18's universal-rules emitter) to ~280 lines, now doing the full V21 scope. The hook reads the project root from the stdin JSON `cwd` field — not from `$CLAUDE_PROJECT_DIR`, which is broken for plugin hooks (anthropics/claude-code#9447); resolves `CLAUDE.md`; parses its fenced JSON path block; reads the spine and any additional SoT docs; detects template state (`[Project Name]` placeholder still intact in any spine doc); detects an unfinished top build batch in `BACKLOG.md` (filtering out template-placeholder batch titles so an unedited backlog doesn't trip the resume signal); and runs a version-footer mismatch tripwire — for every loaded SoT doc, compare the `*No-code method — Version N.*` footer against `PLUGIN_METHOD_VERSION = 21` and flag any mismatch. **Three-tier behaviour:** tier 1 (non-method folder — no `CLAUDE.md` and no method-footer spine docs) emits *nothing* and exits 0; the plugin is invisible. Tier 2 (partial method shape — four sub-cases between missing-`CLAUDE.md`-with-spine-docs and present-`CLAUDE.md`-with-unparseable-path-block) emits universal rules + a single-paragraph gap flag naming the specific missing piece and pointing at `/init-project` or `/migrate`. Tier 3 (complete method project) emits universal rules + the full state summary, including a routing reminder that the hook does not classify the user's opener — Claude does. Footer bumps Version 20 → 21 across the 8 method-side files (3 spec docs + 5 templates). `plugin.json` bumped 0.19.0 → 0.21.0 (V20 missed it). `INVENTORY.md` updated to describe the new SessionStart shape and correct a stale "added in V20" reference to V21. Project's outside-the-repo `CLAUDE.md` gained three rules during the session: a Cowork-first lean ("stay in Cowork; switch to Claude Code only when something needs a real Claude Code session to be tested"), a test-run guidance rule (Claude must explain plugin enabling and treat Alex's Claude Code inexperience as default in any future session that asks for a Taskflow test run), and a "Which CLAUDE.md is which" sub-section distinguishing the three CLAUDE.md files (development project, template, consumer project) so future sessions don't slide between them. Smoke-tested on Windows in `~\v21-scratch`: `claude --plugin-dir <path>` loaded the plugin, `/hooks` confirmed both `SessionStart` and `PreToolUse` registered, the empty-folder session verified tier 1 (silent — no `additionalContext` injected), `/init-project` scaffolded the four templates cleanly, and a fresh Claude Code session against the scaffolded folder fired tier 3 — Claude reported the path block resolved 3 of 3 entries, template state detected in all four spine docs, the routing reminder, and the version-footer mismatch tripwire (which is what surfaced the missed `plugin/templates/` footer bumps; see *Pivots and surprises*). Tier 2 in Taskflow was not pursued — the mid-smoke-test tripwire catch already exercised the structural-mismatch code path end-to-end, so a Taskflow tier-2 run would be redundant; revisit only if real-world tier-2 misfires surface in V22+ usage.

**Decisions taken and why.**

- **Tier 1 emits nothing — a behaviour change from V18.** V18's hook injected the universal-behaviour rules in *every* Claude Code session regardless of project type. V21 narrows that to method projects only. The argument for preserving V18 was "soft visibility never hurts"; the argument for invisibility (which won) was that the plugin should respect folders it has no claim on. If a user installs the plugin globally but works in 20 unrelated folders, those folders shouldn't get method-shaped noise. The change is documented in `INVENTORY.md` and the hook's docstring; smoke testing should verify both tiers (a non-method folder produces a silent session start, a method folder produces the universal rules).
- **Route signal is prose, not a structured marker.** The hook describes detected structural state; Claude classifies the opener. The only consumer of the signal right now is Claude itself, and Claude reads prose well. A structured marker (e.g. `ROUTE: planning`) was considered for future-hook-consumability, but later hooks would re-derive route from state anyway, and the SessionStart hook can only *recommend* a route — Claude makes the final call after seeing the opener. The marker doesn't earn its keep yet.
- **Tier-2 detection tightened via method-footer check.** V21.md flagged the false-positive risk: a non-method folder with an unrelated `BACKLOG.md` would otherwise trip tier 2. Fix: a spine doc counts only if it carries the `*No-code method — Version N.*` footer. Unrelated docs sharing a filename don't.
- **Tripwire half of cross-version reconciliation folded into V21, worker half stays in V26.** Per the `OPEN-QUESTIONS.md` entry's plan (which referenced V20 / V24 in pre-V20-renumbering terms; corrected to V21 / V26 in this session). The tripwire is a SessionStart-time read with no auto-fix; the worker (diff-and-propose) lives in `/migrate`.
- **Method response to direct-edit users — not folded into V21.** Per the OPEN-QUESTIONS entry's own logic. V21 adds foundational reads + tiers + routing; none of those catch manual code edits in a consumer project. The natural home for the "tighten drift detection" shape is V22 (drift logic inlined into the planning subagent); the developer-mode and "Who this is for" shapes would warrant their own session if pursued. Entry remains parked.
- **`plugin.json` bumped direct from 0.19.0 to 0.21.0, skipping 0.20.0.** V20 was doc-only work and didn't bump the plugin version. Aligning the plugin version number with the method version is more useful than walking through every integer faithfully.

**Pivots and surprises.**

- **Workflow misframed early.** I initially described V21's implementation as happening in Claude Code, with this Cowork session as planning prep. Alex corrected: she builds the method itself in Cowork; Claude Code only enters for smoke-testing. Triggered the project-`CLAUDE.md` edits (Cowork-first lean, "Which CLAUDE.md is which") so future sessions don't repeat the mistake. The session's tagging question collapsed accordingly — this *is* V21, not a planning-prep tag, because the implementation lands here.
- **The "Which CLAUDE.md is which" confusion was real and worth pinning.** When discussing project-root detection, I used "the project's `CLAUDE.md`" without naming which one — Alex's, the template, or Taskflow's. Plain confusion, surfaced in chat, fixed in the project's `CLAUDE.md` so future sessions inherit the distinction. Also surfaced that V19's `/init-project` skill (which she'd forgotten she'd shipped) already automates the template-copy-and-rename — easy thing to lose track of across versions.
- **Sonnet's hook-environment summary was confident but unsourced.** Alex pasted it; I asked her whether Sonnet had actually web-searched; she confirmed it hadn't. Direct fetch of the source URLs confirmed Sonnet's claims hold: stdin `cwd` is the documented hook input contract; `CLAUDE_PROJECT_DIR` is broken for plugins per anthropics/claude-code#9447 (closed without explicit fix comment); sibling bug #11649 (`CLAUDE_ENV_FILE` empty in plugins) marked "Fixed in next release" by Anthropic — implies #9447 likely fixed in the same release, but the stdin contract is the correct path regardless of env-var bug status.
- **`planning/sessions/V21.md` couldn't be deleted via Cowork's bash mount.** Permission denied, per the existing memory about phantom git locks / file ACLs on this mount. Hand-deletion via Windows Explorer baked into the push instructions.
- **The version-footer tripwire caught a real oversight mid-smoke-test.** The session-close footer-bump pass earlier in this session covered `sovereign-implementer/templates/*.md` (the canonical templates at the repo root) but missed the `plugin/templates/*.md` copies — the templates `/init-project` actually scaffolds from. When tier 3 fired during the smoke test, the tripwire correctly flagged the just-scaffolded docs in `v21-scratch` as Version 20 while the plugin reported Version 21. Fixed in the same commit by bumping the 5 `plugin/templates/*.md` footers. The two-directory template sync (V19's rule: bundled plugin templates are a copy of the repo-root templates, kept in sync via the footer-bump rule) is easy to miss because CLAUDE.md → *Session close* says "every template" without naming both directories. If it bites again, name them explicitly. **The smoke test paid for itself with this catch alone** — exactly the discovery a session-close audit is supposed to surface.

**Carried forward.**

- **Tier-2 detection rule may need tightening.** If real-world use surfaces false positives (or false negatives — a project that should be tier 2 but registers as tier 1), revisit the detection rule. For V21 ship, the method-footer-in-spine-doc check is the tightest signal available without parsing internal doc structure.
- **Method response to direct-edit users** — remains in `OPEN-QUESTIONS.md`, V21-vs-V20 reference corrected. Revisit during V22 planning at the earliest.

**Build-log housekeeping (post-tag patch — same precedent as V18's BUILD-LOG-was-added-after-the-tag).** The initial V21 entry above was drafted partway through the session and listed the smoke test and V21.md hand-delete under *Carried forward* — items that were then completed before the v21 tag was pushed. The smoke-test outcome has been folded into *What shipped*; the V21.md hand-delete bullet was removed (it shipped); *Carried forward* now lists only items genuinely outstanding past V21. Noted here so the correction is visible rather than silent.

---

## V20 — 2026-05-14 — Crash course promoted to source-of-truth doc; planning shifts

**What shipped.** Crash course.md became a parity-tracked source-of-truth doc — CLAUDE.md's doc-code-parity audit checklist gained a fifth item naming Crash course alongside the spec docs, templates, and INVENTORY. To earn that status the doc got a drift audit: the `[FOLD-IN PENDING]` mechanism paragraph rewritten to describe the dedicated *Fold-ins pending* section of BACKLOG.md (was still describing the pre-V19 in-batch marker), three-sections-of-BACKLOG.md rationale block updated to four, prerequisite carve-out added to *Why batch scope is locked*, UX-principle-conflict surfacing added to *How a test note becomes a feature*, Method contract Required/Prohibited split named in *What's editable*, *Where the actual files live* corrected (Taskflow repo → sovereign-implementer repo, version folders → git tags), and the user's locking-rationale phrasing landed at the top of *Editing surfaces*. Planning shifts: existing `V20.md`–`V27.md` renamed to `V21.md`–`V29.md` (this housekeeping session became V20), new `V24.md` created for the TEST-LOG.md mechanism + protocol, `V23.md` (formerly V22, Before-build subagent) absorbed the batch-sizing principle into its scope. PLAN.md rewritten with the new table. New OPEN-QUESTIONS entry: *Cowork-friendly prose-only rewrite of the method (post-plugin-build)*. Version footers bumped V19 → V20 across 15 files.

**Decisions taken and why.**

- **Crash course earns source-of-truth-doc status, but not by retrofitting unshipped work.** The initial proposal bundled two things: promote Crash course to parity-tracked status (yes) and pre-populate it now with the contents of V20–V27's scope files (no — those files are explicitly provisional; writing them into Crash course would turn provisional plans into authoritative prose). Promotion stands; fold-ins land session-by-session as features ship.
- **Cowork-first stays in the spec for now; de-Cowork-ification is a future deliverable.** Mid-session reconsideration. The current plugin-based method IS Claude-Code-specific — hooks, slash commands, and the `[FOLD-IN PENDING]` mechanism all need Claude Code primitives that don't exist in Cowork. So the spec is correctly documenting current reality. The Cowork-friendly prose-only rewrite is a separate substantial deliverable, parked in OPEN-QUESTIONS pending V29.
- **TEST-LOG.md is an operational tracking doc, sibling of MANIFEST.md.** Sonnet's V20-planning draft handed a clean shape. Re-categorised as operational (not source-of-truth — Claude updates it during builds), dropped the inside-doc "Version: 0.1" line in favour of the method's footer convention, distributed the five protocol rules across NO-CODE-METHOD.md's existing phases rather than as one new section. V25's After-build subagent will enforce the test-confirmation gate; V24 ships the prose mechanism.
- **Batch-sizing principle folds into V23, not its own session.** The principle is a small paragraph in *Before build* plus a mid-build re-batching carve-out — bundling it with V23 (Before-build subagent) keeps doc-code parity clean; the prose ships in the same commit as the implementer.

**Pivots and surprises.**

- **Stale `.git/index.lock` blocked session-file renames.** A zero-byte lock file existed only on the Linux mount's view (Windows side reported no such file). `rm -f` failed with "Operation not permitted." `git mv` recreated its own lock on each attempt. Workaround: plain `mv`, let git detect the renames as 100% content matches at commit. Worth a memory note if it reproduces — for now, recorded here.
- **Read-required-before-Edit caught the renames.** First batch of post-rename header bumps on V21.md and V23.md failed because those files were Read pre-rename under their old paths. Re-Read after rename, edits landed. Standard pattern for future rename-then-edit batches.
- **The "Cross-version template reconciliation" OPEN-QUESTIONS entry's framing weakened during the session.** It pre-commits to "Cowork-first is now the recommended path"; the new Cowork-friendly-rewrite entry partially supersedes that. Both entries stay; the cross-version entry's wording will need adjusting when the rewrite question resolves. Noted in the new entry's working notes.

**Carried forward.**

- **Cowork-friendly prose-only rewrite of the method** — `OPEN-QUESTIONS.md`. Parked until V29 ships; promote sooner if public release approaches.
- **TEST-LOG.md mechanism + protocol** — new `sessions/V24.md`. Five protocol rules, new operational doc class, new template, structural spec, fourth drift check.
- **Batch-sizing principle** — folded into `sessions/V23.md`. Three sub-rules + verification-burden recap step + mid-build re-batching carve-out + Crash course parity item.
- **V27 scope reframed** — was "DOC-STRUCTURE migration + Crash course update"; now "DOC-STRUCTURE migration + Crash course coherence pass" since Crash course is now parity-tracked session-by-session. May need further scope tightening when V27 plans.

---

## V19 — 2026-05-13 — PreToolUse hook + bundled templates + /init-project + Fold-ins pending section

**What shipped.** The plugin now blocks edits to locked source-of-truth docs and gives Claude an unambiguous place to record the proposed change instead. Concretely: a `PreToolUse` hook (`plugin/hooks/pre_tool_use.py`) intercepts `Edit`, `Write`, and `MultiEdit` against `UX.md` and any additional SoT doc declared in the project's `CLAUDE.md` path block; the hook returns a deny decision whose reason tells Claude to add a `[FOLD-IN PENDING]` block to the new *Fold-ins pending* section of `BACKLOG.md`. The hook is registered in `plugin/hooks/hooks.json` with a tool-name matcher (`Edit|Write|MultiEdit`). The 5 templates are now bundled inside the plugin at `plugin/templates/` (a copy of the repo-root `templates/`, both versions kept in sync via the session-close footer-bump rule). A new `/init-project` skill at `plugin/skills/init-project/` (frontmatter `disable-model-invocation: true`, `user-invocable: true`) coordinates a Python scaffold script that recursively scans `cwd` for any of the four destination filenames before writing, echoes the target path back to the user for confirmation, and refuses (pointing at `/migrate`) if any conflicts exist. The structural rewrites for fold-ins pending (`BACKLOG-TEMPLATE.md` ×2, `DOC-STRUCTURE.md` BACKLOG.md section, `NO-CODE-METHOD.md` Editing surfaces + Fold-in vocabulary entry + per-route mentions) brought the method docs into alignment with what the hook tells Claude to do. Smoke-tested on Windows in a scratch directory: plugin loaded, both hooks registered, `/init-project` scaffolded cleanly into an empty dir and refused on a non-empty one, the `PreToolUse` hook blocked an `Edit(UX.md)` attempt with the deny message visible, Claude pivoted to add the `[FOLD-IN PENDING]` block to `BACKLOG.md` as designed, and the `BACKLOG.md` edit proceeded unblocked.

**Decisions taken and why.**

- **Templates live at `plugin/templates/`, not nested inside `skills/init-project/`.** Originally drafted as `skills/init-project/templates/` (one consumer), revised mid-session after recognising the templates also serve `/migrate` (V24) as the reference structure to diff user-authored docs against. With multiple consumers, plugin-root placement is the right semantic home; `/migrate` would have read awkwardly out of an init-only path.
- **`/init-project` refuses on a non-empty target, doesn't merge.** The recursive scan checks for any of the four destination filenames anywhere under `cwd`. Cowork-first authoring is the expected path, so "user arrives with pre-drafted docs" is the *normal* case, not the exception — and that case belongs to `/migrate`, not `/init-project`. Half-scaffolding into an in-progress project would silently mix template-source and user-source content. Refusal is louder and safer.
- **Hook denies with a redirect message rather than silently rewriting the edit.** PreToolUse can technically rewrite an Edit's target via `updatedInput`, but doing so would silently transform `UX.md` writes into `BACKLOG.md` writes without Claude knowing — magical, brittle (the hook would have to synthesize a well-formed `[FOLD-IN PENDING]` block from the raw edit), and against the method's own "be told what's wrong, don't be silently rerouted" principle. The hard-block + reason-text path keeps Claude in the loop.
- **`[FOLD-IN PENDING]` gets its own top-level section in `BACKLOG.md`**, between Red flags and Planning batches. The pre-V19 model nested fold-ins inside the planning batch they originated from, but only the planning-batch-resolution route actually has a preceding batch — new-project, migration, and now the PreToolUse intercept all produced orphan blocks. Giving fold-ins their own section means the user has one clear "things waiting for me to fold in next Cowork session" location regardless of origin. The structural rewrite to `DOC-STRUCTURE.md` and `NO-CODE-METHOD.md` was bigger than the V19 scope said, but the alternative (shipping a hook whose deny message refers to a section that doesn't exist in the template) was a worse inconsistency.

**Pivots and surprises.**

- **`${CLAUDE_PLUGIN_ROOT}` does expand inside skill bodies.** Flagged as a real uncertainty in the V19 plan ("may differ from hooks"); smoke test resolved it on the first try — the `python "${CLAUDE_PLUGIN_ROOT}/skills/init-project/scripts/scaffold.py" check` command in the SKILL.md expanded to the full Windows path correctly.
- **The skill frontmatter shape works.** `disable-model-invocation: true` + `user-invocable: true` produced a working slash command at `/init-project` (visible in Claude Code's UI prefix as `/no-code-method:init-project`). No agent: key needed for skills that just run a script with no subagent.
- **V18's universal-behaviour rules visibly self-policed on the first hook test.** When asked to add a placeholder entry to `UX.md`, Claude refused before the PreToolUse hook even fired — citing the UX.md "no placeholder entries" rule and offering three alternatives. The hook is a backstop, not the only line of defence. We forced the hook test with an explicit "I'm stress-testing the hook, please attempt the edit anyway" instruction, which confirmed both layers work and that the universal rules are doing more soft work than the bare deny-and-redirect mechanism alone.
- **A Discovery surfaced mid-smoke-test.** Claude's first attempt at writing the fold-in block flagged that `BACKLOG-TEMPLATE.md` didn't have a designated section for `[FOLD-IN PENDING]` — Claude placed the block at the top of Planning batches and explicitly noted "this is my guess at the canonical location." That note is what made the structural rewrite (originally pushed to OPEN-QUESTIONS) part of V19's actual shipped work. Taking the user's "let's do it in v19 anyway" call on extending scope was the right move; closing the inconsistency now is cheaper than carrying it into V20+.
- **Hook permission gates appeared at every Python invocation.** First-run UX inside Claude Code's "approve commands" workflow: each `python ...` command (the `check` call, then the `write` call) triggered its own approval dialog. Bothersome on a 4-step skill but correct — broad blanket approval (`don't ask again for python *`) would have been over-permissive. Worth noting for the Crash course in V25.
- **Test scratch dir lived outside the mounted workspace.** Alex created `~/v19-scratch` on Windows; the smoke-test work happened in a Claude Code session pointing at that dir, but I couldn't delete it myself (path outside mounted workspace). Handed back a PowerShell `Remove-Item -Recurse -Force` one-liner. Trivia, but a confirmation that smoke-test directories don't need to live in mounted workspace folders to be usable — they only need to be visible to the user's own Claude Code session.

**Carried forward.**

- **Cross-version template reconciliation** raised as an `OPEN-QUESTIONS.md` entry — fold into V20 (tripwire in SessionStart) + V24 (worker in `/migrate`); entry removed when both folds are confirmed.
- **Step 8 (subfolder-conflict test on Windows) not pursued, reason:** the recursive scan was verified in the sandbox during V19 (`/tmp/scaffold_test2/docs/UX.md` was caught as `docs/UX.md` and the write refused with exit code 2). `pathlib`'s `rglob` and `name` matching are platform-agnostic, so Windows path separators aren't a different code path. Skipping the live Windows test saved usage with negligible information loss.
- **Note for V25 Crash course:** document the per-command Claude Code approval gate so first-time users know to expect dialogs at each Python invocation, not one approval for the whole `/init-project` flow.

---

## V18 — 2026-05-12 — Plugin scaffold + SessionStart hook + JSON path block

**What shipped.** The plugin's bones are now on disk at `plugin/`. A minimal `.claude-plugin/plugin.json` manifest, a `hooks/hooks.json` declaring a `SessionStart` hook, a Python script (`session_start.py`) that emits the eight universal behavioural rules — push back, plain English, no stealth fixes, red-flag surfacing, the rest — as `additionalContext` at every session start. The rules text lives in `hooks/universal-behaviour.md` (copied from `NO-CODE-METHOD.md` → Method contract → Required of Claude; becomes canonical when `NO-CODE-METHOD.md` retires in V26). `CLAUDE-TEMPLATE.md`'s path block changed from markdown bullets to a fenced JSON code block so V19+ hooks can parse paths deterministically without grepping prose. Smoke-tested on Windows: `claude --plugin-dir <path>` loaded the plugin, `/hooks` showed `SessionStart` registered, Claude recited all eight rules verbatim when asked.

**Decisions taken and why.**

- **Plugin lives inside the same repo (`sovereign-implementer/plugin/`), not a separate repo.** The method docs and the plugin code will co-evolve through V27 — every change touches both. One history beats threading version tags across two repos. Going from one repo to two later is cheap; the reverse is expensive.
- **Hook script language is Python, not bash or Node.** Bash has the shell-profile contamination risk Opus flagged in V17 and needs Git Bash on Windows. Node isn't bundled with Claude Code on Windows (the native installer doesn't include it). Python is cross-platform, robust at parsing, and most readable for a non-coder debugging a hook.
- **Path block is JSON, not YAML.** Both parse from Python; JSON wins because it needs zero external dependencies (`json` is stdlib; `pyyaml` would be a plugin install dep), fails loudly on syntax errors, and has no quoting gotchas. Path block is edited rarely — reliability beats prettiness.

**Pivots and surprises.**

- **`UserPromptSubmit` hooks in plugins don't execute** — GitHub issue `anthropics/claude-code#10225`. V18 was scoped to install a `UserPromptSubmit` hook; we pivoted to `SessionStart` (works in plugins, functionally equivalent given the method's `/clear`-after-every-build discipline — every new session re-fires the hook). Saved to memory so V19+ doesn't re-discover.
- **`${CLAUDE_PLUGIN_ROOT}` doesn't quote paths with spaces.** The smoke test failed silently the first time because the expanded path (`C:\Users\Alex\Desktop\Taskflow Planning\...`) got truncated at the first space — Python tried to open `C:\Users\Alex\Desktop\Taskflow` and gave up. Fix: wrap the script path in escaped quotes in `hooks.json`. Any future hook command must follow this pattern; saved to memory.
- **Claude Code CLI wasn't installed on this machine.** Smoke testing required installing it from scratch via Anthropic's native PowerShell installer — adds about 30 minutes to a session but is overdue work (Claude Code CLI is Alex's stated Priority 1, not a V18 dependency in itself).
- **Two working-with-me rules added to the project CLAUDE.md mid-session**, both saved as feedback memories: (1) when uncertain about an external fact, ask Alex to web-search rather than guessing; (2) format web-search requests as paste-able prompts for Sonnet, not as questions to Alex.

**Carried forward.**

- V19+ hook commands all need the escaped-quote pattern from the start (carried as a memory + V19.md note).
- Crash course (V25) needs install instructions covering Python prerequisite, Claude Code CLI install, and the `where claude` diagnostic for the native+npm hook bug (saved to memory in V17 work).
- `BUILD-LOG.md` itself was added post-tag as a working-process improvement, not part of the V18 ship — a separate small commit after the V18 tag.

---

## V17 — 2026-05-11 — Plugin-migration architecture decided

**What shipped.** The migration path from "method as markdown docs" to "method as a Claude Code plugin" was scoped end-to-end. Produced `planning/INVENTORY.md` (final plugin component list — hooks, subagents, slash commands, bundled artefacts), `planning/PLAN.md` (session-by-session roadmap V18→V27), and `planning/claude-code-plugin-feasibility-response.md` (an Opus run grounding the design in actual Claude Code capabilities). Created `planning/sessions/V18.md` through `V27.md` as provisional session scopes. Switched versioning convention from numbered folders (`Version 3/` through `Version 16/` in `Archive/`) to git commits and tags (`v17`, `v18`, ...) — folders archived, going forward each session ships as one tagged commit.

**Decisions taken and why.**

- **Plugin layout = two-layer split.** Per-project source-of-truth content (`UX.md`, `BACKLOG.md`, `MANIFEST.md`, `CLAUDE.md`, additional SoT docs) stays per-project. The mechanical method itself (process, schemas, behaviour contract) becomes the plugin. This split is the whole bet of the migration: discipline becomes structural (hooks deny actions) rather than prompt-based (Claude is asked to behave).
- **Stop hook proposes, user gates** (D1). The build sequencer single-steps one batch per user prompt rather than auto-chaining batches. `stop_hook_active` naturally prevents loops; explicit user gating matches the method's existing `/clear`-after-build discipline.
- **Drift checks inlined into the planning subagent** (revision vs walkthrough). Subagents can't spawn other subagents — Opus confirmed. Drift logic moves from a would-be `drift-checker` subagent into the planning subagent's instructions.

**Pivots and surprises.**

- **The "always-loaded core skill" idea collapsed under Opus's check.** Skill bodies are progressive-disclosure by design — never always-loaded. Universal behavioural rules had to move to a hook (V17 chose `UserPromptSubmit`; V18 later pivoted to `SessionStart` after discovering a plugin bug).
- **Slash commands and skills merged in Claude Code v2.1.101.** Slash commands are now defined as skills with `disable-model-invocation: true` + `user-invocable: true` + `agent: <subagent>`. The roadmap depends on v2.1.101+ from V19 onwards.
- **V18 nearly became a research session.** Opus did the research live during V17, so V18 was promoted to the first real build session instead.

**Carried forward.**

- All plugin construction work — distributed across `V18.md` through `V27.md`.
- Risk of method instability during the migration: explicitly accepted at V17 close. The plugin's per-component context isolation is the testability fix, not a freeze of an unstable method.
