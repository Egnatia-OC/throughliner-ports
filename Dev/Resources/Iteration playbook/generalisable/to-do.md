# Playbook assessment — to-do list

What needs doing beyond the Path A editorial session. Nothing here is urgent.


## Generalise the three strong candidates (this folder)

These are the entries copied into this folder with a plan at `generalisation-plan.md`.

- [ ] **Premise check** — decide: plugin skill, standalone prompt, or convention baked into `/sovplan`? Then draft and test in Taskflow.
- [ ] **Reader test** — decide: plugin skill (`/sovreadertest`), standalone prompt template, or dev-side convention? Resolve the sub-agent cost question (three parallel vs one sequential). Then draft and test.
- [ ] **Rule and procedure extraction** — decide: baked into `/sovclose`, standalone skill (`/harvest`), or session-protocol.md convention? Resolve where extracted rules go in a consumer project. Then draft and test.


## Path B — park, revisit when conditions are met

- [ ] **Downstream calibration** — revisit when there's a second consumer project beyond Taskflow. Until then, E2E testing covers the same ground less formally.
- [ ] **Principle audit** — revisit when Taskflow or another project hits a plateau and Alex has bandwidth for meta-reflection. Not a per-session thing; a periodic review for mature projects.
- [ ] **Reorg priming** — the one portable rule ("if a change is too big for this session, write the next session's opening prompt now") can be added to session-protocol.md's close procedure. Could fold into the Path A session or be done standalone. Low effort.


## Path D — future plugin-development seed

- [ ] **Plugin reader test** — when a plugin-development guide or `/plugintest` skill is needed, this entry is the starting material. No action until then. The pattern: "can a stranger-Claude follow the instructions your plugin ships?"


## Path C — no action needed

These stay in the playbook. Noted for completeness:

- Spec-driven version cut — method-dev only, playbook is its home.
- Framework housekeeping — method-dev only, playbook is its home.
- Sync-procedure hardening — specific to dev-side/plugin-side split.
- Replant — rare by design, method-level only.
- Convergence reader tests (both stages) — specific to convergence strategy. Retire or graduate to maintenance once convergence completes.
