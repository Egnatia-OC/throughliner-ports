# v141 — 2026-05-29 — /sovexplain skill + consumer-doc rationale enrichments

**What shipped.** Four-phase "explain" project completed. Phase 3: enriched consumer-facing docs with design rationale mined from build log — 18 edits to `Guides/Reference manual.md` (session routing, phase detection, build snapshot, close rationale, session-length safeguards, proxy files, MANIFEST read-before-edit, editing surfaces, destructive-command guards, four new "Why the rules" entries, research discipline wrapper, tersify rationale, concurrent-build detection fix); crash course updates across `cycle.html` (5 edits), `docs.html` (1), `reference.html` (3), `why-the-rules.html` (1), `index.html` (1). Fixed stale `Status: active` references → `_method/active-build.md` across all Guides files. Phase 4: new `/sovexplain` skill — `plugin/docs/explain-reference.md` (205-line curated rationale covering all 42 features), `plugin/docs/explain-proxy.md` (topic index for targeted reads), `plugin/skills/sovexplain/SKILL.md` (self-contained, both topic and reactive modes). Doc-parity: added `/sovexplain` to reference manual, crash course, and INVENTORY.md.

**Decisions taken and why.** Curated reference file over mining existing plugin docs at runtime — the Phase 2 work already organized the rationale, and a single targeted read beats hunting across scattered docs. Proxy for the reference doc so Claude doesn't load all 205 lines for every question. Self-contained SKILL.md (like `/sovresearch`) rather than delegating to a procedure doc — the flow is too simple for a separate file. Reactive mode frames its answer with "Based on what just happened" plus a clear signal that the user can ask about something else.

**Pivots and surprises.** Phase 3 was larger than expected — the consumer docs had more gaps than the initial assessment suggested, and fixing stale `Status: active` references cascaded across multiple HTML files. Phase 4 was clean — three files, no complications.
