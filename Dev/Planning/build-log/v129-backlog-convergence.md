# V129 — 2026-05-28 — BACKLOG convergence: naming and test merge (0120)

**What shipped.** Three work streams in batch 0120. (1) Reversed the 0112 BUILD-PLAN→BUILD-PLAN rename back to BACKLOG across the entire plugin: hooks (7), scripts (3), skills (3 SKILL.md), procedure docs (9), canonical docs (2), templates (6, including file renames BUILD-PLAN-TEMPLATE.md→BACKLOG-TEMPLATE.md and .proxies/build-plan.md→.proxies/backlog.md), Guides (Reference manual + 6 crash course HTML), dev-side docs (INVENTORY, session-reference), and test fixtures (14 content edits, 8 directory/file renames, 8 test files). (2) Merged TEST-LOG index into BACKLOG proxy as a new `## Test sessions` section — retired the separate `test-log.md` proxy template, updated CLAUDE-TEMPLATE path block (`"TEST-LOG.md"` → `_method/proxies/backlog.md`), changed folder-mode detection from filename-based to parent-based (`parent.name == "proxies"`), removed `test-log.md` from `_OPERATIONAL_PROXIES`, `PROXY_TEMPLATES`, and `SPINE_FOLDER_PATHS`. (3) Expanded the blocker gate in before-build.md from 2 checks to 5 (batch OQs, planning batches, BACKLOG OQs, test sessions, ideas/red flags). All 279 tests pass. Method V97.

**Decisions taken and why.** Folder-mode detection changed from `name == "test-log.md"` to `parent.name == "proxies"` — more robust and doesn't depend on which proxy file the path block points to. `"TEST-LOG.md"` path block key kept (pointing to backlog.md) rather than removed — avoids rewriting all hook resolution code that calls `resolved.get("TEST-LOG.md")`. Test sessions section placed between Build batches and Open questions in the BACKLOG structure — keeps test tracking adjacent to build tracking.

**Pivots and surprises.** Session spanned two context windows — the rename pass consumed the first, the merge and gate expansion fit in the second. The rename was predicted to be large (batch scope flagged "~30+ files") and delivered exactly that. One fixture directory rename was missed by the background agent in the first window — caught by pytest, manually fixed.
