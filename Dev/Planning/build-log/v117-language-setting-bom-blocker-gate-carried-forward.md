# V94 — 2026-05-28 — Language setting, BOM hardening, pre-build blocker gate, carried-forward removal

**What shipped.** Four work streams in one batch. (1) Language setting: `Language:` field in CLAUDE-TEMPLATE.md, `/sovsetup` Q5 + `core.quotepath false`, `universal-behaviour.md` Language rule, `DOC-STRUCTURE.md` Language section, Reference manual + crash-course coverage. (2) BOM hardening: `utf-8-sig` encoding in `project_state.py` and `session_start.py` to strip Windows BOM bytes from spine docs. (3) Pre-build blocker gate in `before-build.md` — scans for unresolved OQs/ideas before proceeding. (4) Carried-forward removal from build-log format, `close.md`, `validate_docs.py`, idea-sweep triage, `DOC-STRUCTURE.md`, dev-side `session-protocol.md`, Reference manual, crash-course, templates, and test fixtures. 23 files touched during build; 26 more during close (parity fixes + footer bumps). All 279 pytest tests pass. 6 smoke tests pass.

**Decisions taken and why.** BOM scope narrowed from 4 sites to 3 — `user_prompt_submit.py` and `pre_tool_use.py` read Claude Code transcript files (never hand-edited), not user spine docs, so BOM bytes can't appear there. Carried-forward removal was wider than scoped (11 files → 23) because the mechanism threaded through validation scripts, test data, fixtures, post_tool_use comments, and crash-course HTML.

**Pivots and surprises.** Surface area for carried-forward removal was significantly underestimated. The `replace_all` strategy for crash-course HTML missed variant phrasings ("The four prompts" heading vs "walks you through four prompts" body text), requiring targeted follow-up edits.
