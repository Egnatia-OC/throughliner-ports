# v148 — 2026-05-31 — /sovexplain three-way routing + MANIFEST capabilities summary

**What shipped.** Expanded /sovexplain from "why"-only into a three-way router: "what" (capability overview from MANIFEST proxy), "how" (routes to matching procedure doc), "why" (existing explain-reference flow). Added a `## Capabilities summary` section to MANIFEST — auto-generated by /sovclose step 1b after each build, reproduced in the MANIFEST proxy for session-start orientation.

**Decisions taken and why.** Placed capabilities summary between the HTML comment and entries (not at doc bottom) so it's the first thing Claude reads. Made step 1b skip gracefully on pre-V106 templates rather than requiring migration. Kept explain-proxy.md unchanged — routing happens in SKILL.md before the proxy is reached, keeping the "why" lookup path clean.

**Pivots and surprises.** None. Clean implementation matching batch scope.

## Performance
- **Batch completion:** Complete
- **Files in batch:** 9
- **Carve-outs:** None
- **Claude-verified tests:** 0 (no automatable tests — all behaviour is procedural guidance)
- **User-verified tests:** 0 pending
