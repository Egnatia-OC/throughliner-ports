# V75 — 2026-05-26 — "Retire build session" OQ cluster design

**What shipped.** Design session resolving four open questions in the "Retire build session" cluster. Parent OQ resolved: yes, retire the formal session concept — operating model becomes BACKLOG batches + independent skills + hook enforcement. All skills renamed with sov-prefix (`/sovsetup`, `/sovbuild`, `/sovrecap`, `/sovclose`, `/sovgit`, `/sovplan`, `/sovresearch`). Three new skills designed: `/sovclose` (quality gates + record-keeping, dual-path for post-build and post-planning), `/sovgit` (git operations with plain-English narration), `/sovplan` (planning with ordering principles). Merge OQ resolved as "don't merge — rename to `/sovrecap`" with lock-timing fix (Status: active should engage after user confirms recap, not before). Handoff audit completed: all skill transitions must use `[PROMPT]` nudges, never auto-handoff; one violation found (build.md line 76). Core competency gap identified in planning procedure doc: grants Claude authority to reorder BACKLOG but provides zero ordering principles.

**Decisions taken and why.** Sov-prefix on all skills — prevents confusion with native Claude Code features. `/sovrecap` over merge — the pre-build checkpoint is a genuine pause point; BACKLOG must be unlocked during discussion. `/sovclose` dual-path (detect `Status: active` with fully-ticked Files:) — users shouldn't need different close commands for different session types. SessionStart enhancement (top 3 batches) over a new skill — simpler, already fires, no invocation needed.

**Pivots and surprises.** Lock-timing bug: `Status: active` currently engages before the recap, blocking BACKLOG edits during the discussion window. This is a design bug regardless of the merge question. Planning procedure doc's ordering gap was the biggest surprise — Claude has structural authority but zero guidance on how to use it.

**Carried forward.** All design findings recorded in BACKLOG OQs, marked "ready to promote to batch." Implementation is future batch work: skill bodies, procedure docs, sov-prefix rename, ordering principles in planning.md, SessionStart summary enhancement, build.md handoff fix.
