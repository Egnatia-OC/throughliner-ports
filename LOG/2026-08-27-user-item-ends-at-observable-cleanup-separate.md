# [HASH] — plan — [user-item-ends-at-observable-cleanup-separate] kept: walkthroughs end at the observable; another-project steps are filed, never driven

The user's "infuriating" moment: the cycles verification had passed, and the run handed her fixture cleanup in a different project as the next step — leaving the build for housekeeping it neither needed nor waited on. Two rules: a `[user]` item's walkthrough ends at its observable, with tidy-up filed as its own item; and a step requiring action in another project stalls the run by construction, so the run files it and continues. Her framing carried: a non-coder walked out of their build into a second project to delete a test artifact has been handed the method's internals as a chore.

**Queue changes:** [user-item-ends-at-observable-cleanup-separate] filed and cleared; [walkthrough-hands-over-cleanup-that-stalls-the-run] deleted as merged; heading reworded off the lint's flags (it briefly parsed as a [user] item).
**Work processed:** kept — [user-item-ends-at-observable-cleanup-separate].
Rule gate: run at the keep — two amendments; recorded on the item.
