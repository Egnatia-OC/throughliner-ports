# 29ba751 - plan.md + FAQ: add a /plan wind-down re-scan that surfaces un-flagged captures from the session's discussion

A non-coder who thinks out loud generates capturable material they never explicitly flag; nothing re-read the session's free-form discussion, so it under-captured exactly the user the method serves. A wind-down re-scan (end of Step 2, before close) now re-reads the session's own discussion and surfaces candidate captures as one numbered set for a single bulk approval (the bulk-approval inversion). It is best-effort and memory-dependent, named as such to the user; it lives in /plan because the find-then-sharpen flow is planning work and /done would pull planning into an execution close.

**Files touched:**
- plugin/si-plugin/docs/plan.md
- plugin/si-plugin/templates/faq-template.md
- plugin/si-plugin/templates/faq-index-template.md

**Routed to Captures:** none
