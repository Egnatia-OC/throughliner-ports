# 2b21b54 — Added cruise.md's response spine + BLOCKED marker + unattended close answers; done-build.md unattended note

Implemented [cruise-control] concerns 3 and 8. The response spine: route-and-continue files captures and user-run checks (as `[user]` work lines) and the run continues; halt-for-the-user writes a BLOCKED marker and exits on a genuine blocker — an open red flag, a needed SPEC change, or an owner-decision uncertainty (the test is categorical, whose-call-is-this, not a confidence threshold); hard-stop covers the mechanical ceilings. The full per-line close still runs — judgment is never skipped for speed — with its approval moments answered unattended: wind-down auto-files, no push is offered, subagents are declined unless a build plainly matches a researched heavy-data case. Reconciliation while building: the design text routed user-run checks "to Deferred tests," but that section is retired, so cruise.md was built to file `[user]` work lines per current done.md; filed the mismatch as a capture.

**Files touched:**
- docs/cruise.md
- docs/done-build.md

**Routed to Captures:** [cruise-design-refs-retired-deferred-tests]
