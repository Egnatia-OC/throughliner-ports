# [HASH] — the close gains a mail step, because it is the one skill that always runs

Two messages arrived mid-chat, nothing triaged them, the briefing outgrew a shipped test, and a close halted on a failure several steps removed from anything the person committing had done. What broke the loop was the user telling Claude to file them — nothing in the method did, and nothing would have.

The sites that look like they cover it do not. /plan and /next open mail at their openings, but only mail already waiting when the session started, and only if one of those skills runs. The close had no mail step at all. So triage depended on a session happening to begin after the mail arrived *and* happening to be one of two skills.

The close is the one skill that always runs, which is the argument that sited the wind-down re-scan there, and this is the same siteless-duty shape. So it triages waiting mail through the standard three-way routing and archives it.

The second question needed no decision — archiving already follows triage; the gap was only that triage never happened. The third is overtaken by the payload-cap item settled the same day: bodies leave the briefing entirely, so unread mail can no longer push it past the limit, and that item's justification is now the honest-SPEC half rather than the size crisis.

The user's ordering instruction on this item — that the cap item wait until this one was settled — was not followed. The cap item was processed first without the instruction being checked. The outcome is unaffected, since the two settlements agree, but the order was Claude's error and is recorded as such rather than glossed.

Rule gate: run — a step added to the existing close; no freestanding rule and no always-loaded slot.

**Queue changes:** [mail-triage-depends-on-the-user-noticing] kept into Processed, cleared to run.
**Work processed:** kept — [mail-triage-depends-on-the-user-noticing].
