# [HASH] — Plan-after-build blindness kept on the cheapest option: a warning line keyed on this chat's own build working file

Kept after walking the mechanism through with the user: LOG entries are written at the close, not during the run, so a plan session in the same chat reads the run's work as unshipped — the working file is not the cause, only the cheap signal. /plan's opening gains one warning branch; the revisit still reads LOG and skips silently. Reading the working file's ticks as truth refused as a second source of truth about what shipped. Build block on the item.

**Queue changes:** [revisit-depends-on-a-log-that-the-close-writes] moved Unprocessed → Processed, cleared.
**Work processed:** kept.
