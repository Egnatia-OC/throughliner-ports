# [HASH] — Processed the over-asking / close-lean cluster toward a test rezip: merged two gate-redundancy captures into one build line, kept the empty-Unprocessed close-lean fix

Session aimed at a productive test rezip today. Assessed the below-the-line queue (Processed and Unprocessed) for rezip-suitability, using the filter that a rezip only carries target-side builds — so `[user]` verification and website/host-only work can't be in one. The over-asking / close-lean skill-behaviour fixes came out as the strongest concrete, target-side candidates.

Merged [next-double-start-confirm] and [plan-repeated-progress-approval] into one build line [collapse-redundant-progress-gates] — they're the identical over-asking pattern across /next and /plan and were flagged to be treated together. Settled the design question the /plan capture carried: plan.md's Step 1 entry question is the load-bearing fork (discuss-first vs. go-to-queue) and stays; the first item's separate "ready to dig in?" turn is the redundant gate and goes, with the read-the-raw-item-first design preserved. On the /next side, next.md Step 1's Steps 3–4 (send-run, then "Ready?") merge into one present-and-confirm beat.

Kept [plan-assumes-close-after-last-capture] as its own line — an empty Unprocessed must not read as a close signal; it needs a neutral end-of-queue off-ramp rather than a close-lean. Noted it doesn't conflict with the gate-collapse line: one trims opening gates, the other adds a neutral off-ramp at the end of the queue.

**Queue changes:**
- Two new build lines added to Processed above the cleared-to-run marker: [collapse-redundant-progress-gates], [plan-assumes-close-after-last-capture].
- Three Unprocessed captures removed (two folded into the merged line).

**Work processed:** kept — next-double-start-confirm + plan-repeated-progress-approval (merged → collapse-redundant-progress-gates); plan-assumes-close-after-last-capture. Deleted — none.

**Routed to Captures:** none.
