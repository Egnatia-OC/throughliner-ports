# [HASH] — Autonomous /goal session (plugin off): cleared the buildable queue (19 build batches + 1 audit) to prep the next test rezip — close-out hardening, freeform as a fourth /next type, the tag sweep, memory boundaries, scope-lock dash-strip removal, FAQ backfill, and the self-hosting CLAUDE.md/INSTALL.md docs; 1 audit finding filed, 9 host-side deferred tests added; previous session's hash backfilled to 759f495

Autonomous goal session with the plugin off, working QUEUE.md's Batches top to bottom. The first batch [install-upload-path-clarity] was skipped and left in place — it needs a real desktop-app screenshot a goal session can't produce. Everything else buildable then shipped in order, leaving only that screenshot batch in Batches; the queue is now prepped for the next test rezip. There were no push markers in the queue and every Depends-on line was target-side (docs readable at author time), so the ordering resolved in one session. [done-unconditional-read]'s only dependency, [scope-boundary-rule], had already shipped, so its stale Depends-on header was cleared with the batch.

The /next and /done families took the bulk of the work. [next-done-tag-sweep] put response-shape tags on the speaking moments inside the [SILENT] Execute steps (a clarifying line that silence governs the success path while failures, asks, and handoffs always speak) and across the done family's close-out moments. The /done close-out then gained a run of hardening: [done-unconditional-read] made the _build.md read unconditional (memory enriches, never substitutes) and reworded the findings steps so files are the record, not the conversation; [post-close-capture-record] lets a post-commit capture update its own session's entry as a working-tree edit; [done-recommend-next-both-ways] states the capture-overlap scan result either way (the clean case is a plain assessment, not a hedge) in all four sub-docs; [done-single-summary-line] put the single-summary rule in every sub-doc; [push-offer-fit] gated the push offer on a remote existing and defaulted planning closes to commit-only; [done-spec-sync-check] added a build-close spec-drift detector that files a capture and never edits SPEC; and [staleness-flag-fix-path] split the staleness flag into fate-decisions (defer to /plan) versus moved pointers (fixable at close with approval).

[ship-freeform-next-type] added the fourth /next type — freeform, for unqueued or loosely-scoped work that isn't a build, test, or audit. It ships two new procedure docs (next-freeform.md, done-freeform.md), routers in next.md and done.md, a gate that requires a one-line why-none-fit, ask-by-ask scope growth, a captures-can-be-made-not-processed warning, the Freeform subheading in plan.md and post_tool_use.py's ALLOWED_SUBHEADINGS, and consumer-facing template entries. The new done-freeform.md inherited the same close-out rules the other sub-docs got this session (findings-as-files, single-summary, staleness fix-path) for consistency.

Smaller batches: [tag-restatement-trim] removed prose restatements of tag definitions from plan.md and renamed the Step 2 processing order from "oldest first" to file order, top to bottom (the ground-rules "one item at a time" stayed, since no tag reaches the ground rules); [next-test-review-tail] defined next-test.md's review tail to mirror next-build.md's no-new-work tightening; [memory-rule-boundaries] replaced the blanket "not to memory" with an enumerated must-not-hold list plus the explicit freed remainder, host-side in CLAUDE.md and consumer-side in plugin-behaviour.md; [scope-lock-drop-dash-stripping] removed the dash-stripping loop from pre_tool_use.py so a Files: line is a bare path and nothing else (Claude-tested via module import: dash-annotated line denied, bare path allowed, real " - " path no longer truncated, empty/absent sections correct); [faq-backfill] created the FAQ/ folder this project never scaffolded, copied verbatim from the current templates (now carrying this session's freeform and clear-between-steps entries).

Four CLAUDE.md/INSTALL.md self-hosting docs landed: [push-marker-hard-direction] (the marker is hard only for host-side reads, never suspends decided reasoning), [hook-count-reconcile] (2→3 hooks, two enforcing plus advisory post_tool_use, plus the handoff-claim-provenance interim rule, plus a rider fixing the stale "SPEC.md read-only" clause in the same lines), [reinstall-needs-full-restart] (full quit-and-relaunch, not a new session, with the Windows process-exit check), and [rezip-test-version-scheme] (the `-testN` test-build version scheme with auto-increment, and Push dropping the suffix). [verbatim-first-rationale] added the user-engagement rationale and named both failure modes (bundling, forced-wait) on plan.md's verbatim-first instruction.

The audit, [opening-narration-audit], read all four skill openings and the plugin-behaviour.md narration rules and filed one finding: opening narration is bounded per-step but not in aggregate — the /next blocker gate and the dependency-ownership narration rules (unpark, staleness, narrate-ordering) each fire independently with nothing capping the total, and the lever is a rule, not another tag. plan.md Step 1, setup.md, and the done openings were checked and noted as already covered. The finding was first misplaced under Batches and corrected into Captures.

The previous session's `[HASH]` placeholder (goal-2026-06-16.md) was already backfilled to 759f495 in the working tree, so that edit rides into this commit. This session's own entry keeps its placeholder for the next session to fill. The working tree's `-test3`/zip artifacts from prior local testing are superseded by the fresh rezip made at the end of this session.

**Files touched:**
- plugin/si-plugin/docs/: next.md, next-build.md, next-test.md, next-freeform.md (new), done.md, done-build.md, done-test.md, done-audit.md, done-plan.md, done-freeform.md (new), plan.md, plugin-behaviour.md
- plugin/si-plugin/hooks/: pre_tool_use.py, post_tool_use.py
- plugin/si-plugin/templates/: CLAUDE-TEMPLATE.md, faq-template.md, faq-index-template.md
- CLAUDE.md (host-only), INSTALL.md
- FAQ/faq.md (new), FAQ/index.md (new)
- QUEUE.md (batches removed; 9 deferred tests added; 1 audit-finding capture filed), LOG/index.md, LOG/goal-2026-06-16.md (hash backfill)
- plugin.json + si-plugin.zip (fresh test rezip)

**Routed to Captures:** "Bound the aggregate of opening narration, not just each step" — from [opening-narration-audit].

**Unpark candidate for /plan:** [full-tag-placement-recheck] is parked Blocked by [next-done-tag-sweep] + [plan-step1-sequencing] + [setup-self-contained-no-tags]; [next-done-tag-sweep] shipped this session and the other two already shipped, so it may now be fully unblocked.
