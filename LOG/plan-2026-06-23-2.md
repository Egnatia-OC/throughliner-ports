# [HASH] — /plan: processed 9 captures — promoted 5 build batches + [plan-dependency-tracing-gap] (design item blocking cruise-control deployment); unparked [cruise-control] to a committed design session (plan-marker at queue top); parked [method-doc-structure-pass] (folded in [firing-map-middle-band]), [skill-startup-preload-hook], [dont-assume-user-environment]; filed 3 wind-down captures + hook-vs-CLI research

This session cleared the capture backlog and made two larger sequencing decisions.

**Capture processing.** Nine captures, one at a time. Five promoted to build batches: [deferred-roll-testzip-blind] (the deferred-test roll's liveness check moves from version-number comparison to a content stamp of the installed host — version numbers, even `-testN`, can't see build-batch edits that bump nothing); [user-run-assumes-user-can-run] (classify user-run tests by what they require, stated plainly, and at surface time explain what the test checks and why Claude isn't running it — keyed to the requirement, not an assumption about the user's environment); [anything-else-assumes-user-initiated] (the mid-capture closer splits by who raised it — user-raised keeps "anything else?", a Claude-raised discovery uses confirm-and-resume, since the discovery-routing flow is Claude-raised by construction and misfires every time today); [claude-md-routing-gap] (add CLAUDE.md to the routing taxonomy as a three-way SPEC / CLAUDE.md / memory distinction, with placement a self-check on Claude's own routing); [winddown-rescan-at-done] (a file-only wind-down re-scan at the /done close — feasible because the re-scan only files captures, allowed in any session; only routing is /plan-locked).

**Cruise control.** Unparked at the user's prompt: its blocker was genuinely met (the /goal-formalization arc shipped and goal sessions have run for over a week), so it left the shelf for a committed next-session design /plan, marked with a plan-marker at the top of Batches. It is a design session, not a buildable batch.

**Dependency-tracing as a deployment prerequisite.** [plan-dependency-tracing-gap] promoted as a second design item, linked to cruise control via Blocks — scoped to its unattended deployment, not its design. The reasoning: /next's guards catch shallow-plan thrash because a human is present to replan; cruise control removes that human, so the thrash becomes autonomous stalling. Cruise control's design can still go next; its deployment waits on solid dependency tracing.

**Parked.** [method-doc-structure-pass] parked on a "behaviour.md settles" trigger (a whole-corpus routing pass would grade a moving target while the doc is still gaining rules), folding in [firing-map-middle-band] (its behaviour.md slice, removed as a separate item) and repointing [skill-startup-preload-hook]'s reference. [skill-startup-preload-hook] parked, blocked by [method-doc-structure-pass] — pre-assembling a bundle to inject pulls opposite to the progressive-disclosure direction and can't be decided in isolation (plus a host-side reliability test of prompt-hook injection is needed first). [dont-assume-user-environment] split from [user-run-assumes-user-can-run] as the broad plugin-level guard, parked blocked by it (see how the narrow fix lands before generalizing).

**Discussion threads resolved (no batch).** LOG-vs-git-history: resolved as no change — the LOG earns its place and git can't replace it (one commit can hold many batch/session entries, especially under goal and cruise-control runs; entries carry structured sections beyond the commit message; retrieval is plain-markdown via the index without git; entries exist as working state before the commit hash). Rejected alternative: make git history the log. Companion-CLI idea: reframed to a hook + packaging approach after research — the flagship "preload the skill docs" example is served by a UserPromptSubmit/UserPromptExpansion hook plus packaging-time bundling, not a new CLI; research filed at `resources/research/hook-context-injection-vs-companion-cli.md`.

**Wind-down re-scan** filed three captures: [unpark-scan-missed-shipped-trigger] (this session's Step 1 scan reported "clean" while cruise control's shipped-slug trigger was met — a compliance miss), [design-session-queue-home] (no first-class home for design-session items), [lint-reflags-citations-within-session] (the lint re-flags the same citation on every edit).

**Queue changes:** added at the top of Batches — a plan-marker + the [cruise-control] design block (unparked) and the [plan-dependency-tracing-gap] design block (Blocks cruise-control deployment); added at the end — build batches [deferred-roll-testzip-blind], [user-run-assumes-user-can-run], [anything-else-assumes-user-initiated], [claude-md-routing-gap], [winddown-rescan-at-done]. Parked: [method-doc-structure-pass] (with [firing-map-middle-band] folded in and removed), [skill-startup-preload-hook], [dont-assume-user-environment]. Filed to Captures (unprocessed): the 3 wind-down captures. Filed research: `resources/research/hook-context-injection-vs-companion-cli.md`.

**Captures routed:** promoted — [deferred-roll-testzip-blind], [user-run-assumes-user-can-run], [anything-else-assumes-user-initiated], [claude-md-routing-gap], [plan-dependency-tracing-gap], [winddown-rescan-at-done]; parked — [method-doc-structure-pass] (+[firing-map-middle-band] folded), [skill-startup-preload-hook], [dont-assume-user-environment]; unparked — [cruise-control]; filed (unprocessed) — [unpark-scan-missed-shipped-trigger], [design-session-queue-home], [lint-reflags-citations-within-session].
