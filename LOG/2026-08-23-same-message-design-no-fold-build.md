# 3cc6f44 — A design first shown in the offer message can no longer fold into its own disposition

Raised by the user: processing should involve one more turn — agree, defer, or something else — before a disposition is finalised. It ships as a narrowing of the existing fold rule rather than the unconditional turn she first described, on Claude's recommendation and her agreement, because the unconditional version re-adds a turn to every exchange where she had already engaged the substance, which is the over-asking this method has spent months removing.

The fold conditions gained a third limb: the agreement must follow at least one earlier turn on the item's substance. Where a design is first shown in the same message that proposes what to do with it, the user has seen it once and answered only "process now" — and "process it now" answers whether to work an item, never its fate. The existing rule already said a general yes answering a different question cannot fold; this states the same-message case that was being stretched past it.

The recorded instances are from the session that raised it: the end-of-queue gate item and the slug-reference item were each designed and written in one breath after a bare "process now", with no turn on the design itself. Both outcomes stood when reported, which is why this is a narrowing rather than an incident — but the seam her instruction protects was skipped, twice, in one session.

**Files touched:** `plugin/throughliner/docs/plan.md` — the fold-conditions block in the recommend step, plus a pointer from the process-now section so both branches agree.

**Routed to Captures:** none from this item.

Tick: done, confirmed — the block reads as one enumeration including the new condition, the process-now branches point at it, and a grep found no other doc restating the fold conditions.

Rule gate: run — amendment to plan.md's fold rule, parent named as the may-fold conditions block; one condition added to an existing enumeration and nothing evicted. An unconditional extra turn on every keep was refused, on the user's own agreement, as re-adding removed over-asking.

FAQ: not needed because the change adds a pause in Claude's own flow and no user action changes.
