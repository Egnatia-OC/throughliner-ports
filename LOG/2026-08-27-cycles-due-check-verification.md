# 32675a3 — Cycles verification closes at its observable; the cleanup splits off

`[user]` item [cycles-due-check-verification], reached in the walk-through pass
of the 2026-08-27 build run. Not re-driven: the record check answered it.

## Record check

Read the five records on file — `2026-08-23-`, `2026-08-24-`, `2026-08-25-`,
`2026-08-26-` and `2026-08-26-cycles-due-check-verification-2.md`. The last of
them settles the item: steps 1, 2 and 3 are done and were **confirmed from the
world** rather than reported — `CYCLES.md` present in
`DEMOS/Polit Fart Announcer 1` with its fixture observable, exactly one
`[weekly-listen]` entry in that project's Unprocessed, filed by the check rather
than by a person, and no second entry after a later opening. That is the whole of
what this item set out to verify.

## Outcome: done

The item's walkthrough named an observable check and that check passed. Under the
three completion routes this is complete, and nothing here needed re-driving.

## The cleanup split out rather than driven

Steps 4 and 5 of the original walkthrough were cleanup — delete the test
`CYCLES.md` and the filed capture — in another project. Two rules built earlier
in this same run govern that, and this is their first live application:

- a walkthrough ends at the item's observable, with cleanup after the test filed
  as its own item rather than carried as trailing steps;
- a step requiring action in another project is filed, never driven.

So the cleanup is now [cycles-fixture-cleanup] in Unprocessed, carrying its own
walkthrough and naming what it is downstream of. It blocks nothing.

This is the repair the previous record asked for in its own closing words — that
the next session "either split it into its own line or close this one as passed
and drop the cleanup". It does both halves: closed as passed, cleanup split.

## What closes this item

The item stays in Processed until /done removes it under this slug. Nothing
further is owed from the user for the verification itself.
