# d513216 — /plan: promoted 3 build batches from the desktop-upload-removal + 2026-06-29 audit captures ([rezip-push-cli-flow], [install-docs-cli-refresh], [tag-recheck-doc-fixes]), dropped stale [install-upload-path-clarity], parked [watchlist-doc], recorded the cruise-control concern-8 verification-status refinement, and placed the readiness line

This /plan processed five captures plus two threads the user raised in conversation.

The headline work came from the desktop app removing its in-app plugin-upload button (captures [desktop-plugin-upload-removed] + [cli-install-confirmed-rezip-loop-update]). The CLI marketplace install was verified working this session and the committed marketplace.json already backs it, so the remaining work is fixing stale docs in two homes. Split by audience into two batches: [rezip-push-cli-flow] (host-only — rewrites CLAUDE.md's Rezip/Push rituals to the CLI flow, urgent because the documented release path no longer exists), placed at the top, and [install-docs-cli-refresh] (consumer README/INSTALL + an FAQ entry) below it. The "local vs GitHub marketplace" fork in the capture turned out not to be a fork — one committed marketplace.json serves both, added as a local folder for the test loop and as a GitHub repo for release. [install-upload-path-clarity] was dropped as stale: its only remaining work was a screenshot of an upload screen that no longer exists, and [install-docs-cli-refresh] supersedes it.

Three small procedure-doc fixes from the 2026-06-29 [full-tag-placement-recheck] audit ([readiness-line-narration-frequency], [trace-rule-needs-exemplar], [done-freeform-recommend-hedge-gap]) were bundled into one batch [tag-recheck-doc-fixes] — same origin, same low-risk authoring-tweak kind.

[watchlist-doc] was filed and parked: a user proposal for a lighter "watchlist" doc surfacing just the items the user must monitor, motivated by the observation (raised processing [inline-internal-term-marker]) that behavioural-trigger parked items promise active monitoring the method doesn't deliver — a slug trigger fires on its own, but a behavioural one only reconnects if someone files a fresh capture, so nothing reminds the user to watch. Parked for a dedicated design session alongside [design-session-queue-home] and [method-doc-structure-pass], with the two-way-sync requirement flagged as the design risk (a generated view may avoid the drift). [inline-internal-term-marker] itself stayed parked — its slug trigger ([narration-vocabulary]) shipped, but its real condition (observed leakage) hasn't occurred.

On the cruise-control point-and-click question, the user corrected an initial framing: I proposed a run-time "don't build on an unverified batch" rule as the guard, but the user rightly rejected making the run-time check the sole mechanism. Recorded into [cruise-control] concern 8: the guard is primarily plan-time (the readiness line must not clear a batch depending on a batch whose verification is still pending), with the run-time rule kept only as a backstop. This exposed a real gap to settle in the cruise-control design — the readiness definition counts a "producer" as built work without distinguishing built-and-verified from built-with-verification-deferred, which matters under autonomy.

The readiness line (`--- Cleared to run above this line ---`) was placed for the first time, above the cruise-control plan marker — all four queued batches are cleared to run; cruise control stays below as a design block.

**Queue changes:**
- Promoted [rezip-push-cli-flow] (top) and [install-docs-cli-refresh] (below it) from the desktop-upload-removal captures.
- Promoted [tag-recheck-doc-fixes] from three 2026-06-29 audit captures.
- Dropped [install-upload-path-clarity] (stale — upload screen gone).
- Parked [watchlist-doc] (design item, blocked by the queue-structure design session); kept [inline-internal-term-marker] parked (behavioural trigger not met).
- Recorded a verification-status refinement into [cruise-control] concern 8.
- Placed the readiness line above the cruise-control plan marker.
- Deferred-test roll: nothing rolled (host-side lines confirm by observation; the two external install lines wait for a push).

**Captures routed:** Promoted — [readiness-line-narration-frequency], [trace-rule-needs-exemplar], [done-freeform-recommend-hedge-gap] (→ [tag-recheck-doc-fixes]); [desktop-plugin-upload-removed], [cli-install-confirmed-rezip-loop-update] (→ [rezip-push-cli-flow] + [install-docs-cli-refresh]). Parked — [watchlist-doc]. Dropped — none (the dropped [install-upload-path-clarity] was a batch, not a capture).
