# [HASH] — New-user install route promoted to a first-class section, and INSTALL.md given a phrase that proves the guide was read

Three fixes, kept together at processing because the second and third are two halves of one thing: a new user cannot otherwise tell whether the assistant in front of them is working from the guide or improvising from the link's title.

README's Install section is now three named subsections with "New to Claude Code? Start here" first, rather than that route sitting third of three notes. Its line asks Claude to read the guide and walk the user through it step by step, and says why that matters — a Claude that improvises from the link alone skips steps it cannot see, including the pacing rules the guide exists to carry.

INSTALL.md gained the verification phrase at both ends: near the top, telling the user the exact line Claude's first message should open with and what to do if it doesn't; and in the "Instructions for Claude" section, the matching instruction to open with exactly that line and to say it only when the guide has actually been read.

The item's own refusal was honoured: deferring the verification phrase as future work was rejected, on the ground that it is one sentence and serves the same failure the reworded link guards.

Files touched: `README.md`, `INSTALL.md`
Routed to Captures: none
Rule gate: not needed — install documentation, no method rule text.

Depth: short. Ticked as done, confirmed.
