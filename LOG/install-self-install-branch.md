# [HASH] - INSTALL.md: add a terminal self-install branch for users who already have Claude Code

For repo visitors who already have Claude Code, the GUI zip-upload is the slow path; Claude Code can install via the non-interactive marketplace commands. INSTALL.md now offers a terminal path (B.1) - marketplace add FlintCraftTech/sovereign-implementer then install sovereign-implementer@flintcraft, run inside Claude Code or by asking its agent - with the GUI zip-upload kept as the no-terminal fallback (B.2). The branch hands off: the claude.ai chat reading INSTALL.md cannot run the install itself, and the doc says so. Commands sit in their own fenced blocks per the verbatim-copy rule.

**Files touched:**
- INSTALL.md

**Routed to Captures:** none
