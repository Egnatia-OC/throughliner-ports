# [HASH] — /plan: promoted all four waiting captures into cleared build batches — [reference-by-slug-not-status]'s ride-alongs [retire-spectrum-offers] + [sequence-record-then-release] + [proactive-queue-referenced-pushback] (all plugin-behaviour.md) and [captures-as-headings] (queue format + lint); dropped parked [bundling-jargon-recurrence] (its recurrence-watch fired); readiness line moved below [captures-as-headings]

Short session after the prior /plan broke mid-outage — opened by confirming nothing was left dangling (only the hash backfill and the intentional rezip test suffix were uncommitted; the empty IDEAS.md is the live inbox for the parked docs-open-editor workflow, kept). All four captures promoted. [retire-spectrum-offers]: the spectrum rule is rarely used, tension with recommend-don't-menu, and costs tokens every session; the possible base-prompt duplication stays unverified and isn't load-bearing. [sequence-record-then-release]: bundling recurred with both prior fixes active (concise style live, list pre-chunked), proving instruction-level fixes don't relieve the completeness pull — the fix records the full set in the working file and releases one item in chat; this consumed the parked recurrence-watch, dropped with its evidence carried into the batch rationale. [proactive-queue-referenced-pushback]: promoted with a conservatively-bounded trigger (fires only on changes altering something the record already holds; one index read, one entry max) to answer its own token-drain worry. [captures-as-headings]: accepted the known tension with the parked queue-item-model redesign rather than holding a cheap editor-visibility win on an unscheduled design session. Mechanical maintenance: de-statused a "promote-ready capture" reference in [reference-by-slug-not-status]'s rationale (its own rule applied).

**Queue changes:**
- Four batches added above the readiness line: [retire-spectrum-offers], [sequence-record-then-release], [proactive-queue-referenced-pushback] placed after [reference-by-slug-not-status]; [captures-as-headings] last.
- Readiness line moved to sit above the cruise-control plan marker — all six batches cleared.
- Parked [bundling-jargon-recurrence] dropped (trigger fired; evidence carried into [sequence-record-then-release]).

**Captures routed:** promoted [captures-as-headings], [retire-spectrum-offers], [bundling-recurred-despite-output-style] (as [sequence-record-then-release]), [proactive-queue-referenced-pushback]; dropped parked [bundling-jargon-recurrence].
