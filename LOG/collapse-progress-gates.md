# [HASH] — next.md + plan.md: collapsed pre-work ready-checks to at most one in both skills

Merges the identical over-asking pattern — Claude asking the user to okay forward motion more times than the work needs — in two skills.

**/next side.** next.md Step 1 opened the run with two beats: "Send the run" (present it as its own beat) then a separate "Confirm" ("Ready?"). These merged into one present-and-confirm beat: show the run once, ask "Ready?" once, in the same message.

**/plan side.** plan.md Step 2's present-and-interview had the first item land as its own beat and end on a closing prompt inviting the user to continue, with analysis opening the next turn — a separate approval turn before analysis began. That standalone go-ahead turn was removed: the raw item still leads the message (the readability design), but the analysis now follows in the same message, with no content-free "shall I continue?" turn. The load-bearing Step 1 entry question ("anything to discuss before the queue?") stays; the [PROMPT] at the interview's end, where the user's real decision is needed, stays. The view-in-doc pointer paragraph was updated to match.

Principle both share: each skill asks to proceed at most once before work begins. No SPEC/FAQ — interaction smoothing, not a new feature.

**Files touched:**
- next.md (Step 1 Steps 3–4 merge)
- plan.md (Step 2 sub-step 1 first-item gate removed; view-in-doc paragraph updated)

**Routed to Captures:** none
