# [HASH] — Kept and cleared: migrate-checklist.md declares itself tag-free, mirroring setup.md

The declaration route won over tagging: the checklist runs where setup.md runs — where the tag definitions may not be loaded — so the same reason applies and the consistent fix is the same one-line declaration, which also settles the prose approval moment as deliberate.

Rule gate: not needed — a declaration matching an existing sibling's.

**Work processed:** kept — [migrate-checklist-untagged].
