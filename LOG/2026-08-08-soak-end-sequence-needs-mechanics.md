# abc4fdc — CLAUDE.md branch-cycle gains the written soak-end sequence

Built in the 2026-08-08 overnight blitz, run 2 — processed autonomously under the softened bar (departure recorded): the capture was your instruction to record the sequence as performed ("it should always happen at the end of the soak"), the mechanics were already written out in the capture itself, and its named likely answer (a branch-cycle sub-section) is what was built. CLAUDE.md's branch-cycle section now carries the seven-step merge-and-rebranch sequence, the two recurring blockers (the hash backfill dirtying `LOG/index.md` must be committed before any checkout; a file the branch deletes but a stale host still writes blocks the very merge that stops it being written), and the exposure inversion — a deferred merge kept a privacy exposure on main. The two sub-questions the capture left open are stated as open in the written text rather than decided: whether the blockers deserve a mechanical pre-merge check, and whether the rezip rightly follows the merge. Host-only.

**Files touched:** CLAUDE.md
**Routed to Captures:** none
FAQ: not needed because the branch cycle is host-only; consumers never run one.
