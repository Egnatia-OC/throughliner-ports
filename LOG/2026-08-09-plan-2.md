# 8e65a12 — Twelve items cleared to run, docset A retired on the user's call, and the approval flow flipped to write-first mid-session; two captures consolidated by repeal and the misattribution of the user's own reasoning caught in this session's own writing

The session opened on the user's three stated priorities — the new authoring rules, the session that audits the old ones, and integration with Understudy — and those decided its shape. Two of the three were already in the queue; the third was not in it at all.

**Understudy resolved into two halves, one already built.** State-reading is the `.throughliner` editing-state marker, restored at version 2 in the previous run, so nothing was needed there. Message exchange was new, and the user supplied both the mechanism and its precedent: an `INBOX/` folder, as already used in a project they share with their partner. A live test settled the question the user raised and could not answer — whether Claude can still write into another project's folder. It can: a file written to a neutral sibling folder outside the project landed with no block and no permission prompt, and was deleted again. So direct delivery is real and no message has to be carried by hand. The one gap that test exposed is that it succeeded *outside* a build; during a `/next` build the scope-lock would deny it unless `INBOX/` is a permitted location, which is now step 5 of [cross-project-inbox]. The red flag on that item — content crossing project boundaries into repos that may be public — was cleared by design rather than accepted: the user approves every outbound message before it is written, the same human backstop the feedback report already relies on.

**Docset A is retired, which halves the audit's corpus.** The user's reason: both Fable 5 and Opus 5 now work on docset B, so the 4.8 fallback the two-docset design existed to protect is no longer needed. The decision resolved a three-way contradiction the emergency revert had created, and it made the audit scopable — its corpus drops from roughly 67,000 words to 26,000. One thing found by checking rather than assuming: this very session's `/plan` skill loaded `docs/plan.md`, so the host was running the docset the method believed it had retired, and every recovery built into `docs-b/` last run was text nothing was reading. That turned retirement from a deletion into a repointing job: all four SKILL.md files still name `docs/`.

**The rule audit was scoped, and its first job turned out to be arithmetic.** The gate's binding limit is a *count of instructions*, not words, and no such count exists — so "evict what fails admission" had no target and no stopping rule. The audit now begins by enumerating every instruction in the always-loaded corpus with an id, to measure against the 150–200 ceiling. A second finding strengthened the case for doing it soon: `docs-b/plugin-behaviour.md` was 6,162 words at the revert and is 7,269 now, about 1,100 added in a week by recovery builds that each passed the new gate honestly. One week in, the predecessor's failure pattern is already back. A conflict in the item as written was also resolved: it was tagged `[audit]`, and an audit reports rather than edits, so as tagged it could not evict — reproducing the trap its own last paragraph warned about. Rather than cramming a 26,000-word rewrite into a review pass, its output *is* the eviction list, approved during the run and cleared to build immediately.

**The user corrected a failure mode Claude had assumed wrongly, and the evidence was in this session's own writing.** Claude framed the missing-rationale problem as a wrong recorded why causing bad decisions later, and admitted it could point to no instance. The user's correction: it is not about downstream decisions, it is that being repeatedly credited with saying and deciding things Claude said and decided feels wrong in the moment. That reframing found three instances in the session that discussed it — every item written carried "Captured by you," including one narrowed almost entirely from Claude's own reading of the docs, and [retire-docset-a] carried "Their reason: both Fable 5 and Opus 5 now work" when the user had said only that both work now and it's fine. So this is not a new rule but an existing one not holding: the provenance convention already says the credit tracks who *stood behind* the item and stays unmarked in doubt. A hook cannot help — nothing in a finished item distinguishes reasoning the session produced from reasoning the user gave — so sharper wording is the only route, and the user supplied the exemplar from the Understudy project: *"rejected on Claude's recommendation and Alex's agreement"*, which names roles rather than assigning the whole item to one party. The proactive rationale-nudging design that item used to carry was retired outright rather than deferred: it would add an always-loaded judgement-based ask against the instruction ceiling, and its failure mode is the nag the method has spent weeks removing.

**The approval flow flipped mid-session, and using it immediately exposed a dependent rule.** The user asked to stop having drafts pasted into chat — write to the file and report. That inverts two core rules rather than relaxing them, so the design turns on one test instead of a memorised list: *does a revert fully undo it?* Yes for queue items, LOG entries, SPEC edits and build edits, all tracked in git. No for commit messages, which are harder to unwind and never become file content, and for anything leaving the machine, where a revert cannot reach the delivered copy. One test rather than an enumerated carve-out, because a list silently omits approval moments added later. Then using the new flow found something reasoning had not: the keep-step instructs removing an item from Unprocessed *first*, explicitly because the drafted item is "already approved and on screen" — a justification write-first destroys, making remove-first the lossy order. That inversion is folded into the item.

**Two captures were consolidated and repealed rather than rewritten.** [desktop-native-md-open-vs-editor-field] and [line-anchored-link-dead-in-desktop-app] held contradictory claims about the same mechanism. Three observations were on record; only the one from a deliberate `[user]` verification holds: the link opens the file, and the *anchor* is ignored. That falsifies "clicking does nothing" outright and undercuts "opens at the correct line", which came from a session where two of three app-behaviour claims were falsified the same way. The consequence was larger than either capture anticipated — with working mode retired earlier in the same session, the recorded Editor field was the last remaining gate on whether a pointer is emitted at all, so retiring it makes pointing the unconditional default. Its cost is accepted rather than overlooked: a user not on the desktop app may genuinely need an external reader, and that support goes.

**Ordering itself became a queue item, on the user's observation.** Mid-session the user asked whether anything would become unblocked soon and therefore be worth processing now. That question produced the best sequencing decision of the day — three clusters worth taking, and one deliberately avoided because it would write into a corpus about to be inventoried. Claude never offered it. So the capability was not missing, it was never surfaced: the reorder already orders by unblock-potential, silently, *after* the entry ask. The user then corrected the design once more — the existing bulk-drop offer must come first, because ordering items about to be deleted is wasted work. The result removes a question rather than adding one: three opening beats become two, and Step 1's "anything to discuss?" is absorbed.

**Where the record was corrected rather than extended.** Four items carried claims that checking falsified. [restore-reverted-code-fixes] said the subagent cost gate needed only a manifest fix because `pre_tool_use.py` already matched both tool names; it does not — line 406 tests `tool_name == "Task"` alone, so following the item would have left a live cost guard dead *and reported it fixed*. [restore-shell-write-denial]'s open question — whether the sanctioned mover covers the shapes that keep tempting the script route — is answered no, so it must build with the mover fixes or the guard just gets worked around again; its instance count rose from three to five, two of them in this session, one of which was Claude beginning a Python splice and stopping only on recognising the item's own description. [repo-identity-reverted-to-old-name] needed a scope boundary written in, or it would have swallowed the repo-wide product rename. And the line-anchor correction spans two sites in docset B, not the seven claimed — that count covered both docsets.

**Two things left deliberately alone.** [re-author-five-evidenced-rules] looks ready now that its prerequisite has shipped, but four of its five rules land in the file the audit is about to inventory, so it must follow the audit rather than precede it. And the Editor-field question was kept out of the working-mode retirement even though both touch the same machinery, because they rest on different evidence.

**Queue changes:** twelve items cleared to run, ordered so the two code-restores build first (their tools — the queue mover and the shell-write guard — are used by every later build, and the subagent cost gate is currently off), docset A retires before the docs-b edits so there is one copy to touch, write-first precedes the working-mode retirement that depends on it, and the repo-identity correction lands last because it must ship before the next release. Last session's advisory cleared, its persist-condition met. Three dependent references reconciled rather than left dangling: the Claude-downstream evidence in [below-line-revisit-nags-by-another-route], and findings 2 and 3 of [queue-items-on-falsified-premises], both marked resolved with the Editor-field half explicitly left open. Three stale references to the retired nudging thread corrected in [rename-to-throughliner] and [throughline-value-articulation]. The ordering half of [approval-flow-token-doubling-simplification] annotated as decided elsewhere so it is not re-decided there.

**Work processed:** kept — [cross-project-inbox], [self-authoring-rules-compliance-audit] (rewritten and scoped), [retire-docset-a] (from [docset-a-retired-or-frozen-contradiction]), [below-line-revisit-nags-by-another-route] (narrowed), [rationale-misattribution] (replacing [throughline-feature-elevation]), [throughline-value-articulation], [write-first-approval-flow], [plan-entry-ordering-default], [remote-control-opening-mention], [doc-link-rendering-settled] (consolidating two captures), [repo-identity-reverted-to-old-name], [restore-reverted-code-fixes], [restore-shell-write-denial]. Deleted — [opus5-session-start-detection] (moot once docset A retires), [rationale-relocation-invisible-to-consumers] (already satisfied; its live residue split out as [announcement-rationale-split-correction]), [working-mode-render-verification] (verified a retired mechanism), [throughline-feature-elevation] (nudging framing retired), and by repeal [desktop-native-md-open-vs-editor-field] and [line-anchored-link-dead-in-desktop-app].

**Routed to Captures:** [announcement-rationale-split-correction], [write-first-approval-flow] and [remote-control-opening-mention] (both filed then processed the same session), [plan-entry-ordering-default] (same), [agents-md-codex-residue], [spec-sync-trigger-decision-vs-behaviour], [advisory-indistinguishable-from-work-item].

**Red flag cleared this session:** [cross-project-inbox] carries a `Red flag · State: cleared` marker. Cleared by design rather than accepted. The risk: a message written into another project carries content out of this one, and both INBOX folders sit inside git repos that may be public, so an unscrubbed message is a route for private content to reach a published repo. Designed out by requiring the user's approval of every outbound message before it is written — the same human backstop the feedback-channel report relies on, and the reason Claude never sends anything on the user's behalf unapproved. The build is also to offer gitignoring `INBOX/` at setup, and the broader scrub work in [scrub-gate-before-approval] covers these artifacts when it lands.
