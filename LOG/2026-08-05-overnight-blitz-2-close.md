# [HASH] — Second overnight blitz close: 14 items shipped across two runs, 3 captures filed, test build 1.18.0-test1 installed, soak-before-merge recommended

The second run of `resources/overnight-blitz-plan.md`, on branch `overnight-blitz-2026-08-05b`, executed under the plan's three sanctioned departures (approvals deferred to branch review; autonomous processing under the softened bar, this time with the bar's per-item calls recorded below; no push and no release overnight). The complete reviewable delta is `git log main..overnight-blitz-2026-08-05b` — three commits, each with per-item LOG entries.

**Phase 1 — the cleared region (9 items, commit `3affa20`).** All nine cleared builds ran in one sequence per the prior advisory: the subagent-gate tool-name fix, the readiness-marker/orphaned-prose lint checks with the fail-closed /next read (both docsets), the dependency-rot pass (spent lift-conditions, repeatable-event rule, Blocked-by clearing), the docset-A completion-ask corrections with the freeze rule written into CLAUDE.md, the FAQ/ quiet-list addition, the removal asymmetry with its lint backstop and two-limb underspecification test, the 60-replacement vocabulary retirement, the advisory tightening (earns-its-place, partial rewrite, the done-plan contradiction), and the page-to-completion rule. The `[user]` dogfooding item was skipped per the blitz's hard boundaries.

**Phase 2 — triage and sweeps.** All 46 Unprocessed items were triaged against the softened bar after paging the queue to completion. The sweeps found the corpus healthy (consistent with the first blitz's clean result — the capture target is a ceiling, not a quota): the method-compliance pass over the docs changed tonight was clean; the cross-doc greps found only legitimate retirement statements; the hook code read of session_start.py produced one real finding. Two captures filed: [session-start-silent-docset-fallback] (the silent downgrade to docset A when docs-b is missing — the same silent-fallback family as the dead subagent gate) and [merged-plugin-verification-checklist-stale] (its checklist tests for a removed provenance lint). A third finding — [working-mode-render-verification]'s known-stale checks — was already recorded in the item and rides the closing advisory instead.

**Phase 3 — bar-passing captures (5 items, commit `1332c55`).** Processed and built: the downstream-action discriminator for user-only lift-conditions, the closing-advice pair (fresh-chat halves folded into the ladder rungs; /done scoped out), the mover usage-message/cheap-invocation fix, and the disk-change-halt rule.

**Held, with the reason per item — the triage record for the next /plan.** Genuine design forks: [session-sizing-and-break-lines], [next-start-order-handoff], [concurrent-session-support], [next-per-item-queue-removal], [approval-flow-token-doubling-simplification] (also gated on an external issue), [post-close-tail-state], [drive-testing-signals-skill-routing], [done-delta-close], [self-hosting-auto-detection], [scope-file-editable-vs-locked], [setup-runs-outside-behaviour-rules], [no-session-end-or-compaction-hook], [build-scope-vs-described-work-unenforced], [user-tag-over-applied-to-claude-doable-work] (its check's placement is genuinely open), [plan-session-order-diverges-from-file-order], [memory-destination-invisible-to-the-record], [faq-as-claude-readable-reference] (first move is transcript measurement), [doc-filetype-switch-for-line-links] (gated on two external issues). The [user]-deferral trio held to process together: [partial-user-walkthrough-has-no-close-move], [user-completion-logged-against-unverified-work], [user-deferral-undefined-in-walkthrough], [reroute-does-not-suppress-resurfacing]. User-owned decisions: [merge-and-fold-without-asking] (the user's own judgment call to confirm), [no-standard-names-for-workflow-docs-vs-user-files] (consumer-facing naming), [gitignore-scaffolded-faq] (consumer-facing /setup behaviour). Hook-behaviour not fully stated: [shell-writes-bypass-file-scope-lock] (a leading candidate exists but the design isn't settled). [user] items: [report-url-404], [respond-to-rygel-opencode-pr] (outward-facing). Harness questions for /plan: [plan-skill-did-not-engage], [skill-docset-override-unsanctioned]. Partially designed only: [queue-pointer-hard-to-follow-and-possibly-stale] (its interim mitigation is stated but building it wouldn't close the item). Below-the-line Processed items were untouched per the boundaries: [history-rewrite-third-party-scrub], [release-docset-b-work], and the rest.

**Phase 4 — test rezip.** plugin.json bumped to 1.18.0-test1 (working-tree only, never committed, per the carve-out), __pycache__ purged, plugin cache pruned to current+3, CLI checked (2.1.220) and the host re-snapshotted from the branch via `claude plugin update`. `hook_schema_check.py` passed in full, run only after the commits so its backfill side effect resolved cleanly — all fourteen placeholders filled to `3affa20`/`1332c55` and folded into this close's commit.

**Departures exercised:** items processed and built without live approval (every one reviewable in the branch diff and these entries); no push. Standing lint noise: none this run — the queue linted clean throughout, including under the new checks.

**Verification for the morning:** fully quit and relaunch the app; the first session should confirm what actually arrived in its context (the docs-b directive and the state lines), not whether it looks right.

**Files touched:** the branch's three commits across hooks, both docsets, scripts, templates, QUEUE.md, CLAUDE.md, LOG/
**Routed to Captures:** [session-start-silent-docset-fallback], [merged-plugin-verification-checklist-stale]
FAQ: not needed because every user-facing change this blitz shipped restores or tightens behaviour the FAQ already describes; the per-item entries record each disposition.
