# aa78310 — The style-dedup policy repealed in-session: the docs stay canonical, the style deliberately restates them, and a rule living only in the style is a defect

The anchor record for the style trio; the entries for [narration-cadence-promotion-candidate] and [style-negatives-to-rewrite-positive] cite it.

SPEC said a rule carried at style level is stated there and nowhere below — which, applied to the six style bullets restating always-loaded communication rules, would strip those rules from any project that declined the consent-gated style. The three routes weighed: scoping the dedup per project is structurally impossible (the docs ship identical everywhere); making the style mandatory reverses a deliberate consent decision; repeal won, and the style's own purpose argues it — its value is stating the steering at system-prompt level, so restatement is the mechanism, not redundancy. The SPEC sentence was rewritten in-session to the reversed relationship, including the new defect class: a rule living only in the style is a promotion candidate. `resources/research/output-styles-plugin-capability.md` gained a partial `Superseded by:` line — its parent-axis-deduplication reasoning falls for consent-gated layers; the factual findings stand. Nothing was announced about the dedup, so no correction post is owed. The capture was deleted with everything relocated.

**Queue changes:** SPEC rewritten, research superseded, capture deleted.
**Work processed:** deleted after relocation — [style-dedup-unsafe-while-opt-in].
