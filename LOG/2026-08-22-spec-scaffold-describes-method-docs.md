# d7ea8a0 — The SPEC scaffold stops writing the "Project docs" section; the managed CLAUDE.md block is its refreshable home

Kept on the first of the item's three candidates, settled by what shipped since filing: [managed-claude-md-block-never-refreshed] built the refresh capability for CLAUDE.md's plugin-managed block — the right home for what the three docs hold — while SPEC can never get a managed region, since method-owned text inside the user's product truth is the defect itself. The section also fails SPEC's own admission rule. The scaffold stops writing it; the template's managed block is verified by the build to carry the description; existing projects learn through the migration's report-only retired-terms step, which gains one detection and edits nothing; and the scaffold sweep for the same shape (method content copied into consumer docs) rides the build, findings as captures. Reducing the section to behaviour was refused — still method description in the wrong document, still unreachable by refresh. Full disposition and build block on the item.

**Queue changes:** kept into Processed, cleared to run.
**Work processed:** kept — [spec-scaffold-describes-method-docs].
