# b405a38 — Four items processed, and four Discord posts found sitting behind blockers that had already shipped

The session's most useful output was not planned. The user asked why `[rename-to-throughliner]` had never happened, believing it was stuck below the readiness line and that a blocker had never been filed for it. Both halves of that were wrong and the instinct behind them was right. The rename was never held — it has been cleared to run for some time, placed last because it carries `Runs alone`, which is exactly what the method prescribes. What starves it is that every planning session places newly kept work *ahead* of it, so a correctly placed item recedes each time the queue is worked. This session alone put three items in front of it before anyone noticed. Filed as [runs-alone-at-the-end-can-be-starved], with the obvious fixes named and both judged weak: placing such work first stops every run at the top, and reporting how long an item has been cleared needs a threshold nobody can defend.

The user's standing rule — never hold something without filing what it waits on, or it may never surface again — then found a real instance somewhere else entirely. Four `[user]` Discord-post items in Unprocessed carried `Blocked by:` lines naming slugs absent from the queue. All four blockers were checked against LOG and **all four had shipped**, three of them the previous day with LOG entries named after their slugs. Those posts had been ready and invisible. The cause is a coverage gap rather than a bug: `plan.md`'s below-the-line revisit asks "has this blocker shipped?" and walks Processed below the marker only, so a `Blocked by:` sitting in Unprocessed is read by nothing. The queue digest does resolve the reference and print `ABSENT`, but ABSENT reads identically for a blocker that shipped and one that never existed, and no step is told to act on it. All four were unblocked in place, each recording which LOG entry cleared it; the gap is carried as [blocked-by-in-unprocessed-is-never-revisited].

[build-log-restates-plan-rationale] was deleted on evidence rather than on argument, and the finding is recorded here because it is the part worth keeping. The user's question was whether a build LOG entry needs rationale at all, given the deciding why is already recorded at /plan. Checking one real pair falsified the premise: `LOG/2026-08-12-plan-2.md` kept fifteen items and records most of them as a slug in a list with no reasoning attached, and the queue item that *does* carry the reasoning is consumed by /next the moment it builds. So the build entry is not a restatement — it is the only surviving readable copy, and the intuitive trim would push the deciding why into the git history of a deleted block. Nothing changes inside any file, which makes it a finding rather than work.

The other two close-cost items survived and converged. [close-cost-depth-line-never-surfaces] was diagnosed to two causes by reading the docs rather than by taking the report: the depth rule is a floating sentence in `next-build.md` attached to no step, and its signal is an *absence*, so at the close a deliberate "short" and an item nobody considered look identical. The build makes it a required field on the per-item tick — the same required-artifact shape the FAQ-sync and rule-gate obligations already use, and the only shape that survives an absence problem. [index-candidates-pregenerated-for-items-never-reached] lands on that same tick step: its own stated objection, that pre-generating doubles as a readiness check, did not survive checking, because `skill-nonspecific-rules.md` already assigns that check to /plan's keep-step and `done.md` re-authors the entry anyway when scope shifts. The two are placed adjacent so a run cannot edit that step twice.

Mail arrived mid-session and corrected a finding this project had already queued. Understudy withdrew the worked example behind [no-field-for-ordering-preference]: their two held items were not a sequencing preference dressed as a dependency, but a genuine dependency their own session had argued for badly. The item was annotated rather than quietly rewritten, and now states outright that nobody may build on the old example. Their narrower observation was checked against our docs before being filed and holds exactly — the built-but-unverified clearing rule lives only in `done-plan.md`, while `plan.md` can clear an item with `--marker-after` and can lift one on a "shipped" test that never distinguishes built from verified. It was processed the same session as [built-unverified-check-only-at-the-close], fixed by the precedent that already repaired the `[user]`/`[audit]` end-preference rule: each insertion point *names* the close's rule instead of restating it, so two copies cannot drift. Their third point was promoted rather than dropped, and the correction is what promoted it — the placement turned out to be right and their user still could not see her work, so the invisibility survives the fix.

Two failures of Claude's own conduct were caught by the user and filed rather than absorbed. The filing-claim hook blocked on a slug that had only been *cited*, not filed, which is conduct the always-loaded rules positively require — the second recorded shape of that hook firing on mandated behaviour. And when the user's named subset of three items was exhausted while 35 items remained in Unprocessed, the session reached for the empty-queue gate and offered to close. The user had to ask why continuing was not an option. That gate is worded carefully not to lean toward closing *given an empty queue*; applied to a full one it silently reclassifies the remaining work as nothing left to do. The user also corrected its wording: "close out" names a /plan phase the method retired, and the phrase survives in two user-facing gates while the same document states a few lines later that no such phase exists.

**Queue changes:** four items processed (three kept and cleared, one deleted); [rename-to-throughliner] moved to the top of the cleared region so the next run reaches it first and stops there per `Runs alone`, instead of receding behind newly kept work; [close-cost-depth-line-never-surfaces] and [index-candidates-pregenerated-for-items-never-reached] placed adjacent because both edit the same step in `next.md`; four stale `Blocked by:` lines dropped from Unprocessed after verifying each blocker against its LOG entry, leaving the digest with zero unresolved references; the consumed forward advisory cleared, its persist-condition met.

**Work processed:** kept — [close-cost-depth-line-never-surfaces], [index-candidates-pregenerated-for-items-never-reached], [built-unverified-check-only-at-the-close]. Deleted — [build-log-restates-plan-rationale], on a falsified premise, with the finding recorded above. Annotated rather than processed — [no-field-for-ordering-preference], whose evidence was withdrawn by its own source.

**Routed to Captures:** [stop-hook-fires-on-cited-slugs], [built-unverified-check-only-at-the-close] (filed from mail, then processed the same session), [held-items-invisible-during-normal-use], [subset-done-has-no-stated-shape], [plan-gates-say-close-out-for-a-retired-phase], [blocked-by-in-unprocessed-is-never-revisited], [runs-alone-at-the-end-can-be-starved].

**Mail:** `2026-08-13-understudy-correction-finding-1-example.md` opened, routed and archived; a reply was drafted, approved and delivered to Understudy's INBOX. Sending places a file in their mailbox and guarantees nothing beyond that.

Planning state: _plan-c7742136.md, 4 dispositions plus 7 captures and the mail routing.

FAQ: not needed because nothing user-facing shipped — this session changed only QUEUE.md and LOG/. Each decision reaches consumers through the build it queued, and those builds carry their own FAQ obligation; two of them say so in their own text.

Rule gate: not needed — no rule in the method's own text was authored or amended. The commit touches QUEUE.md and LOG/ only.

**Queue changes** and **Work processed** above were filled from the planning working file's disposition list, not reconstructed from memory.

---

## After the close

Work that happened after this session's commit, appended rather than folded into the record above.

The close itself broke a rule this project shipped the same day. `done.md`'s Recommend next, rung 2, says to name the next item as information with **no question and no command string ending the message** — the fix that landed as [close-invites-same-session-next] in `340e7ef`. This close ended on a bolded "Start /next when you're ready", and the harness duly offered `/next` as its suggestion chip. That is the precise outcome the rule exists to prevent, produced on the rule's first real test by the project that authored it. Recorded here rather than quietly corrected, because a shipped rule failing immediately is worth more as evidence than as an embarrassment: it is the fourth instance this session of a rule being read, understood, and then not followed by the session that had just read it.

The user then turned the same mechanism into a design. Their observation is that the suggestion chips are useful and should not be disabled — the recommendation is often *only* legible in the chip, which is how they find what Claude is actually proposing — and that the real problem is which command lands there. Their proposal: mid-session asks should not end on the close option, burying it mid-sentence and putting continue last since continue is the commonly taken route; recommendations should surface last so they are clearer and more likely to be picked up; and the close should recommend running /clear or starting a fresh session, leaving the chip generator no /next to grab. The mechanism they are exploiting is already documented in `done.md` — the harness offers the slash command it just saw — but only ever as a hazard. Nothing had noticed it also runs in the useful direction, and nothing had tried to aim it. Filed as [message-ending-drives-the-suggestion-chip], with the honest limit written into the item: shaping the method's prose around an undocumented third-party heuristic is defensible only because two of the three parts are independently right under existing rules and would stand if the chips vanished.

A related check was run and answered: the chips are not the plugin's and nothing in the user's settings controls them. `CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION=false` would disable them wholesale, which is not what is wanted — a blunt off-switch does not distinguish a useful chip from a badly aimed one.

**Files touched after the close:** `QUEUE.md` (the forward advisory, and [message-ending-drives-the-suggestion-chip]), this entry.
