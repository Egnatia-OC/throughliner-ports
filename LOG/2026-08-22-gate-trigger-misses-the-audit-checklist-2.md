# [HASH] — The compliance-audit checklist joins the gate's trigger set at all three sites, with the in-repo-only limit stated

The checklist's entire content is rules, and a session editing it summoned no gate — surfaced when an item amended it and the gate ran only because the session happened to notice. The prior question the item raised (should the trigger be a path list at all?) was answered by its own fallback at the keep: no fail-safe shape exists that does not guess at what "holds rules" means, so the path is added and the limit said. The second instance — a rule amended in a folder-level CLAUDE.md above this project — gets the honest-limit treatment: the trigger reaches files in this repository only, and a rule living outside it gets no gate, stated where the trigger is described.

Built: the checklist path added to CLAUDE.md's gate trigger and close-step rule, and to rule_signals.py's RULE_BEARING constant; the limit clause on the trigger sentence. A commit touching only the checklist now counts as rule-bearing to both the gate and the script — and to the new audit-lag check, which reads the same constant.

Tick: done, confirmed — grep finds the path at all three sites; the limit clause reads in the trigger's description.

**Files touched:** CLAUDE.md, resources/rule_signals.py
**Routed to Captures:** none
Rule gate: run — amends the gate's own trigger set, its named parent, as a widening of the existing list plus one honest-limit clause; no freestanding rule, nothing evicted. Failure evidence: two instances on the item, one in-repo and one out.
FAQ: not needed because this is host-only; consumers never author method rules.
