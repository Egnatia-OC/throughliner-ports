# 931f782 — The cleared region rebuilt from two items to sixteen, and the length problem finally given a derivation

The session opened to process the queue most-unblocking-first and turned, on the user's question, into the one that settles how long anything in this method may be.

**[law-prose-restyle] cleared, and its group gate expired rather than gaining a field.** The item had been sent back to Unprocessed because its real blocker was a condition over a group and `Blocked by:` takes one slug. Checked against the record rather than against the item's own day-old count: group A is four shipped and one refused, group C six shipped and two resolved otherwise. Group B — the rationale audit — had shipped as an audit, leaving nine unbuilt findings, eight against the same file this pass rewrites. Those eight were **folded in** rather than built separately, because building them first would put two per-paragraph passes over one set of sentences, each able to undo the other's wording. One of the eight carries a decision the user took here: the one-chat-at-a-time rule keeps its history clause, **reclassified as operative**, on the evidence of her own recorded reaction to a stale FAQ entry describing two-chat work as supported.

**The length problem's blocker was never real.** The measurement shipped on 2026-08-15 stated no band deliberately — a range read off a bloated corpus encodes the bloat. That is right, and it does not follow that no figure is available: **July is measured, from this project's own record, before the August doubling.** Captures 177, build entries 229, plan entries 323, index lines 40, against 336, 478, 898 and 76 now. [written-shape-word-bands] ships those as a soft band, a hard ceiling and a named action on breach, per shape.

**What the user was actually reporting is larger than length.** In her words: *"it is the reason I can't show the tool to anyone because it is impossible to use because of all the reading. I operate this whole project by inferring what all the work items and captures say from what you say in the chat."* SPEC says the user keeps Claude aligned by reading and approving the record. Claude's first move was to correct SPEC to describe the present, and she refused it: reading and approving *"has been what I've been shooting for the whole time"*, the original blocker was that no good reading surface existed, and what changed is that verbosity now costs too much time. So SPEC holds the goal and was extended instead — the tool is built for someone willing to read, someone who won't is considered but never the target, and **length is what has to be earned**. The defect is in the always-loaded rules: the write-first clause requiring a report *"specific enough to object to without opening the file"* designs around the artifact not being read. That is [spec-control-model-not-what-happens].

**A measuring tool had been built and nobody appointed to read it.** Her question — *is this an audit?* — was the right one, and the answer is that it should have been. The build finished when the script existed; the reading half was never filed as work, so nothing detected its absence and the report sat for three days. Her decomposition became the rule: making the tool is build work, running it is audit work, both in the same session. Two recorded instances back it, this script and `rule_signals.py`. Tracing its placement caught a ripple — the close's end-preferred reorder would separate a tool-paired audit from its tool, *after* /next, so the break would land on the next run.

**The `docs-b/` rename refusal was overturned, one day old.** Its argument was that renaming moves the "B" out of a live path and into 424 historical references — but a session record naming `docs-b/` in August is an accurate record of August, and nobody proposed rewriting it. The Codex-port precedent it cited is an argument for leaving history alone, not against renaming a live folder. The real cost is 215 live references, and this project has already renamed itself wholesale once.

## Also in this chat

**Four Claude proposals were reversed by the user, and the pattern in them is worth keeping.** Each was a plausible design produced without reading something that was already on file, or without her purpose in view.

- The **SPEC correction** above, which would have deleted the goal while calling it a correction.
- A **prose remedy for length**, proposed while `resources/research/llm-length-instruction-following.md` sat on file recording that a two-tier word range is the primary lever and that a prose remedy had already shipped and measurably failed. Her words: *"we did so much research and work on this and you're just throwing it out."* That research is now marked at the superseded passage, which previously reversed itself sixty lines later with no signal at the first reading.
- A **verbatim-only credit rule**, which she caught would transfer every idea of hers not transcribed exactly into Claude's column. This produced the better diagnosis: the shipped rule applies a *wording* test to an *origin* claim, which is why Claude ends up asking her to prove her work is hers — reported from a live instance in another project the same day. The lint has it backwards in both directions, flagging five legitimate origin claims and passing paraphrase presented under a quote frame. Claude was one turn from deleting those five credits on the lint's instruction.
- A **refusal to extend the measurement script**, judged against deriving the bands when her purpose was different and legitimate: *"I just want to make sure that the legacy work is measured properly to show the growth."*

**Her observation that Claude keeps fixing instances rather than asking whether a rule belongs there** became [fix-level-has-no-site]. The rule gate fires only once a rule is already on the table, so its best question — *could a hook do it instead* — is unreachable from the other direction. Two instances, both from this session, both caught by her. Applied immediately afterwards: two captures were resolved in-session rather than queued, and one proposed rule was refused for having a single instance.

**One proposed audit was withdrawn for lack of evidence.** Claude asserted that several refusals on the record might rest on a no-guarantee argument; searching found none — the instances checked were refused on cry-wolf or unapproved-send grounds. The principle was admitted on its single instance, with the item saying so.

**Two accumulations are recorded and unresolved.** Four cleared items now edit `skill-nonspecific-rules.md`; two edit `post_tool_use.py`; four propose clauses at one step in `plan.md`, down from five after one fold. Built as filed that is ten passes over three files. A consolidation was recommended and not taken up.

**A `CLAUDE.md` edit was blocked by the planning scope-lock**, working as designed, and became [design-judged-by-likelihood-not-guarantee].

**Noted, not filed:** the queue-growth hook counts the readiness marker line as part of the item above it, so moving the marker reported a spurious −8 words.

**Queue changes:** cleared region rebuilt from 2 items to 16; Unprocessed 64 → 54. `[law-prose-restyle]` re-kept and cleared with eight items folded in. Ten new items cleared. The readiness marker moved to the bottom of Processed. `[setup-outstanding-here]` placed last as the one `[freeform]` item, below the two `[user]` posts, since /next halts on it. `[run-the-written-shape-measurement]` left mid-region against the end-preferred default, because moving it past the `Runs alone` rename would separate it from the tool it runs by a whole run.

**Work processed:** kept — [written-shape-word-bands], [research-cited-not-restated], [design-judged-by-likelihood-not-guarantee], [spec-control-model-not-what-happens], [tool-build-implies-an-audit], [measurement-excludes-legacy-logs], [run-the-written-shape-measurement], [provenance-splits-origin-from-quote], [advisory-hooks-repeat-unchanged], [fix-level-has-no-site], [rename-docs-b-folder], [files-line-names-excluded-files], [law-prose-restyle], [setup-outstanding-here]. Deleted — the eight rationale-audit findings folded into [law-prose-restyle]; [length-work-measures-but-does-not-constrain] (superseded); [unquoted-user-credits] (superseded, rule was backwards); [research-index-trigger-misses-design] (folded); [no-specimens-decision-unrecorded] (resolved in-session).

**Routed to Captures:** none — the seven /rescan captures were all processed in this session rather than left.

Rule gate: not needed — no rule authored or amended in the method's own text; nothing under `docs-b/`, `CLAUDE.md`, `resources/self-authoring-rules.md` or `resources/rule-maintenance.md` changed. The gate ran at the keep-step for each rule-bearing item and its disposition was written onto that item; the one `CLAUDE.md` rule this session approved was blocked by the scope-lock and queued instead.

FAQ: not needed — nothing shipped, so nothing changed what a user does.

**Files touched:** `QUEUE.md`, `SPEC.md`, `resources/research/llm-length-instruction-following.md`.
