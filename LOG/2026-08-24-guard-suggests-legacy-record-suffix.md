# [HASH] — Kept and cleared: the overwrite guard offers kind-suffixed names

Processed 2026-08-24. The record-overwrite refusal in `pre_tool_use` suggested the retired numeric suffix hours after the kind-suffix rule shipped — drift by helpful message, at the exact moment a session picks a name. Kept on the answer to the item's own question: the guard cannot know the session's kind and does not need to — it offers both kind-suffixed candidates that are free, numeric only when both are taken, and points at the naming rule. Dropping the suggestion entirely was weighed and lost: a concrete free name is what makes the refusal actionable in one step. Hook suite gains matching cases. Gate: not needed — a hook message change, no method rule text touched.

**Work processed:** kept — [guard-suggests-legacy-record-suffix], cleared with build block.
