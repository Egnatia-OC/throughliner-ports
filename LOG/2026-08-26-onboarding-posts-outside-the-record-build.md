# [HASH] — Six FAQ entries authored from the recovered onboarding posts, into the shipped templates and the project's own copies

The FAQ half of the outbound-record gap, and the half that needed a build: an entry is authored into `plugin/throughliner/templates/`, which a planning session's scope-lock deliberately excludes because editing a template reaches every future consumer. The register half was already closed when Alex supplied all six posts verbatim earlier the same day.

Six entries, one per post — installing, the first `/setup`, `/plan`, `/next`, `/done` and starting fresh, and `/rescan` — each with its index line, written into `faq-template.md` and `faq-index-template.md` and then copied into `FAQ/faq.md` and `FAQ/index.md`. The source was the verbatim text in the recovery record rather than memory of the posts, which is what that record's own limit requires.

**Where the posts were loose, the entries say what the plugin actually does**, since an FAQ entry is read as current behaviour rather than as a snapshot of what was announced. Three places diverge deliberately, each one a correction the recovery record had already identified: the `/plan` entry states the real two-sections-plus-readiness-line shape rather than the posts' two-zones paraphrase, and names the two reasons work sits below the line; the `/next` entry names `[freeform]`, which the post omitted, so a reader meeting a halt is not surprised by it; and the `/rescan` entry carries the record-tail half the post left out, which is what makes it the one-word route for work done after a close.

Later in the same run the install entry was carried to the `#beta` string alongside README and INSTALL.md, rather than being left on the unpinned route an hour after it was written.

Files touched: `plugin/throughliner/templates/faq-template.md`, `plugin/throughliner/templates/faq-index-template.md`, `FAQ/faq.md`, `FAQ/index.md`
Routed to Captures: none
Rule gate: not needed — FAQ content is not rule text.

Depth: short. Ticked as done, confirmed.
