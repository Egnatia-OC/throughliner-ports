# [HASH] — Processed two waiting captures into top-of-Batches build batches: drop the Rezip zip rebuild, and scope git-safety checks to single command segments

This planning session processed the two waiting captures, both promoted to build batches.

[rezip-zip-vs-folder-coherence]: the committed local marketplace installs SI from the `plugin/si-plugin` folder, not the zip, so Rezip's zip rebuild no longer feeds local testing — it only risks leaving a test-suffixed zip that gets mis-archived at the next Push. Promoted a CLAUDE.md build batch to remove the rebuild from Rezip (keeping the version-suffix bump and `__pycache__` cleanup) and simplify the now-moot "Archive accuracy" note.

[git-safety-cross-segment-false-positive]: the filed capture blamed `rm -f` for a false `git push --force` denial, but verifying against `pre_tool_use.py` contradicted it — the regex requires `push`. Recovered the actual blocked command from the prior session's transcript and found the real cause: a cross-segment collision where `git` (from `git add`), `push` (inside the staged filename `rezip-push-cli-flow.md`), and `-f` (from `rm -f`) matched across the `&&`. Verified both tokens are required. Promoted a corrected build batch to scope the git-safety patterns to single command segments and anchor the force-check to the `git push` subcommand, and renamed the slug to reflect the true cause. The capture's wrong "rm -f" diagnosis is preserved here as the intuitive-but-rejected reading so it isn't re-proposed.

**Queue changes:**
- Added [git-safety-cross-segment-false-positive] (build) at the top of Batches.
- Added [rezip-zip-vs-folder-coherence] (build) directly below it.
- Readiness line unchanged — both new batches are cleared and sit above the existing marker.

**Captures routed:** Both promoted — [rezip-zip-vs-folder-coherence], and [git-safety-hook-rm-f-false-positive] (promoted as [git-safety-cross-segment-false-positive] with a corrected diagnosis). Unprocessed Captures now empty.
