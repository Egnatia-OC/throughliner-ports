# [HASH] — reader-test-workflow.js: removed the FAKE_REGISTRY fixture, dropped REGISTRY.md from the simulated session-start output, and updated the doc-routing test criteria to the three-doc model (SPEC/QUEUE/LOG); grep-confirmed no REGISTRY references remain — REGISTRY-retirement cleanup of the dev reader-test harness.

resources/reader-test-workflow.js is the dev-only multi-agent harness that grades plugin-doc comprehension against a simulated bookshelf-tracker project. It still modelled the retired four-doc world: a FAKE_REGISTRY component-list fixture, injected into the simulated project state, with "REGISTRY.md: found" in both simulated session-start outputs, REGISTRY.md in the fake project's CLAUDE.md doc list, REGISTRY.md named in the verifier's doc-routing audit criterion, and a "registry update" step in the /done simulation. Running it in that state would grade a fresh Claude's comprehension against a stale model — the opposite of the harness's purpose. Found during the [retire-registry] grep sweep (2026-06-18 goal session) and queued as cleanup that ships in the same push as the retirement, since it's host-only and not host-side. The harness is kept (a parked item plans to add scenarios to it), so the fix updates it rather than retiring it. Removed the fixture and every REGISTRY reference, repointed the doc-routing criterion to SPEC/QUEUE/LOG, and grep-confirmed (case-insensitive) that nothing matches "registry" anymore.

While in the file, found a second staleness outside this batch's REGISTRY scope: the fake-project CLAUDE.md still teaches "SPEC.md is read-only during builds" — the model [consumer-spec-model-sync] replaced with the spec-edit-batch model at b5c5337 in the real templates but missed in this dev fixture. Routed it to Captures rather than fixing inline, since it isn't a REGISTRY reference — same failure mode (grading against a stale rule), different rule.

**Files touched:**
- resources/reader-test-workflow.js: deleted the FAKE_REGISTRY const fixture; removed the REGISTRY.md bullet from the fake project's CLAUDE.md doc list; dropped "REGISTRY.md: found" from both simulated session-start outputs; removed the REGISTRY.md block from the projectState() builder; updated the verifier's DOC ROUTING criterion to (SPEC.md, QUEUE.md, LOG/); removed "registry update," from the /done simulation scenario.

**Routed to Captures:** the SPEC-read-only-during-builds staleness in the fixture's fake-project CLAUDE.md (a missed [consumer-spec-model-sync] sweep target).
