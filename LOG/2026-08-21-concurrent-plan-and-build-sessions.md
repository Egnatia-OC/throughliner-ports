# [HASH] — Concurrent plan-and-build sessions killed by the user: too hard to achieve, too confusing, and parallel projects already cover the want

The user deleted this item in her own words: *"I'm over it. it's too hard to achieve and makes things too difficult for users to understand. added steps for something that's supposed to be a convenience. if they want they can run parallel projects. it's fine."* Recorded fully because the item was her own raise, was substantial (the single-writer build view shipped partly in its service), and a later session will feel the same pull: the one-chat-at-a-time rule **stays in force**, SPEC's ideate-at-any-point principle is accepted as bounded by it, and the git question the item turned on (worktree versus shared tree with a single committer) is closed unanswered because the feature it served is not wanted. The design progress the item carried — the repeal shape, the git question stated precisely — lives in git history under this item's heading if it is ever revived.

**Queue changes:** [concurrent-plan-and-build-sessions] deleted from Unprocessed.
**Work processed:** deleted on the user's decision, reason recorded above.
