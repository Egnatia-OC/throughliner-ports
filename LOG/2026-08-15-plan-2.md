# e3f50a4 — Fourteen items processed, two same-day scope-lock regressions caught from consumer mail, and the ordering ladder cut from six rungs to three

This session opened on a queue with nothing cleared to run and closed with twenty items ready, but the number is the least interesting thing about it. Three separate threads ran through it, and two of them were the user correcting a design Claude had recommended.

**The scope-lock shipped this morning broke two things, and a consumer found both the same day.** `8e20122` replaced the planning session's ask with a denial, on the user's decision after weeks of re-argument. Two messages from Understudy, opened mid-session at her request, reported that `/setup` cannot complete a migration and that a `/next` close cannot write its own commit-message file. Both were verified here by reading `pre_tool_use.py` rather than taken on report, and both have the same cause: the gate keys on the absence of a build working file, so `/setup` — which never creates one — is classified as planning, and a build close that has just *deleted* its working file becomes planning at the exact moment it needs to write to the project root. `done.md` states the opposite in terms, naming the deletion as the reason the write is safe. The build that shipped the deny traced the hook, the tests and `plan.md`, and never reached the two docs that write outside the standing list. The fixes: a scratchpad marker declaring a `/setup` session, and the commit message moving to the scratchpad, which is the sender's own workaround adopted unchanged because it works in every session type rather than depending on how the hook classifies the session. A third finding of theirs was filed as a design question, and a reply was drafted, approved and delivered to their INBOX.

**The ordering ladder went from six rungs to three, and the user drove every cut.** She proposed line count as a proxy for design completeness. Checked against this session's own evidence, that proxy failed — of eight items taken in line-count order, the longest and fourth-longest were the two that could not be kept at all, because at the top of the distribution length tracks repeated enrichment rather than completeness. But the ordering worked anyway for the reason she gave second, and that reason is the one the rung now rests on: long items are what make the queue expensive to read, so clearing them shortens it fastest, and her comparison is the evidence — a wholesale tersification bought about ten percent and helped less than ordering by length did. She then removed three more rungs. Decay collapsed into file order, because Unprocessed is only ever appended to, so file order *is* filing order and oldest is what decay was reaching for. File order then died by her own observation that longest-first never exhausts, so nothing beneath it can ever be reached. Cheap-to-settle went on her judgment that recent work had absorbed it, confirmed by two findings: the files-named block is now a merge tool used at the keep step, and its stated criterion contradicted itself — `plan.md` calls it "checkable, not judged" while including *subject matter*, which cannot be checked without reading every item's prose. What she insisted on keeping is recorded verbatim, because it is the line between what stayed and what went: unblock-potential is reasoning, but of a type Claude excels at, running a dependency graph, as opposed to judging whether two items feel related.

**Two of her questions found defects Claude had not seen.** She asked twice whether a rung required reasoning across the queue. The first time the answer was no — the digest computes placement contradictions mechanically — but the answer surfaced that the block states no caveat, while it only matches a fixed set of known phrases, so a clean result means "none of the phrases it knows were found" rather than "no contradictions exist". The superseded-research flag already prints such a caveat; this one does not. The second time the answer was yes, and it is the self-contradiction above.

**A recommendation was overturned by one question, and it deserves recording because the failure is a named one.** For the refusals report, Claude recommended making it available on demand only. Her reply — how would I even know to demand it — is the siteless-check failure this project has recorded five times, walked into by the recommendation itself. The replacement surfaces refusals at `/plan`'s opening, bounded to those recorded since the last planning session, which was her tightening of a looser window Claude proposed. The full history stays behind the flag.

**One long-skipped item was settled by running the test it was waiting for.** [invented-rationale-compounds-past-the-shipped-rule] had skipped three times on a stated condition: observe whether rationale volume falls now the length rule exists, and design no fourth mechanism on an untested third. That rule shipped 2026-08-13, so August was split at its ship date. Capture length rose rather than fell — median 321 to 396 words, mean 368 to 461, across 152 items. Three caveats are recorded with it and none rescues the rule. The condition is therefore discharged, and what it discharges is the ban on a non-prose fix: two mechanical checks in the queue lint, one flagging a user-authorship credit with no quoted words attached, one reporting each edited item's word delta as a bare fact. Neither can identify invented reasoning, and the item says so — they make an unsupported credit visible and make growth visible, which is the only mechanism here with a record of working.

**The user corrected a vocabulary Claude took from the shipped rules.** Only items in Processed are work items; before that they are captures or unprocessed items. The shipped text disagrees in at least four places, including the sentence that defines a capture as "one work item appended to Unprocessed", so this is a change rather than a correction of drift. A grep put the scope at about 120 occurrences across 25 files, and she split it: the definitional and user-facing half ships now, the hook and script half becomes its own item, with the argument against the split recorded — the double meaning survives in the code, where nobody reads it.

**A held chain turned out to have been stopped rather than paced.** Four Discord posts sat below the line behind a wake-up capture. The queue recorded that a post went out on 2026-08-14; the user's correction was that none did, because it was never presented to her in `/next`. So holding the item made it invisible rather than delayed — the live instance of [held-items-invisible-during-normal-use], processed later in the same session. Re-reading the chain then found a third problem in text Claude had written minutes earlier: posts two through four block on each other, which implements order rather than one-a-day, so her stated rule has never been mechanically in force. All three corrections are written into the items.

**Queue changes:** Unprocessed went from 63 items to 46; Processed from 4 held and 0 cleared to 20 cleared and 3 held. The three `[user]` items sit at the end of the cleared region so a run reaches them last. Five cleared items had `SPEC.md` added to their file lists at this close, after the spec-sync gate found each would make a SPEC sentence wrong on shipping — the new skill against "four skills drive the workflow", the date field against "work sits below the readiness line for exactly one reason", the ladder rewrite against the rung-by-rung description, the INBOX change against the list of what `session_start` reports, and the skip-to-defer change against what skipping does. SPEC is accurate today, so nothing was edited; each moves when its behaviour does. One INBOX message was opened at the session's start and two more mid-session on the user's instruction, all three archived, and one reply delivered to Understudy.

**Work processed:** kept — [scope-lock-blocks-setup], [scope-lock-blocks-commit-msg-file], [cycle-map-contradicts-dont-cross], [not-before-date-field], [held-items-invisible-during-normal-use], [rescan-as-its-own-skill], [mover-cannot-add-a-new-item], [scripts-dont-report-consequences] (with [digest-lowercases-file-paths] merged in), [inbox-open-step-not-enforced], [audit-axis-is-parent-not-sibling] (split from [standing-audit-programme]), [invented-rationale-compounds-past-the-shipped-rule], [refusals-have-no-visible-record], [decide-items-escape-the-keep-check], [unattributed-mail-has-no-recovery-route], [identify-stuck-reply-senders] (split from it), [half-cap-assertion-has-no-derivation], [ladder-rungs-by-mechanical-proxy], [work-item-means-processed-only], [competition-comparison-article]. Deleted — [prior-plan-scope-lock-not-located], [announcement-cadence-clear-to-resume], [digest-lowercases-file-paths] (merged), [forward-advisory] (consumed), [next-start-order-handoff] (premise overtaken by the shipped rule that most queue order carries no weight). Not kept, with design progress written in — [standing-audit-programme], [setup-repo-privacy-and-publishing].

**Routed to Captures:** [spec-scaffold-describes-method-docs], [plan-does-not-build-keeps-being-relitigated], [gate-trigger-misses-the-audit-checklist], [scope-lock-denies-claude-md], [next-presents-items-setup-will-overtake], [process-now-offer-should-recommend], [work-item-term-in-hook-and-script-code], [two-column-fences-wrap-unreadably].

Rule gate: run — fourteen dispositions were written onto their items as each was processed, which is the gate's sited placement. It refused nothing outright this session, and two items record substantive shaping instead: [invented-rationale-compounds-past-the-shipped-rule] was admitted only after its stated waiting condition was tested and failed, and its fix was constrained to author no rule at all, on the ground that a fourth prose remedy is the one shape the evidence rules out; and [cycle-map-contradicts-dont-cross], [ladder-rungs-by-mechanical-proxy] and [half-cap-assertion-has-no-derivation] are all net subtractions from the corpus. Three dispositions record honestly that nothing is evicted and the addition is unoffset.

FAQ: not needed at this close — no session work changed what a user does. Six kept items carry FAQ entries in their own build lists, which is where those entries ship.
