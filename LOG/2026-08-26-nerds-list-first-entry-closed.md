# First test-rezips entry — posted, closed on the user's evidence

`[user]` item [nerds-list-first-entry], closed in the second 2026-08-26 /next
run. The user supplied a screenshot of the posted message rather than a report,
so the register line below is read off the text as it actually stands in the
channel.

## Posted text, as it appears

> **v1.21.0 — released 2026-08-26**
> Label: stable - [your caveats]
>
> This exact build is today's beta release. It's `1.20.0-test20` — the build I
> ran a full planning session and a build run on today — plus the two doc fixes
> that run turned up. Same code beta testers get from the install route (`#beta`
> points at this commit).
>
> https://github.com/FlintcraftTech/throughliner/releases/tag/v1.21.0

Posted at 15:20 in the test-rezips channel.

## Two observations, neither of them a fault in the item

**The wording is more accurate than the queued draft was.** The draft in the
queue said the entry would be "cut from [rezip name]", which reads as the release
*being* test20. It is not: it is test20 plus two doc fixes committed after that
rezip and never packaged before release. The posted text says exactly that, which
is the correction made when the draft was finalised.

**The label placeholder was left unfilled** — the line reads "Label: stable -
[your caveats]" verbatim. Recorded because the register line must state what the
post claims as it stands rather than what it was meant to claim
([sent-line-written-from-decisions-not-from-the-message], built earlier in this
same run). Flagged to the user as hers to edit or leave; nothing here depends on
it.

## Close

Register line written into `INBOX/sent.md`; item removed from Processed. This
item was not re-presented as a draft, because the doubt raised by
[walkthrough-represents-work-completed-outside-a-session] was put to the user as
a question about the item's state rather than answered by handing her a draft she
may already have posted.
