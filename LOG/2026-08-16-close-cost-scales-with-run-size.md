# [HASH] — A subtraction pass over `done.md`, and an honest measurement that it grew anyway

The headline finding this item was filed with — that a close's cost scales linearly with run size and cannot be reduced by anything the run does — was **accepted and closed at processing rather than built**. The close writes one entry per item because a built item's queue text is consumed when it builds, so the entry becomes the only surviving record. Every route to reducing that cost trades away part of the record. Recording that the linear cost is the price of the record is the honest outcome, and it is written down so a later session does not reopen it expecting a fix to exist.

Two candidates were refused at processing. **Shorter entries for short-depth items:** the short entries measured 120–200 words each, and each is the entire remaining trace of an item whose queue text no longer exists — already lean, and cutting it buys tokens with retrievability. **Index-line versus entry duplication:** `CLAUDE.md` repealed the index-line length cap and records why no replacement figure is defensible, and nothing in the measurement was new evidence against that.

What was kept is the subtraction pass on `done.md`, which is read in full at every close. Three cuts, all applying the delete-and-reread test: a paragraph restating the bulk-approval inversion, whose rule lives in the always-loaded file; an evidence sentence attached to the scrub-checklist pointer; and the rationale paragraph explaining why the post-commit tail loses its commits rather than its work. Eighteen lines.

**One cut the technique called for was refused, and that is the pass's most useful finding.** The defeated-alternative paragraph in the tail section — what was rejected and why it is not reopened — fails the delete-and-reread test cleanly. It was kept anyway, because the throughline rule holds that an intuitive-but-rejected alternative most needs preserving: without the why-it-lost recorded, a later session re-proposes it and relitigates a settled decision. **An auditor applying the delete test mechanically would cut exactly this, and would be wrong.**

**The measurement, stated rather than softened: `done.md` grew this run.** 797 lines at processing, 842 at the close. The subtraction removed 18; [post-close-tail-state] and [log-records-the-run-not-the-chat] added more than that to the same file in the same run. The item already warned this pass is a fixed-cost saving and must not be described as fixing the linear scaling — it must also not be described as having shrunk the file. It helped, and the file still got bigger.

**Built last rather than in queue position**, honouring the ordering preference carried in the item's own prose: the rationale audit runs first so its findings show what the per-paragraph technique actually yields before it is pointed at a second file.

Depth: full — the pass met a paragraph the technique says to cut and the throughline rule says to keep, and stopped rather than cutting it.

Rule gate: run — no rule authored, and the disposition is an eviction pass by definition. What falls is fetched text; the always-loaded count is untouched. Two proposed changes were refused at processing, one because it would reopen a decision `CLAUDE.md` already settled.

FAQ: not needed — text removed from a procedure doc.

**Files touched:** `plugin/throughliner/docs-b/done.md`.

**Routed to Captures:** none from this item.
