# 35261b5 — Two held items dispositioned and the item that recorded them deleted; the general fix it suspected was refused on its own evidence

Yesterday's session found five held items whose blockers had shipped, lifted three, and deliberately held two back on grounds that were real but were not the revisit's test. The reasoning existed only in that chat, so it filed [two-held-items-await-a-disposition] to carry it forward. This session spent it.

**[discord-post-context-adjacency] — the blocker was repointed, not merely resolved.** Its `Blocked by:` named [decay-rung-unreachable-in-practice], which has shipped and been confirmed. What actually holds the post is the one-a-day pacing chain, with [discord-post-cycle-awareness] at its head and unposted. So the field named a fact that had resolved instead of the fact doing the holding, and every below-the-line revisit read the item as ready to lift while being wrong. Repointed at the item that genuinely holds it, so the post now lifts by itself when the one ahead of it goes out and no session has to remember today's reasoning. The queue lint dropped from thirteen standing flags to twelve on that edit alone — the unresolvable reference had been one of them.

**[concurrent-plan-and-build-sessions] — returned to Unprocessed, and its blocker shipping is precisely why it could not stay.** [split-the-cleared-region-for-concurrent-sessions] landed on the 20th, so nothing held it. But the region below the readiness line is for work that is designed and buildable and held by a named item or a date, and this item's own text says the opposite — that the git question *"is the whole of this item"* and its file list is provisional. Lifting it would have put an item failing the keep check's second limb into the region /next builds from. Undesigned work belongs in Unprocessed; that is the queue's four-state model applied rather than an exception invented for one item. Its design progress travels with it, and what would settle it is now written down with each half attributed — the worktree-versus-shared-tree question is Claude's to establish by reading and testing, and whether the resulting arrangement is one Alex would work in is hers.

**The general question it raised was answered no, and today is the evidence rather than an argument.** The item asked whether the revisit needs somewhere to record a deliberate decision not to lift. Neither case needed one. The first was a wrong blocker reference, which the revisit already has a branch for — *"blocker absent from the queue and absent from LOG — a wrong reference → a fault; surface it and fix it this session"*. The second was an item in the wrong section, which the four-state model already answers. Both were covered by existing rules that simply had not been run to their conclusion, and a "deliberately held" marker would have been a fifth queue state — the invented-state failure the always-loaded rules name as recurring and which Alex has caught every previous time.

So the item's own work was done the moment both dispositions were written into the items themselves, which is where the next session will actually look. Deleted.

**What this does not claim.** Nothing here makes a deliberate not-lift decision visible in general; it establishes that the two instances available did not need it. A third case with different grounds could still find the gap real.

**Queue changes:** [discord-post-context-adjacency] `Blocked by:` repointed from [decay-rung-unreachable-in-practice] to [discord-post-cycle-awareness], with a paragraph recording the repoint; [concurrent-plan-and-build-sessions] moved Processed → Unprocessed with its `Blocked by:` line dropped and a settlement paragraph added; [two-held-items-await-a-disposition] deleted.
**Work processed:** deleted — [two-held-items-await-a-disposition].

Rule gate: not needed — no rule authored or amended in the method's own text. Two dispositions were written onto existing queue items and one general rule proposal was refused, none of which touches `docs-b/`, `CLAUDE.md`, `self-authoring-rules.md` or `rule-maintenance.md`.

FAQ: not needed because nothing a user does changes — this is queue bookkeeping inside one project.
