# dc52025 — chat-level record for the 2026-08-17 planning session

This is the chat's own record, separate from the per-decision entries, under the convention settled in this same session for a close that writes several entries.

**Corrections the user gave.** That the ban on procedure vocabulary is really a ban on unexplained passing use, not on the terms themselves. That INBOX is gitignored, which defeated Claude's argument that the address-book rule was over-broad. That a chip reply cannot be detected and a per-session notice would therefore fire at everyone — in her words, "seems a bit much" — and that her own answer is to say "as you recommend" when deferring. That "the close" is residual language from a retired step. That reading only the items a session intends to process assumes a list she will never give, and that blockers and fold-in points can be anywhere, so every item bears on any planning session. That a capture often does not know what work it will produce, which defeated a proposed dependency field. That SPEC's "the design is never lowered to them" widens what she expressed at the time. And that Claude's framing of a fifteen-file ripple as "much larger than the problem that raised it" was arguing against the work rather than describing it — a breach of a rule shipped the day before.

**Errors made and recorded.** The payload-cap item was processed before the mail item despite her recorded instruction to the contrary. A CLAUDE.md rule was drafted from a generalisation of one of her sentences and withdrawn. A dependency was written into one item's prose rather than both, surviving only by luck.

**Decisions reached in conversation and not filed.** That no rule is added about caveats arguing against work: the rule already exists, was broken anyway, and this project has found repeatedly that restating a rule does not make it fire. That the rezip's restart-verification awkwardness has no buildable fix, since Claude stops running when the app quits and the returning session acts only when she speaks.

**Work done outside the queue.** A reply was drafted, approved verbatim and sent into a consumer project's mailbox, after confirming its mailbox exists and its gitignore covers it. Every mention of a correspondent still in QUEUE.md was reworded generically. SPEC.md gained a positioning paragraph.

**Ordering.** The `Runs alone` rename was moved from the head of the cleared region to the end of the buildable work, so the next run clears this session's items rather than building one and stopping. The cost — that the rename waits another run, against an existing capture about starvation — was named rather than left silent.

FAQ: not needed because nothing shipped — every rule settled here is queued as a build, and the one edit that landed (SPEC's positioning paragraph) changes nothing a user does.

Rule gate: run — sixteen dispositions written at the keep-step, each on the item it belongs to and transcribed into that item's entry here. Three proposals refused outright: a CLAUDE.md rule generalised from one of the user's sentences, a `Process after:` field for captures, and a permission change to the planning scope-lock.

**Routed to Captures:** [send-record-lacks-destination-and-intent], [inbox-has-no-outgoing-record], [cross-project-mail-versus-direct-captures], [queue-privacy-default], [spec-overstates-the-reading-position], [split-action-defeats-the-bands-in-aggregate].

**One finding this close made about itself.** Applying the split-per-decision action produced twenty-four in-band entries whose total far exceeds the single over-ceiling entry they replaced, and nothing measures a close. The user found it from the cost as she paid it, and it is filed rather than fixed, because both routes out of it are expensive.
