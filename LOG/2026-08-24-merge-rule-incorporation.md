# a2ce359 — Kept and cleared: the merge rule states incorporation in its operative text

Processed 2026-08-24, from the user's correction minutes earlier (see the [multi-person-attribution-gap] entry): the shipped merge rule says "rewrite the host" and was satisfied by a rewrite that pointed instead of carrying. The amendment writes the requirement into the instruction — the host carries the folded item's facts in its own text; a pointer is extra information, never the incorporation — worded to survive the delete-and-read maintenance test, with the why ("Claude can't reason across two files") kept here in the record rather than in the rule. Gate: run — amendment in place, admitted on the recorded misapplication.

**Work processed:** kept — [merge-rule-incorporation], cleared with build block.
