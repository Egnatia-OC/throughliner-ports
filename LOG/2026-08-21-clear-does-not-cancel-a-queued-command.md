# [HASH] — The clear-versus-queued-command finding routed to Claude Code, kept as the item that files the issue

Settled by the three-way routing rule: the behaviour is Claude Code's, and the plausible local fix — /next announcing a cold start — is refused on the item's own caveat that an empty context and a forgotten one are indistinguishable. The work is search-first, draft, exact-text approval, post by `gh` only on an explicit yes, sent record written in the same turn. Build block on the item.

**Queue changes:** [clear-does-not-cancel-a-queued-command] moved Unprocessed → Processed, cleared.
**Work processed:** kept.
