# [HASH] — Plan: subagent cost-guard (ask-gate hook + hardened rule) and a routine 4.8-compliance audit; 2 captures promoted, 1 parked

Two user-raised threads drove this session, both from a real cost incident: Claude silently escalated a plain "research this" request into Claude Code's built-in deep-research skill — a five-subagent fan-out — exhausting the user's Max usage for the first time in weeks, without finishing.

Thread one — the subagent guard. The user first suspected missing rationale was why the no-subagent rule wasn't followed. Reframed: her global rule already carries its rationale, so in her own sessions the gap was a behavioural rule not holding, not a missing why. The plugin's own plugin-behaviour.md "Tool use" rule, though, is the one un-hardened rule in a compliance-hardened doc, and a recurrence (v1.6.0 added it after the same miss). The user was wary of a mechanical block as railroading users. Reframed: a hook can force an "ask" (permission prompt), not a "deny" — preserving choice while guaranteeing the ask, exactly the "always ask first" she wanted. Feasibility confirmed by web search and filed (resources/research/subagent-ask-gate-hook.md): PreToolUse supports permissionDecision "ask", matchers match the Task tool. Decided: the hybrid — harden the rule (steering) and add the ask-gate (guarantee). Queued as spec-edit [subagent-ask-gate-spec] (the hook changes SPEC's pre_tool_use description) ordered before build [subagent-ask-gate], both above the cruise-control marker so /next reaches the urgent fix first.

Thread two — the 4.8-compliance audit. The reusable checklist she wanted already largely exists (resources/4-8-authoring-heuristic.md), so no new checklist content was built — the real gap is that it's an authoring-time per-text check that never re-examines old rules (why the tool-use rule slipped). Build [method-compliance-checklist] authors a durable routine-audit checklist that references the 7 heuristic points (no duplication) and adds tag-placement and narration-drift lenses; the existing audit [full-tag-placement-recheck] was widened to a third 4.8 lens and repointed at the checklist. Alternative weighed: folding everything into the single heuristic doc — rejected because that doc's value is being short enough to apply in one pass. At the user's ask, the routine checklist also covers tag-checking.

Captures: promoted [unpark-scan-missed-shipped-trigger] to [unpark-scan-mixed-trigger] (the unpark scan now checks every slug in a mixed slug+behavioural Blocked-by; a lint backstop was considered and deferred to avoid the re-flag noise [lint-reflags-citations-within-session] addresses); promoted that third capture to [lint-citation-diff-scope] (diff-scope the citation advisory so it fires once when written, not on every edit — chosen over a session-state file). Parked [design-session-queue-home] linked to the queue item-type-taxonomy cluster ([user-execution-batch-shape], [guided-exploration-batch-type]) — three items, one root gap, one unified decision. Re-parked [dont-assume-user-environment]: its narrow-fix blocker shipped and comes off, leaving a design pass best done once the comms rules settle. Filed wind-down capture [slipped-rule-escalation-heuristic] — when a slipped rule earns a mechanism vs just sharper wording, decided by failure cost (the session's two worked examples).

Cruise-control and multi-spec ([spec-sot-rethink]) both stay parked for dedicated fresh sessions, by the user's choice. No deferred-test lines confirmed this session — host liveness is unresolved without the content stamp, so the section is left for a future /plan to roll.

**Queue changes:**
- Added (above cruise-control marker): [subagent-ask-gate-spec] (spec-edit), [subagent-ask-gate] (build).
- Added (below marker): [method-compliance-checklist] (build), [unpark-scan-mixed-trigger] (build), [lint-citation-diff-scope] (build).
- Modified: [full-tag-placement-recheck] — widened to three lenses (added 4.8 authoring-compliance), criteria repointed to the new checklist, Depends on [method-compliance-checklist].
- Parked: [design-session-queue-home] (new); [dont-assume-user-environment] re-parked with sharpened blocker.

**Captures routed:** promoted 2; parked 1 ([design-session-queue-home]); filed 1 new for later ([slipped-rule-escalation-heuristic]). Also filed research/subagent-ask-gate-hook.md.
