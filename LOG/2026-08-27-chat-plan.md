# [HASH] — chat-level record for the 2026-08-27 planning session

A long planning session on the first rezip of the 1.21.0 line. Every capture was processed — 21 entries kept into the cleared region (each with its own record), two captures deleted outright, several more deleted as merged into the items that resolve them — and the ready region was rebuilt to 29 items with the five `[user]` items batched at the end. The session's biggest decisions were the user's: shared vocabulary replaces standing plain-English names; builds return to reading the queue whole, retiring the generated view and build blocks; warn-don't-enforce for do-it-now requests; and "keep" retired as the disposition term.

**Also in this chat:**

- **Rezip to 1.21.0-test1, mid-session.** The user asked for the previous day's fixes to go live; the ritual ran (suites all passed via the three named suites, cache pruned, CLI update by full path), the content stamps matched (`2299ae4cf5de` source and cache), the user fully restarted, and the session-start payload re-fired in-chat carrying the new build — liveness proven. /plan was re-run afterwards on the user's instinct, correct, that the session was still following the old procedure text.
- **The date error.** After the restart Claude assumed a new day, mis-dated a capture 2026-08-27 on the 26th, and nearly offered a post a day early; the user caught it, and the calendar then genuinely rolled over mid-session. This session ran across 2026-08-26 and 2026-08-27. The fix is queued as [session-date-anchor].
- **Advisory from last session honoured:** [setup-md-tag-free-declaration-contradicted] was surfaced first and eventually settled (option 1, declaration repealed); the spent advisory was deleted at the opening.
- **Dropped at the opening:** [expedite-release-item-stale-in-ready-region], its premise gone once the release-pick item was closed the previous day.
- **The port prompt.** A reusable paste-prompt was drafted and handed to the user for a tester porting the plugin into their own harness — register line in `INBOX/sent.md` dated 2026-08-27. Text as handed over:

> You're helping me port the Throughliner plugin into my own harness — my own environment, not the standard Claude Code desktop-app install. Please read the install guide at https://github.com/FlintcraftTech/throughliner/blob/main/INSTALL.md in full first: it explains what the plugin is, how a normal install behaves, and what a working install looks like. Confirm you've read it by opening with its verification line.
>
> Then adapt rather than follow it literally — for this port:
>
> 1. SOURCE. I'm not installing from the marketplace or the beta branch. I'm using the most current test rezip: the newest entry in the Throughliner Discord's test-rezips channel. Use exactly the build that entry names, obtained however the entry provides it (its attached file, or the repository commit it points at) — read the entry's label and caveats first, and don't substitute the beta branch or the latest release.
> 2. WHAT TO PORT. The plugin is the `throughliner/` package: a plugin manifest (.claude-plugin/), four hooks (session_start, pre_tool_use, stop, post_tool_use — Python 3, standard library only), five skills (setup, plan, next, rescan, done), procedure docs under docs/, and templates. Map each piece onto my harness's equivalents: wherever my environment fires session-start, pre-tool, post-tool and stop events, wire the matching hook there; expose the five skills however my harness surfaces commands.
> 3. KNOWN PORTABILITY POINTS. The hooks expect UTF-8 explicitly (don't rely on the platform default), run standalone (they can't import shared modules), and emit JSON in Claude Code's hook output shape — check what my harness expects and adapt the envelope, not the logic.
> 4. SMOKE TEST. Adapt the guide's smoke test: in a fresh empty folder, the setup command should be reachable, and starting a session should produce the plugin's session-start report. If the harness can't surface one of those, tell me plainly which half is unverified rather than calling it done.
> 5. HONESTY. This is a raw development build, not a soaked release: its label describes only what was seen at posting, and it can misbehave in a project's files. Keep my work in git before pointing the port at anything real. If something in the plugin itself looks broken, I'll report it as a GitHub issue on FlintcraftTech/throughliner rather than patching around it silently.
>
> Work one step at a time and wait for me between steps.

- **Vocabulary corrections through the session:** the user had to ask what "the lean", "arm", "branch", "worked example" and "disposition" meant — instances feeding the shared-vocabulary and jargon items — and directed that checkpoints carry the remaining-to-process count, queued as [checkpoint-carries-remaining-count] and honoured for the rest of the session.
- **Rule-gate refusals this session** (the dispositions listing at the opening showed none in the prior window): refused during processing were a per-step executable-or-checkable constraint on instructions (duplicates the existing check), the middle shape keeping the view with rationale alongside (keeps the bloat), softening the completion-ask bar generally, and read-usage-first on every mover run.
- **Gemini/BACKLOG.md question answered:** the old name survives only in history and the frozen archive; the README fix is queued.

Advisory: not needed — every capture is processed, the ready region is rebuilt and ordered, and a /next simply runs it top-down from [setup-case-d-untagged]; nothing needs orienting beyond the queue itself.
Rule gate: not needed — this commit touches QUEUE.md, SPEC.md and LOG/ only; every rule decision made this session was gate-run at its item's processing, with the disposition recorded on the item.
**Routed to Captures:** none beyond the processed items above — everything filed this session was processed in-session.
