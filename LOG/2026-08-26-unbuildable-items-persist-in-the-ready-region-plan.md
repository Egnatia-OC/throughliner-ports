# [HASH] — Cause established by running the digest on the live queue: cleared blockless items are invisible to every planning-time check; the digest gains the class

Kept on Claude's recommendation and your agreement, with the cause established at processing rather than assumed — the item explicitly required it. The digest was run read-only against Taskflowapp's queue: zero placement contradictions with 26 items cleared, and the file counts 40 items to 24 build blocks. So the rot was cleared items with no build block, and the only detections of that state — the advisory lint (fires on queue edits) and the view generator's halt (fires when a run starts) — never fire at a planning opening. The fix: queue_digest.py's placement contradictions gain the class "a cleared build or [audit] item with no build block", one printed line per item; `[user]` items are exempt by design. SPEC's contradiction-class sentence written at the keep.

**Queue changes:** kept and cleared, in the build set.
**Work processed:** kept — [unbuildable-items-persist-in-the-ready-region].
Rule gate: not needed — a mechanical check added to a shipped script; no rule authored or amended.
