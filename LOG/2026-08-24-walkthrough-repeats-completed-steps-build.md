# 3ed3db1 — Built: walk-throughs read the record before acting — LOG checked under the slug, and the view names the records

From the 2026-08-23 collision where a walk-through re-drafted an article already delivered the day before. Two light parts, per the design you agreed (a progress artifact was refused — it would store position, which this method refuses): the lifecycle bullet gains the record check (list LOG/ under the item's slug, say what the record shows done, resume at the first step that is not), and `generate_build_view.py` emits a `Records on file:` line for a [user] item with records — slug-resolution duplicated from the digest, kind and numeric suffixes honoured. next.md's walk-through lifecycle opening cross-references both. Exercised both ways against this project's live queue: three [user] items showed their records, the discord-post item showed none. This same session then used the check for real — the comparison-article and cycles walk-throughs both resumed from their records.

Rule gate: run — amendment to the same walk-whenever-reached lifecycle bullet [walk-user-line-at-filing] amends (both ride one parent, built in one run); no freestanding rule, nothing evicted. Admitted on the recorded 2026-08-23 instance.

Files touched: plugin/throughliner/docs/skill-nonspecific-rules.md; plugin/throughliner/scripts/generate_build_view.py; plugin/throughliner/docs/next.md; plugin/throughliner/templates/faq-template.md, faq-index-template.md; FAQ/faq.md, FAQ/index.md (re-copied)
Routed to Captures: none
Done, confirmed: view line exercised in both directions against the live queue.
