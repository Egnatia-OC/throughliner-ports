# [HASH] — /plan: processed the two merge-related captures — investigated the installed build (redesign is code-complete in the `-x` fork but never dogfooded; installed host is the old main-line 1.15.0-test1), so dropped [installed-build-vs-redesign-state] as answered, parked [execute-merge-to-main] behind a dogfooding trigger (not the resolved state check), and filed [start-fork-dogfooding] as the concrete desktop kickoff; repaired the git worktree pointers after the project moved C: → G:.

Session ran in remote-control mode. Processed the two unprocessed captures about the `-x` → main merge. The investigation capture [installed-build-vs-redesign-state] resolved cleanly: reading the manifests and git history showed the redesign's middle (docs, hooks, /next, /done, templates, red-flags) is code-complete in this fork's source, but nothing built from the fork has ever been installed — the host running this session is the pre-redesign main-line build (`flintcraft/sovereign-implementer/1.15.0-test1`), not the `-x` fork (`0.1.0`). So the redesign is built-but-unproven: the one outstanding step toward relying on it is the first rezip+install of the fork. Dropped the investigation capture as answered.

[execute-merge-to-main] couldn't promote as its capture assumed: its precondition was "middle finished *and dogfooded*," and dogfooding hasn't started — so merging now is exactly what the merge plan forbids. Parked it behind a behavioural dogfooding trigger, with the resolved state check recorded in the Blocked-by prose. Filed [start-fork-dogfooding] as the concrete kickoff for Alex's next desktop session (rezip the fork, install it *alongside* the untouched main SI so the legal project keeps working, then run real sessions on it).

Mid-session the project folder vanished from C: — Alex was moving it to G: in a parallel reorg. After the move, git broke because the worktree's `.git` pointer and the main repo's back-pointer both still held old C: paths; repointed both to G:, and git resolves again on `queue-redesign`.

**Queue changes:**
- Parked [execute-merge-to-main] with a dogfooding Blocked-by trigger.
- Dropped [installed-build-vs-redesign-state] (investigation answered).
- Filed [start-fork-dogfooding] (unprocessed capture — desktop rezip+install kickoff).

**Captures routed:** dropped [installed-build-vs-redesign-state]; parked [execute-merge-to-main]; filed [start-fork-dogfooding].
