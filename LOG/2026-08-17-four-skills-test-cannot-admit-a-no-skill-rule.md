# [HASH] — the always-loaded admission test gains a limb rather than being restated

The always-loaded file admits a rule only if it fires in all four skills, but the file governs every reply including conversation with no skill running. The test and the file disagreed about what the file is for, and the test is the one that gets applied.

The item named one real risk: that restating the test would weaken the admission control it exists to provide. It resolves in the opposite direction. "Fires whatever is running" would be vaguer than the enumeration it replaced and easier to talk oneself into, so the enumeration stays and the missing case is appended — a rule earns the file if it fires in all four skills **and** in conversation with no skill running. That is a fifth condition, so admission is tightened rather than loosened.

The other two questions fall out of it. A no-skill rule needs no separate home, because the always-loaded file already is that home — it is the only thing loaded when nothing is running. And the filename stays: "skill-nonspecific" already means not tied to a skill.

Two sentences change: the file's opening test, and CLAUDE.md's distribution clause, which states the same test and must not disagree with it.

Rule gate: run — admitted as a limb on the existing admission test; it constrains authoring rather than conduct, so no slot is spent. Failure evidence is one recorded instance, where an item hit the gap and worked around it by siting its clause in a doc that happened to be read at the right moment.

**Queue changes:** [four-skills-test-cannot-admit-a-no-skill-rule] kept into Processed, cleared to run.
**Work processed:** kept — [four-skills-test-cannot-admit-a-no-skill-rule].
