# [HASH] — Law-prose restyle across the thirteen remaining docs: prohibitions reworded into the action, the three protected kinds left standing

The remainder of the restyle admitted at [law-prose-pass-missed-mid-sentence-prohibitions], run in the same sitting as [cut-length-remaining-docs] so one read of each file served both lenses. The pass the parent item taught held across the corpus: most occurrences are "X, never Y" where X already states the action, so the tail comes straight out — restyling and shortening in one move — while the few prohibitions carrying the only content became the positive rule they imply ("Do not open QUEUE.md" → "Leave QUEUE.md closed"; "never auto-send" → "every send waits for approval"; "Routing never re-opens a fate the user has already decided" → "A fate the user has already decided stays closed through routing").

Roughly fifty occurrences were restyled across skill-nonspecific-rules.md (~15), next.md (~20), setup.md (~10), done.md (9), done-plan.md, next-build.md, done-build.md, feedback-and-inbox.md, migrate-checklist.md, rescan.md and recovery.md (a handful each). next-audit.md needed nothing. What remains in each file was checked by grep after the pass and is the three protected kinds only: typed-block `write:`/`never:` contrast pairs, honest-limit statements ("never a guarantee that every risk present has been found"), and prose describing what a mechanism does not cover ("the harness never merges a session worktree"). CLAUDE.md was untouched by this lens — the item's own file list excludes it.

One consequential fix beyond wording: skill-nonspecific-rules.md's inline-offer block cross-referenced "that report's never-a-re-paste default" after the referenced rule was reworded, so the cross-reference was updated to name the reserved re-paste case directly — the repeal-trace limb applied at authoring time.

**Files touched:** the thirteen docs under `plugin/throughliner/docs/` (done-audit.md has no occurrences and needed no pass).
**Routed to Captures:** none from this lens.

Rule gate: run — transcribed from the item: continuation of the restyle admitted at [law-prose-pass-missed-mid-sentence-prohibitions]. Occurrences reworded into the action the rule requires, the three protected kinds left alone, no new rule authored and nothing evicted.
FAQ: not needed because the restyle rewords Claude-facing rule text behind unchanged behaviour — nothing a user does changes.
