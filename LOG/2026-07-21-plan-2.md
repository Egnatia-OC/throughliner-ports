# 963063c — Resolved the two-section reconciliation and settled "work item" as the canonical unit name

This /plan session processed the top of the shelving/vocabulary cluster. It closed the last open thread of the two-section reconciliation (Half B) and settled a long-standing terminology inconsistency, then cleared a LOG-index question that turned out to need no change.

**Half B (the fold disposition):** decided *not* to add a named third route to /plan's keep/delete step — folding stays a judgment call. Instead a short guidance clause will be added under plan.md's delete sub-step, saying that when a capture's content belongs in SPEC, LOG, or another work item, /plan relocates it first then removes the standalone line. A named "fold" disposition was weighed and rejected (anti-proliferation intent; it was drafted and reverted once before); the risk it guards against — dropping content routed through plain "delete" — is closed by the guidance sentence without new machinery. Queued as `[fold-as-relocate-guidance]`.

**Terminology:** settled **"work item"** as the canonical name for a unit of work, over keeping "work line." "Line" names only the heading, not the unit, and pressures over-atomising; "item" doesn't. Supporting rules: "build" is a flavor only, "batch" is retired, and "line" is reserved for literal file lines (the cleared-to-run marker etc.). Queued as a doc-sweep build, `[work-item-terminology]`.

**LOG-index unit (deleted):** the question "what searchable unit replaces the batch" resolves to *the work item is already correct* — done.md names each LOG file by slug, so LOG entry = work item (several per run) while commit = the run (one summary commit). No doc gap, so no build.

**Queue changes:**
- Kept `[fold-as-relocate-guidance]` and `[work-item-terminology]` into Processed, above the cleared-to-run line.
- Deleted `[log-index-unit-two-section]` (resolved, no change needed).
- Removed the spent `[advisory-2026-07-21]` from the top of Unprocessed.

**Work processed:** kept `[fold-as-relocate-guidance]`, kept `[work-item-terminology]`; deleted `[log-index-unit-two-section]`.

**Routed to Captures:** none.
