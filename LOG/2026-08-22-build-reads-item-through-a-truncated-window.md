# d7ea8a0 — The close's per-item fetch specified as a whole-block read, and an ask that re-opens a disposition names it

Kept as the general half — the disposition half shipped the day it was filed, when [build-view-strips-the-gate-disposition] put gate and FAQ lines into the view. The build specifies done-build.md's per-item lookup as heading-to-heading from the pre-run QUEUE.md, never a hand-sized window, and adds the close-site clause of the prior-decisions rule: where a fetched item's own text already dispositions a question, transcribe it, and an ask that deliberately re-opens one names the recorded decision — the user answered a well-formed question with no way of knowing planning had refused it. The item's open question is answered no: the paging rule already covers any unit the reasoning depends on; this was the rule not firing, and the fix makes the fetch mechanical so the rule stops being load-bearing there. Full disposition and build block on the item.

**Queue changes:** kept into Processed, cleared to run.
**Work processed:** kept — [build-reads-item-through-a-truncated-window].
