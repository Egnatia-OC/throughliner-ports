# [HASH] — The `[user]` capability check merged from three passages into one, keeping both anecdotes

The audit called this the densest redundancy in the corpus: within about a hundred lines of `plan.md`'s keep sub-step, three passages restated the same material — the matched pair copied from the always-loaded rules, then the thorough check with its "what would answer this?" reframe, then the thorough check and reframe again with a second anecdote and a citation back to the always-loaded doc.

**The two heavy passages were closer than the audit realised.** Both opened with the same reframe instruction, and both told the *same* anecdote — the honest, thorough search for where a setting was stored, answered hours later by one command that had nothing to do with settings files. That story sat in one document twice, about a hundred lines apart, with unrelated material between.

**Both anecdotes stay, which departs from the audit's recommendation.** It proposed keeping the middle passage and folding in a single sentence of the third. But the two stories teach different failures: reasoning from what a task *sounds* like — "create a GitHub repo" sounded browser-shaped and went to the user when `gh` would have done it in seconds — versus searching diligently for the wrong question. Dropping either loses a distinct lesson. The duplicated one is now told once.

**The merged passage sits at the first passage's position**, where the `[user]` tag is actually drafted, so the check runs at the moment the decision is made rather than after it. It carries the reframe, the name-the-tool test, the trying-is-allowed licence with its reason (the user is in the room, which is what makes this the heavy site), both anecdotes, the answer that where no tool plausibly exists that is itself the answer, the don't-enumerate-everything bound with its rejected inventory sweep, and the contrast with /next's lighter check.

**The first passage's over-tag restatement went**, since the merged check states it operatively; its don't-under-file half and the walkthrough-drafting instruction stayed where they were. The third passage was deleted outright, and the walkthrough-observation paragraph that followed it was kept and reattached.

**No other doc states the thorough check**, so nothing needed keeping in step.

**Files touched:** `plugin/throughliner/docs-b/plan.md`
**Routed to Captures:** none

Rule gate: not needed — no rule authored or amended. Three statements of one existing rule become one; the canonical statement in the always-loaded rules is untouched.
