# [HASH] — The seven flavour-specific close blocks moved out of done.md into the per-flavour close docs

The user asked (2026-08-21) why a planning close ran to ~6,000 tokens; the measurement (`resources/research/why-the-close-is-heavy.md`) showed that close reading 10,297 words of procedure to write 3,603 of record, and named done.md's flavour-specific material as the unqueued lever. This build is the distribution pass the keep settled: the seven blocks its table lists moved into the sub-doc for their flavour, each placed where its entry-writing or judgment step reads — verify-completion (with the memory reconcile) and the routing-sweep record and the build body fields and the red-flag carry-plus-backstop into done-build.md; the audit variants of each into done-audit.md, with approval-outcomes now carried in full there; the cleared-flag recording block into done-plan.md. The entry template stays whole in done.md, per the divergent-copies refusal, with only the plan/setup fields remaining in its per-flavour table.

One judgment the item left open: the isolated-session branch's flavour. It went to done-plan.md, because the always-loaded rules already carry the same isolated-close instruction for every session shape — so no flavour loses the warning, and the branch sits with the close doc that also handles the no-build shapes cloud sessions mostly run. This build also performs the recorded reversal of the one sibling-axis dedup (the 2026-08-15 hoist of the routing-record block), per the user's axis instruction.

Nothing was deleted: the diff shows moves, and a grep confirms no dangling references to the removed done.md sections. A planning close's read no longer includes any build/audit-only block. Statement counts: done.md fell and the three sub-docs rose by the moved material; the board's per-file counts carry the figures.

Tick: done, confirmed — grep for dangling references clean; every moved block findable in its sub-doc.

**Files touched:** plugin/throughliner/docs/{done,done-build,done-audit,done-plan}.md
**Routed to Captures:** none
Rule gate: run — a distribution pass: rules move between files and none is authored, amended in substance, or evicted; the gate's distribution limb is the whole of what this exercises, and the acceptance test is that nothing is lost.
FAQ: not needed because close procedure moved between Claude-facing files; nothing a user does changes.
