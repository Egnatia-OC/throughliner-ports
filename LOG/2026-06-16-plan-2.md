# d39e670 — /plan: cleared the 17-capture backlog into a shaped build queue + queued two Taskflow E2E audits at the top behind a plan-marker

A backlog-clearing planning session. The Captures pile (17 items) had accumulated across several goal sessions; this session turned it into ordered build/spec-edit/audit work, resolved the long-standing /goal-vs-cruise-control fork, and set up E2E auditing of the first real Taskflow /setup and spec-edit runs. The /goal fork resolved as "A on-ramp to B": formalize the goal-session shape first (its multi-batch close plumbing is what cruise control needs anyway), with cruise control kept parked as the named end-goal. The self-hosting-notes inventory (34 findings) was consciously deferred to a dedicated /plan — too judgment-heavy for a long session's tail, and mostly either stays put or funnels into a blocked parked item. The two Taskflow audits were placed at the queue top behind a plan-marker because their findings may reshape the /setup- and SPEC-related batches below, so they must be processed in /plan before those build.

**Queue changes:**
- Promoted (build/spec-edit): [retire-registry], [spec-sync-registry-and-lock] (consolidated SPEC sync), [consumer-spec-model-sync], [readme-usage-cycle], [in-product-rhythm-teaching] (carries the /setup FAQ too), [publish-marketplace-manifest] + [install-self-install-branch], [deferred-test-seams-fix], [close-shipped-slug-crosscheck], [formalize-goal-session], [new-batch-type-touchpoints], [faq-coverage-backfill], [bound-opening-narration-aggregate].
- Promoted (audit): [audit-taskflow-setup-transcript], [audit-taskflow-first-spec-edit] — both moved to the queue top, followed by a `--- Plan session here ---` marker.
- Parked: [full-tag-placement-recheck] (trigger repointed at this round's tag/narration batches), [plan-md-offload-reframe]; cruise-control reframed as the end-goal of the /goal arc.
- Deferred: self-hosting-notes inventory routing → a dedicated /plan (durable note added to the capture).
- CLAUDE.md close-fix: "Target v1.11.0" → "Target v1.12.0".

**Captures routed:** 13 promoted (above), 2 parked, 1 deferred, plus 3 filed this session (present-and-interview forced-wait — left for a future /plan; pull test-session transcripts from .jsonl; plugin.json "two hooks" should be three). Item 8 (/setup-on-dev-project) folded as evidence into parked [self-hosting-support-during-setup] + deferred to item 17; items 12, 15, 16+17 folded into existing batches.
