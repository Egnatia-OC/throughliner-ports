# [HASH] — The stale-CLAUDE.md question answered by the record: the managed block self-heals, user text stays report-only, the host case has its own item

Deleted as covered. Of the item's three candidates: (b) shipped for the region the method owns — [managed-claude-md-block-never-refreshed] refreshes the plugin-managed block at migration, repairing exactly the kind of stale lines the consumer reported; (c) holds for user-authored lines — text the user wrote is theirs, the never-overwrite rule kept its single carve-out deliberately, and the cost is only delay (a queue item next run, or the user editing their own file directly); and the host-side instance — whether /plan may write this project's method rules — is [plan-as-rule-author], the user's own proposal deliberately split for its own discussion, which deciding here would have pre-empted by momentum. No reply owed to the sender (a defect report asking no question).

**Queue changes:** item deleted as covered by shipped work and an open item.
**Work processed:** deleted — [claude-md-unwritable-by-the-skills-that-notice] (covered).
