# c904687 — plan.md floor narration — must quote the digest's opening medians (line-count and first-seen)

From a self-observed slip ([ladder-medians-re-derived-mid-session]): a planning run briefly ranked Unprocessed against a re-computed median instead of the medians fixed at the opening, skipping two qualifying entries before self-correcting — the tenth instance of a correctly-worded rule in context not firing, so sharper wording was refused on that record. The fix makes the fixed figures audible: the floor narration now quotes the two opening medians the digest printed, so a mid-session re-derivation becomes a visible mismatch against a number already said out loud rather than a silent substitution. The noise cost is two numbers in a line that already fires every session.

Tick: done, confirmed (the narration text requires the two medians; the fixed-at-opening rule unchanged).

Rule gate: run — amendment to the floor-narration parent in plan.md's start-of-processing step; no freestanding rule, no slot spent.
FAQ: not needed because the narration's shape is Claude's; the user's actions are unchanged.

**Files touched:** plugin/throughliner/docs/plan.md
**Routed to Captures:** none
