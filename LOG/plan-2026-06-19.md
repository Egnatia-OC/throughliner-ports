# [HASH] — Plan: cleared the 2026-06-18 capture/unpark backlog (4 promotions) and ran a long user-driven method-quality round — commit-then-offer-push reshape, two Simply Sew E2E reads, structural fixes for dependency-integrity, verbatim bundling, and review-vs-test framing

This session cleared the waiting captures and the one unpark candidate, then worked through a series of user-raised improvements, most sourced from fresh Simply Sew E2E sessions.

From the backlog: unparked [reinstall-confirm-nudge] (its blocker [deferred-test-seams-fix] had shipped) and promoted it as a reword of the session-start update message — the existing line already fires only when deferred tests wait, so it's a reword-plus-retarget, not a new mechanism. Promoted [reader-test-fixture-spec-sync] (a dev-fixture sync to the spec-edit-batch model) and [deferred-test-summary-sync] (two doc summaries lagging the two-axis deferred-tests reframe). Amended the parked [consumer-plugin-feedback-channel] to add a user-initiated reporting path and a contact-form/paste-ready mechanism, with a scrub-the-report privacy constraint; it stays parked.

User-raised threads: [commit-then-offer-push] makes /done commit first, then offer push — weighed against the settled [push-offer-fit] dual-ask and chosen on a safety-model reasoning (commit is the safe local action, push is the outward one needing consent); it keeps [push-offer-fit]'s substance and carried two alignment edits. Two Simply Sew E2E transcripts: the 1.57MB one became audit batch [audit-simply-sew-e2e] behind a plan-marker; the 0.92MB one was read inline (preprocess-then-read) for its one focused finding — /next's completeness flag recommended a non-terminating "go add X first" where X was parked behind a chain looping back to that batch, so [next-circular-dep-aware] makes the flag loop-aware (the queue mechanism was right; the failure was surfacing, not reasoning). [goal-session-plugin-state] fixes the goal-session doc after a web search (filed at resources/research/plugin-enable-disable-session-lifecycle.md) confirmed disabling a plugin mid-session doesn't stop its hooks — you must disable and start a fresh session. The dependency-integrity pair [plan-close-dep-check] and [plan-close-dep-lint] came from a twice-seen Simply Sew failure; the lesson driving them — and [verbatim-at-checkpoint] — is to make the check structural rather than rely on discipline or on Claude reasoning well in a messy domain. [verbatim-at-checkpoint] moves the next capture's verbatim to the prior checkpoint so the turn boundary becomes the separator, subsuming the bundling-feedback capture filed this session (the preference was also saved to memory). [review-vs-test-framing] stops dressing a user's eyeball-review of generated output as a pass/fail test.

Dropped a Taskflow image-size observation as not-SI — a Claude Code input limit Claude self-handled. Corrected several new items mis-dated 2026-06-18 to 2026-06-19.

**Queue changes:**
- Added: [reinstall-confirm-nudge], [commit-then-offer-push] (Blocks [faq-coverage-backfill]), [reader-test-fixture-spec-sync], [audit-simply-sew-e2e] (+ a "Plan session here" marker), [deferred-test-summary-sync], [goal-session-plugin-state], [plan-close-dep-check], [plan-close-dep-lint] (Depends on [lint-overflag-fixes]), [verbatim-at-checkpoint], [next-circular-dep-aware], [review-vs-test-framing].
- Alignment edits: [faq-coverage-backfill] gained a Depends-on header + reworded FAQ bullet; the [push-offer-fit] deferred-test line reworded to the commit-then-offer shape.
- Filed research: resources/research/plugin-enable-disable-session-lifecycle.md.

**Captures routed:** promoted [reinstall-confirm-nudge] (from Parked), [reader-test-fixture-spec-sync], [deferred-test-summary-sync], [goal-session-plugin-state]; amended and kept parked [consumer-plugin-feedback-channel]; filed then subsumed the bundling-feedback capture into [verbatim-at-checkpoint]; dropped the Taskflow image-size observation.
