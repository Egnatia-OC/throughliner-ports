# [HASH] — The delta test run over SPEC, README and the sent record: no claim claims Claude Code's work outright, two premises are stale

Audit entry; the planning record is `2026-08-21-claims-need-a-claude-code-delta-test.md`. Two lenses over every claim: does bare Claude Code deliver it, and is it contradicted for the 5-series by the live model research.

**Sources read.** SPEC.md whole (at run start), README.md's "What it does" (all lines including the long ones), and `INBOX/sent.md`'s five claim lines.

**Delta-lens verdicts.** *Throughliner's outright:* the two-section queue with its vetting boundary and readiness line; flavors and the red-flag lifecycle; per-item rationale carried as prose and read back at the close; the scope-lock configuration; the filed-claim stop-hook; the queue lint and credential-shape scan; the editing-state signal and its published contract; the durable cross-project INBOX with approval-gated sends and the sent register; session-start state detection, the format-epoch halt and migrations; forward advisories; the /setup interview and privacy offers; the FAQ. *Partly Claude Code's, with Throughliner supplying the reliability (stated per claim, not a binary):* the throughline itself — Claude Code ships durable project memory that carries intent informally, but nothing structures rationale per work item or makes the record user-vouched; per-session records — transcripts and git history exist, a curated readable record does not; multi-item unattended-in-practice building — one prompt can work a list, but with no vetting boundary or scope-lock; write-first-then-report — bare Claude Code already edits then reports, and the delta is the recoverability test with its show-first carve-outs. *Claude Code's outright:* none found stated as Throughliner's.

**Model-era lens.** SPEC's rationale-for-compliance claim already reads as historical (corrected earlier); the reinstated caps carry their derivation. Two stale premises found, both filed and approved as captures: the total-amnesia framing, and the no-channel-between-projects framing.

**Sent-record claims checked live:** the Discord post's work-cycle claim remains true of the installed plugin after this run's WHO RUNS THESE amendment; today's two GitHub issues were checked at drafting; the Taskflow bridge line records questions, not claims.

**Files touched:** none — read-only.
**Routed to Captures:** [throughline-claim-overstates-amnesia], [inbox-claim-predates-live-messaging].
**Approval outcomes:** all findings approved as-is (in the run's five-finding set).
Rule gate: not needed — an audit edits nothing.
