# 1860445 — /plan: verbosity-steering arc promoted (4.8 authoring heuristic + standing check, response-shape tag redesign, plugin-shipped concise output style at system-prompt priority); SPEC gains brevity-as-control-requirement principle + output-style mechanism entry, both anti-terseness-guarded; [closeout-text-collapse] moved to top; [done-spec-sync-check] promoted; two stale dep headers cleared and both no-slug design-notes routed out; output styles confirmed buildable; two captures filed

This session turned the standing verbosity/brevity frustration into a concrete buildable arc, alongside the queue cleanup requested. The two no-slug design-notes in Batches — "brevity is a user-needs requirement" and the "4.8-ifying heuristic" — were the targets, and are two halves of one design: the heuristic is the checkable list of the same positive/quantified/exemplified/lead-with-decision principles the brevity note calls for, so it was authored first as the foundation the rest applies.

A concern the user raised reshaped the arc and is preserved: forcing concision risks arbitrary terseness, which for a non-coder defeats plain-English accessibility as badly as a wall of text. The reframe that won: the output style's job is not a length cap but to lift the plugin's existing structural rules — lead with the decision, one item at a time, gate detail (already in plugin-behaviour.md Communication + the [SEQUENCE] tag) — to system-prompt priority, the level 4.8 honours. Same chunking rules, higher; never terser. An anti-terseness guard was threaded through the SPEC principle, the heuristic, and the output-style batch.

The output style flipped from parked to a build batch on a web-search finding (filed to resources/research/opus-4-8-verbosity-steering.md): output styles modify the system prompt directly, plugins can ship them in an output-styles/ directory, and force-for-plugin auto-applies the style to every user — dissolving both prior open questions (desktop path, consumer distribution).

An alternative was weighed and rejected, preserved so it is not relitigated: the user's hypothesis that bundling stems from insufficient working memory, fixable with extra accessible memory. It lost because bundling is a disposition problem, not a memory one — within a session Claude holds the whole transcript, and the research attributes bundling to trained thoroughness, not memory scarcity; the one real memory seam, compaction, is already covered by _plan.md / _build.md. Filed as a speculative capture rather than acted on.

The unpark pass promoted [done-spec-sync-check] (blocker shipped) and kept two items parked (inline-internal-term-marker, trigger not fired; [lint-citation-refire], no verified lived experience — the "lint keeps flagging" line in the opening handoff was Claude-authored, not user-verified).

**Queue changes:**
- Moved [closeout-text-collapse] to top of Batches; dropped its satisfied Depends-on: [approval-display-blockquotes].
- Cleared [opening-narration-audit]'s satisfied Depends-on: [plan-step1-sequencing] (stayed in place).
- Added [4-8-authoring-heuristic] (slot 2), [tag-definition-redesign] (slot 3), [verbosity-output-style] (slot 4); the latter two Depend on [4-8-authoring-heuristic].
- Promoted [done-spec-sync-check] to a batch at the end of Batches; renamed malformed [parked] slug to [inline-internal-term-marker].
- SPEC.md: added brevity-as-control-requirement principle (with anti-terseness clause) and the output-style mechanism entry under "How it works".

**Captures routed:** promoted [done-spec-sync-check]; routed both no-slug design-notes out of Batches (Brevity → SPEC principle + SPEC mechanism entry + [tag-definition-redesign] + [verbosity-output-style]; 4.8-ifying-heuristic → [4-8-authoring-heuristic]); filed two new captures (_plan.md offload pressure-release; handoff-claims-treated-as-verified); kept [inline-internal-term-marker] and [lint-citation-refire] parked.
