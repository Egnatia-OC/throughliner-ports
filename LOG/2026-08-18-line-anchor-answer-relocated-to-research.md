# [HASH] — a consumer's line-anchor answer moves to the research shelf and its queue item is deleted

The item carried an INBOX answer to a question this project asked on 2026-08-10: can a reader open a document at a given line? Its own text said the answer changes nothing in the method and that the real work was getting the external evidence onto the shelf. At 485 words against a 265 capture ceiling, the band's breach action pointed the same way.

Filed as `resources/research/line-anchored-file-links.md`. What it holds: nothing is built on their side; a `.md:N` reference **fails silently inside Claude Code before any application is launched**, citing an issue number; where line numbers do survive they appear to reach a *configured editor* rather than the file type's default handler; and nothing confirms a default-handler launch on Windows ever receives one. Two independent observations point the same way, since this project separately watched the desktop viewer ignore an anchor.

**What follows for the method is nothing, and that is why it belongs on the shelf rather than in the queue.** Keep rendering a plain file link with the line named in the prose — which is already what the shipped rule says. The file exists so a later session weighing link rendering finds the answer instead of re-deriving it, and so nobody promises a line anchor on a hope.

The content was relocated before the item was removed, which is what separates this from a plain delete: delete means not worth doing, and this was worth keeping in a different place.

**Queue changes:** [line-anchor-answer-from-understudy] deleted after its content was filed as research with an index line.
**Work processed:** deleted — [line-anchor-answer-from-understudy].
