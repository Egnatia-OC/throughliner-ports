# eebbf81 — /plan: retire /cruise for multi-line /next, rework red flags to cleared/uncleared, and queue the fallout

Opened to plan migrating old-format projects onto the current SI; became a larger reshape. Migration resolved: the plugin is global, so only a project's docs (chiefly QUEUE.md) need converting, and that conversion needs judgment not a script — filed as [migrate-skill]. Main thread: /next already runs multi-line autonomous runs (next.md's run model), duplicating /cruise, so /cruise is retired and multi-line /next becomes the single, unattended queue-clearing skill. Keeping /cruise was weighed and each of its rails rejected — per-line commits replaced by a commit rule (one line → log verbatim, many → summary); budget ceiling undetectable; iteration ceiling arbitrary; red-flag gate redundant. Removing the gate drove a cleaner model: two red-flag states (cleared/uncleared), processing is when flags clear, and a flag that can't be cleared returns to the bottom of Unprocessed — unifying the method's shelving to a single mechanism (return-to-bottom), the shared answer to [defer-for-design-process] and [claude-invents-shelving-hardening]. An early wrong turn — parking a flagged line below the cleared-to-run marker — was caught as accidental parking and rejected. Session sizing moves to /plan time via user-placed session-break lines; verified Claude can't natively read its context budget (the relayed token-tag claim is false), though the status-line remaining_percentage field is a real lever for a companion mechanism — research filed. /next stays unattended, so a lightweight no-progress stop is the one /cruise guard kept.

**Queue changes:**
- Processed +4 build lines: [retire-cruise] (widened to also retire the goal-session mode), [redflag-cleared-uncleared-model], [next-close-commit-rule], [next-no-progress-stop].
- Unprocessed +captures: [migrate-skill], [skip-to-defer-in-plan-loop], [session-sizing-and-break-lines], [work-item-terminology]; plus a refinement + companion-narration note on [quickplan-skill]. ([capture-disposition-over-narration] and [capture-then-offer-process-now] were also filed this session.)
- Filed resources/research/context-window-visibility-to-model.md.

**Work processed:** Moved to Processed: retire-cruise, redflag-cleared-uncleared-model, next-close-commit-rule, next-no-progress-stop. Deleted: next-renders-cruise-obsolete (answered), faq-four-commands-stale (absorbed by retire-cruise), retire-goal-sessions (folded into retire-cruise), cruise-run-verification, cruise-gate-hardstop-verification, cruise-readiness-verification, cruise-unattended-hook-signal (all mooted by the /cruise retirement). Filed as captures (not processed): migrate-skill, capture-disposition-over-narration, capture-then-offer-process-now, skip-to-defer-in-plan-loop, session-sizing-and-break-lines, work-item-terminology.

**Routed to Captures:** wind-down re-scan filed [plan-serves-single-runner] and [provenance-labelling-reconsideration]; added a fresh-instance note to [claude-invents-shelving-hardening].
