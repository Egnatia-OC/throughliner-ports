# fa24343 — Renamed "handover"→"walk-through" across the method and rewrote /next Step 3 to drive `[user]` walk-throughs live, one step at a time

The walk-through-first rule already existed in next.md but failed for two reasons: the closing /done instruction outranked it in the same pass (an "offer" is not a drive), and the word "handover" primed hand-off. The fix targeted both. Renamed handover / hand over / hand-off → walk-through / walk you through across next.md, plugin-behaviour.md, plan.md, the done docs, the FAQ templates, SPEC.md, and the orientation docs (CLAUDE.md / AGENTS.md), leaving the distinct session-handoff and "not-a-hand-off" senses intact. Rewrote next.md Step 3 so /next drives the user one step at a time (waiting after each), says nothing about /done until the step is done or deferred, and presents multiple `[user]` items one-at-a-time — killing the old "present handovers together" bundle carve-out. Clarified plugin-behaviour.md's [SEQUENCE] bulk-approval inversion so it excludes `[user]` walk-through lines, closing the path by which the carve-out could be re-derived. Added a plan.md keep-step nudge to decompose a mixed Claude-prep + user-step item into build items plus one irreducible `[user]` line. Rejected simply re-asserting "walk through first" — that rule was already present and still failed, so the fix had to target the word and the ordering tension, not repeat the instruction.

**Files touched:** next.md, plugin-behaviour.md, plan.md, done.md, done-plan.md, templates/faq-template.md, templates/faq-index-template.md, SPEC.md, CLAUDE.md, AGENTS.md

**Routed to Captures:** none
