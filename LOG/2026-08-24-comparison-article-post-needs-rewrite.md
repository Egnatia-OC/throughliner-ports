# [HASH] — Kept and held: the comparison-article post rewrite waits on the article by field, not prose

Processed 2026-08-24. The approved-but-unposted Discord post describes the superseded article draft, and the rewrite can only run against the final article, which is out for external review. Kept with its authored walkthrough unchanged; the one change from how it filed itself is placement — as a capture it carried its ordering in prose because captures have no `Blocked by:`, and as a held work item it carries the field (`Blocked by: [competition-comparison-article]`), lifting itself when the article closes instead of being re-offered every session. The ordering sentence is written on the article item too, per the known-ordering rule.

**Work processed:** kept — [comparison-article-post-needs-rewrite], held below the line.
