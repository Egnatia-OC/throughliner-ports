# [HASH] — The work cycle stated as a loop with its return edges, at the opening of plan.md and next.md

The block the item drafted, placed verbatim at each doc's opening as its own "The work cycle" section, marked as orientation Claude reads and never recites to the user.

**Why it is written as a cycle rather than a list of steps** — the point the design turned on. A linear "capture → plan → build → close" list would not have prevented either instance that produced this item, because both failures were about a loop *closing*: Claude reasoned that an `[audit]` could not feed a build because a planning session sat between them, which is only wrong if you can see the return edge. So the forward path is not the content; the return edges are. The block names two explicitly — an audit's findings re-entering as captures, and a build's discoveries doing the same — and says in as many words that an intervening /plan is the cycle working rather than an obstacle to it.

**Also carried, from the public build-cycle page:** the session boundary as step 4 (the loop's edge is a fresh session with no memory, which is *why* every return edge routes through a file), the page's own vocabulary for the three stages, and capture promoted from a step to a standing affordance available throughout.

**Both docs get the same text.** Steps 3 and 6 matter more to /next and step 2 more to /plan, but a session reading the whole loop is the point; tailoring per skill would reintroduce the partial view this fixes.

**Cost:** none against the always-loaded ceiling — both files are fetched when their skill runs.

**Files touched:** `plugin/si-plugin/docs-b/plan.md`, `plugin/si-plugin/docs-b/next.md`.

**Routed to Captures:** none.

Rule gate: run — admitted. It adds text to two procedure docs and its defence is a pointed-to failure with two instances in consecutive sessions, which is what the admission test asks for. Fetched rather than always-loaded, so it costs nothing against the ceiling. Nothing evicted to make room, because nothing was displaced.
FAQ: not needed because the FAQ already explains the cycle to users, and this block is aimed at Claude. Settled at processing that one text cannot serve both — the FAQ's job is to reassure a non-coder about what happens to their idea, and this one's is to stop a session mis-reasoning about routing.
