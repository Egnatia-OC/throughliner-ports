# [HASH] — Confirmed the behaviour rules reach no session, cleared eight items to fix delivery and the scope-lock holes, and retired two mechanisms rather than building them

This session opened by proving what the last one could only infer: docset B is live. The session-start payload arrived carrying the docs-b directive on host 1.16.0-test5 — a real session loading docset B, not a hook driven by hand. That cleared the lift-conditions on [release-docset-b-work] and [docset-routing-mechanism], and spent both forward advisories, which were dropped in triage.

It also exposed the thing the rest of the session organised around. The payload measured 54,886 bytes against a truncation boundary near 2KB, so `plugin-behaviour.md` — every standing behavioural rule the method has — reaches no session at all. [session-start-payload-oversized-and-misordered] had predicted 3,524 bytes on the assumption that docset B replaces the inlined rules with a pointer; it does not, and the docset fix cut the payload by 40% against a cap it misses by an order of magnitude. The item was rewritten around the measurement and now carries the FAQ-index block, folded in from its own capture, so one build handles all three bulk blocks in that function.

The consequence showed up twice more before the session ended. [next-over-warns-run-length] was captured as "/next warns runs are too long, and the negotiation costs more than the risk" — but no such rule exists in either docset, and three separate statements say the opposite, the strongest of them in the truncated file. So the item reversed: the rule is right, undelivered, and stated only in context-management sections rather than at the run presentation where the impulse fires. Then Claude claimed five captures were written to QUEUE.md before writing them, violating `plugin-behaviour.md`'s write-then-verify-then-point rule — again from the file that does not arrive. That is filed as its own capture, because a rule failing on the session that measured its non-delivery is the cleanest evidence the item will get.

A scope-lock audit run in a separate free-form session was consumed and moved to `resources/research/`. It inventories 41 mechanisms across four layers and ranks six gaps; five were unfiled and are now captures, and one existing item gained an amendment — [concurrent-session-support]'s silent queue merge is an expansion of agent authority over the artifact that defines scope, which its design had not framed that way.

Six captures were then processed against a priority the audit and the measurement jointly set: a doc edit is worth less than delivery. Two of the six ended by removing mechanisms rather than building them. [sanctioned-override-mechanism] proposed a general override path for four captures that looked alike; working through them showed only one is a departure at all — in the other three no rule existed, and you cannot depart from something never written. It became a stated distinction instead, with the single genuine override handed to [plan-scope-lock-gap-and-emergency-path]. That item then resolved its scope-lock half as an ask-never-deny gate modelled on the subagent gate, on the evidence of this very session: a legitimate /plan write — moving the audit into `resources/research/` at the user's request — would have failed any sensible whitelist. With the gate asking rather than denying, the emergency path dissolved into a record duty and a red-flag triage distinction, and the deny-list-plus-exception alternative was rejected and recorded, because an exception convenient enough for a real emergency is convenient enough to be reached for when it isn't one.

One decision was the user's and reversed a documented convention: the push marker is retired in favour of the readiness line, taking the dependency model from three routes to two. /plan and done-plan.md place push markers that next.md says are gone, so /plan has been placing a gate /next ignores — the failure being that an item needing a shipped host gets built against the old one, silently. The cost is real and recorded: mid-run sequencing goes, and a group needing a push waits for the next /plan. The spec-sync gate was raised at close and deferred to that build by the user's call, against its default, on the grounds that SPEC is already wrong here and editing it now would swap one divergence for another until the docs follow. That reasoning is written down because "we'll fix it with the build" is the promise this gate exists to distrust.

**Queue changes:** eight items cleared to run, ordered by file locality — payload → docset-routing-mechanism → fail-open advisory → /plan ask-gate → spec-diverges → sanctioned-override → blocker-claims → next-over-warns. [docset-routing-mechanism] lifted on a met condition and placed second, because the payload fix creates a second skimmable redirect and its own text requires the two be built adjacently. [release-docset-b-work] held below the line by choice, annotated as unblocked so the next session doesn't re-derive it. Three lift-conditions naming queued slugs rewritten to `Blocked by:`. Two forward advisories dropped.

**Work processed:** kept — session-start-payload-oversized-and-misordered, next-over-warns-run-length, scope-lock-fail-open-on-absent-files-section, spec-diverges-from-shipped-docsets, sanctioned-override-mechanism, plan-scope-lock-gap-and-emergency-path, unprocessed-blocker-claims-go-stale. Folded — faq-index-injected-as-session-start-bulk into the payload item. Deleted — advisory-rezip-and-prove-docset-b, advisory-run-the-cleared-seventeen.

**Routed to Captures:** setup-runs-outside-behaviour-rules, scope-lock-fail-open-on-absent-files-section, no-session-end-or-compaction-hook, spec-diverges-from-shipped-docsets, build-scope-vs-described-work-unenforced, agents-md-undocumented-duplicate, no-mechanical-mover-for-cross-section-moves, reorder-mover-demands-full-slug-set, claude-reported-a-write-that-had-not-happened, lift-conditions-anchored-to-spent-events.
