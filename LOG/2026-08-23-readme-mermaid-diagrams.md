# 007a16c — Kept and cleared: three mermaid diagrams for the README — one idea each, no combined view

Her idea, scoped by her correction: the combined infographic drafted the same day didn't make sense, so the principle is one idea per diagram — structure, the work cycle, the throughline — and simplicity before any high-level combining. Placement hers too: the README, where GitHub renders mermaid natively. Why mermaid, from the discussion: it is text, so one source serves both audiences — rendered for people, node-and-edge declarations for Claude — with the bounds stated that diagrams serve user-facing surfaces only and carry structure, never the why, which stays prose.

Rule gate: not needed — README content, no method rule text.

**Queue changes:** new cleared build item, last of the builds.
**Work processed:** kept — [readme-mermaid-diagrams].
