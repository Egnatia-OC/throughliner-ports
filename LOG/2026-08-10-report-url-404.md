# 10d6474 — flintcraft.tech/report stood up, and the check that found it already live

Slug: `[report-url-404]`. Entry opened live during the /next walk-through, per the
rule built in this same run ([walkthrough-work-unrecorded]) — this is its first
real use, so the entry exists before the item is finished.

## What the world showed

The item was filed on 2026-07-18 after a real feedback report hit a Netlify
"Page not found" at flintcraft.tech/report, which meant no report could be filed
at all, whatever its format. The item's own walkthrough names an observable
check, so the walk-through ran it rather than asking whether the work was done.

**The page exists and is live**, fetched 2026-08-10. It carries: a "Report a bug"
heading; the explanation that a report Claude captured can be pasted in; a
honeypot field; an optional email field for replies; a "Which project?" dropdown
offering Throughliner, TaskFlow and Something else; a single text box labelled
"The bug report (paste what Claude captured)"; and a Send button.

So the first half of the item's done-when — the page exists — is satisfied, and
satisfied in the shape [report-fields-mismatch-webpage] settled on: one free-text
block rather than a field-by-field form. The dropdown is one field more than that
item described, and it is a reasonable one, but it is worth noting rather than
passing over: the method's report format does not currently emit a project name
as a separate value, so whoever files a report picks it by hand.

## Complete

The second half — that a real submission actually lands — was confirmed by the
user during the walk-through: in their words, "it came in". That is one of the
three ways completion can be known, and it is the only one available here, since
a submission arrives in their own inbox and nothing on this side can observe it.
The item is closed and removed from Processed.

## Actions taken by Claude during the walk-through

- Fetched flintcraft.tech/report and read what is on it (above).
- Gave the user the address on request.
- Nothing sent. Claude made no submission, and nothing on the site was edited.

## A misunderstanding worth recording, because it points at a real gap

The user asked why the site was being edited from this project, having said the
site is a separate project. Nothing was edited — only the URL was fetched — but
the question was fair, and the honest answer is that the item had no business
still sitting in the ready pile: its work had been finished elsewhere, and
nothing here could know. That became the capture
[cross-project-work-completes-invisibly].

The mechanism did behave correctly in one respect worth keeping: because the item
recorded an observable check, the walk-through ran it rather than asking the user
whether they had already done it. That is the check-the-world rule working as
designed, and it is what turned a stale item into a one-line confirmation instead
of an interrogation.
