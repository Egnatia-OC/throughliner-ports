# 3cc6f44 — Also in this chat: a build run of eight, two walk-throughs, and what the writing exposed

The chat-level record for the 2026-08-23 build session. Ten records sit beside this one — eight built items and two walk-throughs — and this carries what belongs to none of them.

**Also in this chat:**

- **The session's real finding came from writing, not building.** Two pieces of user-facing prose were drafted here, and both first drafts were wrong in the same direction: the comparison article conceded that semantic search "will beat anything a structured queue can do", giving away the queue's defining property, and the first Discord draft narrated how the ordering ladder was built rather than what a user gets from it. The user caught both. Her framing of the queue as a contract both parties can read — closed, complete, handed identically to every session — is what the article now argues, and it is hers. This is what [method-purpose-orientation] was filed from: nothing in the shipped package states what the method is for, and a consumer's SPEC describes their own app, so consumer sessions run on procedure with no purpose statement at all. This project only appears to have one because its product happens to be Throughliner.

- **A correction to the verification given to the user, recorded because it went out wrong.** Five findings were raised against her edited Discord draft; two were not defects. Her "most-designed work" was challenged as reinstating a claim the record rejected, but she had changed the word deliberately and the distinction holds — only the *quality* claim was tested and refused, and "enriched across many sessions without resolving" is most design effort by definition. Her 60-item figure was challenged as an underivable limit; it is derived, from her own observed experience, which is exactly what the gate asks for. Both were conceded and the post states the derivation.

- **A research finding was corrected before it was used.** `resources/research/auto-memory-staleness.md` claimed a manual `/dream` command is available to everyone. Two sources now put the command behind the gradual rollout. What survives is what the article needed — consolidation is reachable by every user by asking in plain words, and AutoDream is Anthropic's own feature — so the concession holds and the command claim does not. Sources conflict rather than settle, and the file says so instead of picking a winner.

- **An outbound hold-note went to the site project**, on the user's approval of its exact text, asking that the 2026-08-22 comparison article not be published because a longer version supersedes it. Recorded on `INBOX/sent.md` for continuation. The full replacement waits on an external review, on the user's choice between superseding once or twice.

- **The scope-lock refused a write during the close, correctly.** The FAQ files were not in the run's file list, though the close-amending item's own text had flagged an FAQ entry as likely. The build halted, said so, and four paths were added on the user's approval rather than the entry being skipped.

- **The record-naming rule shipped this morning was tested by this very close.** The overwrite guard refused the bare filename for each built item — this morning's planning session holds them all — and offered the legacy `-2`. The records were written with `-build` instead, per the rule shipped hours earlier. The guard still suggests the legacy shape; that is noted here rather than changed, since changing it was not this run's work.

- **One held item is now unblocked and was deliberately left held.** [discord-post-session-start-strength] named the posted ladder announcement as its blocker, so removing that item leaves its `Blocked by:` pointing at nothing and the lint flags it. The blocker shipped rather than being dropped, so the item is genuinely liftable — but lifting is a clearing move and belongs to planning, not to a build close, so it is reported here and left alone. Under the one-a-day pacing it is the next post in the chain.

- **The staleness sweep found drift it is not allowed to fix.** Two queue items name a record this run renamed — pure pointer drift, which `done.md` says to fix at the close, mechanically and without asking. `pre_tool_use` refused it, correctly: a build does not edit QUEUE.md directly, and the queue tool only moves whole entries by slug. The only workaround would relocate a held item to the wrong region over a stale filename, which is worse than the drift. Both pointers are left in place and named in [pointer-drift-unfixable-at-a-build-close].

**Routed to Captures:** [log-retrieve-search-leg], [method-purpose-orientation], [walkthrough-repeats-completed-steps], [inbox-address-book-absent], [spec-owes-close-amend-and-gate-once], [pointer-drift-unfixable-at-a-build-close], [compliance-audit-lag], [guard-suggests-legacy-record-suffix], [comparison-article-post-needs-rewrite].

**Checks run at this close:** all 19 suites under `resources/testing/` pass, run as plain scripts. `resources/rule_signals.py` — five checks run, one found something: rule-bearing commits since the last compliance-audit record are unaudited, filed as [compliance-audit-lag]. That is what was checked and found, and it says nothing about whether the rules are correct.

Advisory: filed — [walkthrough-repeats-completed-steps]

Rule gate: not needed — this record authors no rule; each built item's disposition is on its own record.

FAQ: updated — the close-amending entry, recorded in full on that item's record.
