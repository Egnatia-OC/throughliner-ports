# bbb9900 — Recovery session (SI uninstalled, plugin off): reinstalled SI v1.14.0-test3 via the CLI local-folder marketplace after the desktop upload button vanished; corrected resources/research/claude-code-plugin-install-paths.md and filed 2 install-path captures

The Claude Code desktop app removed its in-app path for installing a local/custom plugin (the upload button is gone). I'd uninstalled SI intending to reinstall from a ready test rezip, and got stranded with no UI way back in. This session drove the `claude` CLI instead: removed the stale broken `sovereign-implementer` directory-marketplace fossil (it pointed at a folder that no longer exists), ran `claude plugin marketplace add "<repo root>"` (registered marketplace `flintcraft` from the already-committed root `.claude-plugin/marketplace.json`), then `claude plugin install sovereign-implementer@flintcraft`. SI is now installed at v1.14.0-test3 (user scope, enabled), pending a full app restart to load.

Key discovery: the install snapshots the plugin into `~/.claude/plugins/cache/flintcraft/sovereign-implementer/<version>` — it does not read the live working tree. So the rezip loop must change: after a rezip, run `claude plugin install`/`update` to refresh the cache snapshot, then full restart. Also corrected the research file's two wrong claims (the `marketplace.json` does exist and works; the install caches rather than reading live). Filed two captures so the next /plan folds the confirmed CLI method into the install-path batches and the CLAUDE.md Rezip/Push rituals.

**Files touched:**
- `resources/research/claude-code-plugin-install-paths.md` (new, then corrected)
- `QUEUE.md` (two captures appended)
- *(ride-along)* `LOG/index.md` + 6 entry files — the previous goal run's hash backfill (`[HASH]` → `77ef065`)

**Routed to Captures:** `[desktop-plugin-upload-removed]`, `[cli-install-confirmed-rezip-loop-update]` (both filed during the session)
