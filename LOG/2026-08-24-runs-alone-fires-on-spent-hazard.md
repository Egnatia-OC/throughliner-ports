# [HASH] — Kept and cleared: `Runs alone` ends the run only when this run performed the work

Processed 2026-08-24, from a consumer report received this session: a run confirmed a marked `[user]` item's folder rename was already done, closed it, then ended the run on the marker's presence — two buildable items unbuilt for a spent hazard, and the user there asked why the run couldn't continue. The fix keeps the rule mechanical (the report's own point): the stop keys on performed-by-this-run, read off the run's working file; an item found already done closes and the run continues. Build items are unaffected — building is performing. The full "Runs alone" carrier set was grepped at the keep and the untouched scripts checked rather than assumed. SPEC's run-bound sentence gained the case this session. Gate: run — amendment to the marker rule's trigger.

**Work processed:** kept — [runs-alone-fires-on-spent-hazard], cleared with build block.
