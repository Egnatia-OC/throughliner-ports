# [HASH] — work finishing in another project is learned by checking, and the notification branch is refused

An item can sit ready here long after the work it waits on has finished elsewhere. [report-url-404] was the recorded instance: the page had been live for some time and nothing here learned it until a walk-through fetched the URL.

**The notification branch is refused on [inbox-delivery-unconfirmed]'s own finding.** Mail is fire-and-forget in both directions, so a notice nobody is obliged to read moves the problem rather than closing it. Nothing pushes, and the docs now say so rather than leaving it to be discovered.

**The checking side already works, and the defect is upstream of it.** The always-loaded walk-through rule checks the world where an item names an observable result, which is exactly what rescued that instance — the item happened to carry a fetchable URL. Nothing anywhere requires an item to *carry* one when one exists, so whether the check can run at all was left to whoever wrote the item.

So the clause lands at the keep-step: an item whose completion happens outside this project names what would show it done — a URL, a file, a branch — **or states plainly that nothing observable exists.** The second half carries as much weight as the first, because it is what tells a later run to ask rather than check, instead of leaving it to guess which case it is in.

The residual is labelled rather than solved: completion visible only to the user, with nothing to test, waits until they mention it.

**Queue changes:** [cross-project-work-completes-invisibly] settled and cleared.
**Work processed:** kept — [cross-project-work-completes-invisibly].
