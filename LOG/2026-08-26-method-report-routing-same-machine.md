# [HASH] — Same-machine reporting arm kept, detected by the address book, with the SPEC sentence written at the keep

Alex's repeatedly-hit gap: her own projects draft stripped public reports when they should mail this project. The address book is the mechanism and makes the arm shippable rather than host-only — an entry exists only where the user supplied one, so external consumers never hit it and nothing scans the machine. The build adds the arm to both copies of the discriminator (the fetched doc and the always-loaded rules, which must agree), renames the "Consumer feedback channel" headings to method-problem terms (the label invites the misroute), and adds one plain-English clause to README; `ANNOUNCEMENT-IDEAS.md`'s old FAQ entry is excluded, still true for external users. The SPEC sentence was written in-session per the keep-step rule: a method problem routes as approval-gated mail where the dev project is a recorded correspondent. Gate: run — amendment to the discriminator's method arm, its named parent.

**Queue changes:** kept and cleared with the builds; SPEC edited.
**Work processed:** kept — [method-report-routing-same-machine].
