# da47e59 — /plan: cleared the plan-marker — processed the Taskflow spec-write audit findings and the full capture backlog; added 5 batches, folded imported-self-description cost evidence into [migration-aware-setup], confirmed/annotated 3 deferred-test lines, dropped the queue-restructuring design thread

This /plan was forced by the plan marker at the queue top: process the Taskflow spec-write audit's findings before the SPEC- and /setup-related batches, since those findings might reshape them. It processed all 12 unprocessed captures plus two conversation-raised items.

The four audit findings resolved with little new build. The list-gap half of the systemic-vocab-leak finding was folded into a new [narration-vocab-expansion] batch (seeded from the "no unprocessed captures" leak finding); the compliance-miss halves were host-version-uncertain, so they were annotated onto the [narration-vocabulary] and [tag-definition-redesign] deferred-test lines to watch for recurrence once the rules are confirmed live, rather than actioned now. The migration-setup downstream-proof finding added one cost-evidence line to [migration-aware-setup]'s rationale (undoing the imported-self-description drift later cost a full remediation batch) — the marker's intended reshaping.

The positive-confirmations finding was resolved with the user confirming Taskflow ran the current test build: [closeout-text-collapse] was confirmed and its deferred line removed; [capture-verbatim-first] (/next half) and [push-offer-fit] (build half) were annotated as half-confirmed; the rest stayed (two-halved tests with only one half exercised, or candidate-only).

A queue-restructuring design thread (split per-batch rationale into per-slug files, from an external critique plus a full repo rebuild) was explored — including a web search confirming the index+detail "progressive disclosure" pattern is well-established (filed at resources/research/index-detail-doc-architecture-for-agents.md) — then dropped. Why it lost: rationale is needed at /next because 4.8 disobeys without the why, so a rationale-stripped execution view defeats its own premise; and the queue is large mainly from self-hosting inflation, so three-artifact machinery would cost every consumer to solve a workshop-only problem. Measuring that parked captures are 36% of QUEUE.md — more than the active batches — reframed the real problem as parked-material accumulation and produced the [parked-staleness-full-step] batch.

No red flags. One operational slip, fixed in-session: a Python splice removing the last capture also deleted a side-filed lint capture sitting between them; it was restored.

**Queue changes:**
- Added 5 batches (bottom of Batches): [plugin-json-hook-count], [memory-dir-scope-lock-exempt], [bold-the-ask], [narration-vocab-expansion], [parked-staleness-full-step].
- Folded a cost-evidence line into [migration-aware-setup]'s rationale.
- Deferred tests: removed [closeout-text-collapse] (confirmed); annotated [capture-verbatim-first] and [push-offer-fit] as half-confirmed; added watch-notes to [narration-vocabulary] and [tag-definition-redesign].
- Removed the plan marker (reason addressed).

**Captures routed:**
- Promoted: plugin-json-two-hooks, scope-lock-memory-writes, bold-ask, no-unprocessed-captures-leak, parked-staleness (conversation).
- Parked: self-hosting-notes-inventory (needs a dedicated /plan).
- Dropped: raw-jsonl-sourcing, preprocess-jsonl (superseded by [transcript-reading-workflow]); vocab-leaks-systemic, silent-path-narrated, positive-confirmations, queue-restructuring-design-thread, migration-setup-downstream-proof.
- Filed new (unprocessed): queue-lint-blind-to-deferred-tests.
