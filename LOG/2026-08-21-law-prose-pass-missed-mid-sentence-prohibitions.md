# [HASH] — The prohibitions remainder kept, with a coverage list as its required evidence

Planning entry. Kept and cleared behind [cut-length-everywhere].

Two things settled. The count was re-taken rather than trusted: 310 occurrences
of "don't"/"never"/"do not" across all fourteen docs — roughly double the item's
~151 estimate, which covered eleven. And the evidence question: the pass emits a
coverage list, one line per occurrence carrying file, line and a one-word
verdict, because a file read and correctly left alone is otherwise byte-for-byte
identical to one never opened — the same required-artifact shape FAQ-sync and
the gate line already use.

The general version — a keep-step clause requiring any traceless-work item to
name its coverage artifact — was refused on timing, not substance:
[keep-step-accretes-from-five-items] asks for one reading of that step before a
sixth clause lands on it. The gap stays open and is stated on the item.

**Queue changes:** kept and cleared, per-file build block written; runs per
file, stops at file boundaries, resumable from the list.

**Work processed:** kept — [law-prose-pass-missed-mid-sentence-prohibitions].

Rule gate: run — no rule admitted or evicted; the pass restates existing rules
in the already-required shape, and the coverage list is what makes an accidental
authoring visible. Disposition on the item.

FAQ: not needed — consumer actions unchanged.
