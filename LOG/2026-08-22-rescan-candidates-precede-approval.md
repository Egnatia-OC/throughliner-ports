# 2625fa0 — Kept and cleared: re-scan candidates are shown as the numbered set before anything is written

The user's position, kept on her parent-naming: the ideation-loop rule extended to the wind-down re-scan, whose exemption sentence ("nothing waits on approval before reaching disk") is deleted in the same move. The cost rounds to zero — the candidates already go out as one numbered message, and showing it first changes order, not length; the accepted trade is that a close now waits at this step, and /done is user-invoked so someone is there. Prior decision cited at processing: write-first's trade was stated when it shipped, and this narrows it at one site rather than reversing it.

Rule gate: run — amendment extending the ideation-loop parent to rescan.md and done.md; the exemption sentence is named and evicted.

**Work processed:** kept — [rescan-candidates-precede-approval].
