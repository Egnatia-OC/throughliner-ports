# 3102929 — The managed CLAUDE.md block kept on both halves: a reporting refresh path plus marker wording that promises what the refresh does

Kept with both candidates, on Claude's recommendation and the user's agreement: the refresh is the real fix and the marker's promise then matches it. The sender's constraint is designed in — the refresh reports what it will replace and moves any non-template text found inside the block below the end marker first, which is what their four drifted rules needed. A silent refresh and a wording-only fix are both refused in the block. Build block on the item.

**Queue changes:** [managed-claude-md-block-never-refreshed] moved Unprocessed → Processed, cleared.
**Work processed:** kept.
