# [HASH] — plan.md: a blocking question /plan can't settle cheaply files as its own [audit] item, not a plain capture

Built in the 2026-08-08 overnight blitz. Two edits to `docs-b/plan.md`: the resolve-now-versus-capture block in the Ground rules gains the routing clause (with both bounds — "cheaply" doing the work, since resolve-now has settled five such questions in seconds while routing two documentation reads once cost a full session cycle; and session-unanswerable questions going to `[user]` work or an experiment, never an audit), and the load-bearing-claims check at the keep-step gains a pointer to it, since that is where the blocking question is actually met. The rejected standing-accumulator mechanism stays recorded in the item's history. Docset A untouched (development). Ripple: plugin-behaviour.md's flavor-marker block left alone — it defines execution routing, not filing, and the rule lives at plan.md's decision points.

**Files touched:** plugin/si-plugin/docs-b/plan.md
**Routed to Captures:** none
FAQ: not needed because this governs how Claude routes a question during planning; a consumer meets only the resulting item.
