# [HASH] — plan — [spec-sync-gate-aligned-to-spec-leads] kept: done-plan's false-SPEC paragraph repealed in favour of the SPEC-leads model

Surfaced at this close's look-back and processed on the user's call ("too important to skip"): plan.md and SPEC say product truth is written at planning time, ahead of the build, while done-plan.md's gate still calls a SPEC edited for decided-but-unbuilt behaviour "a false SPEC". Two eras answering one question; the older never repealed. The lead model wins — /next reads SPEC at run start and builds toward it — with the residual cost stated: between plan and build, SPEC describes intended behaviour, bounded by the cleared item that builds it. This session leaned on the lead rule three times, which is what exposed the tension.

**Queue changes:** [spec-sync-gate-aligned-to-spec-leads] filed and cleared.
**Work processed:** kept — [spec-sync-gate-aligned-to-spec-leads].
Rule gate: run at the keep — repeal; recorded on the item.
