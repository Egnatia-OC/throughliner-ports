# 83e604f — /plan: processed 7 Taskflow E2E captures + 3 close-out design threads → 7 batches, [device-access-consent] red flag (open), 3 parked

Processed the seven Taskflow E2E captures that had accumulated, then three design threads raised at close-out.

The two queue-lint captures — the Blocked-by check blind to Deferred-tests slugs, and the loose-entry detector misreading lines nested under a Parked item — were promoted together as [lint-overflag-fixes], since both are the same hook over-flagging the Captures/Parked region. The capture's "also resolve against shipped slugs via LOG" half was dropped: a Blocked-by pointing at fully-shipped work is a genuine unpark signal, not noise. The parked [lint-citation-refire] was kept parked rather than folded in — its fix is a different kind (a citation-marker convention, not a parsing fix) — and its trigger re-pointed to fire after [lint-overflag-fixes] ships, so citation noise can be judged once the louder false-positives are gone.

The /plan-recommendation-overwhelm capture was parked as [bundling-jargon-recurrence], a recurrence-watch: the rules it broke already exist, and it landed on a possibly-pre-[verbosity-output-style] host, so it is evidence to recheck after reinstall, not new rule work.

The on-device-verification capture split into a red flag and two fixes. The user judged Claude's silent access of their connected phone (over adb, without asking) a privacy red flag — raised as [device-access-consent], open — with its fix queued as [ask-before-device-use] (verification planning must ask before using a connected device). The capture's second thread, /done folding in new build and design work mid-close, became [done-midclose-scope]: build-completing verification fixes fold in, new scope routes out.

Three discussion threads followed. Test-batching: the user was near "codifying" that Claude-run tests collect as batches. Confirmed against the Taskflow record that the batched TaskDao tests are instrumented and device-dependent, deferred 2026-05-24 — about 3.5 weeks before a device was known (2026-06-17), the same assumed-no-environment root as the red flag. Resolved as a third test category the inline-vs-user-run rule never named, mostly downstream of the env-ask fix; the unified test queue the user proposed largely already exists as Deferred tests, with the why-pipeline preserved by source-slug. Documented as [test-routing-rule]. Test closes running full commit ceremony: corrected the user's "no git" to commit-only — the LOG and queue records must persist or the next session opens on a dirty tree — so [test-close-commit-only] has done-test.md override the push-ask like done-plan.md. The audit-vs-inline-read boundary: agreed the line is whether the read needs its own session, documented as [audit-vs-inline-read].

Two items parked for dedicated sessions: [reinstall-confirm-nudge] (a plain-English nudge to run a confirm session after a reinstall, blocked on [deferred-test-seams-fix]) and [consumer-plugin-feedback-channel] (a channel for a consumer's Claude to route plugin observations back to the plugin instead of into their own app queue — needs the cited transcript read plus research).

**Queue changes:**
- Added 7 batches: [reader-test-registry-update] (above the push gate — ships with the REGISTRY retirement); [ask-before-device-use] and [done-midclose-scope] (first two after the gate); [test-routing-rule] (after [deferred-test-seams-fix], depends on it) and [test-close-commit-only] (after it); [lint-overflag-fixes] and [audit-vs-inline-read] (end of Batches).
- Raised red flag [device-access-consent] (open) in the Red flags section.
- Re-pointed [lint-citation-refire]'s trigger to fire after [lint-overflag-fixes] ships.

**Captures routed:**
- Promoted: the two lint captures → [lint-overflag-fixes]; on-device capture → [device-access-consent] + [ask-before-device-use] + [done-midclose-scope]; reader-test capture → [reader-test-registry-update].
- Parked: [bundling-jargon-recurrence], [reinstall-confirm-nudge], [consumer-plugin-feedback-channel].
- Three close-out discussion threads produced [test-routing-rule], [test-close-commit-only], [audit-vs-inline-read].
