# [HASH] — Processed the /next run-boundary fix cluster into Processed and cleared it to run

This session set up a buildable /next culminating in a test rezip. The run-boundary bug — /next ending the whole run at the first `[user]` item, so cleared Claude work after it never builds — had a three-capture fix cluster waiting in Unprocessed, recommended by last session's advisory. Processed all three together: `[next-run-terminates-at-user-item]` (rework next.md's run definition and handover branch so a `[user]` item is handed over, preferably at the run's end, and the run continues through remaining cleared Claude work) and its two placement siblings, `[reorder-home-is-plan-close]` and `[audit-placement-after-build-blocks]` (the /plan-close reorder in done-plan.md gains the rule that `[user]` and `[audit]` items sit end-preferred after contiguous build blocks). Confirmed the build boundary that keeps plan/next clean: the fix changes only how /next *treats* the order it's given and adds no reorder logic to /next — reordering stays the /plan close's job. Consumed `[next-build-advisory-4]` and folded the two supporting captures (`[verso-transcript-not-bug-evidence]`, `[next-nontermination-fix-standalone]`) in as build guidance. The session also opened with a marketing detour — reframing SI's pitch around the felt benefit — which was externalized out of this project to `flintcraft.tech/MARKETING-NOTES.md`, since it's Flintcraft.tech work, not SI method work.

**Queue changes:**
- Moved three items from Unprocessed into Processed, above the readiness line, grouped ahead of the `[user]` items so /next builds them first.
- Consumed and cleared the forward-recommendation advisory `[next-build-advisory-4]`.
- Left `[verso-transcript-not-bug-evidence]` and `[next-nontermination-fix-standalone]` in Unprocessed (their guidance is folded into the build notes).

**Work processed:** kept (moved to Processed) — `[next-run-terminates-at-user-item]`, `[reorder-home-is-plan-close]`, `[audit-placement-after-build-blocks]`; advisory `[next-build-advisory-4]` cleared.
