# 8e20122 — Two sentences cut from next-audit.md, and the one kept on an asymmetric-cost argument

`next-audit.md`'s present-findings step explained that presenting findings as one numbered set is the bulk-approval inversion, and why — the findings being a deterministic result set under criteria the user already approved. The always-loaded inversions block states exactly that and names an audit's findings as its own worked example, so the clause was a restatement and is gone.

The paragraph's last sentence stays: that this keeps the always-show rule intact, since the user reads every finding's wording before any of it is filed. That reconciles two rules against each other, which the parent block does not do.

Recorded because it was close and the question recurs: by the strict bar there is no recorded instance of anyone misreading bulk approval as breaching show-first, so by the letter the sentence should have gone too. It was kept on an asymmetric-cost argument the user endorsed after being shown both readings — wrong to keep costs fifteen words, wrong to cut risks a later session "correcting" the step into approving findings one at a time, which would reverse the rule entirely. That reasoning applies where a misreading would reverse a rule, not merely obscure it.

The open question about the contested-findings step below it is answered no. Its body is mechanics the parent is silent on — the reword-or-drop routing, and waiting for each call before presenting the next — so cutting it would leave those with no home.

**Files touched:** `plugin/throughliner/docs-b/next-audit.md`.

**Routed to Captures:** none.

Rule gate: not needed — this evicts rationale and authors nothing. The operative instruction is untouched.
