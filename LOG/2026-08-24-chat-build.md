# 3ed3db1 — Chat-level record for the 2026-08-24 build marathon: 18 rule amendments and the FAQ rebirth built, the audit run, four walk-throughs addressed, and nine captures from live testing

One /next run built all 18 cleared build items and ran the compliance audit, then walked the run's four [user] items: [competition-comparison-article] resumed from its record, its never-sent external review voided, the Papi draft recovered from a prior session's temp folder and saved by you, then deferred; [adhd-memory-prosthetic-article] and [discord-post-session-start-strength] deferred with it under [articles-wait-for-faq-cycle] — your reason: drafting without reference material on how Throughliner works is exhausting, and the announcement-driven FAQ shipped this session will accumulate exactly that; [cycles-due-check-verification] walked partway, finding two defects, and deferred at your call with its clean opening test still unrun.

**Also in this chat:** the cycles verification surfaced a live method contradiction (plan.md instructs a CYCLES.md write the scope-lock always denies — [cycles-write-refused-by-scope-lock]) and a check failure (the /done-site due-ness check filed nothing over an overdue cycle — [cycles-close-check-did-not-fire]). Two inbound reports were read, filed and archived: Polite Fart Announcer's formal report of the same cycles defect, and Chagora's visibility/forks report ([setup-visibility-remote-and-forks]), whose owed reply was drafted at this close. You raised two designs ([plan-open-github-issue-check], [subprojects-pop-out] — the latter with upward-only one-deep dependencies and mediated cross-queue writes as its experimental parts), one consumer observation on delete-asks ([spent-item-delete-narration]), one on /rescan in planning chats ([rescan-offers-processing-in-plan]), and one mixed finding from a consumer build refusing your directed queue move with a "take the consequences" dare ([build-refuses-user-queue-move]). A field confirmation needing no change: a consumer session produced the close-amend offer-the-fold behaviour on a host older than the commit that named the rule — evidence the wording matches what Claude does unprompted. The new --replace-in operation got two live uses within hours of shipping, both on lint flags. Hook suites: all 20 green via py. Rule checks: five run; the audit-lag line names a2ce359, which this session's audit record covers and supersedes once committed — nothing else found. Session records were checked against the scrub checklist and the credential scan, and nothing more.

Routed to Captures: [plan-open-github-issue-check], [subprojects-pop-out], [articles-wait-for-faq-cycle], [cycles-write-refused-by-scope-lock], [setup-visibility-remote-and-forks], [spent-item-delete-narration], [rescan-offers-processing-in-plan], [cycles-close-check-did-not-fire], [build-refuses-user-queue-move] (plus the audit's six, listed in its own record)
Advisory: filed — [articles-wait-for-faq-cycle]
