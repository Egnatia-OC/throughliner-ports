# [HASH] — Late build run on 1.21.0: 19 items built, one audit, one halted, four user items closed and three deferred

The chat-level record for the second build run of 2026-08-26. Each built item has
its own entry; this holds what belongs to no single item.

## The halted item

**[setup-case-d-untagged] was not built.** It asks for response-shape tags on
`setup.md`'s Case D, and `setup.md` opens by declaring in bold that it stays
tag-free, with a stated reason: /setup runs both on fresh adoptions where the
tag-defining rules are not loaded and inside adopted projects where they are, and
one text cannot carry markers meaning something on one run and nothing on the
other. Building the item as written would contradict a standing decision recorded
in the file it edits.

The doc already contradicts itself — five steps carry tags today — so Case D is
untagged like most of the doc rather than uniquely so, which weakens the item's own
framing. That makes it a design question rather than a wording fix, and the two
coherent answers pull opposite ways: repeal the declaration and tag the doc
properly, or honour it and strip the five existing tags, which would make the item a
no-op. You agreed it belongs at planning. The item is left in Processed untouched
and the collision is filed as [setup-md-tag-free-declaration-contradicted].

## What the walk-through pass cost you, and what came of it

Four `[user]` items closed, three deferred, one drafted and waiting on a clear
posting day. Two of those closures needed nothing from you at all: the release pick
closed on its observable (v1.21.0 published, `#beta` resolving to the release
commit), and the onboarding post's pop-out claim closed because the released commit
carries Case D, so the claim is now true — the register line was corrected here
rather than asked about.

**Two failures in that pass are yours to have been on the receiving end of, and
both are filed.** The cycles verification handed you a cleanup step in a different
project after the verification itself had already passed, stalling a build that did
not wait on it — filed as
[walkthrough-hands-over-cleanup-that-stalls-the-run], with your framing that you do
not want it happening to consumers. Then the run presented you two Discord drafts
you had posted hours earlier, because the only record of that item said "deferred"
— a line written before you posted, by the session that handed the drafts over,
with nothing running afterwards to correct it. Filed as
[walkthrough-represents-work-completed-outside-a-session].

The second has a sting worth recording: that "deferred" line is one of the lines
this same session's audit had flagged that morning, in
[deferred-recorded-for-items-never-offered]. The defect found in the morning
produced a fresh instance of itself in the afternoon.

**And the fix for it collides with a standing rule**, which is why a third capture
followed. Minutes after filing the above, this run asked whether the next post item
was done rather than presenting another draft — a completion ask, which the method
bars in every skill. It produced the better outcome and it was still a rule broken,
so the collision goes to planning rather than being settled by a build:
[completion-ask-bar-collides-with-re-presentation].

## Corrections and course-changes in this chat

- **A wrong line deleted and restored.** Building the three rationale evictions, the
  run cut an adjacent sentence in `plan.md`'s delete branch rather than the one the
  item named. Caught at the close while reading the item's reasoning back; the named
  line was removed properly and the out-of-scope deletion reverted.
- **The gate-line defect fired twice on this run before its fix was built** — a new
  tick landing between another item's depth and gate lines, twice repaired by hand.
  Live evidence for [gate-line-in-working-file-is-positional], recorded in that
  item's entry.
- **Two hooks refused this session and were right both times.** A `sed -i` on a test
  file was blocked by the scripted-write guard, and a direct edit to a capture's
  text in QUEUE.md was blocked as planning work rather than a build's. Both were
  done the correct way instead.
- **One capture filed this session was spent within it.**
  [expedite-release-item-stale-in-ready-region] reported the release-pick item as
  stale in the ready region; that item was closed later in the same run.
- **You raised a rule for promotion.** The host-only rule that a planning session
  must not leave a build to infer — currently justified by this project's
  Fable-plans-Opus-builds split — should probably ship in the method. Filed as
  [build-blocks-must-not-require-inference], with the note that the reason
  surviving generalisation is the session boundary rather than the model names.

## Session facts

**Rule checks:** five run, nothing found.

**Hook suites:** all run before committing, since the staged paths include
`plugin/throughliner/hooks/`.

**Advisory:** filed — setup-md-tag-free-declaration-contradicted.

**Routed to Captures:** release-ritual-suite-step-stale,
build-blocks-must-not-require-inference,
setup-md-tag-free-declaration-contradicted,
walkthrough-hands-over-cleanup-that-stalls-the-run,
walkthrough-represents-work-completed-outside-a-session,
completion-ask-bar-collides-with-re-presentation,
walkthrough-step-used-unexplained-term-of-art.

**Staleness sweep:** one finding, deferred as a fate decision —
[expedite-release-item-stale-in-ready-region], filed this morning, was spent
within the same session when that item closed.
