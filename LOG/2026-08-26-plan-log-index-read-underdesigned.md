# aa78310 — The orientation read redesigned: a derived window replaces the bare five, an intersection test replaces "relevant", and a required line makes silence visible

Each gap got the treatment the method already uses elsewhere. The window is the index lines newer than the most recent planning session's record — the dispositions listing's own window, derived rather than invented. Relevance narrows to a checkable intersection (a line is folded in when it names a slug or file the current queue also names), preferred over better judgment-wording on Alex's observation, recorded on the sibling item, that a session deliberately denied material cannot judge relevance. And the read gains the required-artifact shape: one line either way in the opening narration. Keeping "five" with a bolted-on derivation was refused — no defensible derivation exists for a fixed count across projects with different session rates. Gate: run — amendment to the read-state step's orientation read; the bare five and the untested "relevant" are evicted. The [discord-post-session-start-strength] post was held below the line behind this item, so it cannot announce the underdesigned version; it lifts when the redesign ships.

**Queue changes:** kept and cleared with the builds; the post item moved below the line with a `Blocked by:` naming this.
**Work processed:** kept — [plan-log-index-read-underdesigned].
