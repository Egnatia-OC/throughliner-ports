# [HASH] — Fable-plans / Opus-builds split written into the fresh-sessions rule as two subordinate action clauses

Alex's reasoning, given when she raised this: the note is needed so a planning session knows anything the build model would otherwise have to infer must be written into every build block, and so it knows it is the one in charge of every decision — a capture filed during a build is weighed knowing that session had less full-project oversight than the planning session reading it.

Written as an amendment to CLAUDE.md's "Design for fresh, short sessions" rule, its named parent, which already names the weaker model as the design target. It takes the shape of two subordinate units under one bolded lead rather than a freestanding rule, so it spends no slot: what to put in a build block, and how to weigh a build-filed capture at the keep-step. Both are stated as actions rather than as a description of which model runs where, which is what the item's acceptance asked for.

One thing found while writing it and left alone: the same bullet's closing parenthetical still says 4.8 is the model the plugin is tuned for, which stopped being true when docset A was retired on 2026-08-09. Repealing a sentence is a keep-step decision and this item named only the amendment, so it was captured rather than fixed.

Files touched: `CLAUDE.md`
Routed to Captures: [fresh-sessions-rule-names-retired-model]
Rule gate: run — amendment to the fresh-short-sessions rule, its named parent; subordinate to it, no freestanding slot spent, nothing evicted.

Depth: short. Ticked as done, confirmed.
