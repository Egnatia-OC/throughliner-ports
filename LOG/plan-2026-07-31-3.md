# [HASH] — /plan: queued 4 method builds + merged a corpus-wide dedupe/why-clause audit and prioritised it; reframed concurrent-session support (Drive is user-specific) then skipped; lifted reorder-cost, cleared 2 spent advisories

This /plan opened on a queue whose whole below-line region was gated on the emergency [user-work-surfacing-fix] — which had shipped (`ae92d34`) and rezipped to test8 (`ec021a4`), confirmed against the shipped next.md handover branch (now leads with the walk-through). That unblocked the shelved work and spent two advisories.

Kept four builds: **[next-build-context-self-detection]** (remove next-build.md's false context-self-detection framing, matching its shipped sibling); **[inseason-moves-bypass-mover]** (route /plan's skip-to-defer moves through the mover — scope narrowed to skip-to-defer only, since [queue-move-viewer-lag]/`3ca0e2e` already rejected a byte-move for the re-authoring keep-step); **[cli-tool-web-search-trigger]** (a nudge-then-search trigger so Claude reaches for a CLI tool before a GUI walkthrough); **[log-file-sort-heuristic]** (date-prefix LOG filenames, full migration + index-link lockstep).

Merged two below-line audits into one corpus-wide **[method-docset-dedupe-why-audit]** at the user's request — run the dedupe/why-clause work sooner and across the whole doc set, because a rule restated in another doc is only findable with the corpus read together. Held the dedupe-not-tersify line (the docs' length is mostly load-bearing why-clauses).

Reframed **[concurrent-session-support]**: Drive-sync conflict-copies are Alex's personal environment, not a method-general blocker — most SI users aren't on Drive, so in-repo concurrency is solvable in-method and a structural safeguard is worth building. Claude's proposed detect-and-warn design was judged "all wrong" by the user (no time to explain), so the reframe + rejected design were captured into the item and it was skipped to the end for a later /plan.

**Queue changes:** kept 4 builds above the line; merged+prioritised the corpus audit; lifted [reorder-cost-vs-dependency-model] above the marker (its "after the emergency ships" lift-condition met); cleared advisories [advisory-build-user-work-fix] and [rezip-before-verify-advisory]; skipped [next-start-order-handoff] (waits on reorder-cost building) and [concurrent-session-support].

**Work processed:** kept — next-build-context-self-detection, inseason-moves-bypass-mover, cli-tool-web-search-trigger, log-file-sort-heuristic (builds), method-docset-dedupe-why-audit (new merged audit). deleted — plugin-behaviour-debloat-audit, flavor-doc-core-factoring-audit (subsumed). skipped — next-start-order-handoff, concurrent-session-support. not reached — fable-docset-model-detection, throughline-feature-elevation, session-sizing-and-break-lines, migrate-skill, self-hosting-auto-detection.

**Routed to Captures:** below-line-lift-condition-gap (7 below-line items lack lift-conditions — a future /plan sorts them).
