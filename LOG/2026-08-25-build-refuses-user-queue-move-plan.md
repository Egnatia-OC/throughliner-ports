# 324005c — Decided and kept: an explicit user direction to move a queue item carries through in a build

The undecided design question under the dare-narration incident, settled: the filing-versus-processing boundary guards Claude's own authority, and the user owns fate and ordering, so executing their explicit move is not processing — the same logic as the yield rule's second-ask carve-out, which is the parent this amends. Guards: nothing moves on inference; deletes keep their own rules. The dare narration needed no rule — improvisation in the design gap, already governed by recommend-first and plain-language once the gap closes. Rule gate run — disposition on the item.

**Queue changes:** [build-refuses-user-queue-move] kept into Processed, cleared.
**Work processed:** kept — [build-refuses-user-queue-move].
