# 9925443 — /plan: evaluated the sov.zip repo rebuild and folded it into the queue-split design capture

The user received a second round of outside feedback that SI is too context-draining — this time as a complete rebuilt copy of the whole repo (`sov.zip`), with no explanation beyond "try this." This session evaluated it rather than adopting it. The rebuild is uncommitted working-tree changes on the v1.12.0 snapshot from 2026-06-14, so it predates the Taskflow audit arc and recent captures. It splits QUEUE.md (875 lines) into a slim 23-line QUEUE.md plus BACKLOG.md, CAPTURES.md, DEFERRED-TESTS.md, and 60 per-batch `plans/<slug>.md` files, and rewrites every procedure doc and all three hooks.

The verdict against adopting it: it aims at the wrong target. The only every-session context cost is plugin-behaviour.md (injected each session), which the rebuild barely touched (3,074 → 3,000 words, a 2% cut); its real cuts hit plan.md (−66%) and QUEUE.md, both read on demand — so per-session drain is essentially unchanged. It also splits captures and deferred-tests into separate files, the exact move the existing capture's hard constraint rules out, since both can block batches. The one usable seed is the per-batch `plans/` shape, which independently corroborates viable direction (3) already named in that capture.

Rather than open a new capture, this session folded the rebuild as a "second external input" data point into the existing "Make QUEUE.md easier to handle" capture. It reinforces that capture's conclusion — keep the dependency graph consolidated, and the real cost is the every-session doc — without changing it, and points the eventual design session at `sov.zip` as a concrete reference artifact.

**Queue changes:**
- Amended the "Make QUEUE.md easier to handle" capture (unprocessed, in Captures) with a two-paragraph "Second external input — a full repo rebuild" evaluation of `sov.zip`. No batches added or reordered.

**Captures routed:** none — this session enriched an existing capture; no promote/park/drop.
