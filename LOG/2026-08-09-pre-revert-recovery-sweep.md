# 72f4fe9 — Two-pass sweep of the 65 reverted commits: nine findings filed as five captures, three of them live safety gaps rather than design debt

The emergency revert unwound 65 commits carrying 214 shipped items and separately discarded 31 designed-but-unbuilt work items. Four recoveries had already been queued by hand, each found by chance when a queue item's premise turned out to be stale — which is not a method, it rediscovers the same loss one accident at a time. This audit replaces that with one pass.

The governing constraint shaped every classification: recover the value without re-importing the bloat. The revert happened because plugin-behaviour.md grew from 6,162 to 21,445 words, so restoring the lost week wholesale would restore that. The split that makes the constraint hold by construction rather than by anyone remembering it: artefacts that **cannot** bloat a doc — code, tests, config, gitignore entries — are restored directly, because re-deriving a bug fix from its LOG entry would be strictly worse than taking the fix, and no gate applies where there is no prose to admit. Prose and rules are **never** restored as diffs; the audit's output for them is a work item stating the substance, which then passes through the new admission and eviction tests like any other work.

Pass A read the LOG index at the tag, classifying from the line and opening the full entry only where the line could not decide. The mechanical diff gives **213** lost index lines rather than the 214 the item estimated. Pass B diffed the queue slugs and found exactly **31** present at the tag and absent now, matching the item's count — and that pass is the one whose omissions are unrecoverable by any later method, since the queue file is those items' only record. Pass C judged the three unrestored work-plan documents.

Nine findings, all approved as drafted, filed as **five** captures. The consolidation follows the destination rule rather than convenience: five findings whose fixes are unambiguous repairs with no design call left in them collapse into one work item with numbered points, while findings carrying a decision stay standalone so each can be ordered, deferred or dropped on its own.

The three most consequential are live safety gaps rather than design debt, and that was the surprise of the pass. The subagent cost ask-gate is silently absent — `hooks.json` registers the matcher as `Task` while this harness names the tool `Agent`, so a guard that exists because one silent fan-out exhausted the user's usage in a single run currently never fires. `session_start.py` silently writes the user's absolute path into their repo, undocumented and uncovered by gitignore, in a project that sits inside a synced Drive folder. And the session-start payload is roughly 54,886 characters against its own documented 10,000 cap, having been reduced to 3,288 pre-revert by a change whose entire substance was a deletion.

Two findings are corrections in the strict sense — right text replacing wrong text, nothing for the admission gate to weigh. Three queue items currently sit on premises the pre-revert history already falsified: the old-plugin-history claim (the May-2026 history survives here as `v17`–`v157` orphan tags), the dead-line-anchor claim (links open the file at the top, ignoring the anchor — user-verified), and the working-mode verification (the mechanism was retired at its premise, the field having recorded reading appetite rather than location). And the GitHub repo was renamed outside the method, so every install instruction now names a repository that only resolves through a redirect.

Bucket 3 was the whole difficulty, exactly as the item predicted. Five prose rules survived on recurrence evidence rather than plausibility — outbound correspondence (already recurred since the revert), a lint-checked dependency field, the red-flag backstop in /next that grep showed was never implemented despite SPEC promising it, page-the-whole-queue-before-reasoning (confirmed live this run, which read QUEUE.md truncated at 195 of 363 lines), and check-our-own-conformance-before-blaming-the-tool. Their substance is recorded; their wording deliberately is not.

Counts, kept honest because "all 213 were reviewed" means nothing otherwise: 47 lines are session, close, merge, advisory or hash-backfill records carrying no recoverable change; 19 are code-only; 106 touch a procedure doc. Roughly 80 prose rules and clauses were dropped, counted rather than enumerated — they pass no admission test on the evidence available. Four already-queued recoveries were recognised and skipped rather than re-filed, as the item directed, along with `[restore-shell-write-denial]` filed earlier in the same run.

Pass C's answer is a refusal: the three work-plan documents stay unrestored. All three are plans of work in side documents, which /next never reads, so their steps silently fall through — the exact anti-pattern the method names. Where the work is still wanted it comes back as queue items, with the differential audit's own conversion into a queued `[audit]` run at `34a229e` as the precedent.

One input the audit did not derive for itself: `[emergency-revert-playbook]`, filed by another session while this run was building, independently reached three of the same conclusions — restore-what-cannot-bloat first, the LOG index as the primary recovery instrument, and shipped-versus-unbuilt as two populations needing different routes. Reading it first is why those three were acted on rather than re-derived.

**Files touched:** none — the audit edited nothing. Read: `LOG/index.md` and `QUEUE.md` at `pre-revert-2026-08-09`, against their current versions.

**Routed to Captures:** 5 — `[restore-reverted-code-fixes]`, `[queue-items-on-falsified-premises]`, `[repo-identity-reverted-to-old-name]`, `[re-author-five-evidenced-rules]`, `[work-plan-docs-stay-unrestored]`

**Approval outcomes:** all nine findings approved as-is; none dropped or reworded.
