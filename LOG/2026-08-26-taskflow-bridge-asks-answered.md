# aa78310 — Taskflow's two mail captures merged into the bridge item and deleted: the three answers, and the queue-side handoff question, now ride [taskflow-personal-bridge]

Two information captures from Taskflow's replies were merged into [taskflow-personal-bridge] and deleted, each with its content carried across in the item's own words. The three bridge answers: the bridge breaches no rule of theirs (a SPEC sentence drawn), a separately named additive import is designed (inserts only, no dedup — a visible duplicate beats a silently missing task), and exports will carry completion state with a parent's derived roll-up. The multipart-handoff capture's half — what this queue does with a `[user]` item whose parts moved onto a to-do list — became a named second question on the bridge item's keep, with the sent register's intent field as the candidate machinery (*for completion* can clear; *for continuation* leaves the line carrying a note) and their mid-walkthrough flag as the constraint. Load-bearing on all of it: their side is settled and unbuilt, so the bridge stays unbuildable until their export and additive import ship. The item comes off its date-hold 2026-08-28.

**Queue changes:** [taskflow-personal-bridge] rewritten to carry both; two captures deleted.
**Work processed:** deleted by merge — [taskflow-bridge-asks-answered], [multipart-user-handoff-queue-side].
