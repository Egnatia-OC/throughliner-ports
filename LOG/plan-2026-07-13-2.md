# [HASH] — /plan: queued freeform retirement + /done handmade-work absorption as two release-prep builds; filed release-gate captures

A /plan session aimed at release-readiness, working the user's priority items. Two items processed and kept as cleared builds; the rest deferred when the session closed early.

**[git-panic-on-handmade-edits]** kept and expanded into a build with two jobs: a don't-panic behaviour rule (uncommitted edits Claude didn't make are read as expected user work, not breakage — wording in plugin-behaviour.md, not a hook), and /done absorbing standalone handmade-work wrap-up (run /done with no prior /next → log + commit hand edits), which is what lets freeform retire. Added refinement: when standalone hand edits span several logical changes, /done uses judgment to write a separate LOG entry per change rather than one lump — granularity defers to [log-index-unit-two-section].

**[freeform-retirement-question]** resolved: retire freeform, both the `/next freeform` mode and the `[freeform]` flavor, kept as build [retire-freeform]. Reasoning: its three claimed uses are redundant (ad-hoc audits → first-class `[audit]`), covered by /plan (discussion-first sessions, and freeform's discussion licence actively caused a plan/next boundary breach), or absorbed (handmade wrap-up → /done via the git-panic line). User added that treating freeform as *required* for ad-hoc changes would pile on ceremony. Keeping freeform was weighed and rejected. This build also carries Half C of [two-section-model-reconciliation] (name `[audit]` first-class in SPEC), dissolving that entanglement; the user chose two separate adjacent build lines over one combined line.

The cleared-to-run marker was repositioned so both new builds sit above it (git-panic first, since retire-freeform depends on it); [two-section-model-reconciliation] moved below it with its Half C marked resolved-in-direction and only the Half B fork open.

Items not reached (session closed early): the README/feature-list sync [feature-additions-require-readme-edit], [self-hosting-reproducibility], and the release-point + marketing discussion. A workflow finding surfaced: the release-gate `[user]` verification lines sit below the cleared-to-run marker, so /next never hands them over — filed as [next-user-work-not-surfaced]. And the merged plugin is already installed live (host = target = 1.15.0-test2), so [reinstall-merged-plugin] appears done and the verification pile is runnable — filed as [reinstall-already-done-confirm].

**Queue changes:**
- Kept: [git-panic-on-handmade-edits] (build), [retire-freeform] (build, from [freeform-retirement-question]) — both cleared to run
- Filed to Unprocessed: [next-user-work-not-surfaced], [reinstall-already-done-confirm]
- Repositioned cleared-to-run marker; moved [two-section-model-reconciliation] below it
- Normalized 3 pre-existing prose-provenance lines to canonical form (lint cleanup)

**Work processed:** kept [git-panic-on-handmade-edits], kept [retire-freeform] (from [freeform-retirement-question]); none deleted

**Routed to Captures:** [next-user-work-not-surfaced], [reinstall-already-done-confirm]
