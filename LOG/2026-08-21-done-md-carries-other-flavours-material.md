# [HASH] — The close-doc distribution pass kept, held behind the two freeform passes

Kept with all three questions settled: what moves is the seven flavour-specific blocks the item's own table lists, each into its flavour's sub-doc; the entry template stays whole with pointers, refusing the four-divergent-copies risk; and it runs after the cut and law-prose passes so each paragraph is rewritten once and then moved as final text. Held below the line with `Blocked by: [cut-length-remaining-docs], [law-prose-remaining-thirteen-docs]`. The acceptance test is the item's own: nothing deleted, only moved. Build block on the item.

**Queue changes:** [done-md-carries-other-flavours-material] moved Unprocessed → Processed, held below the line behind the freeform pair.
**Work processed:** kept (held).
