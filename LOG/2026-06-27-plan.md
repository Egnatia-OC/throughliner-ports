# 758aaef — /plan cruise-control design session: resolved concerns (3),(4),(5) + the ask-gate capture, added concern (11), spun off the general [readiness-line] planning primitive

The plan-marker forced this session onto the [cruise-control] design notes. Worked the four self-contained open concerns and deferred the dependency-management cluster (2, 7, 8's sub-question) to [plan-dependency-tracing-gap], since designing them now would grade a foundation that batch is about to move.

The [autonomy-ask-gate-collision] capture resolved into concern (8): under autonomy the only permission "ask" the plugin itself creates is the subagent gate (push, device, destructive-git, and SPEC-change risks are handled by other concerns, not asks). Decision — cruise control uses no subagents by default, the exception being a build that matches one of three context-overflow cases. The user's correction drove this: a subagent is a token-cost tool, not a context tool, and she's never hit context overflow, so an "anticipate a subagent" rule would have Claude hunting reasons to over-ask. Grounded it in a researched list (resources/research/subagent-genuine-need-cases.md) so the trigger is a concrete match, not anticipation. A three-way "envelope" classifier I first proposed was dropped as over-built once she showed it reduces to subagents. Batch-split is named the go-to alternative but explicitly not the only one — she flagged that railroading to batch-split is the same over-reflex.

Concern (3): autonomy removes the human, not the steps — /done's existing no-skip rule covers the unattended run's throughput pressure; mechanical gates carry enforcement, and the judgment steps that end in a user approval get an unattended answer (wind-down re-scan auto-files; no push). Concern (4): dropped the "in the active scope" qualifier — any open flag blocks, which a hook can enforce and which reconciles the concern with the SPEC; added the mid-run-raised-flag halt and named the screen-then-gate limit. Concern (5): no claim-marking format — the standing "Claude-authored handoff content is unverified" rule suffices, because cruise control's input is the queue, user-vouched by construction; CLAUDE.md's interim caveat was dropped, closing the /goal-vs-cruise-control fork. Concern (11) (new): how a run is invoked and bounded — invoked explicitly, runs the queue top-down to a readiness line, no plan-time labelling; skill-vs-/next-mode left open with a lean toward a separate skill built by reusing the existing build/close docs.

The session's largest outcome was the user's reframe of the run-bound into a general planning primitive: [readiness-line], a "good to proceed above here" line /plan asserts every session (attended too), so Claude states the execution-ready frontier always rather than only when the user thinks to ask. Filed as its own parked item, decoupled from cruise control, best designed alongside [plan-dependency-tracing-gap].

**Queue changes:**
- [cruise-control]: resolved concerns (3),(4),(5) and folded the [autonomy-ask-gate-collision] capture into (8); added concern (11); concern (4) reworded ("in the active scope" → "any open flag").
- [readiness-line]: new parked capture (Blocked by [plan-dependency-tracing-gap]) — general "good to proceed above here" planning primitive.
- Plan-marker updated to point at [plan-dependency-tracing-gap] as the next required planning.
- CLAUDE.md "Handoff-claim provenance" rule: dropped the "(interim rule)" framing and fork caveat.
- Research filed: resources/research/subagent-genuine-need-cases.md.

**Captures routed:** [autonomy-ask-gate-collision] — resolved, folded into [cruise-control] (8), removed. [readiness-line] — filed as a new parked design item. [cruise-unattended-hook-signal] — filed from the wind-down re-scan; still waiting for a later /plan to sort.
