# 3affa20 — done-plan.md: lift-conditions must name repeatable future events, plus a Blocked-by clearing step at close (backwards-pointing excluded); plan.md below-line revisit gains a third outcome "spent"

The below-line revisit could not tell a live lift-condition from a dead one: it asks "has it happened yet?", which silently returns "no" both for a condition still waiting and for one whose anchoring occasion has already passed — so items sat below the line for weeks, unanswerable, with the revisit reporting nothing wrong. Two real instances were fixed by hand before this build; this is the design half. Three doc changes shipped, merged at processing with the sibling Blocked-by rot item because both are dependency statements outliving their truth and both land at the same close. (1) done-plan.md's recording rule: a lift-condition names a repeatable future event ("at the next rezip and restart"), never one occasion to piggyback on ("at the emergency's rezip") — stops new spent anchors being written. (2) The load-bearing half, plan.md's revisit classification: a fourth outcome beside cleared / user-only / still-waiting — *spent*: the anchoring event has passed, so the item is surfaced for its condition to be rewritten rather than skipped silently. This finds the instances already written. (3) done-plan.md's dependency-rot pass: at the close, a `Blocked by:` line whose named slug no longer resolves is removed and the removal narrated (if the item still waits on something external, that becomes a lift-condition — the clearing never strands an item). Explicitly excluded: a backwards-pointing `Blocked by:`, where the dependency is real and only the order is wrong — removing it would delete true information, so it is surfaced for a decision, never auto-cleared. The Blocked-by half needed no new detection — the lint already flagged stale lines on every edit across multiple sessions; what was missing was anything that *acted*.

Overnight blitz run (branch overnight-blitz-2026-08-05b): built under the blitz plan's sanctioned departures — approvals deferred to the branch review, no push.

**Files touched:** plugin/si-plugin/docs-b/plan.md, plugin/si-plugin/docs-b/done-plan.md
**Routed to Captures:** none
FAQ: not needed because the change is close-mechanics narrated in plain words when it fires; no consumer-facing question arises.
