# 47d8625 — chat-level record for the 2026-08-26 evening planning session: the release-day run order assembled on test20

The /plan that is itself the live test of the test20 rezip, per the sequence recorded on [expedite-first-beta-release]. The user redirected mid-run from full queue processing to the minimal set readying the next session: build a few items on test20, pick the release build at that run's end, then the release and its dependent posts. Six items processed (each with its own entry); 21 captures deliberately left unprocessed for a later /plan.

**Also in this chat:**
- **Lift:** [cycles-due-check-verification] lifted above the line — its blocker [cycles-check-fires-nowhere] built in the prior run and the host reinstalled at test20, meeting the recorded lift condition.
- **Advisory** from the build run surfaced (process [log-index-window-ripple-unfixed] first — followed) and cleared from Unprocessed.
- **Ready-list order set deliberately:** two builds, then the cycles verification, then the release pick, then the four release-dependent walk-throughs — the release pick moved ahead of the walk-throughs whose first steps need the release published, so the run cannot stall on an unpublished release. One wrong marker placement mid-reorder was caught by the queue mover's own guard and redone with the full-order form.
- **Selector supersession** written onto [weekly-release-cycle] and [beta-tester-pathway] (reasoning in the expedite entry).
- Rule-gate refusals: none since last session (the dispositions listing printed none in the window; two run-dispositions were written on kept items this session).
- The wind-down re-scan surfaced no unfiled captures.

Advisory: not needed — the next step is the build run the ready list already orders, ending in the user's release pick; nothing needs a planning orientation note.

**Queue changes:** ready list rebuilt to 8 cleared items (2 builds, 6 `[user]`); marker after [beta-install-smoke-and-post-edit]; SPEC edited three times in-session (log-index derived window; two cycles sentences).
**Work processed:** kept — [log-index-window-ripple-unfixed], [fresh-sessions-rule-names-retired-model], [expedite-first-beta-release], [beta-day-one-posts], [nerds-list-first-entry]; deleted (relocated) — [spec-sentence-cycles-opening-line]; lifted — [cycles-due-check-verification].
