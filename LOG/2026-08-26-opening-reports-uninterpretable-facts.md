# [HASH] — The backfill count gains its reading in the hook itself; the explain-everything generalisation refused

Sibling of the orientation-read redesign (`2026-08-26-plan-log-index-read-underdesigned.md`). The recognition the close already has moves into `session_start.py`: where every change to a LOG file matches the hash-backfill signature, the uncommitted-changes line names it as the automatic backfill and normal, instead of the bare count that sent Alex to another session for the answer. The generalisation — explain any fact you cannot interpret — was refused as an untestable judgment rule, the same defect this pair removes; further signatures earn recognitions one at a time. The "relevant" question was answered at the sibling. Gate: not needed — hook behaviour, no rule text; suites and direct-drive verification apply as with any hook change.

**Queue changes:** kept and cleared with the builds.
**Work processed:** kept — [opening-reports-uninterpretable-facts].
