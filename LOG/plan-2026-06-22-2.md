# [HASH] — Plan: promoted [checkpoint-post-share-ask] and [release-notify-via-github]; confirmed [approval-display-blockquotes]

This planning session opened with no captures waiting. Both batches came from things you raised in conversation.

[checkpoint-post-share-ask] tightens the /plan capture loop's turn shape — two findings folded into one batch because they edit the same plan.md steps. First, from the evidence-audit project: the first capture's presentation and the per-item checkpoint both end the message on a raw quoted block, which reads as Claude stopping mid-thought with nothing handing the decision back. So those messages should end on the ask, with the verbatim directly above. This refines the shipped [verbatim-at-checkpoint] decision (verbatim lands at the checkpoint) rather than reversing it — a short closing ask keeps the next turn's analysis on the next turn. Second, from Taskflow: when Claude already has a clear park or drop lean, it states the lean, asks "anything to add?", then re-states the recommendation in a separate turn, naming the route twice. Park and drop can't fold straight to an action the way promote does — promote's draft is the safety net, while park and drop are terminal — but the recommend doesn't need its own turn; it merges into the end of the exposition. The standalone recommend-and-wait stays for when the lean isn't clear yet.

[release-notify-via-github] gives your users a way to be notified when you ship a new version. GitHub supports this natively through Watch → Releases, so no custom infrastructure is needed for a first version. An email-list channel was weighed — better UX for non-coders — and deferred as the heavier option for later, alongside the parked [consumer-plugin-feedback-channel] contact form. Two pieces make the native route work: the README documents how to turn it on, and the release ritual starts publishing an actual GitHub Release, because Watch → Releases fires on a published Release, not on a plain push.

Close-out housekeeping: this session's own batch drafts were shown as wrapping blockquotes, the behaviour [approval-display-blockquotes] was waiting to confirm — so that deferred-test line is removed. The deferred-test roll scan otherwise cleared nothing: the installed host already matches the target version, so host-side lines confirm through normal use, and the two external marketplace lines still wait on a release push that hasn't happened.

**Queue changes:**
- [checkpoint-post-share-ask] added to Batches (end) — Build, plan.md.
- [release-notify-via-github] added to Batches (end) — Build: README.md, CLAUDE.md, faq-template.md + faq-index-template.md.
- Removed the [approval-display-blockquotes] line from Deferred tests (confirmed this session).

**Captures routed:** both batches were raised in conversation and promoted; no pre-existing captures were waiting.
