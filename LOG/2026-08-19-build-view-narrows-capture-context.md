# [HASH] — the consumer's build-view objection settled into the design it bears on, and the capture deleted into it

A consumer project sent evidence deliberately ahead of the change: during a `[user]` walk-through a long aside produced two good captures, and their claim was that none of what made them good came from the build. The capture had already narrowed that claim honestly, and the narrowing was checked rather than accepted. Two of their four limbs land — knowing the captures were not already filed, and knowing what queued work depends on the detail — and both are queue-wide knowledge. Reading SPEC and knowing how items get processed are unaffected, since /next reads SPEC at run start and the procedure docs load either way.

Their sharpest point survives the narrowing: the aside happened during a walk-through, which is the phase where the user is talking freely and unprompted material arrives most, so a context cut applied evenly across a run bites hardest exactly where the raw material is richest.

The mitigation taken is the cheap one already named as a candidate inside the host item — the view carries the whole queue's headings and slugs and none of the history, which restores duplicate-detection at almost no cost. What it does not restore is the second limb, and that is recorded rather than papered over: a heading says an item exists and says nothing about what it depends on, so the cross-item awareness behind the second capture stays lost. Routing walk-throughs to a planning session was refused rather than deferred, because concurrency is held below the line behind the very item it would mitigate.

The fold was performed as a **merge** rather than an append, on the rule settled here two days earlier: the host item's read-this-first paragraph was rewritten so the objection, the mitigation, the refusal and the unfixed limit sit in one place, and what came out was the earlier paragraph's open framing. The capture was then deleted into it, and SPEC's derived-view sentence rewritten to say the view carries headings and names with no reasoning, and what that still costs.

**Queue changes:** [split-the-cleared-region-for-concurrent-sessions] rewritten to carry the settled mitigation and its stated limit; SPEC's derived-view sentence rewritten.

**Work processed:** kept — none. Deleted — [build-view-narrows-capture-context], its content merged into the host item.
