# d7ea8a0 — The stale-refusal repair had no live text to land on: its target was built the same day it was filed

Deleted. [plan-entry-split-wording-disagrees] was built later on 2026-08-21 (its record exists at `cc33c1e`), so the queue prose this capture would have corrected left the file with it — and that record's own index line already states its transcribed disposition is "now partly inaccurate", so a retrieve meets the warning before the stale description. LOG history stays as written by convention. The one-sentence edit had nowhere to go.

**Queue changes:** item deleted, premise expired.
**Work processed:** deleted — [plan-entry-split-refusal-describes-old-mechanism] (target already built and annotated).
