# 2625fa0 — Kept and cleared: the /plan-only capture-offer block leaves the always-loaded file for plan.md

Eviction by relocation. plan.md's process-now section was compared clause-by-clause against the always-loaded block and carries everything it does, so the copy deletes with nothing relocated; the close-by-who-raised-it rule and the already-agreed carve-out stay, since both genuinely fire everywhere. Two references repoint to plan.md. Build block on the item.

Rule gate: run — the always-loaded file's own admission test is the ground.

**Work processed:** kept — [plan-only-rule-in-always-loaded-file], cleared, grouped with the other skill-nonspecific-rules.md edits.
