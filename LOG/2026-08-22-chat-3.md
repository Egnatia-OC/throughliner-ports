# [HASH] — Chat-level record for the 2026-08-22 planning session: 27 items processed, cycles designed, the release model rebuilt around channels

A full planning pass: 21 items kept (20 cleared, 1 held), 6 deleted, the queue's whole live backlog processed. Seven SPEC edits landed in-session with the user present: one-commit-per-close, the throughline restated on the vouched-versus-private ground, the INBOX delta against live messaging, the advisory replace branch, the managed-block carve-out, two history passages removed, and the new cycles paragraph. Per-item reasoning is in each item's own entry and, for kept work, on the queue item.

**Also in this chat:**
- The user asked what the brevity style makes redundant ("probably caps"); answered from the record — the dedup audit found the two tone rules, and the caps (median bounds) govern documents rather than chat, so the style cannot replace them.
- The cycles concept is the user's; the definitions/position split, observable-driven due-ness and capture-filing pattern are Claude's design, agreed in discussion. The repealed never-re-propose-the-cycle instruction was cited as permitting the proposal.
- The three-channel release model settled on the user's terminology question: day-to-day rezips are dev builds; each Wednesday's pick is the beta (Discord announcement, GitHub pre-release, the user as first tester); the week-aged pick promotes to stable and goes to the community marketplace listing. Her sequencing, revised once in-session: beta channel and community listing launch together (the listing may be how testers arrive), YouTube videos after both — videos without a listing look bad to viewers, and YouTube may bring the first users. Advertising parked.
- Marketplace research run and filed as `resources/research/claude-marketplace-listing-paths.md` with its index line: official marketplace is the only in-app-default surface, curated with no self-serve path; community marketplace takes form submissions with automated plus human review; names are immutable slugs; the weekly-update cadence question (lightweight refresh vs re-review) is flagged open.
- Captures filed outside processing: [marketplace-submission], [beta-tester-pathway] (rewritten same session around the channel model), [powershell-append-mojibake] and [heading-leading-tag-collision] at the wind-down.
- Error made and fixed: three queue appends went through a PowerShell split that read UTF-8 without declaring it, mangling em-dashes; repaired by replace-all and verified against raw bytes. Filed as a capture.
- The [user-walkthrough-missing-from-view] heading began with a literal `[user]` tag as its subject and mis-parsed as a walk-through flavor; reworded at the close, filed as a capture.
- Standing context: the host still runs the pre-rezip build all session; the evening rezip plus full restart is the gate on all deferred host-side verification.
- This session ran across one calendar day.

**Queue changes:** 20 items cleared in dependency-grouped order (always-loaded evictions first, hook work grouped by file, the two Discord walk-through posts last); [weekly-release-cycle] held below the line; the spent advisory cleared at the opening and a new one filed at this close; Unprocessed left holding two date-held captures, one settler-waiting capture, the marketplace/beta pair, two wind-down captures and the advisory.
**Work processed:** kept — 21; deleted — 6; see per-item entries.
**Routed to Captures:** [marketplace-submission], [beta-tester-pathway], [powershell-append-mojibake], [heading-leading-tag-collision], [forward-advisory].
Advisory: filed — [style-dedup-evictions]
Rule gate: run — dispositions written per item at the keep-step; see each item's `Rule gate:` line.
FAQ: not needed at this close — the FAQ-bearing changes (cycles entry, throughline and advisory restatements) ship inside their cleared build items, which carry the template edits in their Files lines.
