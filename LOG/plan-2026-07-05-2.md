# 3a96208 — Filed [keep-x-identity-until-merge]: the redesign plugin keeps its `-x` identity until merge, so dogfooding installs the fork alongside main SI rather than collapsing onto it

A /next pre-flight hit the `--- Cleared to run above this line ---` marker at the top of Batches — nothing is cleared to run, because the redesign's middle batches (hooks, /next, /done, templates) have shipped and the final batch [fresh-queue-clean-break] is gated behind dogfooding the rebuilt plugin. Working out *how* to dogfood surfaced a mismatch: the installed host is the main plugin (`flintcraft`/`sovereign-implementer` `1.15.0-test1`), while this branch is a separately-named fork (`flintcraft-x`/`sovereign-implementer-x` `0.1.0`), and the rezip ritual is written only for the main plugin. I leaned toward folding the fork's dogfooding onto the main identity, since that's how everything's been tested so far. The user rejected that: the fork must stay under its `-x` identity until merge, because she runs another active project on the main SI at the same time and deliberately juggles the two installed plugins so they don't clobber each other's projects. Filed as a capture rather than acted on, since choosing the fork's dogfooding path is a decision for the next /plan.

**Queue changes:** none — no batches added, reordered, or modified.

**Captures routed:** filed [keep-x-identity-until-merge] (unprocessed, for the next /plan to route).
