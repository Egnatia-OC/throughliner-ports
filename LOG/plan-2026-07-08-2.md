# [HASH] — /plan: promoted [start-fork-dogfooding] to a Freeform batch in QUEUE.md, placed below the two cleared build batches ([session-handoff-offer], [faq-update-check-rewrite]) so one rezip folds their edits into the dogfooded build; Depends on both, Blocks [execute-merge-to-main].

The one waiting capture was [start-fork-dogfooding] — the concrete desktop action that starts dogfooding the code-complete `-x` fork and unblocks the `-x` → main merge. Promoted it to a Freeform batch, since it's an install-and-use action rather than source-editing, a pass/fail check, or a read-and-route. Ordered it just below the two already-cleared build batches rather than at the top: a rezip snapshots the whole fork, so building those two small doc/template fixes first lets one install carry everything and avoids a second rezip. Session ran on remote control against the Google-Drive copy of the project, after the working directory opened onto an empty local Desktop folder of the same name.

**Queue changes:**
- Added Freeform batch [start-fork-dogfooding] below [faq-update-check-rewrite], above the readiness line. Depends on: session-handoff-offer, faq-update-check-rewrite. Blocks: execute-merge-to-main.

**Captures routed:** [start-fork-dogfooding] — promoted.
