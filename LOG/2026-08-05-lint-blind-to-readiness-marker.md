# 3affa20 — post_tool_use.py gains readiness-marker checks (missing/duplicate/drifted) and an orphaned-prose check; next.md in both docsets now fails closed on a missing marker

The queue lint ran four checks and the cleared-to-run marker — the single boundary /next runs on — appeared in none of them: a queue with no marker, or with two, linted clean. Worse, the read side failed open: docs' next.md defined the run as everything above the marker, so with no marker everything in Processed read as cleared — at processing time that would have handed an unattended run all eleven Processed items, including the history rewrite whose own text says it must never be reachable unattended. Three parts shipped. (1) A fifth advisory check: zero markers while Processed holds items, more than one marker anywhere, a marker drifted into Unprocessed. (2) The load-bearing half — /next now fails closed in both docsets: a missing marker means *nothing* is cleared, stated as a new early exit. Docset A took the same correction under the freeze rule (corrections permitted, development barred): a frozen fallback that silently authorises an unattended run over uncleared work is not a safe fallback. (3) An orphaned-prose check, folded in after a live incident: a concurrent session's capture write destroyed an existing item's heading line, leaving its rationale orphaned under the item above, and the damage reached a commit — every existing check validates items it can *see*, so a deleted heading doesn't fail validation, the item just stops existing. The new check flags any non-blank line in Processed/Unprocessed belonging to no `#### ` block (structural `--- … ---` marker lines exempt; one flag per contiguous orphan run). All checks verified against synthetic cases and the real queue, which lints clean.

Overnight blitz run (branch overnight-blitz-2026-08-05b): built under the blitz plan's sanctioned departures — approvals deferred to the branch review, no push.

**Files touched:** plugin/si-plugin/hooks/post_tool_use.py, plugin/si-plugin/docs-b/next.md, plugin/si-plugin/docs/next.md
**Routed to Captures:** none
FAQ: not needed because the new warnings are advisory and self-explaining; the item recorded FAQ judged not needed at processing.
