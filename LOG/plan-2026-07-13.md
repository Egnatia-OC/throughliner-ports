# 0cccd62 — /plan: kept a point-and-work build and a designed feedback-channel red-flag build; merged two audit captures into a reconciliation freeform; advanced the per-model-docset design

This /plan session worked the top of Unprocessed and several user-raised threads. **[nested-si-replaces-multispec]** was kept as a build: its multi-spec half was already dead in shipped docs, leaving point-and-work — a behaviour rule that Claude operates on the folder the session opens in (never scanning parents/children or asking which project), plus a session_start line advising what it sees when a non-SI parent contains nested SI projects. **[fable-docset-model-detection]** stayed Unprocessed but advanced: research confirmed SessionStart is the one hook that can receive a `model` field (not guaranteed present, so a fallback is needed), filed to resources/research/sessionstart-hook-model-detection.md; architecture decided as shared-core + per-model deltas (not two full docsets, which would reintroduce the drift the method fights), corrected to span N models (4.6/4.7/4.8/Fable), not a Fable/Opus binary.

**[slug-trigger-dogfood-confirmation]** was folded as evidence and deleted. **[park-trigger-method-audit]** and a new user-raised capture **[queue-work-only-framing-too-literal]** were merged into one [freeform], **[two-section-model-reconciliation]**, with the decided direction *fold-else-delete* (no new "reference" citizen — the user's origin intent was anti-proliferation); an audit-shape SPEC drift (SPEC frames `[audit]` as a freeform variant while the procedure docs make it first-class) was folded in as Half C, after confirming the audit shape still exists.

**[consumer-plugin-feedback-channel]** was designed in-session rather than deferred to a "dedicated session" (the anti-pattern the reconciliation line rejects) and kept as a red-flag build (state open, scrubbing): a discriminator separating plugin feedback from app work, two trigger paths, a scrubbed report format, and user-pastes-not-auto-submit delivery; part (b) automation stays out of this repo. **[plan-reorder-by-unlock-potential]** was filed new.

**Queue changes:**
- Kept to Processed: nested-si-replaces-multispec (build); consumer-plugin-feedback-channel (red-flag build, open)
- Merged to Processed: two-section-model-reconciliation (freeform) — from park-trigger-method-audit + queue-work-only-framing-too-literal; carries slug-trigger evidence + audit-drift Half C
- Advanced in Unprocessed: fable-docset-model-detection (progress note + research filed)
- Filed new to Unprocessed: plan-reorder-by-unlock-potential
- Deleted: slug-trigger-dogfood-confirmation (folded)
- Research filed: resources/research/sessionstart-hook-model-detection.md

**Work processed:** kept — nested-si-replaces-multispec, consumer-plugin-feedback-channel; merged+kept — two-section-model-reconciliation; deleted — slug-trigger-dogfood-confirmation; advanced/left unprocessed — fable-docset-model-detection; filed — plan-reorder-by-unlock-potential.
