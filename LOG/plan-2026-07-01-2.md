# [HASH] — /plan: processed IDEAS.md into 4 parked workflow-design captures, then carved a token-cutting slice — build batches [editor-awareness-core] + [view-in-doc-group-a] — to the top of Batches

The user dumped a set of ideas into a new IDEAS.md file (itself one of the ideas — a proposed between-sessions ideation doc) and this session processed its three blocks. Confirmed the user's finding that the method re-authors and reproduces doc-bound text into the chat by design (the why-pipeline's adaptive re-authoring is a granted exception), and separated that token-drain lever from the display-location question. Parked four captures forming one workflow vision: view text in the doc and approve in chat ([view-in-doc-approve-in-chat]), an IDEAS.md method doc with a /plan read-first step ([ideas-doc]), editor-awareness as the foundation ([editor-awareness]), and a freer/deeper /setup interview ([setup-interview-depth] — which also corrected a since-retracted "SPEC is too sparse" premise in [spec-sot-rethink]; the sparseness was an illusion of the thin interview).

The user then moved to a Pro subscription, making token drain a direct constraint, and asked to expedite. A LOG check confirmed the cheap token wins already shipped (behaviour.md double-load, dedup trims), and the remaining big lever (progressive disclosure) has uncertain payoff for skill-heavy usage. So rather than convene the full workflow-cluster design session, we carved out its minimal token-cutting slice: [editor-awareness-core] (record the user's `.md` editor at /setup, stored in the generated CLAUDE.md) and [view-in-doc-group-a] (link to doc-resident text instead of re-pasting it, conditional on that signal, degrading to inline when absent). Both are cleared build batches at the top of the queue; the heavier redesign stays parked.

**Queue changes:**
- Added two cleared build batches at the top of Batches: [editor-awareness-core], then [view-in-doc-group-a] (Depends on it).
- Added four parked design captures: [view-in-doc-approve-in-chat], [ideas-doc], [editor-awareness], [setup-interview-depth].
- Slimmed [view-in-doc-approve-in-chat] and [editor-awareness] with carve-out notes; added a 2026-07-01 correction note to [spec-sot-rethink].

**Captures routed:** parked [view-in-doc-approve-in-chat], [ideas-doc], [editor-awareness], [setup-interview-depth]; carved build batches [editor-awareness-core] and [view-in-doc-group-a] from the first two of those. Plus one wind-down capture filed (progressive-disclosure token math).
