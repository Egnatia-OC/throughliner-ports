# [HASH] — Walk-through of the comparison-article item: draft not yet returned from the polishing project

`[user]` item [competition-comparison-article], walked through in the 2026-08-22 multi-item /next run after its 19 Claude-work items were built. Appended live as the walk-through progresses.

- Checked this project's INBOX: empty apart from the outbound register, so the draft the user sent to another project for polishing has not come back through the mailbox.
- The item's pre-drafting verification was already done at processing (2026-08-15): the research file was corrected — AutoDream is live, and "Obsidian memory systems" is a category rather than one project — and the article must reflect both.
- Asked the user whether the polished draft is back in their hands by another route, or whether the article should be drafted fresh here. Their answer: draft it here.
- Re-read `resources/research/auto-memory-staleness.md` (the corrected version) before drafting, per the item's verify-first requirement.
- Drafted the article fresh (~900 words) and handed the file to the user. It names the vault-as-memory pattern as a category, acknowledges that consolidation now ships in Claude Code itself, claims only shipped mechanisms (the digest-plus-full-read planning shape and the build view), and closes on the coherence-over-scale trade rather than a sales line. It is a draft awaiting the user's read; nothing is published, and the Discord post is not yet written — it follows once the article is settled.
- The user approved the article as drafted ("works ok").
- Drafted the Discord post (~1,000 characters) and the user approved its exact text. The user posts it themselves, on their own pacing against the other queued posts, pasting the article URL once the piece is live. Nothing has left the machine from this session.
- The approved post text, verbatim, so the outbound register has something durable to point at:

> **Throughliner update: builds no longer read your queue — and an honest comparison piece**
>
> Two things. First, what shipped: when /next builds your cleared work, it no longer reads QUEUE.md at all. It reads a generated view carrying each item's instructions — what changes, how to tell it worked, what was already refused — and none of the decision history. Your reasoning stays in the queue, whole, where planning reads it; the build gets exactly what was agreed and nothing it could accidentally copy into your documents. Planning still runs a mechanical digest *and* reads the whole file, deliberately — reasoning across items is what planning is for.
>
> Second: there's a new article on the site comparing Throughliner to the memory-system approaches (Obsidian-style vaults, Claude Code's own auto-memory and dreaming). It's not a sales piece — it says plainly where those tools are stronger and what the manual-curation trade actually costs. Link: [article URL once it's up]

- The article's full text was handed to the user as a file; its delivery route to the site project (INBOX message versus the user carrying it across) was put to the user, who chose a message.
- Checked the site project has an `INBOX/` and that its own `.gitignore` covers it (both true), showed the exact message wrapper, and on the user's explicit yes wrote the approved article into that project's mailbox as `2026-08-22-from-no-code-method-comparison-article.md`. The outbound register's line was updated to point at the delivered file.
- Walk-through complete. What remains is the user's: publish the article on the site, then post the approved Discord text with the live URL pasted in, on their own pacing against the other queued posts. Nothing observable exists in this project for either, so the item's completion is recorded here at the close rather than checked from the world.
