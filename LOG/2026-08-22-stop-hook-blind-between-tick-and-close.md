# d7ea8a0 — The stop hook will read this run's own ticks, and SPEC's stop-hook sentence caught up with both suppressions

Kept with the item's one design hesitation dissolved by the record: "nothing else reads that file" is false — pre_tool_use.py already reads this session's build working file for the scope-lock's list and the plan-versus-build decision — so the stop hook reading ticked slugs from the same per-session file is precedented and correctly scoped, and the window closes itself when the close deletes the file. Kept despite the not-urgent note because the false block fires exactly when the session is most confidently right, which erodes trust in every real fire. SPEC's stop-hook sentence, incomplete since the LOG suppression shipped, was amended at this keep to cover both suppressions. Full disposition and build block on the item.

**Queue changes:** kept into Processed, cleared to run.
**Work processed:** kept — [stop-hook-blind-between-tick-and-close].
