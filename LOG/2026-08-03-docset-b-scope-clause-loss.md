# [HASH] — Restored six of docset B's seven reach-carrying scope clauses as inline prose, found the seventh already present, and recorded the other twenty-clause drop as settled

Docset A carries 27 explicit `Scope:` clauses; docset B carries 2. About twenty of A's are redundant with residence — "Scope: every skill" on a rule that lives in plugin-behaviour.md, the cross-skill doc, where position already says it. Shedding those was a sound register decision, and this entry records it as deliberate so a later fidelity pass reads it as settled rather than re-raising it.

Seven carried reach that position does not imply, either extending a rule past where it lives or narrowing it. Six are restored as clauses inside their sentences rather than as `Scope:` labels, per docset B's register:

- the sequencing rule holds in every multi-part exchange, inside skills and out, with no exception for items that seem short;
- consolidate-the-scans covers three named skill openings only, and only when more than one check fires;
- the fresh-session handoff fires in plain conversation as much as inside a command;
- done.md's spec-sync plan branch names all three plan-type closes — /plan, /setup, and a method-doc-only session;
- done-plan.md gained one doc-level statement that its reorder, marker-placement and `[user]`-placement steps each run at every plan-type close, covering three of docset A's clauses in one home.

Two of these matter beyond fidelity: [SEQUENCE] going quiet outside commands, and consolidate-the-scans losing its narrowing, both loosened the over-asking and over-bundling controls — the same territory as [plan-close-offer-reads-as-recommendation].

**The seventh finding was wrong.** Write-then-verify-then-point's separation of just-written from pre-existing text is already in docset B, as the parenthetical noting that pointing at text which already existed carries no write to confirm, so there the re-read is just a resolves-check. The work item asserted that none of the seven survived in B in any form; that claim fails on this one. No edit was made. This is a fifth re-scope on top of the four the item already recorded — the audit pointed in the right direction consistently and was imprecise about reach just as consistently.

**Files touched:** plugin/si-plugin/docs-b/plugin-behaviour.md (sequencing, consolidate-the-scans, fresh-session handoff), plugin/si-plugin/docs-b/done.md (spec-sync plan-close branch), plugin/si-plugin/docs-b/done-plan.md (the doc-level plan-type-close statement).

**Routed to Captures:** none.
