# [HASH] — the wind-down re-scan moved before the LOG entry draft, so the entry is correct at approval time

The re-scan ran inside the commit core, after the entry was approved, forcing the entry's "Routed to Captures:" line to be amended afterwards as a working-tree edit. Checked before changing, per the item's own caution: no recorded reason for the old order was found, and the one thing the amendment path also serves — a capture raised in the post-commit tail — survives unchanged, since no ordering can eliminate it. done.md's LOG-entry step now opens by running the re-scan; the re-scan section states the new position and the why; the commit core keeps a residual check for anything surfaced after entry approval. Processed-and-built in the overnight blitz of 2026-08-05 under the softened bar; autonomous run — recorded departure.

**Files touched:** plugin/si-plugin/docs-b/done.md (wind-down re-scan section, LOG entry files, commit core)
**Routed to Captures:** none
FAQ: not needed because the user-visible flow (one numbered set, one approval) is unchanged — only its position in the close.
