# [HASH] — Nineteen items processed and the rule-lifecycle design finally completed: "cycle" retired for a computed status board whose signals file captures, plus the session's own re-derivation of a graph the hook already computes, captured as work

The session opened by re-deriving, from a 57,000-token read of the whole queue, something `session_start` had already computed and discarded — and the user caught it against the Discord post that had announced the opposite principle a day earlier. That is the thread running through everything below.

**The opening finding, and the correction to it.** `_queue_dependency_facts()` in `session_start.py` walks every `Blocked by:` line, resolves each slug, intersects the sets, and then returns **three integers**, discarding the graph. Its own docstring states the principle it breaks: every reader re-derives the graph, and when Claude does the re-deriving it costs tokens and reasoning. The user's challenge was partly wrong on one detail — this step is not one of the four reorders being repealed, and the mechanical mover was not the cost — and entirely right on the substance. Claude's first framing of the fix was then wrong in turn: emitting the resolved lists would have saved the *reasoning*, not the *read*, and Step 1 requires paging the whole queue regardless. That correction is what widened the work from a hook patch to a digest script plus a narrowed Step 1 read. The page-to-the-end safety rule is not traded away by that: a digest generated from the whole file by code cannot be silently truncated, which is a stronger guarantee than paging gives.

**The rule-lifecycle design, which had defeated several sessions, completed in one pass.** It was first **demoted** — its own `Files:` line read "none yet", failing the keep-check's second limb, and it was found sitting *first* in the cleared region, where a /next run would have halted immediately and built nothing. Splitting it by stage was weighed and refused: that would undo the merge that created it, and its four constraints are cross-cutting. Then it was designed. Four decisions: Constraint A wins over Constraint B, so **"cycle" is retired** for five independent mechanisms and a status board — the word was inherited from the merge cycle, the thing that failed, and a positional cycle reproduces its cause 4 by construction; the board is **computed**, never stored, because a stored file that one session forgets to update is a lying board; the board rides the session-start surface two other cleared items already build; and **a signal that fires files a capture, once** — which is the answer to what stops a board becoming wallpaper, and is the sunset principle properly transferred, since inaction then accumulates cost in the queue rather than sitting as a flat notice. Each of the five stages then got a signal: a calibrated structural rule-count (Measured), the same count crossing its ceiling (Audited), a close-time disposition line on a mechanically-detected file set (Born), near-duplicate detection (Maintained), and a live rule naming a retired term (Repealed). The item returned to Processed with a six-part build list — buildable, where it began the day unscopeable.

**Isolation stopped being a thing to assume.** The research on file said the method "cannot assume an isolation model". A two-string comparison run this session — `git rev-parse --git-dir` against `--git-common-dir` — settles it per session, so the premise becomes *detect, don't assume*. That answered two other items outright, both since deleted into it, and made a `[user]` line asking the user to read their app settings unnecessary. The `_build.md` / `_plan.md` session-scoping was split out first as [session-scope-the-working-file], because it is correct under every isolation model and is broken today, and should not ride a design that has reversed direction twice.

**Three defects were found by reading code rather than reasoning about it.** The queue mover reported an item as below the readiness marker when it had placed it above: `heading_slug` matches any line ending in `[slug]`, not only a `#### ` heading, so a held item's `Blocked by:` line below the marker was matched last and reported as the moved item's position — one unguarded call, at `:404`, where the only other call site is properly gated. `plan.md` was found never to mention end-preferred placement at all, so both of its insertion points into Processed are blind to a rule kept deliberately. And `plan.md` and `post_tool_use.py` were found to **disagree** about a missing blocker — one calls it a fault, the other says only one of its causes is — while neither lists deletion, which is the one case needing re-examination rather than a lift or a repair.

**Consolidation did more than processing.** Unprocessed fell from 51 to 33, but only a minority of that was work being cleared: ten audit findings became three build items and one fold, three narration captures turned out to amend the same rule and became one, two isolation items were absorbed, and **five rationale-growth measurements left the queue entirely** for `resources/research/rationale-growth-measurement.md`. None of those five was work — ask the keep-check's second limb and the answer is nothing — and binding them into one file is the only arrangement where the caveat that word counts cannot separate invented reasoning from genuine decisions travels with the numbers it qualifies.

**Four rules were recorded failing while shipped and correctly worded**, which is the pattern worth more than any single lapse. Three of them ask Claude to notice something about its own reasoning; the user's observation that the same "what would answer this?" reframe should drive the web-search offer produced the diagnosis that both under-fire because they are written as self-assessments of confidence, and a session that has quietly settled on a wrong answer does not feel uncertain. The fourth is different and worse: `_plan.md` was never created across the whole session, a plain procedural step with a mechanically checkable outcome. It cost this close its mechanical record, which had to be reconstructed from memory.

**A Discord rule was added to CLAUDE.md**, replacing the user's own "describe early, they're only friends" position with **announce only what has shipped** — their revision, and the better one, because it needs no revisiting when real users arrive. It was prompted by a post premised on a feature that is designed and held below the line, not built.

**Queue changes:** three items lifted above the readiness marker (their blockers shipped in `08c885b`); nineteen items kept into Processed, all cleared to run; the consumed forward advisory cleared; twenty-two items deleted, of which every one had its content carried elsewhere first; five new captures left in Unprocessed. Processed ends at twenty-two builds plus two `[user]` lines, end-preferred intact; Unprocessed at 33. No reorder was run: [processed-reorder-mostly-unnecessary]'s repeals were decided this session and a decided rule is in force before it ships.

**Work processed:** kept — [hook-emits-counts-not-the-graph], [nothing-audits-the-cleared-region], [ladder-missing-stalled-region-rung], [rule-lifecycle-system] (demoted, designed, returned), [session-scope-the-working-file], [isolation-model-is-not-ours-to-choose], [mover-reports-wrong-side-of-marker], [lift-step-has-no-placement-rule], [advisory-falsified-by-intervening-run], [settled-decision-reopened-by-reframing], [deleted-blocker-has-no-branch], [capability-check-weight-by-site], [what-would-answer-this-as-a-shared-trigger], [processed-reorder-mostly-unnecessary], [prohibitions-rewritten-as-actions], [trailing-rules-sections-repealed], [claude-template-references-retired-test-flavor], [message-shape-rule-three-clauses], [done-invoked-when-user-meant-continue]. Deleted — [rule-lifecycle-system-should-not-be-cleared], [worktree-setting-check], [parallel-sessions-rule-assumes-shared-tree], [concurrent-capture-during-next-costs-interruptions], [gate-section-4-breaks-its-own-rule], [done-judgment-steps-prohibition-only], [done-meta-description-prohibition-only], [next-build-no-preview-heading], [setup-first-item-block-mostly-refusals], [setup-rules-section-duplicates-body], [done-rules-section-duplicates-body], [next-rules-section-duplicates-body], [plan-ground-rules-should-subordinate], [ask-not-at-message-end], [openings-need-more-brief-tags], [completion-sweep-silent-leak], [growth-measured-at-1-77x], [growth-is-concentrated-not-uniform], [processed-items-compress-when-reworked], [growth-happens-at-processing-not-after], [growth-measure-cannot-separate-invention], [forward-advisory].

**Routed to Captures:** [discord-post-session-start-strength], [findings-in-unprocessed-never-flagged], [floor-and-lift-use-different-tests], [plan-file-never-created], [wind-down-rescan-run-out-of-procedure]. [remote-session-start-has-no-owner] was left in Unprocessed by decision rather than oversight, its reasoning written into the item.

**A fifth procedural failure, caught by the user at the close.** The wind-down re-scan ran three times, twice at moments neither document names — the design is one substantive re-scan at the end of processing, with a no-op backstop at the close. The likely cause is that the step is anchored to a *position* ("after all items") that this session never reached, since processing ran in user-directed batches rather than to an empty queue; with the anchor absent the step attached to the nearest pause. That makes five shipped rules recorded failing in one day, and it is the second whose trigger is positional in a queue that moves.

**FAQ: not needed** — nothing shipped this session, the CLAUDE.md Discord rule is host-only and invisible to consumers, and the one user-facing change decided here carries its own FAQ entry in [isolation-model-is-not-ours-to-choose]'s file list rather than needing one written now.

**Self-authoring gate: run, on the CLAUDE.md Discord rule.** It found the off-machine send rule is the parent — show the exact text, explicit yes, user posts — so only two clauses were genuinely new, the 2,000-character limit and ship-before-announce. Everything else was dropped as restatement, which §4 forbids. The rule's reasoning was kept out of the operative text and lives here, per the gate's rationale split.

**Red flags:** none surfaced, none cleared.
