# [HASH] — Brevity output style kept, then reshaped the same day: consent at /setup replaces force-for-plugin

From INBOX mail carrying the user's own request in another project. Kept in the morning with `force-for-plugin: true`; superseded hours later on the user's decision once asked how users would experience it: the style ships un-forced, and /setup makes a project-level switch-or-keep offer — opt-out with acceptance default, existing style checked first and contradictions named, the verbosity rationale stated, scope named as this project, discussion invited before setup continues. Project-level scope was Claude's improvement on the instruction's assumed global scope, approved. Appending to a user's own style file was proposed and reversed on Claude's recommendation with the user's agreement. session_start checks the setting every opening — one short line when not enabled, silence when it is (the user's addition). Claims verified live against the official docs: `resources/research/output-styles-plugin-capability.md`, indexed. SPEC's output-style paragraph rewritten twice — first to the new truth, then to the consented shape — with the 2026-08-14 removal history evicted to this record: the old style was removed for triplication, and the follow-up dedup audit is what stops this one rebuilding that defect. The 2026-08-14 removal reasoning is answered, not refuted: the opt-out default plus the every-session line keeps the fix reaching consumers without the silent override.

**Queue changes:** kept into Processed, cleared to run; [style-level-rule-dedup-audit] filed and cleared immediately after it (sibling entry).
**Work processed:** kept — [ship-brevity-output-style].
