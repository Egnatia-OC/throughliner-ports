# d7ea8a0 — The gate's trigger set gains the audit checklist's path, and states its in-repo-only limit

Kept with the prior question answered by the item's own fallback: no fail-safe trigger shape exists that does not guess at what "holds rules" means, so the path is added and the limit said. The ripple grep found the set stated at exactly three sites — CLAUDE.md's gate trigger, CLAUDE.md's close-step rule, rule_signals.py's constant — and `retired-terms.md` checked clean (zero rule-shaped lines), so the checklist was the only unlisted rule-holder. The out-of-repo instance gets the honest-limit clause: a rule living outside the repository gets no gate, stated where the trigger is described. Consistent with the audit-lag check kept the same session, whose path set already names the checklist. Full disposition and build block on the item.

**Queue changes:** kept into Processed, cleared to run, placed ahead of the first-instance audit so the widened trigger lands before it runs.
**Work processed:** kept — [gate-trigger-misses-the-audit-checklist].
