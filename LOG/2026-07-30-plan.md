# a016a26 — Diagnosed viewer-lag as procedural not a desktop bug; reversed unsound session-sizing deletion; kept faq-sync/viewer-lag/next-stops-audit; filed 8 method captures

A /plan that turned into a deep method-integrity session. The user flagged suspected non-landing deletions; a git check found recent deletions *did* land, but surfaced a genuinely skipped advisory-consume step (filed [plan-skips-advisory-consume]). A resources-hygiene thread produced a **three-way triage** for records — reveals work → QUEUE, clean finding/pass → LOG, re-read-verbatim evidence → resources/ — filed as [resources-purpose-and-triage], with [temp-file-delete-time] for temp-file delete-times. A reviewed Hexboard /plan→/done transcript was a live PASS (recorded here, not queued, per that triage): the consolidated /done close ([plan-step3-shadow-close]) fired end-to-end, the advisory was filed at close, working-mode local rendering worked, /plan discipline held. A /next narration claiming context-awareness traced to next.md:111 contradicting the no-self-detection rule ([next-context-self-detection]); pulling that thread exposed the **unsound 2026-07-28 deletion of [session-sizing-and-break-lines]** — deleted citing a companion CLI that handles sizing "automatically," which contradicts resources/research/context-window-visibility-to-model.md (that capability is unverified; session-break lines + plan-time sizing stay the design). Restored it verbatim. Lesson recorded: don't delete a work item on the strength of an unbuilt/unverified capability. The companion CLI was reframed as a discrete status-line-command context-% reader ([statusline-context-reader]) — the only surface fed the context %, read by /next at discrete decision points, not a daemon. A live viewer-lag test (added a temp line then deleted it; both registered instantly) proved the desktop viewer refreshes fine → viewer-lag is a procedural add-then-delete window, kept as a fixable build ([queue-move-viewer-lag]); its Files list covers plan.md, next.md's live removal, and the close moves. The run-boundary audit ([next-stops-after-one-item]) was enriched with the user's design intent: `[user]` lines are a *placement* concern (end-preferred, don't interrupt Claude blocks), never a run-*terminator* — run continuously, don't bookend every handover with /next+/done. [reorder-home-is-plan-close] records that this placement is /plan's job, not /next's.

**Queue changes:**
- Kept → Processed (above line): [faq-sync-gap] (FAQ-sync rides the SPEC-sync close trigger, host-only CLAUDE.md), [queue-move-viewer-lag] (atomic section-moves / clean deletes), [next-stops-after-one-item] `[audit]` (enriched with run-continuously intent).
- Folded [migration-recipe-hexboard-pass] into [migration-checklist] and deleted it; folded [next-run-boundary-judgment-datapoint] into [next-stops-after-one-item] and deleted it.
- Restored [session-sizing-and-break-lines] to Unprocessed (reversed unsound 2026-07-28 deletion).
- Filed to Unprocessed: [plan-skips-advisory-consume], [post-close-capture-not-logged], [resources-purpose-and-triage], [temp-file-delete-time], [next-context-self-detection], [statusline-context-reader], [reorder-home-is-plan-close].
- Consumed + deleted the stale advisory [next-build-advisory-2] (target already built). Reordered both sections (Claude work ahead of `[user]` handovers in the cleared region; statusline-context-reader ahead of its consumer session-sizing-and-break-lines).

**Work processed:** kept [faq-sync-gap], [queue-move-viewer-lag], [next-stops-after-one-item]; deleted (folded) [migration-recipe-hexboard-pass], [next-run-boundary-judgment-datapoint]; restored [session-sizing-and-break-lines]; filed 7 new captures.

**Routed to Captures:** [plan-skips-advisory-consume], [post-close-capture-not-logged], [resources-purpose-and-triage], [temp-file-delete-time], [next-context-self-detection], [statusline-context-reader], [reorder-home-is-plan-close].
