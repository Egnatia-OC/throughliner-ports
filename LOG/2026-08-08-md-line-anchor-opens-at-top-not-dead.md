# 3633c7d — All seven line-anchor statement sites corrected: a .md :N link opens at the top and silently ignores the anchor

Built in the 2026-08-08 overnight blitz. The instruction survives everywhere — never emit a `:N` suffix on a `.md` link — but every site now carries the true reason: the link opens the file at the top and silently ignores the anchor (tested live 2026-08-07), a strictly milder failure than the "dead, does nothing" the docs claimed, and load-bearing for anyone reopening the doc-bound-text trade-off. Docset B: plugin-behaviour.md's two-link-facts paragraph (which also now marks the code-file half as recorded-not-retested, pending the walkthrough's fourth check), next-build.md's reveal rule, next.md's run-presentation line. Docset A, as a **correction** in A's register: plugin-behaviour.md and next-build.md actively instructed *using* a line-anchored link with a fallback "when it won't resolve" — a fallback that can never fire, since the link always resolves and silently ignores the anchor; the instruction is replaced with plain-link-plus-inline-excerpt, the excerpt carrying the position. SPEC's pointer-weaknesses parenthesis corrected in the same pass.

**Held for you:** the item's outward-facing step — correcting our own inaccurate report on anthropics/claude-code#83475, which describes this as a dead link — needs a public comment under your identity, so it is drafted-and-approved work for an attended session, not an overnight one. The item is removed; the pending comment is named in the blitz-close advisory so it doesn't evaporate.

**Files touched:** plugin/si-plugin/docs-b/plugin-behaviour.md, plugin/si-plugin/docs-b/next-build.md, plugin/si-plugin/docs-b/next.md, plugin/si-plugin/docs/plugin-behaviour.md, plugin/si-plugin/docs/next-build.md, SPEC.md
**Routed to Captures:** none (the pending #83475 comment rides the blitz-close advisory)
FAQ: not needed because the rule's user-visible face (links arrive without line numbers) is unchanged; only the recorded reason changed.
