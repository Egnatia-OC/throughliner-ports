# fa35a89 — Refreshed [merged-plugin-live-verification]'s stale wording and reordered Unprocessed to lead with the shelving/capture cluster

A short planning session that fixed one live hazard and set the order for the next. [merged-plugin-live-verification] sits above the cleared-to-run marker, so /next would hand it over right now — but its wording had gone stale in the same session that unblocked it. It told the user to check that session_start surfaces an "open" red flag and that the lint rejects an "invalid" state, and those states became cleared/uncleared when [redflag-cleared-uncleared-model] shipped. It also still framed the reinstall as pending, and predated the other three /cruise-retirement builds. The refreshed line names the live host (1.15.0-test5), the cleared/uncleared states, and the new /next behaviours the check should now confirm — back-to-back cleared lines, the no-progress stop, and the one-summary-commit close. Nothing else about the check changed; it stays one live confirmation rather than a deferred-test pile.

No work was processed. The session's other output is the order: Unprocessed is reordered by unlock-potential to lead with the shelving/capture cluster, and within that cluster [defer-for-design-process] goes first as the keystone — it settles whether "undesigned work stays a capture" is the whole answer, and [claude-invents-shelving-hardening], [two-section-model-reconciliation] Half B, [freeform-discussion-bleeds-into-processing], [capture-disposition-over-narration], [capture-then-offer-process-now], and [skip-to-defer-in-plan-loop] all resolve against that once it's settled. Both standing advisories are cleared: [advisory-2026-07-14] is spent (its builds shipped), and [advisory-2026-07-15]'s refresh half is done while its planning half carries forward into this session's own advisory.

**Queue changes:**
- [merged-plugin-live-verification] — rationale rewritten in place (Processed, above the cleared-to-run marker); no change to its flavor, slug, or position
- [advisory-2026-07-14], [advisory-2026-07-15] — consumed and deleted from Unprocessed
- Unprocessed reordered by unlock-potential, shelving/capture cluster to the top

**Work processed:** none
