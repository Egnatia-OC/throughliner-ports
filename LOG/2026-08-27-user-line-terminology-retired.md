# 99865ab — plan — [user-line-terminology-retired] kept: "line" retired as the name for a [user] work item

Raised by the user: `[user]` items were being called "lines" again — terminology from when work was a single line, long untrue of the `#### `-heading-plus-prose format. Sits under the shared-vocabulary decision made minutes earlier: one thing, one name, the name the artifact shows. The grep found six files carrying the term-of-art use; templates and README are clean. The reword is judgment-applied, and the build's close appends the Retired: entry so the standing checks catch the term returning.

**Queue changes:** [user-line-terminology-retired] filed and cleared.
**Work processed:** kept — [user-line-terminology-retired].
Rule gate: run at the keep — no rule authored; recorded on the item.
