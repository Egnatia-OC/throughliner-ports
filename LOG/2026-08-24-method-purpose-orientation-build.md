# 3ed3db1 — Built: the purpose statement ships in the always-loaded rules, with a what-this-is-for line atop each skill doc

Your capture, from authoring the comparison article: sessions know the rules and not the point. The four-point brief settled at processing was transcribed into a "What the method is for" block above the work cycle in skill-nonspecific-rules.md — read-here-not-recited, modelled on the work-cycle block, its one non-rule precedent: intent keeps running the project whatever Claude remembers; reading and approving the record IS the product; settled things stay settled because rejected options carry why they lost; recorded intent is what makes drift visible. Each skill doc — plan, next, done, setup, and rescan (checked: its opening lacked one) — opens with one line tying its job to the purpose. The rule count is unchanged: orientation, not a rule. The former what-is-Throughliner FAQ entry now lives in ANNOUNCEMENT-IDEAS.md after the reseed, so no FAQ edit. The honest limit carried from the capture stands: nothing fires when Claude misunderstands the point — this buys better odds, not a guarantee.

Rule gate: run — not a rule: it instructs no behaviour, so it consumes no rule slot and evicts nothing. Admitted as orientation on the work-cycle block's explicit precedent; the cost is always-loaded attention, which is why the block stays short.

Files touched: plugin/throughliner/docs/skill-nonspecific-rules.md; plugin/throughliner/docs/plan.md, next.md, done.md, setup.md, rescan.md
Routed to Captures: none
Done, confirmed: block carries no internal vocabulary; each opening line says what the skill is for.
