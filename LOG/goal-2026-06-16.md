# 759f495 — Autonomous /goal session (plugin off): shipped 10 build batches + 1 audit — the scope-definition arc, the trickle dedup pair, [spec-edit-batch-type] (SPEC becomes a normal doc), plan markers, FAQ trigger + coverage audit, no-planning rule, ask-when-unsure, /next-only trickle-down, and audit bulk approval; 6 captures filed, 5 host-side deferred tests added; previous session's hash backfilled to ed74fbe; stopped before [next-done-tag-sweep]

Autonomous goal session with the plugin off, working QUEUE.md's Batches top-to-bottom. The first batch [install-upload-path-clarity] was skipped (it needs a real desktop-app screenshot a goal session can't produce) and left in place. Eleven batches then shipped in order; the session stopped before [next-done-tag-sweep] to leave room for a clean close, since that batch's tag placement across seven docs is finicky work that shouldn't be rushed under context pressure. There were no push markers in the queue, and every Depends-on line was target-side (docs readable at author time), so the ordering resolved in one session.

**The scope-definition arc.** [scope-anchor] installed one canonical Scope statement in plugin-behaviour.md — build scope is the active batch's entries' described work (the definition, enforced by judgment), and the _build.md Files: list is its mechanical approximation (enforced by the pre_tool_use hook as a backstop), so passing the hook never by itself makes work in-scope. next.md, next-build.md, and plan.md were pointed at the anchor instead of restating it; the "user owns scope" bullet was reworded so "scope" loses its project-scope sense and build scope becomes the word's only meaning; the scope-expansion ask in next-build.md was reframed file-shaped → work-shaped (growth measured against the described work, not the Files: list); and plan.md gained the authoring side — build entries name their files, and the readiness gate now checks the Files: list can be populated. [scope-boundary-rule] then stated the discovery decision rule once in plugin-behaviour.md (needed-minor → ask; needed-significant → split; not-needed → capture-and-continue; premise-broken → halt) together with the defined routing move (capture at the moment of noticing, same flow as a user-raised item), and the skill docs' Scope-management sections now read as elaborations of it.

**Trickle dedup.** [trickle-up-next-md-duplicates] removed four rule restatements from next.md and the per-type docs (SPEC read-only, don't-fix-outside-scope, state-regressions, one-build-at-a-time), leaving each rule canonical in plugin-behaviour.md. [trickle-up-ask-when-unsure] generalized next.md's ask-when-unsure into plugin-behaviour.md Communication (framed as one decision with two branches alongside the web-search rule) and added a self-contained copy to setup.md, whose sessions may never load the behaviour injection. [trickle-down-next-only-rules] did the opposite move for the two rules that only ever fire during a /next build — the only-valid-recommendation-is-/done rule and the long-context nudge — relocating them from the every-session injection down to next.md.

**SPEC becomes a normal doc.** [spec-edit-batch-type] removed the SPEC.md read-only-during-builds rule from pre_tool_use.py; scope-lock alone now governs SPEC, and because SPEC is not a method doc it stays read-only for any build that doesn't list it (verified against a fixture: empty-Files build → SPEC denied; feature build listing other files → SPEC denied; spec-edit batch listing SPEC.md → SPEC allowed). plan.md gained a Spec-edit batch type, the 5-stage pipeline, and an authoring note; plugin-behaviour.md lost both the spec-entry pipeline rule and the now-false read-only rule; post_tool_use.py added Spec-edit to ALLOWED_SUBHEADINGS (with the subheading regex widened to allow the hyphen, lint re-verified); and done.md, done-build.md, and next.md routers now route a Spec-edit batch like a build. One implied SPEC change — rewording SPEC.md line 29's hooks description — was deferred to a future spec-edit batch (the host's read-only lock is still active this session and SPEC is out of a feature build's scope), and filed as a capture. The next.md router branch was a needed minor scope extension the batch's build list omitted (it named done's router but not /next's); that omission pattern was filed as a plugin-behaviour observation.

**Plan markers and the FAQ.** [queue-plan-markers] added a queue-visible planning gate — a `--- Plan session here: <reason> ---` line that /next halts on, sibling to the push marker — with the authoring rule in plan.md Step 3, the queue-top handling in Step 1, the halt in next.md pre-flight, documentation in CLAUDE-TEMPLATE.md and this project's CLAUDE.md, and a consumer FAQ entry. [faq-authoring-trigger] added the host-only rule that a /plan batch introducing a consumer-visible concept carries an FAQ entry. [faq-coverage-audit] (the session's one audit batch) read the FAQ against the current concept surface and filed three coverage-gap captures (no entry for /setup, none for the commit-and-push ask, none for the drift signal), noting three more as already covered by existing captures or pending batches (the SPEC entry, the REGISTRY entry, the working-rhythm cycle). It ran under the old per-finding audit flow because the new bulk flow hadn't shipped yet.

**Two more rules.** [no-planning-in-execution] added the compliance-hardened boundary rule to plugin-behaviour.md (capture-making is open to every session type; processing captures happens only in /plan), an autopilot prerequisite. [audit-findings-bulk-approval] replaced next-audit.md's per-finding loop with a bulk flow — one numbered set for bulk approval, only contested items taken one at a time — added the matching second inversion to plugin-behaviour.md's sequencing rule, and gave done-audit.md's LOG template an "Approval outcomes" line so a dropped or reworded finding leaves a trace.

All authored method text was checked against resources/4-8-authoring-heuristic.md before shipping. Six captures were filed (the deferred SPEC reword, the consumer-facing SPEC staleness in CLAUDE-TEMPLATE.md and the FAQ, the both-routers observation, and the three FAQ coverage gaps) and five host-side behavioural deferred tests were added to QUEUE.md. The previous session's unfilled hash placeholder was backfilled to ed74fbe.

**Files touched:**
- plugin/si-plugin/docs/plugin-behaviour.md — new Scope section; discovery decision rule + routing move; spec-entry pipeline rule and SPEC-read-only rule removed; ask-when-unsure generalized; two /next-only rules removed; no-planning-in-execution rule; second sequencing inversion.
- plugin/si-plugin/docs/next.md — scope-lock pointer to anchor; one-build-at-a-time and ask-when-unsure restatements removed; two /next-only rules added as canonical home; plan-marker halt; Spec-edit routing branch.
- plugin/si-plugin/docs/next-build.md — scope rule points at anchor; scope-grows ask reworded work-shaped; three rule restatements removed; Scope-management elaboration pointer.
- plugin/si-plugin/docs/next-test.md — discovery-rule pointer; two rule restatements removed; Scope-management elaboration pointer; significant-case wording aligned.
- plugin/si-plugin/docs/next-audit.md — per-finding loop replaced with the bulk-approval flow; review tail defined.
- plugin/si-plugin/docs/plan.md — 5-stage pipeline; Spec-edit batch type + authoring note; build-entries-name-files note; readiness-gate scope check; plan-marker authoring rule + queue-top handling.
- plugin/si-plugin/docs/setup.md — self-contained ask-when-unsure rule.
- plugin/si-plugin/docs/done.md — Spec-edit routes like a build.
- plugin/si-plugin/docs/done-build.md — Spec-edit close note.
- plugin/si-plugin/docs/done-audit.md — Approval-outcomes LOG line.
- plugin/si-plugin/hooks/pre_tool_use.py — SPEC read-only rule removed; docstring updated.
- plugin/si-plugin/hooks/post_tool_use.py — Spec-edit in ALLOWED_SUBHEADINGS; subheading regex allows hyphen; messages updated.
- plugin/si-plugin/templates/CLAUDE-TEMPLATE.md — plan-marker documented.
- plugin/si-plugin/templates/faq-template.md, faq-index-template.md — plan-marker FAQ entry.
- CLAUDE.md — SPEC-model reword; plan-marker in QUEUE format; FAQ-authoring-trigger convention.
- QUEUE.md — 11 batches removed; 6 captures filed; 5 deferred tests added.
- LOG/index.md, LOG/plan-2026-06-16.md — previous session's hash backfilled to ed74fbe.

**Routed to Captures:** six — author a spec-edit batch to reword SPEC.md's pre_tool_use description; consumer-facing SPEC model stale in CLAUDE-TEMPLATE.md and the FAQ; adding a new batch type needs both routers (plugin-behaviour observation); and three FAQ coverage gaps (/setup, the commit-and-push ask, the drift signal).
