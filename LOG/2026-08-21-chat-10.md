# 9c98504 — Freeform chat for the two corpus passes: the scope-lock found blocking freeform work, worked around with a hand-written scope file on the user's approval

Chat-level record for the freeform sitting that ran [cut-length-remaining-docs] and [law-prose-remaining-thirteen-docs]; each has its own entry.

**Also in this chat:**

- **The scope-lock blocked the freeform work at its first edit, and that is a method defect, captured.** A session with no build working file falls under the planning standing list, which denies `plugin/throughliner/docs/` outright — so a `[freeform]` item whose work is editing those files cannot be executed as designed. The deny message says to queue the work; the work was already queued, as an item the queue says must be done by hand. Captured as [freeform-blocked-by-standing-list], including that `pre_tool_use.py`'s top docstring still says Rule 4 "asks, never deny", stale against the deny the code now performs.
- **The workaround, user-approved before use:** this session hand-wrote its own build working file carrying exactly the item's fourteen-file list, so the scope-lock enforced an agreed list instead of the standing deny. The file was deleted when the hand work finished, before this close, so the close read the session as the freeform session it is. A workaround, not a design — the capture holds the design question.
- The surfaced INBOX item was `INBOX/sent.md`, the standing outbound register, not waiting mail — nothing to triage or archive, and its newest line (the template-question reply) was noted to the user.
- `rule_signals.py` was run after the corpus edits, per the always-loaded close obligation: four checks run, nothing found.
- The tree also carried 26 LOG entries and index.md modified by session_start's hash backfill from the previous close — the recognised inherited tail, folded into this commit.

- The wind-down re-scan filed one further capture: [claude-md-rename-note-lost-old-name] — CLAUDE.md's folder-rename note gives the same path twice, so the old folder name it exists to record is missing.

**Routed to Captures:** [freeform-blocked-by-standing-list], [claude-md-rename-note-lost-old-name].

Advisory: filed — [forward-advisory], replacing the spent one from the previous build close.

## After the close

The user asked for a rezip and it ran to completion: version bumped to `1.20.0-test15`, `__pycache__` cleared, all three hook suites passed, the oldest cached build pruned, the host updated via the CLI, and the content stamps confirmed identical (`f9aa04e5e2ea`) before the restart was requested. After the full app restart, this same chat received a fresh session_start payload naming the new version and stamp — the liveness proof, without a fresh chat. On the user's suggestion, [rezip-liveness-same-chat] was captured: reword the rezip ritual's liveness step so the restarted chat's own re-fired payload counts as the proof, keeping the fresh-chat prompt as fallback. Both the plugin.json bump and this tail ride the next close's commit.
