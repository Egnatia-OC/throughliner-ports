# [HASH] — Kept and cleared: walk-throughs read the record before acting

Processed 2026-08-24, from the 2026-08-23 collision where a walk-through re-drafted an article already drafted, approved and delivered the day before. The design shrank on a fact the capture missed: a walked item's LOG entry already appends actions as they happen, so progress is recorded — the gap is only that nothing reads it before acting. Two light parts kept: the lifecycle bullet gains the record check (list LOG/ for the slug, resume after what the record shows done), and the build view gains a records-on-file line for [user] items, reusing the digest's slug resolution. The progress-artifact direction was refused as duplicating the record and storing position. SPEC's completion-inference paragraph gained the sentence this session. Gate: run — amendment to the same lifecycle bullet [walk-user-line-at-filing] amends.

**Work processed:** kept — [walkthrough-repeats-completed-steps], cleared with build block.
