# 8330209 — hook-enforcing the behaviour-rules read put to the user and refused, with the research kept and annotated

The item had been parked since 10 August behind a question nobody had asked: whether enforcement is wanted at all. Everything else in it — the transcript scan, the two measurements, the rezip-and-restart experiment — was explicitly held behind that. Leaving it parked a third time would be the failure plan.md names, an entry enriched across sessions while the one available question goes unput.

The answer is no, and the argument is that a read-gate enforces the wrong thing. A `PreToolUse` hook can genuinely force the read; that research stands. But what it proves is that the file was *opened*, and every recorded failure in this corpus is read-and-not-followed: the provenance rule shipped, sharpened and still not holding; the file-the-blocker rule explained five to ten times in a month; the INBOX-opening step skipped in the session that authored it. Four instances of a correctly worded, definitely read rule failing to fire, and no recorded instance of the rules going unread. So the gate would pay the most expensive option in the item's own cost ordering — a transcript scan on every tool call — to close a hole nobody has observed, one layer above the hole everyone has. The rule board is the same experiment already run: built so the rules could watch themselves, it reported clean while five real rule defects shipped in one session.

Two things in the item were stale and would have misled a later reader: it routed the decision to a lifecycle item whose work is consumed, and its first finding had already been split out and shipped as the docstring correction.

Deleting lost no research. The findings live in `resources/research/hook-enforced-doc-reading.md` with the verbatim payload shapes a build would need, and that file and its index line now carry the decision, the reasoning, and the one condition that would reopen it — new evidence that the rules are going unread, which is the specific thing nobody has yet seen.

**Queue changes:** the research file and its index line annotated with the refusal and its reasoning.

**Work processed:** kept — none. Deleted — [behaviour-rules-read-is-enforceable].
