# 11c660d — /plan: processed 8 dogfooding captures (2 deleted/folded, a cruise-control translation note, 3 [user] verifications kept below the line, 2 build lines spawned)

This session processed the eight unprocessed captures that accumulated while dogfooding the -x fork as host. Most were staleness or reconciliation items thrown up by the work-line recut and the fresh /cruise build.

The reasoning behind each disposition:

- **[done-close-skips-view-in-doc-in-local-mode] — deleted, folded into [view-in-doc-approve-in-chat].** /done pasting a LOG entry and a capture in full during a local-mode close is not a cheap standalone fix: both are *fresh drafts headed for a doc*, which is Group B of [view-in-doc-approve-in-chat] (write-then-approve-in-place), deliberately parked until the docs-open-editor workflow session. The shipped [view-in-doc-group-a] only ever covered *re-reading text already in the doc*, so a not-yet-written LOG draft can't be pointed at. Kept the value — the specific /done touch-points (done-plan.md's LOG-entry step, the wind-down/commit capture-drafting steps) — by folding them into Group B rather than building anything now.
- **[cruise-design-refs-retired-deferred-tests] — resolved in-session, deleted.** The [cruise-control] block still named "Deferred tests" and "dependency tracing," both retired by the recut. Rather than surgically rewrite a long historical concern-log (high effort, high risk of falsifying past-tense reasoning, and merge-doomed since the block is spent), added one dated model-translation note at the head of the block that reads the two retired terms in current form. History beneath is left as-written.
- **[cruise-run-verification], [cruise-gate-hardstop-verification], [cruise-readiness-verification] — kept as `[user]` lines, moved below the cleared-to-run marker.** Genuine verification of the /cruise work built this session, runnable only after rezip + reinstall, so they sit below the line as "kept but waiting on something outside the queue." Noted the tension with [test-concept-redesign] (these are the observe-after-reinstall confirmations it argues clutter the queue), but that redesign is parked, so they follow the current model.
- **[done-defaulted-to-retired-deferred-tests] — deleted.** A confirmed fork-transitional artifact with no shippable fix: done.md's "Checks the closing session couldn't run" section already correctly says the Deferred tests section is retired and an unrun check is filed as a `[user]` line. The error came from the fork's QUEUE.md still physically showing a legacy `## Deferred tests` section, which won't exist in a real consumer's two-section queue; and converting this fork's queue early is explicitly forbidden (the merge removes the section). So no sharper done.md guard is warranted.
- **[faq-stale-dependency-tracing-entries] — kept as build line [faq-reconcile-work-line-model], above the cleared line.** A live consumer-facing template that ships and misleads: faq-template.md carries four entries built on removed dependency-tracing machinery. Editing plugin source is building, not planning, so it becomes a /next build line.
- **[done-commit-msg-multi-entry-gap] — decided here, kept as build line [done-multi-entry-commit-msg], above the cleared line.** done.md's commit core defines the message as a single LOG entry's one-liner + rationale, which breaks for a multi-line /next or /cruise run (N entries, one commit). Decided the multi-entry rule: title = a factual roll-up naming the run type and count ("/cruise run: 5 work lines"), body = a bullet list of each entry's index one-liner, both assembled from already-approved text so the commit reviews nothing new. Full rationale is deliberately NOT duplicated into the multi-entry commit body — it stays in the per-entry LOG files (committed), which is what preserves the why-retrieval pipeline; the single-entry rule stays untouched for the same reason. The build writes this clause into done.md's commit core.

**Queue changes:**
- Deleted captures [done-close-skips-view-in-doc-in-local-mode] (folded into [view-in-doc-approve-in-chat]) and [done-defaulted-to-retired-deferred-tests].
- Resolved [cruise-design-refs-retired-deferred-tests] in-session via a model-translation note added to the [cruise-control] block; deleted the capture.
- Moved the three `[user]` cruise-verification lines below the `--- Cleared to run above this line ---` marker.
- Added two build work lines above the marker: [faq-reconcile-work-line-model] and [done-multi-entry-commit-msg].

**Work processed:** kept — [cruise-run-verification], [cruise-gate-hardstop-verification], [cruise-readiness-verification] (as `[user]` lines below the line); [faq-reconcile-work-line-model], [done-multi-entry-commit-msg] (as build lines above the line). Deleted — [done-close-skips-view-in-doc-in-local-mode], [cruise-design-refs-retired-deferred-tests], [done-defaulted-to-retired-deferred-tests].

**Routed to Captures (post-close):** [next-user-step-copy-sounds-confused] — /next's copy sounds confused when the top ready item is a `[user]` step. Filed to Unprocessed after this session's commit; rides the next commit.
