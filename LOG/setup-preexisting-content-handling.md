# LOG entry — setup-preexisting-content-handling

## [HASH] — /next [setup-preexisting-content-handling]: setup.md Case B branch gains two rules — Claude peeks at pre-existing user content before Q1, using it only to frame the question (clarifying parenthetical, never a pre-answer, one example marking the framing-vs-bundling line); and pre-existing content stays untouched during scaffolding and gets named in the closing message as a source doc — both judgment calls observed to land in a real tax-prep /setup run, now written so they don't ride on judgment.

setup.md was silent on pre-existing non-method content in a Case B folder. A real /setup run on a tax-prep folder with one pre-existing brief surfaced two judgment calls, and both landed: Claude peeked at the brief before Q1 and used it to frame a clarifier without pre-answering, and left the brief untouched during scaffolding while naming it in the closing message. A different run could have skipped the peek (asking Q1 cold and missing context the folder already gave) or pre-answered Q1 from the content (bundling an answer into the question, filling the spec with Claude's words instead of the user's). Both behaviours are now explicit rules so they don't ride on judgment. One structural choice came out of the build: setup.md had no Case B branch to put them in — Case B existed only as a Step 1 classification bullet, while Case C routes to its own Step 2C — so the rules landed as a new "Case B: pre-existing content rules" section directly after Step 1, mirroring how Case C's branch follows it. The framing-vs-bundling example is a good/bad pair: a clarifier invites the user's own answer, a pre-answer proposes the answer for confirmation. The example deliberately avoids quoting Q1's exact wording, so the queued agnosticism sweep ([setup-project-agnosticism-sweep]) can reword the interview questions without colliding with it. The test is verified from doc text only; live confirmation needs the next Case B /setup run after push + reinstall, recorded as a deferred test. Pre-flight backfilled the previous session's placeholder hashes to ef3220a.

**Files touched:**
- plugin/si-plugin/docs/setup.md — new "Case B: pre-existing content rules" section after Step 1 (peek-before-Q1 with example; leave untouched, name at close)
- QUEUE.md — [setup-preexisting-content-handling] batch removed at scope-lock; deferred-test line added at close
- LOG/audit-definition.md and LOG/index.md — [HASH] placeholders backfilled to ef3220a at pre-flight

**Routed to Captures:** none
