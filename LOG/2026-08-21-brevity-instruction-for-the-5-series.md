# 746608f — one clause in the always-loaded rules licenses any length, and removing it is the lever the current-model research names

The user's position, which is what makes this urgent rather than tidy: Claude's verbosity is the single thing stopping her promoting the plugin anywhere but GitHub, and the YouTube channel waits on it. She had been hoping the law-prose restyle would fix it as a side effect. It will not — that pass rewrites what Claude reads, and its acceptance test is a rule-statement count that cannot see a line of output.

**The chain that led here, and the user's correction is the load-bearing step**, recorded in full in this chat's own entry: Claude objected that the restyle might make narration rules fire less by removing rationale, she identified that claim as belonging to Opus 4.8, and checking her correction turned up the finding this item rests on.

**The research, cited rather than restated.** `resources/research/opus-5-instruction-compliance.md` and `fable-5-instruction-compatibility.md`, both live and unsuperseded since 2026-07-31. The current model family is verbose by default and narrates readily; **length is a prompt-side control**; and a short brevity instruction steers as well as a quantified target or a list of patterns to kill.

**The defect is one clause, and it is a counterweight rather than a gap.** `skill-nonspecific-rules.md`'s message-shape bullet ends *"giving every explanation the user needs in order to act, in full sentences, at whatever length that takes — what comes out is the padding around it: meta-narration, a restatement of what was just shown, hedging."* On a model verbose by default, an explicit licence for any length is the half that wins. Its second half also states the constraint as a list of things not to do, which the wording rule names as the signal that the action was never specified — so the clause is wrong twice by this project's own standards.

**The sweep found almost nothing else, which is the useful result of running it.** The only self-verification scaffolding in the whole docset is the write-then-re-read before a pointer, excluded by name: the Opus 5 guidance targets verification that buys no quality, and that clause exists for a recorded instance of pointing at text that was not there. The "however long" phrases in two other docs make an obligation unconditional and do not license length.

**Scope was asked and answered honestly: chat only.** The amended bullet governs messages. What Claude writes into documents is governed by a separate rule whose record arm carries the same open licence, and widening there is harder because stripping reasoning out of records is what the throughline exists to prevent. The document half is [processing-appends-where-it-should-rewrite], processed in the same session.

**No figure is introduced**, because the research says a short instruction steers as well as a quantified one — so the ban on underived numbers is satisfied by evidence rather than by omission. The bound that ships with it, from the same research: readable and concise are different things, and readability wins where they conflict.

**Two SPEC sentences were corrected here** rather than left for the build — the claim that rationale is what makes a rule get followed, and the claim that a brevity instruction was tried and measurably did nothing. Both were 4.8 results stated as current truth, and the second had a live cost: it talked this project out of this lever for a whole model generation.

**What is not claimed.** This is the cheapest credible attempt, not a guarantee. It rests on the model guide and on a single clause, and the measurement filed alongside it exists precisely so the outcome is found rather than assumed.

**Queue changes:** [brevity-instruction-for-the-5-series] written into Processed, third in the cleared region, behind the measurement that supplies its baseline.

**Work processed:** kept — [brevity-instruction-for-the-5-series].

**Routed to Captures:** [spec-carries-4-8-era-claims], for the wider class the two corrected sentences belong to.

Rule gate: run — admitted as an amendment replacing one limb of the existing message-shape bullet in the always-loaded rules, subordinate rather than freestanding, so no new slot is spent. **The eviction is "at whatever length that takes"**, repealed outright along with the prohibition list following it, and the limb restated as the action required. No figure is introduced. Failure evidence is the user's sustained report across every context plus the model guide's documented default. A hook was considered and refused: nothing mechanical reads Claude's chat output, a finding reached independently by two other items in this queue.

FAQ: not needed because the user sees shorter replies and does nothing different, which is the FAQ trigger's own test.
