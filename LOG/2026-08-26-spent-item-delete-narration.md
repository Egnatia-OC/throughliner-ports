# [HASH] — Deleting an item whose content has already been relocated is narrated, not asked

Reported from a live consumer session: after a design item had produced six new items and had nothing left in it, the session still asked whether to delete it. The user's observation is the finding — these delete notices always surprise, and narrating the delete probably makes more sense.

The distinction that survived the keep-step. The standing rule protects a *fate* decision: "not worth doing" is terminal, with no later catch, so it keeps its explicit ask. But an item whose whole content was relocated in the same exchange — split into new items, or carried into another home under the relocate-before-removing rule — has already had its fate decided by the relocation, each new home approved as it landed. The ask arrives after the decision it purports to guard, which is exactly why it surprises.

The named risk was that "fully relocated" is a judgment, and a wrong judgment deletes content silently. The mitigation is the narration's shape rather than a test: the one line names where each part went, so a misjudgment is visible in the sentence announcing it, and the removal reverts on objection like any tracked write.

Written as a condition rather than an exception, at both sites — the delete rule and the fold table's delete row — so the two arms sit at the same level.

Refused at planning: narrating all deletes, since a fate decision stays the user's; and a mechanical test for "fully relocated", since it is a judgment and the naming narration is what makes a wrong one visible.

**Files touched:** `plugin/throughliner/docs/plan.md`.
**Routed to Captures:** none.
**Depth:** short.
**Tick:** done, confirmed.
Rule gate: run — an amendment to plan.md's delete rule, its parent, restated as a condition rather than an exception: explicit approval where the item's content has not already been relocated in this exchange; narrated removal where it has.
