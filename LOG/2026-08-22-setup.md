# [HASH] — Setup refresh to 1.20.0-test16: version marker updated, all scaffold checks clean, brevity style declined for this Fable-only project

The version marker read 1.20.0-test13 against an installed 1.20.0-test16, so /setup ran as a migration refresh. Every check came back clean: format epoch already current (4), all scaffold folders and files present, no REGISTRY.md, queue section preambles fine, no stale "Project docs" section in SPEC. Two report-only notes: this project's CLAUDE.md mentions "batch" only to record that the batch type is retired, and it carries no plugin-managed block — expected, since the dev project's CLAUDE.md is hand-written rather than template-based. The only file the refresh changed is `.throughliner-version`.

The brevity output style was offered and declined. The user's reason, in her own words: "this project is fable only" — the style exists to counter verbose models, and this project runs on Fable, so it stays off. Recorded so later setup runs don't re-offer it.

**Queue changes:** none
**Work processed:** none

Advisory: not needed — a setup refresh recommends nothing concrete; the standing advisory from the previous close remains in place.

**Also in this chat:** the commit also carries the previous session's tail — the automatic hash backfill across 22 log entries and the index (placeholder → c904687), and plugin.json's rezip suffix moving test15 → test16, committed as it stands per the standing rule.
