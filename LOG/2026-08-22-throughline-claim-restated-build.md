# [HASH] — README.md and FAQ throughline entries — total-amnesia claim replaced with the vouched-versus-private delta SPEC states

Finding from [claims-need-a-claude-code-delta-test] ([throughline-claim-overstates-amnesia]): README and the FAQ rested the throughline on a fresh session carrying nothing forward, and current Claude Code ships auto memory and context summarization, so the claim failed the delta test on its own terms. SPEC's sentence was rewritten at planning with the user present; this build restated the same ground in README's opening claim, the FAQ's "Why does the method record the reasoning" entry, and — found during the acceptance grep — the FAQ's rename entry, which carried the same amnesia sentence. The delta now claimed everywhere: a structured, user-vouched record of why, versus Claude's private, lossy, unapproved notes, with the fresh-session case as the floor rather than the premise.

Tick: done, confirmed (grep clean of total-amnesia claims across README, templates, FAQ/ and docs/; FAQ/ matches the template byte-for-byte).

Rule gate: not needed — consumer-facing description, no method rule authored.
FAQ: updated — the "Why does the method record the reasoning" entry and the rename entry restated; FAQ/ re-copied.

**Files touched:** README.md, plugin/throughliner/templates/faq-template.md, FAQ/faq.md, FAQ/index.md
**Routed to Captures:** none
