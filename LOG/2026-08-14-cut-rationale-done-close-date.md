# [HASH] — done.md's close-date argument cut, the rule it explains left intact

"Why the close date is right, which is a different argument for each half" explained why the filename prefix and the dates written into a record's words both take the close date rather than the open date. The rule above it already states which date to use, without exceptions, and reads complete on its own — which is exactly what the reclassification test asks.

The argument itself is preserved here rather than lost: for the filename, its only job is the name sort, the close date already does that, and a two-day session sorts correctly either way, so carrying both dates would lengthen every filename forever for an occasional case. For a date written into the words and read later for meaning, the close date is right because the stamp records when the decision was made, and a decision reached in a session's last hour was reached that day whatever day the session opened.

**Files touched:** `plugin/throughliner/docs-b/done.md` (−8 lines).
**Routed to Captures:** none
