# b7b0a38 — /plan: design the buried-user-work emergency fix, resolve the Throughliner rename migration, process reorder-cost + two audits + statusline-reader

A long /plan session, advisory-directed to the buried-user-work emergency first. Opened by verifying (at the user's prompt) that the installed host lags the target — `1.15.0-test7` in cache differs from the working tree on all five core docs, and the completion-ask that inverts `[user]` handovers exists in *both*, so a reinstall alone can't fix it; the walk-through was never actually wired.

The emergency was synthesized into one build item [user-work-surfacing-fix] with a three-part design decided with the user: (1) `[user]` handovers lead with the walk-through offer, never "have you already done this?" — a trailing already-done note only where an item could've been done in a past session; (2) executable plans are queue work items, never a side doc, and a task mixing Claude-work with user moments decomposes into build + `[user]` lines (no in-build touchpoints); (3) a `[user]` line carries a *described* walkthrough drafted at plan time — can't describe the steps → not ready to be a `[user]` line — and `[user]` is earned only by genuinely-can't-do-or-witness work (terminal/installs stay Claude-work). It subsumes and deletes the two symptom children.

The Throughliner rename [rename-to-throughliner] had its gating question resolved by research (filed): renaming does NOT orphan consumers — GitHub's redirect keeps old marketplace registrations resolving, and a `renames` map in marketplace.json auto-migrates each consumer at a one-time `/plugin install`. Decision: big-bang + `renames` map, keep the `flintcraft` marketplace id. The why-pipeline→"throughline" concept rename (thread a of [throughline-feature-elevation]) was folded into this build; threads b+c (rationale provenance, proactive nudging) were skipped, with b flagged to design alongside [provenance-labelling-reconsideration].

Also processed: [reorder-cost-vs-dependency-model] (decision: make /plan's reorders conditional + change-scoped, lean on prose slug-refs, no return to headers/lint); two audits kept as `[audit]` below the line ([plugin-behaviour-debloat-audit], [flavor-doc-core-factoring-audit], the latter with a real dependency on the emergency reshaping its target docs); and [statusline-context-reader], where a live in-session test showed the desktop app did NOT run a custom status-line command (settings load at launch), so a definitive restart-based test was deferred to the emergency's rezip — with the confirmed `used_percentage`-undercount caveat filed.

**Queue changes:**
- Kept to Processed: [user-work-surfacing-fix] (above the line, emergency); below the line: [rename-to-throughliner], [reorder-cost-vs-dependency-model], [plugin-behaviour-debloat-audit] `[audit]`, [flavor-doc-core-factoring-audit] `[audit]`, [statusline-context-reader] — each with a lift-condition.
- Deleted (subsumed into [user-work-surfacing-fix]): [handover-completion-ask-inverts-walkthrough], [install-items-mis-tagged-user].
- Skipped this session (moved to bottom of Unprocessed): [session-sizing-and-break-lines], [next-start-order-handoff], and [throughline-feature-elevation] (re-scoped to threads b+c after thread a folded into the rename).
- Consumed and cleared: forward advisory [advisory-buried-user-work].
- Research filed: resources/research/plugin-marketplace-rename-migration.md, resources/research/statusline-context-reader-feasibility.md.

**Work processed:** kept — user-work-surfacing-fix, rename-to-throughliner, reorder-cost-vs-dependency-model, plugin-behaviour-debloat-audit, flavor-doc-core-factoring-audit, statusline-context-reader; deleted — handover-completion-ask-inverts-walkthrough, install-items-mis-tagged-user; skipped — session-sizing-and-break-lines, next-start-order-handoff, throughline-feature-elevation.

**Routed to Captures:** [inseason-moves-bypass-mover] — /plan's in-session skip/keep moves retype whole prose blocks by hand; wire the mechanical mover into them like the /done reorder.
