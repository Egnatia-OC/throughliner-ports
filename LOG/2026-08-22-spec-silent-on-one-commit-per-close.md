# 2625fa0 — Deleted, satisfied in-session: SPEC now says a close makes exactly one commit and a dirty tree between closes is normal

The item asked whether the commit count and expected-dirty-tree are product truth; settled yes — a consumer meets the "uncommitted changes from a previous session" line at every opening and SPEC is where they would check whether that is normal. Since SPEC sentences are written at planning time with the user present, the two sentences were written in this session (SPEC's /done paragraph) and the item deleted as satisfied rather than kept as a build.

Rule gate: not needed — a SPEC sentence, no method rule.

**Work processed:** deleted — [spec-silent-on-one-commit-per-close], content relocated to SPEC.md.
