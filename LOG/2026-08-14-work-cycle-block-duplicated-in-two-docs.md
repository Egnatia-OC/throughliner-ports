# [HASH] — The work-cycle block moves into the always-loaded rules and is deleted from both procedure docs

Filed 2026-08-13 by Claude at its own close, from a check Alex asked for in the
same session: whether the work-cycle summary given to /plan is the same one given
to /next. It was — byte for byte, twenty-two lines, in `plan.md` and `next.md` and
in no other doc. Re-verified at processing on 2026-08-14: both blocks identical at
234 words, and `done.md` and `setup.md` do not carry it, so this was two copies and
not four.

**Decided at processing: the block MOVES into the always-loaded file, and the
reason is Alex's.** Claude recommended the opposite — shrink `next.md`'s copy to
the two return edges it uses, keep the canonical block in `plan.md`, and leave the
always-loaded budget alone. She overruled it, and her argument defeats the
recommendation rather than merely outweighing it. In her words: Claude needs to
know at all times what the work it is doing means; the cycle does influence how
/setup and /done run, just not in ways that are immediately foreseeable; it has
been a battle without Claude always aware what it is participating in generally,
and the block's arrival could not have been soon enough.

**Why that beats the admission test rather than breaching it.** The test asks
whether a rule fires in all four skills, and it answers by looking for a visible
trigger. This block has none — its effect on /setup and /done is diffuse
orientation — so a trigger-hunting test reads it as not firing there when it does.
The evidence for that is her own experience across sessions, which is not available
to any single session applying the test.

**The cost is accepted with eyes open rather than disputed.** Previously one copy
was paid per session and only by the two skills that move work; always-loaded means
every session pays, including /setup and /done. That is roughly 234 words added at
a moment when three sibling items in this same run are removing text from the same
file. Recorded so a later session does not read the increase as an oversight. Net
across the run the file still fell.

**It is a MOVE, not a copy** — both existing blocks were deleted in the same build,
and neither doc gets a pointer, because always-loaded means both skills already
have it. Landing a third copy would have tripled the drift this item exists to
remove.

**One cross-reference repointed:** `plan.md`'s closing section said the user's exit
is "named in the work cycle at the top of this document", which the move falsified.

**Rule gate: run** — the one addition to the always-loaded file in this run,
admitted on the user's argument rather than on the four-skills test, and recorded
as an admission against the test's grain rather than one that passed it.

**Files touched:** `plugin/throughliner/docs-b/skill-nonspecific-rules.md` (gains
the block), `plan.md` and `next.md` (each loses its copy).

**Routed to Captures:** none.
