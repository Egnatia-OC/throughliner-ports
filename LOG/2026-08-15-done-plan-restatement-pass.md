# b4de5bf — Two restatements cut from done-plan.md, two kept on the placement-site test

Four separate findings were merged into one pass because they shared a file and, more importantly, a single judgment: does a local copy serve the moment of placement, or does it merely restate? Answering that four times separately would have meant reopening the same file four times and risking four inconsistent answers.

Cut: both statements of the never-ask rule, and the dependency-ownership restatement. Neither adds anything to the canonical statement. The never-ask copies are bare prohibitions — "never by asking", "don't reintroduce one under any wording" — and the always-loaded rule now states that action positively, so a prohibition restated downstream is exactly the shape the promotion was meant to end. Leaving it standing would have doubled the text rather than merged it. Dependency ownership is canonical in full; its one local sentence, that order here is low-stakes and reversible so the narration is the catch-point, is an argument about this step and survives as prose without restating the rule it hangs off.

Kept: the retired-headers note and the four-way routing discriminator. Both sit at the exact moment their marker is written, and each carries a sentence the canonical version does not — that `Blocked by:` is lint-checked precisely so it cannot go stale the way `Blocks:` and `Depends on:` did, and that the slug is what makes the below-the-line revisit a single check rather than an interpretation. This is the same argument that saved `next.md`'s routing map from an earlier flavor-table cut: the operative form at the moment of placement is not a duplicate.

Two of the four findings came from an outside redundancy audit's blind spot — that audit stated as its own limit that it was never supplied `done-plan.md` and four sibling sub-docs, so these are precisely what it predicted would be missed.

Rule gate: run — two rule statements evicted, two retained on the placement-site argument. No rule authored or amended; the always-loaded corpus is untouched.

FAQ: not needed because this removes duplicated text from a procedure document Claude reads, and changes nothing a user sees or does.

**Files touched:** `plugin/throughliner/docs-b/done-plan.md`.

**Routed to Captures:** none.
