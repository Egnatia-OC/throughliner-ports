# d9468f3 — chat-level record: a migration that converted nothing, four items processed, and three of Claude's own claims withdrawn on Alex's questions

Chat-level entry for a session that ran /setup, /plan, /rescan and /done. The per-item records are the five entries dated 2026-08-21; this carries what belongs to none of them.

**Mail.** Four waiting messages were read, routed and archived at the /plan opening. Three reported defects in checks that fire on correct work — a converging blocker chain read as an unresolvable loop, the queue lint flagging the two section preambles /setup itself writes, and the mailbox scan counting the operating system's folder-icon file as waiting mail — filed as [digest-diamond-read-as-a-loop], [lint-flags-its-own-scaffolding] and [inbox-scan-counts-os-metadata-files]. A fourth reported that the plugin-managed block in a project's `CLAUDE.md` promises it is kept current and never is, filed as [managed-claude-md-block-never-refreshed]. A fifth report folded into [stop-hook-fires-on-cited-slugs] as its fifth instance, naming a site the earlier four did not: the /plan checkpoint quotes an item verbatim at every pick, so any queue whose items cite each other trips the hook repeatedly. One message is a proposal rather than a defect — whether /setup should be able to start from a template — filed as [setup-from-a-template] with the sender's finding that a template must never carry a decision the copying user has not made.

**Held work released.** The below-the-line revisit found five items whose blockers had shipped and been confirmed in the previous run. Three were lifted with their `Blocked by:` lines dropped and a sentence in each saying what cleared it: [queue-privacy-default], [repeal-falsifies-a-posted-claim] and [rationale-lens-after-the-build-view]. Two were deliberately not lifted — [discord-post-context-adjacency], whose subject has been overtaken twice and which would jump the one-post-per-day chain, and [concurrent-plan-and-build-sessions], whose own text says its design is unfinished. That reasoning existed only in conversation and is now [two-held-items-await-a-disposition].

**Three claims of Claude's withdrawn on Alex's questions, and each correction improved the work.** She asked how items had reached Processed without describing work, which established that they do describe it and that the reader changed rather than the standard — and then that both faults just filed (no epoch bump, no migration recipe) were false, the real defect being a single gate. She asked what standing instructions and one-time steps are, which is a distinction the method's own documents turn on and which nothing explains. And she corrected the framing of the procedure-doc question from "they end when they shouldn't" to "they usually should", which is what made it a design question rather than a rewrite.

**A rule Claude did not follow.** Four inbound messages changed work here and the always-loaded rule to draft a reply unprompted fired for none of them; the mail procedure says read, route, archive and stops. Filed as [inbound-replies-not-drafted]. The one that matters is the template proposal, which asks two direct questions and is waiting on an answer.

**Research filed.** `resources/research/skill-content-lifecycle.md`, with its index line — that an invoked skill's content stays in the conversation for the rest of the session, so no skill invocation ends another, plus the auto-compaction limit that is the one real exception. It is what made [rescan-does-not-hand-back] fixable rather than a platform constraint, and it should be read before any proposal to build a return-to-the-previous-skill mechanism.

**A cost question answered with a measurement rather than a guess.** Alex asked whether /rescan could be made cheaper after a run cost roughly 4k tokens. Measured: the procedure doc is 1,058 words and is the smallest of the five, the three captures it wrote were 1,234 words, and the doc is read once per chat rather than once per run — so repeat runs in one chat cost only what they write. The conclusion was that the mechanism has almost no slack and the cost is the product; nothing was filed, because a capture saying so is not actionable.

**Also filed at the /rescan:** [host-claude-md-names-retired-batches], from /setup's retired-terms report, which had reached chat and nowhere else.

**A second cost question, asked during the close itself, and this one found something.** Alex asked why the close ran to roughly 6,000 tokens, having first asked what the per-entry LOG files were for. The per-entry format is not new — one file per entry since 2026-06-12, 747 of them, and the filenames are load-bearing because shipped-ness is resolved by matching them to slugs. What is four days old is a *planning* record splitting per item processed, and her concern about that split's cost is already on the record in her own words from the day it first ran.

The measurement then inverted the question: this close read **10,297 words of procedure to write 3,603 words of record**, about 2.9:1 against the output, so the entries are the smaller half. `done.md` is a fixed toll on every close in every project, two of its fourteen sections are 45% of it, and a planning close reads and discards the completion check, the build and audit body fields, the audit approval line and the red-flag lifecycle. It is the shape the build view was built for, one layer out — a close reading the whole close manual to run one flavour. Filed as `resources/research/why-the-close-is-heavy.md` with its index line, and the one lever the two queued restyle passes do not cover — distribution rather than wording — captured on her instruction as [done-md-carries-other-flavours-material].

**Both cost questions were answered by measuring rather than by agreeing**, and they went opposite ways: /rescan turned out to have almost no slack, and the close turned out to spend two thirds of its budget on reading instructions.

**The close was refused a write it had just been authorised to make.** /setup's wrongly written format marker was surfaced at the close, Alex decided to set it back to 3, and the scope-lock denied the edit — correctly, since the marker is not on a planning session's standing list, and the close's own declaration marker permits only `README.md`. Her decision is recorded as the first and last steps of [convert-cleared-items-to-build-blocks], and the instance as [close-cannot-correct-a-marker-setup-wrote]. What makes it worth a line is the asymmetry rather than the refusal: /setup could write that marker with no conversation at all, and no session can correct it. That is a third instance of a shape already recorded twice this week, and the item names accepting the delay as a genuine candidate rather than only the two ways of loosening the guard.

**Queue changes:** thirteen captures filed; four items kept into Processed and cleared; one capture deleted as a wrong diagnosis and replaced by two; three items lifted from below the readiness line; one completed `[freeform]` item removed; one SPEC sentence written; two research files and their index lines added. Unprocessed stands at 49, Processed at 23 with 17 cleared.
**Work processed:** kept — [rescan-does-not-hand-back], [setup-migration-gate-is-epoch-3-shaped], [convert-cleared-items-to-build-blocks], [ask-before-writing-a-user-raised-capture]; closed — [setup-outstanding-here]; deleted — [build-block-backlog-unmigrated].

Rule gate: run — four dispositions this session, each written onto its own item at the keep-step and transcribed into that item's entry. Three admitted amendments to existing rules with an eviction named in each; one recorded as not needed. No always-loaded slot was spent by any of them.
