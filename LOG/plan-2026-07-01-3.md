# a9cdbc2 — /plan: promoted [scaffold-field-topup] (add-only session-start top-up for missing settings, editor field first) from the editor-not-backfilled capture; folded the progressive-disclosure token caveat into [behaviour-doc-size-watch] + cross-noted [method-doc-structure-pass]; filed [push-autoupdate-simplification] with research

This planning session processed two captures and filed one.

The editor-field-not-backfilled capture became build batch **[scaffold-field-topup]** at the top of Batches: a session-start, add-only top-up that catches an already-set-up project missing a newer setting (the Editor field is the first case), asking a one-line question where an answer is needed and never clobbering anything the user wrote. Promoted rather than parked because new users are being onboarded while the templates keep moving, so the gap is live now. The harder rewrite case — updating changed template wording without overwriting the user's edits — stays parked as [scaffolding-resync], trimmed to just that remainder once the add-only slice was carved out of it into the new batch.

The progressive-disclosure-token-payoff capture was not promoted. Its finding — that injecting a compact behaviour-doc core and loading the full doc at skill start saves no tokens for real one-skill-per-session usage — was folded into [behaviour-doc-size-watch] as a token-math caveat and cross-noted in [method-doc-structure-pass], then the standalone capture dropped. It retires the token justification for that one lever, not the structural goals (narrowest-home routing, contradictions, untangling) that stand regardless.

Filed **[push-autoupdate-simplification]** as a new capture from a web-search finding: Claude Code checks each installed plugin's commit against its marketplace remote HEAD and can auto-update GitHub-sourced installs. Research at `resources/research/claude-code-plugin-cache-mechanism.md`. Candidate to lean the Push ritual's host-update step on that auto-update path — published-GitHub path only, not local Rezip.

**Queue changes:**
- Promoted [scaffold-field-topup] to the top of Batches (from editor-field-not-backfilled).
- Trimmed [scaffolding-resync]'s parked note to the rewrite-case remainder; add-only slice moved into [scaffold-field-topup].
- Folded a token-math caveat into [behaviour-doc-size-watch]; cross-noted [method-doc-structure-pass].
- Filed [push-autoupdate-simplification] (unprocessed capture) with its research file.

**Captures routed:** promoted 1 (editor-field-not-backfilled → [scaffold-field-topup]); dropped 1 (progressive-disclosure-token-payoff, folded into existing captures); filed 1 ([push-autoupdate-simplification]).
