# [HASH] — /next freeform [two-section-model-reconciliation]: shipped Half A only (park→keep/delete vocabulary in done.md + faq-template.md), stopped when a design fork and a freeform-retirement question surfaced; returned the line blocked, filed 5 captures

Ran the [two-section-model-reconciliation] freeform line. **Half A (retire the parking/defer apparatus)** turned out nearly done already: a corpus sweep found plan.md, next.md, and plugin-behaviour.md were reconciled to the two-section model in prior sessions, and the "defer" usages in the done-family docs are legitimate (return-to-Processed). The only residue was stale "park" routing vocabulary, fixed to keep/delete in done.md and faq-template.md (×3).

**Half B was attempted then reverted.** Drafted Option A (a named "fold" disposition in plan.md's keep/delete routing, plus plugin-behaviour.md and SPEC.md framing edits), but the user flagged that choosing Option A vs B was a *design decision* — planning work that belongs in /plan, not /next. All Half B edits reverted; plan.md, plugin-behaviour.md, and SPEC.md are back to their pre-session state. **Half C (name `[audit]` in SPEC) was never written** — only proposed for approval, which the user questioned.

The stop was triggered by the user asking why design decisions and a SPEC edit were happening in /next, and then questioning `/next freeform` itself. A LOG trace (commits 3c1d9a6, 7563bc0, goal-2026-06-16-2) confirmed freeform was designed as a /next *session type* for retrospective handmade-work wrap-ups, and became a line *flavor* only in the later two-section recut — never designed as one. This opened a genuine question about whether freeform should be retired, which undercuts the premise of Halves B and C. Session stopped by agreement; the reconciliation line was returned to Processed below the cleared-to-run marker, blocked pending /plan, with Half A struck and B/C preserved.

**Files touched:**
- plugin/si-plugin/docs/done.md — Half A (park → keep/delete)
- plugin/si-plugin/templates/faq-template.md — Half A (park → keep/delete, 3 spots)
- QUEUE.md — returned the reconciliation line blocked-below-the-line; filed 5 captures
- (plan.md, plugin-behaviour.md, SPEC.md were edited for Half B then fully reverted — no net change)

**Routed to Captures:** [freeform-retirement-question], [git-panic-on-handmade-edits], [feature-additions-require-readme-edit], [self-hosting-reproducibility], [freeform-discussion-bleeds-into-processing]
