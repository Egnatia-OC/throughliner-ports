# 2c76e53 — Three rationale evictions from plan.md: the orientation read, the cycles step, and the delete branch

From the compliance audit's fourth lens, the delete-and-read test. This item hosted
all three of that lens's evictions in `plan.md`; the sibling captures
[plan-cycles-step-carries-dated-history] and [plan-delete-branch-commentary] were
folded into it at planning and deleted.

**The orientation read** carried two sentences — that the window is "the work done
since the last time anyone stood where you are standing now", and that a read
speaking only when it finds something is indistinguishable from one that never ran.
Delete either and the instruction is complete: read the window, fold in a line when
it names a slug or file the queue also names, carry one line either way. The step
fires at every planning opening, so this is weight every planning session pays.

**The cycles step** ended on the session's own diagnosis written in rule syntax — a
check that spoke only when it filed was indistinguishable from one that never ran,
"which is what it turned out to be". Dated history rather than instruction.

**The delete branch** followed its decision block with a line saying that judging
"fully relocated" is a judgment rather than a test, and that naming each part's
destination is what makes a wrong one visible — commentary plus a restatement of the
block's own narrate arm.

Nothing was relocated. Every evicted why already has a named home: the log-index
window's in the entry for [plan-log-index-read-underdesigned], the cycles clause's
in the entry for [cycles-check-fires-nowhere].

**Two sibling docs were checked and needed nothing.** The item expected shorter
copies of the cycles clause in `next.md` and `done.md` that would have to move
together so the three could not disagree. Both already state the speak-either-way
requirement bare, so there was nothing to cut.

**One correction made during the close, recorded because it is exactly the sort of
thing that otherwise vanishes.** The build first deleted an adjacent sentence in the
delete branch — "Still a delete, just after its worth-keeping content has been
carried across" — rather than the line the item named. Caught while reading the
item's reasoning back at the close: the named line was restored to the queue's
version and removed properly, and the out-of-scope deletion was reverted. The build
did what was agreed and nothing more.

**Files touched:** `plugin/throughliner/docs/plan.md` — three sites.

**Routed to Captures:** none.

Tick form: done, confirmed — a grep for "indistinguishable from a" across the
shipped docs is empty, and each touched instruction reads complete after the cut.

Rule gate: run — an eviction pass over three sites, nothing added, three rationale
clauses removed, each why already held by its named LOG entry.
