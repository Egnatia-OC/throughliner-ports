# 3102929 — File-browser reports gain a check clause on the handoff-claim provenance rule, host-only

Kept as one clause amending this project's CLAUDE.md provenance section: where the user reports file state seen through a file browser, check `git log` and the filesystem before reasoning from it, and name what was checked rather than contradicting the report. Host-only — the section lives here and the reporting habit is this user's; it generalises if a consumer reports the same. Build block on the item.

**Queue changes:** [drive-dates-are-not-edit-dates] moved Unprocessed → Processed, cleared.
**Work processed:** kept.
