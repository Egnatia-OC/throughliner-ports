# [HASH] — Kept and cleared: the purpose statement ships in the always-loaded rules

Processed 2026-08-24 on the content brief settled with the user, so the build transcribes rather than composes: intent keeps running the project whatever Claude remembers; reading and approving the record is the product, not overhead; settled things stay settled because rejected options carry why they lost; short cheap sessions work because intent lives in files. Shape: a short block in the always-loaded file modelled on the work-cycle block (the admission answer — orientation on that block's precedent, not a rule, no slot consumed), plus a what-this-is-for line atop each skill doc. The honest limit carried: nothing fires when Claude misunderstands the point — better odds, no guarantee. SPEC's rules-doc bullet gained the sentence this session.

**Work processed:** kept — [method-purpose-orientation], cleared with build block.
