# d82f538 — The urge to drive verification informally is routed into a skill, stated where the post-close tail is defined

This item was filed on 2026-08-01 with three parts. **Two of them had closed by the time it was processed, and the item shrank to its third.**

Its safety point — that the file scope-lock is fully off in a post-close tail, so the user runs the whole tail with no containment and is not told — is closed, and closed *incidentally*, which is worth recording. `pre_tool_use`'s classification keys on the build working file being **absent** rather than on any positive detection of a planning session, so once the close deletes that file the tail falls under the planning standing list. That containment arrived with the scope-lock restoration the user ordered, which was not aimed at the tail at all. The tail is contained today by a rule that does not mention it — true now, and true only for as long as the classification stays keyed that way.

Its routing point is closed outright: `done.md` names the post-commit tail, defines the marked tail section an entry carries, teaches a session to recognise an inherited tail, and states that a session makes exactly one commit while the tail makes none.

**What survives is the clause folded in from a deleted sibling item, and it is now the whole build.** When Claude finds itself wanting to run or drive testing or verification — especially work that is already a `[user]` line — that urge is the signal to stop and route it into a skill: a quick /plan to structure the lines, or /next to hand over the line that exists. The recorded instance is 2026-08-01, where informal verification in conversation set off the whole tail and its findings came back as loose captures. Same failure family as under-filing a `[user]` line, and it blurs the plan/execute boundary from the far side: executing structured work with no skill around it.

**Sited in `done.md` beside the tail's own definition rather than in the always-loaded rules.** It was folded here originally because "a loose tail" was not a named thing and the rule had no subject; it is named now, which frees it. A session in a tail has just read `done.md`, so the rule reaches it there at no always-loaded cost and with no amendment to the four-skills admission test.

**The weakness in that siting, stated rather than discovered later.** The urge is not confined to a tail. It happened in one, and nothing says it only happens there. Siting it here covers the recorded instance and not the general case. That is accepted because the gate admits on recorded instances rather than anticipated ones, and the reopening condition is a second instance occurring outside a tail.

Depth: short.

Rule gate: run — admitted as a clause on `done.md`'s existing post-commit tail definition, which already governs what the tail may and may not do; no freestanding rule and no always-loaded slot spent. **Two of this item's three parts were evicted at processing rather than built**, both because the code and docs had overtaken them. Failure evidence is one recorded instance, thin and admitted as such on the ground that the clause costs a sentence in a doc the tail has just read.

FAQ: not needed — nothing a user does changes.

**Files touched:** `plugin/throughliner/docs-b/done.md`.

**Routed to Captures:** none from this item.
