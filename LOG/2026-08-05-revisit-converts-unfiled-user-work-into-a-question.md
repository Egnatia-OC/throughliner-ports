# [HASH] — below-line revisit + lift-condition recording gain the downstream-action discriminator: an awaited event downstream of an unfiled user action files a [user] work item instead of recurring as a question

A consumer /plan asked, session after session, whether a collaborator had come back on three shelved items — and the user's response was the finding: there should have been `[user]` work items walking them through messaging the collaborator, and there were none. Every rule behaved as written: the items recorded prose lift-conditions, the revisit classified them user-only, batched them into one question. The mechanism manufactured user-work evaporation from correctly-followed rules, because "the collaborator replies" reads like an external event while being downstream of an action never filed anywhere. The item stated its own discriminator and both firing moments, which is what cleared it under the blitz bar. Shipped: plan.md's revisit runs the downstream-action test before asking any user-only condition (downstream of a user action → propose filing that action as a `[user]` work item, condition rewritten to wait on it; genuinely external → the condition stands); done-plan.md's recording rule applies the same test at the moment a condition is written, which is where it can fire first; plugin-behaviour.md's under-file rule names this as under-filing's second face, cross-referenced. What-went-right preserved explicitly: several conditions awaiting the same person consolidate into one walkthrough — never several that have the user message the same person repeatedly.

Overnight blitz run 2, phase 3 (branch overnight-blitz-2026-08-05b): processed and built under the blitz plan's softened bar and sanctioned departures — approvals deferred to branch review, no push.

**Files touched:** plugin/si-plugin/docs-b/plan.md, plugin/si-plugin/docs-b/done-plan.md, plugin/si-plugin/docs-b/plugin-behaviour.md
**Routed to Captures:** none
FAQ: not needed because the user-visible change is fewer repeated questions; the [user]-item behaviour the FAQ describes is unchanged.
