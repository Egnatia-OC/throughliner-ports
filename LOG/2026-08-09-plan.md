# [HASH] — The method's regressed state recorded as a standing note above both queue sections, the compression cut cleared as the next build in deliberate bypass of its audit gate, and this branch's missing cycle items seeded by hand

The session opened as an ordinary /plan and became a state-of-the-project reckoning. The user's report, in their own words, is that the method "feels nothing like Throughliner": a week ago they were starting to feel they could almost give it to a user, and now they would not. Three complaints drove everything that followed, and each was checked rather than accepted on report.

**The docs are ballooning and the shrinking work is not working.** The user reported `plugin-behaviour.md` at over 18,000 words against roughly 11,000 two weeks ago, in a period where they pushed at nearly every planning session to get them smaller. Measured: **18,600 words**. Their description of what is actually happening — "you're just minusing little phrases here and there to try and save size, and adding paragraphs here and there to fight the regressions" — matches the record: every incident produces a paragraph-sized rule and essentially nothing is ever removed.

**The cause was established rather than guessed, and it is structural.** `resources/compression-pass-plan.md` requires a full-corpus audit as its precondition; that audit sits at a merge boundary; the merge sits behind the differential audit and the branch cycle. So the one mechanism that would shrink the docs has been permanently downstream of the machinery that keeps growing them, and has therefore never run once. The user greenlit bypassing that gate directly, and [compress-plugin-behaviour] was written and cleared as the only work above the readiness line: target under 10,000 words, by **deletion and relocation only** — never rewording, since rewording changes the method while deleting scar tissue does not.

**The three cutting rules came out of dissecting one specimen the user picked.** They pointed at the keep-step paragraph embedded inside the lead-with-the-decision rule and asked why /plan-specific length guidance lives in the always-loaded doc. It does not survive examination: the block's own justification is that two rules governing one moment means whichever was read most recently fires — but the fix that argument implies is *one* rule at the keep-step in `plan.md`, not one rule in the wrong document. It costs roughly 200 words in every session, most of it incident retelling that already exists in the LOG, and it has not worked: [keep-step-recommendations-still-run-long] was filed the previous day as the third recorded slip, by a session that had the paragraph loaded from its first turn. Generalised, that gave the build its method — rule text goes to where the moment happens, evidence goes to the LOG, one statement site per rule — with this block named as cut #1.

**The interaction layer has gone flat, and this session produced its own live instance.** The user's report: "all the interaction elements are gone. there's no offers being made, no questions being asked by claude, just reporting. the only reason I know what to say is because of my vast experience with throughliner." /plan's opening here narrated five findings, each with an obvious action attached, and offered none of them — the consolidate-the-scans rule was read as licence to drop the asks along with the narration, when it governs only how findings are *reported*. The user caught it; Claude did not. That is the product failing, not manners: the method's premise is that a non-coder is led.

**The merge cycle was checked against the user's "total mess that looks different every single time", and the finding is more specific than the complaint.** The cycle *is* fully written in CLAUDE.md. What is true is that it has been rewritten across three consecutive sessions, that nothing enforces any of it, and that the seeding step meant to end the retelling shipped **after** this branch was cut — so `overnight-blitz-2026-08-09` carried none of its cycle items and the cycle lived only in prose. All three were seeded by hand, with the merge item carrying the gate and step 6a. When the user asked what seeds them in future, the honest answer is that the merge item's own walkthrough does — the chain is self-perpetuating but **not self-healing**, since a merge that skips step 6a leaves the next branch bare with nothing detecting it. That gap is now [cycle-seed-backstop-in-plan-open].

**A standing note was placed above both queue sections at the user's explicit request**, recording all five complaints in their own words, persisting until they say the state has lifted — deliberately not tied to any item shipping, because it records how the method *feels to use*, which no build completing can settle on their behalf. It went in first at the top of Unprocessed, per the advisory convention, and the user reported "it's not there" — it was, at line 362 of 478, while every link lands at the top of the file. It was moved to a blockquote under the queue's header prose, above `## Processed`, which is a structural novelty in QUEUE.md: outside both sections so nothing can mistake it for work, and it states that about itself. A `####` item there would have broken section keying, which is why it is a quoted block rather than a work item.

The user held all five greenlight candidates and greenlit nothing. Nothing was built this session.

**Queue changes:** [compress-plugin-behaviour] written and placed at the top of Processed, above the readiness marker, as the only cleared work. Three cycle items seeded into Processed below the line: [blitz-run-2026-08-09], [differential-audit-2026-08-09], [merge-2026-08-09] (the last blocked by the audit and carrying the merge gate in its own prose). The REGRESSED STATE note placed above both sections as a blockquote. A stale `Blocked by: [postcompact-firing-probe]` line removed from [user-postcompact-firing-check] after the queue lint flagged it — its blocker shipped in the previous run; its real wait, the app restart, stays recorded as a lift-condition. [advisory-2026-08-08-cleared-block-run] deleted as spent. [advisory-2026-08-09-restart-then-seed] rewritten to only its surviving half, the pending app restart. [advisory-cleared-run-then-history-rewrite] left in place, its condition unmet.

**Work processed:** none kept or deleted from Unprocessed — the session never entered the item-by-item processing loop, because the user's report redirected it before the queue walk began. [compress-plugin-behaviour] was authored fresh from that discussion rather than processed from a capture.

**Routed to Captures:** [cycle-seed-backstop-in-plan-open], [done-two-stops-consumer-evidence], [interaction-regression-reporting-not-offering]. One further candidate surfaced by the second re-scan — that advisories are filed where nobody arriving at the queue can see them — was drafted and dropped at the user's call.

**Below-line revisit findings, recorded because they are unactioned and will otherwise be re-derived:** [user-postcompact-firing-check]'s lift-condition has cleared (the probe is in the installed host, stamps matching) and a lift was offered but overtaken by the session's redirect. [release-docset-b-work] is **spent** — it describes releasing v1.17.0, which shipped. [docset-b-register-decision-weaker-than-recorded] and [structural-delimiting-experiment] both rest on a two-docset comparison that the docset-A retirement removed. All three need reprocessing. The three tracked Claude Code issues (#84017, #84018, #83475) were checked and all remain open, so nothing lifts there.

**FAQ:** not needed because this session changed only QUEUE.md and LOG/ — no shipped behaviour changed and nothing a consumer meets is different.

**Cycle:** building, seeded by hand; nothing greenlit.
