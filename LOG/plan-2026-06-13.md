# [HASH] — /plan: 5 captures + 4 discussion threads → two new audit batches ([behaviour-doc-firing-map-audit] at top, [self-hosting-notes-audit]), [deferred-test-lifecycle] execution channel redesigned (pre-flight offer dropped → /plan rolls runnable tests into test batches + /done backstop), [closeout-text-collapse] absorbs commit-message passing (git commit -F) and plain-identity presentation, new batches [scope-lock-drop-dash-stripping] and [staleness-flag-fix-path], [done-recommend-next-both-ways] absorbs [done-audit-overlap-scan], [behaviour-doc-size-watch] parked.

This session processed the five waiting captures and four discussion threads the user raised.

Two captures became standalone batches. [scope-lock-drop-dash-stripping] removes the scope-lock parser's dash-stripping loop so the bare-path rule is the single truth the code actually enforces — the parser was silently tolerant of dash-annotated Files: lines while the denial message taught that any annotation breaks the match. [staleness-flag-fix-path] defines whether a flagged stale reference is fixed at /done or waits for /plan: pointer drift on an otherwise-valid item is mechanical and rides the commit with approval, while a fate decision (drop/rewrite/keep) or a content change goes to /plan; the location-vs-content line is written in so the ad-hoc judgment doesn't just move to the next quoted-string flag.

Two captures folded into [closeout-text-collapse]. The commit message is passed via a file and git commit -F to avoid shell-quoting failures — the capture's "PowerShell has no heredoc" premise was wrong (PowerShell has here-strings), but the real lesson is broader: inline multiline strings are brittle to generate in any shell. And the commit step states the derived message's identity in plain words rather than a meta-description, which had left the user unable to tell what was done at the ef3220a commit.

The fifth capture became [done-recommend-next-both-ways] — the /done recommend-next step states the capture-overlap scan's result in all three states (empty, clean, overlap) instead of speaking only when blocked — and it absorbs the queued [done-audit-overlap-scan] one-liner, landing the scan in done-audit.md in the same pass.

On the discussion side: the user challenged deferred tests appearing at /next pre-flight, where the information has nowhere to go. [deferred-test-lifecycle] was amended to delete that step and route runnable deferred tests through /plan into test batches — never build batches, which would break unattended autopilot — with a cheap /done close-out backstop that pays mainly in self-hosting. The user opened a self-hosting distribution arc: [self-hosting-notes-audit] inventories every self-hosting rule and judgment for later relocation to its right doc with rationale. A doc-size review found plugin-behaviour.md is the largest and most expensive doc (injected every session, paid twice per skill session until [behaviour-doc-double-load] ships); the user challenged the trickle-up premise that cross-skill means every-skill, producing [behaviour-doc-firing-map-audit] at the queue top, which maps each rule's firing to sort it into stays-injected / trickle-down / middle-band. The terseness question resolved as no-blanket-pass and parked as [behaviour-doc-size-watch] pending the doc settling.

**Queue changes:**
- New audit batch [behaviour-doc-firing-map-audit] at the queue top.
- New audit batch [self-hosting-notes-audit] after [spec-entry-trigger-rethink].
- New batch [scope-lock-drop-dash-stripping] at the end of Batches.
- New batch [staleness-flag-fix-path] in the /done close-out cluster.
- [deferred-test-lifecycle] amended: execution channel redesigned (title and rationale updated; /next pre-flight step replaced by /plan batch-rolling + /done backstop).
- [closeout-text-collapse] amended: two folds — commit-message passing mechanism, plain-identity presentation.
- [done-recommend-next-both-ways] replaces [done-audit-overlap-scan] (absorbed).
- [behaviour-doc-size-watch] filed to Captures Parked.

**Captures routed:** five — two promoted as new batches ([scope-lock-drop-dash-stripping], [staleness-flag-fix-path]), one promoted as [done-recommend-next-both-ways], two folded into [closeout-text-collapse]; none parked or dropped. Separately, [behaviour-doc-size-watch] was created and parked from the doc-size discussion.
