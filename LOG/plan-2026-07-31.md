# [HASH] — /plan: Throughliner rename decided; next.md + filing-rule + record-integrity clusters processed to cleared; session-file-cleanup reframed prevention-first

This /plan worked the accumulated Unprocessed backlog and made one strategic decision: renaming the plugin from "Sovereign Implementer" to **Throughliner** (full technical identity + positioning). "Sovereign" overclaims a platform/model-agnosticism the method doesn't have — bare-minimum agnosticism died with the Codex shelve — so the name now leans on the tool's real value: durable rationale that survives a forgetful session. Name availability was web-researched (Throughline is a taken software name; Throughliner is clear) and filed to resources/research/. Captured as [rename-to-throughliner] with the consumer-migration path as its gating design question; a mirror-rename of the why-pipeline concept to "throughline" plus rationale-provenance and proactive-nudging ideas captured as [throughline-feature-elevation].

Most of the session cleared method-fix work above the line: a next.md refinement cluster (remove the context-runs-long self-detection stop; reframe the pre-flight "Ready?" as a scope/reorder off-ramp; bundle independent `[user]` handovers), a filing-rule cluster ([resources-purpose-and-triage] three-way triage; [temp-file-delete-time] declare-delete-time; [session-file-cleanup] reframed prevention-first after the user showed the historical-file sweep + don't-ask-again was unsound), the merged CLAUDE.md staleness refresh, the file-granularity heuristic (provenance corrected to the user, who originated it), and two record-integrity fixes (filing-time capture stamp; advisory-clear backstopped at /done). The flavor-doc merge question was resolved in-session (don't merge — on-demand loading + genuinely different procedures + already shared-core-factored).

**Queue changes:**
- Kept to Processed (above the line): claude-md-stale-ports-section (merged with the former claude-md-stale-deferred-tests), next-context-self-detection, ready-stop-redundant, multi-handover-presentation, resources-purpose-and-triage, temp-file-delete-time, session-file-cleanup, ai-file-granularity-payoff, post-close-capture-not-logged, plan-skips-advisory-consume.
- Filed to Unprocessed: rename-to-throughliner, throughline-feature-elevation, log-file-sort-heuristic, plugin-behaviour-debloat-audit `[audit]`, flavor-doc-core-factoring-audit `[audit]`, and rezip-before-verify-advisory (forward advisory, top).
- Deleted: next-nontermination-fix-standalone (advisory already honored by fda7b07); verso-transcript-not-bug-evidence (correction relocated into the transcript file's header).
- Reordered both sections; `[user]` handovers moved end-preferred above the marker; marker positioned after report-url-404. Big undesigned strategic cluster skipped-to-defer.

**Work processed:** kept 10 (all above the line); 1 merge; filed 5 captures + 1 advisory; deleted 2. No red flags cleared this session.
