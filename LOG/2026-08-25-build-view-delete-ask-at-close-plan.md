# [HASH] — Kept: the build view's lifecycle specified — deleted silently at the close, ignored by the scaffold

The capture's open question answered by grep: the lifecycle was never specified — next.md regenerates the view and nothing else says anything, so the demo session improvised its delete question into a genuine gap; this project's own hand-made ignore line was evidence of the gap, not the fix. Design: the close deletes the view silently (every run rebuilds it), and setup ignores it on both paths. Rule gate run — disposition on the item.

**Queue changes:** [build-view-delete-ask-at-close] kept into Processed, cleared.
**Work processed:** kept — [build-view-delete-ask-at-close].
