# [HASH] — The checkpoint gained a specimen of the message it asks for

`plan.md`'s checkpoint now shows the actual shape of the message — a pointer to
the next item, with nothing beneath it — placed before the rationale rather than
after it. The why-the-recital-went passage was compressed in the same edit,
keeping the user-acted-on-it evidence, which is the part that stops the recital
being reinstated.

The failure this fixes was measured rather than estimated. The step says to
present the next item and adds "nothing else. No menu of routes." Claude instead
ended every one of seventeen checkpoints in one session with "Next item, or stop
here?" — a recited two-route menu naming closure — and never presented the next
item at all. Both halves of the step were inverted: the forbidden thing done every
time, the required thing never.

It is worse than an ordinary slip because that string is close to the one the step
was written to remove, and the rationale recording a user acting on exactly that
reading sat in the loaded procedure while it happened.

The hypothesis is testable rather than a shrug about attention. Every nearby step
Claude did follow closely carries a specimen — beat 2's ordering ask, the rung-6
continue offer, the four-routes line, the floor narration — and all were delivered
close to as written. The checkpoint had a prohibition, a message-order list, and
six paragraphs of history: the operative instruction was the shortest part of the
longest passage, and it said what not to write rather than what to write.

No hook and no mechanical check, deliberately: narration is not detectable, and
this project has refused checks that cannot fire honestly. It is a standing
candidate for the narration-drift lens in the compliance checklist, which is the
only thing that would have caught it.

Rule gate: run — admitted as an amendment; the specimen adds no obligation, and
the compression is eviction in the same edit.

FAQ: not needed because the user sees a shorter message, not a new one.

**Files touched:** `plugin/si-plugin/docs-b/plan.md`
**Routed to Captures:** none
