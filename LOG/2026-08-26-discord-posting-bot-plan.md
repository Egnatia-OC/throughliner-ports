# [HASH] — Discord bot designed as two items: your setup line cleared, the posting script held behind it; token risk cleared as informed consent

Kept on Claude's recommendation and your agreement — your words at the disposition: "ok I'm willing to try it." It designs out smaller than "bot": no hosted service, a bot account plus a script making one API call per send or edit, standard library, exact text passed from a file, every send behind the existing exact-text-yes rule with its register line. Split: [discord-bot-server-setup] is the `[user]` line (create the bot, invite it, hand the token), cleared with a full walkthrough; [discord-posting-bot] is the build, held behind it, whose CLAUDE.md half amends the "Claude has no route to Discord" sentences.

**Red flag cleared — informed consent, recorded here as the close requires.** You were told plainly: the bot token is a credential on your disk; anyone or any session that reads it can post as the bot in your server; the design out (gitignored `INBOX/` storage, the never-quote rule) protects against publication and copying into committed files, and the token stays readable on this machine — that residual cannot be removed. You chose to proceed. The marker on both the item and this trail reads cleared on that consent.

**Queue changes:** [discord-bot-server-setup] new and cleared (end of the ready list, with the walk-throughs); [discord-posting-bot] kept, held below the line, Blocked by the setup line.
**Work processed:** kept — [discord-posting-bot], [discord-bot-server-setup].
Rule gate: run — amendment to CLAUDE.md's Discord section (no-route sentences replaced by route-plus-approval); the send-approval rule unchanged and cited; a hosted always-on bot refused.
