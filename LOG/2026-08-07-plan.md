# [HASH] — The pre-compression audit's fifteen findings processed into nineteen cleared work items, two of them code fixes split out for a privacy exposure and a hook asymmetry, and two dead items cut from the cleared region

The session opened as a /next run and never built anything, which is the first thing worth recording. /next reached [no-session-end-or-compaction-hook], ran the documentation check that item made load-bearing, and found the same thing a run the previous day had found: no compaction event can inject context, so the item's whole build is unavailable. **That correction had already been captured — and the capture did not stop the item running again**, because the parent sat above the readiness line with nothing linking the two. Two runs, the same research, the same halt. The general lesson is written onto the surviving capture: a correction filed as a sibling does not disarm the item it corrects; if the corrected item is still cleared, it is still going to run. The parent was deleted at the user's decision and its live content — the ungoverned-after-compaction problem, and the rejected session-end nag — folded into [compaction-reinjection-has-no-usable-event], which now holds one undesigned question: whether re-injection is worth hanging off a per-turn event that *can* inject, and what would keep it from firing every turn.

The session then turned to the pre-compression doc audit that had just landed. **The first read of QUEUE.md was a stale snapshot** — 59 lines had been appended after it, holding the audit's fifteen `[audit-…]` captures, and Claude reported them missing on the strength of that read. Worth noting because it is the viewer-staleness problem the queue already tracks, arriving from the tool side rather than the user's.

Processing ran as a bulk keep for eight items whose builds could be stated without a decision from the user, then one at a time for seven that could not. **Three of the seven turned out not to need the user at all, and finding that out took one command each.** [audit-abort-requeue-stale-model] reported two incompatible queue models with no way to tell which was stale; reading the frozen docset A settled it in one grep — A removes items up front, so its text is coherent and docset B's is the leftover. [audit-plan-marker-implement-or-retire] was worse than reported: the marker is documented in six places and implemented in **neither** docset, so it was never built rather than built and lost; the user chose retire, on the method's own precedent that a gate maintained in two places and honoured in none is worse than no gate. And [audit-resources-shape] had its recommendation reversed by running the thing it dismissed — the mover's "unreachable" test suite passes, with explicit marker-position tests, which makes it a suite with a coverage gap exactly where a live reported bug sits, and an argument for wiring it into the rezip ritual rather than deleting it.

**One finding carried a real privacy exposure and was split out with a red flag.** `session_start.py` silently writes a payload sample into `resources/research/` whenever that folder exists; in this project the file is tracked, committed, and holds the absolute project path including the account name, in a public repo. The flag is cleared by the fix designed here — stop the write, delete the file, route the published copies to the history rewrite that is already rewriting every commit. The exposure is smaller than one already known about this repo, but the mechanism is consumer-facing: any project with that folder gets its own path written there, silently, with no gitignore and no doc saying so. Its sibling split, [editing-state-closing-marker-ignores-adoption], had been reported as a design call about which side moves; SPEC already answers it, so the code moves.

The user proposed making the method's project docs private everywhere and retiring the privacy rules as no longer relevant. **Claude recommended making the repo private and was wrong**, which is filed as its own capture: the plugin's entire distribution runs through this public repo, so making it private ends every install and update — a fact one grep of README.md away, in a session that had already read that file. The proposal was reshaped, then withdrawn by the user, with the sharing-warning half kept as [warn-before-sharing-project-doc-content]. That one earns its place on its own: every existing privacy rule fires at write time, and nothing fires at the moment the user decides to send something somewhere.

**Claude's own behaviour produced three captures**, filed at the wind-down re-scan and approved. The sharpest is that Claude wrote a placeholder token into QUEUE.md as an editing scaffold — the same token-in-prose damage as a capture read aloud earlier in the same session — which makes the point that local attention was not the missing ingredient. The new part is the mechanism: not misreading, but using the live document as scratch space when a purpose-built mover exists.

The close emptied and refilled the cleared region. Both items that had been above the readiness line were deleted — the compaction item for a dead premise, and the report-page test because that work is happening in the site project itself — leaving nothing for /next to run. Processed was then reordered so the nineteen items processed today sit above the line and the sixteen long-standing ones, each held below by its own lift-condition or awaiting the user's greenlight, stay below it.

**Queue changes:** Processed reordered whole (35 items) so today's work sits above the readiness marker and the previously-shelved work below it; the marker repositioned from the top of Processed to below [user-verify-app-ui-premise-claims], clearing 19 items. Two items deleted from Processed ([no-session-end-or-compaction-hook], [report-url-404]) and one deleted after processing ([make-project-docs-private]). Two `Blocked by:` lines re-checked and both still resolve upward. [advisory-build-while-designs-are-fresh] rewritten to what still stands — its item counts and its remote-control claim were spent; the other three advisories left in place with their persist-conditions unmet. No new forward advisory filed: the rewritten one already carries the recommendation, so a second would restate queue position.

**Work processed:** kept — [audit-claude-raised-closer], [audit-done-close-order-contradictions], [audit-scaffold-redflag-text], [audit-shipped-faq-stale-claims], [audit-agent-matcher-gap], [audit-stale-pointer-names], [audit-setup-scaffold-residue], [audit-terminology-anchors] (the eight bulk-kept); [audit-plan-marker-implement-or-retire] (as retire), [audit-next-redflag-backstop-missing] (as implement), [audit-abort-requeue-stale-model], [audit-retirement-leftovers] (three inert files to delete), [audit-resources-shape], [audit-hook-doc-reconciliation] (split three ways), [audit-premise-claims-verification] (split two ways), [warn-before-sharing-project-doc-content]. Created by splitting — [session-start-writes-payload-sample-into-repo] (red flag, cleared), [editing-state-closing-marker-ignores-adoption], [user-verify-app-ui-premise-claims]. Deleted — [no-session-end-or-compaction-hook], [report-url-404], [make-project-docs-private]. Filed to Unprocessed — [placeholder-token-written-into-queue-as-scaffold], [recommendation-made-without-a-one-command-check], [mover-move-section-marker-evidence].

**Red flag cleared this session:** [session-start-writes-payload-sample-into-repo] — cleared by designing the fix in-session rather than by acceptance. The user was told plainly what the file contains, that it is committed to a public repo, and that deleting it forward does not remove it from the history; the fix stops the write, deletes the file, and routes the published copies to [history-rewrite-third-party-scrub]'s replacement set.

**Four premise-bearing claims confirmed incidentally** and recorded on [audit-premise-claims-verification] so nobody re-checks them: the session-start docset directive genuinely substitutes `docs-b/`; the "file modified on disk" warning fires as described and the queue mover trips it; the desktop app omits the `model` field (the captured payload holds exactly `cwd`, `hook_event_name`, `source`); and the mover's test suite passes, though not over the case the live bug describes.

**FAQ: not needed because** this session shipped no user-facing change — it processed queue items, reordered the queue, and recorded decisions. The FAQ-affecting changes it *designed* ([audit-shipped-faq-stale-claims], [audit-plan-marker-implement-or-retire], [warn-before-sharing-project-doc-content]) each carry their FAQ work in their own file lists, to be dispositioned at the close that ships them.

**Queue changes and Work processed above were filled from `_plan.md`'s disposition record**, not reconstructed from memory.

**Routed to Captures:** [placeholder-token-written-into-queue-as-scaffold], [recommendation-made-without-a-one-command-check], [mover-move-section-marker-evidence].
