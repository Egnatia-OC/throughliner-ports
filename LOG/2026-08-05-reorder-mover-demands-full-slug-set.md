# d9162e4 — reorder_queue.py gains a relative move: --move <slug> BEFORE|AFTER <anchor>

A two-item reorder demanded the section's full slug set — twenty-four hand-assembled slugs on the current Processed — because the total form was the only form. Added BEFORE/AFTER anchors to the existing --move (which already proved the relative shape with TOP/BOTTOM); the full-set check stays on the total form, where it earns its keep guaranteeing no item is lost. Same byte-for-byte block preservation and self-checks. Exercised against a scratch copy of the real queue before shipping. Processed-and-built in the overnight blitz of 2026-08-05 under the softened bar (fix shape stated in the item); autonomous run — recorded departure.

**Files touched:** plugin/si-plugin/scripts/reorder_queue.py
**Routed to Captures:** none
FAQ: not needed because the mover is internal machinery consumers never invoke by hand.
