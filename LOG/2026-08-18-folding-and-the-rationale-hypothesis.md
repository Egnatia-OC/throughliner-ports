# 02ec308 — three captures from the user's observation that the ideation loop fixed accretion at the front and not afterwards

The user's starting point: before the ideation loop, a new capture was written to three or four times before anything else was processed, and holding the write until the design settled removed that. Her question was whether the same principle reaches the accretion that happens later.

**Folding is where it lands, and her framing is that folding is native Claude behaviour the method leans on constantly and describes nowhere** — which is why it has no controls. Two findings follow. Folding is **admission without eviction**: adding a rule requires naming what comes out, folding content into an item requires nothing. And the loop's transferable principle is settle-first-then-write-once, which for a fold means **rewriting the host item rather than appending to it** — today's folds appended, leaving both accounts of the same point in place. Measured: four items absorbed content and every one grew.

**Her second idea is the one that could reframe other work.** That rationale inside a build item is read at build time as content to be built, and flows into the shipped docs — which would make the rationale accumulating in the always-loaded rules a symptom, and both passes chasing it symptomatic too. It is testable from git and filed with that test.

**Her refinement is what keeps the fix from breaking audits:** some items need their rationale because the rationale *is* the work. So the distinction is operational rationale against historical rationale, and the method already owns the test — delete the sentence and read what remains.

Her third idea, splitting the cleared region into its own file for builds, is filed with the two places it breaks: planning's keep-step writes into that same file, and merging back can resurrect an item a build already consumed.

**Queue changes:** three captures filed in Unprocessed; the ordering between the rationale hypothesis and the restyle written into both.
**Work processed:** none — filed for a later /plan.
