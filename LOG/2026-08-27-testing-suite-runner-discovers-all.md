# [HASH] — plan — [testing-suite-runner-discovers-all] kept: a runner discovers every suite; the ritual's stale three-suite step points at it

The ritual's testing step named three suites when the folder holds about twenty — the release gate covering a shrinking fraction and reporting a pass over suites never run — and invoked `python`, the Inkscape-interpreter trap the scripting constraints exist for. Discovery replaces enumeration: a stdlib run-everything script both ritual steps call with `py`, so new suites are picked up automatically. CLAUDE.md's close rule already names no suites — excluded.

**Queue changes:** [testing-suite-runner-discovers-all] filed and cleared; [release-ritual-suite-step-stale] deleted as merged.
**Work processed:** kept — [testing-suite-runner-discovers-all].
Rule gate: run at the keep — host-only supersession; recorded on the item.
