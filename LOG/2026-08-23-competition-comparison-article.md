# 3cc6f44 — The comparison article rewritten around Papi and a corrected AutoDream claim; item stays open

`[user]` item, walked through live and **not completed** — its last steps wait on an external review, then publication and confirmation. The item stays in Processed.

**Files touched:** `resources/research/auto-memory-staleness.md` (dated 2026-08-23 re-verification block), `resources/research/index.md` (its line updated in the same move), `INBOX/sent.md` (the hold-note's line), and a new message in the site project's mailbox. The article itself is in the session scratchpad, deliberately — the item's own file list says none in this project.

**Routed to Captures:** [log-retrieve-search-leg], from the user's question about what to implement; [walkthrough-repeats-completed-steps], from the collision this walk-through hit.

Rule gate: not needed — an article authors no method rule.

FAQ: not needed because an unpublished article changes nothing a user does.


`[user]` work, walked through live in this session. Actions appended as they happen.

## Walk-through record

- Walk-through opened. Read `resources/research/auto-memory-staleness.md`: its 2026-08-15 re-verification block stands as written — AutoDream live with a manual `/dream` available to everyone, and "Obsidian memory systems" a category rather than a project. Both findings are eight days old and the article makes claims about third parties, so step 1's fresh-search offer was put to the user.
- User asked for the search. Ran it on both subjects. **Obsidian half reinforced** — more independent vault-as-memory projects found, still a category rather than a project. **AutoDream half partly fell** — two sources now put the `/dream` command itself behind the gradual rollout, contradicting the 2026-08-15 "available to everyone"; consolidation stays reachable for every user by asking in plain words, and AutoDream is still Anthropic's own feature, so the article's concession survives while the command claim does not. Corrected in `resources/research/auto-memory-staleness.md` with a dated 2026-08-23 block, and its `resources/research/index.md` line updated in the same move.
- Step 2: article drafted, on the user's "as you recommend" against the named-project option — it names `obsidian-second-brain` rather than arguing with a composite. Written to the session scratchpad rather than into the project, since the item's Files line says none in this project. Its concession is that automatic curation now ships in the base tool; its case is that every one of these records is written by the machine for the machine and never approved, which is a difference in kind rather than in durability. Ends on [digest-reports-computed-fields-not-summaries] as the shipped fix.
- Step 3, first round: user asked for a nod to Papi (getpapi.ai) in the MCP paragraph, after mentorship from its maker. Checked the site rather than guessing — Papi is a plan/build/review/release project-management layer, not a memory store, and it keeps a decision record of calls made and reversed with reasons. Putting it in the memory-systems paragraph would have misdescribed it, so it was separated out at the top and given a "nearest neighbour" section instead; the article's "the thing none of them do" claim was narrowed to "the thing the memory systems don't do", since Papi partly punctures it. Papi's approval model was deliberately not characterised — not visible from outside.
- Step 3, second round: user objected to "comparisons drift into disparagement" as the article critiquing itself from inside itself, and asked for real credit at each critical moment instead. The line was cut and four credits added: what memory systems do that Throughliner does not attempt; that they are accurate without asking the user for anything; that AutoDream ships free and on by default, with a recommendation to add nothing before seeing what the base tool does; and that the two approaches are not competing for the same slot.
- Step 3, third round: user challenged "semantic search will beat anything a structured queue can do" and was substantially right — the sentence conflated incidental recall (where search wins) with what-was-agreed (where a closed, completely-read set wins). Her framing of the queue as a contract both parties can read was adopted; her determinism point was adopted in a corrected form, since what is fixed is the input to interpretation rather than the interpretation. Sentence replaced with two paragraphs holding the recall concession and refusing the agreed-work one.
- **Collision found, after the drafting, when the user said she was sending the piece to Papi's maker.** Reading `INBOX/sent.md` to record that send surfaced a 2026-08-22 line: an earlier article for this same item was drafted, approved by the user ("works ok"), and delivered into the site project's `INBOX/` as `2026-08-22-from-no-code-method-comparison-article.md`, with a Discord post approved alongside it. `LOG/2026-08-22-competition-comparison-article.md` records that whole walk-through. The item is legitimately still open — its last two steps are the user publishing and confirming — but step 2, the drafting, was already done. **This session re-drafted it from scratch because the throughline retrieve was never run on the slug before drafting.** The build view carries no history by design and the item's walkthrough pointed only at the research file, so nothing in the run's own materials mentioned the earlier draft; the always-loaded rules require the retrieve regardless, and it was not run. Surfaced to the user before any send.
- User's decision on the collision: today's version ships and supersedes the delivered one. On the timing sub-question she chose a hold-note now and the full replacement after the external review, rather than superseding twice.
- Checked the site project has an `INBOX/` and that its own `.gitignore` covers it (both true; yesterday's article still unread there, not archived). Showed the exact text and, on her explicit yes, wrote `2026-08-23-from-no-code-method-comparison-article-hold.md` into that mailbox. Recorded on `INBOX/sent.md` as for continuation.
- The site project's path was resolved from the Desktop-level `CLAUDE.md`, which names it, because **`INBOX/` holds no address book** though the method describes one. Noted to the user; not captured yet.
- Mid-walk-through, out of the user's question about what to implement: filed [log-retrieve-search-leg] to Unprocessed. Her objection shaped it — a search leg must be additive to the shipped five-newest-index-lines orientation read ([plan-reads-recent-log-index]), never a replacement, because search returns only what someone knew to ask for. First step is measuring the index's real cost, since no limit here is derivable yet. The queue lint flagged the heading for a leading article; re-filed with the distinguishing words first.
