# [HASH] — the slug explained once in the queue header and glossed every time one is shown, merged from two ends of one subject

This arrived out of the reading-position item rather than from the ladder. Asked what her position was on the person who will not read the record, Alex answered something different and better: there was the promise of a rule ensuring that the way captures are presented at the start of processing does not assume full knowledge of the capture's content, and at minimum a referent should be given not by name but also by its purpose.

The chat carrying that promise was searched for twice and not found. What the search did turn up is two other instances of the same failure in her own words — *"I don't know what 'checkpoints' is"* and *"I don't know what 'fixing on the spot' means"* — different terms, same shape. The third body of evidence was the session doing it live: every checkpoint sent that day named an item by bare slug, and several messages cited three or four more.

The parent is the always-loaded vocabulary rule, which already asks whether a term is used in passing or explained and already lists a dozen internal terms to translate or omit. A slug cannot simply join that list, because omitting it loses the one handle the user has on an item — so it lands as a third arm on an existing table. Scope is output and never documents, the same split [two-column-fences-wrap-unreadably] settled hours earlier and recorded there; inside queue prose slugs stay bare, since the always-loaded rule requires citing items by slug there and glossing every citation would inflate entries this queue already struggles with. The gloss fires on first use per message rather than per chat, because per chat makes a message readable only to someone holding the scrollback in their head, which is the assumption being objected to.

The item was widened rather than duplicated: a second entry on the same notation would answer the question twice, so the existing capture was rewritten to carry both halves and its heading replaced. What came out is its original three-way choice between header, FAQ or both, now settled as both. One cost is stated rather than discovered — the header clause reaches new projects only, since an existing project's queue header is the user's own text and the migration is barred from rewriting user prose, so for them the FAQ entry is the whole answer.

**Queue changes:** [slug-never-explained-to-the-user] rewritten, its heading replaced, and kept into Processed beside the fenced-blocks item.

**Work processed:** kept — [slug-never-explained-to-the-user]. Deleted — none.
