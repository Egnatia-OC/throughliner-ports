# d9162e4 — Codex-port handoff audit: two findings landed long ago, two never arrived and are now captures

Read the shelved port's two archived handoff items read-only and checked each named finding against main's current docs. Landed without needing anything: the empty-cleared-region resolution path (main's queue-state ladder has an explicit below-the-line rung recommending /plan and saying work is waiting) and the longest-safe-run clearing (multi-item /next bounded by the readiness line is exactly that shape). Never landed, now filed as captures: the port's case for retiring the forward-recommendation advisory outright — main refined the mechanism repeatedly but never weighed removing it, and current evidence cuts both ways ([codex-advisory-retire-argument-unweighed]); and the whole-queue-as-one-artifact rule — chunked reads as complete transport, fail closed when the whole queue can't be obtained — which main lacks entirely and whose failure is live in the desktop app's capped file reads ([queue-chunked-read-fail-closed-unlanded]). The port's persistent-work-lanes and durable-state adaptations were classified as superseded: main's _build.md/_plan.md carry the durable-state spirit, and the port's close-only-on-request model diverges from main's /done design deliberately. Run in the overnight blitz of 2026-08-05; the bulk-approval step could not run live, so both captures await the user — autonomous run, recorded departure.

**Files touched:** none — read the port's QUEUE.md (read-only, per the standing rule), main's docs-b and SPEC for reconciliation
**Routed to Captures:** [codex-advisory-retire-argument-unweighed], [queue-chunked-read-fail-closed-unlanded]
**Approval outcomes:** deferred to the user's morning /plan — no finding dropped or reworded
FAQ: not needed because an audit edits nothing.
