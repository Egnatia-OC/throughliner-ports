# 0ac0792 — Autonomous /goal session: shipped the verbosity-steering arc plus four follow-on batches (8 build batches, plugin off) — commit-from-LOG-entry collapse (done.md + four sub-docs, git commit -F), 4.8 authoring heuristic + host-only standing check, response-shape tag exemplars + lead-with-the-decision rule, a forced concise output style shipped in the package, the deferred-test runnability/rolling lifecycle, dropped the skill-time behaviour-doc re-read, dependency-scan reference-role classification, and the fresh-short-session design target

Third autonomous /goal session, plugin off, manual close. Worked QUEUE.md's Batches top to bottom, shipping eight build batches and skipping one screenshot-only batch, then closed by hand. No push, no reinstall. All host-side behaviour was recorded as deferred tests; the one Claude-runnable test (session_start.py) was run in-session against fixtures.

**[closeout-text-collapse]** — the long-standing, repeatedly-asked-for fix, landed first. done.md's commit core no longer drafts a fresh commit message: the title is the just-approved LOG entry's one-liner verbatim and the body is the approved rationale verbatim, so the commit step reviews nothing new and asks only "commit and push, or just commit?". The message is passed shell-agnostically by writing it to a file and committing with `git commit -F`, sidestepping inline multiline-quoting fragility on every shell; this is writable despite the scope-lock because the sub-doc deletes _build.md before the Commit step (or none existed for a plan close). done.md's LOG entry files section gained the one-text identity (one-liner → entry heading + index line + commit title; rationale → entry body + commit body), and each of the four /done sub-docs' entry-approval steps now says that one approval also covers the commit message. The presentation rule was authored to state the identity plainly and surface only genuinely-new text (a staged-extras line for hash backfills or sweep edits), never a meta-description of the derivation — the failure mode a prior live run hit.

**[4-8-authoring-heuristic]** — created resources/4-8-authoring-heuristic.md (host-only, not shipped in the package): a short checklist of seven positive, exemplified checks distilling the two verbosity research files into a runnable authoring pass — quantify over adjective, show the shape, lead with the decision, state scope, name the pattern to kill with its replacement, write as an action not a prohibition, and guard against over-terseness. This project's CLAUDE.md gained a standing rule that every self-hosting build and spec-writing batch is run against it before its text ships. It lands first because the next two batches are its first applications.

**[tag-definition-redesign]** — plugin-behaviour.md's [BRIEF] and [SEQUENCE] tags each gained a one-line positive exemplar (a model of the wanted output shape, the research's single strongest lever), and Communication gained a lead-with-the-decision rule: every user-facing message opens with the one thing to see or act on, gates the detail behind a request, with a good/bad exemplar pair and explicit scope. Authored against the 4.8 heuristic.

**[verbosity-output-style]** — created plugin/si-plugin/output-styles/concise-sovereign.md, a forced output style (force-for-plugin, keep-coding-instructions) that lifts the lead-with-the-decision / one-item-at-a-time / gate-the-detail structure to system-prompt priority, which CLAUDE.md and skill docs can't reach. It states the non-goal in the style itself — anti-overwhelm structure and plain English, never terseness or a word cap — and notes per-step depth stays governed by the procedure steps. Build-verify confirmed (from the research, checked against the official docs) that a plugin's output-styles/ directory is auto-discovered with no manifest registration and no version bump, so plugin.json was untouched and the version stays 1.12.0-test1.

**[deferred-test-lifecycle]** — gave deferred tests an execution channel. done.md's section gained a scope statement (verification for shipped work only; failures and new needs route to Captures), a runnability tail on every "Confirmed by:" line (Claude-runnable / user-run / external), the new lifecycle (/done writes, /plan rolls runnable lines into test batches, /done's close-out backstops confirmations), and the close-out backstop itself. plan.md gained a Step 1 roll scan and a Step 3 roll-in at test-batch authoring. next.md / next-build.md / next-test.md gained a fourth tick state, `deferred (reason)`, written the moment a test is determined unrunnable so a post-/clear /done doesn't misread it as unfinished. done-build.md and done-test.md now treat deferred-ticked entries as closeable and convert each into a queue line mechanically. session_start.py gained a helper and the version-change report that wires the previously-computed-but-unsurfaced "an update just happened" signal, adding a deferred-tests line pointing /plan at the now-live-testable backlog; a fixture test confirmed the line appears only on version mismatch with a non-empty section. The four lifecycle descriptions (QUEUE.md intro, this CLAUDE.md, CLAUDE-TEMPLATE.md, faq-template.md) were updated to the new lifecycle; done.md's own section had the same stale "/next re-presents" wording and was corrected in the rewrite (routed as a capture noting the sweep omitted the canonical doc).

**[behaviour-doc-double-load]** — removed the "Re-read them before continuing" line from the plan/next/done SKILL.md files; plugin-behaviour.md is injected every session, so the skill-time re-read paid double to insure against an occasional compaction event. The authority sentence naming the doc stays; only the duplicate load goes.

**[dependency-scan-reference-roles]** — replaced plan.md's "park even if the reference is incidental" rule with a two-role classification by linguistic marker: forward-looking language ("once X ships," "depends on") is a dependency and parks the capture; backward-looking language ("observed at," "see," "from") is an evidence citation, does not park it, and is named with its role at recommend time so a wrong call stays user-catchable; ambiguous keeps the park default. Applied identically to the canonical "Routing gate" bullet and the Step 2 sub-step so they don't drift. This fixes the prior backwards tuning where every correct evidence-citation override was a rule violation.

**[short-session-design-target]** — this CLAUDE.md's Rules for Claude gained "Design for fresh, short sessions": the files must suffice without session memory, conversation memory is a convenience not a dependency, with an explicit note that this is about robustness to memory loss and does not change the Model target (4.8 stays the tuning target). The route-observations rule was sharpened with the named trigger — any moment session memory covers for something the docs should carry is a mandatory capture.

**[install-upload-path-clarity]** — skipped and left in place at the top of Batches: its sole remaining item needs a real desktop-app screenshot of the Plugins screen, which a goal session can't produce.

Queue hygiene: all eight shipped slugs were confirmed removed from Batches before commit (the cross-check the prior goal session's survived-batch slip called for). Previous session's LOG hash placeholder backfilled to 1860445.

**Files touched:**
- plugin/si-plugin/docs/done.md — commit core reshaped (derived message, plain identity, git commit -F); one-text identity in LOG entry files
- plugin/si-plugin/docs/done-build.md, done-test.md, done-audit.md, done-plan.md — entry-approval covers the commit message; build/test verify-completion + write-deferred-tests handle deferred-ticked entries
- resources/4-8-authoring-heuristic.md — created (host-only)
- plugin/si-plugin/output-styles/concise-sovereign.md — created (forced concise style)
- plugin/si-plugin/docs/plugin-behaviour.md — [BRIEF]/[SEQUENCE] exemplars, lead-with-the-decision rule
- plugin/si-plugin/docs/plan.md — deferred-test roll scan (Step 1) + roll-in (Step 3); dependency-scan two-role classification
- plugin/si-plugin/docs/next.md, next-build.md, next-test.md — fourth `deferred` tick state
- plugin/si-plugin/hooks/session_start.py — _deferred_tests_present helper + version-change report
- plugin/si-plugin/skills/plan/SKILL.md, next/SKILL.md, done/SKILL.md — removed skill-time re-read
- plugin/si-plugin/templates/CLAUDE-TEMPLATE.md, faq-template.md — deferred-tests lifecycle descriptions
- CLAUDE.md (this project) — 4.8-authoring standing check; deferred-tests lifecycle description; fresh-short-session design target; sharpened route-observations rule
- QUEUE.md — 8 batches removed; 5 host-side deferred tests added; 2 captures filed
- REGISTRY.md — added the output style and the heuristic doc; updated session_start.py description
- LOG/index.md, LOG/plan-2026-06-15.md — previous session's [HASH] backfilled to 1860445

**Routed to Captures:** [approval-display-blockquotes] deferred-test commit-step clause superseded by [closeout-text-collapse] (staleness, → /plan reconcile); lifecycle-description sweeps can omit the canonical doc they derive from (process observation, low priority).
