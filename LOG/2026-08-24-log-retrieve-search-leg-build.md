# 3ed3db1 — Built: the LOG index goes search-first and splits per month

The measured toll that admitted it: the single index ran to ~88,000 tokens if read in full, which the always-loaded rules instructed on every retrieve — a rule sessions were already quietly not obeying (`resources/research/log-index-read-toll.md`). What shipped: retrieval wording in skill-nonspecific-rules.md is search-first over `LOG/index*.md`, with the missed-phrasing limit stated (a search reaches lines carrying the words tried; widen terms before concluding nothing is recorded); done.md's index step gained the month rollover; and the one-time migration split this repo's index — 755 lines stay in `index.md` (2026-08), 142 to `index-2026-07.md`, 278 to `index-2026-06.md`, reconciling exactly with the 1,175 pre-split entry lines. One judgment call recorded: 195 legacy pre-split lines carry no filename date, so each inherited the month of the nearest newer dated line, preserving order. `measure_written_shape_length.py` now reads `index*.md` and runs clean; `queue_digest.py`'s index.md mention is the research shelf, untouched. `FORMAT_EPOCH` was not bumped — a consumer's single-file index still reads correctly and the rollover converts it lazily at the first close touching the index in a new month. The refused alternatives stand as the item recorded them: an age threshold (a bare number), a search leg replacing the orientation read (your objection — search only returns what someone knew to ask for), and any change to QUEUE.md reads. The five-newest-lines orientation read is untouched. The user-facing explanation went to ANNOUNCEMENT-IDEAS.md rather than an FAQ entry, per the reseed shipped earlier in this run.

Rule gate: run — amendment to the always-loaded Index entries and throughline-retrieve wording, superseding the read-in-full-on-every-retrieve sentence, which is repealed by the same edit; no freestanding rule. Admitted on the measured toll — a pointable instance under the reworded single-instance test.

Files touched: plugin/throughliner/docs/skill-nonspecific-rules.md; plugin/throughliner/docs/done.md; plugin/throughliner/scripts/measure_written_shape_length.py; LOG/index.md; LOG/index-2026-07.md (new); LOG/index-2026-06.md (new); ANNOUNCEMENT-IDEAS.md
Routed to Captures: none
Done, confirmed: line counts reconcile (755+142+278=1,175); measurement script runs clean over the split.
