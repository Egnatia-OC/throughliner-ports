# c775914 — Split the Opus 5 build into three phase lines and made it the singular cleared focus; processed FAQ staleness into two builds + two doc fixes

This /plan opened on last session's advisory (docset-A dedupe shipped, freezing-boundary met) and turned into a prioritization session. The user's decisive call: the **Opus 5 build is the only cleared build going forward** — handover, FAQ, and everything else wait behind it. So [fable-docset-model-detection] was split into three phase build-lines — phase 1 (rewrite CLAUDE.md's Model-target rule to the two-docset model) cleared above the marker as the singular focus; phase 2 (author docset B by subtraction, still needing a design pass on its authoring register) and phase 3 (session_start model detection) held below. The previously-flagged top fix [handover-abandonment-and-terminology] now waits behind Opus 5 per the user.

A live discovery drove the other main thread: the method's own FAQ (`FAQ/faq.md`) is ~6 weeks stale, last touched 2026-06-18, and the 2026-07-30 soft-confirm sync clause already failed on its first real test. Processed into two sequenced builds — [faq-sync-hard-gate] (a host-only hard /done close gate with a logged disposition, modeled on spec-sync's teeth; sleeps for consumers by residing in the dev CLAUDE.md) then [faq-backfill]. Also processed two ready doc fixes ([user-line-underfiling-asymmetry], [claude-md-old-history-claim-wrong]) and skipped [self-hosting-auto-detection] with design banked — a seeding clarification (repo-clone seeds self-hosting, /setup seeds a consumer) and an anti-nag recorded-acknowledgement sign.

**Queue changes:**
- Split [fable-docset-model-detection] → [opus5-model-target-rewrite] (cleared), [opus5-docset-b-authoring], [opus5-session-start-detection]
- Processed to Processed: [user-line-underfiling-asymmetry], [claude-md-old-history-claim-wrong], [faq-sync-hard-gate], [faq-backfill]
- Reordered Processed (phase 1 to top of cleared region, marker after the two `[user]` verifications)
- Skipped with design banked: [self-hosting-auto-detection]
- Cleared two consumed advisories: [advisory-plan-after-dedupe], [advisory-dedupe-next]

**Work processed:** kept — user-line-underfiling-asymmetry, claude-md-old-history-claim-wrong, opus5-model-target-rewrite, opus5-docset-b-authoring, opus5-session-start-detection, faq-sync-hard-gate, faq-backfill; deleted — none (fable-docset-model-detection split, not deleted)
