# [HASH] — plan — [capture-blocked-by] kept: a capture may name an open queue item that stops it being offered

From the live instance of [cycles-mermaid-diagrams] returning every session while waiting on an in-queue build it had no way to name. The user's diagnosis question ("do we need a new rung?") was answered no — the ladder orders, it cannot hide — and the gap is that `Blocked by:` cannot reach captures. The supersession: on a capture the field means don't offer while any named blocker is open, skipped silently like a future date, silent re-entry on resolution (the user's choice). Ripple traced by grep: post_tool_use's lint, session_start's counts, the digest, four docs; done-plan.md and setup.md excluded as still-true.

**Queue changes:** [capture-blocked-by] filed and cleared.
**Work processed:** kept — [capture-blocked-by].
Rule gate: to run at the build; the disposition is on the item.
