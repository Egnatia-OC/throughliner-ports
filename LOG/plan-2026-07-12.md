# fcc2329 — /plan: dropped [done-multi-entry-commit-msg] as premise-dead and filed [retire-goal-sessions]

Reopened [done-multi-entry-commit-msg] as planning after a /next build was aborted mid-flight — reading the target docs showed its premise was factually wrong. The batch assumed a "multi-line /next run and every /cruise run" produce a single commit closing several LOG entries, but SPEC (line 40) and cruise.md both specify /cruise commits one line per commit, and /next builds a single line — so no shipped workflow ever produces a multi-entry commit. The only workflow that does is the host-only goal-session close, and the user confirmed that mode was only ever a stand-in until /cruise shipped and is itself retiring. With no lasting home for a multi-entry commit-message rule, the batch was dropped rather than rewritten (weighed against putting the rule in done.md's commit core — rejected: that ships a branch for a case consumers can't reach, the same dead-machinery pattern removed from the FAQ earlier this session — and against writing it into CLAUDE.md's goal-session section — rejected once the user noted that section is retiring). Filed [retire-goal-sessions] to capture the real follow-up: retire the goal-session mode from CLAUDE.md once /cruise is verified live, blocked by [cruise-run-verification].

**Queue changes:**
- Dropped [done-multi-entry-commit-msg] from Processed.

**Captures routed:**
- Filed [retire-goal-sessions] to Unprocessed (blocked by [cruise-run-verification]).
- Filed [reinstall-as-user-line] to Unprocessed (post-commit; instance of [user-work-surfacing-and-todo]).
