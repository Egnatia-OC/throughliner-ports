# 15e10c9 — The GitHub-app org-install dead end reported upstream as anthropics/claude-code#88492

Build entry; the planning record is `2026-08-21-claude-code-github-app-org-install-dead-end.md`. The observation was the user's, hit live starting a cloud session against an org repo; the diagnosis Claude's. Tracker searched on several phrasings, no existing report found. The issue describes the three facts generically — the popup contradicts the settings page, the install button lands where the org cannot be reached, and the working route (`github.com/apps/claude`) is unmentioned — with account and repository names scrubbed. Shown verbatim, approved, posted with `gh`: https://github.com/anthropics/claude-code/issues/88492. Send recorded in `INBOX/sent.md` in the same turn.

**Files touched:** `INBOX/sent.md` (untracked by design; the outbound register).
**Routed to Captures:** none from this item.
Tick: done, confirmed — the issue URL returned by the post.
FAQ: not needed because nothing a consumer does changes.
Rule gate: not needed — no rule is authored or amended; the work product is an outbound GitHub issue.
