# 47d8625 — plan — first test-rezips channel entry filed and cleared, its subject the build the release ships

Raised by the user and processed immediately: the first entry in the locked test-rezips channel, a `[user]` line with a placeholder draft Claude fills once the release version exists. Its subject is whichever build the release is cut from — the rezip and the release are the same code that day, the user's framing. The label is the user's to write at posting in their own three-label wording; entries carry label, version and date only, per the no-packaging-for-now decision. Placed after [beta-day-one-posts]; a channel entry is furniture, not a feed post, so the one-a-day chain is untouched.

**Queue changes:** capture filed and moved to Processed, cleared, after [beta-day-one-posts].
**Work processed:** kept — [nerds-list-first-entry].
