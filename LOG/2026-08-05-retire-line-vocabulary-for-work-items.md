# 3affa20 — corpus-wide vocabulary fix: "[user] line"/"work line" become work-item terms across docs-b, docset A, session_start.py, QUEUE.md and CLAUDE.md; the [user] tag itself unchanged

The user's decided correction: "we keep calling them user lines — it's leftover garbage from before we renamed work lines to work items." Scoped by a fresh grep at build time per the tracing rule, which found the residue wider than the capture's list: 60 replacements across 15 files — both docsets' procedure docs, migrate-checklist, templates/faq-template.md, session_start.py (the one that matters most, since its text reaches the user at every session start), QUEUE.md and CLAUDE.md. Replacements were mechanical and pattern-bounded ("`[user]` line(s)" → "`[user]` work item(s)", "work line(s)" → "work item(s)", "work-line" → "work-item"), applied by script so no prose was hand-retyped; slugs are untouched by construction (the patterns require a space, and slugs are hyphenated). The `[user]` tag itself does not change — it is a parsed marker the hooks read, and renaming it would be a hook-enforced-format change with a completely different blast radius; only the noun around it was wrong. Docset A is in scope as a correction under the freeze rule: A carrying a retired vocabulary while B carries the current one is exactly the self-inconsistency that rule addresses. A residue grep after the pass returns zero matches; session_start.py compiles.

Overnight blitz run (branch overnight-blitz-2026-08-05b): built under the blitz plan's sanctioned departures — approvals deferred to the branch review, no push.

**Files touched:** plugin/si-plugin/docs-b/{plan,done-plan,next-build,migrate-checklist,plugin-behaviour}.md, plugin/si-plugin/docs/{plan,next,next-build,done-plan,migrate-checklist,plugin-behaviour}.md, plugin/si-plugin/templates/faq-template.md, plugin/si-plugin/hooks/session_start.py, QUEUE.md, CLAUDE.md
**Routed to Captures:** none
FAQ: not needed because the FAQ template already says "work item"; the pass brought the rest of the corpus into line with it.
