# 02ec308 — an outstanding /setup can void a run's top item, narrowed to fire only on the collision

Filed from a consumer project's mail and kept on the user's decision, because it is live here rather than hypothetical — [setup-outstanding-here] sits in the cleared region now.

Their project's recorded plugin version was behind, session_start correctly said /setup wanted a session of its own, and /next then presented a run whose top item fixed a stale description inside CLAUDE.md's plugin-managed block — exactly what /setup rewrites from a template already carrying the correct text. The item was void before the run began.

**Their own proposed fix was narrowed rather than adopted, answering the cry-wolf objection they raised against it themselves.** A line firing on every run in a behind project is the shape this project has repealed measures for twice. So the branch fires only where an item in *this* run names a file /setup rewrites — a read of that item's Files line, not a judgment about what counts as plugin-managed content.

**And it reuses an existing mechanism rather than adding one.** `next.md` already carries a drop-an-item-from-this-run recommendation for waiting mail, stated as "the one thing" that may drop an item from a run. This becomes its second trigger, which is also the disposition's eviction: that "one thing" claim becomes false and is rewritten rather than left standing.

One objection recorded on the item is dead and was repealed rather than carried forward — that pointing a user at /setup would send them into a denial. [scope-lock-blocks-setup] shipped 2026-08-15. No hook change either: session_start already emits the fact.

**Queue changes:** [next-presents-items-setup-will-overtake] settled and cleared.
**Work processed:** kept — [next-presents-items-setup-will-overtake].
