# 4cf24fe — /plan: harvested the Taskflow bungled-SPEC audit into [migration-aware-setup] + [retire-registry], queued a first-spec-write E2E audit behind a re-gating marker, amended + reordered the goal-session pair, and filed a queue-architecture design thread

This session opened gated by the queue-top plan-marker: harvest the Taskflow bungled-SPEC audit before the /setup- and SPEC-related batches could build. The harvest preprocessed the 611 KB .jsonl transcript down to 12.6 KB of conversation text (a Python pass stripping tool calls, thinking, and metadata) and read it directly — establishing preprocess-then-read as the way to consume a large transcript. The transcript was Taskflow's own /next audit of its over-broad SPEC (20 findings); 18 were Taskflow-product and stayed there, 2 were method-bearing and were folded in: (1) a blind migration imports the source doc's purpose/self-description sentence, silently re-mandating the detail SPEC should shed — sharpened [migration-aware-setup] with a content-scrub guardrail plus a "state SPEC's purpose up front" entry; (2) retiring a consumer's REGISTRY could orphan content they relocated there — added a retirement-safety guard to [retire-registry]. The harvest capture was dropped and its marker removed.

On a user share of the first-spec-write transcript, filed the "no unprocessed captures remaining" vocabulary-leak capture, then queued a new audit batch [taskflow-spec-write-audit] at the top with a marker re-gating the setup/SPEC batches on its findings. Confirmed [formalize-goal-session] was user-approved at d39e670 (the /goal fork resolved as goal-on-ramp-to-cruise-control; host-only; plugin-off is intrinsic and dev-only); amended its logging from one multi-thread LOG entry to a separate entry per batch in a single close commit, and moved the [close-shipped-slug-crosscheck] → [formalize-goal-session] pair to just under the audit so goal-mode is reachable in three singular sessions.

Weighed and declined (for now) an external critique's multi-file queue split. The principle, from the user's own track record: per-entry splitting works for content read individually (LOG, FAQ) but failed for the queue because it scattered the cross-referenced graph; captures and deferred tests also block batches, so they can't be split out either. Captured the full reasoning as a design thread (the LOG-index model — keep the graph consolidated, only per-batch rationale could ever move) for a dedicated session.

**Queue changes:**
- [migration-aware-setup]: +2 build entries (content-scrub guardrail; state SPEC's purpose up front) + rationale sentence — harvest finding MF1.
- [retire-registry]: +1 build entry (REGISTRY retirement safety) + rationale sentence — harvest finding MF2.
- [taskflow-spec-audit-harvest] dropped (harvest complete); its plan-marker removed.
- [transcript-reading-workflow]: new host-only CLAUDE.md batch (end of Batches).
- [taskflow-spec-write-audit]: new audit batch at the top + new plan-marker re-gating the setup/SPEC batches.
- [formalize-goal-session]: logging amended to per-batch entries in one close commit; the [close-shipped-slug-crosscheck] → [formalize-goal-session] pair moved to just under the audit.

**Captures routed:** Promoted: .jsonl-sourcing + preprocess-then-read → [transcript-reading-workflow]. Parked: present-and-interview forced-wait → [present-interview-forced-wait]. Dropped: [taskflow-spec-audit-harvest]. Filed (unprocessed, left for a future /plan): the "no unprocessed captures remaining" leak, and the queue-architecture design thread. Still waiting unprocessed: plugin.json two-hooks, scope-lock-blocks-memory-writes, always-bold-the-ask, plus the 34-finding self-hosting inventory (still deferred to its own dedicated session).
