# [HASH] — The method map exists and a behaviour description is a complete request

The user's own words (2026-08-21): she had lost control because she couldn't name the parts of the method she wanted changed — close enough to reach for "skill-nonspecific-behaviours", not close enough to land a request. Her complaint was control, not confusion, and the design targets that: the map lists what she can *direct*. Asked where she actually looks things up, she said: asking Claude in chat — so the map is a file a fresh session answers from consistently, and its trigger is her asking, the surface she already uses.

Built: `resources/method-map.md` — one screen, plain language, each part (the always-loaded rules, one procedure doc per command, the brevity style, this CLAUDE.md, SPEC, the hooks, the templates) with what it governs and one example request that would reach it. CLAUDE.md's Audience section gains the clause: a request describing a behaviour is complete with no filename — locate the part and name it in the answer, so each request teaches one name as a side effect — and a question about the method's parts is answered from the map.

Tick: done, confirmed — the map fits one screen with an example per part; the clause reads in the Audience section.

**Files touched:** resources/method-map.md (new), CLAUDE.md
**Routed to Captures:** none
Rule gate: run — one clause admitted on this CLAUDE.md's Audience section, its named parent; subordinate, host-only, a slot spent and said so; nothing evicted. Failure evidence: the recorded near-miss, and the shipped name-the-parts rule reaching only sessions where the user already knows what to ask about.
FAQ: not needed because this is host-only; consumers get the shipped vocabulary rule.
