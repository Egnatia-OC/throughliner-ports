# a2ce359 — Deleted by merge: the consumer's multi-person attribution report folded whole into the identity-layer capture

Processed 2026-08-24. A consumer project's report — several people in one session collapse into a single "you"; the containment test strains when Claude's message may have been answering someone else — was merged into [multi-user-identity-layer] and the standalone entry deleted. First pass folded it as a summary plus a pointer to the archived mail; the user corrected it — folding is incorporation of the actual facts, since reasoning across items fails once the facts sit in a file the queue read never opens — and the host was rewritten to carry the report whole. That correction became [merge-rule-incorporation].

**Work processed:** deleted (merged) — [multi-person-attribution-gap] into [multi-user-identity-layer].
