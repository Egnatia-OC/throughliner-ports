# [HASH] — Deleted with its finding: the terminal-step design question dissolves against the shipped /rescan fix

The reading its own prose asked for was done: /rescan's corrected ending is a hand-back ("resume whatever was running", a return, not a close); the candidate signals exist and are already used where needed (the conversation, and file state — the safety hook decides session type from the build working file's presence); and the other docs' endings claim no conversation is over — plan.md names /done as the exit available at every checkpoint, and the always-loaded one-chat-many-runs rule carries her sometimes-they-alternate case. The general fix the capture held open was never needed: the repair pattern is make-the-ending-a-hand-back, and the others already are. Deleting loses nothing; this entry is the finding's record.

**Queue changes:** capture removed.
**Work processed:** deleted — [procedure-docs-cannot-tell-finished-from-interrupted].
