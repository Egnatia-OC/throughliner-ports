# PENDING — Day-one posts: drafts re-verified, one false claim found, posting sequenced behind the install-post edit

`[user]` item [beta-day-one-posts], walked through live in the 2026-08-26 /next run. Actions appended as they happened. The decision record this resumes from is `2026-08-26-beta-day-one-posts.md`.

## Walk-through record

- The user directed that the posts be presented now rather than held behind the release, her reason being that the release runs after the close inside this same session and the ordering is worth minutes. Recorded as her decision; the verification below is what the step itself required.
- Step 1 (Claude's): re-verified both drafts against what is about to ship.
  - **Timing claim holds.** The announcement's "today's build, patched and re-run this afternoon on top of the version I've been living in since it landed late last night" checks out against the commit record: test19 landed 01:15 today, test20 was rezipped 14:32, and this run patched it.
  - **One claim does not hold, in both drafts.** The announcement calls the pinned "How to install" post "the tested route", and the channel pin calls the beta release "the safe route — pinned in the how-to forum". That post's install command does not yet name `#beta`; editing it is step 4 of [beta-install-smoke-and-post-edit] and is still outstanding. As it stands the pin installs from main, which is the failure [beta-branch-install-pin] exists to prevent.
  - Member count ("all 14 of you") is not checkable from here and stays the user's to confirm at post time.
- Two routes put to the user: run the release, edit the install post's command to `FlintcraftTech/throughliner#beta`, then post both drafts unchanged; or reword both drafts to stop pointing at the pin. Recommended the first — it needs no second machine, costs one line, and leaves new installers with a route. **The user took the recommendation.**
- Step 2 therefore **deferred behind the release and the install-post edit**, on the user's decision and a stated reason rather than on an inference. Item left in place; both drafts stand unchanged and need no rewording.
