# 98a9223 — plugin-behaviour.md + FAQ — expanded Research into "Research and evidence filing": resources/ purpose statement + three-way triage

plugin-behaviour.md + FAQ — expanded "Research" into "Research and evidence filing" with a `resources/` purpose statement and a three-way triage for where findings and records land: reveals work → a QUEUE Unprocessed capture; a finding or clean pass needing no verbatim re-read → the session's LOG entry (a pass is a finding, not work); evidence a future session must re-read word-for-word → a durable file under `resources/`. Stated `resources/`'s purpose — research write-ups (`resources/research/<topic>.md`) and re-read-later testing evidence (`resources/testing/`) only, a durable file earned only by the third branch — and folded the existing research-filing rule in as the research branch. Added a plain-English FAQ entry ("Where does Claude put research findings and test results?") plus its index line, since the filing behaviour is user-visible. Shipped (consumers hit the same drift); live after reinstall. Foundation of the file-cleanup cluster — the not-yet-built [temp-file-delete-time] and [session-file-cleanup] (items 6–7 of the same run) reference this routing.

**Files touched:**
- plugin/si-plugin/docs/plugin-behaviour.md
- plugin/si-plugin/templates/faq-template.md
- plugin/si-plugin/templates/faq-index-template.md

**Routed to Captures:** none
