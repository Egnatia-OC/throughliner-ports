# d7ea8a0 — Both directions kept: behaviour descriptions are complete requests, and a method map lives where asking reaches it

Kept with the one design question put to the user — where do you actually look things up? — answered: asking Claude in chat. So the map is a file, `resources/method-map.md`, because the session asked is a fresh one and a file makes the answer consistent; its trigger is the asking itself, so it is never siteless for her. Direction 1 was confirmed working in this very session (the checkpoint-wording fix landed from a pure behaviour description) and gains its clause on the Audience section: a behaviour description is a complete request, and the answer names the part it found in passing, so each request teaches one name as a side effect. Control, not comprehension, is the target, per the item's own closing warning. Full disposition and build block on the item.

**Queue changes:** kept into Processed, cleared to run.
**Work processed:** kept — [user-has-no-handle-on-the-methods-own-parts].
