# [HASH] — plan — [test-rezips-entries-name-obtain-route] kept as [user]: the channel pin promises a commit line, every entry carries one

Surfaced by /rescan from the port prompt handed over this session: the prompt sends a porter to "the newest test-rezips entry's build", and entries so far provide no way to obtain it — the first links the release page. Both surfaces are Discord posts only the user can edit until the posting bot ships; the entry-format note was written onto the bot item so its script inherits it.

**Queue changes:** [test-rezips-entries-name-obtain-route] filed and cleared; format note added to [discord-posting-bot].
**Work processed:** kept — [test-rezips-entries-name-obtain-route].
Rule gate: not needed — Discord post walkthrough, no method text.
