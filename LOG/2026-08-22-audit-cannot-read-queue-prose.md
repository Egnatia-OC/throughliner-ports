# [HASH] — Deleted, folded: audits pointed at queue prose are covered by the queue-content lint check and by /plan's own reach

Deleted as absorbed into [queue-content-items-are-unbuildable-by-a-run], whose lint check was widened to flavor-blind in the same move — see that item's entry for the design and the stated residual limit (audits scoped at queue prose only in rationale escape the Files-line read). The deeper routing answer recorded: an audit of queue text is in-session planning work under plan.md's resolve-now ground rule, never a queued `[audit]`.

**Work processed:** deleted — [audit-cannot-read-queue-prose], folded into [queue-content-items-are-unbuildable-by-a-run].
