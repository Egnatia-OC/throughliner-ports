# [HASH] — /plan: folded the two red-flag captures into one [red-flag-ship-lifecycle] build, confirmed the reinstall is live (deleting [reinstall-merged-plugin]), and settled [user]-work surfacing (option A, collaborative handover)

This session processed four unprocessed captures. It opened on the user's question about why red flags seemed to block everything, and her correction that a red flag is a *marker on a work line*, not a line in its own right. That clarified the design: the two red-flag captures ([redflag-standalone-tracking-line] — a resolved flag lingering as a workless line; [redflag-resolution-not-forced-at-ship] — a flag vanishing unrecorded at ship) are two halves of one question and were folded into a single build, [red-flag-ship-lifecycle]: a flag must always ride real remaining work, a completed fix flips to resolved immediately, and the close forces a recorded resolve/accept before a red-flag line leaves the queue.

The reinstall was confirmed live in-session — host and target both report 1.15.0-test2 and this /plan ran the two-section procedure cleanly — so [reinstall-already-done-confirm] and the satisfied [reinstall-merged-plugin] `[user]` line were both deleted, making the host-side verification pile runnable.

[next-user-work-not-surfaced] was kept as a build on decided direction: the cleared-to-run marker stays the single gate (option A, positional) — /plan places ready `[user]` work above it — chosen over making /next surface `[user]` work independently, because the user preferred one gate to a second readiness check. The handover was refined to be collaborative: /next offers to guide the user through a `[user]` step, not just name it. At close, the now-ready [merged-plugin-live-verification] moved above the marker; the cruise lines stay below pending [red-flag-ship-lifecycle] shipping and the flag resolving.

**Queue changes:**
- Kept [red-flag-ship-lifecycle] (new build, absorbs both red-flag captures) and [next-user-work-not-surfaced] (build) into Processed
- Deleted [reinstall-already-done-confirm] (confirmed) and [reinstall-merged-plugin] (satisfied)
- Promoted [merged-plugin-live-verification] above the cleared-to-run marker

**Work processed:** kept [red-flag-ship-lifecycle], [next-user-work-not-surfaced]; deleted [reinstall-already-done-confirm], [reinstall-merged-plugin]
