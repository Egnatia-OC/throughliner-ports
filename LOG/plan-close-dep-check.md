# 14e3d99 — plan.md + done-plan.md + faq templates: added a dependency-graph coherence check at /plan's Step 4 close-out and a /done-plan backstop that routes a broken graph back to /plan, plus a plain-English FAQ entry; filed 2 captures ([build-md-inline-files-shadows-scope-lock], [plain-language-edits-surface-in-chat])

A broken dependency graph in QUEUE.md was sailing through /plan untouched and only biting at /done — the first skill that actually walks the graph — which forced a /plan re-run. Observed twice in the Simply Sew project. The fix is structural: check the graph in the skill that owns ordering, before it hands off. plan.md's Step 4 close-out now re-reads QUEUE.md and confirms three things before closing — every `Depends on:` slug is ordered above its batch (or shipped/satisfied), every header slug resolves, and no batch depends on a still-unprocessed capture; a coherent graph closes silently, a broken one is named in plain words and fixed in-session with the user's approval. done-plan.md gains a "Coherence backstop (run first)" that re-reads the same graph and, if broken, stops the close and routes back to /plan rather than committing a queue /next would trip on — /done is a second pair of eyes, not a second planner, so it surfaces and bounces rather than fixing, which would cross the build/plan skill line. A plain-English FAQ entry (plus its index line) explains what an "out of order" or "dangling" dependency message means. The mechanical lint sibling is [plan-close-dep-lint].

Two captures came up mid-build. The scope-lock blocked the first edits because the _build.md template repeats the batch's inline `Files:` line inside the copied Entry, and the hook's parser matches the first `Files:` it sees, finds no bullets under it, and reports "no files listed" — worked around by deleting the inline line, and filed as [build-md-inline-files-shadows-scope-lock] for /plan. The user also raised, from the Simply Sew E2E project, that /next should surface plain-language edits directly in chat rather than summarising-then-hiding them, and should allow a natural in-build "change this one bit" tweak folded into the build's log — filed as [plain-language-edits-surface-in-chat].

**Files touched:**
- plan.md: Step 4 close-out — dependency-graph coherence check before the close line.
- done-plan.md: "Coherence backstop (run first)" section before the LOG-entry step.
- faq-template.md: FAQ entry on out-of-order / dangling dependencies.
- faq-index-template.md: matching index line.

**Routed to Captures:** [build-md-inline-files-shadows-scope-lock], [plain-language-edits-surface-in-chat]
