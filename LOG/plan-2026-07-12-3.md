# [HASH] — /plan: rewrote [working-mode-mechanism] to key the view-in-doc render rule on doc-residency (folding in three render captures + adding next-build.md as the real inline-paste culprit); filed three captures incl. a full-method park-trigger audit

A /plan session that came in to settle [working-mode-mechanism] but first surfaced a recurring behaviour problem. Processing the point-and-work capture, Claude twice reached to "park for a dedicated design session" — and the user named it plainly: in the two-section model there is no skill that revisits a Processed-but-below-the-line item unless a blocker clears, so every park that isn't "blocked by unbuilt work" hides work where nothing looks again. /plan's two jobs are processing Unprocessed AND moving the cleared-to-run line; moving the line is the mechanism that advances a legitimately-blocked item. That critique became [park-trigger-method-audit]: the pre-redesign parking apparatus (parked-shelf review, `Parked:` headers, anti-nag stamps, Batches/Captures Parked sections) still lives in plan.md and plugin-behaviour.md though the two-section QUEUE.md never implements it, and Claude uses that leftover language to defer in-/plan design work to hypothetical later sessions.

The main work: rewrote [working-mode-mechanism] to fold in [working-mode-approval-time-render], [readable-edit-reveal-view-in-doc], and [working-mode-ask-over-narrates], and to define the render rule around doc-residency rather than mode alone — already-written text points (local + editor recorded), not-yet-written approval-time drafts render inline in every mode. Diagnosis found working mode was never wired into any procedure doc (the pointer rules gate only on `Editor:`), and that next-build.md's Build-entries step 3 pastes a readable edit's new text inline unconditionally — the real "always renders even in local" during builds — so next-build.md was added to the batch's Files. Working-mode confirmed as product truth → SPEC capability sentence stays in scope.

[nested-si-replaces-multispec] was interviewed (grep confirmed "multi-spec" is already dead in shipped docs; the live substance is point-and-work session_start resolution) but not routed — left in Unprocessed, the park proposed for it withdrawn as exactly the pattern [park-trigger-method-audit] names.

**Queue changes:**
- Rewrote [working-mode-mechanism] in place (Processed, above line): doc-residency render rule, next-build.md added to Files/Build, three captures folded in.
- Filed [working-mode-ask-over-narrates], [next-recommendation-not-queued], [park-trigger-method-audit] to Unprocessed.
- Removed [working-mode-approval-time-render] and [readable-edit-reveal-view-in-doc] from Unprocessed (folded into the batch).

**Captures routed:** three folded into [working-mode-mechanism] (two removed, one filed-then-folded); three new filed to Unprocessed, unrouted; [nested-si-replaces-multispec] interviewed, left unrouted.
