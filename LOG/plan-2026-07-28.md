# ea2ac18 — Cross-platform: Claude (canonical SI) diagnosed the Codex port's `[user]`-handover deadlock

A cross-platform weigh-in — no build or plan work on this project. The user brought a full transcript from the Codex port of SI, where the Codex assistant ("Sol") tangled itself running a `[user]` E2E walkthrough, and asked Claude — the canonical SI — to diagnose it. Claude read the port's actual source under `.codex/plugins/cache/.../0.2.0+codex...` and verified the cause rather than trusting Sol's narration.

Verified findings: (1) The port wraps a `[user]` handover in a `_build.md` with an empty `Files:` list (its `next.md`) — the canonical method never does this; here a `[user]` line is handed over and left in the queue with no build file. (2) The port's `done.md` designs two coexisting recovery "lanes" (`_build.md` = build lane, `_plan.md` = planning lane) with a Done prompt asking which to close — but its `pre_tool_use.py` exempts only `_build.md`/`QUEUE.md`/`LOG/`, not `_plan.md`, so the planning lane can't be created while a build lane exists. The two-lane design and the hook contradict each other; that is the deadlock. (3) The scope-lock exemption is absolute-path-fragile (strips only `a/`, `b/`, `./`), so absolute paths get denied — Sol's first false "deadlock." (4) All four Codex skills set `allow_implicit_invocation: false` while the port's handoff guidance tells users to name skills in plain language — a genuine contradiction; fresh Setup didn't activate.

Governing principle the user stated for the two ports: Codex never builds SI-for-Claude and Claude never builds SI-for-Codex; either may weigh in on the other. Claude weighs in more strongly right now as the canonical SI and the port actively used and tested across the user's projects. The port's `[user]`-in-`_build.md` deadlock is live evidence for the canonical decision to keep `[user]` handovers out of `_build.md` ([user-handover-lifecycle]). The boundary principle itself is recorded as a standing convention in this project's CLAUDE.md (Cross-platform ports); this entry records the instance.

Transcript: `.codex\visualizations\2026\07\27\019fa2d8-98e3-7de2-be73-8b8151d4381f\sovereign-implementer-chat-transcript.md`

**Routed to Captures:** none
