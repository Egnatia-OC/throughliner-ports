# 02ec308 — repealing shipped text gains a grep-traced limb, and its host-only sibling is subordinated rather than left beside it

A repealed sentence had been restated verbatim in three more live places, stopping a run twice in a run whose premise is that it does not stop. The existing rule does not reach it: `CLAUDE.md` requires a grep-traced ripple when an item changes a format or enum the hooks enforce, and a repealed sentence is neither — though the trace is identical and needs no judgment, since a repealed sentence is a literal string.

The settlement puts it in the shipped doc rather than this project's own file, as a third limb on the keep-step's two-limb check: where an item repeals or rewords a specific sentence, grep its distinctive words before writing the Files line. The Files line is written at the keep-step, so that is where the omission happens — and a consumer repealing a sentence in their own SPEC has the identical problem, so a host-only rule would miss everyone the method ships to.

**The disposition's eviction is the standalone status of the existing host-only rule**, which now declares itself a specialisation of the shipped limb. Two rules on one subject, at the same level, with no declared relationship is exactly the signature folded into the restyle this session — leaving them as peers would have authored the defect that pass is being sent to find.

At 542 words the item split, and along a real seam rather than for length alone: the announcement surface cannot be built until there is a record of what was sent, so [repeal-falsifies-a-posted-claim] is held against the outgoing record while the live-document trace is buildable now.

Both limbs were exercised the same day — the SPEC repeal's grep found seven live sites, and the test-suffix repeal's found a hook comment nobody would have listed from discussion.

**Queue changes:** [repeal-has-no-ripple-trace] settled and cleared; [repeal-falsifies-a-posted-claim] split out and held.
**Work processed:** kept — [repeal-has-no-ripple-trace], [repeal-falsifies-a-posted-claim].
