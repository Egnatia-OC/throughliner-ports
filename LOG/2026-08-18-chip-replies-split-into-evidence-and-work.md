# [HASH] — the chip-reply finding moves to the research shelf and the item keeps only the containment test

At 656 words against a 345 ceiling, this item split along a real seam: most of it was evidence, not work.

The evidence is now `resources/research/chip-replies-and-provenance-evidence.md` — that every user message carries `origin: {kind: "human"}` and `promptSource: "sdk"`, so a typed reply and a clicked suggestion chip are byte-for-byte identical and the transcript records who *sent* a message and never who *composed* it; the loop by which Claude's own proposal is laundered into a user credit; and the containment test as the one signal that survives. It is filed for verbatim re-read because it is the evidence behind a shipped always-loaded rule and cannot be reconstructed without re-reading raw transcripts.

The work that remains is one subordinate clause. The provenance rule already says approval is not authorship and that agreeing to a proposal Claude reasoned out makes the reasoning Claude's — what it does not give is a way to tell agreement from authorship when the reply is one word. It gains that: **a reply wholly contained in Claude's preceding message cannot evidence an origin claim.**

Two routes were refused. A bulk audit of existing credits needs the message that preceded each one, which is not recoverable item by item. Declaring the corpus unreliable in bulk would be an unverifiable claim degrading every honest credit alongside the doubtful ones. And a wrongly-credited decision is re-labelled rather than re-decided, except where the credit is what kept it from being examined.

The user-side convention lands in the FAQ, which is the non-nagging home for the once-per-session notice she refused.

**Queue changes:** [chip-replies-are-indistinguishable-from-user-authorship] split, shortened from 656 words to 398, and cleared. Research filed with its index line.
**Work processed:** kept — [chip-replies-are-indistinguishable-from-user-authorship].
