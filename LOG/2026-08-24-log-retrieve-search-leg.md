# [HASH] — Kept and cleared: the LOG index goes search-first and splits per month

Processed 2026-08-24. The capture's own first step — measure before designing — was run in-session: `LOG/index.md` is 990 lines, ~354,000 characters, roughly 88,000 tokens if read in full as the retrieve rule instructs; filed as `resources/research/log-index-read-toll.md` with its index line. In practice sessions search rather than read whole (this one did; past sessions inferred, not verified), so the design mostly writes down what works: targeted retrieves grep the index files, the read-in-full sentence is superseded, the five-newest-lines orientation read and the whole-queue read stay untouched — the user's original objections preserved. Her growth concern added the split: per-month index files (calendar-derived, no bare-number age threshold), rollover at the close, one-time migration. The file point was initially called early — she still had the growth concern open — and the write was held until both halves were agreed. SPEC's LOG/ line rewritten this session. Gate: run — amendment superseding the read-in-full sentence, repealed by the same edit.

**Work processed:** kept — [log-retrieve-search-leg], cleared with build block.
