# [HASH] — /plan: bulk-processed the Unprocessed queue — 6 captures promoted to build lines, view-in-doc + user-work design sessions run, reinstall surfaced as a [user] line, 2 captures filed

At the user's request, this session processed the Unprocessed queue in one bulk pass instead of the one-at-a-time loop: Claude produced a per-item recommendation list the user bulk-approved. Four items promoted straight to work lines — [next-user-step-copy-sounds-confused] (reword /next's [user]-step handover copy), [update-stale-drive-paths] (fix G:\→C:\ across docs), [delete-x-worktree-folder] (a [user] line needing a restart), and [dont-assume-user-environment] (a communication guard in plugin-behaviour.md, shape settled as a rule not a hook).

View-in-doc cluster: the three items nested rather than being co-equal. [working-mode-mechanism] is the container; [view-in-doc-remote-control-tension] folded into it as the reason it exists (deleted as a standalone); [view-in-doc-phantom-write] is a bug in the pointer path, carved out as its own safety build because its fix (never emit the pointer until the write returned success, then re-read to confirm) holds under any mode design. Working-mode decided: mode lives as a persistent default in the generated CLAUDE.md (like the Editor: field), flippable per-session with one word — no session-start ask; and the render choice collapses to one test — point to the doc only when an editor is recorded AND mode is local, else render in chat — so working-mode and editor-awareness compose.

User-work surfacing: the "label-only, intermixed" half the capture proposed is already built (the [user] tag plus /next handover). Kept: a rule that Claude files discovered user-actions as [user] work lines, with /next filing to Unprocessed and /plan doing the ordering. Rejected, on the user's argument: a separate generated to-do list surface — any surfaced list falls out of sync the moment the user acts on an item, so the queue worked through /next is the only safe surface. Applying that rule, the merged-plugin reinstall — previously an unqueued ritual gating the whole verification block — was surfaced as [user] line [reinstall-merged-plugin] placed above the verifications, and [reinstall-as-user-line] removed as answered.

Filed at the user's request in the close tail: [quickplan-skill] (systematise this bulk pass as a skill) and [concurrent-session-support] (a design session on safe concurrent sessions).

**Queue changes:**
- Promoted to cleared build lines: [next-user-step-copy-sounds-confused], [update-stale-drive-paths], [dont-assume-user-environment], [view-in-doc-phantom-write], [working-mode-mechanism], [user-work-surfacing-and-todo]
- Added [user] lines: [delete-x-worktree-folder] (below the cleared marker), [reinstall-merged-plugin] (above the verification block)
- Deleted: [view-in-doc-remote-control-tension] (folded into working-mode), [reinstall-as-user-line] (applied/answered)

**Captures routed:** filed [quickplan-skill] and [concurrent-session-support] to Unprocessed (unrouted; a later /plan sorts them)
