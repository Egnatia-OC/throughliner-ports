# [HASH] — chat-level record: seven items processed under a user-named order then an interleave, a whole rule family retired, and a date error corrected across fourteen sites

The second planning session in a row, which the user flagged at the opening as unusual and possibly needing a third — her reason being that the proposed items carried enough weight that building against them before they were settled would be wrong. That judgment was borne out: three of the session's decisions changed how every future item is written.

She set the order herself at the opening, naming concurrency and the rationale-in-items question as most confounding and asking to work in that order. When her named subset was exhausted the session did not reach for the close — it presented the next item, which is [subset-done-has-no-stated-shape]'s settlement behaving correctly before that item has been built.

**Also in this chat:**

**A date error, corrected at the close.** Every date this session wrote into the queue and the research file was written as 2026-08-18. Today is 2026-08-19. Fourteen were corrected across QUEUE.md and one research file, and four were left standing because they genuinely refer to the previous session's work — the distinction being whether the action described was this session's or yesterday's. Two "the same day" phrases were made explicit for the same reason. The `First seen` dates the decay rung reads were never affected: those are computed from git, not written by hand. The error was found only because the incoming INBOX message carried today's date in its filename.

**Claude's design lost to the user's, twice, on the same subject.** On the rationale question Claude proposed a marker telling a build which half of an item to ignore; she proposed not giving the build the history at all. Hers is structural where Claude's was disciplinary, which is decisive in a corpus that records four instances of a correctly-worded rule failing to fire. On concurrency Claude had warned that switching sessions would cost her context; she pointed out she already switches between whole projects every minute or so, so switching within one is a straight gain — and added the sharper point that a run occupies the only session she has, making SPEC's ideate-anywhere principle currently false.

**Two of her proposals were refused with reasons recorded.** The queue-merge skill, because making the build view read-only and regenerated removes the merge entirely and a sixth skill is the heaviest answer available. And the lexicographic combination of the size and decay rungs, because one key always dominates and the other starves exactly as before.

**One request of hers found what nothing automatic could.** She asked for a contradiction sweep of Processed against the session's own decisions, saying she was not sure there was anything in it. There were eight, including one Claude had written an hour earlier and one that would have put a false claim into a public article. The digest had reported zero placement contradictions and says of itself that it only matches known phrases.

**An external fact was checked rather than assumed**, on her yes: `anthropics/claude-code#77134` is still open, which left the oldest item in the queue correctly parked with a dated check instead of an open question.

**The mail was read, routed and archived at the close** rather than at an opening, because it arrived mid-session — the case `done.md` exists to catch.

**Rule gate:** not needed — no text under `plugin/throughliner/docs-b/`, `resources/self-authoring-rules.md`, `resources/rule-maintenance.md` or `CLAUDE.md` changed this session, so the trigger did not fire. Every rule decision reached here is a disposition written onto its own queue item for a future build to transcribe, which is the /plan-sited gate working as designed. Three dispositions record refusals.

**FAQ:** not needed because nothing shipped. Five of the seven kept items name `faq-template.md` and `FAQ/faq.md` in their own Files lines, so the entries are written by the builds that change what a user does.

**Queue changes:** seven items processed, three deleted, one capture filed from mail, one from the user's own thought, eight cross-item contradictions repaired, `SPEC.md` edited at four sites, one research finding filed with its index line. Queue moved from 22 cleared / 7 held to 26 cleared / 9 held.

**Work processed:** kept — [split-the-cleared-region-for-concurrent-sessions], [retire-word-band-caps-keep-measurement], [folding-in-has-no-eviction-step], [unattended-run-is-not-unattended], [gitignored-core-docs], [concurrent-plan-and-build-sessions] (held), [rationale-lens-after-the-build-view] (held). Deleted — [rationale-in-items-flows-into-shipped-docs], [measurement-audits-for-the-remaining-shapes], [forward-advisory] (consumed at the opening). Filed — [decay-rung-unreachable-in-practice], [build-view-narrows-capture-context].
