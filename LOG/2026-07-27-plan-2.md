# 0a6320d — Processed the readiness/next-ness, feedback-channel, and /next-hooks clusters into builds; lifted the below-line `[user]` items

A queue-processing /plan. The session opened with a user-raised speed-bump from a Verso migration (Claude had drafted a capture to *regenerate* the stale FAQ from the method docs) and, at the user's request, a manual below-the-line revisit before processing the queue.

The Verso finding was folded into [migrate-skill] as a design facet: a migration refreshes method-shipped boilerplate (FAQ, QUEUE header, CLAUDE-TEMPLATE) by re-copying the current shipped template verbatim, never by regenerating from a best-guess reading — the authoritative version already ships in the plugin. The discriminator the migration must encode: is a file the user's own work (migrate by judgment) or method boilerplate (re-copy).

The below-line revisit lifted all three shelved `[user]` items above the marker: [publish-marketplace-manifest] and [install-self-install-branch] (their wait — the push — is done: local `main` is level with `origin/main` and `marketplace.json` is on the remote), and [delete-x-worktree-folder] (user confirmed the full restart happened). This exercise directly seeded the first processed item.

**Alternatives weighed:** the feedback report was settled on **one free-form scrubbed block** over labelled fields — it dissolves the page/format mismatch entirely and collapses the page to a single text box. [feedback-channel-first-invocation] was deleted rather than built — its routing-speed learning was judged an artifact of the self-hosting app==method case that won't generalize, and the "ask the user when unsure" fast path already exists. [nextness]'s Half 1 was subsumed by [plan-no-below-line-revisit] rather than built twice. [hash-backfill] chose loud-failure hardening over waiting on consumer data (mechanism proven working in this project; two candidate consumer causes recorded).

**Queue changes:**
- Kept (→ Processed, cleared): [plan-no-below-line-revisit] (with [advisory-narrows-plan-scope] concern-1 folded in), [nextness-stale-marker-and-buried-prereq] (scoped to Half-2 buried-prereq split), [report-fields-mismatch-webpage], [report-url-404] (`[user]`), [terminology-sweep-hooks-scope], [next-rules-missing-faq-entries], [hash-backfill-not-firing], [format-change-names-its-enforcer], [next-surfaces-discovery-as-blocking-ask].
- Deleted: [advisory-narrows-plan-scope] (concern-1 relocated into [plan-no-below-line-revisit], concern-2 resolved by shipped reorder), [feedback-channel-first-invocation], [freeform-discussion-bleeds-into-processing] (freeform retired).
- Folded: boilerplate-re-copy facet into [migrate-skill].
- Filed: [claude-md-stale-deferred-tests] (Claude-noticed side-finding).
- Reorder: lifted 3 below-line `[user]` items above the marker; moved [merged-plugin-live-verification] from the top of Processed to below the marker (it's `[user]` and would have gated all Claude builds; also depends on this session's builds shipping + rezip) with a lift-condition.

**Work processed:** kept 9 (slugs above), deleted 3, folded 1 discussion item, filed 1 capture. Items 13–23 (the heavy design threads) left in Unprocessed.
