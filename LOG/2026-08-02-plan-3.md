# 91e0292 — /plan: greenlit [opus5-docset-b-authoring] above the cleared-to-run marker for a /next docset-B build; folded a checkpoint model-switch into it; cleared the spent advisory; filed 3 dogfooding captures

The session's goal was to get Opus 5 docset B (the pseudocode version) authored today. Docset B authoring is a build, so /plan did only the enabling move: greenlight [opus5-docset-b-authoring] above the marker for its own /next run — its lift-condition was already clear (predecessor shipped `d1bfeda`, authoring register settled last session). Folded a checkpoint model-switch instruction into the item: authoring runs on Opus 4.8, but the first-section pseudocode checkpoint judges how a *5-series* model reads the register, so /next pauses at the checkpoint for the user to switch to Opus 5 / Fable 5 before judging — don't pass it on a 4.8 read. Cleared the spent forward advisory [advisory-plan-opus5-phase2-docset-b] (persist-condition met: the three user-line fixes shipped and a test rezip is prepared). The 19 waiting Unprocessed captures were deliberately not processed, to keep today focused on shipping docset B. Filed three dogfooding captures from live observation: [completion-sweep-silent-leak] (the suppressed in-/next completion-sweep leaked into the /plan opening narration, in jargon), [reorder-mover-marker-at-top-blind] (reorder_queue.py can't place the marker from an empty-cleared-region start; worked around by hand this session), and [test-suffix-plugin-json-close-noise] (the `-testN` plugin.json bump is surfaced as a dirty file on every close between a rezip and the next push — the user is tired of it; wants a recognized-signature skip like the hash backfill).

**Queue changes:** greenlit [opus5-docset-b-authoring] above the marker (mover reorder); added a checkpoint-model-switch line to it; cleared advisory [advisory-plan-opus5-phase2-docset-b]; filed [completion-sweep-silent-leak], [reorder-mover-marker-at-top-blind], and [test-suffix-plugin-json-close-noise] to Unprocessed.
**Work processed:** none (no Unprocessed items kept or deleted — the greenlight is a marker move on already-processed work).
