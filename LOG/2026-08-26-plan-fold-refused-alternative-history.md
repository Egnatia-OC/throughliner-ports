# [HASH] — Three history sentences deleted from plan.md and done.md, leaving the rules complete

A merge of three compliance-audit findings of the same shape: a sentence whose deletion leaves a complete instruction is history or rationale riding an operative rule, and belongs in the record rather than in the doc.

The three, all now gone. plan.md's fold-conditions block ended on a defeated alternative — that an unconditional extra turn on every keep had been refused. plan.md's end-of-queue gate ended on the asked-three-times justification for its once-per-rest bound. done.md's record-naming rule ended on two sentences explaining the digest's filename mechanism and the incident where a planning session's records went unseen.

Nothing was added, and nothing else was evicted. The histories survive in git and in the entries that decided them.

The delete-and-read test was applied at each site before cutting: what remains at all three is a complete instruction. The item had recorded one thing to watch — that done.md's digest-mechanism sentence might turn out to be operative under the purpose-clause test, in which case the build was to halt rather than decide. It did not: the bare rule reads complete without it, as the audit found.

**Files touched:** `plugin/throughliner/docs/plan.md`, `plugin/throughliner/docs/done.md`.
**Routed to Captures:** none.
**Depth:** short.
**Tick:** done, confirmed — the three distinctive strings return no hits under `docs/`.
Rule gate: run — pure eviction of rationale from operative text, per the rationale-lives-outside-the-operative-rule split; nothing added, nothing else evicted.
