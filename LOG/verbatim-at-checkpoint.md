# [HASH] - plan.md + plugin-behaviour.md: moved the next capture's verbatim to the prior checkpoint so the turn boundary separates item from analysis

Present-and-interview kept bundling the quote with analysis despite repeated wording fixes, because the discipline failed exactly when the pull to analyse was strongest. The fix is structural, not more discipline: the next capture's verbatim (with the confirm-against-the-file re-read) now lands at the END of the prior item's checkpoint, so when the user continues, the next turn opens directly with analysis. The turn boundary, with the user's reply between, is the separator by construction. plugin-behaviour.md's [SEQUENCE] tag gained a carve-out: a single-item look-ahead that is that item's own presentation is not the forbidden preview.

**Files touched:**
- plugin/si-plugin/docs/plan.md
- plugin/si-plugin/docs/plugin-behaviour.md

**Routed to Captures:** none
