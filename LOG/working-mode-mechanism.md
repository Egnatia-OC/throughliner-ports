# [HASH] — Wired working mode into a first-class `Working mode:` field and rebuilt the view-in-doc render rule around doc-residency

Replaced the temporary "ask remote or local every session" stopgap with a persistent `Working mode:` field (stored in CLAUDE.md like `Editor:`, set once at /setup, overridable for one session with a word). Its window had passed and asking every session nagged. The canonical statement now lives in plugin-behaviour.md's renamed "Working mode and view-in-doc rendering" section, which replaces the old Editor-only "View-in-doc pointers": the render rule keys on doc-residency **first**, then mode, then editor — text not yet written (approval-time drafts) is always inline since there's nothing to point at; doc-resident text points only when mode is local AND an editor is recorded; a readable edit's post-write reveal line-links in local (inline fallback) and pastes inline in remote. This folds in the three related captures: [working-mode-approval-time-render] (an inline draft in local is correct, not a violation), [readable-edit-reveal-view-in-doc] (the reveal's link/fallback behaviour), and [working-mode-ask-over-narrates] (the choice stated plainly, explained only on the first/unset ask). next-build.md's Build-entries reveal was the main "always renders even in local" culprit — it pasted unconditionally; it's now mode-aware. plan.md and next.md's pointer sites gained the mode condition alongside their existing editor check. SPEC gained a Working mode capability paragraph; setup.md asks it as optional Q7; the template carries the field defaulting to local; the FAQ gained an entry plus an alignment fix. This project's CLAUDE.md dropped the expired temporary section and set `Working mode: local`.

**Files touched:** plugin-behaviour.md, CLAUDE-TEMPLATE.md, setup.md, plan.md, next.md, next-build.md, faq-template.md, faq-index-template.md, SPEC.md, CLAUDE.md (this project's)

**Routed to Captures:** [working-mode-render-verification]
