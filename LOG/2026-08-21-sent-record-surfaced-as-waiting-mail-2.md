# [HASH] — The archive directive now names the outbound register as the thing it must not archive

The doc half of the same defect. The scan exclusion stops `INBOX/sent.md` being *counted*; this stops a session that reads the surfacing directive from *acting* on it.

The directive riding the mail notice tells the session to route each message through the triage and then move the file to `INBOX/archive/`. Followed literally against `sent.md`, that files away the outbound register — the one artifact a repeal is checked against, and what /plan's keep-step greps when work repeals something already announced.

`feedback-and-inbox.md` now says, beside the archive instruction, that the register is never waiting mail and is never archived, and that the scan excludes it by name so the two halves agree.

Keying the whole fix on the doc alone stayed refused: a directive is a step and a step can be skipped, which is why the scan exclusion is the load-bearing half. This session is the evidence — the file was recognised for what it is by judgment, which is judgment covering for a mechanism.

**Files touched:** plugin/throughliner/docs/feedback-and-inbox.md
**Routed to Captures:** none
Rule gate: run — admitted as a subordinate clause on the archive directive in feedback-and-inbox.md (its named parent), consuming no slot. Recorded instance: this project's own session openings surface `INBOX/sent.md` as waiting mail. Nothing evicted — the clause narrows an existing directive rather than adding a freestanding rule, and the restatement test is satisfied because the clause *is* the restatement rather than an exception bolted on.
