# 0d02b6a — Blocking asks reworded to lead with the recommended route, at two sites the grep widened from one

Kept on Claude's recommendation and your agreement. The AFK-cats finding: a mid-build scope ask landed as a flat two-option menu though the run had already established the fix was minor, and you spent a turn asking for the recommendation. The fix: the run decides which arm applies and asks one recommended question, the other route named only as the escape. The keep's grep corrected the discussion's one-file scope to two: the scope-growth ask lives in next-build.md, and the discovery table's needed-and-minor arm lives in the always-loaded skill-nonspecific-rules.md. ANNOUNCEMENT-IDEAS.md's description stays true and is untouched.

**Queue changes:** kept and cleared, in the build set.
**Work processed:** kept — [build-scope-ask-lands-as-a-menu].
Rule gate: run — amendments to both existing ask sites; nothing new admitted.
