# [HASH] — Docset A deleted: 40,684 words gone, every skill repointed at `docs-b/`, and the two-docset world written out of the project's own instructions

This re-applies a decision rather than making one. Docset A was retired at `c4cf5af` on 2026-08-08 and the emergency revert brought it back a day later, so the tree arrived at this build holding three mutually inconsistent statements: `CLAUDE.md` describing A as a frozen 4.8 fallback with session_start choosing between docsets, `resources/self-authoring-rules.md` stating A retired and 4.8 unsupported, and `plugin/si-plugin/docs/` physically present at 40,684 words.

The decision is the user's, made 2026-08-09. What they said was that both Fable 5 and Opus 5 work now and it's fine. The reasoning built on that — that the no-strand guarantee the two-docset design existed to provide has therefore expired, since A was only ever kept as a known-good fallback against a bad migration to B — is Claude's, on their call. That distinction is recorded deliberately: this same run built [rationale-misattribution], and the queue item for this work was one of the three it named as wearing Claude's reason-shaped sentence as the user's.

The sharpest fact behind the urgency was confirmed live at the previous planning session: the `/plan` skill loaded `docs/plan.md`. All four SKILL.md files still pointed at `docs/`, which means the host had been running the docset the method believed it had retired, and every recovery built into `docs-b/` over the preceding week was text nothing was reading. Retirement was therefore mostly a repointing job, not a rewrite.

What was actually done: `plugin/si-plugin/docs/` deleted outright; all four `skills/*/SKILL.md` files repointed, then the whole package grepped to confirm zero surviving `docs/` references; `CLAUDE.md`'s Model target section rewritten to describe one docset and no model detection, dropping the frozen-fallback framing and the three-phase rollout that ended in detection; and three other `CLAUDE.md` sites repointed — the folder tree, the batch-type rule, and the self-hosting dependency-ordering paragraph — which the item hadn't enumerated but which the `docset`/`docs/` grep it *did* prescribe turned up.

Two things were deliberately kept. The `docset: B` frontmatter stamp stays on every `docs-b/` file: its original job, proving a session opened B rather than A, is vestigial with one docset, but the behaviour-rules redirect restored in this same run self-checks against it, so it has a live consumer and removing it would be churn. And the historical record was left alone — LOG entries, `resources/research/`, and the two `docset` mentions in `resources/2026-08-09-emergency-revert-plan.md`, which describes what the revert did and was true when written. Only `resources/self-authoring-rules.md`'s retirement date moved, from 2026-08-08 to 2026-08-09, since the revert undid the original.

Verified rather than assumed: `SPEC.md` and `README.md` contain no `docset` references at all, so nothing user-facing changed here and this item raised no spec-sync obligation of its own. Consumers on the 5-series were already being served B, so no FAQ entry was needed either.

The unblocking is the point. `[self-authoring-rules-compliance-audit]` had exactly this as its lift-condition — it would otherwise have inventoried text the running host wasn't loading — and retiring A halves its corpus from roughly 67,000 words to 26,000.

Noticed and not acted on: `plugin/si-plugin/skills/cruise/` is an empty directory. Git doesn't track empty directories, so it cannot ship in the package, and it was left alone rather than turned into a capture.

**Files touched:** `plugin/si-plugin/docs/` (11 files deleted), `plugin/si-plugin/skills/{plan,next,done,setup}/SKILL.md`, `CLAUDE.md`, `resources/self-authoring-rules.md`, `plugin/si-plugin/docs-b/{next,plan,plugin-behaviour,done}.md` (frontmatter notes describing a live docset A).

**Routed to Captures:** `[agents-md-docset-drift]` — `AGENTS.md` carries its own copy of the two-docset Model target section and points at the deleted `docs/` folder. The item's file list named `CLAUDE.md` but not `AGENTS.md`, so the retirement went in on one side only. Captured rather than swept in, since it's adjacent work the cleared item didn't name; the capture also asks the prior question of whether `AGENTS.md` should be maintained at all, given the Codex port it mirrors has been shelved since 2026-07-28.
