# [HASH] — The end-of-queue gate states its refill reset, keeps a plain ending plain, and honours a declared intent to stay open

Two halves, both raised by the user from a session that got them wrong.

The first: the once-per-rest bound said the wrap-up question fires again "only after further work has filled the queue and emptied it once more", but nothing stated plainly that this *resets* the ask. That session hit two such refills, wrongly ended plainly both times, and suppressed questions the rule owed — and its supposedly plain endings drifted into close-leaning framing, nudging toward the exit the gate is worded not to nudge toward.

The second: she declared she was keeping a session open to capture into while testing elsewhere, and the method had no notion of that standing intent. Another session, told the same thing, kept presenting the pre-close message anyway.

Both now live in the gate as operative text. Each refill-and-emptying re-arms the ask. A plain ending carries no close-leaning framing — say the queue is clear and stop. And a declared keep-open-to-capture intent silences the wrap-up ask for the rest of the chat, held in the conversation with nothing stored.

The capture originally named the inline-text switch as the analogue for chat-held state. That switch was retired earlier in this same run, so the wording points at show-first-on-request instead, which holds the same posture.

Refused at planning: storing the intent in a file, against the chat-held posture; and extending the intent to /done, which runs only when invoked and so has nothing to silence.

**Files touched:** `plugin/throughliner/docs/plan.md`.
**Routed to Captures:** none.
**Depth:** short.
**Tick:** done, confirmed — the gate reads with all three provisions and the reset is unambiguous on a second refill.
Rule gate: run — an amendment to plan.md's end-of-queue gate, its parent: the reset stated as operative rule, the standing intent added as a chat-held provision. Nothing evicted.
