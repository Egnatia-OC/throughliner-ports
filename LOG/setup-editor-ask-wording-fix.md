# eed9ba8 — setup.md Q6 editor ask + faq-template.md editor entry reworded — corrected the false "link instead of re-pasting/copying" rationale to the real purpose (identify default .md app so doc links open, needs a default .md reader open), named the skip consequence + token cost, offered skip plainly; and reordered setup.md's FAQ scaffolding step to create FAQ/ before copying templates into it.

The /setup editor question (Q6) and its FAQ entry both explained the editor field with a rationale that was factually wrong: they said knowing the editor lets Claude "link you to your docs instead of re-pasting/copying their text into chat." Claude can't copy or paste — it regenerates every token — and linking to a doc isn't gated by knowing the editor. The question's real job is to identify the user's default `.md` app, so the user can be told doc links only open usefully if they keep a default `.md` reader open alongside Claude. The rewrite states that real purpose, names the skip consequence plainly (docs get written back into chat, at a token cost), drops the copy/paste framing, and offers skip as a plain option for anyone rather than only "if unsure." The remote-control caveat (links aren't useful while Claude drives the screen remotely) was folded into the Q6 wording as a light one-liner rather than a separate step, keeping it from over-promising. The FAQ entry was synced to the same corrected framing. Also folded in [setup-faq-folder-ordering] from the same PFA-5 run: setup.md's FAQ scaffolding step bundled folder-creation and file-copy without pinning order, so a /setup run fired the copy before the `FAQ/` folder existed, failed, then retried — the step now creates the folder before copying the templates in.

**Files touched:**
- plugin/si-plugin/docs/setup.md — reworded Q6 ask + guidance; reordered FAQ scaffolding step
- plugin/si-plugin/templates/faq-template.md — synced the editor FAQ entry to the corrected framing

**Routed to Captures:** none
