# 1215d24 — Plan: cleared the Simply Sew E2E planning gate — 3 audit-finding batches ([spec-edit-build-guard], [plan-winddown-rescan], [red-flag-planning-stage]), parked [spec-sot-rethink], + 2 /plan-flow batches ([recommend-fold-on-agreement], bulk-approval folded into [plan-winddown-rescan]); removed the plan marker

This session was forced by the `--- Plan session here: process the Simply Sew E2E audit findings ---` marker at the top of the queue. It processed the three Simply Sew E2E audit findings, handled two method observations the user raised in-session, and removed the marker once its reason was addressed.

The three findings became three batches. [spec-edit-build-guard]: a spec-edit had been folded into a feature build and nothing mechanical caught it (the scope-lock passes because SPEC.md was listed in Files, and the lint allows both subheadings) — so the fix is belt-and-suspenders, an authoring rule plus a lint backstop, matching the dependency-integrity pair [plan-close-dep-check]/[plan-close-dep-lint]; the lint is the load-bearing half because the authoring rule alone repeats the discipline that slipped. Confirmed with the user that a spec-edit and a feature build are always separate, with no legitimate hybrid. [plan-winddown-rescan]: the close-out never proactively re-read the session's own discussion for things said in passing but never flagged — checked the docs and the step is absent, not present-but-unfired — so a best-effort, memory-dependent wind-down re-scan was added; it lands at /plan not /done because the find-then-sharpen flow it triggers is planning work and /plan has the most free-form discussion, and its surfaced candidates are presented as a single bulk-approval set (audit-findings style) rather than one at a time. [red-flag-planning-stage]: a privacy risk raised and designed-out during planning was handled inline and never recorded in the Red flags section with a state — the rule is build-session-shaped — so it was clarified to record planning-stage risks with a state, screening threshold unchanged so it adds no noise.

The deeper issue under the first finding was parked as [spec-sot-rethink]: SPEC is strictly enforced but too sparse to be worth the ceremony, it defaults to app-shape even when a project is a whole business (seen in Simply Sew), and the fork — one project-shaped SPEC vs multiple specs — is a structural redesign too big for a findings session; it reopens by conscious revisit.

Two in-session observations became further work. [recommend-fold-on-agreement]: when the interview already secured an explicit agreement to promote, the separate recommend step re-asks a settled decision — so it folds into the draft for promote (the draft-approval stays the user's safety net), while park and drop keep the recommend step as their only decision point. The user's second observation — that an ad-hoc capture re-scan in the Simply Sew project returned fully-drafted captures for one approval, which felt right — folded into [plan-winddown-rescan] as the bulk-approval presentation, matching the existing [SEQUENCE] bulk-approval inversion.

**Queue changes:**
- Removed the plan marker "process the Simply Sew E2E audit findings" (reason addressed).
- Promoted [spec-edit-build-guard] (after [plan-close-dep-lint], the integrity-guard family).
- Promoted [plan-winddown-rescan] (after [review-vs-test-framing]); later amended to present surfaced candidates as a bulk-approval set.
- Promoted [red-flag-planning-stage] (after [plan-winddown-rescan]).
- Promoted [recommend-fold-on-agreement] (after [verbatim-at-checkpoint]).
- Parked [spec-sot-rethink] in Captures > Parked.

**Captures routed:** 3 Simply Sew findings promoted ([spec-edit-build-guard], [plan-winddown-rescan], [red-flag-planning-stage]); 1 parked ([spec-sot-rethink]); 2 in-session observations routed ([recommend-fold-on-agreement] promoted; bulk-approval refinement folded into [plan-winddown-rescan]).
