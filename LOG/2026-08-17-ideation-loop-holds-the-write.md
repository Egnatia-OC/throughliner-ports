# dc52025 — an ideation loop: Claude offers to capture and holds the write until the design settles

The write-first rule's test — is the previous version recoverable without the user? — holds, and is untouched. What it assumed silently is that the text being written is *finished*. Mid-design it is not, so it fired at every turn of a discussion that had not settled: six writes across one continuous conversation, with the two re-written items landing at 634 and 586 words against a ceiling of 265.

The clause names when text counts as finished. While a design or a disposition is still being worked out, Claude offers to capture and holds the write until the user says go.

Two constraints came from the user at the keep-step. It reaches every skill, since a capture may be filed at any moment, so the rule sits beside the filing rule in the always-loaded file and the guard exemption is not scoped to one skill. And it is subordinate to nothing else in the method except the write-first rule it amends — the append runs once the loop releases, never as a reason to write earlier.

The open question the item carried was settled rather than shipped open: /rescan and the close re-scan need no wording change, because their existing terms — decided, noticed or asked for and never written into a file — already reach a held design, which is all three.

The loop was adopted for the remainder of the session that settled it, which is where the rest of this chat's records come from.

Rule gate: run — admitted as a clause on the existing write-first rule; no freestanding rule and no slot spent. Nothing evicted.

**Queue changes:** [ideation-loop-holds-the-write] kept into Processed, cleared to run.
**Work processed:** kept — [ideation-loop-holds-the-write].
