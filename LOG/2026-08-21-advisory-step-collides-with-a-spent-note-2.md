# [HASH] — The advisory step gains its replace branch, and the clearing rule narrows to license it

Build entry; the planning record is `2026-08-21-advisory-step-collides-with-a-spent-note.md`. Two rules held together only while a planning session ran between every pair of closes; back-to-back build closes met a spent advisory squatting in the reserved slot. Both halves built as one fix stated twice: done.md's filing instruction gains the replace branch — delete the spent note, file this close's own in the freed slot, with the new note saying it replaced one — and the clearing rule narrows in place from "never this close's job" to "never this close's job to clear one it is not replacing". Clear-at-the-read is textually unchanged for a note a close is not replacing. This very close then exercised the branch: the spent "run the build next" advisory was replaced by this close's own.

**Files touched:** `plugin/throughliner/docs/done.md`.
**Routed to Captures:** none from this item.
Tick: done, confirmed — grep finds no remaining absolute statement of the old rule.
FAQ: not needed because the advisory's user-facing behaviour (read at the next /plan) is unchanged.
Rule gate: run — two amendments to done.md's advisory step, parents named (the filing instruction and the clearing rule). The old absolute wording of the clearing rule is evicted by the narrowed form in place. Nothing freestanding.
