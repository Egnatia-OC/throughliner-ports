# 9ab680d — Promoted [pin-work-line-format] to a build batch pinning the canonical `#### … [slug]` work-line rendering in plugin-behaviour.md, ordered above [templates-spec-recut] to unblock it.

This session opened from a /next pre-flight on [templates-spec-recut], whose blocker gate flagged the unprocessed capture [pin-work-line-format] as work that should land first. The gap it names is real: all three recut hooks detect a work line by its `####` heading — slug at the heading's end, a provenance label in the block beneath, and a `Red flag · State:` marker for a red flag — but plugin-behaviour.md's Line-format bullet lists the four parts of a work line without ever stating that the description line renders as a `####` heading, the one detail every hook keys on. A scaffold or queue rewrite emitting bold lines or bullets instead would read fine to a human while silently breaking the lint and the red-flag scan, with no error. Promoted the capture to a build batch editing plugin-behaviour.md to pin the rendering explicitly, marked as blocking [templates-spec-recut] and [fresh-queue-clean-break] so both emit the stated shape. Traced against the Line-format bullet, the Red-flags marker, and the two hooks' detection regexes; producer is [hooks-work-line-recut] (shipped); no shared primitive. No SPEC impact — internal format convention.

**Queue changes:**
- Added build batch [pin-work-line-format] at the top of Batches, above [templates-spec-recut] and above the cleared-to-run line.

**Captures routed:**
- [pin-work-line-format] — promoted to build batch.
