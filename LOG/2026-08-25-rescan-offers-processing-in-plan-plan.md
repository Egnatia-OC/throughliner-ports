# [HASH] — Kept: /rescan in a planning chat offers to process what it surfaces

One conditional arm in rescan.md (the user's no-per-skill-docs instinct held), keyed on a /plan invocation earlier in the chat — observable, nothing stored, and the machinery genuinely loaded. The arm hands to the ordinary planning loop on the user's yes; /rescan itself still only files, so the boundary holds. The user's write-first concern was already answered by this session's own work: the parent exception names the /rescan candidate set, and process-now writes no capture at all. Rule gate run — disposition on the item.

**Queue changes:** [rescan-offers-processing-in-plan] kept into Processed, cleared.
**Work processed:** kept — [rescan-offers-processing-in-plan].
