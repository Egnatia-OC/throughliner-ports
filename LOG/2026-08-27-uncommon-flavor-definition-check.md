# 99865ab — plan — [uncommon-flavor-definition-check] kept: rare execution markers assigned only against their re-read definition

From the audit: `[freeform]` recommended against its own definition, corrected twice by the user reading the docs back. A slip of loaded text, but a repeatable kind — rare, similar-sounding markers get misremembered confidently, and flavor decides where work runs at the one moment ordering is settled. The clause: re-read the definition in that turn and name why the work matches it. The user's sharpening travels into the contrast sentence: `[freeform]` runs alone in the sense of running *without the method* — a skilless item done by hand for whatever unforeseen reason — where `Runs alone` is method-governed work in an isolated run.

**Queue changes:** [uncommon-flavor-definition-check] filed and cleared; [freeform-recommended-against-its-own-definition] deleted as merged.
**Work processed:** kept — [uncommon-flavor-definition-check].
Rule gate: run at the keep — amendment; recorded on the item.
