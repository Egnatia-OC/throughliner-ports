# a2ce359 — Kept and cleared: the ordering ask stops naming an order, and leads with the flag when one tops the queue

Processed 2026-08-24. Raised by the user: answering "most-unblocking-first" felt like not approving the real order — the named default often isn't what runs, since the order usually comes from the fallback rules, and the fallback has no consumer-safe name. Second facet from a live instance elsewhere: a neutral either/or ask over an uncleared red flag reads too symmetric. The fix (Claude's wording, her agreement): the ask becomes "shall I pick the order?", the floor narration names the picked order after the fact where it can be true, and the red-flag case leads with the flag as what comes first. SPEC's processing-flow sentence rewritten this session. Gate: run — amendment in place, old wording repealed by the same edit.

**Work processed:** kept — [ordering-ask-wording], cleared with build block.
