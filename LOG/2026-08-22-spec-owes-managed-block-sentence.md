# [HASH] — Deleted, satisfied in-session: SPEC's keeping-current paragraph gains the managed-block carve-out

Written with the user present: the plugin-managed block of a project's CLAUDE.md is method-owned and is the one region migration refreshes — reporting what it replaced and moving user-authored lines below the end marker rather than overwriting them; the never-clobbers-user-text sentence now scopes itself to the user's own text. SPEC-only, so the item closed outright.

Rule gate: not needed — a SPEC sentence, no method rule.

**Work processed:** deleted — [spec-owes-managed-block-sentence], content relocated to SPEC.md.
