# [HASH] — plan — [shared-vocabulary-not-standing-names] kept: the method's own terms are the vocabulary; "the ready list" retired

The user's diagnosis and decision: the standing-plain-English-names idea was recursively feeding the method — each session minting terms that get fed back in — so it is dropped, while the plain-language effort stays. Her direction in substance: users and developer speak the same Throughliner vocabulary, even if hard at first, so she can actually help them. Claude's agreed sharpening: the words to use are the ones the user's own artifacts show ("Cleared to run above this line"), so "the ready list" — an alias for a thing the file names differently — is the live instance of the recursion and is retired. Repeal grep found all sites; the queued plain-English-consent post draft quotes the name and was held behind this build.

**Queue changes:** [shared-vocabulary-not-standing-names] filed and cleared; [discord-post-plain-english-consent] moved below the line, Blocked by this item.
**Work processed:** kept — [shared-vocabulary-not-standing-names].
Rule gate: run at the keep — supersession and eviction recorded on the item.
