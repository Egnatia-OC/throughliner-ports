# [HASH] — next-build.md's spec-sync replacement note cut, its lookalike neighbour left standing

"Note what this replaces. The build close used to *sync* SPEC to match what was built…" explained why reading SPEC at run start makes a real check possible where a close-time sync could not. The step above it already routes all three cases — agreement, contradiction, new product truth — and is complete without the history.

The neighbour was the interesting part of this item, and it was flagged at planning so a build would not sweep it up. "The reason clause fires every time, not only when the user sounds impatient" looks like rationale and is not: it states *when* the clause fires, which is the rule. The tell is the reclassification test in reverse — delete it and the surrounding instruction becomes incomplete, because nothing else says the clause is unconditional. It stays.

**Files touched:** `plugin/throughliner/docs-b/next-build.md` (−7 lines; the file also gained the SPEC-ask wording and the SPEC-rules pointer under separate items this session).
**Routed to Captures:** none
