# 2f4f22c — /plan: promoted [session-handoff-offer] + [faq-update-check-rewrite]; retired [fresh-queue-clean-break] (merge reconciles off main's drained queue); opened the remote-control workflow thread

This /plan processed the three waiting captures, then followed a remote-control workflow thread the user raised.

**Captures.** [session-too-long-handoff-offer] → promoted as [session-handoff-offer]: a plain plugin-behaviour rule offering a fresh-session handoff when the user reports the session is degrading. Chosen over folding a pickup-line into the LOG, which would tax every skill open for an occasional need. The rule rests entirely on the user's signal — Claude has no internal read on context fullness. [faq-update-check-test-concept] → promoted as [faq-update-check-rewrite]: its blocker [test-concept-redesign] was folded into the adopted two-section redesign (deleted on main 2026-07-04), so the FAQ entry is unblocked and rewritable to the two-section reality. [log-index-unit-two-section] → parked: under the flat two-section list the batch-as-index-unit disappears, so the LOG needs a new searchable unit; [done-work-line-recut]'s per-work-line LOG file is the partial answer to build from.

**Merge reshape.** Retired [fresh-queue-clean-break]. The fork-side wipe-and-rebuild of this project's bloated queue was redundant — main's queue was already drained 2026-07-04, and the merge is the natural place to reconcile the two copies and convert to two-section, off main's clean base. Recorded in CLAUDE.md's branch section; the alternative (keep the fork-side wipe) was rejected as wasted work on a copy the merge supersedes.

**Remote-control thread.** The user works chiefly from remote control for several days and is token-constrained (80% of weekly usage), making the merge a priority — so from here, bias to capturing/parking over promoting. The view-in-doc direction breaks on remote control, where viewing an edited file means Google Drive navigation and re-download. Interim relief: a temporary CLAUDE.md note asks "remote or local" at the start of /plan and /next and renders doc-bound text to chat under remote control. Parked [working-mode-mechanism] (the proper toggle) and [worktree-override-hook] (a verified real WorktreeCreate hook could stop remote sessions forking a worktree — correcting my earlier wrong claim that a plugin couldn't touch it; user comfortable with it living in SI, permission sought at /setup). Research filed at resources/research/remote-control-limitations.md: the "regenerate self-authored text for approval" waste is inherent to LLMs, not a Claude Code oversight, and the precise feature request appears un-raised — decided not to file, since view-in-doc is the real fix.

[keep-x-identity-until-merge] → routed to CLAUDE.md as a rollout/retirement path so it stops being rehashed each session. Filed a new capture [installed-build-vs-redesign-state]: the session ran under main identity 1.15.0-test1, not the -x fork — a possible drift to pin down, and the thing that answers "when can I rely on the new plugin here."

**Queue changes:**
- Promoted [session-handoff-offer] and [faq-update-check-rewrite] (both cleared to run, above the readiness line).
- Retired [fresh-queue-clean-break]; merge reconciliation reshaped in CLAUDE.md.
- Parked [log-index-unit-two-section], [working-mode-mechanism], [worktree-override-hook].
- Routed [keep-x-identity-until-merge] to CLAUDE.md; filed new capture [installed-build-vs-redesign-state].
- Added temporary remote-control rendering note to CLAUDE.md; filed resources/research/remote-control-limitations.md.

**Captures routed:** promoted [session-handoff-offer], [faq-update-check-rewrite]; parked [log-index-unit-two-section], [working-mode-mechanism], [worktree-override-hook]; routed [keep-x-identity-until-merge] to CLAUDE.md; filed new [installed-build-vs-redesign-state]. Post-close tail: filed [execute-merge-to-main] (merge as Claude-run work; user switched to local mode) — rides the next commit.
