# [HASH] — filing a capture gets a tool, so an append can no longer land in the wrong section

Filing was the last queue operation still done by hand. The mover covers moves and deletions; nothing appended a new entry, so every capture was written by a text edit anchored to whatever sat at the end of the file — and an anchor read before the file changed underneath it put a capture into Processed, above the readiness line, where a run would have tried to build three undesigned routes.

All three of the item's questions answered yes. `reorder_queue.py` gains an append placing an entry at the bottom of a named section; the write guard gains the same exemption the mover already has; and the always-loaded filing rule names the tool the way the keep-step names the mover, which is the part that makes it get used, since an instruction with no tool behind it is what produced this.

The body arrives by file rather than on the command line. A multi-paragraph rationale passed as an argument breaks on this platform over quoting, newlines and em-dashes, so Claude writes the text to the scratchpad with the ordinary editing tools and the script reads it. The queue itself is still only ever touched by the script.

Two constraints came from the user. It is reachable from every skill, since a capture may be filed at any moment. And it is subordinate to the ideation loop — the append is what runs once the loop releases the write, never a reason to write earlier.

The evidence grew during the session that settled it: three more hand-anchored appends, one of which nearly repeated the original failure.

Rule gate: run — admitted as a clause on the existing filing rule, naming a tool that replaces a hand procedure rather than adding one.

**Queue changes:** [no-mechanical-route-for-filing-a-capture] kept into Processed, cleared to run.
**Work processed:** kept — [no-mechanical-route-for-filing-a-capture].
