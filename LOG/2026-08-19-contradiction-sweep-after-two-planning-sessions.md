# cb50e2b — the user asked for a contradiction sweep of Processed and it found six, one of them written an hour earlier by the session doing the sweeping

Her request, and her reasoning with it: it is an oddity of two planning sessions with no /next between them that this becomes much more likely, though she said she was not sure. She was right, and the mechanism is slightly different from the one she named. **/next is what removes items from the queue.** Without a build in between, every decision lands as an edit to an item that stays, so each new decision meets a larger surface of unbuilt items to contradict. Two planning sessions back to back is how that surface gets big; the cause is the missing consumption rather than the doubled planning. This session compounded it, because retiring a whole rule family touches far more items than an ordinary keep does.

Six were found, plus two residuals on a second pass.

The worst was Claude's own, made within the hour. Removing the rationale lens from [law-prose-restyle-heavy-docs] appended a paragraph saying so and left the earlier paragraph introducing the lens as part of the pass, so the item described it as both in scope and removed — a build reading top-down would have done the thing the session had just decided it must not do. It is exactly the merge-done-as-append failure [folding-in-has-no-eviction-step] describes, committed in the hour that item was written. Merging it properly took 63 words off and became that item's first evidence.

The others: [law-prose-restyle-remaining-docs] said the rationale test "stops there" when it is no longer there at all; [split-action-defeats-the-bands-in-aggregate] still had a rule-gate line invoking a band being retired two items above it, and carried a live instruction to give a breach action a second limb when there is no breach left to act on; and [split-the-cleared-region-for-concurrent-sessions] justified breaching a 345-word ceiling that the item directly beneath it retires.

The sharpest was [competition-comparison-article], because it is a `[user]` item cleared to run that produces **public** text. It named the article's honest weak points as "manual curation, a 56,000-token queue read" and said those were answered by [digest-reports-computed-fields-not-summaries]. False twice over: the digest was expressly stopped from replacing the queue read on 2026-08-17, so the full read is still paid deliberately, and the thing that now addresses it is the unbuilt build view. As written the article would have claimed a solved problem that is not solved — the same shape as [discord-post-context-adjacency], which was nearly posted about a mechanism that no longer existed.

Two residuals turned up on a verification pass: the compression item's own **title** still named the retired ceiling, and its 61-word figure needed restating as what it always was — a measured cut-off selecting a work list, not a limit anyone must write to. One mention of the 345 ceiling was left standing in [chip-replies-are-indistinguishable-from-user-authorship], where it is a historical record of why that item was split and is correct as written.

**The check did not catch any of these, and that is the finding worth keeping.** The queue digest reported zero placement contradictions, and it says of itself that it matches a fixed set of known phrases. All eight were semantic. Nothing automatic would have prompted the sweep; a person asked for it.

**Queue changes:** eight corrections across [law-prose-restyle-heavy-docs], [law-prose-restyle-remaining-docs], [split-action-defeats-the-bands-in-aggregate] (three, including its heading), [competition-comparison-article] and [split-the-cleared-region-for-concurrent-sessions].

**Work processed:** none kept or deleted — this was a reconciliation pass over items already in Processed.
