# 324005c — Kept and extended: the planning opening's correspondence scan covers issue answers and, later the same session, incoming issues

The user's proposal from a chance-found upstream answer. Settled: shipped (degrading silently without `gh`), computed newness (comments after the last planning record; a surfaced answer files one capture, deduped by its open existence), planning-only, folded into the mail step per the scan-displacement rule. Extended at the user's unification question: where the project has a repository that can receive issues, new incoming issues surface like mail. Their replace-the-register question answered and recorded on the item: GitHub cannot scope issues to a project — only the per-project sent register can — and `gh` is not assumed, so the register stays the record and the CLI the reader. Rule gate run — disposition on the item.

**Queue changes:** [plan-open-github-issue-check] kept into Processed, cleared, extended with the incoming arm.
**Work processed:** kept — [plan-open-github-issue-check].
