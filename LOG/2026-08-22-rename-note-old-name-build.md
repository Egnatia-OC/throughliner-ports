# c904687 — CLAUDE.md Model target — rename note's lost old folder name restored

Filed 2026-08-21 by Claude at a freeform close's wind-down ([claude-md-rename-note-lost-old-name]): the folder-rename note read "renamed from `docs/` to `docs/`" — the same path twice, the old name lost in an earlier edit. Restored to "renamed from `docs-b/` to `docs/` on 2026-08-21". A one-word restore, not a deletion: the note is what explains why old records legitimately carry the old path, so it must keep saying what that path was.

Tick: done, confirmed (the note names two different paths; nothing else changed).

Rule gate: not needed — restoring a lost word in a history note; no rule authored or amended.
FAQ: not needed because CLAUDE.md is host-only.

**Files touched:** CLAUDE.md
**Routed to Captures:** none
