# 324005c — Raised and kept: method reports go issue-first, with a reply-check scheduled at send where wanted

The user's two linked proposals, processed together: uniting external comms lands at the scan (outbound already one register; issues stay on GitHub, nothing copied), and the method-report route becomes a GitHub issue on the plugin's repository where `gh` exists — public under the reporter's account, stated plainly — with the form for the no-CLI and privacy cases. What the issue buys is the return path. The reply-check is the user's design: only where a reply is wanted, the check method agreed at send (issue comments, or an email route — Claude checks what email connections exist at that moment and asks if any suits), one dated capture filed on the existing `Not before:` machinery. SPEC's reporting paragraph updated at the keep. Rule gate run — disposition on the item.

**Queue changes:** [method-feedback-issue-first] filed and cleared into Processed.
**Work processed:** kept — [method-feedback-issue-first].
