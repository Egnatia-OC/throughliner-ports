# [HASH] — [user] handover confirmed: INSTALL.md B.1 self-install commands work from the remote

Confirmed live this session (the `[user]` handover item, closed and removed from Processed). The exact two commands in INSTALL.md B.1 — `claude plugin marketplace add FlintCraftTech/sovereign-implementer` then `claude plugin install sovereign-implementer@flintcraft` — ran successfully this session, confirming the self-install branch works once the manifest is on the remote. Same run as publish-marketplace-manifest-2. Like that item, this was tagged `[user]` on a false "Claude can't drive the terminal" premise (Claude drove it by full-path CLI), captured for re-tag. (Earlier same-slug entry from a prior session remains at install-self-install-branch.md.)

**Files touched:**
- none (verification handover; the item is removed from Processed)

**Routed to Captures:** see publish-marketplace-manifest-2 (buried-user-work-not-surfaced, handover-completion-ask-inverts-walkthrough, install-items-mis-tagged-user)
