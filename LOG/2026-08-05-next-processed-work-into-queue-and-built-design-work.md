# 3affa20 — next.md states the removal asymmetry (a run removes from Processed, never inserts) with a post_tool_use.py backstop; the underspecification test gains a does-it-say-what-changes limb; plan.md keep-step describe-the-build check now blocking

The plan/build line failed at both ends in a live consumer run: /next offered to move a capture into Processed (processing, which is /plan's alone), and the item it was already building was design work a /plan had let through as buildable. Two premises were corrected at processing and both shaped the fix. First, /next MUST write to Processed — its scope-lock step removes the run's items — so the rule is not a prohibition but an asymmetry: **a run removes items from Processed and never inserts them**, now stated at the removal step in both docsets (A's statement is a correction under the freeze rule — its removal step carried the same gap) and backstopped mechanically in post_tool_use.py, which flags a `#### ` heading newly inserted under Processed while _build.md exists, attributed from the edit's own old/new strings (Write replacements can't be attributed and pass — deny-list design). Second, the underspecification test keyed on "can I name the files?", which design work almost always passes — "Files (rough): plugin-behaviour.md, plan.md" is exactly what an undesigned item looks like — so self-scoping succeeded mechanically while the work remained unbuildable. The test now has a second limb in docs-b: does the item say what changes *inside* the files it names. And the gate that let the design work through in the first place has teeth: docs-b/plan.md's keep-step describe-the-build requirement is now a blocking check — both limbs stated before a keep can proceed, with sharpen-or-skip as the named alternative — rather than a forcing function a session could satisfy by feel.

Overnight blitz run (branch overnight-blitz-2026-08-05b): built under the blitz plan's sanctioned departures — approvals deferred to the branch review, no push.

**Files touched:** plugin/si-plugin/docs-b/next.md, plugin/si-plugin/docs-b/plan.md, plugin/si-plugin/docs/next.md, plugin/si-plugin/hooks/post_tool_use.py
**Routed to Captures:** none
FAQ: not needed because the boundary being restored is already the FAQ's description of how /plan and /next divide the work.
