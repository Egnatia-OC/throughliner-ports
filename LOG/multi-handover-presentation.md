# [HASH] — next.md Step 3 — bundled-presentation clause for multiple independent [user] handovers (+ independent-only carve-out); fixed stale "at Ready" reference

next.md Step 3 — added a clause presenting multiple independent `[user]` handovers together in one message, with an independent-only carve-out. The two-pass /next model can leave several independent handovers for the final pass, and next.md didn't say whether to present them one at a time or bundled; decided to bundle, as the [SEQUENCE] bulk-approval inversion for a deterministic, already-approved set (each cleared in /plan, above the line) — seeing them together lets a non-coder plan and batch them rather than being drip-fed. Genuinely ordered/dependent handovers are still sequenced. Referenced plugin-behaviour.md's [SEQUENCE] inversion rather than restating it. Also fixed the now-stale "confirmed the whole run at Ready" reference that ready-stop-redundant made obsolete (→ "at the Step 1.3 off-ramp"). Target-side; live after reinstall.

**Files touched:**
- plugin/si-plugin/docs/next.md

**Routed to Captures:** none
