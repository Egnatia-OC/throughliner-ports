# 7bc2c58 — Six "batch" descriptions in this project's CLAUDE.md rewritten to the work-item model

`/setup` reported these and nobody wrote them down. The queue stopped being a queue of batches when the two-section work-line model merged to main, so every rule describing "the active batch's file list" or "a new batch type" was describing machinery that no longer exists — read at the start of every session in this project.

Six places changed: the scope-lock line, the cross-doc-reference rule, the FAQ-authoring rule (twice), the hook-ripple rule's cross-reference to "A new batch type touches four places", and the SPEC rule.

Re-grepped rather than trusting the line numbers in the item, which came from `/setup`'s report and had drifted. That was worth doing — the item said five places and there were six.

What each rule *requires* is untouched; only the noun changed. The two surviving mentions of "batch" both name a retirement, which is what the acceptance asked for.

**Files touched:** CLAUDE.md
**Routed to Captures:** none
Rule gate: not needed — terminology correction; what each rule requires is untouched.
