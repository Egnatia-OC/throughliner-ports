# [HASH] — The build view learns to carry the gate disposition, and the who-authors-rules question is split off rather than decided by momentum

Kept with the script-only repair: `generate_build_view.py` pulls the labelled `Rule gate:` (and `FAQ:`) lines from an item's prose into its view block, and CLAUDE.md's halt instruction narrows to fire only where no disposition exists anywhere — its intended case. The repair is compatible with either answer to the authorship question, which the user's /plan-writes-CLAUDE.md proposal raises and which is deliberately not decided here: it is split out to [plan-as-rule-author] in Unprocessed, carrying both arguments (SPEC-symmetry; /plan is the session the user is in) and both costs (the plan-does-not-build boundary; planning load). `BUILD-VIEW.md` joins `.gitignore` in the same build. Build block on the item.

**Queue changes:** [build-view-strips-the-gate-disposition] moved Unprocessed → Processed, cleared; [plan-as-rule-author] filed to Unprocessed.
**Work processed:** kept, with one split.
