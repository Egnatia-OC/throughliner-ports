# [HASH] — Chat-level record for the beta-eve build run: 11 builds, 3 audits, 18 captures, and a pin that ships ahead of its branch

The chat-level half of a single /next run that worked the whole cleared region — eleven build items and three audits — closed here. Each item has its own entry under its own slug; this one carries what belongs to the chat rather than to any item.

**The run's shape.** Everything above the cleared-to-run line was built or read, in queue order, with no per-item confirmation. Both `[user]` items at the end of the run were reached and neither could be walked: each begins with today's release, which has not happened. They stay in Processed untouched.

**The one thing left unfinished, and it is visible rather than silent.** `[beta-branch-install-pin]` ticked as unconfirmed. Its docs half shipped — every install string in README, INSTALL.md and the new FAQ entry now reads `FlintcraftTech/throughliner#beta` — while the branch it names does not exist, because the item's own instruction is to cut it only after today's release commit is on main. Between now and that release the published install route does not resolve. A revert of the pin was offered; Alex's decision was to leave it, on the ground that the release is being pushed through within the hour.

**Also in this chat:**

- Alex asked mid-close whether the run matched the beta-day notes at the end of the queue. It did — the two items that note names as running before the release were the run's first two, and the transcript analysis it deferred "until you supply the files" ran because the files were supplied. The check also surfaced the install-pin gap above, which is how that decision came to be put to her.
- The SPEC contradiction found at the close: `plan.md` now describes the log-index read as a derived window while SPEC and two shipped docs still describe the repealed "handful of newest lines". Put to her with the option of correcting the two shipped docs now; her call was to file it. Filed as [log-index-window-ripple-unfixed], which also records how it got through — the repeal-trace grep was not run when the item was kept, so its file list named one doc.
- Three findings in the compliance audit are against rule text written earlier in this same session. Flagging one's own work was the contested call and was kept, because the item recomputes its scope from the record rather than from the session.
- One item's premise was found wrong while auditing it: `[transcripts-two-failed-builds-analysis]` calls its subject "the failed build", and that build succeeded. The finding set says so rather than reading the transcripts to fit the framing.

Advisory: filed — [forward-advisory]

Files touched: see each item's own entry.
Routed to Captures: [fresh-sessions-rule-names-retired-model], [spec-sentence-cycles-opening-line], [log-index-window-ripple-unfixed], [gate-line-in-working-file-is-positional] — the four filed outside an audit's finding set.

The last of those came from the close's own re-scan: a transcribed rule-gate line in the build working file carries no slug, so three times this run a new tick landed between a previous item's depth line and its gate line. Each was caught and repaired in session; the capture records that nothing would have caught one that wasn't.

This session ran on 2026-08-26.
