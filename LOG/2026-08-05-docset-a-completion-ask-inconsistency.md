# [HASH] — docset A corrected: completion-mode branching removed from plugin-behaviour/plan/next/done-plan, planning ask-gate explained fresh in A's register; CLAUDE.md states the freeze rule

The freeze rule, decided by the user 2026-08-05, is now written into CLAUDE.md's Model target section: the docset-A freeze bars *development* (new capabilities, register changes) and permits *correction* (whatever A needs to stay consistent with itself, the hooks, and SPEC). This build ran the two corrections that settled the rule. First, the surviving completion ask: [user-lines-walk-through-only-no-asks] removed every completion ask, but its procedure changes landed in docs-b only, so A still described completion mode across four docs and — the live half — still carried the trailing already-done note in its walk-through branch, precisely the ask the method removed. That made a SPEC sentence ("no session, at any point, asks whether one is already done") untrue for 4.8 sessions, which is what settled the freeze question: a no-strand fallback that strands the user a different way is not a fallback. A's plugin-behaviour.md Completion mode section is replaced by a "There is no completion ask" section (with the stale `Completion mode:` field named as a leftover to ignore silently); plan.md's async sweep became the no-sweep rule; next.md's two trailing-note paragraphs became never-ask statements; done-plan.md's completed-item close now keys on the user mentioning completion. Second, merged at processing: A's plan.md carried the bare "Never build during /plan" ground rule with nothing explaining the planning ask-gate hook that fires for 4.8 sessions like any other — a fresh account was authored at that ground rule. Both corrections were authored fresh in A's register with the why-clauses inline, never pasted from B — B is lighter by subtraction and its register applied back to A would regress 4.8's rule-following, the trap the item named explicitly.

Overnight blitz run (branch overnight-blitz-2026-08-05b): built under the blitz plan's sanctioned departures — approvals deferred to the branch review, no push.

**Files touched:** plugin/si-plugin/docs/plugin-behaviour.md, plugin/si-plugin/docs/plan.md, plugin/si-plugin/docs/next.md, plugin/si-plugin/docs/done-plan.md, CLAUDE.md
**Routed to Captures:** none
FAQ: not needed because the shipped FAQ already covers the no-completion-ask rule and the stale Completion-mode line; this makes docset A match it.
