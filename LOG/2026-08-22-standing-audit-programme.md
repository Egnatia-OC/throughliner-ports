# d7ea8a0 — The standing audit set designed and kept: one new mechanical check carries the trigger, one delta-scoped audit class, four lenses

Designed in-session, both freeform passes done, as the item's own sequencing required. The set by the item's own test: retired-term naming and near-duplicates stay mechanical (existing checks); one new firing check, **audit-lag**, files one delta-scoped `[audit]` capture whenever rule-corpus commits exist since the most recent compliance-audit LOG entry — riding the existing run-at-rule-bearing-close site, so no audit depends on anyone remembering, and the trigger is a fact rather than a number. One audit class, run by /next, criteria in the checklist: delta scope, parent-axis only, four lenses — self-authoring compliance, mechanism-accuracy, tag/narration drift, and **disguised rationale**, the user's addition this session: history is rationale that does not read as rationale (the SPEC output-style paragraph being the instance found the same day), and the wider class of unmarked why-forms is caught by the delete-and-read test rather than by markers like "because". A matching CLAUDE.md clause on the rationale-lives-outside rule is dispositioned on the item for the build to transcribe. Evicted: the sweep-due measurement in rule_signals.py, which could never fire. The first instance is [post-restyle-compliance-audit] (sibling entry), carrying the one-time history sweep over the whole corpus including SPEC.

**Queue changes:** kept into Processed, cleared to run; the first-instance audit filed and cleared immediately after it.
**Work processed:** kept — [standing-audit-programme].
