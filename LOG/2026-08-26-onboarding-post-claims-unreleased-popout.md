# [HASH] — Pop-out post resolved down the nothing-to-change branch: today's release ships the current build, which carries pop-out

Processed on the back of the beta-day decisions (`2026-08-26-expedite-first-beta-release.md`). Releasing from the current build makes the post's pop-out claim true when the release publishes, so the item kept only its confirming half as a cleared [user] line: Claude confirms the released commit carries the pop-out case, Alex re-reads the paragraph, and the register line's untrue-when-posted warning is updated on her word.

**Queue changes:** walkthrough rewritten to three steps; kept and cleared at the end of the ready list.
**Work processed:** kept — [onboarding-post-claims-unreleased-popout].
