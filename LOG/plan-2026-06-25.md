# [HASH] — /plan: retired the spec-edit batch type (SPEC editable in /plan + via a /next ask; two close-out spec-sync gates replace it; drops [spec-edit-build-guard] + lint) + SDD-compliance research; generalized resources/4-8-authoring-heuristic.md → authoring-heuristic.md (model-agnostic guide + 4.8 section, home for rule-writing rules); /done hash-backfill noise-fix batch; 2 captures promoted, cruise-control concern (6) + [spec-sot-rethink] note added

The session was driven by the user's report that spec-edit batches are a time sink across all projects — often a whole batch for one line. After retrieving why the spec-edit batch type was implemented ([spec-edit-batch-type], f4fba84: to kill a confusing read-only special case and a redundant lock), a design session decided to retire it. Three decisions: (1) drift is prevented by two spec-sync checks at the session close points — a new one at the /plan close and the existing /done-build check hardened to a stop-the-close gate — because last time SPEC was editable in /plan it got left behind; (2) the batch type, its subheading, routing, and the Build+Spec guard ([spec-edit-build-guard]) plus its lint all retire, SPEC becoming a normal file any batch can list; (3) scope bounded to the workflow change, handing SPEC structure to [spec-sot-rethink] and the unattended-spec-edit case to [cruise-control]. Research confirmed this complies with spec-driven development and aligns better than the batch did — SDD's contract is "cite-or-update the spec in the same commit," which the close-out checks implement; SDD doesn't require a separate gated step (filed: resources/research/spec-driven-development-edit-workflow.md). The governing constraint the user set: the checks are behavioural with no possible hook/lint backstop (a semantic judgment), so their sole enforcement is rationale-carrying authoring on Opus 4.8 — every rule ships with its why.

Second thread: the [slipped-rule-escalation-heuristic] capture (when a slipped rule earns a hook vs. sharper wording) was promoted, then — at the user's direction — generalized into [generalize-authoring-heuristic]: rename resources/4-8-authoring-heuristic.md to resources/authoring-heuristic.md (a model-named doc goes stale), making it the home for rule-writing rules with a model-agnostic section plus a "4.8" section, keeping CLAUDE.md to a pointer. The user caught a misroute first — the escalation heuristic is self-hosting (only the developer decides hook-vs-rule), not shipped plan.md/plugin-behaviour.md.

Third: [done-dirty-path-backfill-noise] (user-raised) promoted — teach /done's dirty-path check to recognise the expected hash-backfill files and fold them quietly instead of re-investigating every session. A sharpen-the-rule case per the new escalation heuristic, not a hook.

**Queue changes:**
- Added [retire-spec-edit-batch-type], [generalize-authoring-heuristic], [done-dirty-path-backfill-noise] above the cruise-control plan-marker.
- Added design concern (6) to [cruise-control] (an unattended build's needed SPEC change halts the run, like an open red flag).
- Noted in [spec-sot-rethink] that the spec-edit workflow question is now decided (its session owns structure only).
- Dropped the mooted deferred tests for [spec-edit-batch-type] and [spec-edit-build-guard].

**Captures routed:** [slipped-rule-escalation-heuristic] promoted (→ generalized into [generalize-authoring-heuristic]); [done-dirty-path-backfill-noise] promoted.
