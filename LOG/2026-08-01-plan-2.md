# f3518ad — /plan: SA migration-testing → [setup-as-migration-home] (session_start stale-format halt; retire /migrate skill); [user]-workflow cluster from a live distressing send — [user-completion-ask-friction] (→ /setup preference) + handover→"walk-through" rename & branch-rewrite [handover-abandonment-and-terminology]; merged 5 dedupe findings → [factor-done-family-cores] and cleared the docset-A dedupe batch as the opus-5 gate; resolved throughline naming (folded into rename); filed [throughline-value-articulation]

This /plan opened from an SA consumer testing outcome — the migration recipe ran cleanly and /setup already behaves as a partial migration re-scaffold — and widened into a cluster of `[user]`-workflow findings after a real, distressing live send where a `[user]` handover deferred the user's step to /done instead of walking it through. Threads: (1) migration's home; (2) the friction of the /plan `[user]`-completion ask; (3) the "handover" word and the walk-through branch failing to lead; (4) throughline naming and value articulation for the Discord help doc; (5) lining up the docset-A dedupe run to reach the opus 5 build today. Mid-close, the user identified a privacy red flag: logs/captures/work items can carry revealing content into users' public repos, and Claude's "I removed X" narration can't guarantee the approved text is clean — filed as [scrub-gate-before-approval] (uncleared), with a companion [setup-repo-privacy-and-publishing] for the setup licensing/gitignore/public-repo posture.

**Queue changes:**
- Kept → Processed: [setup-as-migration-home] (detect stale format at session_start + halt with a migrate prompt; /setup is the migration home); [user-completion-ask-friction] (move the completion-ask to a one-time /setup preference, suppressed by default); [handover-abandonment-and-terminology] (rename "handover"→"walk-through" + rewrite next.md Step 3 so it drives the user step live before any /done talk).
- Merged 5 dedupe findings ([dedupe-done-recommend-next-core], [dedupe-forward-advisory-filing-paragraph], [factor-log-entry-template-approval-para], [factor-done-verify-completion-staleness-sweep], [dedupe-spec-sync-gate-sdd-why]) → [factor-done-family-cores]; kept [why-clause-audit-pass-by-pass-criteria] and [dedupe-bulk-approval-inversion-reteach] standalone. All three cleared above the marker as the docset-A dedupe /next batch.
- Deleted: [migrate-skill] (superseded by [setup-as-migration-home]; its migration-notes-doc idea rehomed to [migration-recipe-freshness]); [throughline-naming-referent] (resolved in-session — the throughline is the reasoning spine, the LOG records it; naming-clarity note folded into [rename-to-throughliner]).
- Filed to Unprocessed: [throughline-value-articulation]; and mid-close [scrub-gate-before-approval] (uncleared privacy red flag) + [setup-repo-privacy-and-publishing].
- Reorder: cleared the dedupe batch above the marker; moved [merged-plugin-live-verification] below (prereq pending); held [rename-to-throughliner] below (needs its own run). Consumed-and-cleared advisory [advisory-plan-captures-and-rename].
- Rejected alternatives recorded on the items: dropping the completion-ask entirely (→ toggle instead); re-asserting "walk through first" (→ the rule already existed and failed, so fix the ordering tension + the word); five separate dedupe builds (→ merged, they collide on the same files).

**Work processed:** kept [setup-as-migration-home], [user-completion-ask-friction], [handover-abandonment-and-terminology], [factor-done-family-cores], [why-clause-audit-pass-by-pass-criteria], [dedupe-bulk-approval-inversion-reteach]; deleted [migrate-skill], [throughline-naming-referent].
