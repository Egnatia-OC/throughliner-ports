# [HASH] — Merges that widen nothing are now narrated, not asked, with the Files list as the test; scope-sibling grouping added as a bounded tie-breaker

Five merge proposals in one day, none refused, each costing a full turn — and the user's standing preference is always more per run. The rule that replaces the ask is mechanical on purpose: a merge that adds no files to the run changes *where* agreed work lands, not *what* gets built, so it is narrated and done; a merge that adds files is a widening and still asks. Either way the scope effect is named in the same line ("this adds no files" / "this adds two files"), because the drift this feeds is one-directional — the user always says yes, so scope only ever grows — and the narration is the catch-point that lets them object before it compounds. The user flagged this as their own judgment call, not a finding about all users, and a stored per-project setting was rejected at processing: working mode had just failed as exactly that shape.

Scope-sibling grouping is the positive counterpart: /plan now prefers placing kept items so work sharing a file scope builds together, improving the work-per-scope-opened ratio the caveat worries about. Two hard bounds shipped with it. Sharing a file is **not** a readiness test — a sibling still passes the keep-step on its own merits, or "it touches the same file" becomes a route for undesigned work to reach a build. And clustering is a preference, never a gate — an array of misfits must still run, most urgently when it blocks something — under a fixed precedence: dependencies, then unblock-potential, then clustering as a tie-breaker only. That precedence is also the constraint [plan-close-priorities-over-reorder] must honour if it reshapes the close reorder.

**Files touched:** docs-b/plan.md (keep-execute), docs-b/plugin-behaviour.md (dependency ownership), SPEC.md, templates/faq-template.md, templates/faq-index-template.md, FAQ/faq.md, FAQ/index.md.
**Routed to Captures:** none for this item.
FAQ: updated — new entry "Claude merged two pieces of work together without asking me. Is that allowed?", in the shipped template and the host FAQ.
