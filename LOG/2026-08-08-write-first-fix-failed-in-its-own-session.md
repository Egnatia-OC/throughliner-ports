# [HASH] — Doc-bound text: "I've filed / I've written / I've recorded" is held to the pointer's standard

Built in the 2026-08-08 overnight blitz. One clause added to `docs-b/plugin-behaviour.md`'s doc-bound-text rule, beside the composed-from-the-re-read clause: a bare past-tense filing assertion requires the write to have already returned and been re-read, exactly as a pointer does — the assertion and the pointer are one act making one claim, and only the pointer was covered. Worded against the sign-off case specifically (the tidy closing line at the end of a long answer), per the banked evidence that the discipline holds when attached to a check and fails as a flourish; states that it reaches ordinary conversation, which the six converted step-level instructions never could; and carries its own bound — it has nothing to attach to for pure timing failures, per the folded stop-interruption instance. Ripple: migrate-checklist.md and setup.md's write-first exceptions left alone (step-level application sites, not assertion sites); CLAUDE.md left alone (the clause attaches to the doc-bound-text rule only). Docset A untouched (development).

**Files touched:** plugin/si-plugin/docs-b/plugin-behaviour.md
**Routed to Captures:** none
FAQ: not needed because this governs how Claude reports its own actions; a consumer meets only the result.
