# 007a16c — Kept and held: the beta channel — ref-pinned marketplace installs, one release cycle with beta as a step

The three open questions from capture all settled at processing. Install route: research done in-session (resources/research/claude-marketplace-listing-paths.md, beta-channel section) — a marketplace-add pins to a branch via `#ref`, so a `beta` branch fast-forwarded to each Wednesday's pick serves testers through the README's existing ask-Claude install shape; no zips for testers, and the walkthrough is smoke-tested on a second machine before anyone gets it (open feature requests around ref handling made that caveat necessary). Naming: the user's call — one cycle, called the release cycle, beta a step inside the Wednesday turn, not a sibling cycle. Offer wording: drafted in the build; the launch post filed as its own [user] line. Held behind [weekly-release-cycle], whose build creates the definition this amends.

Rule gate: not needed — README and cycles-doc content; no method rule text.

**Queue changes:** item rewritten with build block, moved below the line; [beta-launch-announcement] filed behind it.
**Work processed:** kept — [beta-tester-pathway].
