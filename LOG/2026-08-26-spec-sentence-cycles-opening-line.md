# 47d8625 — plan — SPEC's two cycles sentences written in-session; capture deleted as relocated

Resolved rather than kept: writing SPEC sentences is planning's own work, so both sentences the capture named were written here. The `session_start` bullet now says the opening reports each cycle definition, its cadence and what its observable currently reads — facts, never a due-ness verdict, which the skills compute. The cycles paragraph now says the check speaks in one line whenever a cycles doc exists, so a check that ran and found nothing due is distinguishable from one that never ran. Capture deleted with its content fully relocated to SPEC, narrated per the relocated-delete rule.

**Queue changes:** capture deleted from Unprocessed; SPEC.md edited in-session.
**Work processed:** deleted (relocated) — [spec-sentence-cycles-opening-line].
