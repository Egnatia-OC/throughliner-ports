# [HASH] — Narration cadence promoted out of the consent-gated style into the always-loaded rules, as one subordinate bullet

The style's cadence bullet had no counterpart anywhere in the always-loaded rules, so a project that declined the brevity style got no steering at all on how often Claude speaks while working. That is the shape the dedup repeal's SPEC sentence names as a defect: a rule living only in the consent-gated layer, where promotion is the fix.

Written as one more gerund-led bullet in the Communication "shape every message the same way" list — speaking between tool calls for the first call, a load-bearing finding, or a change of direction, and closing with the outcome first. It reads as a continuation of the parent's opening words alongside its siblings, so no freestanding slot is spent and nothing is evicted.

The item's refusal was honoured and matters: the style's own cadence bullet stays. Evicting it in exchange would re-apply the repealed dedup policy in reverse, and restatement at system-prompt level is the style's mechanism rather than a duplication to shed.

Files touched: `plugin/throughliner/docs/skill-nonspecific-rules.md`
Routed to Captures: none
Rule gate: run — subordinate bullet under the Communication shape-every-message rule, its named parent; gerund-led like its siblings, so no freestanding slot is spent and nothing is evicted. Admission: the failure is documented (the research names narration the largest identified gap), verbose models do not do it unprompted, it fires in every skill and in plain conversation, and no hook can enforce a narration cadence.

One thing the compliance audit later in this same run turned up about it: the audit checklist's doubled-rules table had been listing narration cadence as already carried by the method's own file. It was not — that row was the coverage this item existed to create. Filed as [audit-checklist-table-overclaimed-cadence]. Depth: short. Ticked as done, confirmed.
