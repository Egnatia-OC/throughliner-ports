# [HASH] — Kept and cleared: chat names items by their heading's words, because the outline is what the user navigates

Raised by her from repeated live failures: reading the queue in Understudy, she keeps being unable to find items Claude names by slug — the outline shows heading text, and a slug is not in it. The fix amends the vocabulary table's slug arm: a queue item named in output leads with its heading's opening words (which the heading rule already front-loads), slug after it for the record; queue-prose slugs stay the cross-reference mechanism. Applied in-chat from the moment it was decided.

Rule gate: run — amendment to the vocabulary slug arm (parent named); reworded in place; recurring user-reported failure clears the more-than-once bar.

**Queue changes:** new cleared build item.
**Work processed:** kept — [slug-references-unfindable-in-outline].
