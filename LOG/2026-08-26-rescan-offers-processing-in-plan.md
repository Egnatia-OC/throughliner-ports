# 3b094b5 — /rescan in a planning chat now offers to process what it surfaced, one item at a time

Captured while watching /rescan surface candidates in a live consumer session. When the chat is already a planning one and the user is present, filing the candidates and stopping there wastes the moment — the standing process-now posture says a capture answered "process it now" should be written once, as a work item, rather than filed and immediately rewritten.

Both open questions were answered at the keep-step. Detection is a /plan invocation earlier in the same chat: a skill's instructions stay loaded for the chat's remainder, so the processing machinery is genuinely present, and the test stores nothing. The boundary is preserved because the arm hands over to the ordinary planning loop on the user's yes — /rescan itself still only files, never routes on its own authority.

Her instinct about scope was right and is recorded as such: this did not warrant per-skill rescan docs. It is one conditional sentence in rescan.md, and a build chat's rescan reads exactly as it did before.

The write-first concern this raised is already covered by work shipped in the same run: the write-first amendment names the /rescan candidate set in the parent's exception, and process-now writes no capture at all.

**Files touched:** `plugin/throughliner/docs/rescan.md`.
**Routed to Captures:** none.
**Depth:** short.
**Tick:** done, confirmed.
Rule gate: run — an amendment to rescan.md's candidate-set offer, its parent; the process-now posture is cited, not duplicated. Nothing evicted.
