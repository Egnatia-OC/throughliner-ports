# [HASH] — /plan: resolved [install-move-breaks-marketplace-path] by adding a project-folder-move recovery note to CLAUDE.md

The capture documented that moving this project folder breaks two absolute-path links that don't self-heal — the local-directory marketplace registration (slash commands go "invalid") and the git worktree pointers (git reports "not a repository") — and it had recurred twice: the C: → G: migration and the move into `Prioritiy projects\`. It asked for a recovery note so a future move doesn't cost another debugging session. Resolved in-session as a CLAUDE.md doc edit rather than a build batch: CLAUDE.md is a host-only project doc outside the plugin package, so there's nothing to build, scope-lock, or rezip. Added a standalone "Recovering from a project-folder move" note just above the Rezip/Push rituals, covering both links (marketplace re-point in place via `marketplace add`; worktree relink of the `.git` pointer and the main repo's back-reference). Placement kept standalone rather than threaded into each ritual because it's recovery, not part of the normal flow. Consumers are unaffected — they install from the GitHub marketplace, which has no local path to break.

**Queue changes:** removed the [install-move-breaks-marketplace-path] capture from the Captures section.

**Work processed:** kept [install-move-breaks-marketplace-path] (resolved in-session via the CLAUDE.md edit).
