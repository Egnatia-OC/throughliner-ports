# [HASH] — /plan: three captures filed (README usage-cycle, self-install via the guided install, in-product rhythm teaching) plus plugin-install research; firing-map-audit hash backfilled; queue assessed push-ready

A planning session with no capture processing — the existing Captures pile was left untouched. Instead it filed three new captures from the user's prep for real-world demos. All three aim to make the repo and the product honestly convey how SI is actually used.

Capture 1, README usage-cycle: the repo front page's "What it does" section should split its run-on intro line and add a best-practice cycle — /setup once, then repeat /plan → /done → /clear and /next → /done → /clear. The four one-line command descriptions don't convey the rhythm. The rhythm is load-bearing: every session closes with /done before /clear, so it's recorded before the context is cleared. The README should also mirror the demos rather than imply the commands can be picked up and used freely.

Capture 2, self-install via the guided install: add a branch to INSTALL.md for visitors who already have Claude Code, where Claude Code installs SI itself via the terminal instead of the GUI zip-upload. It has to be a handoff branch, because the claude.ai-run guide has no terminal. It overlaps the queued experienced-user bypass and app-identification work, and carries a prerequisite — publish a marketplace manifest in the repo.

Capture 3, in-product rhythm teaching: SI itself should surface the same rhythm during use, so a user who skipped the README still learns it. Candidate home is the /setup close-out, coordinating with [setup-closeout-redesign]. Sibling to capture 1.

The self-install idea was grounded by a web search, filed at resources/research/claude-code-plugin-install-mechanisms.md. Findings: the desktop app is Claude Code with an integrated terminal; a non-interactive `claude plugin install <name>@<marketplace>` exists; but the clean path needs a marketplace manifest the repo doesn't have yet, and local-zip install is an unreliable, upstream-pending path.

Two housekeeping items. Backfilled the firing-map-audit entry's [HASH] to c0647b9 (entry file plus index line) at session start. And assessed the push question: nothing in the queue strictly or ideally needs building before pushing. Fourteen commits of completed work — three hooks plus doc changes — already wait to ship, and pushing unblocks the deferred-test backlog.

**Queue changes:**
- Filed three new unprocessed captures to QUEUE.md: README usage-cycle, self-install-via-guided-install, in-product rhythm teaching.
- No batches added, reordered, or modified. No existing captures processed.

**Captures routed:** none (three new captures filed, not processed)
