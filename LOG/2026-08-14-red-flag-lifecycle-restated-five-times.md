# [HASH] — The red-flag clearing lifecycle cut from four sites to two, and the merged section keeps its old heading

The audit found the two clearing paths, and the rule that an unclearable flag returns to the bottom of Unprocessed, stated canonically in `skill-nonspecific-rules.md` and then four more times: twice in `plan.md` about 780 lines apart, and twice in `done.md` in adjacent sections. Five copies of one rule is the worst ratio it found, and the eviction half of this project's own rule gate is exactly what should have prevented it.

**Two cuts, not four.** `plan.md`'s early ground-rules bullet stated the two clearing paths, the LOG-records-which clause and the returns-to-Unprocessed clause — and its keep-step restates all three far below, at the moment a session acts on them. Two copies in one doc with the operative one last: the early copy went. `done.md`'s two adjacent red-flag sections each opened by re-establishing that clearing happened at processing; merged, they lose only that doubled opening.

**Everything site-specific survived, and it was enumerated before the merge**: that the how-it-cleared record goes into the LOG entry, that recording is unconditional, that the marker carries only the state, the carry-through at a build or audit close, and the backstop that stops a close meeting an uncleared marker.

**The merged section kept the old heading, and that was a build-time decision.** It was nearly renamed "Red flags at close" — the natural name for a merged section — and the scope-lock refused the edit that would have followed, because `done-build.md` points at the section by name and was not in this item's file list. Keeping "Red-flag lifecycle at close" leaves that pointer resolving and avoids pulling a sixth file into scope for a cosmetic gain. The scope-lock caught this rather than judgment did.

**Deliberately untouched**, so a later pass does not finish a job that is done: `plan.md`'s keep-step is the operative site and keeps the marker's literal syntax; `next.md`'s uncleared-flag halt is the execution site for the backstop rather than a restatement of it; `setup.md`'s scaffold text and `migrate-checklist.md`'s mapping rows are user-facing and structural.

**A result that runs against the audit's own expectation.** It predicted heavy duplication in the sub-docs it was never shown. On this rule there is none: `done-build.md`, `done-plan.md` and `done-audit.md` all point at `done.md` rather than restating it, and `done-audit.md`'s "an audit doesn't clear red flags" is new content with no canonical source.

**Files touched:** `plugin/throughliner/docs-b/plan.md`, `plugin/throughliner/docs-b/done.md`
**Routed to Captures:** none

Rule gate: not needed — no rule authored or amended. Two restatements evicted, the canonical statement untouched: an eviction pass with no admission.
