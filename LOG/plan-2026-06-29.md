# [HASH] — /plan: resolved the [plan-dependency-tracing-gap] design session into 2 build batches — [dependency-tracing-pass] (plan-time dependency tracing) + [readiness-line] (the "cleared to run" queue boundary, unblocked); restructured Batches (cruise-control → bottom behind a fresh marker, deployment-prereq re-pointed); parked [cruise-unattended-hook-signal] + [goal-session-ran-plugin-on]; filed research/dependency-tracing-at-plan-time.md

The queue's plan-marker forced the [plan-dependency-tracing-gap] design session: /plan orders batches from assumed dependencies, never traced against real code/SPEC, so under heavy coding the graph breaks (the five modes from the Taskflow E2E LOG). The session designed the fix and spawned its build batches. Direction chosen: the bounded "middle" — a completeness check plus a bounded trace — over a lighter completeness-only fix or a heavier whole-codebase graph trace. Prior-art research (filed) confirmed middle and sharpened it: serious build systems and AI planners validate the dependency graph at planning time (the reframe, independently arrived at elsewhere), and even Bazel states static checking can't be complete — so the fix reduces the failures and keeps /next as the backstop, with modes 4 (file-list ripple) and 5 (on-device reversals) named accepted residual rather than solved in /plan. The architecture splits into one mechanical completeness check (every dependency has a producer — extends today's ordering-only check, via lint + close-walk) and three procedural authoring obligations (trace against the named files + SPEC, trace-evidence in the rationale, shared-primitive scan). This also handed [readiness-line] the "ready" definition it was blocked on — traced + complete + ordered + not exposed to an unprocessed capture — so it came off the Parked shelf as Batch B, depending on the tracing batch. Alternative weighed and rejected: splitting the tracing batch into mechanical vs procedural halves — kept whole as one coherent feature.

**Queue changes:**
- Added build batch **[dependency-tracing-pass]** at the top of Batches (the tracing fix: plugin-behaviour.md rules, plan.md authoring + close-walk, post_tool_use.py missing-producer flag, FAQ).
- Added build batch **[readiness-line]** (Depends on [dependency-tracing-pass]) — promoted from Parked; the user-facing "cleared to run above this line" boundary (SPEC, plan.md/done-plan.md close, next.md, FAQ).
- Retired the [plan-dependency-tracing-gap] design block and the plan-marker that forced this session.
- Moved **[cruise-control]** design block to the bottom of Batches behind a fresh plan-marker (still not buildable; keeps the buildable batches reachable); re-pointed its deployment prerequisite from [plan-dependency-tracing-gap] → [dependency-tracing-pass]; recorded the user's "/next offers to continue" idea under concern 11.
- Filed research: resources/research/dependency-tracing-at-plan-time.md.

**Captures routed:** Parked **[cruise-unattended-hook-signal]** (Blocked by [cruise-control]'s skill-vs-mode invocation decision — the user caught that the hook-signal can't be designed until the invocation form is settled). Parked **[goal-session-ran-plugin-on]** (new capture: a goal session ran plugin-on to completion unimpeded — designed behaviour, and it corroborates that only the asking/blocking hooks need the unattended signal).
