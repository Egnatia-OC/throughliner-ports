# b4de5bf — The close sub-docs' shared routing justification deleted, and their identical record block hoisted into done.md

`done-build.md` and `done-audit.md` opened their route-to-Unprocessed steps with a word-for-word identical paragraph, and both carried a byte-identical fenced block naming the record the step sweeps. Duplication twice over: the two sub-docs restated each other, and the justification was separately canonical in the always-loaded write-first rule, which carries the recoverability test the copies asserted without stating.

A correction to the item's own account, found by reading both sites rather than trusting the filing: it claimed `done-build.md` carries site-specific content. It does not. That content sits inside the shared block both files carry. `done-build.md` had no unique sentence at all in this step. Only `done-audit.md` has one — that the findings were appended during the audit, so this step sweeps what else was flagged along the way.

The settled shape took a third option over the two the item offered, because the shared material is two different things going two different ways. The justification paragraph is deleted from both and nothing replaces it: the always-loaded rule already carries it. The fenced record block is genuinely operative and byte-identical, so it is a shared definition rather than a restatement, and it is hoisted into `done.md` once. `done-audit.md`'s own paragraph stays where it is. This removes more text than cutting to pointers and adds less indirection than sending both sub-docs to a third step for the whole thing.

One ambiguity was resolved rather than hoisted. `done-audit.md`'s copy listed "the build working file's notes" inside an *audit* close, which is either copy-paste residue or correct. It is correct: an audit run creates a build working file like any other run and ticks its findings there. The hoisted block says so explicitly, so it cannot later read as wrong for one of its two callers.

Rule gate: run — one restatement of the always-loaded write-first rule evicted from two shipped docs, and one duplicated definition consolidated to a single copy. No rule authored or amended, and nothing moves to always-loaded.

FAQ: not needed because this reorganises procedure text Claude reads; the numbered report the user receives is unchanged.

**Files touched:** `plugin/throughliner/docs-b/done-build.md`, `plugin/throughliner/docs-b/done-audit.md`, `plugin/throughliner/docs-b/done.md`.

**Routed to Captures:** none.
