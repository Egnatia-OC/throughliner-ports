# [HASH] — blitz-plan learnings recorded (soak-before-merge, audits cleared, five more) and the closing-advice misfire captured

The post-blitz review session. The user read the overnight run back and added two corrections to the reusable blitz plan at `resources/overnight-blitz-plan.md`: `[audit]` items are explicitly cleared under the blitz's softened bar (both audits run that night were plainly right to run), and the branch's purpose is a **soak** — run on the test build for a day or two before merging or reverting — so the handoff must recommend soak-first, with immediate merge named as the gamble it is; the first run's advisory pushed straight to merge, and the user's reason for the correction is that yolo-merging shouldn't become general practice even though they chose it this time. Five more learnings from reading the session went in alongside: page the queue to completion before triaging, the capture target is a ceiling of honesty not a quota, run the hook schema check only after committing (it triggers the real hash backfill), reach for the extended queue mover before scripting, and name standing lint noise once at the start. On the user's follow-up, Phase 5 also now states the close explicitly: each run ends with its own /done, the LOG entries and commit land at that close, and the night's final close is a /done too — nothing waits for morning to be logged.

The session also caught a live misbehaviour: Claude advised running /done in a fresh chat, the user challenged it, and the fresh-chat-first closing advice turns out to pattern-match onto /done itself, which always closes the current session. Filed as [closing-advice-misfires-onto-done].

Deliberately not touched at this close: a planning session is open in parallel (its working file present, its dispositions empty), so its state — the spent branch advisory, the proposed lifts from its shelf revisit, the file itself — is left entirely for that session's own close. This close covers only this session's edits, plus the blitz commits' mechanical hash backfills.

**Queue changes:** one capture appended ([closing-advice-misfires-onto-done]); nothing processed, reordered, or lifted — all deferred to the open planning session
**Work processed:** none
FAQ: not needed because the session changed a host-only dev artifact and filed a capture — the closing-advice fix itself will carry its own disposition when built.
