# [HASH] — The blitz branch merge closes, verified against git rather than accepted as done

`overnight-blitz-2026-08-06` carried the blitz plus the planning sessions that followed it — twenty-plus processed work items, deletions, a preserved session transcript, and the editing-state contract another project was waiting on. The merge itself happened at the soak-end sequence recorded in CLAUDE.md; this closes the item that tracked it.

**Closed on an observable check, not a report.** `git branch --merged main` lists the branch, so the merge is confirmed from the world rather than from anyone's recollection. That is the `[user]` lifecycle's own rule about checking rather than asking, applied to a build item: where a completion has an observable result, the close runs the check. It cost one command.

**Why this item existed at all is the part worth keeping.** The merge was originally tracked *only* inside a forward-recommendation advisory — and advisories are transient by design, cleared at the next close. So the one action several other items were waiting on lived in a note built to be thrown away, while the queue referred to a slug from the *previous* branch's merge item that no longer existed. That is the under-filing shape the method already names, and it took a planning session noticing the dangling reference to catch it.

The item also settled a tagging question cleanly: merging is Claude-work, because `git checkout main && git merge` is a command Claude runs and git is present and working. What was the user's was the **greenlight** — a decision, not an action — which is why it sat below the readiness line rather than carrying a `[user]` tag. "Claude can't decide this" is not "Claude can't do this", and that distinction is exactly what the over-tag guard exists for.

Its greenlight arrived at the planning session of 2026-08-08, in the same breath as docset A's — and the difficulty of *getting* that greenlight across several sessions is what produced [greenlight-step-does-not-exist], closed in this same commit.

**Files touched:** none — a git operation plus a host reinstall.

**FAQ:** not needed because nothing user-facing changed; this is a repository operation in the self-hosting project, invisible to consumers.

**Routed to Captures:** none.
