# [HASH] — /plan: promoted [spec-edit-batch-type]; narration-bloat thread spawned an openings audit, FAQ backfill, and a scaffolding-sync capture

Two threads. First, the processed capture [spec-edit-batch-type] was promoted to a build batch: SPEC stops being special — the pre_tool_use read-only lock comes off and scope-lock becomes its sole protector — and spec changes become their own spec-edit build batch type. It sits after the scope-distinction arc with Depends on [scope-anchor]. The one SPEC.md edit the work implies, rewording SPEC's hooks description, was deliberately deferred out of the batch: the build runs under the current host where the read-only lock is still live, so it cannot edit SPEC. That edit becomes the first spec-edit batch after reinstall.

Second, the user flagged narration piling up at the start of skill sessions. The hash-backfill hook was suspected and cleared — its report is conditional (silent unless it fills a placeholder) and its mechanism is sound, so it is not the cause; the real opening bloat is the skills' own opening prose, driven by rules that each say "narrate" with nothing bounding the total. The user asked to expand the audit to all docs, but a prior full tag audit ([output-tag-audit], 0405315) already did that and most of its findings are still queued unbuilt, so a blanket re-check now would re-find queued work. Resolved by sequencing: a narrow new-lens audit now, a full placement re-check parked until the tag fixes land.

A side discovery: this project never re-scaffolds itself. The FAQ folder (added to /setup at 06c24e4) and .si-version are both missing, and with no .si-version the plugin can't even detect the drift. The user had assumed it was being updated all along; it never was — editing the target updates the host, not this project's own scaffolding. A one-off FAQ backfill was queued (a snapshot, accepted as drift-prone), and the deeper sync question captured for design, including the wrinkle that a static .si-version would nag "re-run /setup" after every version bump.

**Queue changes:**
- Promoted [spec-edit-batch-type] from processed capture to build batch, after the scope-distinction arc (Depends on [scope-anchor]); its SPEC.md edit deferred to a later spec-edit batch.
- Added audit batch [opening-narration-audit] (Depends on [plan-step1-sequencing]).
- Added build batch [faq-backfill] at the end of Batches.
- Filed capture [scaffolding-resync] above the divider.
- Parked [full-tag-placement-recheck], Blocked by the queued tag-fix batches shipping.

**Captures routed:** [spec-edit-batch-type] promoted. [scaffolding-resync] filed (new). The eight unprocessed captures and [retire-registry] were not reached.
