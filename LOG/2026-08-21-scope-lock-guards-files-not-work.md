# 35261b5 — A consumer's unguarded-send report cut to two clauses, with half of it already shipped and its central claim overturned

A consumer project reported that its planning session drafted a substantive letter to an external professional which the user then sent, with nothing gating it, while the scope-lock twice refused a four-line correction to that project's own CLAUDE.md she had already approved. Processing overturned two parts of that account and left a much smaller build than the item implied. Both fallen parts are kept in the item, because the reasoning that fell is what stops the same conclusions being reached again.

**"Nothing gated it" does not survive the detail.** Their user read the letter and sent it herself, so the never-send-unseen rule was satisfied — Claude drafted, she approved by sending. What was missing was a record, not consent.

**The record half shipped the day after the item was filed.** [send-record-lacks-destination-and-intent] landed 2026-08-20, and `feedback-and-inbox.md` now requires a line in `INBOX/sent.md` for *"Mail, a method report, a GitHub issue, a public post, a draft handed to another project — anything that leaves this machine."* A letter to an external professional is squarely inside that wording, so any project on the current version now leaves a trace of exactly the act the item says left none.

**And no hook could ever have reached it, for a sharper reason than the item gave.** It said a hook cannot see an email. The real reason is that **the send was never Claude's action**: no tool call carried it, so there is nothing to intercept by construction rather than by omission. That matters because it rules out the whole class of fix rather than one option within it.

**What remains is the honest-limit statement, which is the route the item already leaned toward.** Nothing anywhere says the scope-lock covers files and nothing else. Their user's framing — Throughliner governs work wherever it is conducted, not only work touching this machine, with a `[user]` item about unpicking a physical dress as her example — is true of the *method* and false of the *mechanism*, and that mismatch is what lets a file-lock read as general coverage.

**The vocabulary half is kept and deliberately stays small.** That session's Claude reached for `[freeform]` as though it were a mode a planning session could slip into; it is a work-item flavour, and their user corrected it. The fix is not to name the unnamed thing: the always-loaded rules warn that invented states, tags and session containers are a recurring failure here and that Alex has caught each one, and live unqueued hand-work in a chat is the ordinary case the method already describes without a term. One clause saying flavour-not-mode is the whole of it.

**Both clauses land in the always-loaded file and each spends a slot**, recorded plainly rather than waved past. The first must fire wherever a session reasons about what the lock protects and the second wherever a flavour is reached for, so neither can be fetched.

**One thing this session's own work bears on and does not resolve.** Alex separately proposed that /plan, not the build, should write CLAUDE.md — see the chat record and [build-view-strips-the-gate-disposition]. If that lands, half this item's original grievance about the lock refusing an approved four-line correction dissolves. The two clauses kept here are unaffected either way.

**Queue changes:** [scope-lock-guards-files-not-work] rewritten with a settlement section and moved Unprocessed → Processed, placed last among the build items and immediately before the two `[user]` posts, honouring the end-preferred rule.
**Work processed:** kept — [scope-lock-guards-files-not-work].

Rule gate: run — the disposition was written onto the item at the keep-step. Two clauses admitted on existing always-loaded statements, subordinate rather than freestanding, each spending a slot and each said to. Nothing evicted, three refusals recorded including a mechanical guard on outbound work. Failure evidence is one consumer instance for each half, both from a single reported session.

FAQ: not needed because nothing a user does changes — both clauses correct what Claude may conclude about a mechanism, not any step the user takes.
