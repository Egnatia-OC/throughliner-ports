# [HASH] — [lint-citation-diff-scope] post_tool_use citation advisory diff-scoped to the edit's changed lines

The post_tool_use citation advisory ("is this prose slug-reference a citation or a missing Depends-on header?") re-fired for every unchanged citation on every QUEUE.md edit — a dozen-plus correct-but-noisy "it's a citation, leave it" flags across recent /plan sessions — because the lint was stateless per-edit and re-judged citations it had already cleared. Pure noise, and repeat-flagging trains the rubber-stamping the method's anti-nag stamps guard against elsewhere. Diff-scoped it: post_tool_use.py now derives the current edit's new text (a _changed_text helper reading Edit new_string / Write content / MultiEdit edits), passes it into lint() as changed_text, and the prose-citation check (check 6) fires only for a citation line present in that changed text — so a citation is judged once when written and never re-flagged while it sits unchanged. "Citation vs dependency" is a static property of the line, so an edit elsewhere can't change it; the dangling-ref, dependency-ordering, and subheading checks keep scanning the whole file. changed_text=None falls back to a whole-file prose scan (back-compat and the test default). Run-now fixture passed in-session: fires on a newly-introduced citation; stays silent when an edit elsewhere leaves a pre-existing citation untouched; a genuinely dangling reference still flags; whole-file fallback intact; real QUEUE.md lints clean and py_compile passes. The live-host behaviour is deferred host-side — and the old whole-file host re-flagged a pre-existing citation during this very goal run, confirming the fix isn't live until reinstall.

**Files touched:**
- plugin/si-plugin/hooks/post_tool_use.py

**Routed to Captures:** none
