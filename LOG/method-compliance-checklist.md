# [HASH] — [method-compliance-checklist] new resources/method-compliance-audit-checklist.md (3 lenses, corpus-wide) + CLAUDE.md pointer

The 4.8 authoring heuristic is a per-text check run once at authoring time; nothing applied it as a periodic sweep of already-shipped docs, which is exactly why the un-hardened tool-use rule slipped past for so long (the subagent-cost incident). Authored resources/method-compliance-audit-checklist.md as the standing criteria for a routine, corpus-wide compliance audit of the method's procedure docs: three lenses — (1) 4.8 authoring-compliance by reference to the seven points in the 4.8 section of authoring-heuristic.md (not duplicated, so the two never drift), plus the corpus-wide consistency question the per-text check can't ask; (2) response-shape tag placement (missing / wrong-for-the-step / prose where a tag belongs); (3) narration drift (background vocabulary in user-facing narration, a menu where a recommendation was due, multi-finding openings that should consolidate). Host-only dev artifact — no FAQ, no SPEC change, not shipped in the plugin package, the same status as the heuristic it builds on. Added a CLAUDE.md Method docs pointer so a future /plan scoping a compliance audit reuses it rather than re-deriving the criteria. No test — the deliverable is a checklist the user reads, and the downstream audit validated it by use this same run.

**Files touched:**
- resources/method-compliance-audit-checklist.md (new)
- CLAUDE.md

**Routed to Captures:** none
