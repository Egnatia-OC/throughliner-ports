# 96166c6 — Named "one /plan per batch" as the sanctioned answer for recurring intake, beside the rule that forbids the shortcut

Split from the check-the-documentation item at its processing: that one is the rule half, and this is the **pressure** that produced the invention in the first place.

The shape: a batch of similar things arrives regularly and each needs the same treatment — receipts, evidence files, form submissions, inbound documents, scanned recipes. Every batch needs a fresh /plan to queue an item saying "do the same thing again", and **nothing in the method named that as the sanctioned answer.**

The method's own recorded pattern is that a real pressure with no stated legitimate path reliably produces an invented one — which is exactly what "have /next pick up its own work" was. So the invention was plausibly a symptom rather than carelessness, and the rule half alone does not remove the pressure.

**Narrowed hard at processing: build the SENTENCE, not a pattern.** The item's own text half-dismisses its answer — *"every batch needs a fresh /plan may simply be the correct answer stated out loud"* — and that *is* the answer. It is buildable today and needs no frequency evidence at all, because it invents nothing: it states what the method already requires.

**Why the narrow version is the whole win.** The queue-only rule is a prohibition sitting exactly where real pressure builds — a batch has arrived, it plainly needs doing, and the obvious shortcut is forbidden. Naming the sanctioned path in the same breath as the ban is the cure.

One addition that makes the repetition cheap rather than tedious: a work item may be **written to be re-added** rather than re-derived each time. So once the handling is designed, next month's batch is the same item again — a copy, not a fresh design conversation.

**Designing an actual intake mechanism is explicitly NOT kept, and the reason is recorded so it is not re-proposed as an oversight.** The evidence is one observed instance in one private project. Building method surface on that is inventing for a need nobody has confirmed — the same failure in the opposite direction. If recurring intake is genuinely common it will re-capture itself with better evidence, and this sentence will already have removed the pressure that produces bad workarounds meanwhile.

The FAQ entry earns its place: a user with recurring work asks this directly, and a plain answer is what stops them inventing a workaround.

**Files touched:** `plugin/si-plugin/docs-b/plugin-behaviour.md`, `plugin/si-plugin/templates/faq-template.md`, `plugin/si-plugin/templates/faq-index-template.md`

**Routed to Captures:** none
