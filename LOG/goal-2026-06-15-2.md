# [HASH] — Autonomous /goal session: INSTALL.md guide rewrite plus six behaviour/plan/next/done doc batches; stale already-built batch cleared

Third autonomous /goal session, plugin off, manual close. Worked QUEUE.md's Batches top to bottom. Started where the previous goal session deliberately stopped — the INSTALL.md cluster — cleared it, then continued into the behaviour-doc batches below it. Fourteen genuinely-new batch-units landed (eight in the INSTALL cluster, six doc batches), plus one queue-hygiene catch. No push, no reinstall. Everything host-side was recorded as a deferred test; everything else is self-verifying from the doc text.

**Queue-hygiene catch — [user-edits-rollup-on-commit] was already built.** The top batch was already implemented: done.md's commit core carried the dirty-path rollup sub-step, and the previous goal session's LOG entry (215f96e) lists it among the work it shipped. That session recorded removing 14 batches but only 13 actually left the queue — this one survived. Removed the stale batch and filed a capture about the goal-session removal miss (no mechanical check confirms each shipped slug left the queue at a hand-run close).

**INSTALL.md cluster — implemented as one coherent rewrite (eight batches).** INSTALL.md is a repo-root, consumer-facing file pasted into a claude.ai chat; it's edited directly and has no host-side dependency, so the whole interdependent cluster was built toward its union end-state in one pass.
- **[install-app-identification-check]** — Q2 is now a forced app-identification step (the user reports title-bar text / interface) before routing; wrong-app recovery routes chat-app users to Branch A; the "check Applications folder" hint is gone.
- **[install-separate-ai-instructions]** — the Note-to-Claude block and pacing rules moved to a clearly-marked "Instructions for Claude (you can skip this section)" at the end; a human-facing intro (what / who / what to expect) opens the file; an experienced-user bypass with in-page anchors skips the interview but never the app check; a one-line top signpost points Claude to the back section so the verification concern (Claude must still find its instructions) is covered.
- **[install-paid-plan-upfront]** — a "Before you start — you'll need a paid Claude plan" section sits before Q1 (Pro is enough, not Max; pricing-page pointer, no dollar figure; plan-limit explained as a pause-until-reset, not a surprise charge); Q3's paid-plan discovery is folded out; Step 1 routing no longer carries the stale "no paid plan" line.
- **[install-download-provenance]** — a provenance line (FlintCraftTech = publisher, Sovereign Implementer = plugin, official source) and a click expectation (auto-downloads as si-plugin.zip, or right-click Save As) sit at the download step.
- **[install-define-open-folder]** — the smoke test and Step 2 define "open a folder" concretely (make an empty folder, File > Open Folder), framed as a smoke-test action, with a note that /setup handles real project setup.
- **[install-setup-smoke-test-underspecified]** — the smoke test names where to type (the chat box), the command menu, the success signal (the entry appears as `/setup` or namespaced `/sovereign-implementer:setup` — written both ways from the queue-documented namespaced rendering rather than blocking on a user confirmation), Enter to run, the failure signal (no matching entry), and a diagnostic ladder (check enabled → fresh session → reinstall last) replacing the jump-to-uninstall.
- **[install-updating-later-section-is-padding]** — "Updating later" collapsed to one line; an end-of-guide close line added just before the AI section.
- **[install-upload-path-clarity]** — PARTIAL: the prose landed (confirmed path, hedge dropped, misleading-label heads-up), but the screenshot bullet is user-only (it needs a real desktop-app capture) and a goal session can't produce it. The batch was trimmed in QUEUE.md to that one remaining bullet, with a placeholder pointer left in INSTALL.md.

**Behaviour / plan / next / done doc batches (six).**
- **[log-rejected-alternative-reasoning]** — plugin-behaviour.md why-pipeline Preserve now defines rationale to include a concern raised and resolved and an alternative seriously weighed, each carried with why it lost; the intuitive-but-rejected path always qualifies; a trigger boundary keeps entries from bloating. The four /done sub-docs gained a one-line check at the entry-writing step pointing back at the rule.
- **[research-filing-default]** — plugin-behaviour.md Research gained the canonical Filing rule (file non-trivial findings under resources/research/<topic>.md as part of using them, name the file in chat); the duplicate filing clause was removed from the Communication web-search bullet.
- **[relationships-must-be-written]** — plugin-behaviour.md Dependency ownership now states that a relationship between queue items exists only if written (header or slug-in-prose); queue position never encodes one.
- **[blocked-by-trigger-flavors]** — the Blocked by / Parked bullet now names the trigger flavors: a bare slug fires on landing (the default); any other trigger (findings-generated, clarity-at-decision, external event) must be written in the prose tail.
- **[parked-slot-discipline]** — filing as `Parked:` now requires first affirming no nameable trigger exists (a slug-less behavioural trigger is still a valid `Blocked by:` tail); plan.md Step 1 now includes `Parked:` items in the staleness watch instead of skipping them.
- **[capture-verbatim-first]** — plan.md Step 2's present-and-interview opens with the preamble + verbatim item sent before any analysis, with the post-quote QUEUE.md re-read named as the separator; next.md's pre-flight was restructured so the top batch is sent verbatim (new sub-step 3) before the blocker gate (sub-step 4), with the "Ready?" go-ahead as sub-step 5, dropping the old "don't re-render full entry text" guidance. Host-side test deferred.

**Deferred tests recorded (host-side, after push + reinstall):** [capture-verbatim-first] — the verbatim-first send in /plan's capture turn and /next's pre-flight.

**Hash backfill:** the previous goal session's entry (LOG/goal-2026-06-15.md) carried an unfilled placeholder in its heading and index line — backfilled to 215f96e (the commit that introduced the file). No other unfilled hash-position placeholders exist; the remaining `[HASH]` tokens in LOG/ are prose mentions of past backfills.

**Files touched:**
- INSTALL.md — full restructure (eight-batch union)
- plugin/si-plugin/docs/plugin-behaviour.md — why-pipeline Preserve, Research Filing, Communication de-dupe, Stable-slugs, Blocked-by trigger flavors, Parked discipline
- plugin/si-plugin/docs/plan.md — Step 1 Parked staleness inclusion; Step 2 verbatim-first present-and-interview
- plugin/si-plugin/docs/next.md — pre-flight restructured for verbatim-first send before the blocker gate
- plugin/si-plugin/docs/done-build.md, done-test.md, done-audit.md, done-plan.md — rejected-alternative check line
- QUEUE.md — batches removed/trimmed, one capture filed, one deferred test added
- LOG/goal-2026-06-15.md + LOG/index.md — previous goal entry hash backfilled to 215f96e

**Routed to Captures:** one — the goal-session queue-removal miss ([user-edits-rollup-on-commit] survived in the queue despite being built and recorded as removed).
