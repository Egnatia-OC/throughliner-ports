# 5993a10 — The Claude-runnable premise claims verified: the mover's contract confirmed whole, the post-rename install path settled, and one unanticipated route found

An audit: reads and reports, changes nothing. A failed claim becomes a fresh capture naming the rules that rest on it, because a claim failing may invalidate a rule rather than merely dating it.

**reorder_queue.py's stated contract — CONFIRMED on every remaining limb.** Byte-exact relocation: a moved block's heading and rationale are character-identical after the move. Refuses-rather-than-guesses: an unresolvable slug exits 1 naming the slug and the section. Non-zero-means-nothing-written: after that refusal the file was byte-identical to its backup. Combined with the marker cases now covered by the extended suite (24 checks, all green), the contract claimed at five doc sites is **wholly** rather than partly confirmed — closing the gap that kept this claim partial.

**The post-rename marketplace install path — CONFIRMED, and the open worry dissolves rather than being answered.** `FlintcraftTech/throughliner` resolves, is public, and serves `.claude-plugin/marketplace.json`, so INSTALL.md's command is correct as written. The recorded unknown was what the old-path redirect now does, given the old username belongs to an organisation. It does not arise: `FlintcraftTech` is now an **Organization** and the user is `its-coughfee` (a User, id 283077209). Because the org took the name the repo path already used, **the documented path string never changed**, so there is no redirect for anyone to depend on. `its-coughfee/throughliner` 404s, correctly — the repo was never there. CLAUDE.md's "nobody has verified what that redirect does" can be closed: there is nothing to verify. Derived while there, for [history-rewrite-third-party-scrub]: the noreply address to set before that rewrite is `283077209+its-coughfee@users.noreply.github.com`.

**migrate-checklist.md's validation stamp — STALE, and deliberately no capture.** It still cites host 1.15.0-test6 against a current 1.19.0-test1. But the claim is historical and true — that run did pass on that host — rather than a claim about today. It is **dated, not false**. Re-validating needs a real old-format consumer project, which nobody has to hand. Recorded so a later reader knows it was checked rather than overlooked.

**The MSIX/AF_UNIX sandbox claim — NOT CHECKED, and this is the honest gap.** Its condition was "if a test project is reachable", and no Android/Gradle project is. It stays unverified and dated; if it has been fixed upstream, the FAQ's advice inverts. Recorded as unrun rather than quietly passed.

**The hook-event injection question — both facts established, and one of them opens a route nobody was looking for.** `UserPromptSubmit` and `PostToolUse` still support `hookSpecificOutput.additionalContext`; `PreCompact`, `PostCompact` and `SessionEnd` still do not — so [compaction-reinjection-has-no-usable-event]'s premise **holds**. `transcript_path` is in the payload, but nothing documented lets a mid-session hook detect a compaction from it, and the docs warn the transcript lags the live conversation, so inferring it would race a file that is behind by design. **The unanticipated finding:** `PostCompact` cannot inject but it does *fire*, and side-effect-only events can write to disk — so the missing trigger may exist as a two-hook relay. Filed as [compaction-reinjection-via-postcompact-flag] rather than acted on, with its three unknowns and its honest counter recorded.

**Section G's remaining ~40 claims: NOT worked.** This run covered the four named priorities plus the injection question. Said plainly so this is not read as the inventory having been cleared.

**Files touched:** none — an audit edits nothing. The full fifty-claim inventory stays in `LOG/2026-08-07-pre-compression-doc-audit.md` section G.
**Routed to Captures:** [compaction-reinjection-via-postcompact-flag]
**Approval outcomes:** all findings recorded as-is; nothing dropped or reworded.
