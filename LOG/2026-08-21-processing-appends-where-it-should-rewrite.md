# 746608f — the keep-step's rewrite instruction was already there and marked advisory, so the fix is an eviction rather than a new rule

The user raised this by asking whether a decision had landed: after the ideation loop was made to hold its write until complete, she had asked that all planning work the same way, with the item under discussion rewritten in full at the end of each loop, because it appeared to yield denser writing. It half landed. The ideation loop shipped on the 17th and folding shipped on the 20th as two typed operations, but the general rule was never built.

**What the processing found, and it changes the fix from an addition to a repeal.** `plan.md` already carries *"Read the ITEM AS IT STANDS, not the paragraph being added"* — the right action, at the right site — and its own text marks it advisory: it "names an action and never blocks the keep". So the step names what to do and requires nothing. That is why four re-processed items in one session produced four appends, and the single rewrite happened only because splitting an item triggered the merge branch.

**The trigger is narrow, and not as a compromise.** The rewrite fires where an entry already carries a dated settlement or skip paragraph from an earlier session — the layered case, and the only case where there is anything to rewrite. On first processing a capture is authored into a work item, which is a rewrite already, so a general rule would spend output on every item to change nothing on most of them. The trigger reads a literal in the entry's own text.

**Two bounds go inside the operative sentence rather than beside it**, because a rewrite that drops them is worse than an append: a defeated alternative and the reason it lost survive, and a paraphrase is never upgraded into a quotation claim. Both are recorded failures here, the second found live in this queue.

**A measurement already fires at the right moment and nothing reads it**, noticed by watching it fire repeatedly through this session: after every queue edit the hooks report each item's word growth as bare facts. Pointing the rule at it was refused — it states no threshold by design, and a rule aimed at it turns a fact into a target, which is what the word bands were retired over.

**The user's addition at processing was already in the queue, and was not duplicated.** She observed that offering immediate processing makes the write unnecessary. [ask-before-writing-a-user-raised-capture], cleared to run, already states exactly that. One site is genuinely uncovered — a capture derived from INBOX mail at a /plan opening is written unconditionally with no such offer — and that was folded into [inbound-replies-not-drafted], which owns the mail step.

**This item was appended to rather than rewritten, and the exception is stated in the item itself** so it does not read as the new rule being broken on arrival: it was filed the same day and carries one layer, so there were not two accounts of one thing to merge.

**One cost recorded rather than glossed.** Settling this grew the item by 934 words, in the session where the user reported the queue getting longer. The growth is genuine decision history and it is also the pattern the item describes.

**Queue changes:** [processing-appends-where-it-should-rewrite] processed from Unprocessed into Processed, fourth in the cleared region behind the brevity amendment, the two being the chat half and the document half of one lever.

**Work processed:** kept — [processing-appends-where-it-should-rewrite].

**Routed to Captures:** none; the uncovered mail site was folded into an existing item rather than filed separately.

Rule gate: run — admitted as an amendment converting an existing advisory statement at the keep-step into an operative one, subordinate to a step that already exists, sited in a fetched procedure doc so no always-loaded slot is spent. **The eviction is that statement's advisory framing** — "names an action and never blocks the keep" — repealed, with the action specified. No figure is introduced. Failure evidence is four re-processed items in one session producing four appends where the single rewrite fired only because a merge triggered it, plus a measured 15% queue growth across one session with the item count unchanged. A hook was considered and refused: the growth report it would rest on states no threshold by design.

FAQ: not needed because a consumer's queue items get denser and they do nothing different.
