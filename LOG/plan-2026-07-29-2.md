# [HASH] — Filed 3 method-design captures + rezipped to test6; verified the consolidated /done close live

This session opened to test the consolidated `/done` close shipped earlier today as [plan-step3-shadow-close]. After rezipping the plugin to 1.15.0-test6 and reinstalling to dogfood it, the testing surfaced three method-design threads, each filed as a capture rather than processed:
- **[migration-recipe-hexboard-pass]** — the ad-hoc old→two-section QUEUE.md migration recipe drafted this session was run in the Hexboard consumer project and PASSED (method boilerplate re-copied not regenerated, old-section mapping correct, structure lint clean). A real-project data point for [migration-checklist].
- **[faq-sync-gap]** — the shipped FAQ isn't methodically kept in sync as the method evolves; its two existing triggers (the batch-authoring rule, the push-time sweep) aren't reliably firing. Recorded as gating the migration work.
- **[reorder-cost-vs-dependency-model]** — the user questioned whether /plan's reorder behaviour carries too much per-session token load versus the deliberately-retired explicit-dependency apparatus (Depends-on/Blocks headers + dep-ordering lint + dep-graph check + tracing pass, all removed by the two-section recut). Framed for a /plan pass as "is the reorder too heavy, what's the lighter fix" — explicitly not a snap-reversal to blocking.

Verification: closing this planning session exercised the consolidated done-plan.md close live — the reorder step ran (lifting [faq-sync-gap] above the migration items via reorder_queue.py) and the marker-positioning step walked Processed (placement unchanged). Both fired at the `/done` close, confirming [plan-step3-shadow-close] behaves as designed on test6.

**Queue changes:**
- Filed [migration-recipe-hexboard-pass], [faq-sync-gap], [reorder-cost-vs-dependency-model] to Unprocessed.
- Reordered Unprocessed by unlock-potential (faq-sync-gap lifted above migrate-skill).
- Rezipped to 1.15.0-test6 + reinstalled for local testing (plugin.json test-suffix bump left uncommitted per the rezip convention).

**Work processed:** none — all three filed as captures.
