# [HASH] — /plan: processed the twelve docset-B fidelity findings into three work items, consolidating ten repairs into one, and recorded that the register decision rests on weaker grounds than logged

An advisory-directed session: the forward note asked for the twelve docset-B fidelity findings to be processed before the next rezip makes docset B govern live sessions, and all twelve were. Nine items were kept in total and none deleted.

**The shape changed mid-session.** Three findings in, the user asked whether consolidating small findings into one work item would spare tokens and calls. It does — twelve drafts, approvals and queue writes collapse to one, and /next scope-locks per work item, so twelve scope-locks and twelve re-reads of the same files collapse too. Not a saving in subagent calls, which /next doesn't spawn per item. So the ten unambiguous repairs became one item with ten numbered points, while the two carrying design calls stayed separate. Each was still discussed and decided one at a time; only the destination was shared. The counterweight was named and recorded: a consolidated item can't be reordered or dropped point-by-point, and one contentious point drags the rest.

**Processing did real work the audit hadn't.** Four of the twelve were re-scoped against the actual docs — two narrowed, two widened. The scope-clause finding named one exception; checking found seven. The prerequisite-derivation finding claimed the adjacent rule kept its wording; grepping both docsets showed neither rule has it, so the gap was wider than filed. The SPEC-routing finding said two cases had no routing; one was already fully handled in next-build.md, leaving only a missing signpost. The audit pointed in the right direction consistently and was imprecise about reach just as consistently — carried forward for the next fidelity pass.

**A cross-cutting pattern emerged that no single finding named.** Docset B's subtraction shed the *carve-outs and bounds* from the communication rules while keeping the rules themselves — [SEQUENCE]'s reach, consolidate-the-scans' narrowing, the verbatim-copy exception, the file-structure trigger. The effect is a docset more rigidly one-at-a-time, and more prone to narrate where the method says stay quiet, than intended. That is live: Throughliner's first external user reported the same day that /plan's close-out offers read as the session being over, and a fix applied in-session produced a four-bullet menu the user called excessive. The shape settled on is a one-line description plus a single two-sided ask, with skip and raise-something-else named once at the session's start.

**The register decision was reopened, honestly.** The user asked why the mis-scoped rule that counted against typed pseudocode wasn't simply edited. It was — the branch was fixed and the error never shipped. So the LOG's *decisive* third reason for abandoning pseudocode is thin, and the decision rests on two arguments, not three. Also settled: retiring docset A does not unlock a pseudocode rewrite, and the intuition runs backwards — a bad register change is survivable today precisely because A is the known-good fallback, so the safe window for a re-test is while A still exists.

**Alternatives weighed and rejected, each recorded in its item** so they aren't re-proposed: inverting `docs/` and `docs-b/` (makes the no-strand fallback depend on the redirect being followed); a staleness sweep of Unprocessed at every close (the diff-everything cost the method fights); a version-keyed migration-notes changelog (a second artifact solving what the recipe covers); splitting plugin-behaviour.md under progressive disclosure (always-loaded behavioural rules aren't disclosure candidates — moving them behind an index deletes their effect); and letting the LOG alone carry that decision (it isn't read routinely, and this question had already recurred).

**Queue changes:** Processed reordered — the six docset-B items grouped contiguously, the two `[user]` verifications moved end-preferred. The readiness line moved from the top of Processed (where nothing sat above it) to below [docset-b-progressive-disclosure], clearing five items. [docset-routing-mechanism] was held back by the depends-on-unverified-work rule: it edits the injected directive `session_start.py` shipped, and that hook has never been confirmed live. Lift-conditions recorded for it and for [claude-md-old-history-claim-wrong]. The consumed advisory [advisory-docset-b-findings] was cleared, its persist-condition met. The mechanical mover could not place the marker from an empty cleared region ([reorder-mover-marker-at-top-blind]), so the marker line was relocated by hand first — the same workaround as last session.

**Work processed:** kept — [docset-b-editor-recorded-definition], [docset-b-scope-clause-loss], [docset-b-fidelity-restores] (the ten consolidated repairs), [docset-b-progressive-disclosure], [next-checkpoint-pause-ask-shape], [docset-routing-mechanism], [opus5-docset-switch-live-verification], [write-first-link-dont-paste], [migration-recipe-freshness]. Deleted — none. [migration-recipe-freshness] was found sitting on a stale blocker: the recipe it waits on shipped the previous day.

**Routed to Captures:** [close-advise-new-session], [plan-close-offer-reads-as-recommendation], [audit-findings-consolidate-destination], [audit-must-reconcile-with-prior-verification], [audit-check-elsewhere-before-reporting-missing], [plan-skill-did-not-engage], [decided-no-has-no-home-rule], [write-first-link-dont-paste] (filed and processed the same session), [unprocessed-blocker-claims-go-stale], [docset-b-register-decision-weaker-than-recorded], [skill-docset-override-unsanctioned].
