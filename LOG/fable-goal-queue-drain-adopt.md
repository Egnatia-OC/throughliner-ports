# [HASH] — Fable goal session: drained QUEUE.md, landed the ADOPT call on the two-section redesign with four amendments, held a merge

Autonomous goal session (SI disabled — plain file edits + git) run against FABLE-BRIEF.md: the queue never drains, test-shaped work leaks everywhere, and everything gets parked/deferred instead of finished or dropped.

**Drained the queue (QUEUE.md, 295 → ~110 lines).** Deferred tests: deleted all ~40 passive "observed after push + reinstall" confirmation lines into git history — they were confidence-notes on work that had already passed in-session fixture tests, filed as obligations no one confirmed; only the two genuine external terminal checks remain, plus a standing interim rule written into the section so /done stops filing observe-after-reinstall lines at all. Parked shelf: cut 22 items → 4. The seven queue-structure design items ([test-concept-redesign], [dedicated-design-session-mirage], [design-session-queue-home], [watchlist-doc], [shelving-as-tests], [user-execution-batch-shape], [guided-exploration-batch-type]) were deleted because the adopt call answers them; the rest were parked on triggers no one would act on soon. Kept [consumer-plugin-feedback-channel], [cruise-unattended-hook-signal], [ideas-doc], [setup-interview-depth]. Promoted the two /setup fix captures into one concrete cleared-to-run build batch [setup-editor-ask-wording-fix]. Everything deleted is recoverable at the parent commit.

**Landed the redesign call: ADOPT.** After reading the queue-redesign branch in the sibling `No code method-x` worktree in full (its QUEUE.md, the [work-line-behaviour-defs] + [plan-work-line-procedure] LOG entries, and the recut plugin-behaviour.md / plan.md / done-plan.md), recommended adopting the two-section Processed/Unprocessed work-line model — it fixes the three chronic pains at the root and is where the project's own parked thinking had already converged. Wrote two new captures: [fable-session-note] (orientation for the next /plan) and [adopt-queue-redesign] (the call + amendments).

**Amendments grew 3 → 4 → sharpened, across the session as the user surfaced gaps:** (1) keep a minimal red-flag mechanism (cruise control's gate + SPEC gate need a flag state to read); (2) preserve the plan-time "don't build on unverified work" guard as one placement-judgment sentence; (3) an active whole-queue curation pass; (4) decide the fate of the deleted mechanical slug triggers consciously — lightweight replacement (prose slug mentions grepped in the curation walk) or accept the loss by name. Amendment 3 was then sharpened twice: extended to BOTH sections (Unprocessed is where carry-forward items rebuild the parked shelf under a new name), then re-tuned to actually fire — a per-item walk with a stated verdict for every item (silence is a reported "walked N, all earn their place," never a default no-op), burden of proof on keeping not deleting ("would the user start this soon?"), and a first-class processing step, not a light close-of-session appendix. The tuning is the load-bearing part: the old model already had a staleness review + prune step that slept because anti-nag dampers ("only obviously stale, don't nag") fired on nothing every session; the amendment names that failure so the rebuild doesn't reproduce it.

**Held a merge, then recorded the hold on both sides.** The user asked to merge queue-redesign into main. Investigated and flagged three reasons not to do it blindly: the redesign is ~1/3 done (only plan.md/done-plan.md/plugin-behaviour.md recut; /next, the /done family, and all three hooks still speak the old structure — merging lands a half-converted plugin that must not be pushed/reinstalled as host); the branch is a renamed fork (sovereign-implementer-x / flintcraft-x / 0.1.0 — a blind merge renames the real plugin); and QUEUE.md conflicts, needing a blend not a take-theirs. On the user's call, merge is HELD until the branch's middle batches are finished; no merge performed. The decision was then made explicit in the docs so it doesn't live only in chat: main's [adopt-queue-redesign] gained a "Merge decision" paragraph (hold until [remaining-redesign-batches] ships; keep main's identity; blend QUEUE.md), and the queue-redesign branch's own CLAUDE.md gained a top "You are on the queue-redesign branch (merges back to main)" section (committed on the branch as b644f52) — placed in CLAUDE.md, not the branch QUEUE.md, because [fresh-queue-clean-break] wipes QUEUE.md just before the merge is due. Branch WIP left untouched.

**Files touched:**
- QUEUE.md (main — the drain, the captures, the four amendments + tuning, the merge-decision paragraph)
- LOG/fable-goal-queue-drain-adopt.md, LOG/index.md (this entry)
- CLAUDE.md on the queue-redesign branch (merge-back note; committed separately on that branch, b644f52)

**Commits (main):** fcbc74c (drain + ADOPT call), dd9dff6 (four amendments), 7a4da6d (curation-pass tuning + this LOG entry), 8a89351 (merge-decision recorded in the queue). Plus b644f52 on the queue-redesign branch (branch CLAUDE.md merge-back note).

**Routed to Captures:** the session's deliverable is captures/batches written directly into QUEUE.md ([fable-session-note], [adopt-queue-redesign], [setup-editor-ask-wording-fix]); none routed out.
