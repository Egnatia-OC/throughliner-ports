# 115f851 — Install instructions re-pointed at FlintcraftTech/throughliner, the org spelling matched to the remote, and the wrong "old history is on GitHub" claim corrected in both CLAUDE.md and AGENTS.md

The repo rename happened on GitHub, not through the method, so no revert of ours could undo it — but `96166c6` had corrected the project's identity in README, INSTALL.md, marketplace.json, the FAQ template and CLAUDE.md, and the 2026-08-09 revert put the old name back. The shipped install instructions were therefore naming a repository that resolves only through GitHub's redirect. Not urgent, because the redirect works; worth doing before the next release, because the redirect is the only thing holding it up and nothing announces if it stops.

The distinction that made this need care, and that is now stated in the docs rather than left implicit: the **repo** is `throughliner`, the **plugin installed from it** is still `sovereign-implementer`. So `marketplace add FlintcraftTech/throughliner` changed and `plugin install sovereign-implementer@flintcraft` did not. The two names not matching is expected rather than a mistake, and `96166c6` had already recorded that saying so in the README is what stops a user "fixing" one of them. That note now sits at all three install sites — README, INSTALL.md and the FAQ — because each is somewhere a reader meets both commands together and could reasonably conclude one is a typo.

The scope boundary was held deliberately. README's "install Sovereign Implementer" and the FAQ's product references are `[rename-to-throughliner]`'s job, not this item's. This re-applies one correction that already shipped once; letting it drift into product naming would have merged it into a repo-wide identity change that remains below the line and undecided in timing.

Two things found while verifying were folded in. The org name was written `FlintCraftTech` in the docs and is `FlintcraftTech` on the remote — GitHub resolves both, so nothing was broken, but the remote's spelling was adopted so a reader copying by hand matches what exists. And `AGENTS.md` carried the same wrong "old plugin history is on GitHub" claim as CLAUDE.md. Both were corrected together, because correcting one and not the other is exactly how the second copy gets missed. The corrected claim states what was actually verified: the plugin was rebuilt from scratch on 2026-06-01 and both this folder and the remote start there, the remote is not a pre-rebuild archive, some pre-rebuild history survives locally as the `v17`–`v157` orphan tags which are not ancestors of HEAD, and anything earlier would be on the user's old machine. `[queue-items-on-falsified-premises]` finding 1 is what established that the orphan tags survive — so `[claude-md-old-history-claim-wrong]`'s own framing was itself incomplete, and the wording written here reflects the verified position rather than that item's.

This was built last in the run deliberately: it must land before the next release, and the release follows the run. Verified at close that no `FlintCraftTech` spelling or old-repo reference remains in any of the touched files, and that marketplace.json still parses as JSON.

**Files touched:** `README.md` (the marketplace command in both the plain-English ask and the reference commands, both GitHub URLs, plus the names-differ note), `INSTALL.md` (the paste-ready prompt at line 70 and the command block at line 75, plus a names-differ note aimed at the Claude guiding the install), `.claude-plugin/marketplace.json` (owner name spelling), `plugin/si-plugin/templates/faq-template.md` (the install entry and the release-watch URL, with the names-differ note), `CLAUDE.md` and `AGENTS.md` (the old-plugin-history bullet in both).

**Routed to Captures:** `[shell-write-guard-computed-path-gap]` — filed during this run, from a QUEUE.md corruption and repair rather than from this item's own work. `[release-ritual-contradicts-intent]` — filed in the post-commit tail, after the user stopped a release the CLAUDE.md ritual mandates automatically and forbids asking about.
