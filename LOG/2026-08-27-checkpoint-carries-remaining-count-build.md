# [HASH] — The planning checkpoint says how many entries are left

The checkpoint banned every count and tally, which took the useful number out with
the clutter. A user deciding whether to carry on through another item has one
question the queue can answer — how much is left — and the checkpoint was silent
on it.

The message-order block gains the remaining-to-process count as its own element,
excluding anything dated out and anything skipped this session, and the specimen
shows it.

**The ban is narrowed rather than lifted**, and the distinction is what the
narrowing rests on: **the count is how far there is left to go, not a record of
what has been done.** The banned tally is retrospective — so many kept, so many
deleted, so many skipped — which is clutter at the moment the user is deciding
about one item. How much is left bears on that decision; what already happened
does not. The route menu stays banned, untouched.

**A full progress tally was refused on the item**, as the clutter the original ban
was written for.

**Files touched:** `plugin/throughliner/docs/plan.md` — the checkpoint specimen,
the message-order block (its old element 4 renumbered to 5 with "no count"
removed), and a paragraph distinguishing the two kinds of number.

**Routed to Captures:** none.

Rule gate: run — amendment to plan.md's checkpoint shape; the no-count clause narrowed and the narrowing recorded as a supersession with this instance as its ground.
