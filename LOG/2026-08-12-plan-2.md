# [HASH] — The instruction ceiling re-validated and found unfounded, and seventeen items cleared behind it

The session's largest output is a number that is no longer true. The user asked, before processing [board-reports-one-audience-not-two], whether the existing ceiling research was relevant to Opus 5 and Fable 5, and to re-run it if not. Checking first was their instruction and it was the right order: the direction of that research holds and the number does not. The 150–200 figure carries no source of its own in `instruction-file-bloat-and-subtraction.md`, and its hardest evidence is a July 2025 measurement across the Claude 4 family. The re-run found the same benchmark repeated a year later with frontier models about tenfold better — the vocabulary had to grow from 500 words to 10,000 — and the nearest tested Claude model failing only between 2,000 and 5,000 constraints. Filed at `resources/research/instruction-ceiling-revalidated-for-5-series.md` with two caveats written in rather than buried: neither of the method's actual target models was tested, and the benchmark measures keyword constraints in one generation rather than behavioural rules competing across a long agentic session.

What survives is the part that was always doing the work. Irrelevant content degrading *all* instructions, distractor interference between near-identical rules, and Anthropic's own 5-series guidance to remove prior-model scaffolding are untouched — and the last is the most relevant evidence available, being about the models actually running this. So the eviction case stands and its target changes: [rule-corpus-over-ceiling] was re-aimed from a count-reduction pass to a relevance-driven one, with its per-change disposition promoted from a caution against gaming the counter to the completion criterion, since nothing else can judge it. [board-reports-one-audience-not-two] was widened from splitting the count by audience to removing the ceiling entirely in favour of a growth report. Both carry an explicit instruction not to invent a replacement threshold.

The user's own field observation — that Claude soldiered on impressively even when the docs were at their most bloated — corroborates this and is recorded with the qualification that it is weak evidence in the direction that matters. Soldiering on is what a session looks like under either hypothesis, and three always-loaded rules read at session start and not fired in the same week say the second claim is false. "The corpus is not near a cliff" and "every rule is landing" were kept apart deliberately.

Four items were checked rather than taken at face value, and three of those checks changed the work. [pre-tool-use-schema-checks-failing] was diagnosed to its cause in-session by running the hook against a scratch project both ways: the fixtures write the legacy `_build.md` while the hook resolves a session-scoped name, so the suite has been crying wolf since that rename and the hook needs no change — which also answers its how-far-back question without a bisect. [hash-placeholder-outside-hash-position] had two of its three claims falsified: it does not nag, because the regex cannot match the token it describes, and the second entry it names is fine. Its defect is the silent shape rather than the noisy one, and the fix was redirected accordingly. [retired-terms-write-denied-by-scope-lock]'s open grep was run, establishing that its file is the only close-time write outside the exempt set. [step-1-scan-count-unbounded] was kept but rehomed from `plan.md` to the authoring gate, because adding a scan is authoring work consumers never do.

Three corrections landed on Claude's own reporting during the session, all recorded as captures rather than absorbed: the mover reported an item as below the readiness line when it had landed above; Claude named a shipped item as live work by reading another item's prose as current status; and the filing-claim hook blocked on captures presented as drafts for approval, which is a shape the method itself specifies.

**Queue changes:** three below-line items lifted on shipped blockers, one of them then deleted on the outcome its own text named; seventeen items now cleared to run with one held; the `rule_signals.py` cluster grouped adjacent so a run cannot take its four items separately; [rename-to-throughliner] kept last per its own instruction; the consumed forward advisory cleared, having been falsified by an intervening run.

**Work processed:** kept — [rule-gate-runs-at-close-not-at-authoring], [close-cost-at-run-scale-unexercised], [retired-terms-write-denied-by-scope-lock], [nothing-runs-the-hook-tests-at-a-close] (held, `Blocked by: [pre-tool-use-schema-checks-failing]`), [pre-tool-use-schema-checks-failing], [board-reports-one-audience-not-two], [fetched-docs-have-no-measure], [procedure-doc-size-measured-from-the-field], [board-satisfied-test-section-ambiguous], [conditional-written-as-a-tag], [step-1-scan-count-unbounded], [context-adjacency-offer-is-a-flat-menu], [hash-placeholder-outside-hash-position], [mover-move-section-rejects-marker-after], [command-offered-before-its-flags-were-checked]. Re-aimed — [rule-corpus-over-ceiling], [live-rules-name-retired-terms]. Deleted — [statusline-context-reader] (its restart test returned the outcome its own text said meant deletion); [skip-to-defer-missing-prompt-tag] (merged into [conditional-written-as-a-tag]); [write-first-rationale-belongs-in-log], [backstop-rationale-inline], [device-access-rule-could-be-fetched], [paging-rule-has-two-homes] (all four folded into [rule-corpus-over-ceiling] as its eviction list, content carried across first).

**Routed to Captures:** [mover-misreports-marker-side-on-move-section], [item-prose-named-a-shipped-item-as-pending], [close-time-captures-assert-unverified-mechanism], [no-link-from-research-to-items-scoped-on-it], [stop-hook-fires-on-drafted-not-filed-captures].

FAQ: not needed because nothing user-facing shipped — this session changed only the queue and filed one research file. Every decision made here reaches consumers through the builds it queued, and each of those carries its own FAQ obligation.

Rule gate: not needed — no rule in the method's own text was authored or amended. The commit touches QUEUE.md, LOG/ and `resources/research/` only.
