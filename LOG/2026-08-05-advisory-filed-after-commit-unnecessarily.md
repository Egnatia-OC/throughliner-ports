# d9162e4 — the forward advisory is now filed at the commit core, so a close no longer manufactures its own dirty tree

done.md's Recommend next filed the advisory after the commit, though its content derives entirely from queue state settled before it — so every concrete-recommendation close ended dirty, weakening the signal that a dirty tree means something. The commit core now derives the recommendation (the same overlap scan and ladder) and files a concrete advisory pre-staging, so it rides the close's own commit; Recommend next only presents it, with an explicit escape for the rare case post-commit conversation genuinely moves the recommendation (that update is then an ordinary post-commit tail write). The boundary the third observed instance sharpened is preserved in the wording: genuine post-commit tail writes remain a real category the reordering never claims to eliminate. Shared core, so all three flavor closes inherit it. Processed-and-built in the overnight blitz of 2026-08-05 under the softened bar; autonomous run — recorded departure.

**Files touched:** plugin/si-plugin/docs-b/done.md (commit core + Recommend next)
**Routed to Captures:** none
FAQ: not needed because no user-visible behaviour changes — closes just end with a clean tree.
