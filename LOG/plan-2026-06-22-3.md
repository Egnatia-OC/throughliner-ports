# 39aa0f4 — Plan: promoted [audit-batch-no-doc-write] (guard so an Audit batch can't write findings straight into a doc — plan.md authoring guard + next-audit.md execution guard + FAQ); filed [deferred-roll-testzip-blind] (deferred-test roll's version freshness check is blind to test zips → false "live") and [user-run-assumes-user-can-run] (classify user-run tests by what they require, not the user's workflow; explain the test and why Claude isn't running it); cleared a stale version label on [setup-e2e-verification]; deferred-test roll cleared nothing actionable

Triggered by a consumer E2E session (the "Stephanie abuse" evidence project) the user brought back: an audit "didn't turn up captures." Diagnosis from the transcript — the batch [whatsapp-audit-4] was labelled Audit but authored to write findings straight into a durable doc (its Files named the doc, its Output said "append to it"), and the session followed that over next-audit.md's hardcoded route-to-Captures, silently skipping the review gate. The user's real intent was a vetted lawyer-facing doc eventually, not a write at the audit stage — so the doc-write was a misread. Promoted [audit-batch-no-doc-write] to fix it: an authoring guard (an Audit routes to Captures only; a durable doc is a separate build from vetted findings) and an execution guard (surface, don't silently follow, a batch Output that contradicts the route-to-Captures contract). Parking it for a broader audit/build-boundary rethink was weighed and rejected — the existing audit → Captures → build flow already serves the eventual-doc need, so no new pattern is required.

Two captures filed for a later /plan. [deferred-roll-testzip-blind]: the deferred-test roll decides "is the host current?" by comparing release version numbers, but a test zip moves only the ignored -testN suffix, so the check can't see a reinstall and reports false "live" whenever unreleased build changes sit under a static base. [user-run-assumes-user-can-run]: surfaced from a real friction this session — I asked the user to confirm a terminal-only event she can't act on (she's desktop-app-only). The user then caught that the desktop-only fact doesn't generalize — Claude usually won't know a consumer's environment — which reshaped the fix from "detect tests outside the user's workflow" to "classify a user-run test by what it requires (terminal / phone / eyeball), stated plainly," plus two transparency rules: explain what the test checks, and explain why Claude isn't running it itself.

**Queue changes:**
- Promoted [audit-batch-no-doc-write] into Batches (appended after the three existing batches — no dependency on any of them).
- Cleared a drifted version label on [setup-e2e-verification] (`1.13.0-test1` → `1.14.0`; mechanical, the tests are unchanged).

**Captures routed:**
- [audit-batch-no-doc-write] — promoted to a batch.
- [deferred-roll-testzip-blind] — filed (processed; left for a later /plan to route).
- [user-run-assumes-user-can-run] — filed (processed; left for a later /plan to route).
- [anything-else-assumes-user-initiated] — filed post-close as a tail capture (the mid-capture "anything else?" closer feels mismatched when Claude, not the user, raised the discovery; from a Taskflowapp E2E screenshot). Appended to QUEUE.md after the commit; this line is an uncommitted working-tree edit that rides the next session's commit.
