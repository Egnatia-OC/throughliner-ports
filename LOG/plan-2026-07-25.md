# [HASH] — Plan session — processed [reorder-has-no-move-primitive] into a build, consumed the forward advisory

Processed one item this session, following the consumed forward-advisory's recommended order. [reorder-has-no-move-primitive] — /plan's close-out reorder can't reliably move a work item, because the only edit tool is exact-string replace and moving an item means retyping its full prose, which degrades to a partial sort on a long queue. Decided to fix it with a mechanical mover shipped in the plugin package (not the companion CLI, which needs a separate always-running process the project avoids): the script rearranges QUEUE.md byte-for-byte from a supplied slug order and self-checks. Weighed and rejected two alternatives, carried with why they lost: accepting partial sorts gives up the ordering the queue depends on, and keeping items short contradicts the settled capture-authoring standard that items keep full detail for the user to co-read. Also weighed the user's "give the queue an index like LOG's" idea and folded it in as one candidate form of the mover's ordering input, with a lean toward keeping that input ephemeral rather than a persistent index — a standing index invites reasoning from the compressed lines instead of the real items, the proxy-reasoning the method forbids. Placed below the cleared-to-run line at the user's direction.

**Queue changes:**
- Consumed and deleted the forward-advisory [advisory-2026-07-24]
- Moved [reorder-has-no-move-primitive] from Unprocessed to Processed (below the cleared line)

**Work processed:** kept [reorder-has-no-move-primitive]; advisory consumed

**Routed to Captures:** none
