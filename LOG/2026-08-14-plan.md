# [HASH] — Six items cleared, two deleted, and the rule gate refused an addition for the first time on record

A planning session that processed ten items and produced its most useful output at
the moments Claude's recommendation was overturned. Four of the six kept items are
recorded with a Claude proposal defeated and the reason it lost, which is the part
worth reading later.

**The rule gate refused something, and that has not happened before.** While
deleting [self-hosting-auto-detection], one paragraph of it looked worth saving
into `CLAUDE.md` and the user agreed to that. Before writing, the gate document
was opened in full rather than reasoned from `CLAUDE.md`'s summary — the exact
failure `0150ce4` recorded the day before. Opened, it refused the addition twice
over: admission question 1 stops a speculative rule, and there is no recorded
instance of "host-only by residence" being misapplied for want of that
explanation; and the rationale section settles it outright, because an evicted why
does not have to land anywhere — git history keeps it. Nothing was written. The
user had agreed to a recommendation made before the document was read, so the
agreement was reported back as void rather than treated as authorisation. Every
prior disposition on record has been favourable; this is the first refusal, and
the first evidence the gate can do anything but describe.

**The work-cycle block moves into the always-loaded file, against Claude's
recommendation.** Claude proposed keeping the canonical block in `plan.md` and
trimming `next.md`'s duplicate copy, on the arithmetic: the two docs never both
load in one session, so hoisting the block makes /setup and /done pay for it too —
a net increase of about 234 words at a moment three cleared builds are removing
text from that same file. The user overruled it, and her argument defeats the
admission test rather than outweighing it: Claude needs to know at all times what
the work it is doing means, the cycle does influence how /setup and /done run in
ways that are not immediately foreseeable, and it has been a battle without Claude
always aware of what it is participating in. The all-four-skills test answers by
hunting for a visible trigger, and diffuse orientation has none — so the test reads
it as not firing where it does. The evidence for that is her experience across
sessions, which no single session applying the test can see.

**Three Claude proposals were defeated in sequence on the vocabulary item, and the
last one died on self-hosting.** The item was captured as a `CLAUDE.md` problem;
the user corrected the site — it is not about her reading that document, it is
about Claude carrying the words into conversation. A ban was proposed and refused,
because these are the project's own words and `disposition` names a real line in
her own log entries, so refusing to say it leaves the word in her files waiting to
be met alone. A two-category split was then proposed — procedure scaffolding
unsaid, artifact-naming terms sayable — and she killed it with the self-hosting
case: here the procedure *is* the subject matter, so `pre-flight` names a doc
section she can open and nothing at all in a recipe app, and the same word gets
opposite answers in two projects. What survived is one test that is a property of
the project rather than of the word: does the term name something in *this* user's
world, and if so, show it once rather than defining it. The stop-hook check went
with the ban.

**The asks are still missing, and there is a second cause.** She reported it live,
mid-session, on `1.20.0-test3`. The output style regression already captured as
[concision-build-removed-the-asks] is only half of it: `plan.md`'s hand-over
message instructs "no routes, no options, no question" and ships a specimen with
no ask, which directly contradicts the always-loaded rule requiring one bold
question at the end of every message. During a long processing run that message is
most of the turns. Her instruction is recorded as an instruction rather than a
preference — she spent months designing these and wants them back exactly as they
were — along with what the no-question wording was protecting, so the build does
not restore the four-route recital that once caused a session to be closed she
wanted to continue.

**A red flag was raised and could not be cleared.** Replying to a report from a
private project surfaced that the INBOX has no notion of a correspondent whose
name is itself sensitive. Four exposures are recorded on the capture; the
gitignore covers publication and nothing else, and does not reach the recipient's
repository at all. It cannot be cleared by discussion, so it stays uncleared in
Unprocessed. The correspondent's identity and path are recorded nowhere — not the
address book, not the queue, not this entry — at the user's instruction. The
folder was resolved by testing the single path her words pointed at rather than
listing the parent, so nothing else under it was read.

**Two findings came out of checking claims rather than from any symptom.** The
migration checklist's deferred-test rule, which another project's user called
nonsensical, turns out to be correct and badly formatted: its condition wraps
mid-line inside a fenced block, so "a deferred test only the user can run" renders
as "a deferred test only". She was reading the doc accurately and the doc is at
fault. And this project's own FAQ has diverged from the shipped template — 413
lines against 460 — while `CLAUDE.md`'s FAQ rule sends new entries only to the
template, so the gap is produced by the rule every time it fires rather than by a
one-off slip.

**One slip, recorded because it is a live instance of an open item.** The delete of
[self-hosting-auto-detection] was made by running a Python heredoc against
QUEUE.md instead of using the editing tools, against this project's own scripting
rule. The hook did not stop it, which is the gap already captured as
[py-dash-c-escapes-the-script-write-guard]. The result was verified by the digest;
the method was still wrong.

Two replies were drafted, approved verbatim and delivered — one to Understudy on
the parking case and the deferred-test misreading, one to the private project on
the close-out finding.

Planning state: _plan-f0cf20f2.md, 10 dispositions

Rule gate: run — REFUSED. Proposed addition: the two-seeding-doors explanation into
always-loaded `CLAUDE.md`. Gate document opened in full before the disposition was
written. Parent named — it amends the "Host-only by residence" convention, so it
would consume no slot, and that limb passes. It fails admission question 1: no
recorded instance of that convention being misapplied for want of the explanation,
so the addition is speculative. And the rationale section settles it directly — an
evicted why does not have to land anywhere. Nothing was written to `CLAUDE.md`.

FAQ: not needed — nothing shipped this session; every change is a queue decision.
The FAQ gap this session found is filed as [no-faq-entry-on-deletion-and-recovery]
rather than written here, because writing it needs [own-faq-diverged-from-shipped-template]
settled first.

**Queue changes:** Processed went from 11 items to 17, all cleared to run, with the
`[audit]` and `[user]` lines batched to the end. Unprocessed went from 53 to 46 —
six items moved out, one deleted outright, one deleted into another item, and four
new captures filed. The consumed forward advisory was cleared.

**Work processed:** Kept — [migrate-gate-operative-half-into-claude-md],
[in-session-parking-has-no-home], [discord-post-spec-inversion],
[work-cycle-block-duplicated-in-two-docs], [claude-md-vocabulary-is-unexplained],
[plan-close-out-obligations-survive-the-phase]. Deleted —
[drive-testing-signals-skill-routing] (folded into [post-close-tail-state] first),
[self-hosting-auto-detection]. Skipped this session —
[discord-post-context-adjacency], [discord-post-cycle-awareness]. Amended in place
— [concision-build-removed-the-asks] gained its second cause.

**Routed to Captures:** [inbox-privacy-for-sensitive-correspondents] (red flag,
uncleared), [own-faq-diverged-from-shipped-template],
[no-position-signal-in-a-processing-run], [no-faq-entry-on-deletion-and-recovery].
