# aa78310 — Install-route check run at processing and the item deleted as satisfied: the marketplace route serves main, so no doc sentence could point anywhere older

Filed and resolved in one session. The check read README.md and INSTALL.md end to end: the install is a marketplace add of the repository, which serves whatever main holds — there is no older-version sentence to correct, and the release artifact is never what installers receive. Two consequences were accepted on Alex's yes and recorded on [expedite-first-beta-release] (see `2026-08-26-expedite-first-beta-release.md` for the day's reasoning): today's release runs as the day's last act so main equals the beta at that moment, and installers drift onto dev state between releases until the `beta` branch ships. Her correction of Claude's first account — which conflated main-at-release with "the released state" — is what turned this finding into [beta-branch-install-pin].

**Queue changes:** item created, verified, and deleted as satisfied-by-check.
**Work processed:** deleted — [install-route-latest-release-check].
