# [HASH] — Cleared the Opus 5 audit + phase 3 by position alone, and returned the deprioritised report-page item to Unprocessed after the revisit nagged once too often

A short planning session with one real decision and two method failures caught live.

The decision: **[docset-b-fidelity-audit]** and **[opus5-session-start-detection]** (phase 3) were both moved into Processed above the cleared-to-run marker, audit first. The audit must run while docset B is still inert, because phase 3's rezip is the moment B first governs live sessions, and fixing a file nothing loads is cheap where fixing one that governs real sessions is not. That ordering had previously been written as a lift-condition on phase 3 ("run the audit before this ships"). The user's correction: that is what position already says. /next works the cleared region top-down, so placing the audit above phase 3 *is* the statement that the audit runs first, and building the audit is what unblocks phase 3 — no marker, no condition, no intervening session. The condition was dropped and both items cleared together.

Two failures, both the user's catch, both now captured.

**The revisit nags, and the nagging produced an invented shelf.** /plan Step 1's below-line revisit asks about every below-line item whose lift-condition only the user can answer. [report-url-404] had one, so it had been asked about at three consecutive sessions despite being deprioritised every time in favour of the Opus 5 work. Under that pressure Claude proposed changing its lift-condition to "you raise it" — a fifth state, exactly what the anti-invention guardrail forbids, and a silent-shelving error because it depends on the user reading the queue. The user rejected it and named the correct move: the only postpone is the bottom of Unprocessed. That is where the item now sits. Worth recording that the guardrail is clearly written and was walked into anyway — the failure is not a missing rule but that the revisit creates a pressure the rule offers no legitimate answer to. Filed as [below-line-revisit-nags-deprioritised-work].

**The marker is over-used for ordering.** The same root cause, one level up: work is held below the marker with conditions naming other queue items, when position already carries that. Filed as [marker-overused-for-ordering-position-carries], with the distinction to settle — the marker is for work not greenlit at all, or waiting on something genuinely outside the queue; "this after that" is position's job.

One finding on the way through: **flintcraft.tech/report now exists** in the Flintcraft.tech repo at `site/report.html`, committed and pushed (`edd5e83`) with a working form. It is not live yet (the site has not been republished) and the form carries an email field and a project dropdown where the method settled on a single free-text block. Recorded on [report-url-404] so the progress is not rediscovered from scratch.

Also recorded: **[next-start-order-handoff] went stale.** It had instructed itself to be authored as part of the docset-B build so it would land in B rather than the freezing docset A, and that build shipped without it. The note existed only in a forward advisory, which is transient — it has now been written onto the item itself.

For the record, since it was asked: this session ran on **docset A**. The installed host resolves the skills to `docs/`, not `docs-b/`. Docset B exists but nothing loads it until phase 3 ships.

**Queue changes:** [docset-b-fidelity-audit] and [opus5-session-start-detection] moved into Processed above the cleared-to-run marker (audit first, by position); phase 3's "after docset B exists" and "run the audit first" conditions dropped as met/superseded; [report-url-404] removed from Processed and returned to the bottom of Unprocessed with its progress recorded; the consumed forward advisory [advisory-plan-docset-b-audit] cleared; a staleness note added to [next-start-order-handoff].

**Work processed:** kept — [docset-b-fidelity-audit], [opus5-session-start-detection] (both cleared to run). Returned to Unprocessed — [report-url-404]. Deleted — none.

**Routed to Captures:** [below-line-revisit-nags-deprioritised-work], [marker-overused-for-ordering-position-carries].
