# [HASH] — The advisory step gains a replace branch, and the clearing rule narrows to license it

Kept with both halves as one fix: where the reserved slot holds a spent advisory, the close replaces it — delete, file its own, say it replaced one — and the clearing rule narrows from "never this close's job" to "never this close's job to clear one it is not replacing". Clear-at-the-read stays untouched for notes a close is not replacing, preserving the reason the rule exists. Build block on the item.

**Queue changes:** [advisory-step-collides-with-a-spent-note] moved Unprocessed → Processed, cleared.
**Work processed:** kept.
