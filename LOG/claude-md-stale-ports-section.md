# [HASH] — CLAUDE.md — refreshed 2 stale passages: Cross-platform ports (active Claude↔Codex → Codex shelved/downstream-only) and the Method-docs QUEUE.md description (→ two-section model)

CLAUDE.md — refreshed two stale passages. The Cross-platform ports section still described a live two-port Claude↔Codex relationship with peer weigh-ins, but the Codex port was shelved indefinitely on 2026-07-28; rewrote it to Codex-shelved/downstream-only, the method evolving solely on the Claude side, keeping the Codex source path only as read-only history. The Method-docs QUEUE.md bullet still described the pre-recut model — red flags in a top section with open/resolved/accepted states, Batches with Build/Test/Audit subheadings, a "Deferred tests" section, Blocked-by/Parked headers — all retired by the two-section recut; rewrote it to Processed/Unprocessed with `####`+slug+provenance items, per-line `Red flag · State:` markers, and deferred verification as below-line `[user]` lines revisited by plan.md Step 1. Both are host-doc only (not shipped in the plugin package). Kept the build-stamp and Plan-session-here passages (still current).

**Files touched:**
- CLAUDE.md

**Routed to Captures:** none
