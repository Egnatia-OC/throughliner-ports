# 7bc2c58 — A twenty-item run halts twice on its own scope, and the build view is caught stripping the disposition it exists to carry

The chat-level record for this session. Sixteen of twenty cleared items built; each has its own entry.

**The run halted before it started, wrongly, and the cause is a queued defect firing for real.** Pre-flight found no `Rule gate:` line on [cut-length-everywhere] or [sent-record-surfaced-as-waiting-mail], both of which amend standing rules, and stopped as `next.md` requires. Both dispositions existed. They sit in each item's rationale, above the build block, and `generate_build_view.py` strips everything outside the block as decision history — which is exactly what the unprocessed capture [build-view-strips-the-gate-disposition] predicted. The halt cost a round trip and the user's correction ("I had fable fix it in the previous session") is what unblocked it. Recorded here as the live instance that capture was waiting for.

**QUEUE.md changed underneath the session mid-check**, two lines inserted while the disposition question was being investigated, from another chat open on the same working tree. Surfaced to the user, who confirmed that chat was finished. Worth noting because the shift was only visible by comparing two greps minutes apart.

**The run halted a second time on its own premise.** [cut-length-everywhere] is a fifteen-file rewrite whose acceptance forbids losing any operative rule, which means reading everything deleted — more than one chat holds. Rather than half-finish it silently, the run stopped and put the trade to the user: the length cut, or the fifteen cheap fixes behind it. Her call was to capture the remainder and carry on, which is what happened for that item and for [law-prose-pass-missed-mid-sentence-prohibitions].

**One scope decision was made rather than asked**, and it turned out to be moot. The scope-lock refuses a build any direct edit to QUEUE.md, so [cut-length-everywhere]'s queue limb and [lint-flags-its-own-scaffolding]'s local preamble conversion could not be built here. The second turned out to need nothing: this project's lint reports zero standing flags, against the thirteen its item asserts.

**Three defects were found by building rather than by planning**, each filed: the filing-claim hook is blind between a tick and the close; the shell-write guard misses a path built by `os.path.join(...)` entirely, which is the exact failure it exists for; and the guard's reported bug was a raw-string prefix, not the path check.

**A correction the user gave at the close:** write logs of reasonable length per item, not novels. These entries are written to that.

**Also in this chat:** a permission-breadth over-reach was caught and reverted in [claude-config-points-at-dead-layout] before it landed; a wrong relative path in the run's own scope list (`../../CLAUDE.md` for a file three levels up) was corrected rather than the scope widened.

**Not reached:** both audits. [brevity-amendment-outcome]'s instrument runs but reports one aggregate over 161 sessions and cannot attribute a sample to an installed build, which its acceptance requires — it needs a decision about what to measure against, not a run. [session-occurrence-audit] is a per-occurrence judgment across every procedure doc, the FAQ and SPEC.

**Files touched:** the sixteen items' own entries carry theirs
**Routed to Captures:** [cut-length-remaining-docs], [law-prose-remaining-thirteen-docs], [stop-hook-blind-between-tick-and-close], [shell-guard-blind-to-call-built-paths], [queue-content-items-are-unbuildable-by-a-run], [close-message-duplicates-the-advisory]

**Completed `[user]` item recorded:** [discord-post-cycle-awareness] — `INBOX/sent.md` records it posted 2026-08-21 to the Throughliner channel, with the text in its own LOG entry. Logged and removed from Processed. Two held Discord posts named it as their blocker; lifting them is the next /plan's below-the-line revisit, not this close's.
Rule gate: run — the dispositions transcribed for the two rule-bearing items are on their own entries; nothing further was authored at chat level.
Advisory: filed — [forward-advisory]
