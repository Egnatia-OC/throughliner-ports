# [HASH] — the feedback channel is now three-way: app → queue, method → flintcraft.tech/report, Claude Code → GitHub

The two-way channel had nowhere for a problem in Claude Code itself — the harness the method runs inside — and the design was already settled and exercised end-to-end on 2026-08-04 (a real report split into a strengthening comment on an existing issue plus one new issue). plugin-behaviour.md's Consumer feedback channel now carries the three-way discriminator and the Claude Code branch with the guards that filing proved: offer to file directly only where gh is installed and authenticated, with a draft-for-the-user fallback so the offer never just fails; approval-before-post non-negotiable (full text shown, posted only on an explicit yes, never auto-submitted); duplicate-check first, recorded as a guard that shapes the report rather than merely avoiding repeats; and scrub-by-construction with the counter-intuitive case stated — a report is about sensitive content more often than it contains some, so it describes the sensitivity without demonstrating it. The method-report flow's user-pastes-it-themselves rule is explicitly scoped so the two posting rules don't read as contradicting. SPEC's reporting paragraph rewritten to the three-way shape; README's "What it does" gains the routing sentence; the FAQ's method-problem entry extended with the third case and the three-answer routing test. Processed-and-built in the overnight blitz of 2026-08-05 under the softened bar (design settled and field-tested before tonight); autonomous run — recorded departure.

**Files touched:** plugin/si-plugin/docs-b/plugin-behaviour.md, SPEC.md, README.md, plugin/si-plugin/templates/faq-template.md
**Routed to Captures:** none
FAQ: updated — "Something about the method itself is broken or confusing — where does that go?" extended with the Claude Code branch and the three-way routing test (template copy; project copy awaits the audit-recommended rebuild).
