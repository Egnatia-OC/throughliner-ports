# [HASH] — Chat-level record for the 2026-08-21 planning chat: 32 items processed, the cleared region grown from 3 to 29, two SPEC additions written with the user present

The largest planning session on record here. Twenty-six items kept (twenty-five cleared, one held), six deleted — two by relocation into SPEC, one absorbed into a sibling audit, one killed by the user, one as working-as-designed, one declined with an outbound reply. Per-item records are their own entries, one per slug, each pointing at the settlement and build block riding its queue item.

**Also in this chat:**

- **The Discord chain moved.** [discord-post-cycle-awareness] was posted and closed on the 21st (per `INBOX/sent.md`), so [discord-post-context-adjacency] was lifted into the cleared region — its third subject rewrite still owed at drafting, posting no earlier than the 22nd under the one-a-day rule — and [discord-post-session-start-strength]'s blocker was repointed at it so the chain advances itself.
- **Two captures gained `Not before:` dates with the user's approval** — the field's first uses: [approval-flow-token-doubling-simplification] to 2026-09-21 (waits on a Claude Code issue five weeks quiet) and [taskflow-personal-bridge] to 2026-08-28 (waits on Taskflow's reply to the 2026-08-20 letter).
- **Three items skipped with their settlers named in their prose:** [research-packaged-as-build-work] (settled by the keep-step audit's composite reading), [standing-audit-programme] (follows the two freeform corpus passes), [procedure-docs-cannot-tell-finished-from-interrupted] (follows the /rescan hand-back fix as its worked example).
- **A narration finding against Claude's own reports.** The user: only the last sentence of a three-clause post-write report was necessary. Two shipped rules already cover it — the one-line report and shelving-mechanics-unsaid — so this is the tenth recorded instance of a correctly worded rule with a stated site not firing, joining the class [standing-audit-programme] tracks. No new rule; behaviour corrected in-session.
- **The template reply was sent** on exact-wording approval, the sender's path recorded in the address book after a bounded search the user directed — the instance that produced [address-book-user-directed-find].
- **The session-start mail flag was the known false positive** (`INBOX/sent.md` surfaced as waiting mail — fixed in the target, host predates the fix), and the dispositions listing again printed the full history claiming one session's, the already-filed [dispositions-listing-window-not-bounded] observed live. Neither generated new work.
- The forward advisory ([cut-length-remaining-docs]) was surfaced first-line, followed, and cleared at the read.

Advisory: filed — the region ends in hand-work; see the note's own text.
FAQ: not needed — no shipped doc changed this session; every user-facing change rides a queued item carrying its own FAQ disposition.
Rule gate: not needed — no rule authored or amended in the method's own text this session; every rule-touching decision is a disposition on a queue item, transcribed when its build runs.

**Queue changes:** see the per-item entries; Unprocessed 59 → 27, Processed 7 → 33 (29 cleared, 4 held).
**Work processed:** 32 items — 26 kept, 6 deleted; 3 skipped with settlers; 2 dated.
