# db68f32 — Twelve items cleared around message length and the rule corpus, after a live regression and an outside audit showed three watchers had all stayed quiet

This session started as ordinary queue processing and turned into an investigation of why the method's own checks had not caught anything.

The turn came when the user reported that since the last rezip the turn-by-turn behaviour was gone and the asks with it. The cause was ours: the concision build shipped a cadence rule into the output style — *speak at three moments while you work, and work quietly between them* — which reads as an instruction to keep going, against `[PROMPT]` steps that say stop and wait. The style loads later in the stack and won. The user then identified a second defect in the same sentence that Claude had not: "three moments" is a bare number, which `self-authoring-rules.md` forbids. Tracing why neither was caught produced the session's largest finding — the output style lives at `plugin/throughliner/output-styles/`, which is in neither the rule gate's trigger path list nor the board's growth report, so it is an always-loaded layer sitting outside both the admission gate and the measurement. The user also observed she had never heard of the output style or seen it in the folder structure, which turned out to be accurate: `CLAUDE.md`'s "Where things live" tree does not list it and the architecture enumeration does not count it.

The board was run live for the first time anyone can point to. It returned 0 of 4 signalling, and that reading was accurate — but nothing schedules it, so its silence had never been evidence of anything. The user's challenge to it went further and was correct: it reported clean on a day when five real defects in the rule corpus were found by conversation, and its near-duplicate check reported clean hours before an outside audit found fifteen duplicates. What it measures is whether the paperwork was completed. The approval half is worse: the gate's author and its approver are the same party, and the user, the one independent view, has never been asked about a single rule. Recorded too, because it locks out the alternative: `CLAUDE.md` chose the board over a positional cycle on the grounds that a cycle leaves work "downstream of machinery that never ran", and added a clause forbidding re-proposal — then the board never ran either, so the argument that defeated the alternative applies to the winner with equal force.

The user brought an audit of `skill-nonspecific-rules.md` produced by a separate Claude instance in a chat session. Four of its claims were spot-checked against the live file and held. It found eleven contradictions, fifteen duplicated rules, four misplacements, and ten rules living in the always-loaded file that fire in only one skill — which the file's own opening admission test forbids. At the user's instruction the thirty-one findings were filed as individual captures, against a recommendation to file one item pointing at the research file; then, also at her instruction, consolidated into three work items. The fifteen resolutions for the contradictions and misplacements were proposed by Claude and approved as a set, unmodified.

Two items were reversed mid-session and it is worth saying why. [rule-lifecycle-board-has-no-trigger] was kept and cleared, then returned to Unprocessed once the user's challenge produced [rule-board-measures-paperwork-not-health]: one item gives the board a site and the other asks whether it should exist, so building the first would settle the second by default. [approval-flow-token-doubling-simplification] was rescoped rather than deleted at the user's direction — Claude recommended dropping it because write-first had already removed the doubling from the dominant flow, and the user's point was that if it still helps the show-first moments then it should be about those.

One measurement worth keeping: [queue-edit-echo-costs-context] was deleted after being tested live. One `Edit` and two `reorder_queue.py` calls against QUEUE.md this session produced no echo of the file's opening in any tool result, so the behaviour is not reproducing on this harness, and the method-side half was already fixed by the mover.

The session also ran long in its own narration, immediately after a build aimed at concision, and the user said so twice. The first diagnosis — that `[DISCUSS]` licenses length and procedure tags outrank the style — was correct but incomplete, and the user's correction was sharper than the finding: delivering a lecture is not discussing. That became [discuss-tag-licenses-length]. It is the same shape as the asks regression with the sign flipped, and both are now cleared to run.

**Queue changes:** twelve items cleared to run, led by [snr-ad-contradictions-and-misplacements] marked `Runs alone` at the user's request so it builds in an isolated run; the consumed forward advisory cleared; thirty-one audit captures filed and then consolidated into three items; one item returned from Processed to Unprocessed.

**Work processed:** kept — [plan-opening-splits-into-two-messages], [discuss-tag-licenses-length], [distribute-brevity-rules-further] (narrowed to the tag-reach gap), [communication-rules-untestable-here], [rationale-written-into-shipped-docs] (as `[audit]`), [concision-build-removed-the-asks], [advisory-not-cleared-by-a-build-close], [plan-gates-say-close-out-for-a-retired-phase], [off-ramp-names-no-affirmative], [snr-c-rules-failing-the-admission-test], [snr-b-duplicate-rule-statements], [snr-ad-contradictions-and-misplacements]. Deleted — [queue-edit-echo-costs-context] (measured away), and the thirty-one individual `snr-` captures, consumed by the three consolidated items. Skipped — [rule-gate-has-no-site-in-next] (sharpened: the gate's site is the edit step, not the tick), [invented-rationale-compounds-past-the-shipped-rule] (sharpened: the written-file length rule is an untested third remedy, so the next move is observation). Rescoped and left in Unprocessed — [approval-flow-token-doubling-simplification]. Returned to Unprocessed — [rule-lifecycle-board-has-no-trigger]. Filed — [output-style-missing-from-the-project-map], [rule-board-measures-paperwork-not-health], [work-cycle-block-duplicated-in-two-docs], [claude-md-vocabulary-is-unexplained].

**Planning state:** _plan-a3d9326d.md, 16 dispositions

Rule gate: not needed — no rule was authored or amended in the method's own text this session; every rule change was queued as work, not written.
