# [HASH] — Kept and cleared: kind-suffixed record filenames, so a second record for a slug stays visible to the digest

Noticed by Claude at the session's opening: the digest attributes a record only to an exact `<date>-<slug>.md` filename, so the overwrite guard's `-2` names and shortened build names (`cycles-build`) are invisible — observed live as [weekly-release-cycle]'s blocker printing "only processed" with its build record on disk, feeding the lift decision wrongly. Worked on her say-so. The suffix scheme is hers: kind, not number — `-plan` / `-build` when the bare name is taken, because a number is arbitrary where a kind explains itself; legacy `-N` stays tolerated. The build fixes the digest's matching, amends done.md's naming instruction, and renames today's nonconforming records. This close already writes its filenames under the decided rule.

Rule gate: run — amendment to done.md's record-naming instruction (parent named); nothing freestanding, nothing evicted; the digest change is script behaviour, not a rule.

**Queue changes:** new build item, cleared, first in the region.
**Work processed:** kept — [log-record-kind-suffix].
