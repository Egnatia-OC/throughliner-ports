# LOG entry — plan 2026-06-12 (4)

## e51cf40 — /plan: QUEUE.md — four batches queued: [done-unconditional-read] (/done's _build.md read made unconditional, memory enriches never substitutes), [closeout-text-collapse] (commit title/body derive from the LOG entry's one-liner and rationale), [post-close-capture-record] (post-close captures update the entry's routed line as an uncommitted working-tree edit), [deferred-test-lifecycle] (deferred tick state, runnability tails, pre-flight run-offer, reinstall flag); "oldest first" processing order renamed to file order via [tag-restatement-trim] fold

The session ran two threads. First, user-raised: nothing protects against someone running /clear between skills. The assessment found the seam after /done already file-sufficient — everything is written and committed by close — and the exposed seam is the one before /done, which the user habitually protects by never clearing there. The observed slip behind it: the last /done skipped the mandated _build.md read, plausibly because the whole session sat in memory. [done-unconditional-read] codifies the inverse — the read is unconditional, memory enriches but never substitutes. A memory-conditional read ("read it if you don't remember the session") was weighed and rejected: it hangs on Claude assessing its own memory, which fails post-compaction, when the session feels remembered but isn't. The second thread came from the captures: the close-out texts. /done's four renditions correlate in pairs — index entry with commit title, LOG entry with commit body — and [closeout-text-collapse] makes the correlations literal: two authored texts, one approval. Weighed and rejected there: dropping the index for git log (the index is curated, one line per session; history is full of ritual commits; its filename pointer makes retrieval one hop; post-collapse it costs nothing), and backfilling old commit messages (rewriting a message changes every descendant hash, invalidating every hash the system recorded, and needs a banned force-push — also unnecessary, since LOG entries are committed files and the why-pipeline already lives in git as content). One correction recorded: git log -S searches diff content, not commit messages, so commit-title choice never affected the hash backfill. [post-close-capture-record] closes the tail-capture gap by reusing the backfill's working-tree-edit pattern; rejected: next-session attribution (a cross-entry hop riding on judgment), per-capture follow-up commits (ceremony exactly where the session winds down), and accepting the gap (leaves the entry knowingly false on one line). [deferred-test-lifecycle] answers the execution-trigger capture with announce-once-offer-persistently, carrying two folds — the fourth tick state, `— deferred (reason)`, born from the /clear discussion because a mid-session can't-run determination must survive in the file, and the section's previously unwritten scope statement. Rejected nearby: making deferred-test queue lines tickable — in that section presence is the state; ticks are for lines that persist while their state changes, like _build.md's. The "oldest first" aside was user-caught: Claude-directed placement means file order and age order diverge, so the processing rule's name promises an order the file no longer guarantees; renamed via fold into [tag-restatement-trim], which already edits that line. Two captures remain unprocessed (scope-lock dash-stripping leniency; close-out assessment codification). The session also backfilled the [narration-vocabulary] entry hashes to 8f2cab4.

**Queue changes:**
- [done-unconditional-read] added to the done-family cluster, before [done-audit-overlap-scan]; Depends on: [scope-boundary-rule]
- [closeout-text-collapse] added directly after it; Depends on: [approval-display-blockquotes]
- [post-close-capture-record] added directly after that
- [deferred-test-lifecycle] added directly after [capture-verbatim-first]
- [tag-restatement-trim] amended: rationale records the fold; build list renames "oldest first" to file order

**Captures routed:** [closeout-text-collapse], [post-close-capture-record], [deferred-test-lifecycle] promoted from Captures (source captures removed); [done-unconditional-read] promoted straight from conversation; two captures remain unprocessed. Filed in the session tail, after the commit: the commit message-passing capture (PowerShell 5.1 has no heredoc; fold target [closeout-text-collapse]) — this line updated post-close per the just-queued working-tree-edit rule
