# [HASH] — /plan: unparked and cleared the -x→main merge to run

This session decided to execute the long-parked -x→main branch merge. The trigger was noticing that [execute-merge-to-main] sat in the old queue's Parked shelf behind a self-judged `Blocked by:` gate — the exact rot the two-section redesign was built to kill.

The gate ("dogfood the two-section model live") was found near-unsatisfiable as written: the recut-verification deferred tests all require a two-section QUEUE.md, but this fork's queue only becomes two-section *at* the merge itself, and the pre-merge conversion batch [fresh-queue-clean-break] was retired 2026-07-06. So the block couldn't be fully cleared before the merge, because the merge is what creates the conditions to clear it. Against that, the plugin code is complete (redesign middle built) and has been run across real /plan, /next, /done sessions on the installed -x host including this one — so Alex judged the fork trustworthy enough to merge.

**Queue changes:**
- Promoted [execute-merge-to-main] from Parked to a cleared [freeform] work line at the top of Batches (above the cleared-to-run marker), rewritten with the decision rationale and the CLAUDE.md merge mechanics (take main's plugin.json/marketplace.json; start from main's drained queue; convert to two sections; pull forward by judgment).
- Deleted [merge-stranded-in-park] (the capture that drove the decision; filed earlier this session in /next, its reasoning now preserved in the merge line).

**Work processed:** kept — [execute-merge-to-main] (freeform, cleared). Deleted — [merge-stranded-in-park].
