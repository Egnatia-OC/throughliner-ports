# [HASH] — Plan: 3 batches promoted, 1 dropped, deferred-test labels cleaned, self-hosting inventory dropped, 2 design items research-backed

Three threads ran this session.

First, the user flagged that recent changes had thrown off the narration and asked for a narration/tag review plus a fix batch. The review was already scoped and shelved as [full-tag-placement-recheck], blocked on the 2026-06-16 tag/narration batches — now shipped and live — so it was woken and widened to cover narration-quality drift (vocabulary leakage, menu-vs-recommendation, noisy openings) alongside tag placement. The fix batch was declined as premature: fixes come from the review's findings, scoped in a later /plan, so only the review was queued.

Second, a screenshot showed Claude asking permission to clear satisfied "depends on" notes at a commit — over-asking on cleanup Claude owns. Promoted [cleanup-narrate-not-ask]: change the staleness fix-path in plugin-behaviour.md and four done docs so mechanical queue maintenance (clearing a met dependency, repointing a drifted reference) becomes narrate-and-do, not "with the user's approval"; fate decisions still defer to /plan. Scoped broader than the dependency case at the user's call.

Third, the user listed required README changes after the desktop app's UI changed (install is now "add", new uninstall pathway) and asked for an inline audit. Verified the nine points — conceding one ("spec read-only during builds" is accurate for every build but a spec-edit) — and found more: a retired-model line, a "/done test" inaccuracy, a duplicated question-count, and the same UI staleness in INSTALL.md. Promoted [readme-install-refresh] at the top (Claude-runnable now), and re-pointed [install-upload-path-clarity]'s screenshot at the new "add" screen.

Captures: dropped [lint-citation-refire] (the citation re-fire judged not noisy now the bigger lint false-positives are fixed). Resolved [deferred-test-runnability-tag-precision] in-session by re-tagging six Deferred-tests lines from user-run to observed, so the roll stops over-including. Parked [shipped-slugs-breadth] with a trigger.

Parked-shelf review (the user took up all three review-eligible items): dropped the 34-finding self-hosting notes inventory — its actionable findings have shipped, the rest are no-action or already covered by [self-hosting-support-during-setup], to which a build-checklist pointer was added (full inventory preserved in git history). For the two big design items, ran research and filed it — spec structure for multi-product projects (two-layer/middle-path best supported) and how plugins collect feedback (Claude Code's /bug routes to Anthropic, not the plugin author, so GitHub Issues on the SI repo is the channel). Both stay parked, now research-backed.

**Queue changes:**
- Promoted [readme-install-refresh] (top), [cleanup-narrate-not-ask], [full-tag-placement-recheck] (woken from Parked + widened).
- Re-pointed [install-upload-path-clarity] to the new "add" upload screen.
- Re-tagged 6 Deferred-tests runnability labels user-run → observed: [ask-before-device-use], [test-routing-rule], [test-close-commit-only], [done-midclose-scope], [plan-close-dep-check], [reinstall-confirm-nudge].
- Dropped the 34-finding self-hosting notes inventory; added a build-checklist pointer to [self-hosting-support-during-setup].
- Added research pointers to [spec-sot-rethink] and [consumer-plugin-feedback-channel].

**Captures routed:** dropped [lint-citation-refire]; resolved + removed [deferred-test-runnability-tag-precision]; parked [shipped-slugs-breadth].

**Deferred tests confirmed this session:** [verbatim-at-checkpoint], [recommend-fold-on-agreement], [plan-winddown-rescan], [audit-vs-inline-read], [bound-opening-narration-aggregate] — all observed live in this /plan on the reinstalled host.
