# 99865ab — plan — [builds-read-the-queue-again] kept: the generated view and build blocks are retired; builds read the queue whole

The session's largest decision, the user's own, reached through a long exchange. Her grounds: build blocks duplicate rationale and bloat the queue; a build stripped of the why infers one and infers wrong — the "fresh short sessions" framing being the live specimen, where the true reason is compensating for a build model with less capability and cross-project knowledge; and Throughliner must not be designed for authoring Throughliner — the view fixed this project's own transcription pathology and generalised it into every consumer's architecture. The record's counter-case was put and accepted knowingly: the view's measured wins (the transcription leak, the ~56k-token read) return as costs, and the purpose-instruction-plus-boundary-rule is an untested replacement for a structural guard. The middle shape (view carrying rationale alongside blocks) was refused for keeping the duplication. Replacements designed in-session: the purpose instruction, the boundary rule with the user's unless-specifically-instructed carve-out, and the promoted authoring rule in capability-gap wording. [build-blocks-must-not-require-inference] merged in and deleted; SPEC's build-reads paragraph rewritten at the keep; the held comparison-post rewrite item updated — its view-announcing first paragraph is falsified, never posted, no public correction owed.

**Queue changes:** [builds-read-the-queue-again] filed and cleared; [build-blocks-must-not-require-inference] deleted as merged; [comparison-article-post-needs-rewrite] prose updated.
**Work processed:** kept — [builds-read-the-queue-again].
Rule gate: run at the keep — supersession recorded on the item; retired artifacts (generate_build_view.py, view files) named in its build block.
