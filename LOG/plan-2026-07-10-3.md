# [HASH] — /plan: recovered the G:→C: drive move and ran the cruise-control design session (resolved its last concerns, spawned 5 build batches)

The Google Drive root moved from the `G:\` stream drive to a mirrored local folder at `C:\Users\Alex\My Drive` (Drive switched to mirror mode) — a second, larger move after this morning's `Prioritiy projects\` rename fix. It re-broke the two path-based links a move always severs: the git worktree (git reported "not a repository"; the worktree `.git` pointer and the main repo's back-reference both still named `G:\`) and the desktop marketplace registration. Waited for Drive to finish syncing (the `.git` objects were mid-download and the tree was half-populated), then relinked both gitdir pointers to the `C:\` path, re-registered the local marketplace via the CLI, and reinstalled the plugin. Filed a capture for the stale `G:\` paths the move left across four docs — nothing about the running plugin is broken, so it's documentation cleanup.

With the repo working again, convened the [cruise-control] design session that had been gated behind a plan marker. Weighed collapsing cruise control into /next (which now runs autonomously to the readiness line) and decided against — attended-/next-to-the-line and a fully-unattended runner differ in the safety machinery that only earns its keep when no one is watching. Resolved the last open concerns: (11) a separate skill, not a /next continuation, because a /next mode would gate the feature behind finishing a full batch — worst exactly for the time-poor users cruise control serves; (2) and (7) dissolve under the work-line model, since cruise control executes pre-authored work lines top-down rather than inventing batch boundaries, making granularity and dependencies plan-time concerns. Decomposed the skill into five build batches, cleared them to run, and removed the now-satisfied plan-gate marker.

**Queue changes:**
- Filed capture [update-stale-drive-paths].
- Recorded resolutions for [cruise-control] concerns 11, 2, and 7 in the item.
- Added five cleared build batches: [cruise-skill-loop], [cruise-autonomy-adaptations], [cruise-redflag-gate-hardstops], [cruise-readiness-verification-pending], [cruise-spec-faq]; removed the "Plan session here" gate above [cruise-control].

**Work processed:** kept — the five cruise batches, above the cleared-to-run line. Filed (unprocessed) — [update-stale-drive-paths]. None deleted.
