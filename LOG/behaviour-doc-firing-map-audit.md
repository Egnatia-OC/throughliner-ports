# [HASH] — /next [behaviour-doc-firing-map-audit]: plugin-behaviour.md audited rule-by-rule for its firing map — each rule classified universal (earns every-session injection) / single-skill (trickle-down candidate, owning skill doc named) / two-or-three-skill (recorded as a finding feeding the progressive-disclosure restructure decision); queued rule-adding batches checked for planned-home-vs-firing-map mismatches; all findings routed to Captures.

plugin-behaviour.md is injected into every session, so each rule it carries is paid for in every session whether or not that session can fire it. The trickle-up arc had moved rules up on the premise that cross-skill rules belong in the every-session doc; this audit tested that premise rule by rule. For each rule it mapped the firing — can the trigger fire outside any skill, and which skills' steps fire it — and sorted the results into three bands: universal (earns the injection), single-skill (belongs in that skill's own doc), and the two-or-three-skill middle band (recorded for the progressive-disclosure restructure, no move proposed).

The read produced four findings, all routed to Captures. Two are trickle-down candidates that run against the trickle-up direction for the specific rules involved. Four rules fire only inside /next — "SPEC.md read-only during builds," "one build at a time," "at build completion only /done," and the context-long-mid-build nudge, about 111 words — and belong in next.md. The spec-entry pipeline rule fires only in /plan, about 33 words, and belongs in plan.md. The first collides with [trickle-up-next-md-duplicates], which plans to delete next.md's copies of two of those rules and leave the injection as their sole home — the audit's placement above that batch is what lets /plan correct it before it builds. A third finding records the middle band (Index entries, the unpark and staleness watches, empty-Batches, user-owns-scope, resume-reads-_build.md, the plan/next boundary) as firing data for the [behaviour-doc-size-watch] restructure decision. The fourth flags four queued batches — [scope-anchor], [scope-boundary-rule], [no-planning-in-execution], [audit-findings-bulk-approval] — that plan injection homes for rules whose firing is narrower than every-session, for /plan to weigh before they build. The other queued plugin-behaviour.md additions were checked and their injection homes match, since they fire outside skills via capture-making, approval moments, or memory-routing.

**Files touched:**
- plugin/si-plugin/docs/plugin-behaviour.md (read in full, every section — the audit edited nothing)
- the skill procedure docs (setup, plan, next + next-build/test/audit, done + done-build/test/audit/plan) — read to map each rule's firing across skills
- QUEUE.md — read the queued rule-adding batches as planned additions under the same test

**Routed to Captures:** 4 findings — (1) /next-only rules → trickle-down to next.md; (2) /plan-only spec-entry pipeline → trickle-down to plan.md; (3) middle-band cluster recorded for [behaviour-doc-size-watch]; (4) queued additions whose firing maps are narrower than the injection
