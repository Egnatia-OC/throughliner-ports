# 576506c — The brevity output style shipped: consented setup offer, per-project setting, one-line notice when off

This item reversed the 2026-08-14 style removal and was then itself part-superseded by the user within hours — the full chain matters and is carried here. The original removal killed a style for triplicating rules; the INBOX request that revived it named a different failure (Opus 5's default verbosity in doc-heavy projects) and proposed `force-for-plugin`, which the keep first accepted on the user's fix-consumers-actually-get framing. Asked how users would experience a silent override of their own chosen style, the user overturned it the same day: no `force-for-plugin`. What replaced it is her design with one improvement found in the docs — a project-level switch-or-keep offer at /setup, opt-out with acceptance as the default, written into the project's own `.claude/settings.local.json` so only Throughliner projects change. Appending our rules to the user's own style file was proposed by her and reversed on Claude's recommendation with her agreement — it edits a file the user authored. The triplication answer: the style's body is placement of rules that already exist in skill-nonspecific-rules.md, and the eviction half ran as [style-level-rule-dedup-audit] immediately after this build.

Built: `output-styles/brevity.md` (Throughliner Brevity, `keep-coding-instructions: true`, no force key); the setup.md scaffolding step with the existing-style check, verbosity rationale, project-scope statement and discussion invitation, plus the same offer on the top-up's missing-setting list; `_brevity_style_notice()` in session_start.py — one short line at openings where the style is not enabled (naming any other style set), silence where it is, matched on "throughliner" case-insensitively so namespaced values read as enabled; the README mention; the FAQ entry and index line in both templates, re-copied into `FAQ/`.

Tick: done, confirmed for everything testable now — the session-start suite passes all four branches and the style file carries no force key. UNCONFIRMED remainder: a real /setup run making the offer has not been exercised; it goes live only after a rezip and full restart.

**Files touched:** plugin/throughliner/output-styles/brevity.md (new), plugin/throughliner/docs/setup.md, plugin/throughliner/hooks/session_start.py, README.md, plugin/throughliner/templates/faq-template.md, plugin/throughliner/templates/faq-index-template.md, FAQ/faq.md, FAQ/index.md, resources/testing/test_session_start_output_style.py (new)
**Routed to Captures:** none from this item
Rule gate: run — the style is method rule text at system-prompt level, the strongest always-loaded distribution there is; its content is authored in this build from rules that already exist rather than new ones, and the matching eviction is carried by [style-level-rule-dedup-audit] plus the SPEC paragraph replaced at the keep.
FAQ: updated — "What is the brevity style Claude offered me at setup?", shipped in the templates and re-copied into FAQ/.
