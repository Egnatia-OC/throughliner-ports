# 45b7e49 — /plan: cleared the plan-marker's six Taskflow audit findings — promoted F1–F5 into a new [migration-aware-setup] batch (detect-and-frame), parked the spec-edit footer finding as a recurrence watch, and filed + gated a new bungled-SPEC audit-harvest capture

This /plan opened on the queue-top plan-marker requiring the two Taskflow audits' findings be processed before the /setup- and SPEC-related batches build, since the findings could reshape them. All six were routed.

The five /setup re-run findings (F1–F5) cluster onto one missing capability: /setup has no concept of a project already set up under other names, so an older-vocabulary or foreign source (content but no SPEC.md) is misread as a fresh start, and the session-start hook makes the same misread first. They were promoted as a single batch, [migration-aware-setup], rather than four — they all edit setup.md's migration path and session_start.py's detection, so separate batches would race on the same files. Two reach decisions shaped it. First, detect-and-frame only: /setup and the hook recognize a possible migration and say so, then hand the mapping to Claude's judgment — chosen over a guided old-SI mapping table or open-ended foreign-source guidance, which carry more code and reliability risk for a path the framing already covers. Second, generic softening over marker-spotting: reword so "content but no SPEC" reads as a possible migration, no hardcoded old-vocabulary marker list — chosen because it covers any source, old-SI or foreign, for less code. F3 (a blind UX.md→SPEC.md rename imported a role mismatch) and F4 (the dead path-block detour) fold in as guardrail lines in the migration framing — check each old doc's role fits before renaming, and state up front that docs live at the project root. F4's dead-feature-removal half was already done (the current template carries no path-block). F5 (setup narration leaked internal terms) was upgraded from "mild, maybe drop" to a real promote after verifying its claim: in the unadopted state the session-start hook returns before loading the behaviour rules, so during a first-run /setup the plain-language Vocabulary rule genuinely isn't in context — setup.md needs its own plain-language guard. F5 folded into the same batch.

The single first-spec-edit finding — a spec-edit silently removed a stale doc footer not in its described work — was parked as [silent-out-of-scope-cleanup-watch] rather than acted on: the scope rule already forbids silent out-of-scope changes, so it's a compliance miss, not a missing rule, and the counter-reading is strong (stale residue at the exact edit site, arguably correct to remove). A recurrence trigger fires if a second instance appears. Before removing that audit's header, its embedded testing outcome was preserved: the [spec-edit-batch-type] deferred-test line now records its first half confirmed live (a /next spec-edit edited SPEC unblocked because it listed SPEC.md), second half still pending.

Mid-session the user reported a new Taskflow session had dropped — not E2E testing but an audit of the bungled SPEC (what to move, change, or cut). It was filed as [taskflow-spec-audit-harvest] with its transcript path, and the plan-marker was extended to gate the setup/SPEC batches on harvesting it too, since its findings may reshape the same batches the marker already protects. The marker's first clause is now satisfied; it stays for the pending harvest.

**Queue changes:**
- Added **[migration-aware-setup]** after [consumer-spec-model-sync] (F1–F5; files: setup.md, session_start.py, faq-template.md, faq-index-template.md).
- Extended the queue-top plan-marker to also gate the setup/SPEC batches on harvesting [taskflow-spec-audit-harvest].
- Updated the [spec-edit-batch-type] deferred-test line: first half confirmed, second half remaining.

**Captures routed:**
- F1–F5 (Taskflow /setup audit) → promoted into [migration-aware-setup].
- Spec-edit footer finding → parked as [silent-out-of-scope-cleanup-watch] (recurrence watch).
- New capture [taskflow-spec-audit-harvest] → filed (processed, above the divider).
