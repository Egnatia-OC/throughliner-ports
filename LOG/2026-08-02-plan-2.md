# 1d089e8 — QUEUE.md — settled Opus 5 phase-2 authoring register + greenlit the user-line-cluster fixes for a /next run

This advisory-directed /plan did two things. First, it ran the design pass phase 2 was waiting on — docset B's authoring register. Grounded in resources/research/pseudocode-format-for-skill-docs.md, it settled a **layered hybrid** (typed signature + concrete template + thin prose) with **SaP-style typed pseudocode as the primary register**, and folded the "pick against real 5-series output" empirical check into phase 2's *first build step* (author one representative section, checkpoint how the 5-series reads it, fall back to PromptMN annotated prose only if it reads badly) rather than a separate bake-off pass. The user pushed back on pre-naming a fallback register; it was demoted to a checkpoint-only option, since the hybrid's thin-prose layer already absorbs most "reads badly" cases. That cleared [opus5-docset-b-authoring]'s last lift-condition — now ready to build. Second, the user reprioritised the session to the user-line-handling fixes + Opus 5, wanting /next to run unattended without stopping on deprioritised user lines. So the close greenlit three user-line-cluster fixes above the marker for one /next run and moved the two `[user]` handover items that sat above the marker below it, each with a recorded lift-condition, so /next builds the three fixes and stops clean at the marker with no handover. Intended sequence: /next → /done → test rezip → next /plan starts docset B authoring.

**Queue changes:**
- [opus5-docset-b-authoring] — authoring register settled (layered hybrid, pseudocode-primary, empirical check folded into first build step); lift-condition cleared → ready to build.
- [handover-abandonment-and-terminology] — build scope extended with the bundle-carve-out kill + [SEQUENCE]-wording clarification folded in from the deleted standalone; greenlit above the marker (top pick).
- [user-line-underfiling-asymmetry], [user-completion-ask-friction] — greenlit above the marker for the same /next run.
- [report-url-404], [working-mode-render-verification] — moved below the marker with recorded lift-conditions (genuine pending prerequisites).
- Consumed advisory [advisory-plan-opus5-phase2] cleared; new advisory [advisory-plan-opus5-phase2-docset-b] filed, pointing the next /plan at docset B authoring after the rezip.
- Rejected this session (why-pipeline): a separate bake-off pass before phase 2, and a pre-named annotated-prose fallback register — both dropped (phantom dedicated pass; hybrid prose layer already covers "reads badly").

**Work processed:** deleted [handover-bundling-fights-one-at-a-time] (folded into [handover-abandonment-and-terminology]); [line-anchored-link-dead-in-desktop-app] and the rest of Unprocessed left unprocessed (session scoped to user-line fixes + Opus 5).
