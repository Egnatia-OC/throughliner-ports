# [HASH] — Close cost kept on option (b): the sibling-citation clause already covers build closes, and done-build.md is amended to say so

The user asked for option (b) to be checked first, and the check moved the item: the sibling-citation clause shipped in the seventeen-item run's own commit into done.md's shared "LOG entry files" section, worded flavour-neutrally — so it already reaches build closes by residence. The gap is that done-build.md's entry step never points at it and its "one entry per built item is unconditional" sentence reads as forbidding grouping, though the two are compatible (a sibling keeps its own entry file and cites the carrier). Options (a), (c), (d) deferred, not rejected: (b) first, then [brevity-amendment-outcome] measures. Full settlement and build block on the item in QUEUE.md.

**Queue changes:** [close-cost-scales-with-run-size] moved Unprocessed → Processed, cleared, first in the region.
**Work processed:** kept.
