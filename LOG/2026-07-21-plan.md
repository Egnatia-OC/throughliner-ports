# c803253 — /plan: merged shelving-model captures into [single-shelf-model-hardening] build + added [capture-disposition-narration-discipline]; filed 3 behaviour-bug captures; synced SPEC's Unprocessed definition; reordered Unprocessed

This planning session opened by capturing two testing observations about live plugin behaviour, reordered the whole Unprocessed backlog by unlock-potential (at the user's request), then processed the top shelving-model cluster.

Core work: [defer-for-design-process] and [claude-invents-shelving-hardening] were merged into one build line, [single-shelf-model-hardening]. The decision they'd circled: Claude keeps inventing new "shelves" because the docs define Unprocessed as "captured, not yet discussed," which contradicts the standing rule that undesigned-but-worthwhile work stays a capture in Unprocessed — such work *has* been discussed. Fix: no new state or tag; redefine Unprocessed as "not yet fully processed," state the single-shelf rule out loud (one holding place, one shelving move), add an anti-invention guardrail carrying the three logged instances as its why, and give recurring meta-items proper homes (SPEC/CLAUDE.md/docs, not permanent queue lines). The intuitive alternative — a new "fold"/"defer" tag or state — was rejected as exactly the proliferation the item exists to fight.

[capture-disposition-over-narration] was kept as a small separate line, [capture-disposition-narration-discipline]: the model half was absorbed by the hardening line, leaving only a narration-discipline fix (don't lecture the user on shelving mechanics; drop day-scoped "won't be designed today" for "capture now, design later"). Kept separate to avoid mixing a narration concern into the 9-file model reword.

SPEC.md line 23's Unprocessed definition was synced in-session to match the redefinition (spec-sync gate).

Three behaviour-bug captures were filed from testing: [next-double-start-confirm], [plan-assumes-close-after-last-capture], and [plan-repeated-progress-approval] (cross-linked to next-double-start-confirm as one family).

**Queue changes:**
- Reordered all of Unprocessed by unlock-potential then importance.
- Merged [defer-for-design-process] + [claude-invents-shelving-hardening] → new Processed build line [single-shelf-model-hardening] (top of Processed, greenlit).
- Kept [capture-disposition-over-narration] as [capture-disposition-narration-discipline] (Processed, greenlit).
- Filed 3 captures: next-double-start-confirm, plan-assumes-close-after-last-capture, plan-repeated-progress-approval.
- Synced SPEC.md line 23.

**Work processed:** kept — defer-for-design-process, claude-invents-shelving-hardening (both merged into single-shelf-model-hardening); capture-disposition-over-narration (→ capture-disposition-narration-discipline). Deleted — none.
