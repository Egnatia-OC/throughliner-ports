# 576506c — Ladder-post walk-through: third subject rewrite drafted, posting deferred to tomorrow on the pacing rule

`[user]` item [discord-post-context-adjacency], walked through in the 2026-08-22 multi-item /next run. The item's required third subject rewrite happened here: the post's subject is now the whole sequence — six rungs cut to three, the bottom rung then found unreachable in practice, and the ladder rebuilt as four rungs so it cannot starve — the got-wrong-twice-and-corrected register the item asked for.

Verification before drafting: the four-rung ladder is shipped and recorded as confirmed live on the installed host, and the head of the pacing chain ([discord-post-cycle-awareness]) was posted and closed on 2026-08-21, so nothing held the item and today was the earliest posting day.

The user deferred posting to tomorrow — their one-a-day pacing, with the comparison-article post also now in their hands. The item stays in the queue; the draft below is what they post, and the final yes on sending happens then. No line in the outbound register yet, because nothing has been approved to leave the machine.

The draft, verbatim, so tomorrow's session does not recompose it:

> **Throughliner update: the queue-ordering ladder, got wrong twice and rebuilt so it can't starve**
>
> When /plan orders your unprocessed ideas, it works down a ladder of rules. That ladder has been rebuilt twice, in public view, and the sequence is the honest story.
>
> First it had six rungs, and three of them asked Claude to *weigh* something — and weighing is exactly where a mechanism quietly stops being reproducible. So it was cut to three: an uncleared security flag first, then whatever unblocks the most other work, then longest-first. Every surviving rung read a computed field or subtracted two line numbers. Simpler, and every ordering decision became checkable.
>
> Then the bottom rung turned out to be unreachable in practice: the rung above it ranked everything and never ran out, so old short items could sit unreached forever while the ladder claimed to reach them "in principle". That's a starvation bug in a rulebook rather than in code, and it got the same treatment: the ladder is now four rungs — the flag, the unblocker, then items that are *both* long and old (a set small enough to actually finish), then an alternating rung that takes the oldest items with every other pick required to be a long one. Alternation is what makes the neglected work reachable at all, and it needs no tuning number — a weight would just be a guess.
>
> Still true throughout: no rung asks for judgment. Ordering your queue costs arithmetic, and you can check any of it.
