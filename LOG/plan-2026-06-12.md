# LOG entry — plan-2026-06-12

## [HASH] — /plan: 4 captures routed — deferred tests seeded into [deferred-tests-structural-home], pre-flight verbatim-send folded into [capture-verbatim-first], [push-marker-hard-direction] promoted

Three captures waited from earlier sessions; a fourth was filed and processed in-session. The two deferred-test captures (pre-scope-lock abort, old-log retrieve) moved into [deferred-tests-structural-home]'s seed list as their own bullets. A fold into the seed-list sentence was drafted first; the move won because it keeps the filed wording and reasoning intact — the why-pipeline favours carrying original prose over a compressed re-author. Each test kept its firing condition as plain prose, since a Blocked-by header has scan semantics that don't apply inside a batch.

The /next pre-flight waiting capture folded into [capture-verbatim-first] as a second build entry — same fix, second doc: send the top batch verbatim before the blocker-gate thinking starts, with the mechanical halt-marker check allowed to run first so a halted queue sends the halt instead. The batch title stretched to "Present the working item verbatim-first"; the slug is immutable and stays.

The session's new thought: the push marker reads as a solid barrier but is only hard in one direction. Decided-but-unshipped standards shaped this session's own behaviour — blockquote displays, verbatim-first quoting, a why-pipeline judgment call — because in-repo sessions read the queue and the discussion, not just the installed plugin. The hard case is host-side reads (an audit of injected rules, a live hook test), which genuinely wait for push + reinstall; everything else about the line is a ship-by aim. The risk worth recording: a session treating the line as solid could suspend decided reasoning ("not shipped yet"), breaking the why-pipeline. Promoted as [push-marker-hard-direction], targeting this project's CLAUDE.md push-marker convention. This entry itself applies the principle — written as a per-entry file under the committed-but-unshipped scheme.

**Queue changes:**
- [deferred-tests-structural-home]: first build entry re-counted; two seed bullets added (the moved-in deferred tests from [next-pre-scope-lock-abort] and [drop-log-per-release-split])
- [capture-verbatim-first]: title stretched to cover /next; rationale extended; next.md pre-flight build entry added; test widened to both skills
- [push-marker-hard-direction]: promoted from a same-session capture, appended at the end of Batches — no dependency applies

**Captures routed:** 4 — two moved into a batch's build list, one folded into an existing batch, one promoted. Captures now empty.

**Other:** pending log hash backfilled to 4c00801 at session start.
