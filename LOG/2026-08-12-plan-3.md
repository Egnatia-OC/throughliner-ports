# d73b53f — Twenty-one items processed on the decay rung: three captures deleted as already fixed, a dead pre-flight check found by testing a claim, and the queue given a way to say "must run alone"

This session opened with one item holding other work up and no red flags, so the ladder fell to unblock-potential for a single item and then to decay — captures filed on 2026-08-11 and 2026-08-12 whose evidence was still live. That rung did real work rather than acting as a tiebreaker, and three things it produced are worth more than the item count.

**Testing a capture's claim reversed it, and found a dead check.** [write-over-unread-untracked-file] reported that the /next pre-flight's glob `*_build.md` "did not match the plain `_build.md`". Measured against `fnmatch` and PowerShell alike, it does match — what it cannot match is `_build-<session-id>.md`, which is the only name a working file has had since the session-scoped rename shipped. So the interrupted-build check has been silently reporting "no interrupted build" every time, and a genuinely interrupted run would be invisible to it. That also settled a disagreement the original capture recorded and could not resolve: the glob and the session-start hook contradicted each other, and the hook was right.

**Three captures described defects that no longer existed.** [mover-misreports-marker-side-on-move-section] was fixed in `7c9922a`, with the fix's own comment carrying the same diagnosis the capture reported and a purpose-built passing test; its sibling was fixed in `e5d169b`; and both assertions named by [session-start-payload-over-its-headroom] now pass. The likely cause is the host/target gap — the session that filed them was running an installed plugin older than the fixes, so it observed real symptoms from a stale build. This is recorded as a finding rather than filed as work, because the keep-step sharpening decided in this same session already catches it: an item asserting how a mechanism behaves now has its mechanism read before the build is described. That clause was, in effect, tested three times on the day it was written.

**The queue gained a way to express solitude.** [rename-to-throughliner] carries an instruction in its prose that it must not share a run, protected only by sitting last in the ready region. This session cleared twelve items ahead of it, which made visible that position never bound anything — /next runs to the readiness line regardless. Below-the-line needs a named blocker and nothing blocks the rename; `[freeform]` means work /next must not build, which is the opposite of what is wanted. So a `Runs alone` marker was designed, read by /next as a run bound. It cannot bind the very next run, because /next reads the installed plugin and the host must be reinstalled first — recorded in the item rather than left to be discovered.

Two decisions turned on evidence this session generated about itself. [plan-file-never-created] weighed repealing the planning-working-file requirement on the ground that a twenty-item session had completed without it; this session's own file carried the order, the reasoning behind it, every disposition, and a mid-session corruption note, so the repeal was refuted rather than merely declined. And [close-time-captures-assert-unverified-mechanism] weighed doing nothing on the ground that the right thing happened both times it mattered; twice in this session reading a mechanism changed the item, so the clause was written.

The session also produced three failures of its own, all in the same place. The keep-step's two-write move corrupted QUEUE.md three times — once silently undoing a move, once orphaning a heading, once splicing a heading into a neighbouring item's paragraph. Every one was caught by re-running the queue digest, which nothing in the procedure requires. The cause is structural: after the first write the file holds two near-identical copies and the removal is an exact-string match, so the obvious string is ambiguous by construction. A three-edit sequence was designed — mark the original with a placeholder heading, write the destination copy, delete by the placeholder — and it worked on every attempt afterwards, including the hardest case.

**A parallel session committed this session's in-progress work.** Partway through, `432fb22` — titled for a post-close tail filing four captures — swept in roughly eighteen of this session's processed items and all of its captures. Nothing was lost and no slug is duplicated, but the record now attributes a day of planning to another session's commit message. That became a third instance on [close-sweeps-unattributed-hand-edit], and it falsified the fix that item had just been given: naming the staged files produces the word "QUEUE.md", which is true and says nothing about whose work is inside. The item now carries an explicit instruction not to ship that line believing it covers this case.

One premise was contested and then re-confirmed. Presented with [close-sweeps-unattributed-hand-edit], the user first said they had not made the hand edit it describes, then recognised the occasion and confirmed they had. Both halves are recorded in the item, because the claim had stood for two days on nothing but what a session believed it saw, and the commit it names cannot settle it either way.

**Spec-sync.** No SPEC sentence is wrong today — nothing shipped. Two kept items will change product truth when they build, and both now name SPEC.md among their files so the spec moves in the same commit as the behaviour: the `Runs alone` marker adds a second run bound to a SPEC sentence that currently states the readiness line is the only one, and the superseded-research check adds a flag to the digest output SPEC describes. Separately, and not created here: SPEC already says captures are written before they are reported, while the shipped close still shows them for approval first — a pre-existing drift that [wind-down-contradicts-write-first] resolves by moving the procedure to match SPEC rather than the reverse.

**Queue changes:** seventeen items kept into Processed above the cleared-to-run line, one held below it naming its blocker, three deleted. [no-way-to-mark-work-that-must-run-alone] placed first in the ready region; [rename-to-throughliner] left last, as its own text requires. Two SPEC.md file-list additions made at the spec-sync gate. Two dangling slug references repaired where deletions left them pointing at nothing. The consumed forward advisory cleared — it oriented this session, which processed the item it named second.

**Work processed:** kept — [plan-quiet-list-fixture-also-stale], [contradicted-flags-mixed-disposition-runs], [board-header-counts-inert-signals], [wind-down-contradicts-write-first], [gate-still-declares-the-old-ceiling], [no-link-from-research-to-items-scoped-on-it], [index-cap-sample-was-three-entries], [plan-file-never-created], [wind-down-rescan-run-out-of-procedure], [advisory-read-but-not-surfaced], [wind-down-caveat-uses-an-unverifiable-limit], [session-date-start-end-or-both], [close-time-captures-assert-unverified-mechanism], [keep-move-second-write-corrupts-queue], [close-sweeps-unattributed-hand-edit], [write-over-unread-untracked-file], [no-way-to-mark-work-that-must-run-alone]; held below the line — [stop-hook-fires-on-drafted-not-filed-captures], blocked by [wind-down-contradicts-write-first], which will most likely delete it rather than build it; deleted — [throughline-value-articulation] (folded into [rename-to-throughliner]'s positioning half, with its content relocated first), [mover-misreports-marker-side-on-move-section] and [session-start-payload-over-its-headroom] (both already fixed).

**Routed to Captures:** [keep-move-second-write-corrupts-queue] and [no-way-to-mark-work-that-must-run-alone] (both filed and then processed in the same session), [spec-ask-in-build-reads-as-a-violation], [half-cap-assertion-has-no-derivation] (relocated out of a deleted item rather than raised fresh), [python-resolves-to-inkscape-interpreter].

**Rule gate:** not needed — this session authored no rule in the method's own text. Every rule decided here was written into a queue item as work for a later build, and each of those items records that the gate runs at build time, where the rule is actually authored.
