# LOG entry — audit-definition

## [HASH] — /next [audit-definition]: plan.md thinking-work rule and audit sizing gate, next-audit.md intro and read-the-target step — audit batch type redefined by its output contract (findings route to Captures, no direct edits) instead of "systematic read of target docs"; the thinking-work rule's *build* qualifier dropped and audit recast as a separate category, not an exception; closing test sharpened to apply per seeded item (check-items must be finding-shaped, decisions resolve at planning time); "Open every file" → "Read every artifact"; setup.md and done.md swept for stray audit-as-docs-read framing

The audit batch type was written from the case where the plugin audits its own procedure docs, and all four defining passages assumed that shape. plan.md's thinking-work rule called audit "the one exception" and defined it as a systematic read of target docs. The sizing gate's example of a vague target was "the procedure docs." next-audit.md framed the work as opening files. The real defining property was always the output contract: findings route to Captures, and the audit makes no direct edits to what it reads. That property is what separates audit from build and test, and it holds for any artifact type — procedure docs, the user's spec, code, UI flows, workflow output. The rewrite keys the definition on that contract. The thinking-work rule loses its *build* qualifier, since thinking work can arrive in any batch shape. Audit becomes a separate category rather than an exception — the exception framing is what previously let decision-shaped thinking work pass as audit batches, observed at [output-tag-audit], where a seeded reconciliation item's resolution was a planning decision made mid-/next. The closing test is now two-sided and applies per seeded item: output is decisions → planning work, run it in /plan; output is findings from a systematic read, routed to Captures → audit batch. Seeded check-items must be finding-shaped; decision-shaped checks get resolved at planning time, not queued. The sweep of setup.md and done.md found nothing to revise: setup.md has no audit references at all, and done.md's two are the router line and the LOG filename rule — the docs-as-target framing was confined to plan.md and next-audit.md. One small generalization rode along in next-audit.md's read step: evidence references became "precise references (file:line for files)" so a non-file audit isn't asked for line numbers. Procedure substance is unchanged — read many, propose many still describes how audits run. Pre-flight also backfilled the previous session's placeholder hashes to e51cf40.

**Files touched:**
- plugin/si-plugin/docs/plan.md — thinking-work ground rule rewritten; sizing-gate vague-target example generalized
- plugin/si-plugin/docs/next-audit.md — intro leads with the output contract and names artifact variety; read step says "Read every artifact"
- QUEUE.md — [audit-definition] batch removed at scope-lock
- LOG/plan-2026-06-12-4.md, LOG/index.md — placeholder hashes backfilled to e51cf40 at pre-flight
- _build.md — created at scope-lock, deleted at close

**Routed to Captures:** none
