# c13886c — /plan: reordered Unprocessed by dependency, kept 3 release-prep builds, and set the undesigned-work-stays-a-capture rule

This /plan session reordered the 15 Unprocessed captures by dependency at the user's request, then processed the first four top-down before closing. The through-line was tightening how not-ready work is held: the user established that undesigned work stays a capture in Unprocessed rather than being processed into Processed (even below the cleared-to-run line), because processing implies it's been thought through. This is a deliberate anti-proliferation stance — captures is the single holding place, a workaround for Claude's tendency to invent ad-hoc shelving. Acting on it, the stranded [two-section-model-reconciliation] (Half B, an open design fork sitting below the line where neither /next nor /plan would pick it up) was moved back to Unprocessed, reworded to just the live fork.

Three items were designed enough in-session to become build lines. [feature-additions-require-readme-edit]: a host-only trigger (piggybacked on the SPEC-sync discipline) that syncs the README feature list when a user-facing feature is added/removed, plus fixing current README staleness (missing /cruise, stale "spec read-only" and "batch" vocab). [plan-reorder-by-unlock-potential]: a /plan-close step that walks both sections and auto-reorders (Unprocessed by unlock-potential, Processed by build-order) and narrates — fork resolved to auto-reorder-and-narrate, not ask, since ordering ownership already sits with Claude. [next-recommendation-not-queued]: /next and /done file their "do X next" recommendation as an advisory capture worded "Last session advises processing X next", placed atop Unprocessed and cleared once /plan and the user agree order — advisory, not dictation.

[self-hosting-reproducibility] was left a capture, enriched with the Codex cross-harness gap (SI's self-hosting rituals assume Claude Code); the new [defer-for-design-process] capture asks to make "undesigned work stays a capture" an official process. "Design session" phrasing was stripped from the queue — it named a mechanism that doesn't exist. Items 5-15 remain unprocessed.

**Queue changes:**
- Reordered all 15 Unprocessed captures by dependency
- Kept as builds (Processed, above marker): [feature-additions-require-readme-edit], [plan-reorder-by-unlock-potential], [next-recommendation-not-queued]
- Left as capture: [self-hosting-reproducibility] (+ Codex wrinkle)
- Filed: [defer-for-design-process]; [claude-invents-shelving-hardening] (wind-down)
- Moved [two-section-model-reconciliation] from Processed-below-line back to Unprocessed (reworded to open Half B fork)
- Stripped "design session" phrasing from 5 queue locations

**Work processed:** kept 3 (all → Processed as builds); 1 left as capture; 1 returned to Unprocessed; 1 new capture filed. Items 5-15 unprocessed remain.
