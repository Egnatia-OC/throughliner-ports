# ae84933 — Chat-level record for a three-item build run: a run bound the view could not see, and a close that overrode a recorded disposition

The `Also in this chat:` record for the run that built [advisory-step-does-not-fire], [log-entry-kind-not-distinguished] and [split-action-defeats-the-bands-in-aggregate]. Written as its own entry because the close wrote several.

**Also in this chat:**

**The run's bound was invisible to the file the run is supposed to read.** `next.md` states two bounds — the cleared-to-run line, and a `Runs alone` marker, which ends the run before the marked item — and states that a run reads the generated build view and never QUEUE.md. `generate_build_view.py` never emits `Runs alone`; the literal does not appear in the script. [rename-docs-b-folder] was sitting fourth in the cleared region carrying the marker, deliberately placed so a run would stop before it. A run following the procedure as written would have swept it into a nineteen-item run and made every path in its own scope-lock list stale mid-build. It was caught only because the pre-flight grepped QUEUE.md for the literal rather than trusting the view. Filed as [build-view-drops-runs-alone].

**A truncated read produced two wrong outputs, and neither announced itself.** The build had to transcribe each item's rule-gate disposition, which the view does not carry. It read each item through a grep window of about 36 lines; all three items run past 50. Both the labelled `Rule gate:` line and each item's FAQ disposition sit below where the window stopped.

The first consequence was a capture asserting that [advisory-step-does-not-fire] carried its gate reasoning as unlabelled prose with no `Rule gate:` line. It carries one. That capture was deleted at this close, the same day it was filed.

The second reached Alex. The close concluded the FAQ gate fired on that item, put the question to her, and got approval to add four FAQ files and write a consumer entry. The entry was written and the copies verified. Then the item's own disposition turned up: *"No FAQ entry, on the FAQ trigger's own test: a consumer sees one more line in their session record and does nothing different."* Planning had considered the identical question and answered it the other way. All four files were reverted on her instruction and the recorded decision restored.

**The rule that would have caught it is already shipped.** The always-loaded rules say to page the whole file before reasoning over it, and to name a read that stopped short rather than reasoning from it quietly — with the reason stated in the rule itself, that a truncated read looks like a complete one to whatever reasons over it. That is exactly what happened. This is the seventh recorded instance in this corpus of a correctly worded rule with a stated site failing to fire, and the corpus's conclusion about that class stands: stating it again, or harder, is not a candidate direction. Filed as [build-reads-item-through-a-truncated-window].

**A third thing that class exposes, worth stating separately.** Alex was asked a well-formed question and answered it. Nothing in the ask told her a decision already existed, because the session asking had not read it. Whatever else changes, an ask that re-opens a dispositioned question should say that it is doing so. Recorded inside that capture rather than as its own item.

**The mailbox scan's false positive was met and not acted on.** `session_start` reported `INBOX/sent.md` as waiting mail with a directive to archive it after reading. It is this project's own outbound ledger, not correspondence. It was left in place, and no duplicate capture was filed — [sent-record-surfaced-as-waiting-mail] already covers it.

**A rename was made and reverted inside one item.** `shipped_slugs()` was briefly renamed to `record_kinds()`, which is the honest name for what it returns after the build, and the rename was reverted: four other queue items and QUEUE.md prose cite the function by name, and the rename was scope the item did not grant. Recorded in that item's own entry too, because it changes how a later reader should read the function's name.

**Uncommitted work from another chat rides this commit.** `LOG/2026-08-21-convert-cleared-items-to-build-blocks-2.md` was already modified at session start — a backfilled hash and a tail describing edits made in a planning chat running alongside. That is another session's finished work, read as expected rather than as a broken tree, and folded into this commit as the file-safety rules direct. Its own tail already records the two-chats-one-tree problem that produced it.

**The advisory step, built in this very run, collided with itself at this close.** Its own rule says to file the note under a reserved unique slug at the top of Unprocessed, and separately — correctly — that clearing a spent note is the next /plan's job rather than a close's. Those hold together only while a planning session runs between every pair of closes. This close arrived to find a spent note in the slot, pointing at a freeform conversion that shipped at `461c999`. Filing alongside it would have put two entries under a slug that must be unique, which the lint flags and which stops the queue mover dead on the whole file. Resolved by deleting the spent note, filing this close's own in the freed slot, and moving it to the top — three mover calls where the step describes one. Filed as [advisory-step-collides-with-a-spent-note].

**A stale sentence found by the staleness sweep, deferred rather than fixed.** [plan-entry-split-wording-disagrees] is cleared to run and describes `shipped_slugs()` as resolving shipped-ness from filenames — which this run changed underneath it. Its refusal's conclusion is untouched, since the argument turns on a decision producing one *file* rather than on how a record's kind is read; only the supporting description is now incomplete. Rewriting reasoning is planning work, so it was filed as [plan-entry-split-refusal-describes-old-mechanism] rather than edited here.

**Checks run at this close.** `py resources/rule_signals.py .` — four checks run, nothing found; run because the staged paths include `docs-b/`. The hook-suite gate did not fire, since no file under `plugin/throughliner/hooks/` is staged, but all nine suites under `resources/testing/` were run anyway and pass.

**Routed to Captures, this chat:** [build-view-drops-runs-alone], [spec-silent-on-advisory-disposition], [build-reads-item-through-a-truncated-window], [plan-entry-split-refusal-describes-old-mechanism], [advisory-step-collides-with-a-spent-note]. One further capture, [gate-disposition-written-as-prose], was filed mid-run and deleted at this close as false.

Rule gate: not needed — this entry authors no rule. The three built items carry their own dispositions in their own entries.

Advisory: filed — [forward-advisory], recommending a planning session before the next build run, and replacing a spent note in the same slot.

FAQ: not needed because nothing here is a shipped change. The one FAQ edit this chat made was reverted within the same close.
