# 576506c — The standing audit set built: audit-lag fires mechanically, the sweep-due measurement is deleted, the history clause lands

The user's strategy (2026-08-14): dedupe first, restyle, then maintain the corpus routinely — and the design's own correction, that admission gates cannot prevent drift, so the goal is audits cheap enough to run without dreading them rather than none at all. The set the keep settled, by the item's own test (mechanical where detectable, audit only where judgment is irreducible): retired-term naming and near-duplicates stay mechanical; one new mechanical check, audit-lag, is the whole trigger design — `rule_signals.py` reads git for commits touching the gate's staged-path set since the most recent compliance-audit LOG entry and files one `[audit]` capture scoped to the changed files, satisfied while one is open. Its trigger is a fact, not a number, and no audit in the set depends on anyone remembering. One audit class runs as ordinary `[audit]` work against the checklist: delta scope, parent-axis only, four lenses — with the fourth, the user's own (2026-08-22): history is rationale that does not read as rationale, detected by the delete-and-read test with no "because" required.

Built: `signal_audit_lag()` replacing the AUDITED entry (deleted, not renamed — its trigger was the repealed ceiling and a measurement that can never fire is one nobody reads); header counts now five firing checks and one measurement, in the code's docstring, labels and clean-run note; the checklist's trigger and scope sections restated (audit-lag as the trigger, delta scope as the routine form, the corpus sweep not the standing shape) and Lens 4 written out with the SPEC output-style paragraph as its founding instance; CLAUDE.md's rule_signals counts updated at three places and the "Rationale lives outside the operative rule" passage gains the history clause as a subordinate unit. Refusals honoured: no time- or count-based trigger, no standing corpus sweep, no escalation on a standing capture.

The check was exercised live in this run and its first run exposed a real weakness: the boundary matched yesterday's *planning* record named for the audit item, which would silence the check before the audit itself ran — filed as [audit-lag-boundary-matches-processing-record] rather than patched in-flight.

Tick: done, confirmed — the board runs with 5+1 counts, the existing rule_signals suite passes, and audit-lag was exercised against the live repository.

**Files touched:** resources/rule_signals.py, resources/method-compliance-audit-checklist.md, CLAUDE.md
**Routed to Captures:** [audit-lag-boundary-matches-processing-record]
Rule gate: run — two amendments and one eviction, dispositions transcribed from the item: (1) CLAUDE.md's "Rationale lives outside the operative rule" gains the history clause as a subordinate unit of that named parent — failure evidence: the SPEC output-style paragraph, found and rewritten 2026-08-22, having survived every earlier audit; (2) CLAUDE.md's rule_signals description updates its counts. Evicted: the "is a corpus sweep due" measurement in rule_signals.py, deleted in this build.
FAQ: not needed because this is host-only; consumers never author method rules.
