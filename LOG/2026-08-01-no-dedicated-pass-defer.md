# [HASH] — Name skip-to-defer as the only defer, and ban the phantom "dedicated pass" framing

A live /plan three times narrated that a big undesigned item "deserves its own dedicated pass / focused session" — a container SI doesn't have. The generic-Claude prior ("big/undesigned → give it a focused session") fills the vacuum whenever nothing names the SI move or forbids the phantom one.

Three changes: (1) plugin-behaviour.md gained a "No dedicated-pass state" named anti-pattern before the anti-invention guardrail — it states plainly that no dedicated-pass/its-own-session state exists, that item size earns no special session type, and bans the "needs its own pass/session" narration, carrying the recurrence-why so it holds on 4.8. (2) plan.md's Step 2 checkpoint gained a "Recommend skip-to-defer when an item won't design out this session" block, so Claude can *propose* sharpen-then-skip (skip was previously only a user-picked off-ramp). (3) The skipped-this-session record was made explicit: it's the _plan.md slug and nothing else — no durable queue marker; next session the item is ordinary Unprocessed again.

The existing FAQ skip/drop entry was extended to cover Claude-recommended skip and the no-"own session" model, rather than adding a new entry.

**Files touched:**
- plugin/si-plugin/docs/plugin-behaviour.md — "No dedicated-pass state" anti-pattern
- plugin/si-plugin/docs/plan.md — recommend-skip block + skipped-record clause
- plugin/si-plugin/templates/faq-template.md — extended the skip/drop planning entry

**Routed to Captures:** none
