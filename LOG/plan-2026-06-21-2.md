# [HASH] — Plan: corrected the deferred-test gate (reinstall, not release push), promoted [surface-installed-host-version] + [blocked-by-lint-flags-shipped-citations], rolled [setup-e2e-verification] test batch, filed [deferred-test-runnability-tag-precision], confirmed [deferred-test-seams-fix] live

This planning session's pivot was the deferred-test roll. The roll first concluded "nothing rolls" because the deferred-test convention says host-side work "goes live after push + reinstall" — but the user pushed back: a private test-build reinstall already makes host-side behaviour live (the section's own test-build confirmations prove it), so a release push isn't the gate, the reinstall is. Only the two external marketplace lines truly need a remote push. That correction drove two outcomes. First, [surface-installed-host-version] was promoted: the session-start hook already reads the installed version for its drift check but never surfaces it, so every /plan has to ask the user what's installed — the batch surfaces it and fixes the conflated convention wording. A hand-saved version was rejected because the user reinstalls without Claude in the loop, so a saved value goes stale and would mislead. Second, with the host confirmed current (1.13.0-test1 = target base), the four /setup deferred lines that need deliberate runs were rolled into a new E2E test batch [setup-e2e-verification]; the rest of the deferred section stays put, confirming through normal dogfooding. Also promoted [blocked-by-lint-flags-shipped-citations] — the lint flagging shipped citation slugs in Blocked-by tails as dangling, which fired seven times this session, a live demo — kept standalone from the parked lint family since [lint-citation-refire] can't be judged until reinstall. The wind-down filed one capture, [deferred-test-runnability-tag-precision] (some user-run deferred tags are really observe-during-use, making the roll over-include). Finally, this session's own roll behaviour confirmed [deferred-test-seams-fix] live — it surfaced the host-side deferred lines, asked which cleared, and rolled the runnable ones, instead of silently producing nothing.

**Queue changes:**
- Promoted to Batches: [surface-installed-host-version] (session_start.py surfaces the installed version + corrects deferred-test convention wording in plan.md/done.md/QUEUE.md), [blocked-by-lint-flags-shipped-citations] (fix the lint Blocked-by-resolution false-positive in post_tool_use.py).
- Added test batch [setup-e2e-verification] at end of Batches; moved four /setup lines out of Deferred tests into it ([setup-closeout-redesign], [retire-registry], [migration-aware-setup], [setup-preexisting-content-handling]).
- Removed [deferred-test-seams-fix] from Deferred tests — confirmed live this session.

**Captures routed:** Promoted: [surface-installed-host-version], [blocked-by-lint-flags-shipped-citations]. Filed (processed, routing deferred): [deferred-test-runnability-tag-precision].
