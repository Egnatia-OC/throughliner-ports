# LOG entry — plan-2026-06-12-3

## 09be806 — /plan: two capture folds, a new cross-reference batch, stale-header cleanup, and the verbatim-first mechanism found

The session opened on a handoff note from the previous /next session, which flagged that the top batch and a waiting capture proposed the same deny-message-teaches fix in the same file. Building first would have left the capture for a second pass over pre_tool_use.py, so the fold came first. One thread ran through the whole session: three captures and two live observations all showed installed procedure text beating decided-but-unshipped standards — behaviour lives in shipped docs, not in session awareness, and twice this session not even an in-session correction held.

**Queue changes:**
- [git-add-safety-hook-gap]: the Files:-bare-paths capture folded in; title widened to "Extend pre_tool_use: deny blanket adds and commit -a; denials teach the fix". The scope-lock denial gains the bare-path rule, next.md documents it where Files: lines are authored, one synthetic test added. Tolerant matching of annotated Files: lines was rejected — loosening a safety mechanism to absorb formatting noise is the same trade the batch already declined for the git patterns.
- [doc-crossrefs-by-name]: new batch from the step-number capture, placed second in the queue so every doc-editing batch below it inherits the protection. Host-only CLAUDE.md rule — cross-doc references name their target, never a step number — plus a one-time conversion sweep. Grep-before-renumber was rejected because it guards an empty set once the sweep lands; lint detection was rejected because a renumbered pointer doesn't dangle, it silently retargets, which a lint can't see.
- [approval-display-blockquotes]: the commit-fence capture folded in. The verbatim-copy rule joins the rewrite with a sharper boundary — fences only for genuine paste targets; commit messages move to blockquote because Claude runs git commit itself, so no copy affordance is lost. The sweep gains a pinned confirmed offender (done.md's commit core) and a second host-side test (commit title and body arrive as blockquotes).
- [capture-verbatim-first]: rationale extended with instances two through four. Second: the 2026-06-12 /next pre-flight bundled the batch with gate findings; the backfill-first wait it also observed has since dissolved ([hash-backfill-as-hook] shipped). Third: this session bundled a working item with its analysis despite the standard being maximally salient. Fourth: one item later, past an explicit promise to send separately — which surfaced the mechanism: in this harness only tool work or a message end separates sends, so intent can't carry the fix. The plan.md build entry now names the separator: after the quote is sent, re-read the item from QUEUE.md — which doubles as a guard against context-drifted quotes, itself observed live when a capture removal failed to match on a dropped final sentence.
- Four satisfied or stale dependency headers cleared: [deferred-tests-structural-home] off [git-add-safety-hook-gap] and [session-start-dirty-tree-check] (shipped, pushed, host reinstall verified); [done-closeout-extraction] off [log-rejected-alternative-reasoning] (shipped, target-side). The reader-test parked item's blocker rewritten to its true remaining trigger — refreshed workflow run once — with the shipped slug moved into body prose so the header stops flagging once the lint hook goes live. Rationale prose keeps every satisfied dependency as a citation, so nothing is lost to history.
- Hashes backfilled to 4a886f0 in LOG/index.md and the queue-format-lint-hook entry file (model-side — the backfill hook isn't in the installed host yet).

**Captures routed:** four processed, Captures emptied. Two folded into pending batches ([git-add-safety-hook-gap], [approval-display-blockquotes]), one promoted ([doc-crossrefs-by-name]), one parked ([lint-citation-refire] — blocked on live lint-hook experience, since the hook has never run installed). Two further live observations this session — the third and fourth verbatim-first instances — were folded into [capture-verbatim-first] at the moment of noticing.
