# [HASH] — TOOLS.md designed: an always-writable home for a project's environment facts, read before any absent-assumption

Kept on Claude's recommendation and your agreement. The intuitive home — the project's CLAUDE.md — lost on this project's own machinery: the scope-lock denies it to planning sessions and to builds whose items don't list it, so recording one path would cost a capture→plan→build chain nobody would run. The design mirrors the cycles doc: `TOOLS.md` at the project root, created on first use, added to the scope-lock's always-writable set like the scratchpad; next-build.md's environment check writes a fact the moment it is learned and reads the file before assuming a tool absent. SPEC's Tool facts paragraph written at the keep, with you present.

**Queue changes:** kept and cleared, beside its paired environment-check item.
**Work processed:** kept — [no-home-for-a-projects-tool-facts].
Rule gate: run — amendments to the environment-check rule and the lock's quiet list; the CLAUDE.md-section alternative refused as unreachable under the lock.
