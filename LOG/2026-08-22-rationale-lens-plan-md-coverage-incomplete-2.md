# 576506c — The rationale lens ran over the whole of plan.md: seventeen removals, with the coverage ledger the acceptance demanded

The 2026-08-21 pass covered done.md whole and plan.md only by signature-grep (~40% read), the same shortcut that produced the mid-sentence-prohibition misses found the same day. This build discharged the restated acceptance: one straight-through read, every paragraph through the delete-and-reread test, and a per-heading coverage ledger so a silent skip is distinguishable from a clean judgment.

Seventeen removals, each relocated to this record (git history backs every one): Ground rules lost the SDD-contract clause (rewritten to its operative half), the session-boundary justification, the SPEC-lead explanation, the SDD-guidance sentence, the SPEC-grows preamble and the SPEC-floor explanation (paragraph tightened to its operative clause); Capture discipline lost the end-preferred batching rationale and the Runs-alone mechanism sentence; Step 1 lost the advisory-clear orientation clause, the hook-cap because-clause, a so-clause on mail ordering, the group-lift proxy explanation, the lift-is-a-clearing-move clause and the placement-flags gap sentence; the opening lost the absorbs-the-old-opening history clause; Step 2 lost the floor re-derive so-clause and the rung-3 enrichment explanation, with two more clauses reworded operative. Judged operative and kept clean: the intro, the record-kind block, the ladder's refusal records (composite score, weight, alternation), the keep check, the capability check, the merge table, the checkpoint, the process-now section (the user's quoted words kept under the quote claim) and the end-of-queue gate. The ledger in the build working file lists every `##`/`###` heading with its outcome, matching a grep of the file.

Counts: non-empty lines 1054 → 1023; rule-statement count effectively flat (321 → 322 — a reformat split one statement), which matches the prior pass's finding that the statement count is not a proxy for removed history; line count is the signal.

Tick: done, confirmed — straight-through read, ledger complete against the heading grep, counts reported.

**Files touched:** plugin/throughliner/docs/plan.md
**Routed to Captures:** none
Rule gate: run — a transcribed continuation of the rationale lens already admitted at [rationale-lens-after-the-build-view]; rationale paragraphs removed to the record, no new rule, nothing evicted beyond what the lens itself removes.
FAQ: not needed because this is Claude-facing rule text behind unchanged behaviour.
