# [HASH] — /plan: pruned 7 passive-watch parked captures; promoted [reference-by-slug-not-status]; opened the queue-item-model / test-concept redesign cluster

Driven by the user's concern that parked captures had banked to more than half the queue and were invisible in her editor's outline. We ran a decisive parked-shelf prune: dropped seven passive "watch for a second instance" or speculative-redundant captures ([narration-vs-menu-drift], [sweep-include-canonical-doc], [silent-out-of-scope-cleanup-watch], [inline-internal-term-marker], [plan-md-offload-reframe], [present-interview-forced-wait], [shipped-slugs-breadth]), keeping [bundling-jargon-recurrence] because this very session gave it its recurrence.

Two authoring insights landed. [narration-vs-menu-drift] and the spectrum-offer rule are the same tension — a spectrum is a structured menu — so dropping the former led to [retire-spectrum-offers] (retire the rarely-deployed, possibly-base-prompt-redundant spectrum rule). And this session itself bundled all six prune candidates at once despite the concise output style and a pre-chunked list, recorded as [bundling-recurred-despite-output-style] — evidence the output-style fix is necessary but not sufficient.

The session then surfaced a deeper knot the user has circled for weeks: the "test" concept is overloaded and the deferred-test backlog banks endlessly. Verified the backlog is this project's, not Taskflow's (~65 lines here, 2 there). Key insight preserved in [test-concept-redesign]: split passive "observe-after-reinstall" confirmations (which are confidence-notes, closer to done) from the small handful of genuine must-run tests — most of the backlog then evaporates. Alternatives weighed and rejected, with why: everything-as-capture just moves the pile; a separate test skill breaks queue dependency ordering; a first-class "dedicated design session" type is a mirage since /plan is already the design skill ([dedicated-design-session-mirage] — the real fix is importance-ordering captures, blocked by the queue's too-many-buckets model). These converge into one queue-item-model redesign cluster with [design-session-queue-home] and the batch-type items.

**Queue changes:**
- Promoted [reference-by-slug-not-status] to Batches (cleared region) — a wording rule that capture/batch prose references items by slug, never their status; the expensive convening-time re-check half deliberately dropped (token-drain risk).
- Dropped 7 parked captures (above); kept [bundling-jargon-recurrence].
- Parked [push-autoupdate-simplification] (blocked on verifying desktop auto-update), [shelving-as-tests], [dedicated-design-session-mirage], [test-concept-redesign].
- Filed processed captures [captures-as-headings], [retire-spectrum-offers], [bundling-recurred-despite-output-style], [proactive-queue-referenced-pushback].

**Deferred tests:** annotated four host-side lines with partial confirmations observed this /plan session on build 1.15.0-test1 ([view-in-doc-group-a] pointer form, [checkpoint-post-share-ask] merged park/drop asks, [dependency-tracing-pass] build-batch tracing, and [tag-recheck-doc-fixes] readiness-line silent-confirm); no lines removed — each still needs a /next- or /setup-side observation to fully clear.

**Captures routed:** 7 dropped, 4 parked, 4 filed (processed), 1 promoted; the unprocessed capture "don't bake status into prose" promoted to [reference-by-slug-not-status].
