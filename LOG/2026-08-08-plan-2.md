# [HASH] — Planning session: five items cleared to run — the greenlight close rule, the `.in_use` stamp fix, the shell-write extension, the shipped correspondence rule, and docset A's retirement greenlit — plus the FAQ untracked and the stop-chip accident resolved as issue #84877

The session opened on the blitz advisory's delivery check, which passed by verbatim comparison: the session-start context named host `1.19.0-test3`, matching the recorded literal, so the soak is running the blitz's build. The silent below-line checks found real news: `git branch --merged main` shows `overnight-blitz-2026-08-06` merged, so [merge-blitz-2026-08-06-to-main] is complete (closed separately below is not needed — it remains for the next close since its removal belongs with the merge record; noted for the next session). The session-start self-hosting question was answered yes and recorded in CLAUDE.md.

The session's spine was the blitz's six triaged holds, processed in unblock order. [greenlight-step-does-not-exist] became the biggest structural fix: the user ruled that self-hosting rules belong in CLAUDE.md, so the design is a cycle-status close rule in the FAQ-sync-gate shape (one consolidated question at every /plan close, a required `Cycle:` LOG line as teeth) plus rewording the six items that pointed at a step no doc implements — no shipped-doc changes. The user's own opening confusion about when the merge-cycle audit runs became the item's third live demonstration. [build-stamp-reported-matches-nothing-on-disk] was solved during its own interview: the CLI's `.in_use` bookkeeping file was inside the fingerprint — installed test3 and the target are byte-identical apart from it, and excluding it reproduces the target stamp exactly. The capture's "branch changes aren't live" conclusion was false in the deferring direction. [shell-write-matcher-blind-to-in-scope-targets] resolved to extending the denial to all detectable scripted writes in the project (scratchpad excepted; the mover untouched by construction), flipping the test's case-4 pin. [companion-project-correspondence-unsupported] was headed for host-only until the user overruled the premise — vibe coders run many projects — so the outbound rule ships in docset B beside the incoming-claims rule, with a FAQ entry. [compaction-reinjection-via-postcompact-flag] was skipped per its own instruction, with a route update banked (the routing clause shipped, but the firing question needs a live experiment, not an audit).

Mid-session the user proposed recording Fable-on-low as tested and dropping 4.8. The retrieve found [retire-docset-a] already designed with three conditions; the user resolved all three on the record — the measurement harness consciously waived ("waive the harness and greenlight it"), no 4.8 users in the community, blocker verified merged — and the item lifted above the line with the README tested-models note folded in (no new files). Consequence recorded on both below-line experiment items: the register-measurement window closes with A, so both need reprocessing.

[untrack-faq-needs-your-yes] was resolved in-session with the user's explicit yes: `git rm --cached FAQ/faq.md FAQ/index.md` ran, files remain on disk as the local help mirror, removal staged into this commit. This was the item's entire content, so it was executed rather than requeued, and the item deleted via the mover.

A live incident produced the session's last thread: the user accidentally sent a reply chip carrying "stop" — chips habitually carry affirmatives, so the click-as-agreement habit fired a session-ending reply. Filed as [stop-chip-accidental-send], processed at the user's request, and resolved the same session: duplicate-check found no prior report; GitHub issue anthropics/claude-code#84877 was posted with the user's explicit yes, then edited twice at their direction — the reference to #84375 dropped after the user challenged Claude's title-derived citation (the full body was then read and the claim verified, but the user preferred no mention), and the chip-activation flow corrected to click-focus/Tab/Enter. The method deliberately made **no** wording change to the two-sided checkpoint ask: the chip-generation theory is unverifiable from here, and the ask's shape was earned against a real failure. Revisit only if accidents recur. The issue is tracked as the fourth filed harness bug on [queue-pointer-hard-to-follow-and-possibly-stale], and the desk finding "suggested replies never appear" on [user-verify-remote-control-rendering] was corrected in the same act — they appear sometimes, unpredictably. Session output, by the artifact test: the issue's final text lives at the URL above; the sent wording is the issue itself, so a pointer suffices.

An out-of-shape note: the mid-close mover run tripped the modified-on-disk warning on QUEUE.md once; work stopped, `git status` and a structural re-read confirmed it was the mover's own rewrite (27 items, one marker, regions intact) before any further write.

FAQ: not needed because no user-facing method change shipped this session — the kept items carry their FAQ work into their own builds, and the untrack is repo housekeeping matching already-shipped behaviour.

**Queue changes:** five items moved into Processed above the line ([greenlight-step-does-not-exist], [build-stamp-reported-matches-nothing-on-disk], [shell-write-matcher-blind-to-in-scope-targets], [companion-project-correspondence-unsupported], [retire-docset-a] lifted from below); [advisory-overnight-blitz-2026-08-08] cleared as consumed (delivery check run, all holds through /plan); [advisory-cleared-run-then-history-rewrite] left in place (its persist-condition — the history rewrite — is unmet); reprocessing notes added to [docset-b-register-decision-weaker-than-recorded] and [structural-delimiting-experiment]; corroborating stamp-instability data point banked on the stamp item; skip note banked on [compaction-reinjection-via-postcompact-flag].

**Work processed:** kept — [greenlight-step-does-not-exist], [build-stamp-reported-matches-nothing-on-disk], [shell-write-matcher-blind-to-in-scope-targets], [companion-project-correspondence-unsupported]; resolved in-session and removed — [untrack-faq-needs-your-yes], [stop-chip-accidental-send]; greenlit and lifted — [retire-docset-a]; skipped — [compaction-reinjection-via-postcompact-flag].
