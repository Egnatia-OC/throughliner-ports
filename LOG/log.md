# LOG

Full session entries, newest first. Each entry is written by /done. This file covers the current release — older entries are in per-release log files (LOG/log-v*.md).

## [HASH] — /plan session: 2 batches promoted (narration-vocabulary, audit-definition), 1 capture parked, 2 captures filed

Three captures processed this session, with the loop-language capture reshaping considerably under discussion. First, the _build.md FAQ-entry capture parked itself by its own terms — its substance depends on what the refreshed reader test surfaces, so editing the FAQ first risks immediate rework. Parked with the dependency on [reader-test-refresh] made explicit. Second, the "loop / Step 2 leaking into chat" capture got reframed away from a narrow loop-language audit toward the broader background-vs-user-facing vocabulary discipline. Push-back: the original capture proposed a sequence-naming audit, but the underlying issue is that plugin-behaviour.md's "no internal terms in user-facing chat" rule rides on Claude's judgment of which terms count as internal — too abstract to catch actual offenders like "loop" and "Step 2." Fix: a Vocabulary section under Communication naming background-only terms explicitly with a translate-or-omit rule. Marker-based enforcement (inline tags on internal terms in procedure prose) deferred to a follow-up capture — ship the rule first, see whether leakage persists, then decide. Third, the audit-batch-too-narrow capture surfaced two embedded assumptions in the current definition (docs-as-target, Claude-reads-files procedure shape) and corrected one of them: who-reads is always Claude, but what-gets-read (procedure docs, the user's spec, code, UI flows, workflow output, any artifact) is implementation detail. Generalized the definition to lead with the output contract — findings to Captures, no direct edits — across plan.md and next.md. The reader-test-refresh reclassification question raised by the capture was deferred as a separate decision rather than bundling it into the definition batch.

One new design thread surfaced and got captured rather than promoted: a fourth /next type, freeform, for sessions that don't fit build/test/audit — the trigger case being user-applied handmade changes that need a LOG entry and commit but have no read-and-route or build shape. The risk identified isn't that well-defined audits would drain there (the contract is tight enough to keep them in audit), it's that less-disciplined /plan could reach for freeform to avoid finding a tighter type. Mitigation would live at /plan.

**Queue changes:**
- Promoted from Captures: [narration-vocabulary] (vocabulary rule + list for background-only terms), [audit-definition] (generalize audit-batch definition to output contract)
- Parked from Captures: FAQ entry for _build.md's four functions (behind [reader-test-refresh])
- Hash backfill: LOG/log.md and LOG/index.md placeholders → f123eed

**Captures routed:** 3 processed out (1 parked, 2 promoted); 2 new filed (marker-mechanism follow-up conditional on whether [narration-vocabulary] alone holds; freeform /next type consideration with /plan-side discipline as mitigation).

## f123eed — /plan session: 7 batches promoted, 3 captures filed, audit unpark cascade

Heaviest /plan session in a while. Three threads, all sparked by accumulated decisions catching up at once. First, the audit-as-batch-type landing unblocked three Parked audits (trickle-up rules to plugin-behaviour.md, output-tag prose-to-tag conversion, in-scope/out-of-scope distinction) — all promoted oldest-first as audit batches with target and criteria locked in. Second, push-back on the /next-after-/next drift narrowed its framing: the mechanical safety net (session_start detects _build.md and routes the next /next to resume) already prevents dual builds, so the real cost is the missed /done — a lost LOG entry and commit for the just-finished batch. Fix slated as a Scope-discipline rule in plugin-behaviour.md rather than a Step 7 wording change. Third, two structural questions surfaced and got their own batches: _build.md's purpose isn't named anywhere in the procedure docs so it reads as vestigial (fix: [BRIEF] narration at create / resume / consume moments), and the per-release log file split was being maintained on a theory of Claude's retrieval that didn't match reality (the why-pipeline goes through index hashes, not version groupings — design threads span releases anyway). Drop the split, one growing log.md. Existing log-v*.md files stay in place; retrieve still works via hash. One procedure tweak fell out of the session itself: unpark candidates had no structural home in /plan, so they got smushed into the entry question — fold them into Step 2's capture-processing sequence with the same promote/keep-parked/drop choice. Three captures filed: FAQ shelf behind reader testing, internal sequence-naming language leaking into chat ("loop," "Step 2") flagged after the user couldn't follow it, and the audit batch type's current "Claude reads docs" framing conflating the plugin-dev case into the generic batch definition.

**Queue changes:**
- Promoted from Parked: [trickle-up-audit], [output-tag-audit], [scope-distinction-audit]
- Promoted from Captures: [next-done-recommendation] (narrowed framing)
- New batches from discussion: [fold-unparks-into-step-2], [narrate-build-md-purpose], [drop-log-per-release-split]
- Hash backfill: LOG/index.md placeholder → 3814815

**Captures routed:** 3 added (FAQ shelf for _build.md's four functions, loop-language leak, audit-definition too narrow); 4 promoted out (3 unpark + 1 active capture).
