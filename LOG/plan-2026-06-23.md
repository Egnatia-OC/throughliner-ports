# bad6f21 — Discussion on SI's exposure to AI context/memory constraints — ran and filed deep research on the context-window / cost / memory trajectory (resources/research/context-window-cost-memory-trajectory.md), and filed three method captures: [claude-md-routing-gap] (CLAUDE.md missing from the routing taxonomy + Claude self-catching its own misroutes), [method-doc-structure-pass] (logical-structure pass over all method docs, gated on dedup-by-universality), [plan-dependency-tracing-gap] (plan-time dependency tracing weak under heavy coding — five modes from the Taskflow E2E LOG)

The user was weighing SI's future against AI memory and context limits — others call the plugin heavy on memory, and she wanted to know whether that ground is moving. A deep-research pass ran across three axes. Verdict: cost is the safest axis (per-token price falling fast), cross-session memory is coming but still enterprise-beta, and the real risks are quality-at-scale (big windows degrade before their limit) and tier-gating. Her report that the 1M-context option vanished from her desktop UI fits documented gating, not a bug. The finding is filed as research.

The discussion then surfaced two method gaps and one proposal, all routed to Captures for a later /plan. A first self-hosting "conflation" framing was weighed and dropped once the user clarified consumers don't even know the working/procedure docs exist — the real issue is placement-routing, captured as [claude-md-routing-gap] (CLAUDE.md is absent from plugin-behaviour.md's routing taxonomy, and Claude misroutes its own placement between memory, CLAUDE.md, and SPEC). The user then proposed a logical-structure pass over all method docs, distinct from a wordiness trim; the load-bearing insight is that dedup is gated on universality — "trickle-up" moves a rule into the every-session behaviour.md, so a non-universal rule should stay duplicated rather than hoisted — captured as [method-doc-structure-pass]. Finally, the user reported a high dependency-management error rate in the Taskflow build; reading that project's LOG showed the execution guards work but plan-time dependency tracing is shallow (five modes), captured as [plan-dependency-tracing-gap].

**Queue changes:**
- Three captures added (raw, below the Captures divider; not processed): [claude-md-routing-gap], [method-doc-structure-pass], [plan-dependency-tracing-gap].

**Captures routed:** none processed — three filed for a later /plan.
