# [HASH] — /plan: audited two Taskflow E2E sessions (host 1.12.0-test4) — filed three captures (parked-item lint false-positive, bundled/jargon /plan recommendation overwhelm, device-verification spilled into /done) and annotated five deferred-test lines as test-build-confirmed

The user brought two Taskflow E2E session transcripts for audit — a /next session (a SPEC spine-rewrite spec-edit, then the first real UI build, batch 0002, which became a live on-device run when a Pixel 6 turned out to be connected) and the /plan session that rolled the batch-0001 TaskDao tests into a test batch. Per the self-hosting rule that any observed plugin behaviour routes to Captures, the transcripts were slimmed with a Python preprocess pass (3.2 MB and 608 KB raw → 57 KB and 17 KB) and read for behavioural findings.

Three findings were filed as captures. (1) The post_tool_use queue lint flags a sub-bullet nested inside a `Parked:` item as if it were its own loose parked entry, and fired on every QUEUE.md edit across both sessions — a sibling to the existing capture about the lint's Blocked-by check being blind to Deferred-tests slugs, so they likely share a fix. (2) A /plan recommendation overwhelmed the user by bundling a not-yet-actionable secondary decision into the primary ask, in jargon — the user replied "sorry I don't understand"; filed with a host-version caveat since the sessions may predate [verbosity-output-style] going live. (3) The 0002 build deferred its on-device verification on an untested "no device" assumption; when a device was in fact connected, the verification — and a user-directed header redesign — spilled into the /done close-out, raising whether build-time verification should ask about available channels and whether /done needs a fold-in-vs-route-out rule.

The host was identified as test build 1.12.0-test4 (Taskflow's `.si-version`), not a released host. Five deferred-test lines showed their behaviour satisfied in these transcripts and were annotated as confirmed on the test build, each kept pending a released-host pass — matching the queue's existing practice of annotating test-build observations rather than removing the line. Two halves were checked and deliberately not advanced ([spec-edit-batch-type]'s second half was not exercised; [push-offer-fit]'s no-remote half doesn't apply since Taskflow has a remote).

**Queue changes:**
- Captures: filed three new unprocessed captures (parked-item lint false-positive; bundled/jargon /plan recommendation overwhelm; device-verification-assumption spill into /done) — the first placed beside its sibling lint capture.
- Deferred tests: annotated five lines as confirmed on test build 1.12.0-test4, kept pending a released-host pass — [capture-verbatim-first] (/plan half), [verbatim-first-rationale], [push-offer-fit] (planning-close commit-only half), [staleness-flag-fix-path], [done-recommend-next-both-ways].

**Captures routed:** none promoted/parked/dropped — three new captures filed this session, left for a focused processing pass (the two lint captures want a fix-together pass).
