# 2625fa0 — Kept and cleared: restore `docs-b/` to the folder-rename note

A one-line restore, not a deletion — the note is what explains why old records legitimately carry the old path, so it must keep saying what that path was. The planning session cannot write CLAUDE.md under the standing scope-lock, so it clears as a small build.

Rule gate: not needed — restoring a lost word in a history note.

**Work processed:** kept — [claude-md-rename-note-lost-old-name].
