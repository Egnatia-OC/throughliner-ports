# [HASH] — /plan: 14 captures processed — 5 batches promoted, [spec-edit-batch-type] amended, 3 parked, SPEC hooks line + a deferred test reconciled, /done-commit-message dropped (fix already built)

This /plan session cleared the firing-map audit cluster and a batch of accumulated housekeeping captures. After the four firing-map findings, the user asked to triage the rest by weight and run the light (fold/park/reconcile) and small-batch clusters this session, reserving the heavy forks — the /goal-vs-cruise-control decision, the 34-finding self-hosting inventory, [retire-registry], /setup-on-dev, deferred-tests-vs-test-batches, self-install, and README+rhythm — for focused sessions of their own. Mid-session the user caught Claude bundling the verbatim capture item with its analysis instead of sending it first as its own beat; that became a filed capture and then a promoted fix, with the rationale sharpened to "availability, not obligation" and both failure modes named (bundling, and forced-wait). The /done-commit-message frustration rested on an out-of-date premise: its fix [closeout-text-collapse] is already built in the target's done.md and only awaits a push + reinstall, so it was dropped rather than reprioritized.

**Queue changes:**
- Promoted [trickle-down-next-only-rules] (two /next-only /done-recommendation rules move from the injection to next.md; narrowed from the capture's four — SPEC-read-only is deleted by [spec-edit-batch-type], one-build-at-a-time stays in the injection).
- Amended [spec-edit-batch-type] to fold in the spec-entry-pipeline /plan-only decision (plan.md canonical, injection copy removed).
- Promoted [hook-count-reconcile] (CLAUDE.md "2 hooks"→3), later broadened to also carry the Claude-authored-handoff interim rule. SPEC's hooks section was updated directly in /plan to name the advisory third hook.
- Promoted [reinstall-needs-full-restart] (INSTALL.md + CLAUDE.md) and [rezip-test-version-scheme] (CLAUDE.md Rezip/Push).
- Promoted [verbatim-first-rationale] (plan.md verbatim-first instruction gains its why-clause).
- Folded two /goal-fork inputs (shipped-slug cross-check at close; handoff-claim provenance) into the "First autonomous /goal" fork capture.
- Parked [firing-map-middle-band] (Blocked by [behaviour-doc-size-watch]), [sweep-include-canonical-doc] (Blocked by a second instance), [scaffolding-resync] (Blocked by first content-level drift instance).
- Reconciled the [approval-display-blockquotes] deferred-test line (dropped its superseded commit-step clause); resolved the firing-map narrower-additions question in-session (keep all four in the injection, recorded to [firing-map-middle-band]).

**Captures routed:** Promoted: [trickle-down-next-only-rules], [hook-count-reconcile], [reinstall-needs-full-restart], [rezip-test-version-scheme], [verbatim-first-rationale]. Folded: spec-entry-pipeline → [spec-edit-batch-type]; goal-removed-13/14 + claude-authored-handoff → /goal fork capture (interim rule into [hook-count-reconcile]). Parked: [firing-map-middle-band], [sweep-include-canonical-doc], [scaffolding-resync]. Resolved in-session: firing-map narrower-additions, hook-count SPEC line, [approval-display-blockquotes] clause. Dropped: /done-commit-message (fix already built).
