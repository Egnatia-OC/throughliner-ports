# AGENTS.md — pointer

This project's governance lives in `CLAUDE.md` — read that. This file is a pointer only: it was a Codex-era copy of `CLAUDE.md` that drifted (the Codex port is shelved), and it carries no rules of its own. If a tool loads `AGENTS.md` by name, everything it needs is in `CLAUDE.md`.
